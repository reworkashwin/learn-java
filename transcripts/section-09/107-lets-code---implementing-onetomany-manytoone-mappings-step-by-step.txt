We have our entity class ready with the required ManyToOne and OneToMany configurations. As the next step, we want to send the data present inside these entity classes to the client applications. To achieve the same, we need to create the corresponding DTO classes, because to the client applications, we should share the data by using the DTO classes. That's why what I'm going to do is, I'm going to create a job DTO corresponding to these entity classes. Let me go to the DTO package and here I'm going to create a new java record class with the name JobDto. Inside this record class, I'm going to paste the fields information. Please get these fields related code from the GitHub repo. So these are all the fields that I want to send as part of the Jobdto to the client application. Please make sure you also define the same fields with the same name because on the UI application side, I developed the display logic by using these field names. If you try to give different field names, then the UI display logic may not work as expected. Once I have this Jobdto, I need to go to the CompanyDto class because inside this CompanyDto only, along with the company details, we want to send the associated or related list of JobDtos as well. So that's why towards the end, I'm going to create a field of type list of JobDto and the field name should be jobs. Here also, please make sure you are giving the same field names inside your CompanyDto. So now my DTO classes are ready. Next, I'm going to open the company controller. So we know inside the CompanyController, we have a REST API, which is going to send the company's data to the client application. From where this company's data coming? It is coming from the getAllCompanies() method in the company service. So let me open CompanyServiceImpl and here I have a method with the name getAllCompanies(). Inside this method, we are invoking a findAll() method by using the companyRepository and this is giving me a list of company records due to the OneToMany and ManyToOne configurations that we have done so far, by the time this line of code is executed inside each of the company entity object, I'm also going to have the corresponding job entity objects as well. From these company entity object, we need to iterate all the jobs data and all the data present inside the job entity object, we need to transform into the JobDto object. So that transformation, we are going to do by creating a private method. So what I'll do is, I'm creating a new private method the name of this method is transformJobToDto. So basically we're going to transform the job entity to a DTO object. So the input for this method will be job entity object,the return type will be JobDto. So by using these method, I'm going to transform all the data from the entity object to the corresponding DTO class. From where I have to invoke this new private method from these method itself, from these transform to DTO maybe we can rename this method to transformCompanyToDto. So this method, it is going to receive company object inside the company object, we're going to have the list of job entity objects as well. That's why I'll try to retrieve all the jobs present inside and company object, and during the iteration for the each job entity object, I'm going to invoke the transformJobDTO method. And towards the end, I'm going to collect all the list of job DTOs by using this variable name. This jobs DTO, I need to provide as an input to the constructor of the company DTO. I'm getting a compilation error here because we changed the method name, let me mention the correct method name. So this is the only change that we need to do. Do you see how easy it is? We just have to invoke the findAll() method and behind the scenes, the JPA is going to take care of executing the required SQL statements to fetch all the data from both the tables. What are the two tables? Companies and jobs. So let me save these changes to the build, once the build is completed, I will restart my application because we have added good amount of changes. I'm also going to clean the console so that we can see the SQL statements that are generated by the JPA. First, I'll try to test the API by using the postman. Under the spring collection, we have a folder with the name company. Under this company, we have a request with the name GetAllCompanies. Before invoking this REST API, please make sure you are sending the header with the accept and with this value. As soon as I click on the send button, you will be able to see that I got a response. This time for each company, along with the company details, we might have received the job details as well. For example, the first company with the id 1, the name is TechCorp Inc. For this, if you can scroll down, there is a jobs data, which is an again an array. Under this job, I have multiple job details. So this is the first job followed by second job, third job, fourth job. So this way, all the jobs that are associated to a company I received as part of the company object itself. Now, if we provide this response information to the client applications like websites, they are going to understand the data that we send and accordingly, they will display the company's data and jobs data. We can see the demo as well by going to our UI application. But before that, let's try to see the SQL statement that is generated by the Spring Data JPA. If you see here, these are the SQL statements that are generated by the Spring Data JPA, by using these SQL statement, it fetched all the job related information along with the company's related data. The Spring Data JPA framework, it is going to work with the Hibernate framework behind the scenes, and both these frameworks are going to work together and they are going to generate the SQL statements that best suits your business logic. We are Java developers, we just have to invoke the methods like findAll() from our Java code. Inside this parent entity object itself, we are going to have the child associated records as well. The same we are trying to access here by using these getJobs() method. This is our UI application. If you start the UI code in your local system and try to access these path inside the homepage, you will be able to see this time there are some jobs details as well. So in total, there are thousand jobs out of which six are being displayed here. If you want, you can click on this load more jobs, which is going to display more number of jobs. Along with the job details, we also have company's data as well. The end user can click on the company's information or the job information as well. For example, if I click on this Apple related job, it is going to take me to the job detail page, so this is the job ID. This is a job title and this is the company which posted this job. This is the pay grade,we have other details as well. If someone click on these view company profile, it is going to take them to the company detail page. So these are the company details, if someone click on the jobs, it is going to display all the jobs which are posted by the Apple recruitment team. I hope you're happy with the progress that we have made. But with the logic that we have written so far, there is a small problem. Let me explain you what is the problem. Inside database, under the jobs table, we know there are thousand jobs and all these jobs, they have a default status as active. So if you see, this is a status active as of now, all the thousand jobs, they have the status as active. But apart from active, the jobs, they can also have other status. For example, I can update for a few of them as closed. The first two jobs, I'm updating them to closed. After updating, I'm clicking on the submit button, which is going to take care of running the update statements behind the scenes. Now, if I try to reload this page, you'll be able to see there are two jobs with the status as closed and the remaining 998 jobs are active. With this kind of data setup on the UI, how many job records we have to display? Only 998. We should not display the closed status jobs to the end users. Why should someone should apply to a job that is already closed? So that's a logic here. But inside my job portal, still we are displaying the thousand jobs. So here you'll be able to see there are thousand jobs that are being displayed in the UI. Why this is happening? Because inside our service layer logic, we are invoking the findAll() method, which is going to fetch all company's records along with the all job records. But instead of fetching all the job records, we want to fetch only the job records with the status as active. So how to communicate our custom fetching requirements to the Spring Data JPA? Because with the methods that are available as of now, I don't have any method that helps me to fetch the company's data along with their jobs data, but the jobs data should have the status as active only. This is somewhat advanced concept inside the Spring Data JPA framework I'm going to cover those advanced topics in the coming sections. For now, I'm not going to fix these defects inside our code, after a few sections once we discussed all other topics of Spring Data JPA, at the right time, I'm going to introduce one of the concepts that is going to help us to run our own custom queries by using the Spring Data JPA framework. Until that point, please bear me with this defect.