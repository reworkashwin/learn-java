First, let's try to understand what are the configurations that we have done inside the Company.java. We have created a field with the name jobs and the data type of this field should be a collection. It can be a List or Set or any other collection data type. The reason why we need to create this job as a collection field is from the definition, it is clear that one record of company can have many job records to store all those many job records we need to define the job field with the collection data type. That's why we have defined this field by using a List and the ArrayList with this what is going to happen is whenever we are creating an object of Company entity class, we are going to have an empty ArrayList with the name jobs and inside the jobs list, we can add any number of job objects. Once this field is created on top of this, we need to mention the annotation @OneToMany Using this, we are telling to the Spring Data JPA framework that we are trying to establish the one to many relationship between the company and job entity. So the data type that we are trying to store inside the List is job. To this annotation, we can provide various configurations. The first one that we are trying to provide is by using the mappedBy property and to this, we are passing the value as company. So with this configuration, what we're telling to the Spring Data JPA framework is inside the job entity, there is a field named company and that field owns the foreign key if you open and check the Job entity class inside that we have created a field with the name company of data type Company. So the same field name here, we need to mention this mappedBy configuration. We need to mention inside the entity class where the foreign key column is not present inside the company table we don't have any foreign key column. That's why we need to mention mappedBy here. So by using these mappedBy, we are giving a clue to the Spring Data JPA framework that go and check inside the Job entity. And inside this entity, we have a field with the name company, which represents the foreign key inside the jobs table. After the mappedBy, I have provided the cascade configurations by using the cascade property. To this cascade property, I am trying to provide the constant with the name CascadeType.ALL. Apart from all, there are many other constants as well. We are going to discuss about cascade in detail in the coming lectures. For now, let's try to understand what is the meaning of cascade. With this cascade, we are telling to the Spring Data JPA framework If something happens to the parent entity, which is a company entity, do the same to its child entity objects as well. What is a child entity here? Job is a child entity. Usually inside the business logic, what we are going to do is first, we are going to create the object of Company followed by we can populate all the fields and also save some job records inside this List. Once the Company object is fully populated along with the child records, we can invoke the save() method by using the CompanyRepository interface. So at that point of time, when we are using the save method, we know what is going to happen behind the scenes. The company record will be saved into the database by using an insert a SQL statement. So since we are providing these cascade configurations, we are telling to the framework, since you are saving the parent record, please also save its child records, which are job records. The same applies for update and delete as well. So this is going to make our life easy because we don't have to save or delete the jobs separately or manually with a single line of code, both the company and the job related records will be updated or saved into the corresponding table. So that's the purpose of cascade. I hope you're clear. If something is not clear, don't worry we have separate lecture explaining you about the cascade configurations. Next we have orphan equal  true. Let's try to understand the purpose of this. By using this configuration, we are telling to the framework, if a job is removed from the job list, delete it from that database also. Let's imagine inside my Java code by using the Company object, I'm invoking the getJobs(), which is going to give me the list of job records. On top of these, I am invoking the remove() method and passing a given Job object. With this code, what is going to happen? The given Job object will be removed from the jobs list that we are maintaining inside the Company object. But here, if you see, we are removing the Job object only from the List that is maintained inside the Company object. If we are mentioning this orphanRemoval as true, what Spring Data JPA framework behind the scenes it is going to do is whenever it encounters this kind of Java code, it is also going to take care of removing the record from the jobs database table as well. Otherwise, we are going to left with an orphan record in the database. In case if the jobs table allow a nullable foreign key column, then with this kind of code, if you don't have this orphan removal as true, we will end up having a job record with the null foreign key. But since in our scenario, we made the foreign key as not null, obviously we are going to get some runtime error when this kind of code is being executed without this configuration. It is always recommended to define orphanRemoval as true so that any orphan child records will be automatically deleted by the Spring Data JPA and Hibernate itself. At last, I have an explanation on why we have created an empty array list, which I already explained a few seconds back. Now let's try to understand about the configurations that we have done inside the Job entity. We know from the jobs perspective, the relationship will be many to one. That's why we need to mention this annotation. But before that, we should also take care of mentioning @NotNull. By mentioning this @NotNull, we are telling to the Spring Data JPA framework that every job must belong to a company. No job should exist without a company. If someone tried to save a job without a company, it should result into an error. Next to by using these ManyToOne configuration, I'm trying to provide fetch related configurations. Fetch is also is an another important and interesting topic inside the JPA relationship. Here to the fetch, I'm trying to provide the constant fetch type as lazy. Apart from lazy, we also have another constant with the name eager. So these are the two different values that are supported inside Spring Data JPA. I have a separate lecture explaining you about fetch type in detail. But right now, let's try to understand what is the purpose of these fetch type. I have the explanation around these in the next slide. Whenever we are mentioning these fetch type configurations, this is going to improve the performance. So with these, what we're telling to the Spring Data JPA framework is whenever you are trying to fetch a job record from the database, please don't fetch automatically its company details as well. The reason why we do this usually is to achieve some performance. Let's imagine you fetch the job record from the database by using the Spring Data JPA. So you're going to have all the data in the form of Job object. Inside the Job object, you're going to have all other fields along with the company details. But let's imagine inside your business logic, you are only referring to the all other than the company field. In this scenario, you are never using the company related information inside your business logic. But the moment you try to write some business logic inside your Java code by invoking the getCompany() method at that specific instance point of time, the Spring Data JPA framework is going to execute an actual SQL query to fetch the corresponding company details from the database. If you have business scenarios where in the majority of the times you're not going to refer to the company information available inside the job object, then go with the lazy. Otherwise you can mention the fetch type as eager. With the eager fetch type, what is going to happen is always the Spring Data JPA, while it is trying to fetch the job related information, it is also going to fetch the company related information as well. So the fetching effort it is going to perform eagerly while it is fetching the job details with the optional as false we are telling that the company relationship is mandatory every job must have a company. This reinforces the NotNull configuration that we have mentioned here. Next we have @OnDelete and @JoinColumn. First, let's try to understand the purpose of @JoinColumn. So by using the @JoinColumn annotation, I'm trying to provide the column details inside the job table, which is representing the foreign key column. So we need to mention the column name by using the name attribute the column name is company_id. And since it is a not null column, we have to mention nullable as false. So this tells Hibernate, there is a column named company_id in the jobs table, which stores the company's ID and this is a foreign key column. So if there is the table which holds a foreign key column, such a table entity class should have these @JoinColumn annotation. The other entity class or the other table entity class, it should have the mappedBy configurations that we have discussed previously. I hope this is clear to you. There is no confusion. After the @JoinColumn, we have @OnDelete configurations. So just below many to one, we have mentioned OnDelete configurations. To this @OnDelete annotation, we have mentioned the action by using a constant called cascade. So let's try to understand what is this. By using this annotation, we are directly telling to the database, not to the Hibernate framework or Spring Data JPA. If a company record is deleted, automatically delete all its jobs. That's what we are trying to convey by using this. So this ensures that referential integrity. This is very similar to orphanRemoval that we have discussed on the Company entity class. With the orphan removal, we are giving the responsibility of deleting the orphan records to the framework. Whereas with the OnDelete, we are giving the responsibility directly to the database. Here I also gave an example. If company one is deleted, all the job rows with the company_id one are automatically deleted by the database. With that, there won't be any orphan jobs and mismatch errors. Before I try to close this lecture, let me explain to you about two more important concepts. The first one is bi-directional relationship. So far we have created two entities with the name Company and Job. So both these, they have relationship and they are having bi-directional relationship. It is also possible to establish the uni-directional relationship as well. Let's try to understand what is this bi-directional. If both entities like Company and Job, they know about each other and their relationship, such a relationship, we call them as bi-directional. When we establish the bi-directional relationship, we can navigate the relationship in both directions either by using the Company object or Job object. So inside the Company entity, we have mentioned the annotation with the name @OneToMany, whereas on the Job side, we have mentioned it as @ManyToOne. So since the configurations are done on both sides, the relationship is bi-directional. So from the Company object, we can invoke the getJobs() method to navigate to the jobs record from the job record. We can navigate to the company record by using the job.getCompany(). If it is a uni-directional, then only the navigation from one direction is possible. Next I want to answer about a question on who owns the relationship. There are two entities involved. So who is owning the relationship? In our example, job entity is the owner who is owning the relationship. Always remember the entity class or the table, which is having the foreign key column, that entity or that table entity, it is going to own the relationship. Since the jobs table has a foreign key column with the name companyId, it is going to own the entire relationship that we have established so far. Whereas Company entity is on the inverse side that means on the opposite side, always the opposite side, it is going to have the mappedBy configuration by using the mappedBy  configuration we're telling to the Spring Data JPA framework, use the foreign key in the job table. I don't have anyone inside the company's table. Whereas on the owning side, we're always going to mention @JoinColumn annotation, which we have discussed a few seconds back. I hope you're clear. Let's try to discuss more details about cascade and fetch configurations in the next lecture.