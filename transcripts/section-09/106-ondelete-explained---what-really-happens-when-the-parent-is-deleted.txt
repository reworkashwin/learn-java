As part of this lecture, let's try to understand the purpose of @OnDelete configurations that we have done inside the job entity class. By using these @OnDelete annotations, we are telling to the database directly to automatically delete the related records when a parent record is deleted. While we are creating a jobs table by using the insert statement, next to the foreign key configurations, we also mention these ON DELETE CASCADE. Let me show you the same. This is the SQL statement that we have executed. After the foreign key configurations related to the company id column, towards the end, I have mentioned these ON DELETE CASCADE. . Since we have mentioned these configurations, the JPABuddy, while it is generating the job entity class, it also generated the On Delete configurations. Basically, by using these On Delete configurations, we are directly telling to the database to take care of deleting the child records as well when a parent record is being deleted. The Hibernate framework or the JPA framework, they won't get involved in this scenario. It is always going to be the responsibility of the database server. Even if someone is trying to delete the company record directly inside the database, in that scenario as well, the database is going to take care of deleting the corresponding child records or job records. While using these @OnDelete annotations, we can also provide various constants to these action parameters. So far inside our logic, we have mentioned a constant with the name cascade, but apart from these cascade, we also have other options as well. Let's try to understand what are those. The other valid values are NO_ACTION with these no action we're telling to the database. If a parent is being deleted, please don't delete any child record. So this will be that default behavior, but here you may have a question, but what if the foreign key column inside the child table is a not null column? So in that scenario, obviously the database is going to throw an foreign key constraint violation exception, maybe at later point of time in the transaction. But if your child table has a foreign key column as nullable column, then this NO_ACTION is going to make sense because the child records are going to be retained and there won't be any exception as well. So such a child records with the null foreign key column, we call them as orphan records. With cascade, we are telling to the database to automatically delete the child rows as well when the parent role is being deleted, we are using these constant as of now inside our code. We also have RESTRICT, RESTRICT means immediately block the deletion of the parent if any child exists, this is strict version of NO_ACTION with restrict, we can't delete the parent record unless we delete the child records by ourself. So this is like a safety net. If someone is trying to delete the parent record, they have to make sure that there are no child records for a given parent. Next we have SET_NULL with the set null whenever the parent being is deleted, but the child records under the foreign key column, the NULL value will be set. But to use this option, the foreign key column should be a nullable column. Instead of null, you can also set some default value as well by using these SET_DEFAULT constant. I hope these constants are clear to you. I know you may have a confusion between @OnDelete annotation and CascadeType.REMOVE. So we also have Cascade type configuration, which is going to do the pretty similar job, but there is a very major difference between both these two approaches. Let's try to understand what are those. With OnDelete, the delete operation is always going to happen at the database level. It is the database responsibility, whereas with the CascadeType.REMOVE, it will be the responsibility of the Hibernate framework, it is the Java code that is going to take care of removing the child records. In the scenario of OnDelete, the database will delete the child records, whereas with the CascadeType.REMOVE, Hibernate is going to delete the child records. Performance wise, this option is faster because the database directly handling it, whereas Hibernate option is slower because there is an extra layer between database and your code. This is the example which we already explored. We need to use this approach when we want the database to manage the deletions and this one when we want the application level control. The main difference you have to remember is @OnDelete is a database rule, whereas Cascade type is a Hibernate instruction. Basically these @OnDelete configurations, we need to mention inside the entity class, which is owning the relationship. Since the jobs table has a company ID foreign key column, this is the right place to mention the @OnDelete. I hope everything is clear, but before I try to close this lecture, I want to clarify an important doubt that usually developers will have. So if you closely observe here, we have made one delete configuration, which is taking care of deleting both the parent and child records. And if you go to the company also, here also we have some delete related configurations. Let inside the company also under the CascadeType.All we have remove as well, which is going to take care of removing the child entities and here we also have orphanRemoval as true, which is also going to take care of deleting some orphan records. So if you closely understand, we are trying to handle that delete related scenarios by using three different approaches. This is the first one, and this is the second one, and on delete is the third approach. You may think that we are performing a lot of duplicate effort here around the delete operation, but that's not the case. Let me explain you each of these approaches they have their own unique scenarios. Let's imagine someone is deleting the parent record, which is a company record by using the Java code. Like maybe they might have executed like companyRepository.remove() method and to this remove() method, they might have passed company object, which is a parent object. In this scenario, what is going to happen with the CascadeType.All configurations since the parent is being deleted, the child records also will be deleted automatically. When the child records are also deleted automatically by the JPA framework or by the Hibernate framework, then there is no involvement of orphanRemoval configurations because we don't have any orphan records. So let's consider an another scenario. In this scenario, someone is trying to remove a child from the parent by invoking the parent.get jobs. So what is the parent here? It is a company object. So from the company object, they invoke the get jobs() method and by using these get jobs, they again invoke the remove and to this remove() method, they passed and job object. So if you see here, we are not deleting the parent record here. We are only deleting the child record. So in this kind of scenarios, this cascade will never come into picture because the cascade will come into picture only when the parent record is getting affected. But with this code, we are only deleting the child record. There is also another possibility, someone might set null by invoking the job.setCompany. So here also what we are doing, we are removing the relationship between the job and company. In both these scenarios, what is going to happen? The job record or the job object, it is going to become an orphan. Unless we have this orphanRemoval as true, this kind of orphan records will stay inside your database. That's why to handle these kinds of scenarios, this orphanRemoval true configuration is very much required. Now let's go to the third approach, which is @On Delete. With the help of these @OnDelete configurations, we are making sure that there won't be any orphan records in the database. Even if someone is trying to delete the company record manually, directly inside the database, this configuration, it is also going to be useful in the scenarios. Let's imagine one of the new developer, he forgot making these cascade configurations and orphanRemoval configurations inside the company entity class. One day, the same developer deleted the company object, which is the parent object directly by invoking the remove() method by using the companyRepository. Since there are no cascade configurations and orphanRemoval configurations, we will end up having a orphan record in the database. Such orphan records will be taken care of by this third configuration. I hope this is all making sense to you. So none of these configurations are duplicate to each other. They are handling different flavors of deleting a record. I know I'm taking a lot of time explaining all these theory concepts, but all this is very much required for a backend developer. All these concepts that we discussed, these are applicable for all kinds of relationships, not only for one to many or many to one, they are applicable for other relationships as well. In the next lecture, let's see that demo of our code changes.