Relationships is a complex topic in real life, but coming to the Spring Data JPA, though it is an advanced topic, it is very easy to understand. I'm there for you to make it very clear for you so that you can crack any kind of interviews around the Spring Data JPA relationships. But don't ask me any advice around the real life relationships advice. I'm not an expert around that. So what is the purpose of relationships in Spring Data JPA? Inside real world applications, data is often connected. For example, if you take a customer information, this customer may have an address, in the similar lines, a book data will have connection with the author related information. Since it is not a good practice to maintain all the related information in a single table, in real applications, this kind of related data, they're going to be maintained in different tables, but the tables are going to have a relationship between them in the form of foreign key reference. So by using JPA relationships, we are going to model these connections between the entities in our database. We know when we are developing Java related logic by using the Spring Data JPA framework, we have to create the entity classes that represents our database tables. Inside these entity classes, by using the Spring Data relationship configurations, we can communicate to the Spring Data JPA framework on how two different tables are connected with each other. Without relationships, as I said previously, developers, they need to write complex joint queries. The Spring Data JPA framework, it supports four main types of relationships. The first one is OneToOne. You need to use these relationship configurations if one entity is linked to exactly one other entity. For example, if you take a user, a user will have only a single passport of a country. And similarly, a passport will always be belong to a single user. So there won't be any scenario where a single user will have multiple passports or a single passport is belongs to multiple users. These kinds of relationships we can establish by using the OneToOne configuration. In case one entity is linked to multiple entities, then we have to use @OneToMany annotation. What do I mean by entity here? Entity here is nothing but a record in a table. If you take our company and job tables, so a single company record may have multiple job records in a job table. That's why here in this scenario, we have to use OneToMany configuration. In the vice versa scenario where multiple entities are linked to one entity, we need to use ManyToOne configuration. So if you take the same company and job scenario, if you look from the job table perspective, we can say multiple job records belongs to a single company record. That's why from the job perspective, we need to provide the configurations by using ManyToOne relationship. At last, we have ManyToMany. If you have a scenario where multiple entities are linked to other multiple entities, then we have to use these ManyToMany relationship. ManyToMany relationship is the advanced relationship in all these four. We're going to discuss all these relationships throughout this course. I'm going to show you the demos as well, but right now, since we are focusing on the companies and job stable data, let's try to understand which relationship is going to be suitable followed by we can explore the code configurations as well. As I said before, if you take any big company like Amazon, the company can post multiple job openings like Java developer, tester, DevOps engineer, cloud architect. So one company, since it can post many jobs so here from the company perspective, we need to use OneToMany relationship. But if you look from the job table perspective, each job opening belongs to only one company. But since we are going to have multiple jobs under a company, all those job records, they are going to be mapped to a one company. From the company perspective, the relationship is always going to be OneToMany, whereas from the jobs perspective, the relationship will be ManyToOne. So in this scenario, using OneToMany and ManyTo relationship is the correct approach. We already discussed on how the data is going to be maintained in the database tables. In the coming lectures, I'm going to show you the code configurations as well. But before we wind up this lecture, I want to answer one of the burning question that you may have. Why not OneToOne relationship when we are looking from the job table perspective? You may argue with me, this job record, which is 101, it belongs to a single company. One record is mapped to one record in the other table. So it has to be OnetoOne, right? Then why you are saying ManyToOne? So this can be your question? Let's try to understand the same. If we are using OneToOne, that means the other side, like inside the company's table also, there will be only one record mapped to the job record. But is that true? It is not because there are many other jobs also, which are mapped to the same record. That's why here we have to make sure to use the ManyToOne relationship from the job table perspective. The thumb rule is OneToOne means both sides, they should have exactly one record. Whereas OneToMany means one has many, while the other has only one and the vice versa will be ManyToOne. I hope you're clear. If something is not clear, don't worry. When we are exploring the code changes and seeing the demo, everything is going to be super clear for you.