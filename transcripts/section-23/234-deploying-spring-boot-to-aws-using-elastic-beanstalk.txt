We have a database ready inside the cloud. As a very next logical step, we want to deploy our backend application as well into the cloud. Whenever you want to deploy an application into the cloud, the most basic way of doing this is, you have to take a cloud server. Cloud server is very similar to your local system, but this cloud server is going to attach to the internet and can be accessed through a IP address. Inside AWS, we can get these servers by using a product called EC2. EC2 means Elastic Compute. If you click on this link, here we have an option to launch an virtual server by using this launch instance. Once you have a cloud server, you're going to copy your code into the server and install all the required libraries like Maven, JDK. Once all the required dependencies are installed, you need to run some commands, which is going to start the deployment of your application. Once the application is started, you should also configure the required security groups to control the access of your application. Whether you want to expose your application to the outside world, or whether you want to keep it internally within your organization. Such kind of decisions you need to take and implement them by using security groups. You should also take care of creating a load balancer. The purpose of the load balancer is, in case if you are deploying your application over multiple servers to solve the humongous traffic that you're getting, in this kind of multiple instances scenario, there will be a load balancer sitting at the front of all your instances. Whatever traffic that is coming from the outside world will be processed by the load balancer. From the load balancer, the request will be forwarded to the one of your application instance. And doing this manually is no more a feasible option because it requires a lot of effort as well. To solve these kinds of problems, AWS has some ready-made products. One such product is, AWS Bean Stalk. So there is a product with the name Elastic Beanstalk. Let me click on the same. As soon as you land on this page, you're going to have some brief introduction around this product. . Since I already created an environment by using this Beanstalk, I'm getting this page. You can search for Elastic Beanstalk in Google. Inside the search results, if you open any of the AWS link, a page like this is going to open. As you can see, by using Elastic Beanstalk, we can easily migrate, deploy, and scale full stack applications or backend applications on the AWS. So it is going to simplify the full stack application deployment, reduce application management, improve security, scalability, and availability, and minimize the cost as well. When we are using this product, the deployment process is going to be very easy. You're going to see that in the demo in few minutes. Inside this page, you need to click on this create environment option. I'm fine with this web server environment option. Under the application name, I'm going to give it as job portal. So based upon the application name, the environment name will be populated. And by default, when you're deploying an application by using the Beanstalk, it is going to give you a domain name. By using that domain name, you can access your backend application. So whatever domain name that Beanstalk is going to provide, it is going to have a name like this towards the end. So since I'm using this region, that's why here Asia-Pacific-South-1 is being used. So based upon the region, this value will change, but towards the end, you're going to have this elasticbeanstalk.com. Here, I can give a domain name as job portal and click on this check availability. I have created other environments. In those environments, I'll use the same domain name. That's why I'm getting this error. Let me give a name as eazy-jobportal. If I click on this check availability, this domain name is available. If you're not providing a domain name, a random domain name will be given by the Beanstalk. In a typical enterprise application, the organizations are going to have their own domain names, those also can be configured. Right now, since we don't have our own domain name for the website, I'll go with this option of Beanstalk domain name. Coming to the platform, we need to select the Java because our backend application runs on Java. Under the platform branch, I'll go with the default selected JDK, which is Java 25. I'm fine with the default platform version. Coming to the application code, I will select this upload your code option. Under the version label, I will give some version number like v1, and here I will select the local file let me click on this choose file. I'm uploading the jar that I have generated previously, once the jar is uploaded, I'm fine with this default preset. Let me click on this next. Inside this second page, we need to configure the required roles for the Beanstalk. So whatever Beanstalk environment that we're going to create behind the scenes, it is going to create the underlying resources like a virtual server by using EC2, a load balancer and required number of security groups. To perform all those actions, we need to configure a role for the Beanstalk. By default, you're not going to have any roles. That's why what we can do is, we can try to click on this create role and open it in a new tab, which is going to take you to this page of creating a brand new role. Here, the global will be selected because the roles are not specific to a given region. We need to make sure to select this AWS service, followed by the use case will be Elastic Beanstalk. First, let me create a role for the environment. If I click on this next, by default, AWS is going to recommend to add these two policies for this role. I'm fine with this default recommended permission policies. Let me click on this next. Here, I'm going to give name to my role. I'll give the name as Elastic Beanstalk Env. Env means environment. The name can be anything, but please give a name which is more meaningful. If I click on this create role, a brand new role is going to be created. Seems there is a role already with the same name. Let me update the name to environment and click on this create role. This time the role is created successfully. This is the role name. I need to remember the same. Next, I'm going to click on this create role one more time. This time also, I want to create a role for AWS service. Under this use case, I need to select the Elastic Beanstalk. Last time we created a role for environment, but this time we have to create a role for compute as well. By using this compute roles only, the Beanstalk is going to create the underlying virtual servers by using the AWS EC2 instances. Let me click on this next, and this time, these are the three permission policies that are being recommended by AWS. Let me click on this next. I will give the name as something like ElasticBeanstalkEC2Role. Let me click on this create role. The new role is created. Now I can go to the other tab here, if I click on these refresh icons, this time I'll be able to see the new roles that I have created. This is the role that I need to select for the service role coming to the EC2 instance profile role. I need to select this EC2 role that I have created. After selecting these required roles, you can click on this next. And inside the third page, I don't want to change anything. Let me click on next. Under this fourth page, I'm going to scroll down, scroll down. Under the capacity section, instead of single instance, I'm going to use the load balanced. When you select the load balance, you're going to have an option of defining minimum instance and a maximum instance. I will mention the maximum instances as 2. So based upon the traffic, the Beanstalk itself is going to decide whether your application should have a minimum one instance or maximum of two instance. For real enterprise applications, these maximum instances will be a high value like 20, 30, or 10 based upon the traffic that is being handled by the backend application. For our demo purpose, I'm intentionally keeping the max instances with a low number like 2. Regardless of whatever traffic that my application is handling, a maximum of two instances will be created by the Beanstalk. Coming to the fleet portion, we need to select the combined purchase options and instances, I'm fine with these capacity optimized spot allocation strategy. Let me scroll down, scroll down, scroll down. Towards the end, I'm fine with other default values. Let me click on this next. Inside this fifth page, I'm fine with these default values. If you scroll down further towards the end of this page, you will see some environment properties already defined by the Beanstalk. So this is the place where the Gradle is installed. And this is the place where Maven build is installed. Regardless of what build tool you are using inside your backend application, it is going to work automatically because both the famous build tools are going to be installed by the Beanstalk itself. The JDK also will be installed and taken care. Apart from these default provided environment properties, we should also define our own environment properties. Let me click on this add environment property. The first property that I'm going to define is, SERVER_PORT. To this, I will assign the value as 5,000. By default, we know our application is going to start at the default port 8080, but I want to change that behavior because when we are using Beanstalk, whatever load balancer that is being created by the Beanstalk, it is always going to forward the traffic at the port 5,000 by default. So that's why we need to make sure to start our backend application at the port 5,000. Let me create one more environment property. This time, I'm going to mention the property name as SPRING_PROFILES_ACTIVE. To this, I am mentioning the value as prod because I want to start my application by using the prod profile. In the similar lines, we should also define the database-related connection details as well. By default, inside the application-prod.properties, we have configured the default host name as localhost and default port number as 3306 and default database name as jobportal and this is the username and password. Since we want to override these default values, we need to make sure to create the environment variables with these names. So let me create an environment variable with the name as DATABASE_HOST. To this, I need to provide the hostname value given by the AWS RDS. So this is my complete hostname value. Let me add a few more environment variables as well. I'm fine with this default port number, which is 3306 and database name also jobportal. I'm fine because anyway, we also created a initial database with the name jobportal. But the username and password, we need to override. That's why I'll try to override those values by using these environment variables. Under the username, I'll mention the value as admin. Coming to the password, I'm going to mention the password given by AWS RDS. Usually in a typical enterprise application, the passwords are not going to be feed like this using plain text. . Instead, the passwords will be injected by using the AWS secret manager and AWS parameter store. So those are advanced DevOps concepts. For now, we should be fine with these plain text password. After defining all the required environment variables, we can click on this next button. In the last page, the Beanstalk is going to ask you to review all your details. Once you feel good, you can click on this create button. A brand new elastic Beanstalk environment is going to be created. And this is going to take again, five minutes to complete. I hope you are clear with all the steps that we have followed. Inside the next lecture, let's try to test our application, which is deployed inside the cloud environment.