Inside this lecture, I'm going to introduce about project to Lombok. So what is this project? It is a Java library, which is going to make Java developer life easy. It is going to save the developers from writing lot of boilerplate code. Basically, this library has a lot of annotations by using those annotations, we can make our life easy. I'll show you a demo around this Lombok, but before we try to use the annotations provided by these Lombok library, we need to make sure to add the required dependencies inside the pom.xml. To know the details around the Lombok dependency details, we need to go to the start.spring.io. And here you need to search for the dependency with the name Lombok. As you can see, this is a developer tool. This Java annotation library, it helps the developers to reduce boilerplate code. You can select this dependency and click on the explore button. And this is how our pom.xml is going to look like. You need to copy that dependency related to the Lombok and the same we can mention inside our pom.xml. So just after the H2 database related dependency, I'm adding the dependency related to Lombok. After adding the dependency of Lombok, we should also take care of mentioning the plugin details around the Lombok. As soon as we add the Lombok related dependency, there are two plugin details populated inside the pom.xml. The same I'm trying to copy and inside our pom.xml file so far, we have a single plugin, which is Spring Boot Maven plugin. I'm going to delete the same and paste the two plugins that we have copied. In the two plugins that I copied, we have the Spring Boot Maven plugin as well, which we deleted previously. Let's try to understand what is the purpose of mentioning these plugin details inside the pom.xml. Using these plugins, Maven is going to perform a lot of actions. So if you see, there is a plugin with the name Maven compiler plugin. So whatever configurations that we provide to this plugin, those details will be considered during the compilation of our program. So during the compilation of our application, we're telling to enable the annotation processing for the annotations mentioned inside the Lombok library, unless we process these Lombok annotations during the compilation time, we can't see the magic provided by the Lombok library. I'll show you the demo in a few minutes, which is going to give you clarity on the purpose of these configurations. The next plugin we have mentioned is spring-boot-maven plugin. So whenever we are packaging our application, we don't want the Lombok related dependencies to be packaged into the application jar file because the Lombok library is only required during the compilation time, but not during the runtime execution time. That's why under the configuration, I have mentioned the excludes. Under these excludes, we can mention any number of libraries. So far, I want to exclude only the Lombok library. That's why I have mentioned exclude along with the groupId and the artifactId of Lombok. After making all these changes, you need to click on the sync Maven changes and do the build. Once the build is completed, all the Lombok related libraries will be downloaded into your local Maven repo. Let me stop my application followed by you need to open the settings of your IntelliJ and here go to plugin and under the plugins, please search for Lombok. There is a Lombok plugin for IntelliJ. If it is not already installed for you, please make sure to install the same. By default, this plugin is going to come along with the IntelliJ IDEA installation. In case, if you don't see this disabled option, you need to make sure to install this plugin and restart your IDE. Since this plugin is already installed inside my IDE, I'm seeing this disabled option. In your case, you will see something like install option using which you need to install the plugin. So this plugin has in total 25 million downloads with a rating of 4.26. That shows how famous this Lombok library among the Java developer community. Next, you need to go to the build and execution deployment section, and here you can click on this compiler. Under this compiler section, if you can open this, there is an annotation processes. Let me select the same and please make sure you have enable annotation processing because the entire Lombok library, it is going to work based upon the annotations. Unless we enable those annotation processing, we can't see the magic provided by the Lombok. Let me apply and click Okay. I'm doing the build. Now to show you the magic of Lombok, I'll go to my Company entity class. So if you see here, we have good amount of Java fields and for all of them, we have getter and setter method. And this is all boilerplate code. That means this is not related to my Java business logic, but I have to create this code as part of the Java standards. Whenever I want to read the data from a Java object, I need to use the getter method. And when I want to update a field inside a Java object, I need to use a setter method. That's the whole purpose of these getter and setter methods. And if you see the total length of my Java class is around 180 lines. And this makes my Java code source code clumsy as well. To save ourselves from these kind of boilerplate code, what we can do is we can use the Lombok annotations. So first I'm going to get rid of all the getters and setter methods. And with that, my Java file, it is going to have source code of only 60 lines. That means we removed around 120 lines of code. Now I'll go to the top of this class. And on top of this class, I'm going to mention a couple of Lombok related annotations. The first one is @Getter. So this annotation, it is going to take care of generating the getter methods inside your Java class in the similar lines, I'm going to mention @Setter method as well. You can either mention both of them in two separate lines, or you can mention them in the same line. Please make sure you are separating them by using a space. If you see, these are the annotations from the Lombok library. When we add these two annotations, what is going to happen is, though your source code does not have the getter and setter method, by the time your code is compiled and converted into the bytecode, this Lombok library, it is going to update the bytecode to have the getter and setter methods. We can confirm this behavior as well. As of now, if you see inside my Company class, I don't see any getter and setter methods. We have only Java fields inside this class. But if I look for the list of methods available inside this class, you will be able to see, we got all the getters and setter methods. So who is doing this magic? It is the Lombok library. There are two good advantages with this Lombok library. The first one is we don't have to maintain the source code for all these boilerplate code inside of a Java source files. And with that, we are going to have smaller Java files, which is going to improve the readability and maintainability of the code. That's the first advantage the next advantage is the developer can simply mention these annotations and save himself from developing the boilerplate code. This way, there are a good amount of other annotations as well in the Lombok. Before I try to show you all the list of available annotations, let me show you how these Lombok annotation, it is also going to help us in creating the constructors of the class. We know the constructors of the classes are also boilerplate code. They will not have any business logic for them. Also, we can use Lombok annotation. I'll go to the controller class here. We have created a constructor of CompanyController. I can get rid of these and instead of these, what I can do is, I can simply mention an annotation @RequiredArgsConstructor. So when I'm mentioning this annotation behind the scenes, the Lombok, it is going to generate a constructor by taking all the final marked fields as an input. Since I have only one such field, a constructor with a single input parameter is going to be generated. So let me do the build. Once a build is completed, if I look for the list of methods and constructors, you'll be able to see there is a constructor created by the Lombok, which is accepting ICompanyService as a dependency. Since we have a single constructor, we don't even have to mention @Autowrired annotation. And that's how that dependency injection is also going to be taken care when we are mentioning this annotation. So the same we can mention inside the service class as well. So here also we have a constructor, all these kind of boilerplate code, we can get rid by using Lombok annotations. Let me do the build once a build is completed, we can start the application and test the REST API one more time but before that, let me add one of the property inside the application.properties file. I said previously, the SQL statements will be generated by the Spring Data framework we as a developer, we are not writing any SQL queries, but at the end of the day, our relational databases, they can only understand the SQL queries. So who is generating the SQL queries? It is the framework. We can see the SQL queries that are being generated by the framework by setting a property. The property is spring.jpa.showsql. By default, it is going to have false, but if we set this to true value, the SQL statements will be logged into the log files or onto the console. But setting this value always true is not recommended because even inside the production, you're going to print the SQL statements, which is going to attract some performance problems and security issues. That's why what we can do is instead of hard coding the true, we can inject the property value by using an environment variable. The syntax is ${}. First, I'm going to mention the environment variable as SHOW_SQL. This variable name can be anything. In case if this environment variable is not available, I want to set true as a default value. Inside the higher environments like production, we can inject an environment variable with the name SHOW_SQL with the value as false. And with that, this setting will be disabled and no SQL statements will be printed onto the console. If this environment variables concept is not making sense to you, that's perfectly fine because we're yet to discuss about this advanced concept inside these codes. Let me save this, do the build. Once the build is completed, I'm going to start my application. Once the application is started, I'm going to clean the entire console from the postman. I'll try to invoke this API one more time. We got the response successfully. Inside the console, you will be able to see the SQL statement that executed by the Spring Data Framework. Behind the scenes, the framework executed the SELECT statement and it has all the column names along with the table name. Now our code is leveraging the Lombok annotations and still the REST API is working fine. That means all the getter and setter methods that are generated by Lombok and the constructors that are generated by Lombok, they are working fine without any issues. Now, if you go and try to see the plugin details that we have added inside the pom.xml, it is going to make sense to you. Under these maven compiler plugin, we have added some configuration saying that the annotations present inside these library, they need to be processed and enabled. That's what we are trying to communicate by using these configurations. Unless we process these annotations during the compilation phase, we can't see the magic of Lombok. Lombok supports many other annotations as well. If you click on these features and select this table, you'll be able to see there are a good amount of annotations. So far, we have seen this getter and setter method demo. There is a @ToString annotation, which is going to generate the toString() method logic @EqualAndHashCode annotation, which is going to generate the hashCode() and equals() method inside your Java class. Around the constructor, we have @NoArgsConstructor, @RequiredArgsConstructor and @AllargsConstructor. When someone is using @NoArgsConstructor, a default constructor is going to be generated. Whereas with the @RequiredArgsConstructor, a constructor is going to be generated by considering only the final variables. Whereas if someone mention @AllArgsConstructor, the constructor by accepting all Java fields as input parameters will be generated. There is a @Data annotation, which is going to take care of generating getter, setter, @ToString, @EqualAndHashCode and @RequiredAgrsConstructor. In case if you want to avoid mentioning all these annotations, you can mention this @Data annotation. Similarly, there are many other annotations which we are going to explore throughout this course. At any point of time, if you have any question, you can click on these annotations and learn more details on how they are going to work, what magic they are going to do. The details that we discussed around the Lombok library, I have documented them in these slides for your reference. But at any point of time, if you want to explore more about Lombok, please go to the Lombok website and refer to the official documentation.