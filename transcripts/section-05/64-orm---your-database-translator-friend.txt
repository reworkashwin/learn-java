In the initial days of Java programming language, all the developers, they used to use JDBC library to interact with the database. But after a few years, a new ORM framework came into picture with the name Hibernate. So this Hibernate framework is used extensively by the developers to interact with the database by using Java code. This framework came as an extension to the JDBC library. But when developers are using this Hibernate framework, it is going to abstract away lot of complexities related to the database interaction. So this framework is an ORM framework. So what is ORM? Object Relational Mapping Framework. I will explain you in few minutes what is this ORM in detail. Inside this website, if you click on this ORM link, you will see an important statement saying that this framework is JPA compatibility. So what is JPA? Jakarta Persistence API. In other words, we can also call it as Java Persistence API. Here, if you try to read these paragraphs line by line, so they highlighted right alongside its own native API. Hibernate provides an implementation of the Jakarta Persistence specification. We can assume that JPA is a specification or guidelines that any framework has to follow when it wants to communicate with the database by using JDBC library. But the fun fact here is first Hibernate came into the market and it became so popular at the time there is no JPA specifications. By looking at the popularity of this Hibernate framework, what Java team they have done is they drafted the first version of JPA by inspiring from the success of Hibernate 2 version. And this Hibernate is a major influence on drafting the JPA specifications. Usually the way it works is first the guidelines or the specifications will be developed, followed by the implementation framework or the implementation libraries will be developed. But here it happened in other way and this shows how good this Hibernate framework is. But the problem with this Hibernate is, it is developed and maintained by some other organization or open source community. They don't have any direct relationship with the Spring ecosystem. But inside Java backend applications, majority of the organizations, they use Spring, Spring boot, right? That's why what Spring team they have done is, they came up with a project with the name Spring Data JPA. By using this Spring Data JPA, they make sure that there is a project available inside the Spring ecosystem by adhering all the JPA specifications. And behind the scenes, this Spring Data JPA, it is going to give you the flexibility to select one of the JPA specification implemented framework. Frameworks like MyBatis, Hibernate, all these are JPA specifications implementation framework. By default, Spring Data JPA, it is going to use the Hibernate as the implementation of the JPA specifications. Spring Data JPA framework made the backend developer life so easy. It has a good integration with the Spring boot and it is going to work very similar to Spring and Spring boot frameworks. The developer never have to worry on learning the separate framework like Hibernate because all that complexities are abstracted away by using the Spring Data JPA. We're going to see the magics of the Spring Data JPA in the coming lectures. You will be surprised by the minimum amount of code that we are going to write to interact with the database. I documented a few of the important details on how Spring Data JPA and Hibernate are related. Let's try to discuss them one by one. Like I said, JPA, which is Java Persistence API or Jakarta Persistence API, it is a specification, it is not an implementation that defines a standard way to manage relational data in Java applications. We can think of it as a contract or a set of rules that describes on how Java objects should be mapped to a database tables. On the other side, Hibernate is a JPA implementation, the actual ORM framework, ORM means Object Relational Mapping framework that does the heavy lifting. It handles the actual database operations, SQL generation, caching, lazy loading, transaction management at the ORM level. It is going to sit in between your Java objects and the database. Spring Data JPA is an abstraction layer on top of the JPA implementations like Hibernate. It provides repository pattern, query derivation, pagination, and reduces boilerplate code. All these jargons, it may not make sense right now, but by the end of this course, everything is going to be clear for you because we're going to discuss all these concepts. Spring Data JPA, it always sits on top of any JPA provider or implementations like Hibernate, Eclipse Link, OpenJPA, MyBatis, and so on. So these are all the JPA implementation frameworks, but inside the industry, Hibernate is a famous implementation framework. The advantage of using Spring Data JPA inside our applications is, it is going to hide most Hibernate complexities, and it has good integration with the Spring and Spring Boot ecosystem as well. In simple words, we can say Spring Data JPA uses JPA specifications, and these JPA specifications are implemented by Hibernate framework. I can give some real life analogy as well. You can assume JPA is like an international car standards that every car must have, like every car should have a steering, brakes, gears, just a rule book, but you can't drive the vehicle by taking these rule book alone. It's just a standard that every manufacturer has to follow. Now coming to the Hibernate, it is like Toyota, who is an actual car manufacturer that builds real cars by following the standards defined here. On top of the standards, they're going to add extra features like cruise control. In the same way, though Hibernate is an implementation of JPA, it is going to provide some extra additional features as well. Now coming to the Spring Data JPA, it is like Tesla autopilot. You just say, take me to work, and it is going to handle all the driving automatically. And behind the scenes, the Spring Data JPA, it is going to use the logic written by the Hibernate, just like how Tesla autopilot, it is going to use the functionalities under the Tesla car. I hope this is clear. Before we try to close this lecture, let's try to understand what is an ORM framework. Inside Java, we know all the data has to be stored inside the objects. Now coming to the relational database, all the data, it is going to be maintained in the form of tables, columns, and rows. By using these ORM framework, we're going to establish the mapping between an Java object and the relational database. So that's why this name came ORM, Object Relational Mapping. I'll explain by taking an example as well, how this mapping we are going to establish. In simple words, we can say ORM is a translator between your Java objects and database tables. The problem that ORM solves is, Java always thinks in objects like User object, Order object, Product object, Company object. And each of these objects are going to have their properties and methods. Whereas database think in terms of tables, rows, columns, foreign keys, SQL statements. This ORM is going to act as a bridge that automatically converts data between these two different models. The benefits of ORM are, the developers, they have to write Java code, but not the SQL code. The developers, they are always going to think in terms of Java objects, but not the database tables. There is no need of manual conversion, ORM does the translation for us. We are going to write less code. For example, when you are using this ORM framework, you need to write only one line of code. But with the traditional JDBC library, you have to write at least 10 plus lines. When you are using this ORM framework, there'll be few errors because there won't be any typos inside the SQL statements. Since the SQL statements are going to be generated by the framework, there won't be any scenarios around the typos or the SQL statement errors. And the last advantage is around that database independent. Regardless whether you are using MySQL database or PostgreSQL database or Oracle database, the Java code that you're going to write is always going to be the same. For example, if your project is using MySQL today, and for this, you have developed some ORM related logic, and tomorrow if you are migrating to PostgreSQL, you don't have to worry as a developer, the same ORM logic will work for the PostgreSQL as well. Though behind the scenes, these databases, they are going to work in a different style and they're going to have different SQL syntaxes, still your Java code is going to work. Because the ORM framework is going to take care of generating the SQL statements based upon the underlying database that you're trying to use. Inside the next slide, I have an example on how the ORM framework is going to work. Let's imagine inside the database, there is a table with the name USERS. And inside this USERS table, there are columns like Id, name and email. As a developer, what you have to do is, you have to create corresponding Java classes. So here I have created a Java class with the name User. And inside this class, we should have corresponding Java fields with the same names like id, name, email. In the similar lines, I also have another table with the name Orders. So this Orders table, it has a foreign key reference with the User table. That's why we are able to see userId column here, which belongs to this User table. In the database world, we call this link as foreign key link. In this kind of scenarios, this is how you need to create the corresponding Java classes. You can mention the class name like Order. And inside this, you're going to mention the Id field. Coming to the userId field, you're going to represent it by mentioning a user field of type User. So the data type of the user field is User, which we have here. This way, by using the Java references, we should be able to represent the foreign key relationships of the database inside our POJO classes. At last, I have the amount field as well, but the corresponding amount column, when you define all the required Java classes representing your database tables, then you can start communicating with the database by using normal Java code itself. For example, here, whenever you are updating a name value into the User object by invoking the setName() method, behind the scenes, the ORM framework, it is going to understand that this fellow, he wants to update the name column with this new value inside the database. And with that, it is going to generate this kind of update SQL statements behind the scenes. And it is going to take care of generating, executing, committing the transaction and rollbacking the transaction in case of any issues. Similarly, there is a concept called repository inside the Spring Data JPA. By using those repository interfaces, if you invoke the save() method and pass a Order object, the framework is going to generate the insert SQL statement to store the orders related data into these orders table. We are going to see all these in action in the coming lectures. Similarly, there are methods like findAll(), which is going to execute the select statement. And in case if you are trying to fetch the orders of a user by using user.getOrders(), the framework is going to generate this kind of SQL statement, which is going to fetch the required data based upon our Java logic. As a Java developer, you're always going to communicate your business requirements around the data by using Java code itself. The actual SQL generation, executing of them will be taken care by the ORM framework. So what is the ORM framework that we are going to use? It is the Hibernate. And we are never going to directly interact with the Hibernate, instead, we are going to leverage Spring DataJP because it is going to make our life super easy when we want to interact with the ORM framework like Hibernate. I know you may have some questions and few points may not be clear on how the mapping between the Java classes and database tables is going to work. Don't worry, when we update our Spring Boot backend application to interact with our company's database table, everything is going to be clear for you.