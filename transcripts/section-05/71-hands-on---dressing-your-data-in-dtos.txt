To get started with the DTO pattern, I'm going to create a new package with the name dto. Under our root package, I'm trying to create this dto package. Inside this new package, I'm going to create a new Java class with the name CompanyDto. We know inside these DTO classes, we are going to simply define Java fields and their getter methods, but not the setter methods. Because once the object of these DTO classes are created, they need to be immutable because using these objects only, we are transferring the data from one layer to other layer. We should not have any setter methods and all the Java fields should be of type final. Instead of creating our fields as final and defining the getter methods alone, inside the Java, we can follow a technique. If we create our Java class as a record class, then behind the scenes, the Java library is going to take care of marking all the fields as final and creating only the getter methods. So this is my record class. Inside these parentheses, we need to mention all the Java fields. Since we are going to copy CompanyEntity object data into these DTO class, we need to make sure to first understand the fields that we have inside the Company. So these are all the fields. We can define the same fields inside the DTO as well. But before that, you need to do an exercise. As a developer, you need to understand which fields will be required by my client application. In our scenario, we know this information will be leveraged by the job portal UI application. For that application, most of this information will be required except the last three fields. My UI application, it won't require createdBy, updatedAt, and updatedBy. Because we are never going to display this kind of metadata or audit related information in the UI application. But it may require this createdAt value. Because inside the UI, if the end user want to sort the company's details based upon the created date, under such scenarios, this field data is required. That's why what I'll do is, inside my DTO class, I will try to create the same fields except these three last fields. So here I'm trying to define the same fields. This is the syntax that we need to follow whenever we are defining fields inside the record class. So we don't have to mention access modifier like private or final. We just have to mention what is the data type and the field name. Please make sure you are maintaining the same field names inside your DTO class as well. Because inside my UI application, I have considered the same field names to display the data. If you change any of these field names, then the UI code may not work as expected and you will be forced to fix the issue on the UI side. To avoid all those issues, please make sure you are using the same field names that I'm using inside my CompanyDto. Basically, I'm trying to use the same field names across the company entity and the DTO class. Once we have this DTO ready, we need to go to the service layer. Inside the service layer only, we are trying to interact with the database layer. So here by using these companyRepository.findAll(), we are trying to invoke the database operation. This method, we know it is going to give us the list of Company entity objects as an input, but I don't want to share the same company list back to the controller layer and to the client application. Instead, I want to provide CompanyDto objects as an output. So I have updated the signature. We need to update inside the interface as well. Once we updated these method signatures, we need to make sure to copy the data from the Company object to the CompanyDto object. To write the mapper logic, I'm going to create a private method, which is going to return CompanyDto object. The method name is transform to DTO and to this method, I'm going to pass the object of Company. From this method, I'm going to return the object of CompanyDto and while creating the object of this record class, we need to pass all the fields data like Id, name, logo, and so on. From where I can get this information from the Company object. Let me copy few lines of code. If you see, I'm trying to read the Id information from the Company object, similarly, name, logo, rating, and so on. This is how we can write the mapper logic. Here my requirement is to just to copy the data from one object to other. In case if you want to do some transformations as well, you can do that inside the same method. You will see inside the industry, few developers, they are going to write this mapper logic by using some libraries instead of writing the mapper logic manually by transforming the data from each field to field. What they're going to do is they're going to leverage few of the libraries available inside the open source community. The libraries are like mapstract and model mapper. These libraries, they are going to help us in copying the data from one object to other object without writing a lot of code. But I'm not going to use these libraries inside this course. There are two reasons why I'm not using these libraries. The first one is these libraries, they are not officially recognized by the Spring Boot team. That's why if you search inside the start.spring.io for the dependencies of these projects, you won't find anything. Whereas for other libraries like Lombok, we should be able to find the starter projects. Since these libraries are not officially supported by the Spring Boot team, many enterprise projects, they will not be comfortable using these libraries. They may have some security vulnerabilities. That's why inside the real applications, you will see very less people using these libraries. Whereas inside the tutorials or in some low critical applications, you will see some developers using these libraries to a great extent. That's one reason. The other reason is if we are using these two libraries, then you will not have full control on your mapping logic. Whereas with this kind of code, the developer is going to have the full control on the mapper logic and the developers can tweak this logic based upon their business requirements to any extent. I hope you are convinced. In case if you want to learn more details about these two libraries, please refer these websites and try to learn about them by using the official documentation. So once we have these private object ready, what I will do is, since I have created these private method inside the controller class, but it should be inside the service class. So let me paste it here, followed by, now what I'll do is each of the record that we have inside this company list, I'm going to iterate them by using stream(). After the stream(), I'm going to invoke the map() method using this map() method for each record. I'm going to invoke the transform to DTO method. Once a map logic is executed towards the end, I'm going to invoke the collector method to this collector method. I'm going to pass collectors.toList() as an input. And with this, what is going to happen? I'm going to get all the transformed DTO objects as a list. The same I can try to return from this method. Now my service layer is sending the response in the form of DTO. The same has to be accepted in the controller layer as well. So here, instead of list of company, I need to mention it as List of CompanyDto. Let me update the same here as well. So let me save these changes. Do the build. Once the build is completed, I'm going to restart my application. This time from the postman, if I try to invoke the same REST API, we're not going to see the fields like createdBy and updatedAt and updatedBy. We can confirm the same as well. Let me click on this send button. Yes, this time we are seeing only the created date, but not the other three audit fields. This way we're going to use this DTO pattern to a great extent throughout this course. I hope you're clear on how to use this DTO pattern on a high level. If something is not clear, don't worry, by the end of this course, everything is going to be clear because we are going to see many demos around the DTO pattern as well.