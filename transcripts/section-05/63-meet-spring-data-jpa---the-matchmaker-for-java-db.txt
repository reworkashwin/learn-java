Let's first try to understand what is the Spring Data project in the Spring ecosystem. As you can see, under this overview page, we should be able to read some basic information around this project. So Spring Data project, it is going to provide a familiar and consistent Spring-based programming model for data access while still retaining the special traits of the underlying data source. This statement looks very complex to understand, but let me try to explain the same by using simple language. Inside the IT industry, we have traditional relational databases like MySQL, Oracle, Postgres. So all these are SQL based databases. And similarly, we also have no SQL databases like Mongo database. We have caching related databases like Redis. And similarly, we have other types of databases like Elasticsearch. Neo4j is a graph based database. Similarly, we have other databases like Apache Cassandra, Couchbase. Since there are many database options available inside the industry, there is a good chance one project might be using Oracle database and the other project might be using Mongo database. And this way, different projects may use different databases based upon their requirements. Regardless of which underlying database that a project is using, the Spring Data project, it is going to provide a familiar and consistent Spring-based programming model, which means it is going to provide a set of interfaces, classes, and configurations that we can follow consistently across any database that you're trying to use. Let's imagine today you are using an SQL based database like MySQL, and tomorrow you may migrate into a NoSQL database like Mongo database. When you're migrating from one data source to other data source, you don't have to learn a lot around the Spring Data ecosystem, because the way it is going to work is always going to be familiar and consistent across any data source. So you can assume this Spring Data project as an umbrella project, which is the parent project for all the sub-projects of Spring Data. So now let's try to understand what is this Spring Data JPA. So Spring Data JPA sub-project is built on top of the JPA specifications. So what is JPA? Java Persistence API. Inside Java ecosystem, whenever you want to communicate with a relational database, there are certain standards that are defined by the community people. So by using those standards, this Spring Data JPA sub-project is developed. If you are using any of the SQL databases like MySQL, Oracle, or Postgres, you need to make sure you are using the Spring Data JPA. So inside this sub-project, we're going to have interfaces, classes, and configurations that are more specific to the relational databases. Whereas if you select the other sub-project like Spring Data MongoDB, you're going to have more specific classes and interfaces around the NoSQL databases like MongoDB. So based upon which database that you're trying to use, and accordingly, you need to use one of these sub-projects of the Spring Data. Throughout this course, we are going to use the Spring Data JPA because we are going to store our data in a traditional database like MySQL database. So whatever concepts that we are going to discuss throughout this course, they're going to help you in future when you are migrating from one of the sub-project to other sub-project. That's why as long as you perfect the Spring Data JPA concepts, you should be able to migrate any of the sub-projects in the future based upon your project requirements. We also have another sub-project with the name Spring Data JDBC. Both these Spring Data JDBC and Spring Data JPA sub-projects we should use when we are storing our data in a traditional relational databases like Oracle, MySQL, Postgres, and so on. But what's the difference between these two projects? Spring Data JDBC is a lightweight sub-project, which is going to provide some simple JDBC features. If you are building a lightweight application with less complexity, then you can go with the Spring Data JDBC in case if you are looking for some advanced options like applying caching, relationship mappings, and such scenarios, you need to consider the Spring Data JPA. So basically Spring Data JPA is recommended for enterprise organizations where the requirements and the database will be complex in nature. As long as you learn Spring Data JPA, you should be easily cope up with the Spring Data JDBC by exploring the official documentation. Since Spring Data JPA is going to be used majorly inside the industry, I'm going to focus more on to the Spring Data JPA inside this course. Both the Spring Data JPA and Spring Data JDBC behind the scenes, they are going to leverage the core Java JDBC library. Whatever steps that we have discussed previously that developer used to follow by using JDBC library, all these steps right now will be taken care by the frameworks or the projects like Spring Data JPA. So these projects, they're going to abstract away all these steps from the developer so that the developer can simply focus on the business logic. We're going to see the demo of all these in the coming lectures. I documented the details around the Spring Data project and Spring Data JPA as well inside these slides. As I explained, Spring Data is a part of the Spring framework that simplifies the database access in Java applications. It provides an easy and consistent way to interact with the databases without writing boilerplate code. This is an important point. Spring Data is an umbrella project that includes several modules or subprojects to work with different types of databases like relational, NoSQL and so on. If you take all your application business logic, it is going to present inside your Java classes, which you have written. Such classes that are going to be present inside your Spring application logic. Just below the Spring application, we're going to have an umbrella project Spring Data. So this Spring Data project, it is going to provide some consistent interfaces and Java method Java classes that we can use to interact any of the databases in a consistent manner. But behind the scenes, this Spring Data umbrella project, it is going to leverage the specific project related to the database that you're trying to use. For example, if you're trying to use traditional relational database, then you need to make sure you are adding the starter project around the Spring Data JPA or Spring Data JDBC inside your pom.xml so that the Spring Data will use this subproject to execute the queries. Whereas if you are trying to use NoSQL database, please make sure to add the starter project related to the Mongo database. The same applies for other types of databases. Inside the next slide, I mentioned on why we should use Spring Data project. When we are using Spring Data project, it is going to help us in reducing the boiler plate code for database operations. This we already discussed the next important help that we are going to get from Spring Data is it is going to simplify the CRUD operations. So what is CRUD? CRUD means Create, Read, Update, and Delete operations on top of a database. We can execute any of these four operations, regardless of which operation you're trying to execute. The Spring Data has a simplified approach that developers can follow. We're going to discuss about those approaches in the coming lectures. And this Spring Data, since it is part of the Spring ecosystem, it is going to work seamlessly with the Spring Boot framework as well. And as we discussed, Spring Data supports various kinds of databases. Inside the slide, I highlighted the differences between Spring Data and Spring Data JPA. This is a very important interview question. Never get confused between these two projects. They are two different projects which have different objectives, but there is a relationship between them. Spring Data is like a parent project, whereas Spring Data JPA is a child project or a sub project of Spring Data. Spring Data provides common functionalities for different types of databases, whereas Spring Data JPA, it is going to help us to interact with the relational databases only like MySQL, Postgres, Oracle, and H2 databases. So when we want to interact with any of these relational databases, we don't have to write complex SQL queries. The Spring Data JPA framework is going to take care for us. We can assume Spring Data as a toolbox that contains different tools for working with the databases, whereas Spring Data JPA is a tool that is specifically designed for JPA databases. Behind the scenes, this Spring Data JPA framework, it is going to use the Hibernate as the default JPA provider to manage the database. So inside the Java ecosystem, there is a Hibernate framework, which is an ORM framework. We are going to discuss about what is this Hibernate? What is its relationship with the JPA in the next lecture? For now, please note this Spring Data JPA behind the scenes, it uses Hibernate as the implementation provider for the JPA specifications.