So far, we developed an entity class representing our company's database table. Using these entity class objects, we're going to store or represent the data that we want to store or fetch from the database table. The next component that we need to create is a Repository interface for the database table. So just like how we have created these Company entity class, in the similar lines, we're going to create a Repository interface representing company's database table. I'm going to create a new Java package. The package name will be com.easybytes.jobportal.repository. Inside this package, I'm going to create a new interface with the name CompanyRepository. Let me select interface and press enter. This interface, it has to extend one of the interfaces that we have discussed in the previous lectures. In case if you are looking only for the CRUD operations related methods, then we need to extend the CrudRepository or ListCrudepository. Since I want to extend all the functionality from the framework, what I'm going to do is I will try to extend the interface available from the Spring Data JPA. The interface is JpaRepository. So whenever we are extending these interfaces from the framework, we need to take care of providing two details. If you open any of these interface, they accept two input parameters as generics. The first one represents what is the entity class that represents your database table followed by what is the data type of the primary key field. Let me go to CompanyRepository. And here first I have mentioned company, which is the entity class representing the database table followed by inside the company, the Id, which is acting as a primary key column. The data type of this field, we marked it as Long, the same we need to mention here. On top of this interface, we can mention an annotation @Repository and mentioning this annotation is completely optional. Even though if you're not mentioning these annotations, still the bean of this interface is going to be created during the startup of the application. How the framework is going to know to create the bean of these interface. Since we are extending the JpaRepository, which is one of the interface from the Spring Data JPA or Spring Data project, the Spring and Spring Boot framework, they are going to have clear directions behind the scenes to create a bean of these interface during the startup. Yeah, you may have a question, Madan, bean is nothing but a Java object, but this is an interface, how can we create an object of an interface? This can be a question. Let me answer the same. Before the framework tried to create a bean of this interface, first, what it is going to do is it is going to generate the implementation code for all the abstract methods that we are inheriting from these JpaRepository interface. Once the implementation logic is generated for that implementation class, the bean is going to be created. So all this is internal to the framework. I'm just sharing for your reference. Once this interface is ready, you can inject these interface as a dependency into the controller or service layer. Let's try to do the same. What I'll do is I'll go to the CompanyController class that we have. Here we have a REST API, which is returning a simple updated companies list. From this controller layer itself, I can invoke the methods exposed by the CompanyRepository, but we should not do that. The best practice is from controller layer, the code should go to the service layer. From the service layer, the repository layer invocation has to happen. That's why what I'll do is I'll try to create a new Java package with the name service. Inside this package, I'm going to create a new interface with the name I CompanyService. So I indicates it is an interface. Inside this interface, I'm going to create an abstract method. This abstract method, it is going to return List of Company objects representing various records from the database table. The method name I'm going to mention as getAllCompanies() and this method, it is not going to accept anything as an input. Now I'll try to write an implementation class for this interface. Just under the service package, I'm going to create a new Java package with the name impl. impl means implementation package. Inside this package, I will create a new Java class with the name CompanyServiceImpl. And this class, it is going to implement an interface with the name ICompanyService. Inside this interface, we need to make sure to overwrite the method, getAllCompanies(). As of now, I'm returning an empty list. On top of this class, we need to mention at @Service annotation. And with this annotation, the spring framework, it is going to create the bean of this class during the startup. I can try to inject this as a dependency into the controller layer. I'll go to the company controller and here I will try to define a variable with the name private final ICompanyService companyService, followed by I will create a constructor. And inside this constructor, I'm going to write this logic of this.companyService is equal to companyService. On top of this constructor, we can either mention @Autowired annotation or we can ignore it as well. Because as we discussed in the previous sections, if you have a single constructor inside a class, then mentioning the @Autowired annotation is optional. With this logic, the dependency injection is going to happen by using the constructor injection. So here you may have a question, why you are trying to inject ICompanyService instead of CompanyServiceImpl. The reason why I'm doing this is, this code, it is going to give me flexibility to change the implementation code in future to switch to some other implementation class without making any changes inside this controller class. So far, I have a single implementation class for this interface. That's why this code may not make sense, but in real applications, you may have multiple implementations. For example, inside QA environment, you may want to use one implementation, whereas in production, you may want to use another implementation. In such scenarios, you can mark one of the implementation as primary so that the corresponding primary implementation class will be injected into this controller without making any changes inside this class. Once the bean of this CompanyService is injected using the same, I'm going to invoke the getAllCompanies() method. The written type of this method is List of Company. Let me catch the output with the variable name as companyList. So whatever company list that I'm going to receive from the database, the same, I want to send under the body of the response. So here, instead of this dummy string, I'm going to pass entire company list. We're getting this compilation error because under the response entity, we have mentioned string instead, it should be List of Company. So with this, now our REST API is ready, but from our service layer, we are sending the empty list this is not what we want. Instead, we want to make a call to the database and retrieve all the records. To make this work, what we have to do is into the ServiceImpl, we need to inject the bean of CompanyRepository. Let me create the field of type CompanyRepository. And here also, I'm going to create the constructor. By using this constructor, we are doing that dependency injection. We know during the startup, the bean of this CompanyRepository is going to be created. The same will be injected as a dependency into this constructor. And inside the constructor, we are assigning that bean to the CompanyRepository field that we have here. So now I can use this CompanyRepository inside these getAllCompanies() method. Let me mention CompanyRepository here as soon as I press dot, I have various methods that I can use to talk with my database table. So what is the database table that I'm trying to talk? It is the Companies. If you go to these CompanyRepository here, we have mentioned the entity as company. And inside this Company entity class, we have mentioned the table name as companies. And that's how the framework is going to know which table we are trying to talk and what are the columns that the table is going to have. So let me go to the service class and here I'm going to invoke the findAll() method because I want to retrieve all the records from the table without any where conditions. In the coming sections, we are going to discuss how to define some where conditions by using spring data JPA. It's going to be very interesting topic for now our requirements are very simple. We want to fetch all the records from the companies table. That's why I'm trying to invoke the findAll() method. If I want to pass any sorting requirements or pagination requirements, we need to invoke other methods for now. Let's keep it simple because we are still learning the basics. So the return type of this findAll() will be List of Company. So let me catch the output by using company list, the same I'm going to return as an output from this method. Instead of doing all this, what I can do, I can simply return whatever output I'm getting from the findAll() method as an output from this method. So this is the only code that we have to write. If you see the beauty of spring data JPA here, nowhere I'm writing an SQL statement, nowhere I'm creating a connection, nowhere I'm executing the SQL statement. There is no exceptional handling, there is no transactions handling. Everything is going to be taken care of by the framework. As a developer, I just have to tell what is my requirements to fetch the data. And I told that by using Java language itself, by invoking these findAll() method. If you have various other requirements, like if you want to delete a record, you have delete methods. If you want to update or insert a record, you have save related methods. We're going to explore all of them. But now let's see the demo of findAll(). Before we try to test these code changes inside the company entity class, I forgot to make some changes. So far, we only declared the Java fields. We have not declared any getters and setter methods for the Java fields. As a standard, we need to make sure to create the getters and setter methods as well. That's why what I'll do is I'll try to generate getter and setter methods for all the fields that we have defined inside this class. So the getter and setter methods are created. Now I'm good. Let me do the build. Once the build is completed, I'm going to start my application. From the postman, I'll try to invoke my REST API. We need to make sure to mention these HTTP method as GET. Please make sure you are mentioning these version related header. As soon as I click on this send button, I got all the company records as a JSON output. So this is a JSON array and there are a good amount of company details. So all these details from where they are coming from the database. This is the exact magical moment that I'm looking to show it to you. Do you see how easy it is to speak with the database by using Spring Data JPA? Spring Data Project is a very good project inside the Spring ecosystem. I want to teach you as much as possible around Spring Data through this course. That's why please make sure you are completing this entire course to gain the entire knowledge of Spring Data and Spring Data JPA. Once you are clear with the concepts that I'm going to discuss throughout this course, you can explore further about these projects by looking at the official documentation. I hope you are happy with this demo. This is the first baby step that we have taken in our Spring Data JPA journey. We are going to discuss a lot of concepts. We are going to do a lot of optimizations. I hope you are excited.