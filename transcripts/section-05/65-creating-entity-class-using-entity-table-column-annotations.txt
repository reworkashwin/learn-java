Since we are inside the new section, inside my workspace location, I'm going to create a new folder representing the section that we are right now. Into this new section folder, I'm going to copy the spring boot project from the previous section. We have this project with the name job portal. The same I'm trying to paste into the new section. Whatever code changes that we are going to make inside this section, you can find them under this section folder. I have opened the project using my IntelliJ. To get started with the Spring Data JPA, you need to make sure that you have added the required starter projects inside the pom.xml. In our pom.xml, we already added required dependencies. First, we need to make sure there is some database running behind the scenes. As of now, we are using the H2 database, but in the coming sections, we're going to migrate from this in-memory database to actual production database. Apart from these database-related dependencies, you should also make sure to add the starter project related to the Spring Data JPA. So this is the starter project, please make sure this dependency is present inside your pom.xml. Next, if you try to open the application.properties, here we have configured all the database-related details. This is my database URL, username, and password. By using all these details and the driver class name, the Spring Data JPA behind the scenes is going to create a connection with the database during the startup of the application. As a developer, I never have to worry about creating the database connection. I am always going to focus on my business logic. So next, we know inside our database, we have a single table with the name companies. As a next step, we need to create a corresponding Java class inside our project representing the company's database table. What I'll do is, I'll try to create a new package. The package name will be com.easybytes.jobportal.entity. There is a reason why I'm giving the name entity here. You will get to know the reason in few minutes. Inside this entity package, I'm going to create a Java class with the name company. We should not use plural name here because by using this class, I want to represent a single database record. Since each of the company class object is going to represent a single database record, we need to make sure we are not using plural form name here. Once the Java class is created, we need to create the Java fields inside this class representing the columns inside the company's table. But before that, we need to make sure to mention few annotations on top of this class. The first annotation that I want to mention is @Entity. So this annotation is coming from the Jakarta Persistence API. Since the Spring Data JPA framework is going to follow the JPA specifications, we need to import this annotation from the Jakarta Persistence package. So let's try to understand what is the purpose of this @Entity annotation. Whenever we are mentioning this annotation on top of a Java class, it is an indication to the Spring Data JPA framework that so-and-so entity class, it is going to represent a database table. I hope now you are clear on why I have mentioned the package name as entities inside this package I'm going to create all the entity classes for the tables that we are going to create throughout this course. Next after the @Entity annotation, we should also mention @Table annotation. This @Table annotation also is coming from the same package. And by using this @Table annotation, we need to invoke the name parameter and to this, we need to pass the table name that we have defined inside the database. We can get that table name by opening the SQL file. The file is jobportal-schema.sql. So this is that table name that we have mentioned. Let me mention the same here. In case if your table name and the entity class name is same, then you don't have to mention these name parameter. The framework will consider that the class name is same as the table name. Since here the table name and the class name are different, I need to make sure to provide the actual database table name by using the name parameter. So whatever name that we are providing here, the same table name will be used by the framework while it is generating the SQL statements. Next we need to create Java fields representing the columns that we have defined inside the company's table. We have created various columns like ID, name, logo, industry, size, and so on. So let's try to create each field one by one. So the first column that I have is, ID and it is always going to hold some numeric value. Since we have mentioned the column type as BIGINT, we need to make sure we are using the corresponding Java data type as well. So what I'll do is, I'll type private followed by Long and the field name. The field name is ID. On top of this field, I need to mention @Column annotation. Using this annotation, I need to invoke the name parameter and to this name parameter, I can mention the actual column name. The actual column name is Id itself. So here also, if your Java field name and the column name are same, you don't have to mention the name parameter. Mentioning this @Column annotation also is completely optional. As long as you make sure that your Java field name is matching with the column name, you should be good. But I strongly recommend you writing this annotation along with the name attribute because it is going to improve the readability of your code. Apart from name attribute, we have many parameters supported by the @Column annotation like length, nullable. So there are a good amount of properties. We're going to explore all of them throughout this course. But right now, I'm more interested around the nullable property. By default, nullable true is going to be constructed by the framework. But since we have marked this as a NotNull column inside the database, we need to make sure to configure the same inside our Entity class as well. Since we mark this Id column as a primary key column, which is a NotNull column here, we need to make sure to mention nullable as false. And since this Id column is going to act as a primary key value, we should also make sure to mention one more annotation on top of this field. The annotation is @Id. We can try to see the source code of this annotation on top of this annotation. We have some documentation as well. So whenever someone is using this annotation on top of a Java field, it is going to help the framework to identify the primary key of an entity. So what is an entity? An entity represents the database table. Since this Id column is going to act as a primary key, we have mentioned @Id annotation. The next configuration that we need to worry is whenever we are defining a column as primary key, the value for that primary key column can be provided by the developer or it can be auto-generated by the framework. So we have various options to convey that strategy and how the primary key value should be generated and auto-incremented. We need to mention one more annotation just below this Id. So the annotation is @GeneratedValue. Using this annotation, I'm going to tell to the framework how the primary key Id value should be generated, who is going to take care of generating the primary key value. Let me invoke the property or the parameter with the name strategy. To this strategy property, I'm going to pass IDENTITY as a GenerationType. GenerationType is an enum class present inside the framework, and this enum class has various enum constants. As of now, we have configured identity. So whenever we are using these identity generation strategy, that means we are telling to the JPA framework, please give the responsibility of generating the primary key value to that database identity column. Inside our SQL statement, we have made this configuration saying that for this column, we have configured auto increment value. Whenever a new record is being inserted into this table, a new incremented value will be assigned as an Id by the table itself. That's why we are configuring identity inside our Entity class as well. Apart from identity, we have other strategies as well. For example, if you are mentioning this table, then it is an indication to the JPA framework like Spring Data JPA that it should assign the primary key values by using an underlying database table to ensure uniqueness. What Spring Data JPA framework will do is, it is going to maintain a separate database table where it is going to maintain what is the current unique value. And whenever someone is trying to insert a new record, it is going to increment the unique value by one and assign the same to the new record. The next strategy that we have is sequence. In this sequence, it is going to work very similar to table, but instead of creating a separate database table to maintain the unique values, this approach, it is going to use that database sequences to generate the primary key values. Apart from this table, sequence and identity, we also have UUID approach. When we are using this approach, we are telling to the framework, always generate a unique identifier by yourself by following these standards. Basically, this approach behind the scenes, it is going to use the UUID class present inside the java.util package to generate a random unique string value. But when you are using this approach, you need to make sure that your primary key column and the Java field, they both are of type string. At last, we have AUTO strategy as well. If you're not sure which approach to use, you can mention the AUTO and the framework, it is going to identify one of the approaches based upon the underlying database that you're trying to use. For few databases, one approach might be good, whereas for other database, the other approach might be good. For example, the identity approach that we are using, this is going to be a good approach for the MySQL databases. Since we are planning to use MySQL database in the coming sections, I'm configuring the strategy approach. But in case if you have plans to migrate to some other database in the future, and if you're not sure about which strategy to use, feel free to use this auto generation strategy. Throughout this course, I'm going to use this identity. I recommend you also using the same. So this is the story around the Id field. Since this is acting as a primary key column, we need to mention these extra configurations like @Id annotation and generated value. For the next two columns, it's going to be very easy. What is the next column that we have after the Id ? It is the name, which is of type varchar. So for varchar, the equivalent Java data type is string. And this one also Not Null. And we also configured unique as well. So what we have to do is, we have to create a Java field with the name name and data type as String. On top of this, I'm going to mention @Column annotation with the name as name itself, followed by nullable as false. Coming to the unique, we need to set the value as true, by default, it is false. So this way, we need to define the Java fields corresponding to all the remaining database columns. So what I'll do is, I'll try to paste a few lines of code so that we can try to discuss very quickly one by one. I pasted new Java fields. You can find all these code inside the GitHub repo. You don't have to type all of them manually. So far, I'm trying to show you a manual approach where you're going to type each and every line of code. But there are some plugins available inside IntelliJ using which we should be able to generate the entity Java classes. I'm going to show you all those techniques in the coming sections. For now, since you are trying to learn the basics, I'm trying to explain each and every concept slowly. After the name, we have the logo column. That's why I have mentioned the Java field name as logo. And on top of this also, I'm mentioning the @Column annotation, and the name of the column is logo and coming to the length, it is 500. If you go to the SQL here, the length for this column, I have mentioned as 500, the same we need to configure even for the name also, we have mentioned the length as 255. What we can do is, we can invoke the length attribute. And if you see the length attribute, by default, it is going to construct 255. That's why we can ignore this. Coming to the name of the column, let's try to maintain it as complete capital letters so that when the SQL statements are printed into the logs or onto the console, we should be able to differentiate what are the column names and table names very easily. I'm going to update for Id as well. Let me mention for table name as well. After the logo, we have industry for the industry these are the configurations. Next we have size, rating since the rating column, it is going to allow us to store a decimal value with a precision as three and scale as two, what we can do is inside the Java, we need to make sure to use the BigDecimal as a data type and coming to the precision and scale, we can set them by using these properties that are available under the @Column annotation. Next we have locations founded, using founded we are going to store the year when the company was founded that's why I have mentioned the data type as Integer. Coming to the description, these are the configurations. The reason why I have mentioned @Lob annotation on top of this column is the description column here we have mentioned the data type as text. That means this column can store a lot of text data. We are not restricting to any length like we are doing here by using varchar. So for these kind of columns, we need to mention an extra annotation. The annotation is @Lob. So what is Lob? It represents a Large Object. I hope this is clear to you. Now after the description, we have fields like employees, website, and at last we have audit related fields as well. CreatedAt, it is going to represent the column createdAt. So here the exact column name, we have mentioned it as created_80. The same we need to mention here. It can be in capital letters or small letters. Maybe what I can do is, I can mention it as capital letters. Whenever you have these kind of separators inside your column names, like here, I have an underscore and at these scenarios, you need to follow these camel case strategy. The next word first letter should start with a capital form. That's what I have taken care here. Let me update here as well. It should be createBy, and here it should be updatedAt, and at last it should be updatedBy. Since this createdAt and updatedAt, they are going to store the TIMESTAMP I'm trying to use Java Instant as the data type for these two fields. Under the createdBy and updatedBy, we are going to store the username details who updated or inserted the records. So that's why here I have mentioned String as a data type, the length will be 20 and these updatedAt and updatedBy, they are optional columns. That means they are nullable columns. That's why I didn't mention nullable as false for these two Java fields. So this is how you need to create an Entity class representing your database table. So this is the first basic step that you need to take care whenever you are using JPA framework and ORM framework. So there is a company's table inside my database representing this table. I have created an entity class or Java class. As a next step, we are going to leverage these entity classes while we are interacting with the database by using the Spring Data JPA. The important concept details around the entity and the annotations that we have used, I have mentioned them in this slide for your reference. Don't be under the impression that this is a one time activity for every table. You need to create a corresponding entity class. As I said, inside this lecture, we have followed the manual approach of typing each and every Java field, but there are some plugins available inside the IntelliJ, which are going to make our life easy in generating these entity classes automatically. Once we discuss about the basics of Spring Data JPA, I'm going to explain you all those productivity hacks that you can use.