So far we have created an entity class representing the company's database table. As a next step, we need to write Java methods that we can invoke under various scenarios. For example, if I want to create a new record into the company's data table, I want to invoke a save() method similarly, if I want to read the data from the company's table under such scenarios, I want to use a Java method like read() or find() or fetch(). That's why as a next step, we're going to focus on building the required Java methods to interact with the database table. The beauty of the Spring Data JPA framework is when you are using this framework, you don't have to write any of these methods. Instead, the framework, it is going to provide all of them. As a Java developer, you just have to make few configurations and everything it is going to work out of the box. I know at this point of time, nothing is going to make sense to you, but please bear with me. In few lectures, everything is going to be clear for you when we see a demo. But before we try to make some code changes, we need to understand few important interfaces that are available as part of the Spring Data and Spring Data JPA projects. The first important interface that I want to introduce is, Repository interface. So please make sure you are typing repository and this interface is present in the Spring Data project, not Spring Data JPA. Regardless of which database you are going to use, the same can be confirmed by looking at the package the package is org.springframework.data.repository. So there are many other repository related classes as well. Please make sure you are opening the interface from this package. This is a marker interface. That means this interface is not going to have any abstract methods or default methods. It is a simple empty interface. And this interface is present in the Spring Data project under this package. Here you may have a question, which is why framework developers, they develop these repository interface as a marker interface or as an empty interface, let me answer the same. Instead of Spring Data or Spring Data JPA or any other modules of Spring Data, we are going to have many interfaces exposing various functionalities of database operations. All those interfaces, they have to make sure they are implementing these Repository interface. That means any interface that is providing the functionality around the database operations in the Spring Data or any of its module projects, they are going to act as a child interface for this interface. All these child interfaces, which are implementing these Repository interface, they are going to be considered as Spring Data repository files by the Spring Framework. And during the startup of the application, the framework will take care of generating the implementation code for all these interfaces and the abstract methods that they are going to have. If you try to look for the child interfaces of this interface, you're going to have a good amount of interfaces. We don't have to remember all of them, but we need to at least remember some super important interfaces. Let me explain them what are those? The first one that I want you to explore is CurdRepository. So this is again, an interface present in the Spring Data project itself under the same package. So this interface, it is extending the Repository interface that we have discussed previously. For all the interfaces that you see in the Spring Data or the Spring Data JPA projects, you will see these kind of generics like T and Id. So this T represent what is the entity class name or the database table that you are trying to deal. Right now, we have one such entity class with the name Company. So whenever we want to interact with the company's database table, we need to make sure we are replacing these generic T value with the Company entity class details. I'll explain you how to do that in the next lecture, but I hope you are clear about what is this T represents. Now coming to this ID, this represents what is the data type of your primary key column. If you go to this company, we marked one of the field as primary key column by using this @Id. The data type of this field is long. So this Long, we have to mention under these ID  generic value. I hope you are clear. The same applies for all the interfaces that we are going to discuss. Now let's try to understand what kind of methods that we have inside these interface. If you closely try to understand the name of this interface, it has CRUD. CRUD means create, read, update, and delete. That means this method, it is going to help us in providing the Java methods that we can use to perform the insert operation, read operation, update operation, and delete operation. Create is same as insert operation. If you look for the list of methods inside these interface, all these are the abstract methods and the implementation code for all these abstract methods will be generated dynamically by the spring data framework. So there are find related methods using which we should be able to fetch the records from the database. findAll() means it is going to fetch all the records from the database table. findById() means by using the primary key ID value that we are going to pass to this method, it is going to fetch the single record. In case if there is a possibility of returning multiple rows based upon a ID value, then we need to use these findAllById(). Similarly, there are save related methods, which are going to allow us to store a brand new record inside that database table. To this save() and saveAll() methods, we need to pass the objects of our entity class. The same save methods can be used to for update operation as well. The framework is going to decide whether it has to do the insert operation or the update operation. If there is no record available inside the database for a given object data, then it is going to perform the insert operation. Otherwise, it is going to perform the update operation. We also have delete related methods, which we can use to delete the records from the database. We can delete all the records or a specific record by passing the object of that record. Or we can also delete the records by passing the Id values. Similarly, we have count() method, which is going to give us what are the total number of records available in a database table. There is an existById() method, which is going to help us to understand if there is a record available inside the database for a given ID value. So these are the methods that are provided by the CrudRepository. Now let's try to look for the child interfaces of this CrudRepository. So there is an interface with the name list CrudRepository. This is the next important interface. This interface also expose some methods. For example, if you uncheck these inherited members option, you're going to see the abstract methods that are exposed by these interface alone. Whereas if you check these option, it is going to show you all the methods that are inherited from the super interface as well. So just to understand the methods that are newly introduced by these ListCrudRepository, please make sure you are unchecking this. And these are the three methods that are exposed by this interface. We already saw a findAll() method inside the CrudRepository interface as well. If we go to the CrudRepository here, there is a findAll() method already available, but the written type of these find all method is iterable of T. That means if your database is giving multiple records as an output, it is the responsibility of the developer to iterate them and to convert them into a collection object. If you want to avoid all that painful process, you can use the methods available from the list CrudRepository. The same findAll() method is available inside this interface as well. The methods available inside this interface, the written type for all of them is going to be List of object. So instead of returning the iterable of objects, it is going to directly return you the collection object in the form of List, which means you don't have to manually iterate all the records and copy them into your list. So that's the only advantage of using list CrudRepository compared to the CrudRepository. We have a few other important interfaces as well. Let's try to discuss about them in the next lecture as well.