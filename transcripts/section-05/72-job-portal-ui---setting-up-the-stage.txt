So far, we are testing our backend REST APIs by using Postman as a client application. But we as humans, we will feel good if we see something visually. So whatever response that we are trying to send from our REST API by using this JSON, the same we can see visually on how this data is being displayed on the job portal UI application. As a next step, we'll try to test this REST API by setting up an UI application. If you're not interested in setting up this UI application, then feel free to ignore all the setup that I'm going to do inside this lecture. But still, you can continue watching this lecture so that at least you're going to see how that data that we are sending from our REST APIs, it is going to visually appear in an UI application. But please make sure you are testing the REST APIs at least by using Postman. Under the section5 folder present inside the GitHub repo, you will find a new folder with the name job-portal-ui. Inside this folder, I have all the UI related code. This UI application is built by using React library. We need to open this folder by using an IDE. For UI related projects, it is not recommended to use IntelliJ because it is a heavy IDE. Instead, we should use very lightweight IDE like Visual Studio Code. This IDE is majorly used by all the UI developers, the same we are going to use inside this course as well. From this website, you need to download and set up this IDE. Once you set up, you can open the Visual Studio Code, and this is how it is going to look like. Here, you need to select the Open Folder option. And please make sure you are selecting the folder job-portal-ui, which is present under the section5. As soon as you click on Open, you are going to get this pop-up. Please make sure you are selecting this Yes, I trust the authors. And this is how the UI project is going to look like. You don't have to open any of the source code because this is not a UI focused code. I'm not going to explain any code on the UI. If you are interested to become a Java full stack developer, I have another course where I focused more on React and less on Spring Boot. My recommendation is, once you complete this backend focused Spring Boot course, please pick up my Java full stack course as well. Inside the full stack course, I discussed everything around the UI, like the basics of React library, how to beautify your application by using Tailwind CSS, all the UI related concepts are discussed in depth. Once we open this project using Visual Studio Code, we need to start this application so that we can access this UI application from the browser. In the top, you have a terminal option. Please select the new terminal option and using this terminal, you need to execute some commands. But to execute those commands, you need to make sure you have installed Node.js inside your local system. What is the purpose of Node.js? Using Node.js, we should be able to run JavaScript everywhere. Before Node.js, the JavaScript code can only be executed inside the browser. But right now, since we are building UI applications by using React library, which internally using JavaScript, in order to execute that JavaScript from our Visual Studio Code or from our local system, we should make sure to install the Node.js. So to install the Node.js, you need to come to this website and click on this download option. Here there are multiple options to download Node.js. The most easiest one is, you can try to download the pre-built installer for macOS you can download this installer and try to install the Node.js. Whereas for Windows, you need to select Windows here and the corresponding architecture. Once you download this installer, you can try to install Node.js. So it's going to be very easy process. Once the Node.js is installed, you can confirm whether it is installed properly or not by using a command node -v. When you run this command, you should get a proper output like this. So this gives a confirmation to you that Node.js is installed successfully. Next we need to run a npm command, which is npm install. With this command, what I'm trying to do is, I'm trying to download and install all the packages or the libraries that my UI application is trying to use. So just like how we have pom.xml on the backend side, inside the UI application also, we have a file with the name package.json. Inside this file, we have defined so many dependencies along with the libraries details. So all these packages or the libraries, we need to install inside our local system so that we can start the application. As soon as I execute this command, you'll be able to see a node-modules folder will be created along with all the installed packages. What I can do is, I can get rid of these node-modules and install this command. So all the required packages are getting installed. As you can see, the node-modules directory is created and inside this, we have all the packages and the libraries that are required by our UI application. So once this command is executed, you can try to start the application. To start this react application, you need to run the command npm run dev. As soon as I press enter, my application is going to start at this port number. Let's copy this enter URL. Inside the browser, I'm trying to access the same. As soon as I press enter, my UI application is going to appear. If you scroll down here, we're trying to display some jobs data, but as of now, our UI application is not sending any jobs related data. We are going to update our backend application in the coming lectures. That's why after a few seconds, we're going to display a message saying that there are no jobs to display here. Just below the jobs, we are going to have company's details. So here the company's details are also right now it is showing zero, but we are sending the company's data from the backend. Then why it is not working. Let's try to understand the same by looking at the developer console inside my browser. If you look at the Chrome, if you press F12, it is going to open this developer console. If you go to the console here, if you see there is an error. So what is this error? My UI application, it is trying to communicate with this backend REST API and the path of this REST API is also correct, but the browser, it is stopping the communication due to some cars policy. Here we are clearly seeing that the request to this API from this origin, origin means from this website has been blocked by the browser due to CORS policy. There is a very good reason on why the browser is stopping this communication between two different applications. Our UI application started at this port number, whereas our backend REST API, they are available at this port number. So from the browser perspective, they are two different applications. When two different applications are trying to communicate with each other, the communication is going to be blocked by default by the browser. We're going to fix this problem in the next lecture, but before we try to fix this problem, maybe what we can do is in the next lecture, first, we'll try to understand what is this CORS policy, why browser is blocking the communication and what are the options that we have to allow the communication between the UI and the backend application. I hope you are clear around the steps that we have followed to set up our UI application. I'm going to upload a PDF with all the instructions to this lecture as well. Please download the same and try to follow the same steps. If you want to set up the UI application, whatever error right now that we are facing, we are going to solve it in a few minutes.