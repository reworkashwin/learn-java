Inside this lecture, let's try to understand what is CORS. CORS means Cross Origin Resource Sharing. What is the meaning of origin? Origin is nothing but your domain URL or application URL. When two different applications which are having two different origins. If they are trying to share the resources with each other, then cars come into picture. That's why this name has been given. Cross origin means two different origins. They are trying to share the resources. For example, here I have given a scenario when a CORS issue may arise. A front-end application might be running at this port. Just like our application, the ReactUI application, it is running at this given hostname and port number. And a back-end API might be running at this given port number. So if you see, these two are different origins because the port numbers are different. In these kind of scenarios, the browser, it is going to stop the traffic between both of them due to this CORS policy. Why browser is doing? We know inside the web, the browsers are going to be used to a great extent by every human on this earth. Due to this, it is the responsibility of the browsers to stop any bad actors or hackers who are trying to hack the sessions of the end-users. So that's why whenever browser sees that a communication is trying to establish between two different applications, it is going to stop that communication by default. For example, think like I'm trying to open netflix.com and this is the origin name or domain name for the Netflix application. But behind the scenes, if this Netflix application, if it is trying to communicate with some other application, which is having some domain name like example.com, then this is an indication to the browser that two different applications are trying to communicate with each other. And I should stop that communication by default because this example.com might belong to a hacker who are trying to trick the end-user to steal the session from the netflix.com. We saw this behavior in action as well in the previous lecture, and we saw this kind of message. Most of the people, they think a CORS is a security issue or security attack, but this is not a security attack. Instead, it is that default protection provided by the browsers to stop sharing the data or communication between two different origins. How the browser is going to identify if the origins are different, it is going to consider all these parameters. We know each of the domain URL. It is going to have various parameters like what is the HTTP protocol, whether it is HTTP or HTTPS, what is that domain name? So this domain name is nothing but the host name and similarly a port number. So usually a combination of all these will be the origin. If any of these parameter is different, then browser is going to assume two different origins they are trying to communicate with each other. So here I have an example, a UI application, which is hosted at a domain, domain1.com it is trying to communicate with the backend API, which is deployed at this domain. Here, the protocol is same, but these domain1 and domain2, they are two different domains. And with that, it is a clear indication to the browser that two different origins are trying to communicate with each other. And it is going to stop the communication. In our local scenario here, if you see the HTTP protocol is same, the host name is same, but the port numbers are different. That's why we saw in the previous lecture that the browser is trying to stop the communication between these two applications. So this is the history about cors. But there might be some very valid scenarios where two different applications might want to communicate with each other. For example, a Netflix website may want to communicate with a PayPal website during the payment by the end user. The same applies for any organization as well. I might deploy my UI application at a domain like example.com. Whereas all my backend REST APIs, they might have deployed at some other domain name like example1.com. And I want to allow the traffic between these two origins. Here, I have a very valid scenario. In these kinds of scenarios, we should be able to communicate to the browser from the backend saying that if the request is coming from so-and-so UI application, please allow the traffic. How to communicate that to the browser? Let's try to understand the same. There are multiple options available inside the Spring Boot to fix the CORS policy. The first one is you can use the @CrossOrigin annotation on top of your RestController class or on top of the REST API method itself. Here on top of the REST API method, I have mentioned the @CrossOrigin annotation and using this annotation, I invoked a property origins and to these origins, I have mentioned the origin details of the UI application. With this, what is going to happen is whenever the browser sees the two different origins, they are trying to communicate with each other. The browser, it is going to first send a preflight request to the backend server. So preflight request is a request that is going to be triggered by the browser before the actual request. So what is the actual request that we are trying to send? The UI is trying to invoke the REST API, which is hosted at this path. But before invoking these REST API, the browser, since it detects cross-origins, they are trying to communicate with each other. It is going to send a preflight request to the backend. As a response to this preflight request, the backend should send some confirmation to the browser to allow the traffic from so-and-so domain name. When we are using this annotation, the Spring Boot is going to take care of everything in the backend. It is going to prepare a response to the preflight request that is coming from the browser. And as part of this preflight response, it is going to send all the information that is required to establish the communication between our UI application and the backend application. But the drawback with this approach is if you are mentioning this annotation on top of a REST API method, then these configurations, it is only going to work for this REST API method. In future, if you are trying to add one more REST API method for that, again, you have to define this annotation. If you want to make these configurations applicable for all the REST API methods inside your controller, you can keep the same annotation on top of your controller class. And this is how we are going to enable the cors communication between two different origins. So it is the backend that should always decide from whom that it wants to accept the traffic. Because inside the backend only, we are going to store all the sensitive data. The UI application can only send the request to the backend APIs, but it is the browser and the backend application they are going to decide whether to allow the traffic between the UI and the backend APIs. This option is very simple to use, but there is a drawback with this option. In real applications, we are going to have a good amount of controller classes. It will be a very cumbersome process to mention this annotation on top of all those classes. And in future, if you want to update these origin value to something else, then you need to visit all the controller classes and the REST API methods to update these values. Instead of defining these configurations on top of the controller class or on top of the REST API methods, we should be able to configure the CORS configurations globally for our entire backend application. I'll show you the other options that we have for global CORS configurations. But before that, let me highlight you here instead of mentioning these value, if you mention asterisk, then your backend REST API, it is going to accept the traffic from all kinds of UI applications. Please don't try to use this asterisk symbol for production applications. It is like attracting all the hackers inside the world into your backend application. In case if you want to mention multiple origins, you should be able to do that as well. After these origins, you just have to mention curly braces and within these curly braces, you can define multiple origins and all these origins, you can separate them by using comma. Let me show you the second option. This option is by using a Filter. You have to create a config file inside your Spring Boot application and this file name can be anything. On top of this file, you need to mention @Configuration. Inside this config file, you need to create a bean of type CorsFilter. So here I have created a Java method on top of this method, I have mentioned @Bean annotation. By the time this method executes, it is going to return the object of CorsFilter. The same will be converted as a bean by the Spring framework. But before you try to return the object of CorsFilter, you need to convey all your CORS related requirements by using the object of CorsConfiguration. So once you create the object of this CorsConfiguration, you can invoke various methods like setAllowedOrigins(). Using this method, you are going to define what are the list of origins from where you want to accept the traffic. So here I have defined only one origin detail. But in case if you want to accept from multiple origins, you can define all of them by using comma as a separator or you can also mention asterisk, but mentioning asterisk is not recommended. Just like AllowedOrigins, we can also invoke the setAllowed() methods. Using these allowed methods, we can define for which kind of HTTP methods we want to accept the traffic. Whether we want to accept the traffic only for the GET methods or POST methods. In case if you want to accept for all kind of HTTP methods, you would need to mention asterisk symbol. But before mentioning asterisk symbol, you need to make sure you are invoking these Collections.singletonList(). Using this config, we should be able to define what request headers we want to accept from the UI application. So here I have mentioned I want to accept only a request header with the name content-type. All the remaining headers that my UI application is trying to send, those will be filtered by the browser. So the backend will never receive any other headers other than this header. And similarly, we can also invoke one more method with the name setExposedHeaders(). Using the exposed headers, we should be able to define what headers that we want to expose from backend to UI application. We also have a method with the name setToAllowCredentials(). To this, we can set a value as true. With this configuration, we are telling to the browser to send all the credentials as well to the backend application. When I say credentials, all the cookies that are created by the UI application or any authentication related tokens, all of them will be passed on to the backend application. This way there are multiple methods available inside these configurations. By using all these methods, we should be able to control what kind of traffic that we want to accept. Once this Configuration object is created, the same we need to pass as an input to the method of registerCorsConfiguration(). So where this method is available under these UrlBasedCorsConfigurationSource. So first I have created the object of this class. Using this object, we need to invoke registerCourseConfiguration(). And here I have mentioned the URL pattern as forward slash followed by two asterisk symbols. With this syntax, I'm telling to the framework, please apply these course configuration for all the URLs or the REST API paths available inside my application. And at last, we need to pass the source as an input to the constructor of CorsFilter. Once we return the object of this CorsFilter, the spring boot framework is going to take care of applying these CorsConfigurations globally for your enter backend application. Next, we have an option to configure the CorsConfigurations by using spring security. Spring security is a project inside the spring ecosystem using which we can secure our spring boot applications. We are going to discuss about spring security in the coming sections. Using the same, we are going to enforce authentication and authorization. During the same discussion, I'm going to explain about this option on how to configure cors related configurations by using spring security. The last option that we have is by using web MVC configurer. We already implemented this interface inside our project as part of implementing the API versioning strategy. Inside the same class, this time, you need to override a method with the name addCourseMapping(). And this method, it is going to provide a corsRegistry() as an input. Using this registry, we need to invoke addMapping(). And to this mapping, we can provide the URL pattern or the API path patterns for which these cors configuration has to be applied. Here, I have mentioned the mapping as forward slash API slash two asterisk symbols and with this, I'm telling for all the rest APIs, which are having a prefix as API, for all of them, apply these cors configuration. After the addMapping(), we can invoke allowedOrigins(), allowedMethods(), allowedHeaders(), exposedHeaders(), allowedCredentials() methods to provide our cors related configurations. So here I have mentioned a single domain name. Similarly, you can define any number of domain names. The same applies for HTTP methods, allowedHeaders() and exposedHeaders(). We can also invoke a maxAge() method. To this method, we can pass a numeric value. Here I'm passing 3600. This 3600 means the framework, it is going to consider it as 3600 seconds. That means 60 minutes. So for one hour, the browser, it is going to cache these configurations and it is not going to make any preflight requests. If a client application is invoking the same backend REST API. After the one hour, the browser, it is going to make a preflight request to fetch these configurations again. By using these max age, we are going to have some performance improvements. Instead of browser asking for each and every request, it is a good option to cache these cors configurations on the browser side for a limited period of time, like for one hour or for one day, based upon the business requirements. These are the options that we have. We're going to implement one of them inside the next lecture. When we make these cors configurations inside our Spring Boot application, by using any of these options, what is going to happen behind the scenes is the Spring Boot framework. It is going to send some headers as part of the preflight to request a response. So what are those response headers? It is going to send these kinds of headers under these headers it is going to provide all the values that we have configured, like what are the allowedOrigins(), what are the allowedMethods() ? So by looking at these response headers, the browser will decide whether it has to allow the traffic or not. At last, I highlighted when not to use the cors configurations. If the communication is happening by using a same domain name, then there is no cors is required because they both belongs to the same origin. So here the frontend is deployed at the path mywebsite.com, whereas the backend is deployed with a path as mywebsite.com/api. If you see the origin is same for the frontend and backend, that's why there is no cors is required in this kind of scenarios. We didn't see any course error while we are testing with the postman. Do you know why? Because postman is a client application, which is going to send the traffic directly by using HTTP protocol. The traffic is not getting initiated from another domain when we are using the postman. That's why we didn't face that problem while we are testing with the postman. As a backend developer, it is very important for you to understand about these cors error. Otherwise, in real applications, you're always going to have a fighting with the UI developers. The UI developers, they will say, I'm always getting an error and you will be saying, I'm getting a proper response when I test my REST API using postman. But the real culprit here is the cors error. Unless both UI developer and the backend developer, they have a knowledge around these cors , the problem won't get resolved. It is always the responsibility of the backend developer to make these cors configurations so that the traffic from the UI will be allowed. In the next lecture, let's try to implement course related configurations inside our backend application.