Inside this lecture, let's try to implement AOP by using the Before Advice. Let me open a class with the name AuthController. As of now, inside this controller, if you look at the register method, during the registration process, this method is going to be invoked. Before we try to save the user details into the database, we are performing some validations. Like first, we are checking whether the password is compromised or not. If not, then we're also checking if there is an existing user available for a given email or mobile number. If both these validations passes, then only we are saving the user into the database. Otherwise, we are simply returning bad request as an response. This is a classic example where we can use before Advice. With all these kinds of validations logic, my method business logic became too much clumsy. In my scenario, I just have to perform two validations. But in real enterprise applications, you may want to perform 10 different validations. Doing all of them as part of your actual business logic may make your Java method look clumsy. So that's why what I'll do is, I will move all these validations logic into a aspect. Let me go to the aspects package and here I will create a new Java class with the name RegisterValidationAspect on top of these class as usual, we need to mention @Aspect annotation followed by @Component. I'm also going to mention @RequiredArgsConstructor to this class I will inject a couple of dependencies. The first one is compromisePasswordChecker followed by jobPortalUserRepository. Let me also mention @Slf4j annotation here. Now inside this Java class, I'm going to define a Java method, which is going to have my aspect logic. You can get this Java code from the GitHub repo as well. So the method name is validateBeforeRequest. On top of this method, I have mentioned @Before annotation and to this annotation, this time I'm passing the execution as an input by using the String literals. So by using the String literals, I can mention my String data in multiple lines as well. So this is going to improve the readability of my execution criteria. So this aspect logic, I want to be intercepted only for a method with the name registerUser() under this class. Since I want this aspect to be intercepted only for a single method, I need to mention its entire details here, while using Before Advice you need to accept that join point as an input using the join point. I'm trying to understand what are the input arguments to this method we know we are going to receive the input data in the form of RegisterRequestDto. The same I'm trying to read by using this args. Here I have done the casting as well to the RegisterRequestDto. Inside this request variable, we are going to have the username and the password provided by the end user. So first I'm trying to log a statement saying that Validating user registration request. Next I have defined errors map as part of the very first validation by using the compromisedPasswordChecker. . I'm performing the validation. If the password is a weak password, I'm going to write these error into the errors hash map. Otherwise, I will move on to the existing user check where I'm going to invoke this method to identify whether there is an existing record inside the database. If yes, then I will populate the errors hash map with these details and towards the end, I'm checking if the errors are not empty. Then I'm logging a warning log missing that registration validation failed and throwing an exception with the name RegistrationValidationException. The reason why I want to throw this exception is whenever these validations failed, I don't want the actual method to be invoked. If we throw the exception from the aspect logic, then the aspect will never invoke the actual Java method. In case if these two validations are not satisfied, then these exceptions we are not going to throw. We are going to simply log this message, Registration validation passed. And with that, the actual method will be invoked. I need to create a custom exception with this name. Let me go to the exceptions package and here I will create a new exception with this name this exception, it is going to extend a runtime exception anyone who are invoking this exception, they need to pass an hash map with the errors details. For these errors variable, we also have a getter() method. Next I'll go to the GlobalExceptionHandler. As of now, we don't have any exception logic that handles our RegistrationValidationException. With this kind of setup, we will always throw the internal server error. Instead of that, I want to throw 400 bad requests. So that's why what I'll do is, I will introduce a new method inside this class. Let me paste a new method here. You can get the same from GitHub repo as well. This method, it is going to handle RegistrationValidationException. Whenever this exception is arising inside my application, we are going to simply return the response with the status as bad request and inside the body, we are going to send all the errors that we are receiving by invoking ex.getErrors() method. Now I can go to the AuthController and here I can get rid of all these validation logic. That is going to make my Java method very simple and clean as well. Let me save all these changes do the build once a build is completed from the postman, before I try to invoke the register API. I need to first get the CSRF Token because register API uses HTTP POST method. Let me get the CSRF Token this is my CSRF Token. I need to copy the same and now I'll go to the register API here under the headers against to these X-XSRF-TOKEN. I need to mention the header value and under the cookies also, I need to make sure I have the same value. I'll try to invoke the REST API with some weak password, like 123456. If I click on the send button, you will be able to see I'm getting the 400 bad request with the error details as well. Inside the console, we are also seeing logs from the Aspect class saying that Registration validation failed. For a reason, choose a strong password and mobile number is already registered. Email is already registered. This gives a confirmation to us right now the validation part is being taken care by the Aspect oriented programming. Let's try to provide some valid values and see if everything is working, email I will give it as test-aop@gmail.com and I'll give some random mobile number coming to the password I need to give a strong password like eazybytes@1803 to this if I click on the send button this time, the registration is successful because all the validations are passed that I have defined using the Aspect logic. Guys, please remember whatever Aspect logic that you are writing by using the Before Advice for such methods, you don't have to return anything because the actual business method that are going to be invoked after this Aspect logic, they're not going to accept any input from your Aspect logic. By using this Before Advice we can only execute some Aspect logic in case if you want, we can also stop the invocation of that target method like we are doing by using these throws exception. Always you need to throw an exception from your before Aspect if you want to stop the execution of the target method. This is the sample code snippet that we have discussed to implement the Before Advice by using the Spring AOP. Before Advice is a perfect match when you want to perform some input validations or when you want to execute some preconditions, security checks, blocking, invalid requests and so on. For example, if you have a requirement to accept the request only from a given country location or from a given IP address, then you can write a before AOP which is going to perform those validations and to this before advice this time under the execution, you need to provide a syntax such a way that it matches all your Java methods. And if the request is coming from a given country or from a given IP address, you can simply throw an exception due to which your target method will never get invoked. But please make sure whatever exception that you are throwing from your Before Aspect, you need to catch that by using some Global Handling Mechanism so that your client applications will get some kind of response as an output.