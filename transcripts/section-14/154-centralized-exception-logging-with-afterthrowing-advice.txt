Let's see the demo of AfterThrowingAdvice inside this lecture. Whenever we are defining an aspect logic with this AfterThrowingAdvice, it is going to be executed only when the target method is throwing some exception. Since you are already familiar with the process of creating the aspect logic, behind the scenes I created this class, the class name is ExceptionAuditAspect. On top of this class, I have mentioned these three annotations, followed by I have defined a method. Just above this method, I have mentioned the annotation @AfterThrowing. To this annotation, we need to provide two kinds of information. The first one is, by using this point cut we need to provide the execution pattern syntax. Here I am trying to intercept all my methods under the job portal and its sub packages. Next, after the point cut we need to invoke the throwing and to this throwing, we need to assign a value. For example, here I have assigned ex. The same ex value I need to use as a variable inside my method input parameters. So using the ex, I'm trying to accept exception details. We know the aspect logic is going to be invoked only when the target method is throwing an exception. So that exception details, which are being thrown by our target method, we need to catch as an input inside our aspect method. The method name is LogAfterException. To this method, I'm passing two method input parameters like join point and exception. First using the join point, I'm trying to get the method name and the method arguments followed by I'm trying to log all these details. All these logs, they are using the error log severity. By using these first log message, I'm trying to log a message saying that exception occurred inside a method with so-and-so method name followed by these are the method arguments. Now coming to the exception details, I'm trying to log them by using these ex variable. Using the same, I'm trying to understand what is the type of the exception that is thrown by the target method and towards the end, I'm also trying to exception message details by invoking these ex.getMessage() method. If required, based upon your business requirements, you can also define many other logic like sending metrics, pushing audit events or triggering alerts to the operations team. So it's up to you what kind of logic that you want to write. I'll save the changes. Now I'll go to the CompanyController and inside this, we have a REST API, which is going to return the list of company details. What I'll do is, from this method I'll try to throw an runtime exception with the message as AOP testing. So with this, what is going to happen whenever I'm invoking this REST API, this method, it is going to throw this runtime exception. Since this method is throwing an exception, whatever aspect logic that we have defined by using the AfterThrowing will be executed by the framework. After this aspect is executed, whatever logic that we have written inside the GlobalExceptionHandler to handle the runtime exceptions will be executed. So let me save the changes, do the build once the build is completed, from the postman, I will try to invoke this REST API. As soon as I click on this send button, I'm going to get the response from the GlobalExceptionHandler with a status as 500 and with this response, now let's go and check the logs inside the console logs we are seeing the log statements that we logged by using the class ExceptionAuditAspect. So this is the very first log statement followed by the arguments are empty. Next the exception type is runtime exception and the exception message is AOP testing. I hope you are clear with this demo. So let me go to the CompanyController and revert all these changes so that we're going to get a successful response. So this time when I invoke this REST API, we are going to get a valid response. And with that, this aspect logic is not going to be executed. Let me go to the console and clean all the logs. Now if I invoke the same REST API, this time I got the successful response and inside the log statements, I don't see any error logs because the aspect logic is never invoked. Whatever example that we have discussed around the after throwing, I have documented the same inside this slide for your reference.