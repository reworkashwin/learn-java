To this @Around annotation, I'm going to provide the point cut requirements. First inside the double quotes, I need to type execution and after the execution, we need to mention parentheses. Within these parentheses, we need to provide our requirements. First, I will mention asterisk because I'm fine with any return type value of the targeted method. I'm not mentioning any modifiers pattern because it is optional. After this first asterisk, I'm going to mention the declaring type of pattern by mentioning my package name com.eazybytes.jobportal. And this time, I'm going to mention two dots followed by asterisk symbol. So with this, I'm telling to the framework that I want you to intercept each and every method available inside the job portal package and its sub packages as well. Next, I need to mention the name pattern as well, coming to the name pattern, I will mention asterisk, with this asterisk, I'm telling I'm fine with any kind of method name and towards the end, we need to mention input parentheses. And inside these parentheses, I'm going to mention two dots. With these two dots, I'm telling to the framework that I'm fine with any number of input parameters of the target method. I know it's a complex syntax, but you will get used to this when you start implementing at many places. Let me save these changes to the build, once the build is completed, I will try to restart my application. So with these, now what is going to happen is, each of the Java method that is being invoked inside my application, all of them they are going to be intercepted. And while those methods are being intercepted before those methods get invoked, this logic will be executed, followed by the actual method will be executed and towards the end, this aspect logic will be executed. My application started successfully. I will clear this console, let's go to the logback.xml here. We need to make sure under the com.eazybytes.jobportal, we need to mention the level as INFO. Because inside our aspect logic, we are using info, that's why here we should mention either trace, debug, info, but not error and warn. Let me save these changes do the build. I will clean the console from the postman, I will try to invoke these company's public path, and to this, I got the response but inside the console, if you see first I have the log statement from the logging and performance aspect class. The log statement says Entering method, CompanyController.getAllCompanies(). Next, we are trying to display the input arguments since this method does not have any arguments, we are displaying empty data. Next, it also intercepted CompanyServiceIMPL method as well. That's why here we are seeing Entering method CompanyService.IMPL. And for that also arguments is empty, followed by next, it printed the statement saying that Entering method list to crud Repository.findAll(). And after the JPA method is executed towards the end, we should be able to see some other log statements as well. So this is the end statement from the CompanyServiceIMPL method is executed successfully. It took a total 272 milliseconds to process. And this is the statement related to the CompanyController.getAllCompanies(). And it took around 277 milliseconds. So are you seeing the power of AOP here, without me writing any of the log related or performance monitoring logic inside my Java method, still I'm able to execute my cross-cutting logic. In future, if someone asks you to change this info to debug, then it's very easy you just have to change it in a single place. With that, the change and the testing efforts also will become very less. So with these changes, now, if I try to test the same REST API, this time the statements are not going to be logged because we have configured debug inside the aspect. So to see these statements, we need to go to the log back.xml,and here we need to mention debug, but I'll not mention debug. I will continue to use this info. I don't want to clutter my console logs with all these log statements. You can enable these statements only when required, for example, while trying to identify an issue. The example and the syntax that we have used to implement the Around Advice by using the Spring AOP, I have documented them in this slide for your reference. Please be aware when you are using the Around interface, you need to make sure to catch the result from the proceed() method and return the same result from your aspect method. And your aspect method also should accept this proceeding joint point as an input, otherwise you can't invoke these proceed() method. With the Around, you also have flexibility to not invoke the actual Java method under certain scenarios. Based upon your business requirements, you can always play with these aspect logic.