We are trying to implement some AOP logic, which is going to take care of logging and performance monitoring inside my backend application. As part of this AOP logic, we have taken care of Aspect, which is the WHAT part and the Advice as well, which is the WHEN part. As a next step, we need to understand which part as well. By using these WHICH part, we are going to tell to the Spring AOP, which methods inside my application that the framework needs to intercept and execute the given aspect logic. This WHICH part we are going to define by using point cut. Few of the developers, they consider point cut answers where the point cut logic needs to be executed. So which are where both of them, they refer to the point cut. Now let's try to understand what is a point cut. A point cut is a rule that tells Spring AOP where an aspect should run or which Java method should be considered to intercept and execute the aspect logic. In simple words, by using this point cut, we tell to the Spring framework run this advice ONLY for these methods. So you can think of it like  Afilter or A selector or A targeting rule. The most common way of defining this point cut is by using the execution. The execution designator is the most frequently used and gives you the highest level of control while defining the point cut requirements. Why this name execution given? We usually say intercept this method execution. That's why this name execution came. This execution designator, it follows a specific pattern or syntax that we need to aware. Right now we are using the annotation like @Around, @Before and @After to define the Advice while defining these @Advice annotations to these annotations we provide this execution syntax as an input. This execution, it expects various values. First modifiers pattern. We know by using these execution, we are trying to intercept the methods. The same method criteria details we need to provide by using this execution. All of our methods are going to have modifiers like public, private, protected. Those modified details we need to mention as part of the first criteria. Mentioning these modifiers pattern is completely optional. Here also I have mentioned question mark ? Question mark means it is an optional parameter or optional value. In case if you want to intercept only public methods, you just have to mention public here. If you're not providing any modifier pattern, since it is an optional, then all type of methods that are going to be intercepted. After the modifies pattern, we need to mention what is our return type pattern. This is a mandatory input. We know all our methods that are going to return output. It can be a String or it can be an object or it can be void as well. So those requirements we need to mention by using this pattern. In case if you are mentioning String here, then the methods that are returning only the String as an output will be intercepted to execute your advice or aspect logic. In case if you are mentioning asterisk symbol, then any kind of return value is acceptable. As part of the third input parameter, we need to mention various values like declaring type pattern, name pattern and param pattern. Using these declaring type pattern, we need to provide the package details. If you don't want to mention the package details, it's completely optional. That's why here I have mentioned question mark?, but the name pattern of the method is mandatory. For example, here, if you mentioned set asterisk, then all the Java methods that are starting with the set will be considered for the interception by the spring AOP. If you are mentioning simply asterisk, then all Java methods are going to be invoked for the param. Also, we can define various patterns. For example, I can mention int comma int. That means only the methods that are accepting two input int parameters needs to be intercepted. Similarly, I can use various other patterns as well. I'll show you some examples, which is going to make more sense to you. So this is the typical method signature. And after the param, sometimes few of the methods, they also going to throw exceptions. If you want to construct that type of exceptions that are being thrown from a given method, such requirements you can define by using these throws pattern, but mentioning these throws pattern is completely optional. If you're not providing these optional input parameter, then all kinds of methods, regardless whether they're throwing or not throwing will be intercepted. So this is the tabular representation of the concept that we have discussed. So modifies pattern is optional, but whenever you're defining these components, you can provide valid values like public or private. You don't have to mention asterisk instead of asterisk you can simply go with the option of not mentioning any value under this component. Coming to the return type pattern, it is a required component here also you can mention values like string or asterisk, asterisk means any return type. Under that declaring type pattern, which is an optional, we can mention the package name like com.app.service. So with this, we are telling all the methods that are defined under this package needs to be intercepted and whatever conditions that we are mentioning with these execution, they are going to be applied by using the and operator. It's not like or operator, the given target method has to satisfy all these pattern component configurations. Next, by using the name pattern, we can give the name of the Java method. If we are using asterisk that matches any method name. Coming to the param pattern, which is the required again to the param pattern if we are mentioning two dots, then it is an indication that you are fine with any number of type. This any number includes 0 as well. Whereas if you are mentioning simple parentheses without any dots, then it is a indication to the framework that you are looking to intercept the methods, which are not accepting any input parameters. You can also provide like asterisk comma String with this asterisk comma string you are telling that I'm fine with any data type for the first input parameter, but coming to the second input parameter, it has to be a String data type. So this way you have complete flexibility coming to the throws pattern. It's optional. When we want to use this pattern, we can mention something like throwable with that all the methods that are throwing a exception will be matched to this condition. Inside the next slide. I have given some common wildcards. Asterisk means any return type or any method name. We can use plus as well. For example, here under the name pattern, if I mentioned the class name as AccountService+ then it is an indication to the framework that it has to intercept the methods inside the account service and all its kids are child classes as well. If you are using two dots, then it matches zero or more elements. We already discussed about it. Coming to the two dots. If you are using these two dots under the parameter section, it matches any number of input arguments, including zero. Whereas if you are using the same two dots under the package section, it matches the package and all its sub packages. For example, here I have mentioned com.app.service..*. So with this, I'm telling to the framework, please intercept all the methods available inside the service package, along with the sub packages as well. So here I have given some examples as well. If your goal is to intercept any public method, then you just have to mention execution followed by public. After the public, you need to provide the return type pattern for the return type pattern I'm mentioning the asterisk after the return type pattern, since it is a declaring pattern is optional. That's why I have ignored these for the name pattern, I again mentioned one more asterisk, coming to the input parameters I have mentioned two dots that means I'm fine to accept any input parameters. Next if you are looking to intercept the methods that are starting with the set, this is the execution expression that you need to consider. So first you need to mention the asterisk indicating that you are fine for any kind of return type pattern, followed by you need to mention the method name pattern, the declaration type pattern is optional. That's why you can ignore it and directly you can mention set asterisk followed by two dots within the parentheses. In case if you are looking to intercept the methods in a specific interface, it's very easy. You just have to mention this syntax com.svc.AccountService.asterisk. Here I have not used two dots. I have used only a single dot. That means all the methods inside these interface or inside this class will be intercepted. But the Java method name, I have used asterisk here that's why all the methods will be considered. If you are looking to intercept methods in a package and sub packages, then it's very easy. You just have to first mention what is the return type return type is asterisk. That means you're fine with any return type followed by declaring pattern com.svc.,. After the two dots, I'm also using asterisk. So the two dots asterisk, it matches the service package and all sub packages as well, including the packages like svc.impl. And I'm fine with any Java method name. That's why I have mentioned asterisk again here and towards the end, we need to mention two dots inside the parentheses as we're not looking to enforce any restrictions around the input parameters. So this is the way on how to define point cut whenever you want to implement AOP program. So now by using this logic, let's try to implement some point cut logic for the Around Advice that we have developed so far.