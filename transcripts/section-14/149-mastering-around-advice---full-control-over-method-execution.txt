While implementing AOP inside a Spring Boot backend application, we can use any of these Advice Types. There is an Advice with the name Before. We can configure this advice by using this @Before annotation. If you are using this annotation on top of the aspect logic that we are defining, then it is an indication to the Spring Framework, please invoke the aspect logic before the target method starts. Just opposite to before, we also have After. When we are using this advice type, it is an indication to the framework to invoke the aspect logic only once the target method finishes its execution. In this scenario, with the after advice, the aspect logic will be executed regardless of target method is successfully executed or not. In case if you want to execute your aspect logic only if the target method logic is executed successfully, then you need to use AfterReturning Advice with these advice type the aspect logic is going to be executed only after the target method returns successfully. No exception should happen, whereas with the AfterThrowing advice type, the aspect logic will be executed only if the target method throws an exception. So these are the Before and After. We also have Around advice type. This is a powerful advice type. By using this advice type, we are going to wrap the actual target method execution around the aspect. So first you're going to execute some aspect to logic, followed by you're going to execute the actual method. After this actual method is executed, you're going to again execute some aspect logic. So if you see the actual method is surrounded by the aspect logic. So just before the actual method is invoked and after the method is processed, the aspect logic is going to be executed. That's why we call this advice type as Around. So this Around advice type, it is going to give full control to intercept to modify or suppress that target call. For example, let's take a scenario. There'll be some aspect logic 1 followed by the actual method invocation, followed by aspect logic 2. If one of the criteria that you have written inside this aspect1 is not met, you can suppress that target method invocation. With that, this actual method and aspect to logic will never get invoked. If you want, you can only control the actual target method and still execute the aspect to logic as well. I know you may have too much confusion here. Maybe let's take a scenario and try to implement the same by using AOP and using the Around advice type. In the same process, I'll also explain you on how to define point cut. So by using point cut, we are going to define WHICH methods of my backend application needs to be intercepted. Once we are clear with the point cut aspect and advice type implementation, we can also explore other advice types as well. I will get rid of all the log statements that we have returned because we are going to implement all these log statements by using AOP programming. I'll also remove the @Slf4J annotation. Let me go to the ServiceImpl. Here also I'll get rid of these two lines followed by I will remove the annotation on top of the class. Let me close all these files and now inside my project, I'm going to create a new package, the package name will be aspects. Inside this aspects package, I will define all the AOP logic. Before you try to implement AOP programming inside your backend application, you need to make sure that the AOP dependency is already present inside your application. If you look for aspect, you will see an annotation interface is available. If this is not present, that means you don't have AOP dependency added. You'll also see some other classes like AspectJAfterAdvice, Around Advice. All these classes, they are present under the library with the name Spring-AOP. Inside our backend application, as of now, we didn't add any AOP related dependency, but still we are able to see the AOP related classes and interfaces inside our classpath. How that is possible? It is due to the few of the starter projects that we are using. We are using a starter project with the name Spring Boot Starter Data JPA. If you click on this artifact ID, it is going to show its pom.xml. Inside this pom.xml, you will see other dependencies as well. Let's open the Spring Boot Data JPA artifact ID. Inside this pom.xml, you can find a dependency is added with the name spring-aspects. Anytime inside Spring Boot, if there is a magic happening, you can always assume it is due to AOP. When we are adopting Spring Data JPA, we have developed various methods inside our repository interfaces. For example, if you go to the RoleRepository, there is a method with the name findRoleByName(). But for this method, there is no implementation logic. But when we are invoking this method from our controller layer, some query is getting executed and we are getting the expected result as an output. How this is all happening? That is due to the magic of AOP. The Spring framework, it's going to have a proxy object. That proxy object, it is going to intercept the calls to this method. So whenever someone is invoking this method behind the scenes, it will take care of generating the SQL statement based upon the method name. It will take care of executing it, extracting the results from the database and assigning them to a POJO object called role. I hope you are clear. Now, since a Spring AOP library is already present inside our project, we can directly start developing some aspect related logic. First, I will try to define an aspect logic. So by using aspect logic, we're trying to answer the question, what ? I will define, what cross-cutting logic that I want to execute as part of the aspect. The class name, I'll give it as logging and performance aspect. The name of the class can be anything. On top of this class, I'm going to mention @Aspect. With this, I'm telling to the Spring framework or Spring Boot framework that this class has some aspect logic. Let me also mention @Component annotation followed by @Slf4j for the logging purpose. Inside this class, I will define a Java method. You can get this method from the GitHub repo as well. On top of this method, I will mention the annotation @Around. With this @Around annotation, I'm telling to the Spring framework to execute this aspect logic that I have defined around a given Java method. As of now, we are getting a compilation logic against to this @Around annotation, because to this @Around annotation, we should also give the point cut requirements. So we are telling around a Java method, but we are not telling which of the Java methods that the Spring framework has to consider to execute this aspect logic. My application may have hundreds of Java methods, whether I want this logic to be executed for all of them or a given set of methods, that kind of point cut information, I need to provide to this @Around annotation as an input. We'll give that input in a few minutes. But before that, let's try to understand what logic I have written inside this aspect logic. The method name is logAndMeasureExecutionTime(). I want to achieve two targets. The first one is to implement the logging followed by understanding on how much time a given method has taken to execute its business logic. To this method, we are providing an input of type proceeding join point. Whenever we are trying to implement the Around advice, we need to make sure to accept these proceeding join point as an input to our aspect method. If you open this interface, you will see the definition says proceeding join point exposes the proceed method in order to support the Around advice in AOP programming. Now let's try to understand what logic I have written inside this method. At the very starting line, I'm trying to understand what is the current time by invoking System.currentTimeMillis(). I'm going to have some number which I'm trying to catch with the variable startTime. Followed by from this join point, we can invoke the getSignature() method. After the getSignature(), I'm invoking the toShortString() method, which is going to give me the method name that right now being intercepted by these given aspect logic. Similarly, we can also fetch what are the arguments or the input parameters that are provided by the invoking party when they are invoking the actual Java method by invoking the joinPoint.getArgs(). Since a Java method can have zero input arguments or one or two or any number of arguments, we're trying to catch them by using object of array. The reason why I'm using object here is, I don't know whether my target method is accepting an Int variable or String variable or person data type. Since I can't predict that, I'm trying to catch by using this object. Next I have added a log statement saying that entering method and to this String format, I'm providing the method name. This time I'm invoking a info method that accept a String formatted String and an argument object. Next, after the first statement, I'm also trying to print what are the list of arguments that are received to a given targeted method. In case if you're not comfortable printing the arguments onto the console, you can get rid of this log statement because few projects, they don't encourage developers in printing method input parameters onto the console for the security reasons. For example, if there is a scenario where your method receives sensitive information as an input, like SSN, phone number, email details, password details. So such details, we don't want to log, for those kinds of scenarios, you can get rid of these log statement for now, I'll go with this one. After the logging and identifying the startTime, I want to execute the actual Java method. So to invoke the actual Java method that is being intercepted by this current aspect, I need to invoke the proceed() method by using the join point. With this proceed() method, I'm telling to the proxy object that is exposed by the AOP to go ahead and invoke the actual Java method. So whatever Java method is providing as a response, the same I'm trying to catch by using this result. Here also I'm using object because I'm not sure what is my target Java method is going to return. It can be a String, it can be a void, it can be a person data type, can be list of jobs, it can be list of companies. Since we can't predict for each and every Java method available inside our application, it's better to accept as an object. After the actual method is executed, I'm again trying to identify what is the currentTime from this currentTime, I'm subtracting the startTime. And that is going to give me the total executionTime of my actual Java business logic. So this total executionTime, I'm trying to log by using this log statement executionTime in so-and-so milliseconds. I'm also logging a statement saying that method executed successfully, and this is the method name. And towards the end, I need to make sure I'm returning the result from this point cut. If I'm not reaching the result that is being returned by the actual business method, then whoever is trying to invoke my actual Java method, they are not going to get any response and that will defeat the whole purpose of programming. I hope you are clear with the discussion that we have done so far. So far, we have answered what and where. So by using this aspect logic, we have defined what logic needs to be executed as part of the AOP programming. And we also answered where as well, by using these @Around annotation, we're telling to the framework that my interceptor logic should be executed around the actual Java method like before and after. So if you see here, we've wrapped the actual method invocation around our aspect logic. This is the before aspect logic. And this is the after aspect logic. The only next question that we need to answer to the framework is, which of the methods that needs to be intercepted inside my Java backend application. To answer the which part of the AOP programming, we need to understand a syntax.