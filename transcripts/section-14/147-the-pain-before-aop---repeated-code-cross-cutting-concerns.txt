As of now, we just explored on how to implement logging inside a Spring Boot application, but we actually did not implement any serious logging inside our business logic. We just created a dummy controller with the name LoggingController, but we didn't implement any logging related logic inside any of our actual business logic methods. Let's try to do the same in this new section. Let's go to one of the controller with the name CompanyController or ContactController. As of now, inside our application, what is happening, whenever a client application is invoking our REST API, the control will come into the controller method. From here, it will go to the repository layer and we are going to get the expected output. Ours is a very simple application, but in real enterprise applications, a single action on the client side may result into invoking hundreds of Java methods on the backend side. When this kind of complex setup is present inside a project, it is going to be very tough for the developers to understand how the client request traveled inside the backend code. If an error is happening or exception occurred, developers can't guess under which method the issue is, because there is no clues without logs. That's why to handle these kinds of issues, usually developers, what they're going to do is, at the entry of each method, they're going to put a log statement, something like entering so-and-so method. And similarly, when they're done with the Java method logic, towards the end, they're also going to have one more log statement saying that exiting from the so-and-so method. Let me show you what do I mean here. Inside these CompanyController, I'll mention annotation @Slf4j, with that, we're going to have a log variable with the name log. Here at the very first line, I just mentioned a log statement saying that log.info with a message as entering method method name, the method name is getAllCompanies(). In the similar lines, I can also mention one more log statement towards the end, just above the return statement saying that exiting method getAllCompanies(). The same I need to repeat in the service method as well. Let me go to the service method, getAllCompanies(). Inside this class, first, I need to mention @Slf4j followed by, I'm going to mention the method name here, entering method getAllCompanies(). Let me do the same, just above the return statement here, I'm going to mention exiting method getAllCompanies(). With these loggers, what is going to happen? Anytime whenever someone is invoking my REST API, these logs, they are going to be printed. If the info log level is configured, let's imagine there is a scenario where there is an exception happening inside the service class. So while invoking this findAll(), an exception occurred. And with that, whatever log statement that we have towards the end, it is not going to be printed. When the developer is checking the log statements, he is able to see entering method log statement, but he didn't see exiting method log statement. So that gives a clue to the developer. There is something happened in between these two log statements, and that's how they're going to approach the problem. Here we just added method related log statement, but in reality, developers, they also going to add a lot, many log statements about printing the variable values at runtime and understanding which conditional logic is being executed at runtime. Everything they're going to understand by looking at the log statements. Off course, inside the local system, they can debug the code line by line, but sometimes few of the production issues, they will not be reproducible inside the local system. There'll be very tough defects, which will occur only inside the production environment. In this kind of scenarios, these log statements are going to be super helpful for us. Let me save these changes do the build, once the build is completed, I will go to the logback.xml. So here, as of now, we have configured error for the package com.eazybytes.jobportal. And with that, whatever log statements that we have here, they're not going to display. That's why here, what I'll do is, I'll update this to INFO. I'll save this and do the build once the build is completed, I will try to restart my application. By the way, just a reminder, since we are into the new section, I copied all the previous section code into this new section folder. On top of the same, I'm trying to make all these code changes. Now I'll go ahead and try to invoke this public companies-rest api. Inside the postman, under the security folder, we have a request with the name GetAllCompanies_Public. . If I click on this, I'm going to get the expected output. Let's also check the console as well. Inside the console logs, you'll be able to see, I have the statements like exiting method getAllCompanies(). And this log statement is happening from the CompanyController class. So similarly, if you can scroll up, you will see other statements as well. This is a statement from CompanyServiceImpl. I hope you're clear on the importance of log statement inside the business logic. But with this very basic approach, we have a problem. The problem is, we need to add these log statements in each and every java method that we have inside our application. Ours is a very simple application. It has at a max of 100 java methods. But real enterprise application projects, they are going to have thousands and thousands of java methods. Maintaining these log statements in all those methods is a very complex process and in future, if someone comes and asks to change this info to debug or to some other logging mechanism, it is going to be very tough revisiting all the java methods and making the updates. And moreover, this logging is not the actual business logic. Why should we put all the non-business logic into the java methods? This is where we are going to explore one of the beautiful concepts of Spring Ecosystem. The concept is AOP. AOP means ASPECT-ORIENTED PROGRAMMING.. By using this AOP, we can add superpowers to our code without mixing our business logic with logging, performance monitoring, and so on. From the next lecture, let's focus on understanding this AOP. This is a mandatory skill for any Spring backend developer. We're going to spend a good amount of time understanding this AOP program. By the end of this section, you're going to become an expert around the AOP. I hope you are excited.