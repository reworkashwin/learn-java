By using @Query annotation, we can provide our own custom queries to the Spring Data JPA and Hibernate framework. This annotation we need to define on top of an abstract method inside the Repository interface. So here you see, I have a method with the name findAllWithJobsByStatus(). On top of this method, I have mentioned the @Query annotation. To this @Query annotation, we need to provide the query that needs to be executed whenever someone is invoking this abstract method. So whenever a method has a @Query annotation on top of it, then Spring Data JPA is not going to consider the name of the method, which means that derived method name concept is not going to be implemented for this method by the Spring Data JPA. Because the framework identified that there is a @Query annotation on top of this method and it is going to execute this query simply and return the results. To this @Query annotation, we can provide the query in two different formats. The first one is by using the JPQL and the other one is by using the native query. I'm going to explain you the difference between them in a few seconds, but before that, let's try to understand why @Query annotation is the right choice for complex requirements. By using @Query annotation, we can communicate what exactly we want to the framework. There won't be any compromises. We can decide to use which join, like I can decide to use normal JOIN or LEFT JOIN or RIGHT JOIN. We can decide to use fetch strategy or not. We can decide on the filtering conditions as well. These filtering conditions can belong to any table. It can be parent or child table, regardless of where the filtering columns are available we should be able to apply our filtering conditions by using the @Query annotation. And whenever we are using the @query annotation, it is going to solve the N+1 problem completely. So here, if you see inside the query, along with the join, I'm also using the fetch keyword. With this fetch, I'm telling to the Spring Data JPA and the Hibernate framework, please fetch the child records as well while you are trying to fetch the parent records, which means the framework, it is going to fetch the both parent and the child records by using the same query. There won't be any extra SQL calls. Always our intent is going to be very clear to the framework and to other developers as well. When we are defining our requirements by using the @Query annotation, they don't have to do any guessing work from the method names like we have to do when we are using derived query methods. But does that mean derived query methods is bad? Off course not. Derived query methods, they are super helpful for the developers whenever they want to query the data from a single table. Now let's try to understand on what is the JPQL and native query. JPQL means Java Persistence Query Language. Usually whenever we want to execute an SQL query onto the database, we will use SQL syntax. For example, this is an SQL query that I want to run to fetch the company's and its associated jobs based upon the given status. Here I have to use distinct because I want to fetch only the distinct company records. We know for a given company, there can be multiple child job records. And that's why when we run these query, we're going to get thousand records, which are going to have duplicate company's data. But I want to fetch only the distinct company's data that's why we need to use these distinct keyword inside the SQL query. Since I want to fetch all the columns inside the company's table, I need to use C.* here And next after the from, I'm mentioning the company's database table name. So this is the table name that we have inside the database. So for this table, I have given a shortcut synonym C followed by I'm trying to join these company's table with other table that I have inside the database with the name jobs. So here I'm using a simple join in case if you want to use left join, you need to mention left here and to the jobs table also, I'm giving a short synonym j. Whenever we want to run a join query, we need to mention what is the condition that the database has to consider when running that join query. That's why immediately here, we need to mention on and here c.id. So inside the company's table, there is a id column and this has to match with the company_id column inside the jobs table, because this column has a foreign key reference with the primary key column inside the company's table. So this is going to fetch all the companies and its associated jobs. But while fetching the jobs data, we also want to apply an extra filtering condition. That's why we need to mention where condition followed by j.status.  Inside the jobs table we are going to have a status column inside this column I'm going to look for jobs record only having the value active. So this is a normal SQL query. So these type of SQL queries, we call them as Native SQL query. So this is one format of query that we can provide to the @Query annotation. Now let's try to understand what is JPQL. Whenever we want to provide a query by using the JPQL, we need to think in terms of entity objects and properties. We should not write the actual table names and actual column names. Instead, we need to refer to the entity class name and field names that we have defined inside the entities. So here, if you see, most of the native SQL keywords are same inside JPQL as well. That's why here there is a SELECT and DISTINCT. But here instead of C.*, we just have to mention c. This c is a short synonym for the company entity class. Inside JPQL with this syntax, we're telling, I want to read all the fields present inside these entity class. The company's database table inside our backend project, it is represented by using an entity class with the name Company. The same class name I have to mention here. Next I need to mention JOIN and followed by I need to mention fetch as well. Using this fetch, I'm telling to the Spring Data, JPA and Hibernate framework to fetch all the child records as well as part of a single query. So that's the importance of this fetch. Next I need to mention what is another table or the child table that I want to consider while fetching the data, inside the C which means inside the company entity, the jobs relationship is going to be represented by a field with the name jobs. That's why here I need to mention c.jobs. So the join query will be applied between company and c.jobs. And this c.jobs data is going to be represented by using a short notation j. So this time I don't have to mention these on query because based upon the One-To-Many and Many-To-One configurations that we have defined inside the company and the job entity class, the Spring Data JPA is aware about the foreign key relationship. That's why behind the scenes, when it is actually running the SQL query on top of the database server, it is going to add these behind the scenes. Now coming to the where condition I have to mention, because where conditions depends upon my business logic. So under the where condition, we have to use j.status is equal to ACTIVE. So whatever status that you're seeing here, it has to match with the corresponding field name inside the job entity class. In our scenario, both the database column name and the field name are same. That's why here you're able to see exact values. But in case if your column name and field names are different, please make sure to mention the exact column name here and the exact field name here. The primary difference between JPQL and native SQL query is JPQL it is always going to work on entities and its fields, but not on tables and columns. So we can think of JPQL as a SQL that speaks Java. Here you may have a question? Madan, why should we learn JPQL when I'm already familiar with SQL? It's a very fair question. Let me answer the same. When we are using JPQL, we are going to get a lot of advantages. For example, when I'm writing this query, I don't have to provide this on condition. The framework is going to understand automatically. The second advantage is, the developer never have to know what is the table name, what is a column name, because developer can always refer to the entity class so that the developer never have to access the database to understand the table name, column name, and so on. The greatest advantage of JPQL is it is portable across databases. If you are going with the native SQL queries, these native SQL queries, they are going to change from database to database. The way you write for MySQL, Oracle, there'll be some minor differences because few of the keywords supported by Oracle, they are not going to be supported by MySQL. So that kind of precaution you need to take care when you're providing your SQL queries by using the native format. But with JPQL, you don't have to worry about the underlying database. You're always going to define your requirements by using the syntax and behind the scenes, the framework is going to take care of generating the corresponding native SQL query based upon the database that you have connected. So this is the example of JPQL inside the next slide, I also explained about what is a native query as well. The JPQL queries, they works very well with the entity relationships. We're going to see a demo on what are the advantages that we get when we are defining our queries by using JPQL compared to the native SQL queries. I hope you're clear with this discussion. Inside the next lecture, let's see a demo of these JPQL and @Query annotation.