Inside this section, we're going to explore on how to write custom complex queries by using @Query annotation. @Query is a very beautiful concept inside the Spring DataJPA. As a backend developer, it is mandatory for you to understand on how to use this annotation and under which scenarios it has to be used. Apart from @Query annotation, we're also going to discuss associated concepts as well inside this section. As of now, we are facing a problem while we are implementing a business requirement. The requirement is when we are trying to fetch the company details, we want to fetch only the active associated jobs. So this is the entity relationship that we have inside our application. So we established One-to-Many relationship between Company and Job. As we discussed in the previous lecture, we can't solve this problem by using Derived Query Methods. Like we can't define a method like this because the Derived Query Methods, they have certain problems or limitations. The first one is by using that Derived Query Methods, it is very hard to express complex joins, especially when you're trying to query the data from multiple tables. And these Derived Query Methods, they can't run the queries by using SQL concepts like FETCH JOIN. They also cannot control join type when i say join type, inside SQL, we have various kinds of joins like left join, right join, a simple join. Those kinds of joining of the multiple tables cannot be controlled by using that Derived Queries. And one more major problem with the Derived query Methods is, they cannot award n plus problem. I'll explain to you what is this N+ problem ? in few seconds. Overall, what I want to highlight from this slide is, please use that Derived Queries only for simple queries or for simple requirements. Now let's try to understand what is this N+1 problem. The N+problem occurs when Hibernate executes one query to fetch the parent entity and an additional query to fetch each child collection leading to performance issue. I know this definition is very hard to understand. Let me explain you by taking our company and job scenario. We know as per our database setup, a company can have multiple jobs. So that's why we are using One-To-Many relationship between these two entities. And this is the code that we have written inside the company's entity. Let's imagine we're trying to fetch all the company's data by using findAll() method. With this, what is going to happen? Behind the scenes, the Spring Data JPA will try to fetch all the company's record along with their associated jobs. So first to fetch the company's records data, it is going to first execute the query SElECT star from companies and all the company's data will be assigned to these company entity objects. But inside the company, we also have jobs related field, which is having One-To-Many relationship. By looking at these configurations, what Spring Data JPA will try to do is, it will try to load the jobs data as well for each company. Let's imagine we are using the default fetch type LAZY for this scenario. We know inside Spring Data JPA for the One-To-Many, the default fetch type is LAZY. And with that, when we first execute this method, only company's data will be present, but not the jobs data. Once this company's data is available under this company's list, let's imagine as part of the step two, the developer is trying to iterate all the company's data by using lambda streams or by using a for loop like this. And while iterating these company's data, the developer also trying to access the jobs details associated to each object. So here I'm trying to simply execute the company.getJobs() method followed by size() method to understand what is the size of jobs for a given company. When we are executing this line of code at this point of time, a query will be executed to fetch the jobs associated to this current company. So with that, what is going to happen? If you imagine there are 30 companies that we loaded from our database, this for loop, it is going to be iterated 30 times. And whenever we are executing these line, this kind of SQL query is going to be executed. SELECT star from jobs where company_id is equal to 1. So inside this for loop, this kind of SQL query, it is going to be executed 30 times. And here also, we executed one SQL query and it is a single query. So in total, we are executing 31 queries. Here the company is a parent entity, whereas jobs are the child entities. If you take the data setup of 30 companies, we are going to execute 31 SQL queries. But inside a real enterprise application, we may have thousands of companies or thousands of parent records. For all these thousand parent records, we're going to execute another extra query for each parent object to fetch the child records. If you take a scenario where there are 10,000 parent records available inside a database table, in that scenario we will end up executing 10,001 SQL queries. That's why this name N+1 has been given to this problem. What I'll do is, inside the next lecture, I'll show you a demo of these N+1 problem followed by, I'm also going to explain you what are the various approaches that we have to avoid these N+1 problem. Once we fix this N+1 problem, we can discuss about the @Query annotation as well on how to use them to solve the problem that we are facing with these business requirements.