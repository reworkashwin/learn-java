Inside Company Repository interface, first I'll try to create an abstract method, which is going to return list of Company object, the method name I'm going to mention it as findAllWithJobsByStatus, the name of the method does not matter here because we are going to use @Query annotation. So this method, it is going to accept job status as an input. So anyone who are invoking this method, they need to provide what is the job status that the query has to consider while fetching the records. On top of this method, I need to mention @Query annotation to this @Query annotation I need to mention the query that I want to execute whenever this method is invoked. As I said before, to this annotation, we can provide the query into different formats. The first one is JPQL, which is the most famous and recommended one. The other one is, by using the native SQL queries. Inside this lecture, let's use the JPQL query. So first I need to type SELECT, since I'm looking to fetch the DISTINCT company records, I need to use DISTINCT here, followed by I'm going to mention the c, which is the short form for the company entity. Next I need to type FROM followed by the class name of company's database table. So the company's data table entity class inside our project is, company. To this entity, I'm assigning c as a shortcut. Next I need to mention JOIN because I'm looking to join these company with other table. After the JOIN, I should also mention FETCH because by using these, I want to tell to the Spring Data JPA and the framework to fetch all the child records as well, along with the parent as part of a single query, with the which table we want to join with the jobs table. So the jobs table is represented by jobs field inside the company entity class. So as soon as I type c., I'm getting so many recommendations from my IntelliJ. Jobs is the correct field name. Let me select the same and these job table is going to be represented by shortcut j followed by I need to mention WHERE condition inside the where condition I want to consider the status field under the job. As soon as I type j., the IntelliJ is giving me all kinds of recommendations. So under the job entity class, I have a field with the name status. The same I want to use. Next I'm mentioning is equal to followed by colon status. Since I want to inject the status value dynamically, I need to mention these colon and this variable name here. So whatever variable name that I'm using here after the colon, the same, I have to mention under the method signature by using @Param annotation. So to this @Param annotation, I'm providing the same name status. So with this, now my framework knows whatever this method receives as a param, the same needs to be replaced here under the where condition. So with this, both our N+1 problem and active jobs fetching defect, both of them will be resolved. So let me save these changes. After defining our abstract method with the @Query annotation, we need to go to the CompanyServiceImpl from this place only we are invoking the findAll() method. This time, instead of invoking the findAll() method, I want to invoke find all with jobs by status. So this is the new method that we have introduced to this method I want to pass ACTIVE as status, but instead of directly mentioning the constant value here what I will do is, I will go to the application constants and here I will create a new constant with the name ACTIVE_STATUS. Under this constant, I have the value ACTIVE. The same, I will try to refer when I'm invoking the new method that we have introduced. Let me save these changes, do the build once the build is completed this time, I will try to test my API by using the UI application because from the postman, it is not easy to understand how many jobs we have received inside the response. Let me also go to the console and here I will delete all the existing logs. The UI homepage, I am trying to refresh this time. If I scroll down, I got 998 jobs. If you're not able to see, let me go to the light mode. As you can see here, 998 jobs are available in total under my UI application. So this gives a confirmation to us that our search criteria is working. Now let's try to understand whether the N+1problem is completely solved. I will take these logs and paste it here. As you can see, there are 251 lines of logs, but coming to the SQL query, there is a single SQL query is triggered by the framework. So this is the select query where it is trying to fetch all the columns from the company along with the job table as well. So these benefits, category, description, these are all from the jobs table. If you scroll down and look for the actual SQL query that ran by the framework. So it ran the joint query on companies and jobs, and it also used the on condition though we didn't mention this on condition inside our JPQL, but eventually by the time the actual SQL query is running behind the scenes, the framework taken care of adding this. So the condition is j1_0.status. So j1_ is a short notation for the jobs table. So the same query you will see multiple times, because as I said before, the react library, it will trigger the same request multiple times during the local environment. Maybe what I'll do is, I'll try to trigger the same REST API this time with the postman as well. Let me clean the logs. I'm invoking my REST API inside the response also you can look for closed value and there are no results. Whereas with the active, there are good amount of results. So there are 998 matches. Let me copy these logs and paste it here. This time you will see there are only 60 lines and you can also see the query is filed only once and as part of a single query, both the parent and associated child records are being fetched. And since a single query is being used, the N+1 problem is never going to arise. I hope you are clear on how to use this @Query annotation with the JPQL syntax, throughout this course, we are going to use JPQL syntax wherever required. Inside the next lecture, let's see a demo of @Query annotation along with the native SQL query as well.