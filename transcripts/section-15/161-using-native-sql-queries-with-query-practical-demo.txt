Let me copy these two lines of code and paste it again. This time I'm going to use the native query. That's why I'm going to mention Native towards the name of the method. Coming to the query, I need to update this because I want to use the actual native SQL query. So here, instead of c, we need to mention c.* because inside SQL language, when we want to fetch all the columns, we need to mention this asterisk symbol. Next, instead of mentioning the entity name, we need to mention the actual table name. So what is the actual table name? It is companies. And this table, it is going to be represented by these shortcuts, followed by I need to use a JOIN keyword and FETCH. We should not use FETCH is a keyword which is specific to the JPQL. Now since I'm looking to join with the jobs table, I need to mention what is that exact table name. So let me delete these and type jobs j. Next, I need to mention the join on condition as well. So let me type c.id is equal to j.company_id. So this is the exact column that I have under the jobs table. Next, I need to mention WHERE condition WHERE j.status is equal to colon status. This style of passing the parameters is called named parameters because we are trying to inject the dynamic values by using the parameter name. There is also another style which is positional parameters. So instead of colon status, what I can mention is I can mention ?1. With this, I'm telling whatever first parameter that you see under my method, please bind the same as a dynamic value for the status column. In case if you want to pass multiple dynamic parameter values, you can define ?2, ?3 and so on. And in whatever order that your method accept the parameter values, the same will be assigned dynamically at runtime. Since our method is accepting a single parameter, mentioning these one also is optional. We can simply mention j.status is equal to ?. So here we are getting a compilation error because this @Query annotation, it is assuming whatever query that we passed is a JPQL query inside the JPQL query using the asterisk is not allowed. That's why we are getting a compilation error. So to fix this problem, whenever we are trying to share actual native query to this @Query annotation, we need to give a clue as well by invoking another attribute with the name nativeQuery and to this I'm setting the value as true. With that, my compilation issue resolved. With this code, anyone who are invoking this method, this actual native query will be executed blindly by my Hibernate and JPA framework. They're not going to make any optimizations. But there is a drawback with the native SQL query. Let me show you a demo around the drawbacks. I can copy this method name and I can go to Company Service Impl here. Let me use the Native() method. After doing the build, I will clean the console logs from the postman I'm trying to click on the send button. This time inside the response, if you search for closed, there are two records and along with the closed, if you look for ACTIVE, you will see 998 record. That means inside the response, I got all jobs, including closed and active. Let's confirm the same inside the UI app as well. If I refresh this homepage, you will be able to see total 1000 jobs are fetched from the backend database. Let's try to understand the console logs as well. I will clean these logs and invoke this REST API one more time. The number of logs lines are total 118, but here in total three SQL queries are filed. The first one is to fetch the company records by joining both companies and jobs and using these on condition and the status. So this is used to fetch the company records. The next query is around fetching the job details for a given company. Since we are using the batch fetching strategy, the Hibernate framework tried to query all the child records by using these in condition. We have mentioned the batch size as 10. That's why first it tried to fetch the job records by running the SQL statement three different times. So this is the first time, and this is the second time, and this is the third time. So this gives a confirmation to us that we are back to the N+1 problem, though it is not a pure N+1 problem, but still it is a problem because we are firing unnecessary queries to fetch the child records. That's the first problem. The second problem is when we are fetching the records of company and jobs, the job status active is not getting considered. There is a very good reason on why this is happening. Let's try to understand the same. To illustrate this slide, I have given an introduction about Native Query. Native Query means it is a pure SQL statement. While providing native Queries, we need to make sure to pass the native Query as true. So this is an example that we just discussed. Before we try to explore the disadvantages of native Query, let's try to understand under which scenarios the developers will use native Query. You need to construct these native Queries whenever you want to leverage the database specific features. For example, if you have the databases, they are going to provide various features like window functions, PIVOT, ranking, all such database specific features. If you are looking, obviously you need to pass the native SQL query. In the similar lines, if you are looking for any performance optimizations, like by providing data specific hints, you need to provide your entire SQL query by using the native Query format. Developers, they also use native Queries when they are working with the legacy databases where they want to execute some complex stored procedures. Native Queries can also be considered while implementing complex aggregations where JPQL cannot handle. Though native Queries providing all these advantages, they are very rare scenarios. Unless you are exploring these advantages, don't try to use the native Queries. In 99% of the scenarios, developers, they will end up using JPQL queries. That's why here I also highlighted not recommended for everyday use. But using native Queries, it also attracts some disadvantages or trade-offs. The first one is, the query that you have returned, since it is a native format, it is always going to be dependent on the database that you are using. In future, if you are migrating to some other database, then you're going to break the portability. You need to make sure all your native SQL queries are working properly by doing the testing of all the scenarios. With the native Queries, there won't be any compile time checking. The errors will happen only at the runtime when the query is getting executed on the database. By using the native Query, whatever you are providing, it is going to be blindly executed on the database server by the framework. Whereas with the JPQL, the JPA framework or the Hypernate, they'll try to convert this into a normal SQL query. And to perform this job, they're going to do some checking during the compilation time and since the SQL query is getting generated by the framework, there won't be any chance that your SQL query is going to fail at the database server due to some syntax issues. Whereas with the native SQL query, for example here, if you forget to mention this asterisk and simply mention c, then you won't get any compilation problem your server also is going to start perfectly. But when you are executing this method, at that point of time, a runtime exception is going to occur. With the native SQL query, you're going to get full SQL power. You can use any database features like window functions, PIVOT, ranking, and so on. Coming to the performance, you will see native Queries are faster in few scenarios, especially around the complex queries. But those performance optimizations are very negligible. Unless you identify there is a serious performance problem with the JPQL, don't go for the native SQL query. So this is the golden rule that you need to remember always, if you are simply looking to execute some CRUD operations, then always leverage the methods provided by the Spring Data JPA. Otherwise, if you are looking to build some custom queries, then for simple queries, you can go for the derived query method approach. For complex query, you can go with the @Query annotation and to the @Query annotation, you can pass the query by using JPQL syntax. Only for any unique scenarios where you want to use some DB specific logic or keywords Under such scenarios, go with the native Query. Now going back to our original issue on why the native Query is not working based upon our expectation. So when we are running these query, we are telling to the database to provide all the company's data, but our database does not know about our JPA relationships that we have established inside the entity class. That's why as part of these company's data, it is not going to provide any job specific data, because database simply works based upon the column names it does not understand the JPA relationships that we have inside the framework. So the above native SQL query, it guarantees only one thing. It is going to return companies having at least one ACTIVE job, but it does not guarantee which jobs will be loaded, how jobs will be filtered and whether the jobs are loaded eagerly or lazily. So this SQL query is going to simply bother about loading the company's data, but not the child records data because it does not understand the One-To-Many mapping configurations that we have done under the entity class. There are some workarounds to overcome these problems. For example, let's imagine once the company's data is loaded and it is available under a object name called company, by using this company entity object, we can invoke the getJobs() method. At this point of time, the Hibernate will try to load the child records associated to this company. So when it is trying to load the child records, the Hibernate does not aware about the status that we have provided inside the native SQL query because the child fetching is going to happen outside of these native SQL query. So that's why the Hibernate is fetching all jobs regardless of their status. So it is going to run simply this query. If we want to communicate our where condition to the framework in this scenario, we can use an annotation @SQLRestriction on top of the jobs field. So here I'm mentioning this annotation and against to the status field, I'm providing a value called ACTIVE. By looking at this annotation, the Hibernate, whenever it is trying to load the jobs inside the company, it will try to apply these where condition. So this time, this is the query that it is going to run. But the problem with this configuration is, even in some other scenario, if you are trying to fetch the company and the child records and a such scenario, also the same status will be used. For example, if you have a scenario where you want to fetch all the job records, then it is not going to work because here we have restricting to always to fetch the jobs based upon the active status. Off course, there are some approaches inside the Hibernate to dynamically convey these status value to the Hibernate by using some annotations, like @Filter definition. All those are in-depth concepts of Hibernate. We don't have to leverage them for our scenario because the recommendation is always to go with the @Query annotation along with the JPQL. I'm just trying to show on how to use the native query as well. That's why we're discussing all this stuff. The other problem that we're seeing is, N+1 problem. Why the N+1 problem arises here is, when the framework is trying to fetch the jobs for a given single company, it will try to execute any of these SQL statement individually. But since we are using a annotation like batch size and a property inside the application.properties file, the N+1 problem is reduced to a great extent because the Hibernate will try to fetch the child records by using the IN-query. Always remember native SQL queries does not control relationship loading because the native Queries they don't understand the relationships that we have defined under the entity classes. The relationship loading is going to be controlled by the annotations that we have defined inside the entity classes. Let's try to apply this fix and see if it is working in our scenario. Here just below the batch size, I'm going to invoke SQL restriction and to this I will pass status is equal to inside the single quotes I will pass the value ACTIVE. Let me save this, do the build once the build is completed, I will clean my console. I will refresh my homepage and with that this time we're able to see 998 jobs are fetched. But the N+1 problem, it will not be solved completely because with the native SQL query, the child jobs they are going to be loaded separately by the Hibernate framework. There are many other approaches as well to fix the limitations of the native SQL query. But as I said before, don't explore much about the native SQL query always try to adhere to the JPQL queries. Let me comment this code and I'll switch back to previous method that we are using so that our application works without any issues. And with this method, we can also avoid the N+1 problem completely. This time if I refresh the page, still I'm seeing the 998 jobs.