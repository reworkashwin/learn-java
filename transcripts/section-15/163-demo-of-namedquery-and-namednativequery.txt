On top of my Company entity class, I'm going to mention @NamedQueries annotation from the Jakarta.persistent package. To this annotation, like we discussed, we can pass any number of @NamedQueries. So first I need to mention curly braces, followed by I can mention @NamedQuery annotation. By using these, I need to invoke the name property while we are defining the name first, we need to mention the entity name followed by whatever name that we would like to use. Let me mention the method name as fetchCompaniesWithJobsByStatus. So this is the query name followed by we need to mention the actual query. So this actual query I can get from the repository as well. So here this is the JPQL query that right now I'm using the same I will mention here. So this way we can define any number of queries inside the @NamedQueries. In the similar lines, let's try to define @NamedNativeQueries as well. So instead of @NamedQueries, I just have to mention @NamedNativeQueries from the Jakarta.persistent package. Here we need to use the annotation @NamedNativeQuery. Coming to the name, I'll just mention Company.fetchCompaniesWithJobsByStatusNative. Under the query, I just have to mention the native SQL query. I can get the same from the CompanyRepository. So this is the native SQL. Let me paste the same here and after the query, this time, we should also invoke result class. To this result class, we need to pass company.class. So now with these configurations, the Spring Data JPA framework, it will try to look for a method matching with these names. So we can go to the CompanyRepository. What I'll do is, I'll try to copy these abstract method and replace the method name with the name that we have used inside the entity. So with this now, since I'm maintaining a method with the same name, I don't have to mention @Query annotation. Let's do the same for native method as well. I'll copy these and go to the native class. Let me take this method name and mention it here. We can try to test one of these methods. Maybe I'll copy this method and I'll go to CompanyServiceImpl and invoke the same. Before I test the REST API, let me clean the console from the postman if I click on the send button, I will see the response. Inside the response, I should not see any job with the closed status. So there are no results. But with the ACTIVE status, there'll be 998 records. So this gives a confirmation that our configurations are working. Let's copy the logs as well. From the logs, I see a single query is executed to fetch both the parent and the child record. So there won't be any N+1 problem with these @NamedQuery as well, because we are using the JPQL syntax behind the scenes. But if you try to use the @NameNativeQuery method, then obviously you're going to have an N+1 problem and the job records are also will not be fetched based upon the status. To make it work, you have to make sure to uncomment this SQL restriction and to use these batch size. I will try to keep all these methods for your reference under the CompanyRepository, but please make sure to use only the methods which are using the JPQL inside your CompanyServiceImpl. From the UI page also, if I try to test the scenario one more time, I'm able to see only 998 jobs. I hope you are clear on what we have discussed throughout this section. We discussed about an existing defect inside our application. The same we tried to fix by using the @Query annotation, we understand on how to pass the query to these @Query annotation by using various syntaxes like JPQL and nativeQuery we also understand what is an N+1 problem and how to overcome it by using batch fetching strategy or by using JPQL queries. Please refresh all these concepts one more time by referring to the slides that I have provided. We are going to reuse all these concepts again and again throughout this course, whenever we are trying to fetch the data from the multiple tables. For the simple scenarios where you want to fetch the data from a single table, you should be fine with the derived query methods.