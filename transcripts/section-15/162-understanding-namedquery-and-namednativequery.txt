There is an annotation with the name @NamedQuery, which is closely aligned to the @Query annotation. Let's try to understand the purpose of this annotation. Sometimes we as a developers don't want to clutter our repository interface by defining many @Query annotations. And when we are defining a query directly on top of the repository method, it also brings some drawbacks. We can't reuse that query in some other repository as well. So to overcome these challenges, we can use the @NamedQuery annotation. So this annotation you need to mention on top of your entity class. For example, I have an entity class with the name Company. On top of this class, I have mentioned @NamedQuery annotation. While using this annotation,first we need to define the name attribute. By using this name property, we need to define a name to our query. So usually the standard way of naming the query is first, you need to mention the name of your entity, which is Company dot whatever name that you would like. So this is the name that I'm trying to use, fetchCompaniesWithJobs ByStatus, followed by you need to mention comma, and by using this query property, you can provide your query. Here the query needs to be provided by using the JPQL syntax only. If you want to use native SQL query, there is another annotation, which I'm going to explain to you in few seconds. The beauty of this approach is the same query can be reused in many other repositories. That is one advantage. The other advantage is, your repository interface, it is going to look very clean and simple. If you are maintaining the method name, same as the name that you have defined after the entity name, then the Spring Data JPA framework is going to do the magic. So whenever someone is invoking this method, it is going to look for if there is any query defined with the same method name under the company entity class. Since there is a query defined with this same name, this query is going to be executed. In case if you want to use a different method name inside your repository, that is also perfectly fine but in this scenario, you just need to mention @Query annotation. And by using this @Query annotation, you need to invoke the name property to this name property you need to provide whatever name that you have defined here. So Company.fetchCompaniesWithJobsByStatus. And this is your JPA method name, which is different from what you have defined here. So this is also perfectly possible. But I strongly recommend you going with the same method name as you define here, because you will get saved from not mentioning this @Query annotation. The benefits of named query is all queries that are going to be centralized in one place that is inside the entity class. All these queries, they are going to be validated at startup. Any errors will be caught early. The queries that we have defined here, they can be reusable in multiple repositories and your repository interfaces, they are going to look very clean. Usually developers, they don't open entity classes a lot. They will only open repository interfaces to a great extent because they may want to invoke various kinds of methods based upon their business requirements. So that's the whole reason on why developers, they'll try to keep their repositories clean, even though they're fine making their entity classes looks clutter. And one more advantage is, with the named query, since you are going to mention all your annotations on top of the class, so the content of the class is always going to look clean and simple. When we're using these @NamedQuery annotation, we can define the query only by using the JPQL syntax. But what if you have a requirement to provide the native SQL query? For such scenarios, you need to use another annotation with the name, @NamedNativeQuery. This is also going to work very similarly like nativeQuery. You just have to pass a name, query. This time the query has to be provided by using a native SQL format. But apart from name and query, this time while using the @NamedNativeQuery annotation, you should also invoke the result class property and to this, you need to mention on how the data from the SQL needs to be mapped to a Java class. So here, since we are looking to map the data to a company entity class, I have mentioned company.class. But you may have a question, why this result class is became mandatory for this @NamedNativeQuery, but not for the @NamedQuery. On top of that, you may also argue, why can't the Spring Data JPA framework can guess the returned class name based upon my method signature. So let's try to understand the same. We know JPQL works with entities. So JPA already knows which entity to return from the query itself. So inside the query itself, we are going to mention all the entity class details. Whereas native SQL queries, they are going to work with tables and columns that's why JPA has no idea how to map a row to an entity class. And the framework cannot interpret the return class details from the repository method signatures, because whatever methods that we define inside the repository, they are not going to be parsed while these queries are getting compiled. We know all the queries that we define by using the @NamedNativeQuery and the @NativeQuery, they all are going to be validated during the startup itself. So during the startup, since the method signatures inside the repository is not yet evaluated, the framework cannot interpret the result class details. So that's why we must explicitly specify the result class to tell JPA how to map the result. We also have plural form of this annotation with the @NamedQueries and @NamedNativeQueries. In case, if you are looking to define multiple @NamedQuery and @NamedNativeQuery inside an entity class, you can mention them under these plural form of the annotation. For example, first I have to mention @NamedQueries and inside these, I can mention any number of @NamedQuery, within the parentheses first I need to mention Calibri says, followed by I can mention @NamedQuery and to the @NamedQuery, as usual, we need to provide the name and query in the JPQL format. This way I can provide any number of @NamedQueries, each of them, they should be separated by using comma. The same syntax applies for @NamedNativeQueries as well. Let's see the demo of these @NamedQuery and @NamedNativeQuery in the next lecture.