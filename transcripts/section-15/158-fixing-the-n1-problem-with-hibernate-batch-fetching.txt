We know this API is going to be invoked from the UI application whenever we are trying to access the homepage. We can try to test this API one more time, but this time, we are going to look at the console logs to understand what is really happening. So I'll go to the console logs and I will clean the existing logs, followed by I'll open the application.properties inside the application.properties, please make sure you have these properties with the name spring.jpa.show-sql. So by using these properties, we are trying to log all the SQL queries that are being run by the Spring Data JPA framework. You can trigger the REST API either by using Postman or by using the UI application as well. If you have not set up the UI application, you can use this Postman approach. Since I have my UI application running, what I'll do is, I'll try to refresh this page, which might have invoked the REST API. Let me copy all these logs that I have, and I'm pasting them inside a text editor. As you can see here in total, around 4,000 lines of logs has been printed. So if you look at my logs, there is a SQL query that keep printed onto the console. So where it is trying to fetch all the jobs related data from the jobs table based upon a given company id. If you keep scrolling up, you will see the same query is printed multiple number of times. This is due to the reason of N+1 problem that we have discussed. So this gives a confirmation to us right now, inside our application, we have a N+1 problem. Why this is happening? If you go to the Company Service Impl, we are trying to load all the companies related data by invoking this findAll() method. And with that, all the company's data will be loaded from the database. If you open these companies entity class, as of now, when we defined One To Many configuration details here, we have not configured any fetch strategy we went with the default fetch strategy, which is lazy. And with that, what is going to happen? The child records of jobs, they're not going to be loaded when these line of code is executed. Instead the associated job details will be loaded only when these method is getting executed. So this method is going to be executed for each company object that we have, because here we are using map() method. And inside this method, we are invoking these getJobs() method. And at this point of time, a fresh SQL query will be executed behind the scenes to fetch the associated jobs for a given company. Since we have 30 different companies inside our backend database table, in total 31 SQL queries is going to be executed for this scenario. But if you are invoking the REST API using an UI application, you will see double number of SQL queries, like 31 into 2, you will see 62 queries logged into the console. The reason is whenever using react library inside and development environment, every request it is going to make two times behind the scenes, just to make sure that all the react re-rendering logic is working fine. So that's a behavior of react inside local and development environment. Whereas inside the production, that is not going to happen. But if you are testing the same REST API by using the postman, you will see only the 31 queries. So this gives a confirmation to us that right now there is an N+1 problem inside our application. . You may be thinking to fix this problem by simply using the eager fetch type. So here we can configure the fetch by using this fetch property to this I'm passing FetchType.EAGER. Even after using eager fetch type, still, we are going to see the problem. Let me go to the console and I will clean my entire console. In the UI, I'm going to refresh my homepage and these are the logs. Let me copy this logs and paste it here. This time also I'm seeing the same query is getting printed multiple times. That means our N+1 problem is still there even when we are using the eager fetch type. The reason is due to the hibernate behavior behind the scenes. Let me mention some comments here and inside these comments, I'm going to mention the reason on why it is not working. So whenever we are using Many To One and One To One in this scenario, only one child record will be present for a given parent. This type of associations in other words, we can also call them as single valued associations, with these kind of associations hibernate it is always going to use join in the main query. When I say main query, when it is trying to fetch the parent record at that point of time itself, it is going to use join query and load all the child records as well. But coming to the collection valued associations, like One To Many and Many To Many, where there will be multiple child records for a given parent record under such scenarios, the hibernate default strategy is not to use join by default. Instead, it often uses a secondary select one query per collection unless configured otherwise. Why hibernate following this strategy is, to avoid performance issues. Now, even after configuring these fetch type, our problem is not solved. That's why I'm removing that. But there are some alternatives or solutions to solve the N+1 problem. The first one is inside the application.properties, you just have to define a property here just after these hibernate format SQL property I have pasted a new property with the name spring.jpa.properties.hibernate.default_batch_fetch_size. . To this, I am passing a default value of 50 otherwise I can change the default value by these environment variable as well. Whenever we are using these property inside our project, it is an indication to the hibernate to always try to run the queries as a batch with a given size. Whenever hibernate identifies that the framework is trying to load the child records for a given parent to record lazily under such scenarios, instead of loading the child records for each parent individually, it is going to batch the SQL query and that batch size we can configure as well here as of now, I have mentioned 50. That means up to for 50 parents, the child records are going to be fetched as a batch. As of now, inside our data, we have only 30 parent records, which are company records, but still I'm trying to mention higher value like 50, because this is a reasonable number for production setup as well. So this is a global setting for your entire application. When you are defining this property, this property is going to be leveraged for all entities you have inside your backend application. I'm also going to show you on how to override this global setting for a given specific entity. First, let's try to test our REST API with this new property. I'm doing the build, once a build is completed, I'll go to the console and clear all the logs. This time I'll try to invoke the REST API from my postman and I got my successful response. Let me copy the entire logs. As you can see here, this time, the logs are only of 63 lines. If you closely look at the SQL statement, there are only two. The first one is used to fetch all the company's data from the company's table, followed by we have the second query, which is used to fetch the jobs data from the jobs table by using these where condition. Inside the where condition, the framework this time, it mentioned all the company ID details into these in query. If you try to count all these question marks, there'll be 30 question marks. In case if we have 80 company records, then you will see the second query printed two times, because the batch size that we have set is 50. So there'll be two different SQL queries, one with the 50 and the other one with the 30 company IDs. With this small setting, right now we are saved from N+1 problem, but we didn't solve that problem entirely. In the next lecture, I'll explain you on how to overcome the N+1 problem completely. But whatever solutions right now we are discussing, these are also required under certain scenarios. I'll explain about those scenarios in a few minutes. Now let me go to the company entity here, just above the jobs field. I'm going to mention @BatchSize annotation to these. I'll mention the size as 10 this time, because I want to show you the demo on how this batch size is going to work. So whenever we are mentioning batch size inside an entity, then whatever global batch size that we have configured inside this properties file, it will be overrided by the framework for the specific entity and its child records. So let me save these changes, do the build once the build is completed, I will clean the console followed by, I will invoke the REST API from the postman. These are my logs. Let me copy them and paste it here. So this time we have 130 lines of logs. Coming to the query related to jobs, there'll be three queries. So this is the first one. Next we have the second one followed by third one. And just above these, we are going to have the main query, which is going to fetch the company's data. So this is working based upon our expectation. But if you are testing the same API from the UI application, as I said before, you will see the queries double number of times because the react library, it will send all the requests two times during the local development. We try to solve the N+1 problem by using both these approaches. The first one is by defining this property inside the application.properties file. The next one is by using the @BatchSize annotation on top of the child field inside an entity. You can use both these approaches, but don't be under the assumption this solves the entire N+1 problem. Batch fetching does not remove the N+1 entirely instead, it converts the N+1 queries into one plus N divided by batch size queries. In order to get rid of N+1 problem completely, we need to prefer running the queries by using join fetch. This is what we are going to explore in the next lecture. But I still recommend you maintaining this property inside your backend application always. Because this is a global setting. And even if a junior developer is not doing a good job in querying the multiple tables by using the join fetch, under such scenarios, this property is going to save your application from the performance issues. And this join fetch solution also, it is specific to each use case, and it is not going to solve your problem entirely at the global level. So that's why as a safe net, always define this property inside your application.properties file. With this, I'm assuming you're clear on how to solve the N+1 problem by using the Hibernate batch fetching strategy.