We have the data layer or repository layer ready for our new REST API. As a next step, we need to build the controller layer and service layer as well. What I'll do is, I'll create a new package with the name contact under this package, I will try to create a new sub package with the name controller. Inside this controller package, I will create a new Java class with the name ContactController. On top of this class, I'm going to mention a couple of annotations one is @RestController followed by @RequestMapping. And to this @RequestMapping, I'm passing the path as contacts. Anyone who wants to invoke the REST API that I'm exposing using this controller, they need to make sure to send this path. Let me create a method that returns String as an response. That's why I'm trying to wrap it using @ResponseEntity to this, I'm passing String followed by, I'll mention the method name as saveContactMsg(). And to this method, the UI application, it is going to send the request by using a DTO class. The DTO is ContactRequestDto. Before this DTO, we need to mention an annotation what is that, it is at @RequestBody. Long back, we discussed about these annotations. Since we want the springboard framework to convert the data from JSON to Java object, we need to make sure to use these @RequestBody annotation. For now, let me return a null value. As a next step, we need to build a service layer as well. So to build the service layer, we need to create a new package under the contact. Let me create a package with the name contact.service. Under this service package, I'm going to create an interface. The interface name is IContactService. Inside this interface, we're going to have an abstract method. The abstract method name is saveContact(). And this method, it is going to accept request to DTO as an input. And the output from this method is a Boolean value, confirming whether the contact data is saved into the database or not. Now, let me create an implementation class for this interface. Under the service package, I'm going to create an impl package, followed by I will create a new Java class with the name ContactServiceImpl. This method, it is going to implement IContactService on top of this class, I'm going to mention @Service annotation next, I'm going to overwrite the method with the name saveContact(). We followed the similar style of programming while we are building the company related REST API as well. I'm trying to do the same. Hope you all understanding this. Please take this opportunity to revise those concepts one more time. When I'm creating the service classes and interface for the company REST API, I made a mistake I created the service package outside of the company package. Let me move this to company package, followed by I'm going to refactor. Let me open this. Seems the service package is moved to the dot controller. What I'll do is I'll open any of this interface and remove this controller. And here I will click on this more actions and select this move to package, which is going to create a separate package under company so now I have a service package. Into this new service package, I will try to move this IMPL as well. Let me open this CompanyServiceImpl. And here I will try to update the package name to company.service.impl Now I can hold on to this error, select more actions and use this move to package option. With this, now we should be good from company packages as well. Let me go to the ContactServiceImpl. Into this class, we need to inject the bean of ContactRepository interface. So how to do that? It's very easy first, we need to mention @RequiredArgsConstructor annotation followed by I'm going to mention private final ContactRepository. Once this bean is injected, we need to use the same inside our business logic. Using these ContactRepository, we should be able to invoke a method save(). So this save() method, it is going to accept the entity object. What is the entity data type that we want to save? It is the Contact. When we pass a Contact entity object to this save() method behind the scenes, it is going to take care of generating the insert statements and using the same it is going to save the data into the database table as well. But right now we have the DTO in our hand. That's why from this DTO object, we need to transfer the data into the entity class. To write the mapper logic, which is going to transfer the data from my DTO to my entity object. What I will do is, I'll try to create a private method, which is going to return the Contact entity object. The method name will be transformedToEntity(). And this method, it is going to accept the object of ContactRequestDto. So first I'll try to create an object of entity inside this entity object we are not going to have any data. It is our responsibility to transfer the data from these DTO object to entity class. One way of transforming the data is we have to invoke setter method for each field, like setName(). And to this method, we can assign the output that we're going to get when we invoke the name method of the DTO. All the getter methods inside the record classes, they're not going to have the get to prefix they're going to use a field name as method name itself. This is done intentionally because inside the record classes, since there is no chance of having the setter method, the Java team, they decided to have the Java methods as with the normal field names instead of get name. So when I invoke the name() method, I'm going to get the name value present inside this DTO object. The same I can set into this Contact entity object. This way, I need to repeat this logic for all the fields. Instead, we can use a better approach as well. If the Java fields present in your DTO class and the entity class are same, then you can invoke BeanUtils. This is an utility class present inside the Spring framework. Using this, we need to invoke the copyProperties() method. To this copyProperties(), we need to pass the source object and the destination object. The source object is ContactRequestDto, whereas the destination object is Contact. Behind the scenes, what this method is going to do is, it is going to copy all the field values from the source to destination object. But while copying, it will only look for the exact matching field names. If you open the DTO class, we have these five fields. Only these five fields data will be copied into the entity object. Because we are having the same Java field names inside the entity class as well. The id will have zero anyway, since we are inserting a new record, the framework is going to take care of inserting the id value. Whereas other fields like createdAt and createdBy, we need to take care. Coming to the status, anyway, it is going to insert the default new value. Let me go to my mapper() method. Before I try to return these Contact entity object from this method, I need to make sure I am populating few not null fields as well. For example, I need to take care of createdAt. To this method, I'm going to pass the current date and time value by using Instant.now() method. Next, I'm going to invoke the contact.setCreatedBy(). To this, I'm passing a value called system. This value can be anything, but right now, since the REST API itself is creating the record, I'm providing this system value. Once we have some login functionality and authentication functionality inside our application, we will have the logged in username under this createdBy. We are going to develop security for our application in the coming sections. After populating this, we can return Contact object. Now, while I'm invoking the save() method by using ContactRepository, what I'll do is I will invoke my new private method and to this, I will pass the contactRequestDto. So this save() method, it is going to return me an entity object, which represents the record that's stored inside the database. By the time my save method returns the Contact entity object, it is going to have the primary key Id as well populated. That's why what I'll do is I'll try to catch the output by using Contact object and just above this line, I'm going to create a boolean with the name as a result. By default, it is going to have false. Next, I'm going to write an if condition here I'm going to check if the Contact object is not null and if the contact.getId() is also not null, that gives a confirmation to me. A new record is inserted inside the database. If this if condition is satisfied, I'm going to populate the true value to the result. And towards the end, I'm going to return result from this method. So with this, now my service layer logic is ready. I need to go to my controller layer. And here I need to mention @RequiredArgsConstructor followed by I'm going to mention private final IContactService. Since I'm using this constructor, the auto wiring of this bean into this controller is going to happen behind the scenes. Using this contactService inside the business logic, I can invoke the saveContact() method to the same I can pass a contactRequestDto, whatever output I get I'll try to catch the same using the variable isSaved. Based upon this boolean value, I want to return different response to the client application. Here I'm going to write if and else condition. If the boolean value is true, I'm going to return a status created. Since we are creating a new record, we need to return 201. In case if the record is not saved, we're going to return 500 along with this String value. On top of this method, we need to make sure to mention @PostMapping annotation because we're trying to save a new data into the database table. Let me also mention version details as well. I will invoke the version and to this, I will pass the version number as 1.0. I'm assuming we have built all the logic related to this new REST API. In the next lecture, let's try to test our REST API by using both Postman and the UI application.