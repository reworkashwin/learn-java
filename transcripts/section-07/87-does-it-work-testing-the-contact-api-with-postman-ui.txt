Since we made a lot of changes to build a new REST API, behind the scenes, I restarted my Spring Boot backend application. Please make sure you are also doing the same. We can try to test our REST API first from Postman. Inside the Postman collection that I shared with you, you will see a folder with the name contacts. Under this, you will have a request with the name send contact message. If you click on the same, you will be able to see that this request is going to invoke a REST API with this path by using the HTTP POST method. And to this, first under the headers, we need to make sure to send the accept header along with this header value. Even if you don't send this header, by default, the framework is going to consider 1.0 as the default version, but still, it is a best practice to send this header. Next under the body, we need to send our request data. Inside this window, you need to make sure to select the raw followed by JSON, and this is the JSON object that we are trying to send. So inside the JSON, I have fields like name, email, user type, subject, and message. The field names inside the JSON object, they should match exactly with the DTO class that we are using inside our REST API methods. I make sure all these fields are same inside my JSON object. If I click on this send button, boom, I'm getting 404 not found, but let's see what is the problem in the backend. If I open the console, I'm able to see an error related to status column. For this status column, seems the null value is going since this column is a not null column in the database, the insert operation is failing. Let's try to understand what is the issue. First I'll go to the contact, here on top of the status field, we have mentioned these @ColumnDefault with the value NEW. So previously, I was under the assumption that the JPA is going to construct this default value while inserting a new record. But I was wrong. The purpose of this annotation is, while the Hibernate is trying to create a new database table for this entity, it is going to consider this default value and mention it at the database schema level. If you open the database table, you will be able to see for the status, there is a default column value define NEW. This means by using this annotation, we can only update the definition at the database schema level, but not at the record level. We can fix this problem very easily. We can go to the ContactServiceImpl and here just before returning the entity object, I'm going to invoke setStatus() method and to this, I will pass the value as NEW. Let me do the build, once a build is completed, if I try to invoke the same REST API, we are going to get a successful output. But before that, I want to highlight you about the error response that we are getting. We are always getting 404 whenever an error is happening on the backend side, but that is not good, right, we should get the actual error details as part of the response, along with some meaningful error code like 500. We need to enhance our application to achieve the same, which we are going to do in the coming sections by implementing proper exception handling inside our application. But now please bear with this 404 error. I'm clicking on the send button and boom, we got a successful response saying that request process successfully with the status as 201. We can validate inside the database as well. Since I'm using premium version, I will try to validate from the IntelliJ itself. If you are using the free version, please use the sqlectron validate whether the record is inserted or not. Let me also use sqlectron, I don't want you to feel bad in case if you are using IntelliJ free version. So here we have a connection jobportal database we need to click on the same under the tables, we have two tables. Let me click on the contacts and you'll be able to see there is a record inserted into the database. It is the same data that we sent from the postman. This gives a confirmation to us that our REST API is working perfectly. Now let's try to test the same from the UI application as well. To test from the UI application, you need to make sure that your UI code is up and running. You have to run this npm run dev command And with that, your UI application is going to start at the port 5173. This is the homepage of the UI application. Let's go towards the end, and in the footer, we have a contact us link. In this form, I'll enter the name as Will Smith, email as will@  gmail.com. Please make sure you are entering all the valid data inside this, because there are some UI validations for this full name and email address and for the message as well. If you try to send some blank data, you're going to get these kind of UI validations. We're going to discuss more about validations in the coming sections. I'll select the subject as technical issue and coming to the message. I will mention it as not able to create an account in your website. As soon as I click on the send message, I am going to get a confirmation in my UIP saying that the message is sent successfully. In the backend also, we can confirm by going to the database. Let me run this select query one more time, you will see there is a record with the name Will Smith along with the subject and the message that we sent from the UI application. I hope you are happy with the progress that we are making. Inside this section, we built a new REST API for the contact us page. In the same process, we also explored about the JPA Buddy plugin along with the properties spring.jpa.hybernate.ddl-auto.