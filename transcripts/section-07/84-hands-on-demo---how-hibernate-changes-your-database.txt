Since we are into the new section, I'm going to create corresponding section folder inside my workspace. Into this new folder, I will copy the backend project from the previous section. Now let me open this job portal backend inside my IntelliJ. Once I open this project, I'll try to start my backend applications so that the database container also will be started behind the scenes. Whenever you're trying to start your Spring Boot backend application, please make sure your Docker desktop is also running behind the scenes. Unless your Docker desktop is running, the database container cannot be created by the Spring Boot framework. Inside my Docker desktop, I'm able to see that my database container is started without any issues. Now if I go to the homepage of my UI application, I should be able to see all the company's data. Let's go to the application.properties and here we want to mention a new property with the name spring.jpa.hibernate.ddl-auto So these are all the valid values for this property. If you are using a premium version of IntelliJ, it is going to show you all the valid values. Otherwise, you need to find this information in the official documentation. Against each of these values, we have a description also. We already discussed all these valid values. To show you the demo of this property, what I'll do is I'll try to mention validate value. We know with validate, the Hibernate, it is going to simply validate the schema and it is not going to make any changes to the database. In case if there are some mismatches between my entity class and database schema, or if I did not follow any standards also, the framework is going to throw some validation errors. Let me mention validate value followed by, I'm going to do the build. Once the build is completed, I will try to restart my application. This time the Hibernate, it is going to perform the validations. Previously, since we are using MySQL database as the database, the none default value will be considered for this property. With the none value, Hibernate is not going to manage or validate our database schema. Now, since we have mentioned validate this time, I'm getting an error during the startup. So let's try to understand what is the problem here, it is saying under the table companies, there is a column found with the name text and for these, we have mentioned the data type as LONGVARCHAR. So let's validate the same I'll go here and I'll open the tables and companies there is a description for this we have mentioned the description as text, whereas inside the Company entity class for the description, we are using something like String with the @Lob annotation, which means Long object. But what framework is recommending, it is expecting tiny text as a data type for the database column. So as of now, we have mentioned text, but instead it is recommending to use tiny text this time, what I'll do is under the application.properties, instead of mentioning validate, I will mention update. So with the update, what is going to happen? The Hibernate, it is going to update the schema if necessary. So let me mention this value and do the build. Once a build is completed, I'm going to restart my application. So this time you will see that Hibernate, it is taking care of updating our database table as well. So if you scroll up, scroll up, there is an alter command using this alter command, the Hibernate framework, it modified my company's table. So for the description column, it mentioned that data type as tiny text. So if I open my database here and refresh these, you will be able to see this time the description column has tiny text. Since we didn't follow one of the best standard, the Hibernate is trying to enforce that standard. What I'll do is, I'll open the job portal schema file and here, as of now, I have mentioned text. Instead, I'm going to mention tiny text so that both our database table and this script will be in sync. Now, let me show you a scenario where an entire database table will be created by the Hibernate based upon an entity class. So what I'll do is I'll try to create a new entity class under the entity package. This entity class will be Contact. On top of this entity class, I need to mention a couple of annotations. I can copy them from Company entity class. Let me mention the same here. This time, the table name will be contacts. Into this entity class, I'm going to paste a few lines of code you can get this code from the GitHub repo as well. All this code is self-explanatory since we already discussed as part of this Company entity class, you should be able to understand these annotations very easily. Using this id field, I'm trying to represent an Id column, which is going to act as a primary key inside the database. So how we are communicating that by using this @Id and @GeneratedValue, to the @GeneratedValue I'm passing the strategy as IDENTITY strategy. Next, these name, email, user type, subject, and message they all represent the various fields that we have inside these contactors form. In total, the end user is going to submit name, email address, user type, subject, and message. The same we need to store inside the database that's why I have created the corresponding Java fields along with their column names inside the entity class after the message, I am also trying to create a column with the name status. So whenever an end user submitting a contact to message by default, it should have the status as new that's why I'm trying to use an annotation @Column default with the value as new.  Even inside my business logic, if I'm not populating the value to this field, the new default value will be constructed by the JPA framework. Using this status in the coming sections, we are going to build an admin functionality where the admin of the website, he can log in and check all the new messages. Once all of them are handled, the admin can close the contact messages. That functionality we're going to build in the coming sections. Apart from status column, we also going to need some audit related columns like createdAt, createdBy, updatedAt, and updatedBy. Now our entity class is ready this time, when we start our application, a brand new table will come under my database. So far, I have a single database table here let me restart my application but before that, please make sure here, update value is present next to the property. So since there is a mismatch between the entities and database schema, the Hibernate is going to match them by creating a database table. Let me do the build, once the build is completed, I'll restart my application. Once the application started successfully, you will be able to see a new table is created inside my database. If you're not able to see this new table, please make sure you are refreshing the connection so that the new table will be visible. If I open these contacts database table under the columns, you're going to find all the columns based upon the configurations that we have defined in the entity class. I hope you're clear on how to use the property spring.jpa.hibernate.ddl-auto you should be able to use any of these values, but please make sure to use the corresponding value based upon your use case. By default, I recommend you to set this value to none so that the Hibernate is not going to interfere with your database schema. But in case if you're developing some low severity applications, or if you're developing some unit testing application, based upon your use case, you can use any of these values. I'm not showing the demo of create and other valid values because it's going to drop my company's table along with the data, which I don't want, but I hope you understand this concept. What I'll do is I'll set this value to none and comment this entire property because anyway, the default value that it is going to consider with MySQL is none. So now we have a database table ready for the contacts to REST API. As a next step, we can start building a new REST API.