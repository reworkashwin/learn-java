So far, whenever we want to create an entity class representing a database table, we are manually typing the code inside the entity class. But manually creating and typing the entity class will be a cumbersome process in enterprise applications because we're going to have hundreds of tables inside the enterprise applications. That's why inside this lecture, I want to show you a better and a quick approach that you can use to generate the entity classes based upon your database tables. To use this feature, you need to use the IntelliJ Premium version. In case if you are using the free version, don't worry, you can get the code of all the entity classes from the GitHub repo. You never have to type the code manually. But the reason why I want to show you this approach is, in case if you are working in an enterprise organization, there is a good chance that your organization may provide you a premium version of IntelliJ. Though you might be using a free version right now, maybe because you are a student or you are working in an organization where they are not providing the premium version. But in future, there is a good chance that you may end up working the premium version of the IntelliJ or there is a good chance that IntelliJ team may move this premium feature to free version as well. That's why I want to give a heads up around this feature. To get started with the entity generation, you need to go to the settings and look for plugins. Under the plugins, you need to go to the marketplace and install two plugins. So what are those plugins? The first one is JPA Buddy, this I already installed. And the other one is Jakarta EE JPA model. So this is the one, please make sure you are installing this one as well. Since both these plugins, they already installed inside my IntelliJ premium version, I'm not again installing them manually, but please make sure you are installing both these plugins. Once these plugins are installed, you need to go to your application.properties and inside the premium version of IntelliJ, you're going to see this database icon. If you click on this, we know what is going to happen we are going to see the database connection details under which we can see all the list of tables. So here I'm going to right click on this contacts table and select an option, create JPA entities from database. So this option will generate the entity class automatically. But before that, let me delete the current entity class that I have prepared in the previous lecture. So I'm deleting these now I'll go to these database and here I'm selecting the create JPA entities from database, which is going to open this pop-up and here I need to mention the target package. The target package is entity package and I can give the name to my entity class. The class name is Contact. There won't be any parent class. Based upon the columns definition, the JPA buddy plugin, it's going to provide all the details here. So it is saying inside your Contact entity class, I'm going to consider the ID generation strategy as identity, which is the default recommended one. You just have to make sure it is selected. Next coming to the Id column for these, the attribute name inside the entity class will be Id the mapping type will be basic and the attribute data type will be Long. Similarly, for all the columns, you will be able to see what is the corresponding attribute along with the mapping type and the data type of the attribute. I'm going to share more details around this mapping type in the coming lectures. For now, all our fields are going to have basic. In case if we want to build some advanced relationship between multiple tables, we can use some advanced mapping types as well. We're going to explore those concepts in the coming sections for now, all of them will be basic. So please validate all the attribute names and the data type in case if you want to change anything, you should be able to change the value, but in most of the scenarios, we should be fine with whatever recommended by the JPA buddy. As soon as I click on, okay, you will be able to see I got a new Contact entity class. Since we are using Lombok library, it also added @Getter and @Setter annotations. If you're not using Lombok library, it is going to generate the getter and setter methods as well. You can review this enter entity class everything should be fine. We don't have to make any changes. Once the entity class is ready as an extent, what do we want to generate? We want to generate the Repository interface. Even the Repository interfaces, we don't have to create them manually. Whenever you are opening the entity class, you're going to have the JPA designer option. You need to click on the JPA designer few of the features from the JPA designer are available in the free version as well. So here you can click on this plus symbol, which is going to show you an option to generate the Spring Data JPA repository. Similarly, you should be able to generate the DTO classes as well. Let me first generate the Repository interface. I'm going to select the entity name as Contact, but the same, the repository name is recommended as ContactRepository. I'm fine with these. Coming to the parent, I'm going to use the JpaRepository in case if you want to use another parent, you can select them coming to the package, I'll mention it as repository package. As soon as I click on, okay, you will be able to see I got a new interface, which is extending JpaRepository and this repository, it is referring to the Contact entity. Now let me generate the DTO class as well. I'll go to this entity class and select the JPA designer and click on these new actions and here I'm selecting the DTO. So first we need to mention the package where the DTO class needs to be generated. It should be under the DTO package coming to the domain entity, it will be the contact. For this Contact entity, I want to generate a DTO class. I will mention that DTO class name as ContactRequestDto because I want to use this DTO when the UI application is trying to send the request data to my backend REST API. Next I'm going to select the Java record because I want to create this DTO as Java record class. Now we should be able to select all the fields that we want to maintain in that DTO class. Since the UI application will not know about the Id value while it is trying to submit the new request, I don't want to maintain this inside that DTO. Instead I'll try to maintain only email, message, name, subject, and user type. So even the status also, we don't have to accept from the UI because we are going to default this to new value in the backend. By default, the JPA Buddy is going to use the same field name inside your DTO class as well. In case if you want to change it, you can change the field name here. I'm going to click on this OK, which is going to generate a new DTO class as a Java record under the dto package. I hope you are seeing the power of JPA Buddy and JPA designer. Even if you are using the free version, you should be able to use some limited features of the JPA Buddy. From now onwards throughout the course, I'm going to leverage the JPABuddy and JPA designer to generate all the repository layer entity classes and interfaces. This is going to save some time inside the course as well. In case if you are using the free version, you don't have to be demotivated. You can get the code from the GitHub repo and copy them into your project. The details around the JPA Buddy plugin I have documented in this slide for your reference. So this slide has details around what is JPA Buddy plugin and what are the advantages that it is providing for the developers and how to install this plugin.