Once the profile is updated by the end user, the user can go ahead and start applying for the job or save a job as well. Inside the home page, the end user can see all the listed jobs here, he can either open any of these jobs, which will take him to the job detail page. Inside this page, the user is going to have these two options. As a next enhancement, let's try to implement the required backend changes that supports saving or unsaving a job. The end user can save a job from the job detail page or if the end user is present on the jobs page here under the jobs list, we are giving an option to save or unsave the job. To implement the required backend changes, first we need to think about what are the database changes that we need to do to implement this feature. We already have users table and jobs table where we are storing users related information and jobs related information. So if you try to understand the relationship between these two tables, like users and jobs, it will be @ManyToMany relationship. Why? Beacuse a user can save multiple jobs and similarly a single job might have saved by multiple users. Since there is a chance of having multiple relationship from both sides, in this scenario, we need to make sure to use the @ManyToMany JPA relationship out of all the four relationships that we have inside JPA, @ManyToMany is a bit complex one. That's why inside this lecture, I'm going to spend a few minutes explaining you about what is @ManyToMany, how to implement that by using Spring Data JPA, all those details I'm going to explain in detail inside this lecture. As I said before, we need to use @ManyToMany relationship when both sides of the relationships have multiple references to each other. We already saw an example inside our application, a user can mark multiple jobs as favorites and a job can be favorited by multiple users. This is how the  @ManyToMany relationship is going to look like. A user can favorite or can save multiple jobs, vice versa also possible, a job can be marked as favorites by multiple users. I also gave a very common layman scenario as well. Imagine a music streaming app, a single song can be part of many different playlists and a single playlist obviously can contain many different songs. So this both ways relationship can be implemented by using  @ManyToMany relationship. While implementing  @ManyToMany relationship, we need a third table. We already have users table and jobs table, but to store the  @ManyToMany relationship details around these two tables, we need an intermediate table. So this intermediate table, in other words, we can call it as a third table or joint table. Here you may have a question? Why do we need these joint table or middle table? Can't we reuse both these tables to establish the @ManyToMany  relationship? That's a great question. Let me try to answer the same. Let's imagine this table is not present inside your database schema. If a user has multiple favorite jobs, all those multiple job details, where are you going to store? Let's imagine you want to store inside the jobs table itself. Let's imagine a user with the ID 12 saved a job with ID 1 and ID 2. So against these job ID 1 and 2 records, you should also store the user ID detail as well like 12 and 12 here. In future, if some other user like 13 want to save the same job like 1 and 2, then again, you need to make duplicate entries inside the jobs table with the new user ID as 13 and 13. The problem with this approach is, inside all the duplicate rows, the details around the jobs will be same. The only detail that is changing between these duplicate records is the user ID information. The same also happens if you try to implement the saving mechanism in a vice versa manner. For example, if you are trying to store the favorite job ID details inside the users table itself, then you will end up duplicating the same user record again and again, just to store the favorite job ID details. In real enterprise applications, typically tables like users and jobs, they're going to have a good amount of columns like 50 columns, 60 columns, and even 100 columns as well. Just to store the job ID or user ID values, you will end up duplicating the entire record, which has a very similar data under all columns, except one column, which is either user ID or job ID. And that is going to bring a lot of drawbacks into your database. The main problem is you're going to unnecessarily duplicate the number of rows, which will increase the size of your database storage. So that's why to avoid these main problem, when you are trying to implement  @ManyToMany relationship, having a middle table or joint table is mandatory or recommended by the JPA team. As a developer, we have to create a joint table like saved_jobs. Inside this joint table, we should only have two columns. One is representing the user_id, the other one representing job_id. So both these columns, they are going to have foreign key link with the primary key columns of the jobs and users table. When a user trying to save multiple jobs, the user_id is going to be repeated. And for these user_id rows, the corresponding job IDs also will be maintained inside this joint table. So the beauty of the joint table is we end up only maintaining the data strictly related to the relationship. So when a client application as a backend application about to give me the saved jobs by the user ID 12, then the job of the Spring Data JPA is very easy. You just have to come to this table and look for the records with the user ID 12, and against to this 12, the job ID one and two are there. Based upon these job IDs, the actual jobs data will be fetched from the jobs table, and these jobs data will be provided to the client application. The vice versa scenario is also possible. Let's imagine a client application is asking the backend server, give me the list of users who saved a given job. In this scenario, the JPA is going to look for the records with the job ID as one against to these one job ID records, two user IDs are present like 12 and 13. These user details will be fetched from the database and both these two user details will be provided to the client application. I hope you are clear on why this joint table is required while implementing  @ManyToMany relationship. Now let's try to answer a question that you may have, which is, why can't we use @OneToMany relationship? . @OneToMany means a job is marked to favorite to only one user. And that is not going to make sense from our application perspective. That's why in these kinds of scenarios, @OneToMany or  @ManyToOne  is not going to make sense. You should strictly use @ManyToMany relationship, which allows sharing the jobs across multiple users. So this is how the database design is going to look like. There'll be a users table, jobs table and saved_jobs table, which is going to act as a joint table. This joint table connect with both of the main tables by using the foreign keys. When implementing  @ManyToMany relationship, please make sure to use a set, not a List while you're trying to create a field inside an entity class. The reason is very obvious. Using a set, we can avoid duplicate entries. For example, we know a single user can save multiple jobs. So to avoid the scenarios of loading the same job again and again into the entity field, if we define it as a set, the underlying collection data type is going to take care of avoiding duplicate entries. . Now coming to the bridge table, for this bridge table or join table, we don't have to create any Java entity class. JPA handles insertion of data into these join table based upon the @ManyToMany configurations that we define inside the users and jobs entity classes. When defining these @ManyToMany relationship, don't forget the inverse side mapping as well. Always use mappedBy on one side to ensure we have a bidirectional relationship that stays in sync on both entities. Let me show you some code snippets on how to perform these @ManyToMany relationship. To implement these relationship, we need to use two main annotations. The first one is @ManyToMany, and the other one is @JoinTable. So this @JoinTable is specific to @ManyToMany, we can't use this annotation with any of the other JPA relationship. Inside our backend application, the users table is being represented by the job portal user entity class. So this entity class is going to act as a Owning side. Why user is a Owning side? Because it controls insert or delete in saved jobs. So at the end of the day, inside our backend application, the saving of the jobs is going to be initiated by the end user. And since the end user is being represented by using the job portal user, we have to make sure that this is the Owning side in @ManyToMany relationship. So inside the entity class, which is acting as a Owning side, we need to make these configurations. First I need to create a field of type set of job. So the other side entity class is job. So here I'm using set instead of list to avoid the duplicate entries. Please make sure you are initializing the set by using these new HashSet so that for the JPA framework, it is going to be super easy to add or remove the entries from the saved jobs object. On top of this field, first we need to mention @ManyToMany annotation followed by, since this is a owning side, we should use a @JoinTable annotation to this annotation, first we need to provide the name of the join table. So here I have mentioned the table name as saved_ jobs. Apart from the table name, we should also provide the join table column details as well. First by using the joinColumns attribute or property, we need to tell what is the column inside the join table that is going to have a foreign key reference with the job portal user. So the column name is user_id, the same I'm trying to provide, but while we are providing this column name, we need to use again an annotation with the name @JoinColumn. So for this annotation output, we need to assign to the joinColumns. As a nextstep, we should also provide inverseJoinColumns as well. By using these inverseJoin columns, we need to convey what is the column name, which is going to have the foreign key relationship with the jobs table. So that column name is job_id. So here also we need to use the same annotation, the output from this annotation, we need to assign to the inverseJoinColumns. So under the joinColumns, we need to mention the column name, which is maintaining the foreign key relationship with the Owning entity, whereas by using the inverseJoinColumns, we need to mention the column name, which is maintaining the foreign key relationship with the other entity table. So these are the configurations that we need to do on the owning entity side. Coming to the inverse side, which is the job entity class inside this class also, we need to create a field of type of set of job portal user. On top of this field, we need to use @ManyToMany annotation. This time here, we need to use mappedBy attribute to this mappedBy, we need to provide the same field name that we have defined inside the Owning entity, the field name we have defined is saved jobs. The same I'm trying to mention here. After making all these configurations in order to perform various CRUD operations, we need to use this kind of code. Once the user record is fetched from the database for a given user ID, by using that user object, you just have to invoke that getSavedJobs() method, which is going to give you the list of savedJobs object, and inside that set, we need to add a brand new job details. That's why we need to invoke the add() method on top of the set object. Once adding the job, we just have to invoke the save() method, and to this save() method, we need to pass the user entity object. So this line is going to take care of making an entry into the join table. So behind the scenes, when this line is being executed, the Hibernate, it is going to execute this SQL statement. So the user ID information is going to be present inside the user object and job ID details will be present inside these job object. In case if the user is trying to unsave a job, then in that scenario, we just have to invoke the remove() method on top of the saved jobs set object. To this remove() method, we need to pass what is a job that we want to remove from the join table. So when this save is getting executed, the Hibernate, it is going to execute this SQL statement to delete an entry from the join table based upon the given user_id and job_id. So getting saved jobs for a given user is going to be very easy. We just have to fetch the user record based upon a given email. By using these user object, when we invoke that getsavedjobs method, the JPA and the Hibernate, they are going to provide as set of jobs that are associated for a given user. So here the default fetch type will be lazy, which is good. So that's why when this first line is executed, the saved jobs will not be fetched. Only when the developer is asking by invoking these getsaved jobs at this point of time only, the framework is going to look for the saved job records for a given user. As a important bestpractice, please make sure you are avoiding CascadeType.All when you are using @ManyToMany annotation. Because if you are deleting a user that could delete all the associated jobs as well, which is very dangerous in a typical enterprise application. At the end, I want to highlight that we can use these @ManyToMany relationship only when the join table has no extra columns other than two columns representing the pure relationship between the two entities. In case if there is a scenario where inside the join table, if you want to store some extra columns data, then using many to many is not recommended. We're going to discuss that scenario as well in the coming sections. For now, let's focus on the functionality of implementing saved jobs inside our job portal application.