Behind the scenes, I have developed three new backend REST APIs that are going to support save job functionality on the client side. You can refer this code inside the GitHub repo as well. Please try to understand what are the changes that I have done. Once you are clear, please make sure you are building the same REST API, same business logic inside your backend application as well. If you're facing any problem, you can always refer to my code inside the GitHub repo. Under the UserController, the first REST API that I have developed is, save job. This is the path that is going to be supported by this Java method, saved-jobs and job ID will be a path variable followed by job seeker. Since these REST APIs can be invoked only by job seeker, I make sure to mention this job seeker towards the end of the path. And since we are going to insert a new record inside the join table by using this operation, I make sure to use @PostMapping. This method is going to accept job ID as a path variable. Along with that, the Spring security framework will take care of injecting these authentication related objects. . From the authentication, I'm trying to get the current logged in userEmail. And this email along with the job_id, I'm trying to provide as an input to the savejob method inside the service layer. Since this operation is going to create a new record, I have mentioned this @ransactional annotation on top of this method. Next by using this userEmail, I'm trying to get the user record or JobPortalUser object. Once the user is fetched, we should also validate if there is any job is present based upon the given job_id. . For the same, I'm invoking this findById method by using jobRepository interface. If there is a job is present, we are going to continue with the logic. Otherwise we are going to throw a runtime exception. The next line is going to execute the actual core logic of our REST API. So first by using this user, we are invoking the get savedJobs method, which is going to give us the savedJobs object. As of now, since the user does not have any savedJobs, we're going to get a brand new set object. Using the same, we are invoking the add() method and to this add(), we are passing the job entity object that we fetched from the database. Since this job and the user, they both are being tracked inside and Hibernate session, we don't have to invoke the userRepository dot save and to this save, there is no need of providing user object. So the Hibernate framework is going to take care of automatically understand our requirements and accordingly it is going to make an entry into the join table by taking the user_id and job_id from the respective objects. Once the details are saved, I'm trying to send the saved job details as a Dto from this method. So to transform data from job entity to Dto class, we are using these static method that we have under the ApplicationUtility. So whatever Dto we are reaching from these method will go to the controller layer and the same way are reaching to the client application as well with the status as 201. So this REST API is going to take care of saving a job. What about the vice versa operation, which is unsaving a job that is going to take care by the next REST API. The path of this REST API also is same, it's just that this time it is going to use @deleteMapping as we are going to delete an existing record inside the join table. The name of this method is unsavedJob. This method also accept job_id and authentication as an input from this controller method. We are going to call unsaved job method inside the service layer. First we're trying to fetch the user object followed by job object. Once we have both these objects in our hand, using the user object, we are invoking the get saved jobs method, which is going to give us set of saved jobs, which are related to a given user. By using this set object, we are invoking the remove method and to this remove method, we are providing the job object which we fetched from the database based upon the received job_id. This is the line which is going to take care of unsaving the job inside the database. Based upon this logic, the JPA is going to run and delete SQL statement against to the join table. Once the details are deleted, the request will go to the controller layer from the controller method, we're simply returning the status as OK with the message as job unsaved successfully. So these two REST APIs are going to support saving and unsaving, but we are also giving an option to the end user to see the list of saved jobs under the saved jobs page. So if the user click on this, this page has to display all the saved jobs by the end user. To display the data on this page, we need a supporting backend REST API, which is going to return all the saved jobs by an end user. That's why I have developed the third REST API and the path of this REST API is saved-jobs slash jobseeker. Since we are going to read the existing data, I'm using @GetMapping on top of this Java method. This Java method, it is not going to accept anything as an input from the client application, but the Spring Security Framework will inject the authentication object into this method. By using the logged in userEmail, I'm invoking this getSavedJobs() method inside the service layer. So here we are trying to fetch the user object based upon the email. Next by using the same user object, I'm invoking the getSavedJobs() method, which is going to give me set of saved jobs. By using that set, I'm trying to invoke the stream API followed by map() method. And under this map logic for each job object, I'm trying to convert that into a Dto object so that we can send a list of job Dto from this method as an output. So this list is going to be accepted as an output inside the user controller, so whatever we received, the same we are trying to send to the client application. Since we want to expose these three REST APIs only for the jobseeker, what we have to do is, under the PathsConfig, under this jobseekerpath() method, we need to add the two path configurations that we have added around the saved jobs functionality. After making all these changes, please make sure you are building and restarting your application. Let me show you a demo from the client application. What I'll do is, I'll try to log in by using John credentials. So John is a job seeker. As of now, he don't have any saved jobs under his profile. Even inside the join table that we have, there are no records inserted. Now let me go to find jobs menu and here I'll try to click on the save on random job. As soon as I click on this, the job is saved. I got a confirmation message as well. Let me do the same for other jobs as well. So in total, I saved three jobs. The same I will be able to see here if I navigate to the saved jobs path. So these are the three jobs that I saved. We also have other option to save the jobs. Let me go to the homepage and here if I open any of the job and the job detail page also, we have the save job option. Let me click on the same. So from here also, the job saving functionality is working. In total right now, we saved four different jobs. The same I can see under this page. In case if I'm looking to unsave any of the job, I just have to click on this love symbol. But before that, let me check whether there are four records are inserted inside the join table. I'm refreshing these data. As you can see, there are four records are inserted for the user_id 3. So the user_id 3, he saved total four different jobs. These are the job IDs of those jobs. Now let me unsave a job. And with that, the number is reduced to three. It also, if I refresh, I'm going to see only three records. We can also unsave from the job detail page as well. If we go to these view details here, as of now we have saved, but if I click on this button, the job is going to be unsaved. Now the number of saved jobs reduced to two, even inside the database as well, we are going to have only two records. I hope you are clear on the saved job functionality, which we tested by using the UI application. Now let's try to test the same functionality by using the postman as well. Inside the postman first, I make sure to generate the CSRF token followed by, I am logged in by using smith@gmail.com. I've taken the JWT token and under the save job request present inside the job seeker folder here, I'm giving some job ID with the ID 912. If I click on the send button, the job is saved and I should be able to see that as well inside the database. If I reload this page, you'll be able to see for the user ID 17, a new job with the id 912 is saved. . Let's invoke the GetSavedJobs, which is going to fetch all the saved jobs for a given user. And at this Auth, I have provided the job token related to smith. If I click on the send button, I'm going to get the job details, which are saved by the smith. The job has id 912. Now let's try to test the unsaved job operation as well. This is going to use delete mapping, and this is the path I'm passing the 912 as a job ID and at the auth, I make sure to pass the job token and CSRF token as well. If I click on the send, the job is going to be unsaved successfully inside the database, if I try to refresh this time, there should not be any record for the user ID 17. I hope you're clear. Please make sure you're also implementing these back-end REST APIs. Once you're satisfied with your code changes, you can try to test these changes inside your local system, either by using react application or by using postman as well. At any point of time, if you're facing a problem, please compare with my code, or you can also shoot a question to me.