Once the end-user saves his profile data, in the future, he may want to check existing profile data as well. To support that displaying of the profile information to the end-user, we need to build a REST API that uses HTTP GET method. So this is that simple REST API that I have developed. It is using @GetMapping, and this is the path with the version 1.0. The method name is getProfile to this method, the client application, they're not going to send any input, but the Spring security framework, it is going to inject the current authenticated user details. From this, we're trying to fetch the current logged in user email. The same I'm trying to pass as an input to the getProfile method under the UserServiceImpl. Here, by using the userRepository, we're simply trying to fetch the user record for a given email. Inside the job portal user, we already have a mapping with the profile record as well. We have configured OneToOne Mapping based upon this mapping, when we are trying to access the profile information by using this user.getProfile, the associated profile details are also going to be loaded by the Spring Data JPA. If the associated profile object is null, we're going to simply return null. That means the end-user never saved his profile data. Whereas if there is some existing profile data is available, we're going to simply invoke this mapToProfileDto with the boolean value as false. Based upon these boolean value, we are going to execute these else condition. Under this else condition, we are populating the profileDto object without resume related data and profilePicture data. So this updated DTO object will be passed as an output from this getProfile() method. Let's go to the controller class. From this getProfile()method, we're trying to send this profile DTO as a response under the response body. So this REST API is going to help us to display the current profile information. But whatever data that we're trying to send by using this REST API, it is only going to have text fields related data, which means only these text fields will be displayed. But apart from these text fields, we also want to display this picture that is uploaded by the end-user. To display this profile picture, the client application, they will invoke another REST API and this is the one, the method name is getProfilePicture(). Off course, I can try to send the profilePicture under this profile DTO as well, when they are invoking the getProfile() method. The reason I have separated the byte data and the text data into two different REST APIs is, there might be some scenarios in real applications where the client application they may require only the text data, but not the byte data. To support those kinds of scenarios, I'm trying to segregate the get REST APIs to return all the profile information individually. This is the REST API. The path has a picture, which confirms that this REST API is going to return the picture related byte data. From this REST API method, we are simply invoking the getProfilePicture() method available inside the UserServiceImpl. First, we're trying to fetch the user record. If the user record has a profile record, we're going to simply invoke this mapToProfileDto, but this time we are invoking with the true boolean value. And with that, we're going to send the profilePicture byte data as well as part of this DTO. So let's go back to the main method. As you can see, we are returning the profile DTO. Let's go back to the controller. Inside the controller, once we receive the profile DTO object, we are invoking the profilePicture() method to get the byte data of the picture. If these byte data is empty, we're going to simply send this response. Otherwise we're going to send that entire byte data under the response body. But while sending the response body, we should also give enough clues to the client application saying that this time I'm sending the byte data, but not the JSON data. By default, the client applications, they will always assume that the response data has some JSON data. To override that default expectation, we need to provide some clues by sending contentType header inside the response. Here I have created an object of HTTP headers using the same headers object. I'm invoking the setContentType() method. So this method is going to take care of sending a response header with the name content-type. To this method, I need to pass the media type. So this media type we already stored under a field called profilePictureType. That information we are passing as an input to the parseMediaType available under the media type. If you open this method, so this is going to accept the media type as a String. By the time this method is executed, the media type will be parsed as a one of the supported media type value and the updated media type information is going to be available under these media type return value. The same we are trying to parse as an input to the setContentType(). Next we are also invoking setContentLength() method, which is going to populate the response header with the name content-length. Under this, we are parsing the what is the total size of the bytes that we are trying to send. So that length information we can read from this byte array. We just have to invoke picture.length. So under the response, I'm trying to send the byte data as a response body and this headers data and with the status as OK. So this REST API can also be invoked when a employer is trying to look at the list of job applicants that are applied for a given job, so that the employer can see the profile picture of the candidate who applied to a given job. In the similar lines, there is another REST API, which is going to send the resume data to the client application. So under these files, we're going to show what is the resume that is uploaded by the end user. If the end user click on that file name, the client application or the browser, they need to show the content of the resume so that the end user knows which resume he has uploaded into the job portal. To support that operation, we have this REST API, which is going to send the content of the resume file. So this is going to work very similar to the above REST API. So this time from the profile DTO, we're going to look for the resume field. The same resume byte data we're going to send under the response body along with the content-length response headers. This time we're sending an extra response header with the name content disposition form data. By using these response header, we're telling to the client application that we are trying to send a file. Since it is a file, we are also giving instructions to the browser on how this file has to be handled on the client side. With this attachment, I'm telling to the browser, please download this file as an attachment. When someone is trying to look for the content of the file that I'm trying to send, when the attachment is being downloaded and displayed to the end user, we're also telling please use this name as the attachment name. If we are not sending these response header, there is a good chance that the client application, which is a browser, it may display the binary data to the end user. So few of the browsers, they do that without this response header. The same I'm trying to avoid here by sending these response header. I hope you're clear on all the four REST APIs that we have developed to support the My Profile REST API. Please take some time, try to understand all these REST APIs. Make sure you are developing these REST APIs inside your backend application as well. Once you feel you are ready, come to the next lecture, where we can try to test all these four new REST APIs.