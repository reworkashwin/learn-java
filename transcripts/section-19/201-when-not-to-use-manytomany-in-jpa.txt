Apart from saving a job, the end user can also apply for a job. When a user applied to a job, his or her application details will reach to the employer for their review. So that the employer can work on the next action. If you go to any of the job, like here, if I open the job, here there is an option to apply. As soon as I click on this, a pop-up is going to open to enter the cover letter. So the end user has an option to provide a brief cover letter using which he can introduce himself and explain why he is a great fit for this position. But this cover letter is an optional without cover letter also the end user can still submit the application. . To support the submission of the application, we need to develop required REST APIs on the backend side. So first we should build three different REST APIs. The first one that supports submitting an application. The second one that allow the end user to withdraw the application in case if we don't want to continue further, you can simply withdraw the application. The last API is going to help the end user to see what are the list of jobs that he has applied under this page. Before building REST APIs, let me first create a supporting database table. So this time I'm trying to create a database table with the name job_applications. Under this table, I'm going to have all these columns like id, which is going to act as a primary key followed by user_id and job_id, which are going to have foreign key reference with the users table and jobs table. Next we have applied_at column. Under this column, we're going to store the date and timestamp values based upon the time an user is applying for a job. Next we have status column. Under this column, by default, we're going to store pending as a status. So when an end user is applying for a job, the initial status is going to be pending. This status is going to be keep on updating based upon the actions that are being performed by the employer. When an employer reviewed this application and mark for the next phase, the status is going to update it to REVIEWED,followedby SHORTLISTED, INTERVIEWED, OFFERED,REJECTED,WITHDRAWN. So withdrawn is a status which is going to be updated when the end user himself is withdrawing the application. . So this status is visible both for the end user and to the employer. After the status, we have the cover letter. So this cover letter column is an optional column. Since under this, we are going to store a good amount of text data, I'm trying to use text data type here. Next we have notes column as well. And these notes column will be used by the employer for the internal notes. After the notes, we have audit related columns and foreign key related configurations. I'm also trying to create a unique key. The combination of user_id and job_id is going to act as a unique key inside this table. Let me create this table. Once that table is created, next we need to understand what kind of JPA mapping that we can use for this scenario. Here also you may be thinking to use @ManyToMany because a user can apply for multiple jobs and vice versa is also possible. A job might get applied by multiple users. This is a classical scenario of @ManyToMany, but here we can't go with the @ManyToMany because this join table, which is job_applications under these, we are storing a lot of extra information like applied_at status, ID, cover_letter, notes, and audit related columns. So in these kinds of scenarios, as I said before, it is not possible to use @ManyToMany. Let's try to understand why it is not possible. Whenever we are using @ManyToMany configurations internally, the JPA is going to think that the join table exists only to connect to user and job table. So JPA assumes the table has only two columns, user_id, job_id. There is no business meaning, no life cycle of its own for this table. Moreover, we're not going to create any entity class for the join table, but we can't use these kinds of configurations when we have a join table with the extra attributes. As of now, under our join table, we have many other extra columns. When these kinds of extra attributes are present inside your table and these kinds of scenarios, you need to ask yourself a question, where did these columns like status will go? Which entity is going to own it when these columns should get updated? Since there is no place in @ManyToMany to map status notes and other columns, it is not recommended to use @ManyToMany configurations. So here I have given a tabular explanation on which scenarios we need to go for @ManyToMany. So if the join table has a entity class, then we can't use @ManyToMany. We need to go with the other relationships like @OneToMany and @ManyToOne. If there are extra columns, we can't go with them @ManyToMany. If there are some business logic related columns, then it is not possible to handle those columns with the @ManyToMany, auditing related columns are also not possible to get maintained by using @ManyToMany life cycle control is also not possible when we are using @ManyToMany. If you closely look at these explanation, the whole problem on why these are all not possible with the @ManyToMany is, when we are using @ManyToMany, the join table is not going to have any entity class. If there is no entity class, then there is no concept of having extra columns, business logic, auditing, and any other life cycle controls. With this, now you may have a question? If we can't use @ManyToMany, then how to handle this scenario? Let me explain the same. First you need to create an entity class representing your join table, which is job_applications. Inside this entity are going to have all other extra attribute columns. But if you look at the configurations that we need to do on top of the user and job field, these are the configurations. Basically we have to use @ManyToOne JPA relationship because many job application records belongs to a single user. And in the similar lines, many of the job application records, they belongs to a single job. Since the join table is going to have the foreign key columns, we should also make sure to use @JoinColumn annotation. And to this annotation, we need to mention what is the exact column name. We already know about the purpose of these @OnDelete annotation. Long back we discussed about this. Now coming to the configurations that we have to do inside the respective entity classes like Job and JobPortalUser in those entity classes. We need to define the bidirectional mapping configurations, here, since we are using @ManyToOne, then definitely on the other side, the mapping should be @OneToMany. And to this annotation, we need to use mapped by along with the field name that we have defined inside the job application entity class. The same kind of configuration we have to do inside the JobPortalUser entity class as well. We need to make sure to create these fields by using set, whereas inside the job application entity, since they are going to be mapped to a single record here, we have to create the fields as a normal fields. We don't have to use any collection data types. After making all these configurations, saving, deleting, reading, updating the records is going to be very easy. For example, if a user is applying for a job inside the service layer, you have to write this kind of logic. First, you have to create a brand new object of job application into this object, you need to assign user object, job object, followed by you can populate other columns like status. Towards the end, you need to make sure you are invoking the save method by using the jobApplicationRepository interface. Here invoking the save method is mandatory because this application object is a brand new object, which is not being tracked by the hypernetization. In case if the end user is trying to withdraw the application, we just have to define a derived query method inside the Repository interface with the name like deleteByUserIdAndJobId. And that is going to take care of deleting the record inside the join table. Coming to the fetching of the applied jobs for a given user, first, you need to fetch the user record based upon the email. By using this user object, we just have to invoke the getJobApplications, which is going to give us a set object on top of that object, we need to invoke the stream() followed by map(), and towards the end, I'm trying to transfer that data from job application to into a Dto class. And finally, I'm going to return all the collected Dto objects as an output from my service layer. I hope you're clear. If something is not clear, don't worry. When we are seeing the demo of this code, it is going to be more clear for you. Before I try to close this lecture, let me complete that JPA related configurations. First, what I'll do is, I'll try to create a brand new entity for these join table with the name job_applications. This is the class name. I'm going to have BaseEntity as a parent. If you scroll down for the user_id and the job_id, the JPA buddy, it is already recommending to use @ManyToOne. We are fine with these default recommendations. Once you review all these columns, along with the details, you can click on this ok, which is going to generate a brand new entity class. I don't want this attribute overwrites. Let me delete this. Now I need to go to the JobPortalUser and here I can type the @OneToMany configuration. Since I'm using a premium subscription of the IntelliJ, I should be able to update the entity directly from these database window. I need to select users and here I have to select this option under the references, I'm going to check these checkbox. Let me click on ok. And with that, we're going to have @OneToMany configurations. So towards the end of this file, the required configurations are added by the JPA buddy. Let me do the same for other entity as well. I need to go to job.java and here I'm going to click on this database and select the jobs right click followed by I'm going to select this option. Inside this window, I need to select the checkbox and towards the end, if I click on okay, the JPA configurations will be added by the JPA buddy. Towards the end of the file, I have the @OneToMany changes added. Next I need to create a Repository interface as well. For that, I need to go to the job application. After opening this entity class, you can click on the JPA designer and here there is an option to create a Spring Data JPA repository. Inside this window, select the entity as job application. And this will be the name of my Repository. I want this Repository to get stored inside the package with the name repository. As soon as I click on ok, I'm going to have a brand new repository interface. Inside this interface, I'm going to add few derived query methods. All these derived query methods, I'm going to use inside my service layer when I'm building REST APIs. The first one is going to help me to understand if there is a record already present inside this table for a given combination of user_id and job_id. The next method is going to help me to delete and record inside this table for a given user_id and job_id. And the last method is going to help me to fetch all the job applications based upon a given user_id. . Apart from fetching the records, I also want to apply the sorting as well. That's why I'm using OrderByAppliedAtDescending order. Basically, this method is going to help me in reading all the job application records that belongs to a given user in an descending order. Inside the next lecture, let's focus on building required REST APIs for the job application feature.