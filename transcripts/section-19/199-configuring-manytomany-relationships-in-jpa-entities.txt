Inside the schema sql file, just after this profile related create statement, I'm going to add one more create sql statement that is going to help me in creating the join table which is saved jobs. This table, it is going to have only two columns with the name user_idand job_id. So the primary key for this table is the combination of user_id and job_id. This primary key is a composite primary key since it is considering more than one column. Next I also define the foreign key relationship that these columns has, this user ID column, it is having a foreign key relationship with the ID column present inside the users table. In the similar style, the job ID column, it has a reference with the ID column present inside the jobs table. Let me execute this statement. Once it is executed, we can verify whether the table is created by refreshing this and here I'm able to see a brand new table with only two columns, user ID and job ID. As I said before, when you want to use @ManyToMany relationship, we need to make sure that the join table does not have any other columns other than the columns which are going to represent the pure relationship with the other entities like users and jobs table. As a next step, I need to define the @ManyToMany configurations. Let me go to the JobPortalUser and here I can define @ManyToMany manually or what I can do is I can go to these database and select the users and here I have an option create entity attributes from database. If I click on this, it is going to show me what are the references that right now my entity is missing. As you can see under the references, my entity class, it is missing a configuration related to the jobs. So let me select this and here the JPA buddy is recommending me to go with the @ManyToMany. I'm fine with that, let me click on this Ok, and that is going to bring @ManyToMany configurations into my entity class. So just below the profile field, a new field with the name jobs is created. So instead of jobs, what we can name it is we can make it as saved jobs. That is going to give more clarity to us on top of this @ManyToMany has to be used, followed by join table to the @JoinTable annotation, we need to provide what is a join table name. The join table name is saved jobs followed by what are the joint column details, by using the joinColumns and providing the user_id column details followed by by using these inverse joinColumns, I'm trying to provide job_id related column details. We already discussed about these configurations. That's why I'm not explaining in detail about them again here. Now let's go to the job entity class, which is going to maintain the inverse side of the relationship. So here also I can type the configurations manually, or I can go to these database and here by selecting jobs, I can look for the option, create entity attributes from database under the references, I need to select the users. Let me click on ok. And that is going to add @ManyToMany relationship configurations inside these entity class. Here, since the job entity is going to act as an inverse entity, we have to use this mappedBy, and to this mappedBy, we need to provide saved jobs, which is a field name that we have created under the job portal user. To make it more clear, instead of users, let me update the field name to savedByUsers. So these are the only changes that we need to do on the @ManyToMany configurations. We don't have to create any entity class that representing these saved_jobs table behind the scenes, the JPA framework, it can understand the structure of our middle table based upon the joint table configurations that we have provided on the Owning entity side. Inside  the next lecture, let's focus on building REST APIs that are required by my client application to save or unsave a given job.