Behind the scenes, I made all the required changes that are required for the my profile page. You can get this code from the GitHub repo. The reason why I have done this behind the scenes is, I want to focus more on explaining you about new complexities and new concepts, instead of typing all this code and wasting your time I want to spend more time explaining you about the new topics. Since my profile functionality is related to an user, under the UserController, I have developed 4 different REST APIs that are related to profile. Let me go to the very first one, which is going to help the end user to save his or her profile details. So there is a method with the name createOrUpdateProfile. Using this method, we are going to support both creating of a new profile data or updating the existing data. On top of this method, I'm using @PutMapping with this REST API path. The path is profile slash jobseeker. Since this REST API can be invoked only by the jobseeker, I'm making sure that this path is ending with jobseeker. I'll come to this consumes, this is very important. This method, it is going to accept 3 different input data types. If you try to understand the my profile page by using this basic info, we're going to send all this information using text format or JSON format. But coming to the files like PDF and profile picture, they are going to be shared in a different format, which is by using byte array. In these kinds of scenarios where the request is going to have various kinds of data like JSON data, byte data, we need to make sure to use this consumes and mention media type as multi-part form data value. So this is going to tell to the client applications that these REST API is fine to consume multi types of data under the request body. Since we are accepting various kinds of data, this time we can't use the @RequestBody annotation. If you are trying to use the @RequestBody annotation, the request has to come in a single format. It can be a JSON data or it can be a byte data. But right now we are accepting a combination of data. So each of these data we need to extract separately by using these @RequestPart annotation. The name itself is self-explanatory. We are trying to extract a part of the request. So the client application, they have to send entire profile related JSON with the name profile. That's why here to this annotation under the value, I'm mentioning the name as profile. So whatever profile related data that end user is going to send like job title, location. So all these text to fields data, they're going to come in a JSON format. That's why I had given the variable name as profileJson. We can't use requestbody and convert that into a Dto object. We have to do that manually as we are accepting multiple types of data. Requestbody and Dtos can be only used when we are accepting a single type of data like JSON. After the profile information, the client applications, they can also pass profile picture as well. So that @Requestpart, it is going to come under the name profile picture. And since that is going to come as a file or as a byte data, I need to use that data type as multi-part file. So this class represents an uploaded file received in a multi-part request. The file contents are either stored in memory or temporarily on disk. In either case, the user is responsible for copying file contents to a session level or a persistent store as and if desired. The temporary storage will be cleared at the end of the request processing. The client applications, they are going to send these profile picture files tab. Unless they come to this tab and click on this update profile, this resume and profile picture, they are not going to be sent to the backend application. Only these basic information will be always sent to the backend application. That's why for the profile picture, I have mentioned required as fault so that the request can be accepted even in the scenarios where the profile picture is not present. The same applies for resume also. Here also, we need to use the @Requestpart with the required as faults and with the value as resume. And this is also of type multi-part file. As a fourth parameter, I'm trying to inject the authentication object. From this authentication object, I'm trying to retrieve the current logged in user email. This user email, along with all other details like profile JSON, profile picture, resume, I'm trying to pass as an input to the create or update profile method inside the UserServiceImpl class. Since this method is going to update the data or create new data, we need to use this @Transactional annotation. Inside this method first, I'm trying to fetch the current logged in user details by using this method inside the userRepository. There is a change that we need to do inside the job portal user if you go to these entity class, in the previous lecture, we have created profile entity class. Inside this class, we have a OneToOne Mapping with the user entity. These are the configurations. The inverse side of these OneToOne Mapping we need to do inside the job portal user. So inside the job portal user, I have created a field of type profile. On top of this field, I need to mention @OneToOne annotation with the mapped by as user. So inside the profile entity class, the user is being represented by using this user field. The same we need to use here along with the mappedBy property. We discussed about all these mappedBy and mapping configurations long back I hope you remember them. If not, please try to refresh all those concepts by looking at the respective slides that I have shared. After updating the job portal user, if you go back to the UserServiceImpl, here we fetched the job portal user based upon the email. From this user object, I'm trying to fetch the associated profile data for a given user. How to do that? We just have to invoke getProfile() method by using this user object. If there is no profile data associated for a given user, we are going to get this profile object as null. Under such scenario, I am trying to create a brand new object of profile, and to this profile object, I am trying to set the user by invoking setUser() method. So this way, I'm trying to map user and profile data. In case if the profile object is not null, then this if condition is not going to be executed. As a next step, I want to convert this JSON data into a Java object. In all the REST APIs that we have developed so far, we simply used @RequestBody annotation along with the Dto class. Behind the scenes, the Spring Boot is going to do the conversion of JSON to Java objects by using a Jackson library. But right now, we have to do that manually. To perform that conversion from JSON to Java object, first, we need to make sure to add these dependency, the dependency is com.fasterxml.jackson.core and the artifact ID is jackson-databind. So please make sure you are also adding this dependency inside your pom.xml. Now if we go back to the UserServiceImpl, here first I'm trying to create a Object to mapper class. . So this class is present inside the library that we just added. Using this objectMapper object, we need to invoke the readValue() method. To this readValue() method, we need to pass the JSON String as the very first input parameter. Coming to the second input parameter, we need to tell what is the target data type where the data has to be transformed. So here I have mentioned ProfileDto.class. By the time this logic is executed, we're going to get a brand new object of ProfileDto. Inside this Dto, we're going to have all the data that is sent by the UI application, like job title, location, experience level, professional biography, professional website. So this profile picture related information and resume related information, we're not going to get from the UI application as part of this ProfileJson. That's why we're going to process them separately. Once I have ProfileDto with the myProfile first tab, as the next step, I'm going to invoke this mapToProfile private method that I have defined inside this class. To this, I'm passing the profile object that we have here, the profileDto object, profilePicture and resume data. . Let me go to this method. Inside this method, first, I'm trying to copy the data from Dto to profile object by invoking all these methods. This is all the simple code. I hope you don't need any explanation. Now coming to the profilePicture, I'm checking first if the profilePicture is not empty. If yes, then only I'm trying to set all the profilePicture related information into the profile object. So by using this profilePicture.getBytes(), I'm going to get all the bytes data related to that multi-part file. So that bytes data, I'm saving into the profile picture field. Coming to the file name, I'm trying to read that by using this getOriginalFileName(). This name will be saved into the database by using this profilePictureName column. And towards the end, the content type, I'm trying to save as the profilePictureType. Since we are trying to deal with the file, there is a good chance that we may get IO exception. Since this is a checked exception, we need to handle this. If I'm facing any exception, I'm going to throw a runtime exception. In the similar style, we are also going to handle the resume as well. We are trying to read the bytes data and the same we are trying to store into this field. Next we are saving the resume name, resume type. Here also we have handled the IO exception. And towards the end, we are going to return the profile entity object. This updated profile entity object, we're going to pass as an input to the save method of the profileRepository. We know behind the scenes, this is going to take care of either creating a brand new profile record or updating the existing profile record. And towards the end, I'm trying to send the updated information as a Dto to the client application. To transfer the data from entity to Dto, I have developed this mapToProfileDto method. So this method is going to accept profile object and a boolean value. So from this method, I'm passing this boolean as false. I'm going to explain the purpose of this boolean in few seconds. If you go to this method here, by using these boolean, we are trying to identify whether the client application requires binary data as well. When I say binary data, it is related to profilePicture and resume. We should not try to send these binary data unnecessarily to the client application, because that is going to create some performance issue. If all this byte data has to travel over the network, it is going to impact the performance of the application. That's why in the scenarios where the profilePicture and resume related bytes data is not required to share to the client application, under such scenarios, this boolean will be sent as a false, . and if it is false, this else condition is going to be executed, where we are going to create the profileDto record object by passing null for both the profilePicture and resume fields. Whereas if the boolean is true, we are going to send the profilePicture byte data and resume related byte data as part of the Dto. So there are some scenarios where we need to send this information to the client application. I'm going to explain about those scenarios in the coming lectures. Finally, the UserController, it is going to receive the updated data in the form of profile Dto. The same we're trying to send as a response from this method. I know this is super complex because we're trying to use all these new annotations and all these new concepts around the files. So please take some time to digest all this information inside backend applications, whenever the files are involved, we need to make sure to configure these consumes as multi-part form data value. And we should also accept that file information by using these multi-part file. Since this is a job seeker related REST API, what I have done is, under the PathsConfig, this time I have created a new method with the name jobseekerspath. Under this, I have mentioned all the REST API paths related to job seeker. Under the JobPortalSecurityConfig, I have injected the job seeker path bean as a dependency by using these job seekers path here, I'm trying to iterate each path and for each of the path, I'm trying to configure role as JOB_SEEKER, so that only the users with these roles can access these REST APIs. As a next step, we need to understand the next set of REST APIs as well. Let's continue this discussion in the next lecture as well.