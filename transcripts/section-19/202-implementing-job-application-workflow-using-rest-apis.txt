Inside the UserController class, this time, I have developed 3 different REST APIs with the path Job-Applications. By using this very first REST API method, I am going to allow the end user to apply for a job. So this is the entire REST API path. To this method, the client applications, they have to pass the data inside the ApplyJobRequestDto. This is a brand new Dto that I have created behind the scenes. By using this Dto record class, we are simply going to accept two different values. The first one is, what is the jobId of the job to which the user wants to apply. This is a mandatory field. Next, the client applications can also send cover letter data as well. This is an optional data, that's why I have not mentioned any validation related annotations on top of this field. Since I want to perform this @NotNull validation, what I have done is, inside the UserController, before this Dto, I have mentioned both these annotations, @RequestBody and @Valid. Apart from this Dto, the Spring security framework, it is also going to inject authentication object into this REST API method. By taking the logged in user email along with the DTO, we are going to invoke this applyForJobService method. On top of this service method, we need to make sure to mention this @Transactional annotation as we are going to create a brand new record. First I'm trying to fetch the user record based upon the email, followed by I'm reading the job ID from the Dto class by using both the user ID and the job ID. I'm checking if there is already an existing record present inside this table. If yes, I'm going to throw a runtime exception, otherwise I'm going to load the job record from the database based upon the job ID. So both these job object and user object, I have to set into the job application entity object. Here I'm creating a brand new object of this entity class by using the new operator. So this application object is not being tracked by Hibernate session because we created inside our method. Next I'm trying to update various columns like user, job, applied_at, status. So by default, we are going to set the status as pending and coming to the cover letter data, we're going to read that from Dto and assign it and the audit related columns will be taken care by the Spring Data JPA. And towards the end, I need to invoke the save method by using the jobApplicationRepository interface. To the same, I need to pass this entity object and this is going to take care of inserting a new record into the join table. Apart from creating the data into the join table, we may also want to update one important column inside the jobs table. Inside the jobs table, there is a column with the name ApplicationsCount. As of now, for all the jobs that we have created inside our database, they are going to have some random number populated. So that number you need to read and increment it by one. If the employer is creating a brand new job, by default, it is going to have the applications count as zero, regardless of what number we have against your job, we need to first read it and add one value. If it is null or zero, we need to update the number to one. Here I don't have to invoke the same method by using the job repository because the job entity object is loaded here. And since it is loaded from the database, the job entity object will be tracked by the Hibernate session. So automatically the updating of the data into the jobs table will be taken care by the Hibernate. And towards the end, I'm trying to convert all the job application data into a Dto class. So let me open this private method, which I have created inside these service class. Behind the scenes, I also created a brand new Dto class with the name JobApplicationDto. So using this Dto, we're going to expose all the information to the client application, like all the details around the job application table. Along with that, the job information and the userProfile information also, we're going to expose by using these ProfileDto and JobDto. The reason why I'm exposing all these details is, in the coming sections, we're going to introduce one new feature under the employer login where the employer can see who all applied for a given job. When looking at the applicant's data, the employer may want to see the resume of the job seeker and all other details around the job. So that's why I'm trying to send these information as well as part of these JobApplicantsDto. Inside this private method, by taking this job application object, I'm trying to populate all the information like profile information for a given user, if the profile information is present, I'm populating that into these ProfileDto. Similarly next, I'm also populating all other information related to the job application. So while creating an object of these Dto, along with all the details that are going to be present inside the job application table, I'm also passing ProfileDto and JobDto as well. Once this method is executed, the request will go back to the controller. Here we're simply returning 201 status along with the JobApplicationDto. In case if the user is looking to withdraw an application, the next REST API is going to be invoked. To this API, the client application, they should also pass what is the job ID for which the job seeker want to withdraw the application. So based upon this path variable and the logged in user email, we're going to invoke withdrawApplication method present inside the UserServiceImpl. So here first we are trying to load the user record based upon a given email. Next we're checking based upon the given user ID and job ID, if there is any existing record present inside the jobApplicationRepository. If it is not present, then the user cannot withdraw the application that's why we are throwing a runtime exception. In case if this if condition is not satisfied, we're going to delete the entire record by invoking these derived query method with the name deleteByUserIdAndJobId. So here I'm going with an option of deleting an entire record. Instead of deleting an entire record, you can also go with an option of updating the status to withdraw from pending. It's up to you what, how you want to handle the delete. I'm going with the delete option as the employer will get saved from seeing a lot of junk data under his login. Here I also want to highlight in real enterprise applications, in most of the scenarios, the data should never get hard deleted from the database. That means we are never allowed to delete an entire record from the database table. Usually inside enterprise applications, they're going to follow a concept called soft delete. Under each table, they're going to have some switch columns like is active, is deleted. They're going to update those switch columns to yes or no to understand whether the record is soft deleted or not. Please be aware about the soft delete. If you're working in an enterprise project, always make sure you are soft deleting the data, but never try to hard delete. Since we are using the job portal application for training purpose, I'm going with the hard delete option. Once the record is deleted, we should also update the job count inside the jobs table. So that's why I'm fetching the job record based upon the job ID, followed by I'm checking whether the application count is greater than zero. If yes, I'm going to decrement that count by one. And calling the save method by using the job repository is completely optional here. After this logic is executed, the control will go back to userController method. From this method, we're simply returning with the status as ok, along with the message Application withdrawn successfully. The next REST API that I have developed is GetREST API. By using this REST API, the job seeker can fetch all the job details that he has applied. To this method, the client applications, they don't have to pass any input. Based upon this authentication object, we're going to understand who is the logged in user and what is the email. Based upon this email, we're going to call these getJobSeekerApplications() method inside the service layer. Inside this method, first we are loading the user object based upon the user email. By using the same user object, we are invoking the getJobApplications() method. At this point of time, all the job application records that are belongs to this user will be loaded lazily by the Hibernate. So all those records we are trying to stream followed by we are invoking the map() method and inside this map logic, we are invoking these mapToJobApplicationDto() method, which is going to take care of transferring the data from job application entity object to Dto object. The list of Dtos, we're going to return to controller layer inside the controller layer, the same list we're going to return to the client application with the status as 200. These three newly introduced REST API paths as well. We need to configure inside the job seeker paths method. So here I have mentioned two paths. The same path is being used both for @PostMapping and @GetMapping. That's why we have here only two paths instead of three paths. I hope you are clear with all the changes. Please try to understand all these changes. By the time you come to the next lecture, please make sure to complete all these changes inside your backend application. For any questions, you can refer the code present inside the GitHub repo. Inside the next lecture, let's try to test all these three REST APIs, both by using UI application and Postman.