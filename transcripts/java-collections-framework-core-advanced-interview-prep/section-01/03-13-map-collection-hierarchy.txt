Hello guys. Welcome back to our Java Collections Framework course. So in our previous video we have explored collections uh framework hierarchy. And by the end of our previous video I have said that we can consider map also as a collection in Java. So that's it. Today in this video we are going to explore the map collections hierarchy. So if you observe here. So here we have the map interface. And it is completely a different interface. It won't uh extends actual collection interface. It won't extend it. It is a completely different interface in Java. So in Java the map is a part of Java Collections framework, but it is not considered a collection in the strict sense because it is not extending or implementing actual collection interface. Instead, it is a separate interface that provides a way to map keys to values. Whereas a map is conceptually a collection of key value pairs, it doesn't adhere to the same structure as other collections like list, set or queue, which all are the subinterfaces of a collection. So here this distinction is important because map operations and behaviors are different from those of other collections. When we explore this interface in person, then you will get to understand what is the difference between this and the actual collections. So as you can see here. So in map hierarchy the map interface is the root interface. And from it here you can see we have sorted map interface and hash map class. These two are directly implementing and extending it. So here this sorted map is also an interface which extends map interface and hash map is a class which implements map interface, whereas linked hash map is the child for hash map class. So here this linked hash map class extends hash map class and here three map class implements sorted map interface. So let's also verify this hierarchy in our Java. So let me just try to import map here. Import Java dot this map interface also found in util package util dot map. See. Now you can see this map interface won't extend any other interface because in its hierarchy it is the parent route one okay. So as mentioned here let's try to import sorted map. Now it should be implementing. So it should be extending map import Java dot util dot sorted map. So if we open its implementation now here you can see sorted map extends map interface. Got it. And what else we have here? Hash map class input or Java dot util dot hash map. So it is a class here you can see it is a class which extends abstract map and implements map. Here you can see. So it's directly implementing map interface. And it has uh some other interfaces also. And it extends abstract map class which also implements map interface. What else we have here. So linked hash map which extends hash map class. Let's import that one. Also import Java dot util dot linked hash map. See. Public class linked hash map extends hash map. Got it. And the final one we have here is Treemap which extends sorted maps for implements. Sorted map is interface. Right? Import Java dot util dot Treemap. Right. Tree map C tree map implements navigable map. Okay, so here we have another, uh, interface in between these two. Uh, please note it down which is navigable map. And if you open its implementation navigable map which further extends sorted map. So public interface. Navigable map extends sorted map. Okay. So that's the updated one. So just to clarify that, uh let me show my Java version in command prompt. So if you are using older versions then you might be finding some other implementations here. So let me just show you the version of my Java feed type Java version, and you can see I'm currently using Java 11.0 .22, which is 2024 released LTS long term support version. So that's the version I'm currently using. So that's the hierarchy of map collection guys. Although it is not actually a collection in serious terms, but we consider it as collections because it follows all the rules of collection. That's the only reason why we consider map as collection. So that's it for this video guys. Hope you got the clarity by now regarding the hierarchy of collection framework. What are the things we are going to learn in this course is all present in these two. So these all are the things we are going to learn throughout our course in detail and in depth. So that's it for this video guys. Let's meet up in the next lecture. Up till then stay happy and stay secure.