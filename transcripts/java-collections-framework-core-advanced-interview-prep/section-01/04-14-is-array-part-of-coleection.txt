Hello everyone. Welcome back to our Java Collections Framework course. In our previous video we have explored the overview of collection framework. Both. Here we have explored both hierarchies, collection hierarchy and map hierarchy. So while exploring these, I have said that we won't consider array as a collection in Java Collections framework. So today in this video we are going to see why we won't consider arrays as collection in Java. So let's get started. So there are some key differences that set them apart. Arrays and collections. So they are the first and main key difference is flexibility. So as here you can see arrays have a fixed size. We all know that arrays having fixed size means once they are created their size cannot be changed. We all know that, right? While we are creating array, we are supposed to give it a size like this. When we do this for this array, five times of int size is going to allocate to this array five times in size. Let's suppose we have. In Java the size of an int is four bytes right. So when we do this four into five how much. 20 right. So 20 bytes of size is going to allocate it to this array. And this size is fixed. You can't increase or decrease this size in array. So that's the main reason a normal array can be flexible. Whereas in collection can dynamically grow or shrink in size. Which means here in array we can't add new elements into it because its size is fixed, but in collections, let's suppose a. Take an example of ArrayList. So this is the basic syntax for creating an ArrayList. Here I'm creating an ArrayList of integer type. Sorry for my writing, just uh, understand what I am doing here, but that's it. We're gonna see we're gonna explore it more in our further practical videos. Don't worry about it if you don't understand it, just understand what I am going to say here. That's it. So here I'm creating an ArrayList of uh with the name R list, uh equal to new ArrayList class. Okay. So here I have created an ArrayList. So here we haven't declared any size. Here you can observe here we are not declaring any size here because it can dynamically grow or shrink in its size. let's suppose if you want to add something, just we can add it by using the add method RR list dot add. Here we have dot, then add whatever we want to add. We can pass that element right here. So it is a integer right. Uh when we create a list of type integer then we need to pass int values here like 50. So then this 50 will get assigned to will get added to this array list. Got it. And if you want to add more then you can add by again calling this method on this array list. Add hundred and that's it. And you will also get added to it. So this operation you can perform on a normal arrays because they are not flexible. Whereas in collection these collections are flexible you can add elements into it whenever we want, because we know that this collection can dynamically grow or shrink in size, which makes it possible to add new elements into it. Delete already present elements from it. So when we delete elements using the remove method here, I want to remove 50. So when we do this the occupied size by 50. This element size also get cleared from the memory when you do this, which is a big advantage compared to using arrays. Got it. So that's one thing. And here we have some other points to note. Also like functionality, collections provide a rich set of methods for manipulating data such as add remove contents. This method generally would do. If you pass some element in this contents method, then it will check if the first element present in the collection or not. Whatever element you pass in this method, it will check if that element already present in collection or not. If it contains, then it will just simply return true, or if that element won't have in that collection. If it didn't find that element in the collection, then it will just simply return false. But in arrays we have limited built in methods like size, length, etc. and collections also support more complex data structures like lists, sets and maps. So that's the one more advantage of using collections other than just as using simple arrays. And next thing we have here is interfaces. Here collections are part of Java Collections framework. We know that Collections are part of Java Collections Framework, which implements various interfaces like list, set, queue as we have already explored them, and arrays do not implement these interfaces, so they are built separately. So there are they do not implement these interfaces in any way or and are not part of the collection framework. So that's the main reason why we won't consider arrays as collections in Java and type safety. Also, one of the major concerns when we want to consider arrays as collections, because arrays are type safe, which means they can only hold elements of a specific type. So if you create a int type array. So in this array you can only hold the integers right. So that's what type safe means. So in such a declaration you have to only store integers, right? So that's what type safe means. Whereas in collections, collections can hold objects of any type. You are getting it right. So here in collections we can store any type of objects no matter what you are creating. But we can also utilize the type safe functionality in collections also by using the generics. So while creating ArrayList I have created a user generics. Now let me show it to you again ArrayList. So if you want to use a type safe type safety on ArrayList other than using objects, you can just utilize the type specific classes like integer float classes. You have to mention that particular class integer or float. We have float and we have double also And we can use string also here because string is a class, right? So when we use such classes in generics, then we can implement this type safety on our ArrayList or collections, which means here we can only include integers in this ArrayList or no matter what, you name it here. So if you use double here, then you should only include double values. And if you use a string here in generics, then you can use only the string values. Other than this. If you use object, then you can store any type of objects into that particular ArrayList or collection. Got it. So that's the advantage in collections. So here you can see collections can hold objects of any type. But with generics you can enforce type safety. So by using specific type classes here you can enforce this type safety in collection also. Whereas in our arrays you can't ensure the type safety. Sorry, it is only type safety. It's definitely having type safety, which means you won't add any other type of elements into array. If you specify the int, then you have to only include the integer values. If you mentioned care here, which means you are only supposed to store character values. So that's the type safety differentiation in both arrays and collections. So we have another point to consider here. So it is performance. So the main advantage of arrays is these are performance. Arrays can be more efficient in terms of performance for certain operations like fetching an element because it is continuous or it stores the elements in continuous memory location. Right. Which makes it efficient for operations such as searching, etc. whereas in collections You having flexibility, which causes non-collection additional overhead for certain operations to perform efficiently, like searching, because here you need to go through each and every element to reach it. Whereas in the arrays, if you know the location of a particular element, then you can access it directly. Whereas in collections, if you even know the address where it is, then also you need to go through every element before it. That's the one of the disadvantage of using collections rather than arrays. So it's totally dependent upon you to whether to use arrays or collections based on a certain functionality you are implementing in your programming. In some cases, arrays do well than collections like a while we searching elements, etc., whereas in some cases collections would do a better job in cases such as if you want to add new elements into arrays, you want to add a new elements into a collection. And as well as when you concern about your size, you don't know how many elements it can take. You don't have clear knowledge regarding that. Then you can simply use array list so that it will dynamically allocate memory in your program. So that's it guys. We have explored why we won't consider arrays as a collection in Java. So these are the reasons why we won't consider areas collection in Java. And if you consider one advantage in arrays that would be performance. That's the only reason if you want to compromise collections in some situations so that you can perform more efficient operations. So that's it guys. That's it for this video. Let's meet up in the next lecture. Up till then stay happy and stay secure.