Okay, guys, uh, welcome back to our, uh, Java Collections framework course. I'm your instructor, Abdulhakim Sheikh. And today we are going to learn about Iterables. Uh, for each method, how to use it, we are going to see. So in our previous video, we have, uh, explored all of the methods present in iterator. And why do we use iterator? Also, we have explored that okay. And in this iterator we have uh used a for each remaining method. So it is our for each uh method also similar to this, but there is a slight variation. I will tell you that once we have explored the for each method. So before going to explore our uh, iterables for each method, let me just comment, uh, our previous code out. So just commenting this part iterator practice. Now here I'm keeping this, uh, implementation as it is fruits, and in this fruits array list object we have apple, banana and cherry as elements. And first we are printing that elements out say apple banana, cherry. And now we are gonna iterate over each element in our fruits using foreach. So basically for each method in the iterable interface in Java is a default method introduced in Java eight. It allows you to iterate over each element of collection and perform a specified action on each element. Let me show you a quick example to you. So we have this list. Now I wanna just I am just printing for each practice. So here I just added a comment for each practice for each method, uh, which is available in iterable interface for better understanding. Okay. Now let me just, uh, ad for each on our fruits list. Okay. Fruits dot for each. And here we need to perform some action. So here we can iterate our elements in two ways. One is lambda by using lambda expression, and another one is by using the, uh method reference. So first let's explore uh by using lambda expression. By using lambda expression. So here, uh, let me just, uh, give really our fruit. Let me just give a for you it. This is the lambda expression, similar to uh, the code which we have, uh, explored in our previous example. Here you can see in for each uh, remaining. Also in our previous video, we have explored the, uh explored the trait. So exactly. Uh, same implementation. We are doing system dot out dot println. And here I'm just passing fruit. And let me just remove this extra semicolon. Okay. Now if I run this we are going to, uh, get the all the elements like this apple, banana, cherry. So here we are, successfully iterated over, uh, each and every element in our fruits list. Now let's see by using method reference, we can use this method reference in a for each remaining also. Got it. So here I'm just, uh, calling fruits again on fruits. I'm calling for each. And the action would be, uh, we just need to use a system dot out then. Colon. Colon. This is what we call method reference. Okay. Just giving println. That's it. Uh, let me just also print it out. A short printing or in double quotes printing element using lambda expression. And let me change this to elements using method reference. Okay. Now if I run this now you can see different ways to iterate over our collection elements. See printing elements using lambda expression, we will get the same output and printing elements using the method reference. We are getting the same output. Okay, so then what's the difference between this for each and using this for each remaining? So if you observe it clearly here, here we are calling this method on iterator object only. We are calling this method on iterator only. So you can't call a for each remaining on normal list here. This for fridge would work on every collection in our collection framework because it is a method of iterable, and for each remaining is a method of iterator. So it is only work on iterator. Got it for each remaining only works on iterators, whereas our for each will work on all the collections which are available in our collections framework. And one more major difference between a for each and for each remaining is that. So let's suppose let me create a iterator here. Iterator string at equal to fruits dot iterator. Right. So here I called iterator method on this fruit. Now let's suppose if I want to skip the first element we can do that in iterator like a if it dot has. Next okay here I'm just calling it dot next. So by doing this I'm skipping the one element while we're printing, uh, each and every element using it for each remaining L or let me just, uh, system dot out dot print ln. So what do you think what we are going to get now, let me just, uh, print out a message also. Let me give that message as printing using for each remaining after processing iterator. Okay. So I'm giving this message. And if I do, uh, run our program, what do you think what output we are going to get. So let's see. So here you can see banana and cherry. We are only getting because here we have already called it Dot next. So that is why the element has been got skipped from getting printed. This is happening because here we are calling this forage remaining on it. So whenever we call Itnext, the cursor has moved a step further by skipping the first element. Got it. So our cursor's current state at this point is our cursor. Is it? After performing this operation, our cursor will get to the second element, which is banana. That's why the banana is getting printed first, then cherry. And apple is getting ignored because we have already moved a cursor. One step further, one element further. Okay, so that's the benefit of using a for each remaining. And this can be only used on iterator. Okay. And forage can be used on each and every collection directly like fruit start for each. Here we can't call a for each remaining on fruits. Okay fruit start for each remaining see. The method for each remaining is undefined for the type list. Got it. So that's the main difference between for each and for each remaining guys. I hope you got the clarity by now why we use A for each and for each remaining in iterator. So basically the for each method is highly versatile and can be used for a wide range of actions from simple printing, complex transformations, and parallel processing. It leverages the power of a lambda expression and functional programming to make your code more concise and readable. So here I have just used a basic example. When we explore collections even more, we are going to see the very complex versions of your using this for not much complex, but it is pretty much interesting. We will be getting further ahead in our course. That's it for this video. Let's meet up in the next lecture. Up till then, stay happy and stay secure. Bye bye.