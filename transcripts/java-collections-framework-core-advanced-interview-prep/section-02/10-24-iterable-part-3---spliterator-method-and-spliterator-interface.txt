Hello everyone. Welcome back to our Java Collections Framework course. So as we have already explored the methods of iterable interface in which we have three methods such as a iterator for each Unsplit narrator. In our Iterable part two video, we have explored the for each method, and in our Iterable part one video, we have explored the iterator method and uh, its interface, this iterator interface, uh remaining methods. Also we have explored such as as next, what are the methods we have here is in this, uh, interface. Let me keep it down like this one is has next and another one is uh, next method. And we have also explored the remove method from it. And then we have uh, also explored the for each remaining method. It's a small f, sorry. For each remaining method. So these four methods we have explored which is of iterator interfaces methods okay. So as of now we have explored these two methods of iterable. And today in this video we are gonna explore in-depth about this split editor method and its interface iterator. So without further delay, let's dig into and understand our split editor method. So let's explore our split editor method through some key points. Here you can see it creates a split editor over element split editor method. It creates a split editor over the elements. What is mean by this point. So if we explore our method declaration in our iterable interface, let me show it to you. So this is our iterable interface. And so if we scroll it down to end here you can see we have our splitter method in our iterable interface. Got it. So here you can see the return type. We have split editor interface. That's why it is returning a split editor object. That's the meaning of it. It creates a split editor over the elements. Whatever our collection upon. We called this a split editor. When we call it the this split editor method on that particular collection, then it returns a split editor object. That's the meaning of it. So if you observe here carefully, I have previously said that we can't create objects for interfaces. Right. So why here? I'm saying this as a split editor object. because when I mentioned that you can't directly create an object of an interface, you can directly create a mint. You can't initiate an interface using any keyword like we do for other objects, like a new ArrayList like that. We can't do that on interfaces. You can't initiate an interface using the new keyword like you would do with a normal class. However, you can return an object that implements the interface. That's what we are doing here. We are returning an object that implements split elevator interface. Got it. In the case of split elevator method, it returns an instance of class that implements split elevator interface. Right? The method split elevator. Uh, here you can say split elevators. Dot Split. Narrator. Unknown size. In which we are passing iterator on characteristics as zero, which creates and returns an object of an anonymous class. Here we don't see any class there to create object for it. We are just returning an anonymous class object which implements split editor interface. Thus the meaning of it. So this is actually an interface split editor. If you press Ctrl and click on it, you can see here public interface split editor in which also we have lot of methods like tray advanced for each remaining which we are going to explore a bit later in this video. So let's come back to our presentation. So we got our cleared by this point. But this point actually means it creates a split editor or over the elements or the collection. Got it. So let's move to the next key point. Here you can see it is also known as Splittable iterator. You can see its resemblance in its term itself. Split operator Splittable iterator. Got it. It is a combination of two separate terms Splittable and iterator. Combined together we call it as split editor. Okay, now let's move to the next point. So it says it is an interface in Java dot util package. We all know that. And it plays a crucial role in partitioning and traversing elements, partitioning and traversing elements of a particular collection in parallel. Mark this point parallel here. So it basically what it does is it partitions the collections, which means it's split the collection and traverse the elements, which means it iterates the elements of a particular collection in parallel. That's what it means. So here two operations are getting performed in parallel. That's what a splittable iterator do. A splitter would do. You will get to know more about this point when we explore our splitter editor method practically. Okay, so let's see an example first. So to understand this method clearly let me create a new file here with the name split test dot Java. Okay we got the class. Public class iterator test. And let me create a public static void main. So first here let me create an ArrayList List. Integer. So in this list I want to store up to 50 even numbers from 0 to 100. We will be having 50 even numbers, right? So let me name it as even num equal to new ArrayList. Let me import this quickly. Now let me just import it manually. Import Java dot util dot list and import Java dot util dot ArrayList. Okay, so that's it. Now let's add even numbers into it. To do that here I'm running a for loop from I equal to one and I up to I less than or equal to 50. So we only want 50 Elements into it, right? That's why I'm giving condition as I less than or equal to 50. And I'm incrementing the I. So here we need to add perform add operation on our even num array dot add. So here I am just uh doing this I into two. So on first iteration I is one. One into two is a two. It will uh, two will get uh, stored into it. And then when I gets incremented it would be two. Then two into two will get four. Then I will be three. On next iteration I would get incremented to three, then three into two. It would add six into it. Then on next iteration I value would be four. And here we will get four into two as eight. So we will we'll store eight into our event name like that. We store up to 50 even integers. Okay, let's, uh, print it out and see if we are getting these events correctly or not. For that, let me call System.out.println. And in here I'm just printing out our event name. So let's run this. So here you can see we got the even numbers from 2 to 150 even numbers. Okay. So now let me call a splitter on it. Splitter. Eater integer. My splitter eater equal to even num dot splitter eater. Let me just, uh, import this splitter eater interface. Also. We got that here. Okay, so if you explore this split editor interface here, you can see we have different methods to advance for each remaining. Here also we have separate for each remaining and try split estimate size which is we are going to use it now. So here I have Oracle documentation about our split editor. So if you want to read all about this you can just go through this documentation. And please do spend some time on it to understand what exactly is this split editor interface is. Okay. So currently let's just move to the method summary of it. So these are the main methods which we are going to experience somehow in our collections. So these are characteristics estimate size for each remaining get comparator or get exact size info has characteristics trade once and price split. Okay so let's see the estimate size first. So here let me just print out estimate size. What exactly this two is. It just returns a long value. See its return type is set to long. What it does. It returns an estimate of the number of elements that would be encountered by for each remaining traversal. And we call this estimate size. Then it would probably count each and every element in that split narrator object. And it does a by using for each remaining, which means it just count the number of elements which has to be traversed, which are not traversed. It Got it. We already explored this kind of situation in our iterator. Also, for each remaining elements it would count. Keep that in mind. So as we know that it's just a queue the estimated size of our splitted data object. So let me just print it out. System.out.println. Here I am just giving my splitted data or dot estimate size value and it is. Let me call our my iterator object. And then on this object I'm calling estimate size. Now if I run this let's run this. Now here you can see uh, we got all the elements printed out. Then here you can see my splitted data dot estimate size value is 50. Got it right. We have 50 elements in our split editor object. That's why it is giving the 50 here. Okay. We have explored the estimate size. Now let's also explore the for each remaining. Okay. So if you explore the for each remaining declaration in split editor. So here in this we are passing action which is a consumer class object. So for that here let me create a consumer class object. A consumer integer m giving c equal to. Or I can give a equal to action right. Here we need to give action. I'm giving it. Is it like this using lambda expression system dot out dot print length. My Litteratur dot for each remaining and make it equal to. And then let's concat concatenate with our x value. So this is the action. Let me import our consumer class quickly. It isn't showing okay. Let me just import it manually. Import Java dot util dot consumer. So it is in function right. Util dot function dot consumer I think. Yeah it is a this consumer class is present in function package. Okay. Here I have created it a stored action in a. And now I'm calling my split data object my split data dot for each remaining. And here I have just passed the a c by watching this, you should be already cleared. Now what exactly is this a means? So if we explore previous examples in iterable test. So here you can see we have uh parsed like this right. Fruit lambda expression. And sorry it here L lambda expression on System.out.println in which we have passed this element. So in this for each remaining also we are just passing the consumer class object. This whole part is nothing but consumer class object. So if you let me show this to you clearly in iterable in interface we have for each remaining right. See here also we are uh, we are passing an action which is nothing but a consumer class object. So instead of giving this directly, at this place. Here I have created a separate object using consumer and I'm just passing that a reference here. It's the same. Both ways are same here. I have just added one more line. That's it. Okay. Now if I run this run Java now here you can see you can able to iterate over each element. So this is the iterated part iterator part in splitter header okay. See my splitter editor dot for each remaining equal to 246 up to 100. All even numbers as we have explored our for each remaining method. Now let's explore the try advance. Okay, so in try advance. Also if you can see here we are passing action consumer object. Right. And if you read the definition here and let us first look on this return type. It is boolean, which means it either returns true or false. Okay, now let's see the definition. So if a remaining element exists then it performs the given action on it. Whatever action you have given here it performs that action on it. Returning true else false. In some cases, if you haven't passed any action in such cases, it just returns false or it would throw some exception. So let's see it in action. Okay, here I am. Let me call instead of calling for each remaining. Let me just comment this out. Here I'm calling my iterator dot three advance. So here I'm passing the action here. Now if I run this we probably getting the one element. See, this is because we need to keep it under for loop to iterate over each element. If you do like this, we will be getting only one element getting printed. So if you do like this, let me create a for loop here and in it, and let me create a split editor integer. Here I'm creating my split into two equal to even num dot split editor okay. It's a separate split writer created here and now here I'm calling my split editor 2.3 advance. And in that advance I'm passing a. Let me just, uh, comment it out. I don't know why it's, uh, showing, uh, throwing some error here. Okay. We also need to keep a semicolon here also after trade once. Now, if we try to run this, let me set this to screen. Okay. Now if I try to run this now, you would probably see all elements getting iterated. So what's the difference between this and this for each? I mean what's the difference between them. It is nothing. Because if you see the for each remaining implementation. So here instead of using for here we are using while loop. And in that also we are using three advance. If you see here we are just iterating in for each remaining using thread once. Whereas in here we have used a for loop and here we have directly used advanced. We may have used a while loop. Also, there is nothing much difference between both here. In for forest remaining also we are using try advanced itself. Got it. But as uh come to definition here you will be getting a slight difference. A slight difference. See here in try advance. If a remaining element exists, perform the given action on it, returning true if it performed well, else return false. Whereas in for each you can see perform the given action for each remaining element sequentially in current thread until all elements have been processed or the actions throws an exception. So if you open for each remaining method detail here you can see implementation requirement. Here you can see the default implementation repeatedly invokes try advanced until they did uh until it returns false. It should be overridden whenever possible and it throws a null pointer exception if a specified action is null okay. Whereas in try advance which is a boolean or return type which is having boolean return type if a remaining element exists. Performs the given action on it. Return true if it performs a particular action. Else return false if the split reactor is ordered. The action is performed on the next element in encounter order. Exception throws are by the action relayed to the caller. Same point here. You can also see it in this for each remaining if this split order action are performed in encounter order, exception thrown by the action are relayed to the caller C, so there is a no much difference between them. Hope you got the clarity by now. What is a thread once and for each remaining in action? So while exploring these, uh, definitions or documentations, my suggestion is for you is please don't go into every in-depth details into it because if you do, you will get confused. Eventually you will get demotivated to learn these things. So once we explored and we got enough grip on our Java collections framework, then it automatically get into your brain. What exactly the meaning of this definition you have explored here? Okay, so my suggestion is to please come to analyze these definitions once after we have understand enough collections. Okay. So just keep that in mind. So as we have explored advanced for each remaining. Now let's try to understand the try split method. Okay. Just ignore this and just focus on what I mean to say here. So the try split is basically used to split a split elevator collection into Two halves. Two halves. Okay. Split means what? Divide into two halves. Right. Splitting. So the meaning of that method is in its term itself. Okay. Try split. Hair splitting our collection into two halves. Okay. And we can only perform this split method on a split object. Right. Because it is a split elevator method. Okay. Let's see. So here I'm creating a split editor. Alright. Before going to do that let me just comment these things out. We don't need them because we got clear understanding what these two. Okay. Now here I'm creating a split editor. Split editor integer. And here I'm calling I'm giving a name as half Equal to even dot split. I call this now again I'm creating a split data object. Integer. Now I'm giving half to why I'm doing this. I will explain it to you once after. Performing our split action. Okay, now here. Instead of doing the same as before here, I'm. Just doing a slight different. So here I'm calling half and half. I'm calling tri split. Okay. Whenever we do this, our even num collection would be split into two halves. One half would be get. Uh, got stored into this half and another half stored into this half. And you might be thinking I said. Two halves. Right. So first half would be in this and second half would be in this. But that's wrong. So whenever do you perform this split operation on this, then you would probably getting first half in this half half two and second half in this. Okay. Let me just prove this to you. For that let me clear this. And to prove this to you, let me let's also print them out using for each remaining. So if you call half dot for each remaining here instead of doing this I'm calling lambda expression here some dot out dot print ln on here. Or let me just print L okay. Now if I run this you'll probably seeing the numbers from 52. Right. See 5254 up to 100. Which means we got second half values in this half. Okay. So if I do this same here with the half two. Let me comment this out. Now if I run this you'd be seeing first half elements say 246 and up to 50 complete 25 elements. Got it. And let me just, uh, uncomment this one also. Here, let me just print out some message. Also, as in for better understanding, let me just keep this half as this has second half second half and this has first half. Here I'm calling uh split on second half. And this would be second half. Second second half okay. And this would be first half if I run this now, you can see second half values up to 101st half values up to 50. Got it. So that's what a tri split method would do. It splits a split editor collection into two. And by doing this. So whichever object you have called tri split upon, it just keeps the second half into that iterator object. So we have called a tri split on this object right. So just keep that second part of that collection into this object. And it just stores the remaining half I mean first half to the object which we are assigning this operation to. Got it. And these two operations are done parallelly allocating second half and half first half then parallelly. Okay so that's it guys. That's what a tri split method about hope you got the clarity by now. So as per this video, I'm just keeping these methods only. We have more methods which we will gonna eventually exploring in our further collections. Okay, as of now, just try to understand the methods which we have explained it to you here. What are the methods which we have explored here? Estimate size, split data? Uh, it is the terrible interface methods. So it, uh, split methods are these estimate size for each remaining try at once and then try split. So as of now, just keep these, uh, methods and their, uh, functionalities in mind. Okay. So that's it for this video, guys. Let's meet up in the next lecture. Up till then, stay happy and stay secure. Bye bye.