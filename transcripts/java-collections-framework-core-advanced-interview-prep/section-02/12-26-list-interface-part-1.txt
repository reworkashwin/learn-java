Hello everyone. Welcome back to our Java Collections Framework course. In our today's session, we are diving into the fundamental component of Java that plays a crucial role in everyday programming. Yes, we are going to explore the list interface. Whether you are developing a straightforward application or a complex system, understanding how to effectively utilize lists will significantly enhance your coding skills. So let's begin with a fundamental question what is list? Imagine you are organizing a playlist or a event. You want songs in a particular order, right? That's essentially what a list is a collection that maintains a specific sequence of elements. Here we are referring to how the elements are items. You can say that in Java. In Java terminology, we can consider them as objects in this list collection are arranged or accessed. So what is mean by exactly? These are ordered means sequence in a list. The elements are stored in a particular order, which means that if you add element in a certain sequence, like adding them one after the other, they will stay in the order unless you explicitly change it. For example, if you add apple, banana and cherry, apple, banana, and cherry to a list in that order in this sequence, when you retrieve or display the elements, they will appear as apple banana, cherry In the same sequence you have added. What if you do like this? You have. You have added cherry first, then apple, then banana. Then these will appear in the same order which you have added like cherry, apple, banana. Got it. So that's why we call the list as ordered collection. And we are calling here sequence because our list interface, our list collection has index based access. Here index based access means each element in the list is associated with an index like zero, one, two, just like in arrays, which is a numerical position that starts from zero. This index reflects the sequence in which elements are arranged. See. In our example, Apple is at zero index and banana at index one and cherry at index two. This order matters because you can access or modify the elements based on their index. Let's suppose if you want to add or replace this banana with some other fruit. If you want to add watermelon instead of banana here, they can. Then you can use just like. Let's suppose this, uh, collection objects name is fruits. Now, if you want to replace here using the, uh index, then you can just replace it like this. Sorry. It would be like a fruits dot set. Then we need to pass the index. Our apple is at index one right. So here I'm passing index one. Then the element which is watermelon like this. We can use index based addressing on lists. Got it. And list. The collection will also have the consistency in order. What is mean by that? Consistency in order means here the list preserves the order of elements over time. If you don't change the order explicitly, like by adding new elements at a specific position or removing elements, the sequence remains consistent unchanged. Even if you perform operations like add, adding new elements or removing some, the remaining elements will still maintain their relative order. Whatever you add will be added to the end. Why this sequence is important in lists. Because this sequence is crucial when the order of elements affects how they are processed. For example, in a to do list, the order might represent the priority or sequence in which tasks should be completed, write in a playlist. It represents the order of songs to be played. So here the list maintains a specific sequence of elements means that the list keeps the elements in the order you define, and you can access them in that same order, ensuring consistency in how they are stored and retrieved. Now let's try to understand the list interface in Java. List is more than just a collection. The main thing here is it's the part of collection framework which provides the a standardized way to manage group of objects, and the list interface defines an ordered collection, which we have already explored, how it stores the elements and that allows duplicates. This is the new thing we are learning here. About list. lists can have duplicate elements, which means you can add identical elements at different positions within the list. Let's suppose in our previous example we have apple, banana and cherry, right? What about if you want to add one more apple to it then we can add. So this duplicates elements can occur in a list unlike the set interface. So in list we can include duplicate elements. So whereas in sets it is just opposite to lists. Here you can't add duplicates. But in list you can have duplicates. And not only 1 or 2 duplicates. You can have as many as duplicates you want in lists. So now let's explore its implementation through our VS code. So here I have opened our previous example collection test. Java. Let me create another folder to test out our list interface. Okay, so here I'm creating a new folder with the name list interface. Okay. Let me select it. And here let me just create our file list test dot Java list int f test dot Java. Okay. And class test I n t f test. Here let me just import our list so that we can explore it. Import Java dot util dot list. So here you can see we have our list interface which extends our collection interface. And here are some essential methods defined by the list interface that you will frequently encounter. Let's explore them one by one from start. So here we have the first method size. So it probably returns the number of elements in the list. And this the same method we also explored in our collection interface if you remember that. Let me just show it to you. Import Java.util.collection. Right. So if I open collection here also you can see the size which also returns the number of elements in the collection. So here the collection interface is the route interface in the Java collection framework under this collection defines this size method to return the number of elements in any type of collection, whether it is a list, set or queue. Got it. Whereas in our list interface, the list interface, extends the collection interface and inherits the size method. This list interface represents an ordered collection, also known as sequence, and allows potential access and insertion of elements. While both interfaces provide the size method to count elements, the list interface offers additional functionalities like maintaining order of elements and allowing for positional access, which are not guaranteed by the more general collection interface. Got it. Let's just verify that. Here I'm creating a list of integers list equal to new ArrayList. And here let me just add list dot add one list that add two and list dot add three. So if I call a size method on this list list dot size, then you will probably get the size of the list as output. So we should be getting three as output right. And we got that output as three. See. So next we have is empty. Just like in collection it also returns the. That particular list contains elements in it or not. If we have elements in it then we will probably getting false if we have if we don't have any elements, if the list is, uh, null, then you probably getting is empty method as true. And here we have the contents method. Just like in collections, this one also returns whatever object you are passed here. If that object present in the collection which you have called this contents method on, then it probably returns true, if not false. And here we have iterator, which just iterate over the elements in the list in a proper sequence. Whereas in collection here this iterator won't follow sequence. This just returns an iterator over the elements in the collection. There are no guarantees concerning the order in which the elements are returned, unless this collection is instance of some class that provides a guarantee. Got it. Whereas in a list iterator you would be having proper sequence. Next we have two array method. The list interface inherits the two array method from the collection itself. Therefore, the behavior of two array in the list interface is the same as the collection interface. For example, you can convert an list to an array like this. Integer q square brackets and then your array name is equal to. You can just pass that list on upon. You can call two array method. And in the two array you should pass the new integer whatever type you want and just pass integer of zero. And you would probably see that this array having these elements. So two array method in list also works same as in collection which we have already explored. Now we have our add method. So here the add method in both the collection and list interfaces is used to add elements, but there are some difference in their usage and functionality. Let's explore what is it? So in collection interface let me just open it. So here you can see how very sad it is. Yeah. Here we have it. So here you can see this add method. Adds the specified element to the collection and returns true if the collection changed as a result of the call. If not, then it just simply returns false. Okay, now that we know that. Whereas in list inherits this add method from the collection interface and also provides an overloaded version of it. What is mean by overloaded? So if you scroll down a bit, let me just find that overloaded version of this add. So if you can see here we have another method with the return type void. And here we can see just like a. Here we need to pass index and then element. Why we are calling overloaded version of our add method. Because here they both sharing the same name add. And here they have return types variation. If method name is same and the properties are different, like number of arguments are return type, then we can call that method as overloaded function right. So that's the difference. You can see this overloaded version in collection interface. So here we have it. And just add an element to a particular index. It won't remove the element at the previously at that particular index. And just add new element to that index and adjust remaining elements to the above indexes. Got it. Let me just show it to you. Here we have this list right 123. At index zero we have one. At one we have two at the two we have three. So let me just print that list. Now if I run this here you can see we have this One, two, three. Now, if you want to add an element at particular index, then we can just call list dot add. Here we need to pass the index. Let's suppose I want to pass it element at index one. Here I am passing one and element would be our ad is 30. Okay. Now if I run this you can probably see one at index zero and 38 index one and two x index three. Like that. If I run this sorry I need to print it again. A short list after operation ad at index. Now if I give a list like this. And if I run this now here you can probably see 130 and two and three. just added that particular 30 object at index one as we have expected. Whereas in collection you can see that added index in our collection interface, because collection won't follow indexes, only list follows index based values. That is why you can see this extra method in our list interface. Now let's move to our next method which is remove method. And here also we have two types of remove methods. In one we are passing object and this remove method return type is boolean. And if you see another remove method in our list interface which is of type E, and here we need to pass the index. So this remove method just removes the element at specified position in the list, which means whatever index you pass And the list that eliminated that particular index will get removed. And if you use this, uh, remove which is of Boolean type, and here you need to pass the object. So whatever object you pass here that particular object will get removed. Let's verify this. Here we I want to remove that list. Let me give message as System.out.println. Removing element from list. And here I want to remove that element. 30. For that I am using list dot remove. See here we have two removes one is index and one is object. Let's use this object one. Here I'm clicking on the this remove. And here I am passing 30. It is considering as index but it should consider as object remove. Let me just do like this instead. Integer val equal to 30. Now if I run this let's see what happens now. So it just returned true. And I think 30 is got removed. Let's verify it as sort list values after removal. Let me just print out our list. If I run it now now you can see 30 got removed. When I pass direct value here it just considering it as index. So if I pass 30 here it is considering as index. And if I run this you would probably see index out of bound exception. See index out of bound exception. So instead of doing like this, if you want to remove that particular element, store that particular element in a as an object and then pass up that object here. Then this will get considered as object. And that object will get removed. What if we do like this? Is this work still? No, it won't work because the type is different, right? So don't do such mistake thinking. If you pass string as element or object, you would probably see the result. It won't work like this. So hope you got the clarity by now how this both removes work. Let's see the index version of it also. So instead of val here, if I pass zero then you would probably see one is getting removed from the list. But in this you can see 30 231 got removed because at index zero we have one. That's the reason one is getting removed. So let me just set it to well and let's move to the other method of our list interface. Okay. So after this remove we have contains all just like in collection. It just returns true if the collection is available in list. If this list contains all of the elements of the specified collection which we have passed in this, uh, call, then it just probably return true. Otherwise it returns false. And next we have adder, just like in collection interface, and just appends all of the elements in this specified collection to the end of the list. Add the collection to the list in a order. See in the order they are returned by the specified collections iterator. So if I want to add another list to this list. Let me just create that list. List. Integer. List two equal to new ArrayList. And here let me add list two dot. Add 30 list two dot add 40 list two dot add 50. And now if I want to add this list to our list one sorry our list, then I can use our add method like this and call our add method on our list. Wherever you want to add. So here I'm calling this one. And here also we have two methods on these two. Both belongs to Boolean types. Returns a Boolean types. So one is having one parameter and another one having another overloaded idle method having one more parameter, which is a accepting index, which means you can add this collection from a particular index. Got it. So let me just show first one to you. Here I'm just adding list to and let me print out complete list. After adding list two on list. And let me just print out our list again. Now if I run this, you'll probably see complete list after adding list two on list as one, two, three, 30, 40, 50, whereas another idle method. Let me just show it to you instead of printing this. If I want to add this list to elements from index one. So here I will keep this one. And from this 30, 40, 50. And then you will probably seeing two and three in the last. So let me just do like that add all. And here I'm passing one and in list two. So I'm passing list two here as collection. And if you observe this if I run this I would probably see 130 4050 and two three. So that is the difference between this ad and the ad which we have used previous. So this ad is the overloaded method of that particular error. Hope you got the clarity by now what exactly the difference between these two adults. Okay, now let's move to another method which is at all, which we have just explored now. And now we have remove all. So whatever collection you pass in this demo are as simple as that. That particular collection will get removed. Got it. So what if if we have duplicate elements, then the duplicates also will get removed or just the elements get removed? Let's suppose if we have. Let me show this through an example. Now you can see in our list we have 130 4052 three. Right. Let me just add 32 times list two dot, add another 30. And let me just do this for uh how many 30s we have now. Total 530. So if I run this now, you can see for that is one, two three. Four five. Got it. Now, if I do remove all or let me just create another list. List three. List. Integer listed three. Equal to new array list. List three dot add. Let me keep 13. This also list three dot add 40. Let's just keep these two values and let me call remove all on our list list dot remove all on here. I'm passing the collection list to three and let me just print out list values after removing all operation. And let me just to print a list again. So if I run this now, you would probably see 130 and 40 got removed. Sorry. We need to use remove. All right. My bad, my bad. Remove all. Let me just pass. Collection three. And let me just, uh, keep this in a sort for status removing all. Let me keep it as list one dot. Remove all and list three. Now let me run this. And here you can see the operation removal is true. And if you see all duplicates are got removed. Only remaining 152 three. See all five times 30 got removed from the list. So that's the remove all for you. We already have explored this removal in collection framework also. Okay, now let's move to the next method which is retain all. So this one also we have explored in our previous collection, which retains only the elements in the list that are contained in the specified collection. Simple. Just a perform an intersection operation. Just print only common elements. Let me show it to you in list three. We have 30 and 40. In list one we have 30, 40 and other elements also. Let me keep one more element which is not in our list one as of now, which is 41. So if I call retain, retain all on list one and list three, let me just call a retain all on list one and pass list three. Then you would probably see the common elements which list one and list three having as 30 and 40 and remaining. Everything will be getting removed if you perform written all operation. Let me just keep it in assert and let me give a message as performing retain all operation. Let me just print it. Status and calling list one dot retain all and let me just pass all list three. So if I print that list one now list values after retaining all from list three. Okay. Now if I print this list one. Let's see. So here you can see list values after retaining all from list three, which are empty now because we have already removed 30 and 40 here. Right. So to see the results, we need to stop this removal operation before. So for that, let me just comment these things out. Okay. Now if I perform this you would probably see the results. See performing retain all operation. Uh, it got true because it found the common elements. And now you can see the duplicates also. 35 times and 41. So that's what a retain all method perform the operation. It won't bundle up these 30s as one and just returns 30 and 40 as similar element. Instead it just returns the duplicates also. So you need to keep that in mind about retain all method. How it's working. Okay, now let's move to the next method which is replace all. So here the replace all method in the list interface is a default method introduced in Java eight. It is used to replace each element of the list, with the result of applying the given operator to the element. What is mean by that? So let's break down how it works. So here you can see default void replace all. Here uh we have a parameter which is of unary operator type which is a interface unary operator, which is a functional interface that takes a single argument of type E and returns the result of the same type. So this operator is applied to each element of the list. So here you can see objects dot a require not null operator. Here we are passing this operator in a require non null because this line ensures that the provided operator is not null if it is null, a null pointer exception is thrown. Okay. And then we have list iterator and l I and we are calling Listiterator on this. So what is mean by that? A list iterator is obtained for the list, and this iterator allows bidirectional traversal of the list and modification of elements. We will see what is mean by that. In short okay. And here you can see while l I dot has. Next we are also going to explore this list iterator Uh, method. So once we explore this, you will probably see what, uh, this here actually doing. Okay. So as you can see, L'édit has next, which means this loop iterates over each element in the list until it finds until it reaches the end of that particular list. And here we are setting, uh, for each element, the operator is applied to the current element, which is la dot next. And the result is set back into the list using list dot set. So this is how this, uh, replace all function works for better understanding. Let me just keep this, uh, separate replace all example dot Java SVM. And let me just create a list of string and list equal to. Let me just import it and showing an error. Quick fix. Import list Java dot retail. Got it. Okay, now error list. And in this error list I want to pass arrays dot as list. So we can pass arrays like this a b c okay. Here I have passes past this a, b, c as a list in this array list. And we have a, b, c elements in this list. As of now instead of doing a list dot, add a list dot add b list dot add c. I have just done like this. And now if you want to perform replace all. So currently these are in smaller case alphabets right. A b c I just want to turn this to uppercase. Now I can use replace all on this list dot replace all. And here I can just do this string colon colon to uppercase. So if I perform this now if I print our list let me just print before also list list before replace all. Let me just print that list and list after replace all one list. Let me keep replace all here also. Okay. Now if I run this now you would probably see. And before we are having a, B, c and after replacing and performing this operator now you can see we have a, b, c. So how do I know this? Operator so as of now just remember that we have some operators we can perform on our replace all method. Regarding these operators we will discuss in another video. So as of now just focus on the replace all functionality. So when we pass this operator in our replace all and it just checks that given list on operator first it just checking the provided operator is not null and doesn't. Is it having elements or not. So if it found null then it just simply returns an exception as unsupported operation exception. And if it found elements then it just iterate over it using list iterator and on that list iterator l I just like a iterator in our collection and iterable if it has next element, then it just simply sets the operator like operator dot apply lr dot next whatever operator you have passed. Like in our case here I have passed two uppercase. So it just setting that operator on the element. And here we are getting that particular element using Dot next. And we are setting that particular operator on returned element from la dot next using this LA dot set. So that's how we are replacing these elements to this using an operator. So that's how our replace all method works. Hope you got the clarity by now. Now let's move to the next method which is sort. So by name you can say that this method sorts the list according to the order introduced by specified comparator. The sort method in the list interface is used to sort the elements of the list according to the order specified by the comparator. So if you read the, you can read this also here. This method was introduced in Java eight and provides a convenient way to sort lists in a place. Let's see an example. Here, let me create a list I n t of sorting dot Java. Okay. Let me call PS vm here and let me copy this thing here also. And instead of ABC let me pass banana apple cherry. Now if I call list dot sort by default. If you pass null here it will sort the elements in natural order which means A, b, c like that. Let me just print a list before. Sorting list before. Sorting and list after sorting. Now, if I run this and before you can see the sequence as we have defined banana, apple, cherry and after sorting you can see the sequence has ordered as a, b, c. So which is a natural order. You can see on elements. So what else we can have. Now let's explore sorting with custom comparator. Like let me just print out. Sort or sorting in natural order. And let me comment this part as. Sorting in natural order. Okay. So this is how we can, uh, we can sort in natural order. Now let me just comment this part. And now let me introduce you sorting with custom comparator like a. Let's use a reverse order sorting with custom comparator and sorting in reverse order. Now let me call list dot sort. And here I'm passing comparator dot reverse order. So here we need to call this comparator. What is it. Is it a class or interface. It is an interface. See here on this comparator we have different methods which we are going to explore in some other video. And here you can see we have a reverse order in it. So if you see here we have reverse order as static. That's why we are able to call it like this using interface interface dot reverse order. We can call such methods only if they are static in this way using class name or interface name. So if methods are not static, then you would probably creating an object and from through those objects only you can call those instance methods. But static methods you can call directly like this using class names or interface names. So here I'm sorting it in reverse order. Now if I print out the list now you can see list after sorting in reverse order list. Let me just copy this part here and let me paste after this list before sorting in reverse order. Now if I print this, if I run this program, you would probably see list before sorting in reverse order is banana, apple, cherry and lift a list after sorting in reverse order. Then you would probably see cherry. Banana, apple. What it does do is it just first sort out the elements. Apple, banana, cherry and then it reverse them in alphabetical order. It won't just give reverse to this. If you're thinking like a. If you perform reverse order then you would probably getting cherry apple banana. You won't get like that. It just first sort the elements. Then just a do the reverse on the sorted elements. Cherry. Banana. Apple. You can see the sequence here. Alphabetical sequence here. Read from reverse a, b, c, c. So here you can see if you observe here carefully in place of sort method in the list interface. This method modifies the list in-place, meaning the original list is sorted and no new list is created and just sorted on that particular list itself. It won't create another list. Like if you want to sort arrays, then you'd probably going to create a new array, right? For sorted elements. Unlike that, you would probably see these elements getting sorted in that particular list itself in collection framework. So here we are providing comparator, which means here you can provide a custom comparator to define the sorting order. If no comparator is provided, which is null. Here you need to pass null if you want a pass null. Also, you would be getting error there. So if you won't pass comparator then you'd probably be passing null, which is the natural ordering of the element. So in summary for a sorting, you can say that this sort method in the list interface is a powerful and flexible way to sort lists using either natural ordering or custom comparators. So as of now we have explored until replace sort method in our list. Okay. And remaining will be exploring in our part two of list interface. So that's it for this video guys. Hope you got the clarity by now on the methods which we have explored until now in our list interface. And we will also going to explore them once again when we exploring our list implementations like array list, link list, etc.. So don't worry about them if you don't get these methods as of now. So my suggestion is here is just focus on what is going on and how these things are returning output. So that's it for this video. Let's meet up in the next lecture which is probably going to be List Interface Part two, in which we are going to explore all of the remaining methods. Okay.