Hey everyone, welcome back to our Java Collections Framework course. Today we are diving even more into the world of Java collections. Specifically, we are targeting the map interface today. So whether you are a beginner or an experienced developer, understanding map is crucial for effective Java programming. So without further delay, let's get started. First things first, what is a map in Java? In simple terms, a map is a collection that maps keys to values. It allows you to store data in key value pairs, making it perfect for situations where you need to look up values based on some keys. So for example, here you can see we have some keys. And for each and every key we have a value for us key we have value as United States. And for Burke we have value as Brazil. And for key we have value as Spain. So this is the format how we are going to store data into our maps. Now let's have a look at our map interface overview. So the map interface is part of Java.util package. Just like the other interfaces in collection framework such as list, set and queue. However, unlike other collections like list set, queue map does not extend the collection interface. Rather, it follows its own hierarchy in which map interface is the root interface, and this interface also provides its own set of methods tailored to handle key value pairs. Now let's talk about some of the key methods in the map interface that you will be using frequently. So here are a few essential ones, such as put method in which we are passing two arguments. One is key and one is value. So it probably adds a key value pair to the map interface or map collection. Next method we have is get method in which we are passing an object type key, so it probably returns the value associated with the specified key. So first it checks for the key on our map collection. If it finds one then it probably returns. That key is associated value as written. And next we have remove method in which also we are passing an object type key. So it probably removes the key value pair for the specified key. Whatever key you have passed here. And just find that key and removes the entire key value pair from our map. A next method we have here is contains key. So in which also we are passing an object type key. So what it does is it just checks if the map contains a specified key or not. Whatever key you have passed here, it just finds for that particular key on our map collection. If it finds then it just returns true or false. Our next method we have here is contains value. So similar to contains key and as well as unlikely to this method. It just checks for if the map contains a specified value or not. Instead of finding key it just finds for value on our map collection. So if it finds that particular value then it just returns true or false. And next we have size method. So based on its name you can say that it just returns the number of key value pairs in our map collection. And next we have Isempty method. So it probably checks if the map is empty or not. If it is empty, then it returns true or false, as simple as that. And finally we have clear method. So it probably removes all the key value pairs from our map collection. So these are the main essential methods you have to keep an eye on while working with map collections. So hope you got the clarity by now. What are these methods do? So don't worry if you don't understand these as of now, because we are going to see these methods in action in a bit. Now let's see these methods in action. I'm going to use a hash map, which is one of the most commonly used implementations of the map interface. A hash map is a hash table based the implementation that allows for fast access to data. So let's explore it through an example. Let me create a new folder for it. So here I'm creating a new folder with name Mapinterface. Let's set up this folder. Let me give a name as map I n t r f s test dot Java and let me do necessary things here. So as we all know mapinterface is results in package java.util right? So let's import it Java.util.map. So this is interface. We need an implementation also to explore our example. For that let me import our HashMap which is also results in Java.util okay. So here I have imported these two. Now let's explore our example. So here I'm giving map and for k I'm going to set type as integer. And for value I'm going to give a string. So this is how I want to store my data in this map collection, let's give map equal to new HashMap. Okay, so that's the basic declaration of a map. Now to add map we have a method known as put in which we are going to pass a key and a value. Let me give key as one and value as Apple. And let me add one more thing. Map that put key as two and value as banana. And let me do this again. He has three and value as three. So these three things I have added into our map collection. And this is the basic way to add data into our collection. Let me give a comment as adding key value pairs in map. Let me print this map and show it to you how it looks like. So if I run this now, you can see key as one and value as apple. So in between we have equal to symbol to equal to banana and three equal to three. So this is how our map is going to store these values store data. And as I said we can retrieve a value using get method right for that. Let me show an example. Retrieve a value. Let me give a System.out.println. And in here value for key two is what is the value for key two. It is banana right. C for key two we have banana. And let's just get that using map dot get. Let's pass two as a key. And let me run this. Now you can probably see we will be getting banana there. See we got banana value for key two is banana. And if you want to remove a key value pair then we have a method known as remove right. Let's also explore it. Remove key value pair. For that let's use map dot remove. And in remove we need to pass a key. Right. Let's give three. It should remove our cherry. So after removing. Let me just print out our map. Map after removal. Let me just print out our map. So if I run this now, you can probably see our cherry got removed. And to check if the key exists in our map or not, we have a method known as contains key. Right? Let's also explore it. Check if a exists. For that let me give a short contains key three. If contains. We'll be getting true. Or if it doesn't contain this key, we'll be getting false as a value. Let me just call our map dot contains key method and let me pass our key value as three. So just we have removed this three right. So if we run this we'll probably getting false. As a result. If I run this I can probably see contains key three false. What about key two. Le Figaro Key2 here. And let's also pass Key2 here. And if I run this now, you can probably see through there because our Key2 is still there, right? So that's why we are getting true. Let's check if the value exists or not. To check value we have method such as map dot contains value right. So let's also explore it. Assert contains value. Contains value theory or not. Let me keep question mark there. And here also doesn't matter but looks good. And let me call our method on map. Map dot contains value. Let me pass value as cherry. Okay so if I run this we'll probably getting result as false. Say contains value cherry false. Because we have just removed that whole key value pair here. So that's why we are getting false if we try to access banana instead. Let me give banana here. And if I run this now, you can probably see the result as true. Contains value banana, which proves our point. And we have next method size. Right. So to check size of the map number of key value pairs in it we have a method known as size. For that let me give System.out.println. And in it we add messages map size. Let me call our size method on our map. So if I run this our program you can probably see size of the map is getting added to our output. Map size is currently two because currently we have only two key value pairs such as. For key one, we have value as Apple and for key two we have value as banana. So these two are the two key value pairs. That's why we are getting size as two. And to clear the map we have a method known as clear right. So if we do this map dot clear. Now if I print our map again. So if I run this now, you can probably see our map has empty. Right. So that's how this clear method works. Now let's explore one more method. So it is empty method to check if the map is empty or not. Let's also explore. It. Is map empty? If it is empty will probably getting result as true. Map dot is empty, right? Okay, if map is currently not empty then we will probably getting value as false. So if I run this now, you'll probably getting result as true, right. Because we have cleared our map here. So currently our map is empty. If I run this now, you can probably see that true as the answer here is map empty? Yes it's true. What happens if I comment out this clear method? Okay, so if I run this we can probably see result as false because as of now map is containing some key value pairs. Pace these two, right. Apple and banana. So these all are the necessary methods you should remember while working on our map. So that's it for this video guys. We'll be gonna explore more about our map interface while exploring its implementations. So don't worry. So if you open this maps implementation here, you can probably see there are a lot of methods which we aren't discussed as of now, such as you can see here we have put all method key set method values. Method entry set get value set value equals hash code select. There are a lot of methods which we are gonna explore while exploring our map interface implementation, so don't worry about them as of now. So as of now, just remember these methods on how to use them while working on our map collections. So that's it for this video guys. Hope you got the clarity by now. Thank you so much for watching and I will see you in the next one.