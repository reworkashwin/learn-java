Hello everyone. Welcome back to our Java Collections Framework course. I am your instructor, Abdul Hakim Sheikh. And today in this video we are going to learn about iterable interface. So to put it simply, what this iterable interface do. Here you can see uh, just keep these two things in mind. It navigate collections. Here you can see the slogan iterable interface navigating collections one element at a time. Let's suppose we have a collection. Uh, something like this. We have some objects or elements in it, so it does navigate any collection. It may be ArrayList, it may be Hashset, or it may be theoretically anything, uh, any data structure it supports iterable interface implementation classes. We have, we consider, uh, which we consider as collections in our collection framework. So this iterable interface, this iterable can navigate through those collections. And it navigate in such a way like one element at a time. Like this. It navigates through the collection. So just as of now, just keeping this in mind. And now let's forward in our video to understand it in a better way. Okay so iterable here. You can read it clearly. The iterable interface. The iterable interface is fundamental part of Java collection, fundamental part of Java collections framework. And it allows objects to be easily iterated over. So if you see here it is the fundamental and frontmost interface in collection framework. So that's why we are calling it as the fundamental part of Java collection Collections framework, and it allows objects to be easily iterated over. So no matter which objects you are using, you have created if it is ArrayList object. Linked list object vectors or stack array. Dequeue tree set. Linked hash set has set. And priority queue. No matter which object, you have created a collection object. Uh, this iterable interface allow that to be easily iterated over. What does this exactly mean? So this iterable interface represents a data structure. So these are data structures. Uh, it actually represent, uh, data. This data structures that can be uh, these can be iterated over. Actually it provides a method that produce an iterator. Let me show this, uh, method to you clearly. So let me just show it to you here. So here I have created a, uh, file with the name iterable test dot Java. So here I'm just, uh, importing our iterable interface. Import Java dot lang package. Right. It is present in. Yes it rubber. Yeah. So if I, uh, press control and click on this iterable, we can see its implementation. Now here you can see public interface as iterable. And it is providing an iterator method. See iterator. So here you can see returns an iterator over elements of type t. So that is why we are calling uh we are saying it as it provides an iterator. So what does this, uh, type T actually means. Here you can see the definition for it. And the uh param type T the type of elements returned by the iterator. So it is uh basically a generic type. T means if you mention a in your collection, if you mention integer type there in generics, then it is probably an integer. And if you mention float there, then it is probably a float. If you won't mention anything then it is. By default it is a type of object. It returns the object type iterator. So here we are. This iterator is another interface. If I open this uh this iterator. In this iterator you can see we have a method like Hasnext. And we also have next. Basically when you create a iterator using a collection uh, let's suppose let me just show you. Iterator uh string I t equal to this is an iterator object okay. I t equal to fruits dot. Let's suppose this is a collection which we have created using ArrayList. List and I'm calling a iterator method. This iterator method on this fruits dot iterator. Let me erase this iterator string. It let me enter here it equal to fruits or dot. I'm calling iterator on this collection. So in this fruits let's suppose we have. This is a fruits array. And this we have apple banana and cherry. When we create a when we call iterator on this method sorry this collection these elements are stacked up on a will get stored in this IT iterator object which is created using the iterator interface which has the reference of a iterator interface. And by doing this we can add a. You can utilize some methods of iterator on it, such as as I have show it to you already has next, which is a boolean. And next. By using this we can move to next element in this particular collection, object it and using hash. Next we can verify that after the the current element does the we have an next element or not. It just if we have. Let's suppose this is right. Apple banana. Cherry apple. Banana. Cherry. So currently it is on when you call next method on this it the iterator would be on this element a you are on this element. And when you call has next on this it then it probably checks for next element. So currently we have the next element which is banana. Then it just returns the true value True. Okay. And then again, if you call next, then it will, uh, move this, uh, iteration to this. Now, currently we are having BL mint in it. Now if you call hash next then it will check if we have another element in our iterator. Uh, or not. So we have cherry in it, which means it again returns true. Now if you call uh next again this cherry will be the current element. Okay. Now if you perform hash next on this element, then it will check for another element in the same collection, which, uh, we haven't anything here. So then it just, uh, simply returns false, which means, uh, it doesn't find any element to the next, uh, to this element. So as simple as that, uh, this is how it works. And if you call, uh, next on this element. You won't find anything again, then it probably, uh, just give some error or some warning, which we will gonna explore. In short. So this is how this, uh, iterator works. Let me show it to you again. So here you can see we have the hash, uh, next and next on the iterator interface, not iterable. Observe it clearly it is a separate interface which we have created a method using this interface in iterable interface. Just remember this thing. These two are different. Iterable and iterator are both different uh interfaces. So in iterable interface we just created a iterator method using the iterator type okay. So if we convert our uh collections to this iterator object. So this uh iterator type then we can use these two methods on those, these methods no matter uh these two methods like a hash? Next. Next. We can also use a remove method on our iterator. On our iterator and we also have the for each remaining also. This method we can also use on our iterator. So how many methods we have on iterator one. Two. And this is three and four. So almost we have four methods we can perform on our iterators. According to your requirement you can use these methods accordingly. Okay. So that's about a iterator interface methods. Let me show this to you through an example. So here let's create a collection using list string for you. It's fruits equal to new. Array list. Wait a minute. Uh, I need to do this, uh, in our main method. Let me just call my main method. Yes. VM. Public static void main string args. Okay, so init I'm going to create our collection uh list. So when I press tab on the list you can see this uh list interface is automatically got uh imported by our VS code. Let me show this to you. If you, uh, here I'm just typing l I yes. So I'm pressing Tab here. Now, you can see this, uh, list is getting imported automatically. So if you won't get this automatically, then you can, uh, manually import this list interface. Okay. Just keep that in mind. So here I'm creating a list of, uh, string fruits equal to new array list. Okay. And in this fruits I'm adding, uh, fruits dot add apple fruits dot add banana and then fruits dot add cherry. Okay I have added these three elements in it. Now let's create a iterator. Uh let's call our uh iterator method on these fruits. Or before that let me just, uh, run this and show it to you the output, how we are getting it. For that, let me just print it out in our System.out.println. So here I'm running my Java program. Now you can see the output apple banana cherry. These three elements we are having in our fruits list. Now, let me just, uh, create a iterator. Iterator. Okay. String I t equal to root. Start iterator I'm just calling this method on this, uh, iterator. Now. You can use a loop here. While loop condition would be like a, uh, if we have the next element, then we can continue this loop. We have method. Right. It has. Next we can use that uh, in our while loop here you can also use it in for loop. So just this method's purpose is to let me show this to you clearly. Again it is a boolean. It returns true if it has the more elements. If it doesn't find any elements, then it simply returns false. As simple as that Okay, so if it has the next element, then only this while loop is going to be run. Okay. Now we can, uh, just print it out. The element containing in the I t using Itnext method. See? So if I run it again now you can see apple, banana, cherry. We are able to iterate on this list one by one. As I have said it earlier, while starting over this starting this video on the introduction part. So this is a iterator. A iterable is a navigating collection, one element at a time. This is the meaning of that. It iterates over one element at a time. Got it. What are the other methods we have here? We can remove some element from our iterator. So actually when we are going to call our it, It's a remove on this, uh, iterator. It's actually going to be reflected over on our fruits collection. So when we remove our element using this, it's remove then it probably going to that a particular element is probably going to be removed from the fruits. Let's see if it works or not. So here let me create a string string with the name next fruit equal to it dot next. So as of now let me just comment it comment this out. Why I am commenting that out I will uh, say it to you after removing an element. Okay, so here I'm setting a condition. If banana cricket banana dot equals, uh, the next fruit, then what we are gonna do, we are going to just remove that Eliminate it. Remove. Now let's verify by printing routes here. Let me clear this screen here. Okay. Now, if I, uh, run my Java program. Now, you can see on start, uh, when we print this, uh, fruits, it will be like this apple, banana, cherry. And after removing it. So if you print this fruits, then we are missing banana here, which means we have successfully removed a banana from our fruits. So what if, uh, some, uh, people would do some silly things here? I have commented this assert statement here this fruits after removing. And let me just, uh, uncomment this. Okay. Now, if I run this, uh, we will probably end up getting an error illegal exception. Because, uh, if you see here we, we are calling Itnext two times. Let me just show that to you. So if you see if you can see, we can. We are printing banana here and then we are getting an no such element exception. Sorry I said illegal exception. It's not giving that. It's just a giving no such element exception. Why? Because we have already iterated over an element here using Itnext. And we are storing that next element in next row that would be in starting Apple. Right. So first it checks banana dot equals to next fruit which is apple which is probably not. Then it returns false. Then uh this uh if loop will get terminated. And here we are just printing it dot next. So here we are having current element as apple right. So here we are again calling the next one from this apple which means it is a heading over to banana and just printing that element out. That's why we are getting banana here. And when we call, uh, on the next iteration, it has next from banana we have cherry. That's why it got, uh, returned. True. Then here in the next fruit, we are calling it, uh, next, which means we are currently at banana. Right. So here, this next fruit will be getting cherry in it. Okay. And then if banana dot equals or this if loop will also get terminated because cherry is not equal to banana okay. Then again here we are calling System.out.println. Itnext from cherry. We are calling dot next. As I have said earlier while uh representing this. So when we call next from the last element then we are probably gonna get an error. So uh, this is that error which I am speaking about. No such exception. Sorry. No such element exception. Got it. It's, uh, not technically an error, it's just an exception. That's it. So to avoid this, uh, instead of using Itnext, just use a next float to print that element. Okay. If I run this again now, you can probably getting all three elements, but, uh, you might be getting a doubt regarding. We have removed banana here. Then why we are still getting banana right here. We are getting this because this banana is already got stored in next fruit, right? So when you are printing that next fruit. So when you are removing a itremo it is not getting removed from next fruit, it is getting removed from the actual fruits. So that is why we are getting banana here. Also, let me, uh, run you through this, uh, iteration again from start. So here on start it dot has next. So we have Apple. It returns true okay. On next fruit. We are calling it next, which means we are storing apple into this next fruit. And then if a banana equals next fruit, which is probably not because banana is never equals to the apple, that's why it is getting terminated. And so here we are just printing this apple out. And after it it's checking it has a has next or not a next element we have or not. So from Apple it checks for banana. We have banana. That's why it returns true. Which means we are on this level now. So we are calling it next and storing it on next fruit from apple when we are calling it next, which means we are storing banana in our next fruit. Okay. Now if banana dot equals next fruit, which is probably true because banana equals banana. Now we are removing that banana from our fruits. So we are removing this banana from fruits not from our next fruit, the still having mixed fruit as banana as of now. So when we remove this it will get removed and our next fruit will be the will remain same. Okay, now we are calling System.out.println. Next fruit. So we are having banana. So we are still getting banana here. And then next cherry as simple as that. So don't make this silly thing expecting that you would get banana removed from your list. If you do like this, it's actually getting removed from list, but you are still printing banana from this next fruit variable, so just keep in that mind it won't affect our next fruit it affect on the actual list. Got it now. And we have left with the one more method in our iterator interface which is a for each remaining. So here you can see the action, the action to be performed for each element. here. We have an action here. What exactly is this? Let's see through the example here. So basically here we are just printing out the elements like this right. Instead of doing this we can also let me just print out a new way to print elements. Okay. So here I'm just calling for each on this ID okay. For each element remaining I t dot for each element remaining. Now in this we need to pass the. We can call the element with any name we want. We can just do like L or element. No matter what name you give here it will work. And then just give this arrow symbol. And then here I'm just calling S out. System.out.println. Println. Here, let me just remove this s. See we got that. And here I'm just printing whatever I mentioned here yearly in our yearly. See. So at this actions place we are actually performing this action which is System.out.println. So we are passing this method as action in our for each remaining method as an argument. Got it. And in that we are just mentioning this yearly. See. Now if I run this, we are getting the same why we aren't getting any elements here. Let me just comment this out and let me see if this works or not. Let me run this again. Yeah. It's working. See, these elements are getting printed if you do like this also. And let's remove one element. Let me just comment this out. And let's see if we are getting one element removed or not. So when we perform this. So if you observe carefully here why we are not getting elements when we uncomment this section. Because here we are updating our IT elements right after performing all these operations in our IT, we have already, uh, iterated over all other elements present in fruits. So we have already iterated over. That's why we are not getting any other elements here. See if I open. What does this doing? It's just calling hash. Next. Currently we don't have any further elements in our IT. That's why we are not getting this while loop. This action get performed. So after doing this, if you just do like this, uh, instead you create a. Let me create another iterator for it to return the string, updated it, naming it as updated id equal to fruit start iterator calling iterator on it. Type mismatch cannot convert from iterator to iterable. Oh sorry sorry sorry. My bad. It is iterator. Got it now instead of it for remaining for each remaining can call updated it for each remaining got it. If I run this we can get our updated elements as we have expected. See Apple and cherry banana got removed. So that's the reason why we won't get any elements before, because we are already iterated over all other elements or cursor is at the end of fruits. That's why you won't get any elements printed. So this is also one way to print our elements to get our elements. Here we can also perform other actions also. Not only this here as a action, we are just printing. There are a lot more you can do with this for each remaining. So it's a task for you. Uh, please, uh, research about it and what other, uh, actions we can perform on this method. So do a quick research on it. You will get to know all other ways to use this for each remaining in your feature. So that's all about iterator interface guys. We have explored each and every method uh, present in our iterator. So let's come back to our iterable interface which is having three methods. See here we are. We have explored already explored this iterator as of now. And now we are remaining with two other methods. Those are for each and split editor. So as of now, just end this video right here guys. We will continue our iterable interface, uh, in our next video because uh, this video already got a very lengthy. So let's continue in our next video. Uh, in our next video we are going to see the for each. And if we have time then we are also gonna explore this split editor interface and its methods. All methods in it okay so that's it for this video guys. Hope you got the clarity by now. What actually is iterable. And in this iterable we have also explored the iterator interface and all its methods Hasnext. Next remove and for each remaining. So that's it for this video. Let's meet up in the next lecture. Until then, stay happy and stay secure. Bye bye.