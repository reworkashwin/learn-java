Hello everyone. Welcome back to our Java Collections Framework course. Today in this video we are going to learn about our search interface in Java. So before going to learn about our search interface in Java, let's understand it with a simple question. What exactly is a set in general terminology. So a set is a group of unique items or a set is a collection of distinct objects considered as an entity. These objects are called elements or members of sets. And sets are used to group objects that share a common property or characteristics. You may have already learned about this in your school maths, right? So that's what a set means in general terminology. Now let us try to understand what exactly is this set interface. The set interface in Java is a part of Java Collections Framework, and is designed to store elements that must not contain duplicates, and it models the mathematical set abstraction, ensuring no two elements focus on this term. No two elements in a set are equal. What exactly we mean by that? So in our set collections we can't include duplicate values, which means in our list we can have elements like this. Right 2322. So this duplication possible in lists right. So same thing won't possible in set won't allowed in sets because sets only contains unique values. If you add duplicate values it don't give any error. But still when you do like this set that add to. And again, if you do set that add two. It won't give any error. Instead just add only one two into this set. It don't add another two element into this set because it maintains the uniqueness in our set collection. So let me prove this to you practically. Let me open my VS code. And here let me open a new folder. Let me create a new folder here. And I'm creating a folder with the name set interface. And in this folder let me create a file with set test dot Java. Public class set test. And let me type PS vm. And here I'm importing our set interface. Now let me create a set collection. Now set of integers set equal to new. So here we need to create an instance of set implementations which means set classes. What are these set classes we have. So if you observe here we have these set implementations such as hash set, linked hash set and tree set. We can create an instance of any of these implementations right. Let's just go with hash set. Okay. Now here I'm creating an instance of hash set or it's giving an error. Cannot infer type arguments for hash set okay. Sorry integer right. My bad. I have given integers there. That's why we are getting that error. So after removing that we got rid of that error. Now we can proceed with it. So let me prove our point to you, which is we can contain duplicate elements in our list. That's our point, right? Let's prove it through this example. So if I here I'm adding set dot add to. Let me give set dot add three. Now if I do this again set dot add two. You won't get any error. But if I run this now you probably see the output as only two and three. Okay. My bad. We need to print them to see the output. Right. Let me just print them also. It's out set. Now if I run this I'll probably see two and three. So as you can see this third element this second two you can find on our set because we have already two added into our set, which proves our point that in set we can't include duplicate elements. Now let's explore the hierarchy of our set interface. Okay, so as we have already know that this set interface is a subtype of our collection interface, right? Because this set interface extends this collection interface. That's why we can say that this set interface is a subtype of our collection interface, and it does not introduce any new methods beyond those specified in collection. You won't find any new methods, such as in list. In set insert, you won't see any new methods other than the methods available in collection interface. Got it. Whereas in list we have a lot of methods which are not present in collection, such as we have a lot of indexed methods. Right? To remove with index. To add with index like that we have lot of methods and we have another method known as Listiterator, right? We have iterator in our set interface, but we don't have any set iterator like that. It only have the iterator method which is already present in collection interface also. But whereas in our list interface we have a lot of these are separate method for itself. So make sure to remember this that our set interface won't have any separate methods for itself. It only have the methods which are already available in collection interface, which makes that every method in set are inherited from collection interface. Right? So that's it. Now if I come down to hierarchy from set, we have our set implementation such as hash set and linked hash set and reset. So these are the set implementations. Nothing but classes which implements directly or indirectly set interface. So these two directly implements set interface. Whereas this tree set indirectly implements this set interface because it is one step further below. In between the set and tree set, we have another interface known as sorted set. On each one of these implementations we will be having separate videos on them, so don't worry about them. Let's try to understand them briefly okay so here this hash set stores element in a hash table. That's one point stores elements in a hash table. And it offers constant time performance for basic operations such as add remove contains method and size operation. So for this operations, no matter how much big our collection is, it just gives the constant time, which means O of one. And it does not guarantee the order of iteration, which means if you store elements in this order, such as one, two, three, four, five, six, and when you are printing this collection, when you are printing, you won't be getting these elements printed out in same order which you have printed. It may be printed out like this, like 321645 like this. So that's why we can say that our hash set collection won't guarantee the order of iteration. Let me just prove this to you through an example. So here we have already created a instance of hash set. Right. Let me add some values. So here let me add some more values 698 ten. Let me give some random values. It is seven. Okay I have added these values to our set. Now if I run this the order 23456. See we have added 2369 right. But we are getting 235687. Like this. Order changed. So that proves our point right. We are not getting these elements in the same order which we have added into our set. And one more thing we have mentioned here, which is it gives the constant times. What is mean by that? So in list, if you want to add an element you need to first traverse the elements right. So it traverse the elements and find the perfect location which you have provided. Like if you give an index, Do you feel, given particular index to add element, and after adding that element to that particular index, it need to adjust the remaining elements to the some other indexes. Right. So such operations, such basic operations also on list will take more time than same operation which are getting performed on hash set. If the list collection is very big, then adding, removing, then performing such basic operations also takes more time than hash set. Whereas in hash set, no matter how much big collection is, it won't take much time. It's only do that particular operation on constant time, no matter how much big our collection is. It just take the same time on every collection of hash set. That's why we calling it as constant time operation. Whereas linked hash set extends hash set and as well as implements set interface also. So let me give linked headset instead here. Okay. Let me open this implementation. Now here you can see public class linked hash set extends hash set as well as implements set interface. Got it extends hash set and implements set interface. And it extends hash set with a linked list running through all its entries, which makes it possible to maintain the insertion order. It maintains the insertion order because it is just like a linked list running through all its entries, whereas in hash set we won't keep up with insertion order, right? Let me prove this to you through an example. So here we have already added some elements into our set. Right now if I run this set and at this point we are implementing this set with linked hash set. Now if we run this, we must get these elements in the same insertion order. Let's see. Now we can see (236) 923-6958 ten. See we have. We got the same insertion order as we have inserted elements into our set. Right. Which proves our point. And whereas tree set it implements navigable set between these two we have already explored it. We have a navigable set interface and which is backed by tree map, which makes it possible to order elements based on their natural order. Order elements based on their natural order. If it's string, then it makes alphabetical order. No matter how you insert elements, those elements get sorted to their natural order. So if you observe carefully here, this hash set won't maintain order, which makes it possible to perform speed operations. Perform operations in very quick. Remaining this to this hash set performance is very high and whereas in linked hash set it maintains the order which we have inserted insertion order which makes it less performance than hash set. Let me indicate it like this. This is high performance and this is normal performance. And whereas in tree set it maintains natural order maintains natural order. Like if you add integers then it makes like it sort integers from small to big. And if you insert strings then it maintains alphabetical order which makes it lesser performance than these two. So that is why I am indicating this tree set with this symbol. Hope you got the clarity by now how this works. So let's also explore this tree set example on our VS code. So here let me add a tree set instead of linked hash set. So if I open this tree set now you can see here this tree set implements navigable set. And which navigable set further extends sorted set. That is why we are getting elements in a sorted form. And this sorted sets extends set interface. Got it. How hierarchy is set here. And let me just show it to you. So here we have randomly added some numbers into it. Let me keep these things in between this. Now if I run this. These elements must be sorted out when we are printing. Right. So if I run this now, now here you can clearly see these numbers are sorted out in their natural order. And instead of this let me just make a string set one here and low there a pill banana. Let me keep cherry here and then banana. Now if I run this you can see all these elements are sorted out in an alphabetical order a, b, c, h. Got it. So that's how our tree set works. And operation time it takes is more higher than linked hashset and hash set. And on this sets you can probably perform all of these methods like size is empty. Contains iterator to array which we have already explored in our previous videos. Right? Add method. Remove contains all. Add all. Retain all. Remove all. And let me just show an example to you. How do we convert a normal list to a set using add on method, which is pretty much interesting. Let me show this to you normally. How do we convert? Let me create a list collection first list teacher equal to list equal to new ArrayList. Let me take array list. Let me import those two list and array list. So now let me add elements into it using arrays. Dot has list. Let me pass some elements into it. One comma 2235 comma 25 comma five comma nine. Right. I have just randomly added some values. And in this you can see the duplicate values which is possible in list right. And now let me create an integer set. The name num set equal to new set. Now we can use add method to add particular collection to our set. Right now our current set is empty. NUM set is empty. By using add method I wanted to add this list to this num set. Let me just try out that using num set dot add al and here I'm passing the collection list. Now instead of set let me just print out our num set. Now if I run this you can probably see all of these duplicate segments were removed and a unique set got printed out. Right. So this is how we can convert a normal list to a set. And if you observe here instead of doing this, we can also do the same operation in a simple way. So let me just show it to you. Instead of calling add on method, we may pass this list into this hash set call. If you do this, we will get this same results. See 1258589. So these are the two ways you can how you can convert a normal list to a set. So that's it for this video guys. Hope you got the clarity by now how this hash set are linked. Hash set and tree set. Overall this set interface works. So that's it for this video. Let's meet up in the next lecture. Up till then stay happy and stay secure. Bye bye.