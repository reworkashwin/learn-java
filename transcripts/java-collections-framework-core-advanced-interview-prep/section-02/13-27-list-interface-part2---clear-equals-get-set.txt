Well. Hello guys. Welcome back to our Java Collections Framework course. So in our previous video we have explored a lot of methods in our list interface such as size is empty iterator two array add method, different add methods and different remove methods. Contains all method. Add all and add all with index. Remove all, remove all. Uh retain all, replace all. And finally we have also explored the sort method of our list interface. So today in this video we are going to explore these four methods. Clear equals hashCode get unset. These five methods we are going to explore in this video. Okay. So without further delay let's get started with our clear method. So here you can see this clear method removes all of the elements from the list, and the list will be empty after this call returns. So whichever list we are calling this clear method upon, that list will get cleared. Okay, so in our previous example we are having these three values are as this right. 3040 3030 and 3030. Now let us try to clear this list three using our clear method. Okay. Currently this is our output. 3040 3030 3030 okay. Now I'm going to clear our list. Three. After this out after clearing list three values are. Let me just give a list three. Let's call clear method upon it. And it just, uh, don't return anything. That's why it's giving error. If you see here, it's a void, which means it won't return anything. That's why we are getting this error here. So here, let me just print list three. Before that. Let's just, uh, do this. Let's say three dot clear. And list three. Sorry for that. Okay. Now, if I run this, you can probably see the empty list here. After clearing list, three values are empty. Now let's move to our next method which is equals. So you can probably tell what it does by reading its name itself. So here you can see it's a description compares the specified object with the list for equality. Returns true if and only if the specified object is also a list. That's one condition, and both lists have the same size. That's the second condition and all the corresponding pairs of elements in the two lists are equal. That is the third condition. If these three conditions met true, then you would probably getting result as true for equality. And here you can see two lists are defined to be equal if they contain the same elements in the same order, order also matters. This definition ensures that the equals method works properly across different implementations of the list interface. Okay, let's verify that. Let me create a new file here a quality check dot Java. And here let me type PS vm public static void main. And in this let me create a list of integer equal to new. Let me import this list. Why doesn't getting imported automatically? I don't know, let me import it manually instead import Java.util.list Okay, now let me give it like this. List equal to new ArrayList. Let me import ArrayList also ArrayList. And in this ArrayList, let me add some elements like this race to use arrays. We also include the. We also need to import arrays. Now I can convert this arrays dot as list. Okay, in that let me pass some elements one comma two comma three. So these are the elements. Let me just copy paste our list implementation. And here I'm naming this second list as list two. Now let's check uh equality of these two lists. For that let me call assert on init list dot equals Girls list two. Let's see if both are equal or not. If I run this now. So as the description these two lists size, these two list size is equal. Because these two are having only three elements and order is equal. They are they both in same order. Literal is in same order 123, one two, three. And they both are of same similar type integers. What else? The conditions we need to specify that first condition is both must be list. Here you can see both are of list. Now if I run this it should return true. See? We are getting true. What if. If, uh, list two is collection instead of this? Let me call collection. Now, if I run this, what would you think? What is going to return there? Let's say that. See? We are still getting true. Why? It doesn't see the interface implementation here. This list already inheriting the collection. In such case, you include that a collection on another list. It won't matter if you do like this, then also it. Consider this list as also a collection. Let me check. Like this. If we do this list two dot equals list one, then. Also you can probably see true as an answer. These two objects ultimately are belongs to the collection. So what it checks for is here this one this type. So these both types are similar. That's why it's giving true. So what if if we messed up with order. So here instead of 123 I'm giving 132 in our list. One. So if I run this again, it probably say false here because order matters. If order is not correct then you would probably getting false there. Got it. So that's about equals method guys. Now let's move to our next method which is hash code. So it's similar to the method which we have seen in our collection. It just returns the hash code value for this list. So this method is inherited from the collection interface itself. So it does do the same thing as we have explored in our previous videos while exploring collection interface. But and now let's explore the get method. So get method it just returns the element at the specified position in this list. So you would be passing an index. It's a search for that index and grab that element at that particular index. And it just return that index, index or object, whatever you may call. So here it index one. We have three right in list. If I do like this list dot get index one. What do you think. Which element we are going to get now. It's obviously three right. See. So obviously three because index one on list we are having three as element. That's why we are getting three there. Okay. Now let's move forward in our list interface. And now we have set method. So set method is probably used for replacing the element at specified position. Here you can see it takes two arguments. One is index and another one is element which is to be added to the replace to the collection. One is index and one is element. So at this particular index we are adding this element. We are not just adding this element, we are removing the element which is previously there and putting this element right into the previous element's position. That's what this set method would do. Let me verify that to you. So here at index one we are having three right. So if I just do like this and one more thing here. So here you can see the element previously at this specified position it returns that element. So if I print this uh if I call this a set method in assert System.out.println. It probably saying the element which is uh get removed. Let me do like this. Let me call assert. And if I do list dot set at one and I'm gonna replace it with, uh, 33. Okay. So at this line we are getting Output has three because these three will get removed, right? So it will just return the removed element. And it says this, uh, 33 in this uh, index one position. Got it. Now here, let me print our final list. What are the elements in it? So if I run this, you would probably see the three here because it got removed from the list. And you can see the 33 here because we are replaced with this three with this 33. So that's the set method for you. Now let's move to the next method. Here we are having add with index and remove with index. These two methods we have already explored. So as of now remaining methods we will gonna explore in our next video. So that's it for this video guys. Hope you got the clarity by now about these methods. And let's meet up in the next lecture. Up till then stay happy and stay secure.