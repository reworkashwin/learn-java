Hello everyone. Welcome back to our Java Collections Framework course. Today in this video we are going to learn about queue interface in Java Collection Framework. So what exactly is this queue interface here. Queue interface in Java is a part of Java Dateutil package. We all know that. And it obviously extends collection interface. And it represents a data structure designed to hold elements prior to processing. Now at this point, what is mean by this prior to processing let me explain it to you. So basically a queue is like a line of people waiting to be served. We all agree with that, right? Just like a line, the first person who arrives is the first person to be served as the last person who arrives has to wait until everyone before them. Everyone before them has been served. In programming, a queue is a data structure which provides a way of organizing data that holds items like tasks, messages, or any type of other data in a similar way. So if you consider messages, you are storing a message in a format no matter what message comes after must be in the same format. Which first message in. And these processes are items you may call waiting to be processed, just like people waiting in the line. When we say queue holds elements prior to processing, it means queue stores items temporarily until it's their turn to be handled or processed by the program. Imagine you have a list of print jobs for a printer. The printer will handle these jobs one at a time. The queue would hold all the printer jobs, and as the printer becomes available, it will take the first job from the queue, print it, and then move to the next job which is on the line. So a queue ensures that items are processed in the order they were added, just like serving people in the order as they have arrived in the line. So that's what prior to processing means. So that being said, let's try to explore our queue interface practically in our Java. For that, let me open my VSCode. So here let me create a new folder for our queue interface practice. Let me create a new folder with the name Queue interface. And in this folder let me create a file with the name queue interface dot Java. And let me do the necessary things. Let's import our Q interface now. Import Java dot util package. Right. And in util package we need to import new interface. So now let's open its implementation and explore it completely. So here you can see our Q interface extending collection interface. And here we got the first method Boolean add. So by writing its name you can say that this method is used to add elements into our queue. So how it adds here you can read its implementation details. So it inserts the specified element into the queue if it is possible to do so. What is mean by possible to do so. So here it means you might be adding a element which is not supported by the queue. Which means there might be a typecasting issue Right. So here you can see immediately without violating capacity restrictions in the context of Q without violating capacity restrictions, means that the element can only be added if the Q has enough space to accommodate it. If the Q has a fixed maximum capacity, you can't add more elements than this limit. For example, if your Q has a capacity of ten elements and it already contains ten elements, trying to add another one element would violate the capacity restriction. In such case, insertion would be fail or be rejected. So upon success, it will return true, and if it can't add that particular element successfully, then it just throws an illegalstateexception if no space is currently available. Got it. Now let's explore this scenario through an example. For that here let me add a Q known as linked blocking Q. So here I might have just used priority Q go as an example, but here I am using linked blocking queue because to show you how we can restrict our queue for certain capacity. Got it? For that, let me just import our linked blocking queue using import Java dot util dot. So here we need to make use of concurrent package and from concurrent package I am including linked blocking queue. This one. Here you can see we have three other types like linked blocking DQ transfer queue we have and linked blocking queue which we are going to use now in this video also. So let me import that one linked blocking queue. And now let's create a object of it. For that let me include like linked blocking queue which is of integer type. I want to include integer elements in it. That's why I am giving here integer obviously. And let me give a name as queue equal to new linked block in q. And here I'm specifying this size. So here I have specified the capacity as three. Now let's keep this in a try catch block because I have said it would throw an exception right. Illegal state exception if no space is currently available. So that's why let's make it in try catch block. So in try let me add some elements into our q using q dot you can use put or you can also use add. So here I'm using add. And let me do this for three times for two and three okay. Now we have successfully added and completely utilized the capacity of our linked blocking Q which is three. So we have added three elements into it. So here let me give exception as instead of exception just let me give interrupted exception. Or we can also make use of this one right. Legal state exception. Let me do that instead. Okay. In such case, let me just print stack trace. E dot print. Stack trace. And finally, after adding elements into it, let me just print our queue using a sort. Now, if I run our program, you can probably see our result as one, two, three. Right. Let me allow this. Now you can see one, two, three. Now what if I try to add one more element Q dot add four. What's going to happen if I run this now? You can't see any error here. But after running this you would probably getting an illegal state exception. See java.lang. Illegal state exception. Queue is full. You can't add more elements into it. Can't add more elements than capacity which you have specified here. That's why we are getting this illegal exception. Illegal state exception. Sorry. Okay. Now let's see add method for you. If you try to add more elements than capacity, then you just throw an Illegalstateexception and if you try to make it in capacity, then you'd probably add elements to I mean, insert the specified element into the queue. That's the whole functionality of it. And here you can see it also throws some more exceptions like Classcastexception null pointer exception Illegalargumentexception. Now let's explore what exactly is this Classcastexception. So if you read its names carefully you can see class cast exception if the class of the specified element prevents it from being added to this queue, then this Classcastexception would occur in. Simply put, it's just like a typecasting error. It occurs when you try to convert in string into integer or any other type into a specific type, which is not possible. In such case, you will be getting this class cast exception as a result. Let's explore it through in When practical for better understanding. Okay. So here what are the other class implementations we have for our queue. Let's say it in our hierarchy. Collections hierarchy. So here you can see we can make use of priority queue array de queue. Linked list also. So let's explore linked list example for representing Classcastexception okay. Let's use it. So for that here here I'm including queue defining the type as object. So that first. We can add in string and an integer into our linked list okay. Queue to equal to new linked list. And by the way I have imported linked list also. But here I'm just adding Cu2 dot. Add any string. Like let me give my name and let me add another value into it, such as an integer ten, since it throws an exception. When we try to convert this string into an integer, we know that, right? So that's why. Let's keep it in. Try catch block integer num equal to. Here I'm trying to convert it into integer q two dot whole. What this method exactly do. So if you open this method implementation here, you can see it just retrieves and removes the head of this queue. So at this point what is the head of this queue. Currently it is this one right. Head of the queue will be the element which you have first added into it and haven't removed it. That will be the head of the queue. So when you do this pole method on a particular queue, it just returns and removes the head of this queue or returns null if the queue is empty. That's what it do. So here we are, just removing this element from our queue and trying to convert it into integer. So when we do this, it just throws in classcastexception. And for our reference, let me just print out our num value here. Okay. And if you get any exception then you should probably try to handle it in a correct way. Okay. So for that here I'm printing this message called classcastexception plus a dot git message. So if I run this you would probably see a exception which would be one is Illegalstateexception because we are adding extra elements than its capacity here on queue and one is Classcastexception. See here you can see class or dot lang dot string cannot be converted as class Java.lang.integer that's a message of Classcastexception. Instead of this, let me just keep it as e dot print stack trace. Okay, now if I run this sorry for this, we don't need to keep it in assert here. We can just go like a dot print stack trace, and let me clear the screen and let me try to rerun our program again. So if I run this it probably see here Classcastexception here I have just commented this one out. That's why we are not getting Illegalstateexception. Instead we are only getting classcastexception now. And here you can see the message as previous Java.lang.string cannot be java.lang.integer. So that's how we get Classcastexception. So other than these two we also have null pointer exception and illegal argument exception. So this probably occurs if the specified element is null and this queue does not permit null elements and this one occurs illegal. Argument exception. If some property of this element prevents it from being added to this queue, in such cases you will be seeing an illegal argument exception. We're gonna more explore about this while we exploring our class implementations, don't worry about them. So now only focus on the functionalities of these methods. Okay, now we have our method also, which also inserts the specified element into this queue. If it is possible to do so immediately without violating any capacity restriction, then what is the difference between this method and our add method? Both tries to add or insert elements into our queue, right? Then what is the exact difference of it? So if you read this part carefully here, when you are using a capacity restricted queue, this method offer method is generally preferable to add, which can fail to insert an element only by throwing an exception in. Simply put, the offer and add methods in our queue interface are both used to insert elements, but they handle capacity restrictions differently. As we have explored in add method, it inserts the specified element into the queue. Returns true if the element is successfully added. An exception is thrown. Will like illegal state exception if the queue is full and it cannot accept more elements, whereas in our method it inserts the specified element into the queue if possible and returns true if the element is successfully added. Otherwise returns false if the queue is full, it does not throw an exception if the queue is full. What is mean by that? Let's explore it. So instead of Add, let me just do this instead of and let me include this offer. Now if I run this now you can probably see one two, three is getting printed and four can't see in our queue. Just ignore this for so instead of giving an exception, it just don't accept the more values more than its capacity. So here in this case I just ignore the for and didn't give any exception for this. So if we use Add here we would probably getting an illegal state exception. Right. So that's the difference between add and offer method. If you got the clarity by now. And let's try to focus on these two methods of our queue interface pull and remove. So basically these two operations these two methods do the same operation retrieves and removes the head of this queue. Whatever queue we call these methods upon, it just removes the head of that particular queue. And what's the exact difference between these two. So here it is. This method. This remove method differs from only in that it throws an exception if the queue is empty. So if the queue is empty and again you call this remove method on that particular queue, then it just throws in no such element exception. Whereas this poll method won't return any exception, it just prints an empty queue even if you call pole method on an empty queue. Got it. So let's explore it through an example here. Let me comment this part out. And here instead of offer let us keep that offer here. Before printing our queue let me do the operation of remove. So here I am calling remove method on our queue first. And I'm doing this for three times. Now if I run this you would probably getting an empty queue because we have removed all the three elements which are present in our queue, right? So before removing, let me just show you again, we are having these three elements and after calling remove three times we have successfully removed these three. And let me just show it to you how it removing the elements. For that let me comment. These two removes out. So if I run this you would probably see one is getting removed. It is removing the head of the queue. That's why one is not removed, because one is the element which is inserted first. So as our Q follows first in first out methodology, that is why one is getting removed first. Then after that, two will be getting removed from our Q because currently two is our head of the Q, right? So after one, two will be getting removed, and after that three is getting removed from our Q and currently our Q is empty. Now if we call remove method upon this empty Q, then you would probably getting an exception such as such. Element exception. See no such element exception. Got it. Instead of using remove method, we use pull method instead. This one also just does the exactly same as our remove method. It just removes the head of this q. Now if you see our Q is empty. Now if we call pull method again on our q, now it probably getting an empty queue again. Instead of giving an exception which just returns an empty queue. So that's the main difference between a remove and pull method. Guys, hope you got the clarity by now what exactly this pull and remove methods do. And that's being said. Now let's explore these two methods element and pick methods of our queue interface. So as you can see these two also does the same operation. But there is a slight difference between these two, such as these two elements used to retrieve but does not remove the elements. These retrieves and this does not remove any element. Whereas this remove and pole method. This retrieves and removes the head of this queue. And whereas our element and peek retrieves but does not remove the head of the queue and just retrieves the head of the queue. And this method differs from peek only in that this element method throws an exception if the queue is empty, just like remove no such element exception if the queue is empty, whereas our Peek method does not return any exception if the queue is null or empty. Got it. Just like our poll method, when you want a exception while retrieving or removing an element, then you'd probably using the remove method. And if you don't want an exception while retrieving and removing an element, then you'd probably using poll method. And when you want an exception while only retrieving element and not removing element from our queue, then you would probably using this element method. And if you want to only retrieve an element and does not remove element and does not even want an exception in such case, you might be using this Peek method instead. Now let's explore this peek and element methods also through an example. And we haven't yet explored the retrieval part of this poll method. So I am leaving this to you. If you print this call on a System.out.println. You would probably getting that whatever element is getting removed, it will be getting returned to the cursor. So just do the practical yourself. You will be getting clarified how it's working. Okay, now let's focus on element and Peek. Okay. Without removing these just use it to remove the elements. Let me do this System.out.println. And let's keep message as retrieving head element or retrieved head element. Colon two dot element. Okay now let me do this for two more times. Now if I run this see retrieved head element 111 we got one. So after doing this let me just remove the element also using q dot pole. Now if I run this it probably saying one, two, three. And finally our queue got empty. Now, if I do the same steps again, if we call this element method again on our empty queue here, at this point we know that our queue is empty, right? Now here I'm calling element method again. Now if I run this program, we will be getting an exception as no such element exception. Now if I run this, you would probably see that exception as no such element. Exception? Got it. So instead of element if we do this p. So if I run this now you would probably getting a null element as a result. See instead of giving exception it just returns the null if the queue is empty. So that's the difference between element and queue. Guys, hope you got the clarity. By now we have explored all the methods of our collection queue interface. So that's it for this video. Let's meet up in the next lecture. Up till then stay happy and stay secure.