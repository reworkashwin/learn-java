Hello everyone. Welcome back to our Java Collections Framework course. Today in this video we are going to explore our collection interface. So if I want to give a definition to this collection interface then I would just simply call it as it is a gateway to efficient data management. Why I'm saying this as a efficient data management gateway. So to understand this, let's dig into our collection interface and explore it together. So collection collection is an interface which present in Java.util package. We all know that right? So if you observe here, we can import our collection interface like this into our Java code using the package Java.util. Till upon. We can call our collection interface like this. We can import our collection interface into our Java code. And this interface extends iterable interface. So if you can see here this is the syntax of our collection interface. Public interface collection. Here it is extending iterable interface. So when a interface extends another interface we use the keyword extends here. And when a class on to extend a interface we use the term implements. I hope you note that a difference. So here this collection is interface. That's why it is extending iterable interface. And our collection interface is introduced in Java 1.2 Java 1.2. Whereas this iterable interface introduced in Java 1.5, you need to keep note of this thing. Iterable interface is introduced in Java 1.5, whereas our collection interface is introduced in Java 1.2. At the time collection interface introduced in Java 1.2, it is the topmost collection topmost root collection. So the root interface, when the time it is introduced to collection is the root interface. And when a. In Java 1.5 Java developers are Java 1.5, they have introduced the iterable interface and generics. At this time they made collection interface extend this iterable interface. They made this iterable interface a root interface In Java 1.5. Hope you got the clarity about its history. So now let's explore our collection interface, its code, and let's see what are the methods available in our collection interface. So here we have its code. So if you see here we have the size method which can return the number of elements in the collection. So if I create a collection for example let me create collection PS vm. Here I'm creating a list integer nouns equal to new array list Java.util.list okay. Now let's add some elements into it. Now I'm start. Add one num start. Add Two four. One. Two. Three. Four. Okay, let me print our list. Sorry. Numbers. Right. And after this, let's try to print its size. Let me give you a message. Size of nums is press nums dot size. Got it. So if I run this now, you can clearly see here we have four elements in our list. And it's showing size of nums is four. So that's the size method available in our collection interface. And here we have a boolean method which returns true or false is empty. If uh the collection is empty then it returns true. Here you can see returns true if the collection contains no elements. If the collection contains a it return false if the collection contains elements. So we have this list right? Let me go. Message as checking if a Nums contains elements or not. Okay. Now nums dot is empty. Now if I run this it returning false because it contains elements. Let's suppose, uh, let me just remove these four or let me comment these things out. And if I run this, you'd probably see. True. See? As simple as that. Now we have contains method in which we are passing an object. So it is nothing. But, uh, just here we are supposed to pass our element, uh, thinking that is the element present in, uh, the collection or not. So if we pass that element, it checks that, uh, element for the Collection, which we call this contents method upon. If it found the parsed elements in that particular collection, then it just simply returns true. If it doesn't find a elements, you have passed in that. In this contents method upon the collection which you have called this contents method on, then it just simply returns false. Okay, see here returns true if this collection contains the specified element. More formally, returns a true if the only. Only if this collection contains a at least one element. Our collection may have duplicates, right? So if it finds at least one element, no matter which element you have passed here, it finds that element on collection. Then it just, uh, simply returns true. Let's verify this. So here I'm uncommenting this out and let me just print System.out.println. And here is for available in collection. Here let's call contains method on nums nums dot contains. Here I'm passing for. And let's just run our code and see what it returns. So if you see here is for available in our collection. True because four is available. Got it. Instead of four let's give some other value which doesn't have in our collection. See, I'm passing 50 now. Now, if I run this here, you can see it's returning false because it doesn't find 50 in our collection. Nums. Got it. So if we open its implementation. So that's it. Now we have iterator which is overridden method from iterable. We do the same as our iterable iterator method, which we have already explored right in iterable. While exploring an iterable interface. So we don't need to go through this again. So if you read a, its definition here returns an iterator over elements in this list in proper sequence. And we have two array. We can convert our collection to a normal array. Static array. So let's also verify this. Here we have nums right. Let me give a comment as a converting collection to an array. Okay. Now here I'm converting it int here are equal to nums dot two array. And here I am passing new integer. Let me go like this. Enter zero. Okay, we need to do like this I think. Integer. And here I need to give it as integer. Yeah. Now this will work. So if I print our uh array does this work. We can print a arrays like this. Right. If we print array, uh, simply like this, we will be getting a, uh, some value like this. Got it. So to, uh, print elements of an array, we need to use, uh, loops. Or we can simply call it serverless like this using index. So array Error. Error of zero. If I call this, uh, we will suppose to get this element right. One. We should be getting one there. Say one, which means we have successfully converted our collection to an array. Hope you got clarity by now how to use this two array method. So let's explore uh, our next method which is add. Actually this is a Boolean method. So if successfully adds that element to a particular uh, collection which we have called upon this add method. So if it does then it will just simply return a true. And if it can't add that particular element then it just simply returns false. Uh, let's verify that. Here I'm calling uh giving comment as adding elements Into collection. Okay, so here I'm just printing out a sort. Does this add our element? And let me just call add method nums dot add. Here I'm passing a file. Let's see if it returns true or false. It must be returning true right. See. Does this add our element? True. Which means it has successfully added. Let me verify this to you guys. Assert this and nums. Uh, let's print out nums. So if we print nums now, here you can see why it has been added to our collection. Got it. Now let's move to our next method, which is remove. So as you can see, it's functionality by reading its name itself. IMO means we use this method to remove a particular element. Whatever element you have passes here this remove method. That particular element will get removed. We have other overloaded methods of remove also, which we are going to explore uh, in a moment. So let's see this method first. Remove removing elements from collection. Okay. Now I want to remove element five. So I can just, uh, simply call um start remove. And here I am passing five. It's showing index five. Is this. Uh it's removing index. Then we are supposed to get an, uh, index out of bound error. Let's see this. Yeah, we are getting index out of bound exception, which means, uh, in this remove, we are supposed to pass the Index of which we are going to remove that element. So here if you want to remove. Four which means we have to pass uh here index as three because zero based index right. So if you pass three then we are supposed to remove that element. Let's, uh, print out uh nums. Now to verify this. So here you can see sorry. Uh, we have another element right added. So if you. Want to remove four, then we are supposed to. If you want to remove five then we are supposed to. Give the index as four. So if I run this here, you can see we have successfully removed our five. No matter which index you have passed, that particular index elements will get removed. Without mentioning, can't we remove just this? I mean, we can't do that. I think we have only this. Method. Only in our Java. if it's a string collection, then what? Let me create a string collection to verify this. List string and let me give a collection here. Collection string str equal to new ArrayList. Stairs dot add hello str. Start at there. Now instead of numb start remove can do like this. STR start. Remove. Does this work? If I run this okay here is instead of stairs. Let me just instead of nums let me just print out stairs. If I run this again. Yeah. On strings we can pass elements like this. But on integers we are supposed to pass index. Let me verify this, uh, on here, uh, for this list, we are calling this, uh, we are setting this as a list. Right. Let me just change this list to collection. And here, let me just give a nums now. So if I pass a three now, now it's, uh, taking it as objects when we create list implementation here, then it's supposed to, uh, considering it as index. Now, if we implement this, uh, using collection, and if you pass three here it is considering it as object. Now it will, uh, check for object three in that particular collection and just, uh, remove that. So if it is supposed to remove three instead of four because now it focuses on elements not on index. So if you mention its implementation as instead of collection, use a list here. Then it would consider as index. Here that would be a separate method, and this remove is a separate method. This remove is from collection. Got it. That remove is from a list interface okay. So collections remove method. It just removes the particular element. It won't uh remove the index. Okay. So let me run this and uh verify this to you instead of str. So let me just give nums here. So if I run this again see number three got removed. If I give one here one will be getting removed. See if I give uh give five here, five will get removed. So if we do this on our list implementation, we are, uh, we are supposed to get an index out of bound exception, right? Because list implementation focuses on index, whereas collection focuses on objects. Whatever object you pass here, it will just remove that particular object from the collection. As simple as that. Okay. So that's remove for you. And we have contains our and it is a boolean. So if you read it here it's implementation. It's documentation. It returns true if the collection contains all of the elements in the specified collection. Let me verify this. Checking contains all method okay. And this is a boolean. Let me mention it like this. Okay now let me just give nums dot contain all. Here let me pass a elements like this two Comma three. Comma four. It is this work? No. Here we are supposed to pass a collection for that. Let me create another collection here. Instead of nums collection integer events equal to new ArrayList events dot add to events dot add four. We are having two and four in this collection right now. Let's verify this. Let me pass events here. Let me cut this and let's just print out in here nums dot contains all and here events message has contains all. If it returns true means we are having two and four in that particular element. See, we are having two and four. What if I add six also here. Even start add six. We don't have six in that particular collection right. In this collection we don't have six. Now it is supposed to giving false. Let's check this. See. False. We got false because we don't have six in this collection. So that's a contains all method for you. Now let's move to next add on. It adds all of the elements in the specified collection to this collection. The behavior of this operation is undefined. If the specified collection is modified while operation is in progress, this implies that the behavior of the car is undefined. If the collection is this collection and this collection is non-empty. So if you don't understand this, just ignore it. So what it does, uh, is whatever collection you pass here, it simply add that collection to the particular collection which you have called upon. Can we do like this? Can we pass raw elements into order? Let's check that. Also checking add all method, which is a Boolean method. Right. Is that. Yeah it is Boolean. If it successfully adds the all of the elements then it just simply return true. If it can't add then it just simply return false. Assert uh adding uh here elements. Okay gnomes dot add all. Here I'm passing events. Let's see if it adds all events or not. We are having already. Right. Already two and four. Now it will again add two and four. and it will extra add a six on. It is supposed to be giving a result as true. Now for that, let me just print out our nums array to verify that if a addition is successful or not. Okay, now you can see it has returned true. Adding all elements true and 1234. And after that it just added that collection. Concatenated that collection into this collection 246. But if you do the same operation on sets, then you would probably seeing these duplicates get removed. And six only six will get added to this. We will explore that when we explore sets. So as of now just focus on, uh, this logic only how it works on collection interface. Now we have remove all. So instead of removing particular uh element we can just remove every element from our collection. So to verify this, let me just call removing all. Is this a boolean? Yes, it is a boolean. So removing all elements. Let me just call it on nums. Not remove all. Here we need to pass that particular, uh, collection which we want to remove. If we, uh, if you want to remove events, then let's suppose let's pass events here. Now it should be removing the this part. Let's see if it does its task or not. After this let me just print out our nums. Okay. If I run this now, you probably see one and three are remaining because, uh, two, four. Also, this two four also got removed. These duplicates also got removed. When you call this removal, can we pass a elements like this. If I pass two. No can do this. We are supposed to pass a collection only. So that's the removal method for you. Now let's explore remove if this one also a boolean. And here we can apply filter while removing particular elements. So here if I want to give filter like a I want to just remove the even numbers. So what is the logic for it to get even numbers and mod two. Then result would be zero right? So if we got a that condition as true, then we are supposed to remove that particular element from the collection. Let's just apply it here. Removing elements based on filter. Okay. Let me just print out removing filter elements. Let me call our names dot. So currently we don't have any even numbers in this right. Let me just comment this part out so that we can get our even numbers also in it. Okay. Now if I run this, uh, here, uh, I also commented this part out. Don't worry about that. So if you can see here we are having even numbers in this list also. Now if we perform this operation like run, start, remove if here I'm passing the condition lambda expression n mod two equal to equal to zero. Okay. So this is the condition. Let me keep it like this. Okay. If I run this, it should be returning. True. And after that, let me just print out this out once. Okay. Now let me run this. If you see removing filtered elements. True. And we got all the, all the even numbers got removed from it. See two four and this two four, six. We can also perform some, uh, here. Uh, we can give whatever condition we want instead of, uh, removing events. If you want to remove hours, then we can, uh, set it, set this to one. Now, if I run this, see, all the odd elements got removed. Now only remaining with even numbers. So that's how we can, uh, give filter here. So that's the remove if method for you. As you can see, it's implementation. It's just taking that filter. And that particular collection object is just calling this a required non-null method, which is supposed to check that a specified object reference is not null. This method is designed primarily for doing parameter validation in methods and constructors, as demonstrated below. And then we are creating an iterator. Each iterator and on this edge we are passing Hasnext. So if it's if it has next then we are doing filter dot test. Here we are testing the method. This method is supposed to evaluate the predicate on the given argument. It returns true if the input matches the predicate, otherwise false. If it doesn't match the filter, then it is just supposed to return false. So if it returns a true, then just remove that particular element and set removed equal to true. Okay. And finally it returns that a removed status. If it removes everything and it is have at last it is set to true. Right. So while returning. While getting status as a removed as true. So if we aren't supposed to remove any element, then the status is set to false. Okay. We'll be getting false when we call this remove on empty, which means none collection or in the process we've got any exceptions. Then also we will be getting status as false because it is by default set to false. You are only setting it to true once removing the element. Got it. So that's the remove method. Now let's uh move to next method which is uh Sir. Retain all. So if you hover on this you can see it's a definition. Retains only the elements in this collection that are contained in this specified collection. In other words, removes from this collection all of its elements that are not contained in other collection. Here we are supposed to pass a collection rate. So whatever collection we are passing here, whatever elements present in this collection and the elements which also contains in this collection also, which we are calling this retain all method on those elements, only retains other than these elements. Those are matched elements can call it as intersection. It just performs intersection operation other than a common elements. It removes the those other elements from the collection which you have called this retain all method on. It doesn't remove elements from a collection which is. Get passed here. Got it. Let me verify this to you. And this is a boolean also. And this also Boolean method. Okay. Checking. Retain all method. Boolean and performs intersection operation and go on our collection. Okay so here we have two lists already right. Let me also comment this part out. As of now, if I print or let me just create two separate lists for better understanding. Here I am creating list one collection or collection one. Collection integer collection one. Or let me just list one equal to new array. List one collection. Integer list two. Equal to new array list. Okay. I have created two lists and here I am adding list one dot. Add one. List one dot. Add to list one dot. Add three. List one dot add two. Okay, now I'm adding list two values. List two dot add three list two dot add. So let me just paste this two times more. And this is five. And here I am passing six. Okay now if I print both list list one values or list one and list two values are. List two. Got it. If I run this now, you probably see we have these two list values. Got it. Now if you want to perform return operation on this. So what happens if we perform return null. So what are the common elements. Three and four. Right. So three and four only remains on list one if you perform on list one. If I perform on list one dot, uh, let me just also print it out in insert retain all and just giving like that. Now let me give an. Let me give a question mark like that. Returns a. And here I'm calling list one dot. Retain all list two. Got it. And let me just give it like this for a better way, okay? And after this, let me just print out our list one value. Let me give message as updated a list one. Values are like this okay. Now if I run this now, you can see we are only remaining with three and four, which are common. What about a list of two? Do you or do these values remaining values? Also getting removed from list two? Let's also verify this. Or let me just copy paste this part here. Okay. Now if I run this you can probably see, uh, these two values are not getting changed. They remain same. So only the collection which you have called, uh, retain retain all method. That collection only get updated. Got it. Let me give message as remains unchanged. So that's the return all method for you. Now let's explore next method. Okay we have clear method. So it just removes all of the elements from the collection. This collection will be empty after this method returns okay. And it is a void. It doesn't give true or false like that. And just remove everything. Let me just call that method on system or server. Let's see what it returns. It is supposed to return nothing right. And let's see what happens if we print void method on sort. Let me give comment as hearing everything from list two. Here let me give void clearing list two. Let me just call list two dot clear. Okay. Okay. We can't just call this on undefined for the argument type. I suppose we can't call this method on assert. What if we call out of or s out? Now it doesn't give any errors because it doesn't return anything. It just removes everything. Now let me give you an updated list of values. And before that, let me give a message as clearing an updated list of values. And these are list two. Okay. Now if I run this see updated A list two values are empty because we have cleared everything from our list two. So let's move to the next method Which is equals. So you can say by its name itself what it does is it just returns. It just checks to collections. If both are same then it just simply returns true. As you can see here mentioned you can perform this equals on lists, sets and objects. Got it. So if you perform at checks are these two collections. Now let me create two new collections here. List three and list four. Let me create these two lists. List of four and list three. For that, let me create collection. Empty chair list three equal to new ArrayList and collection integer list of four equal to new ArrayList. Okay, here I'm adding two elements in list three. List three dot add three. In step three. Dot add five. Let me give four here. And list four dot add three. Let me change this to four. Okay. Now on both lists we have the same elements right. Now let's verify this. Let's call equal. It just returns true or false right. Boolean checking equals method two collections. Boolean. It returns boolean. Okay. Now here I'm calling on assert is equal. Let me just call. Equals on list three dot equals. List of four. Okay let's see. Here's a list three. Equal to list four. Let me give it like this. Okay. Now, if I run, this just returns false. Why? We are supposed to pass object. Let's read its implementation here. Compares the specified object with this collection for equality. Will. The collection interface adds no stipulations to the central contract for the object articles programs implement the okay. It just checks two objects. Equality, right. Instead of doing this, let me give list three here. Here I'm just assigning list three to list four. Now it's supposed to give a true but just checks equality of two objects. So list three is object. So here we are just assigning that memory location to list four. Which means these two memory locations are same. Now that's why it's just checking that memories of these two and finding these two are equal or not, and returning if both are equal otherwise it just returns false. If elements are of both same, then also two objects. Memory locations are different. In any case, until you just assign that object to that particular object somehow, even if the both objects containing similar elements, then also these two objects memory locations are different. That's what we have explored here on previous. We have just given similar elements to both lists and I have checked the trait. Then we got false and after just assigning list three to list four, we are getting as true. So that's the meaning of it. And just check two objects equality. So that's the equal method for you. And we have hashCode method. So here you can see it just returns hashCode value for this collection. And it is the int value. It returns integer hashcode. So let's verify it. Checking hash code method. And it returns int. Okay. Now sort hash code of list three is list three dot hash code. Got it. Okay. We need to give concatenate operator here. So if I run this we are getting its hash code value as 1058. And if we call the hash code upon four, I think we would be getting same code as list three. Now let's verify this also. Here instead of list three, list four. Let me give it like this. Run this. See? Both hash codes are same. What if if we return Uh, the hash code of list one on one. Also, we are having three and four right now. Let's perform it on list one. Also, if I run this. Okay, now we are getting a hash code for list one also as 1050. What about, uh, list two, which is currently empty? Now, if I run this, uh, we are getting it as just one because we don't have any elements in it. Let me just add some values. List two dot add file. I'm just adding some other values other than three and four. List two dot add eight. Okay. Now let me run this. See now we got a different. So if I add uh three and four in this one also, then we are supposed to get 1058 again. So if I run this. Oh, sorry. Debug operation got started. How do I stop this? Let me just delete these things. Now, if I run this again. See, we got our 1058 again. So we are getting hashCode based on the elements currently on every list. We are having three and four. That's why we are getting 1058. If I change this value to something else like 98, our list two value will be getting changed. If I run this see list two value got changed to 1152. What if I change the value here 98 on this to three. I have performed that. Okay. See for list three and list two. We are having the same value on list four. Also, we are having same value because we are just assigning a list three to list for this one. So that's a hash code. Now let's move to our next method which is split editor which we have also already explored it. It is just a overridden method and do the same operation on collection. Also we have already explored it on our previous video. So I'm just skipping this for now. And we have stream. I'm not introducing this to you as of now. We need a separate video to understand this stream. Okay, so as of now, just note this as we are also having stream in our collection and parallel stream. Also when we explore stream after that we will be exploring parallel stream also. So as of now, these are all the methods we are having in collection guys. So that's it for this video. Let's meet up in the next lecture. Up till then, stay happy and stay secure. Bye bye.