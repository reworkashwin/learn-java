Hello everyone. Welcome back to our Java Collections Framework course. In our previous video we have explored these four methods such as set, get equals, unclear. And this hashCode method we have already explored in our uh, previous videos while we exploring collection interface. And these two methods added index and remove at index. We have explored these two methods in list Interface part one video. Okay, so today in this video we are going to explore these methods, these remaining methods index of last index of list iterator. And here we have sub list and split editor. And we have different overloaded of methods. See of with no parameters of width one parameter of width two parameters and half with three parameters. And here we have off with four parameters also, as well as we have off with five parameters, and also we have off with six parameters and seven parameters. Eight parameters. Nine parameters. And we have ten parameters also in our list of with ten parameters. And finally we have off with spread operator. Here we can use off with a number of parameters. So we're gonna see what exactly this off method would do. Okay. And finally we have copy of method also. So after exploring all of these methods we are gonna also explore this static copy of method. And what is what it does do. So without further delay let's dive right into our video. Okay guys. Now let's focus on our index of method. Okay. Where is it? So here we have our index of method. So what it does do actually. So if you read its description here it returns the index of the first occurrence of the specified element in the list. What exactly mean by that. First occurrence means let's suppose we have this list with the number of elements 1234648494. Like this. We have some elements in our list. So if you see we have four at many places. So first occurrence of an element means. So if we consider that particular element or object as four then its first occurrence in our list is at this place, right. It is occurring In our list first at this position. This is what first occurrence means. So if you pass that particular element in this index of method, it just returns that first occurrence of that particular element's index. So at what index this for is it. So if you see 0123. So this method would just returns this three index to our method call. Got it. So for a better understanding let me just show it to you through an example okay. So let me just create a new file index of test dot Java. And here let me type Publicstaticvoidmain. And here let me create a list of integer list equal to new ArrayList and we need to import this to list an ArrayList. Import Java dot util dot ArrayList. So I have done this. Now let me add some elements into our list. For that here I'm using arrays dot as list. For that we also need to import our arrays. Got it. Now in this list I'm passing the elements 1234. Sorry I am including this in double quotes. We don't need 546474 okay. Now if I pass let me just keep it in a sort and let me give a message as first occurrence of Element four. And then first index is or element four is at index is. Let me give like that. And here I'm calling list dot index of I'm passing the object as four. Now if I run this what do you think. What will output we are getting after running this. So we probably getting three as output because this four is occurring first time at the index three. Right. So if I run this you'll probably see that as output. See. First occurrence of element four is at index is three. And just like that we have last index of method also. So if you see its description it returns the index of the last occurrence of the specified element. Which means if we call last index of element four, you will probably getting 0123401234567899. Right. So it probably getting index of element four at last occurrence is as nine. Last occurrence of element four is at index is. Let me just run this now. Sorry. Here I need to call last index of for run this it probably see nine here. Got it. So that's what. Index of one last index of r. Now let's move to the next method which is list iterator. So it returns a list iterator over the elements in this list in a proper sequence. So if you see it's a name It pretty much similar to the iterator, right? So this method is similar to the iterator method in collection interface and collection interface iterator method. We can call that method on every collection you will find on collection framework. You can call it on every last collection, but this list iterator method will be only called on only list collections. Got it? If you call. If you want to call this list iterator on some other collections like set, then it won't work because this iterator. This list iterator only developer for list objects. Got it? And if you can see here we have another method, another overloaded Listiterator method. And here it in this method we are passing an index. So what this method would do it just a returns illustrator over the elements in this list in a proper sequence and starting from the specified position in the list. So that's what the difference between this and this here. By using this list iterator method overloaded method, we can start iterating over the list object from a specified position with source from a specified index. That's what it means. Whereas this iterator, it just starts from beginning wherever the illustrator cursor ended from that cursor point. You can iterate over, whereas this list iterator you can just access element from a specified position. And what's the difference between this iterator, this list iterator and the actual iterator from the collection? So if you open this list iterator Interface. This is the child interface of actual iterator interface. Got it. Which means we will find the methods available in this iterator interface, as well as some other methods in this which only belongs to this interface. Right. Because it is a shell, it will inherit all the properties from parents and some extra properties itself. That's the meaning of it. So based on that, we have the same methods which are available on iterator interface in our list interface. List iterator interface also like Hasnext next and remove. So these are the methods which are inherited from iterator interface. And along with this list iterator interface have how some more methods as well such as hash previous. So basically the iterator method of collection interface just one directional iterator, which means we can go in only one direction. Whereas in this list interface we have this list iterator right. This can provide cursor in two way directions which means it is. This method is bidirectional. Here you can see the method which make it possible by using this Listiterator method. You can iterate over next values as well as you can iterate over previous values. Also see previous. So to check if we have next values or not, we have. This method has next and to check we have previous values or not. We have. This method has previous and just checks. Let's suppose and this is how an actual iterator moves its cursor, right? Like this. Only one direction. Whereas in Listiterator can move its cursor in bidirectional. So if you starting from this point, you can check for previous values as well as you can check for next values also. And to check next values we have this method has next. And to move to next values. We have this method next to move to next values and to check previous values. If we found if we have previous value or not. We have. This method has previous and to move towards previous values. We have this method. Previous. Got it. And along with this we have some more methods. Also we have this additional methods because our list has an additional advantage of having index based access. That's why we have these two additional methods as well, to get the index of next value and to get the index of previous value. You can see these are an iterator interface because a normal collection would not have the index based access, only the list have this advantage of index based access. Got it. So this remove method which we are inheriting from iterator interface itself. And we have. Additionally we can set values to the our illustrator which means we can replace values. And as well as we can add new values to our list iterator. So that's the difference between a normal iterator and list iterator. And this list iterator only works on the list objects. You should make note of it. Now let's see an example practically. So here I'm creating a new file with the name list iterator test dot Java. And here let me type. Public static void main. And first let me include all the necessary imports such as. Import Java.util.list. Import Java dot util dot ArrayList. And let me also include import Java.util dot arrays. Okay. Now here let me create a list collection first. List integer. List equal to. Let me create a list here new ArrayList. And in this array list I'm adding some elements using arrays as. List Array start as list. Let me add some values here 123456678759. Okay, I have added these values. Now I want to perform this state later on this list. Now first let's explore the first Listiterator method without index. Okay. This Listiterator method returns this list iterator object. Right? That's why we need to keep this list iterator of integer okay. And we also need to include it. Import Java dot util dot list iterator. And here I am giving it as l I equal to. We need to call list iterator method on this list list dot list iterator. As simple as that. Now we have successfully created a list iterator upon this list collection. Now we know that this normal Listiterator would start the cursor from this value, right? So if you do la dot next, now you will get this value as one. Let's verify it. S out la dot next. If I run this now you would probably get the value as one. And you got it right. Now let's iterate over each element and try to print out each and every element using a while loop. Here I'm using a while loop to check the condition. We can make use of the illustrator method which is LR dot hasnext. So if we find values then we can just print them out using this LR dot next method. So here I'm using a sort LR dot next. So if I run this you'd probably see the each and every element getting printed out one by one. See 123, four, five, six and up to nine. What if we do the condition if we run the method here instead of hash? Next we give a hash. Previous. What do you think? Does this run? Let's see if I run this now. It probably seen nothing here because before one we won't have anything else, right? That's why we are not getting values are not getting printed. What if if I do this LR dot next. And if I do this for two times first, uh, let's see now I have called this for one time. If I run this, Would probably see we got this element as well as we got some exception. Okay, okay. Why we are getting these values. Because here I am using LR dot next right. So instead of next we need to use LR dot previous. Sorry for that. If I run this see we got the value as only one. Because when we do this our cursor is moved to this position at two. And when we check LR dot has previous it got the value one. So we have the value one. That's why it returned. True. And it just printed out that LR the previous value which is one. And on the next iteration it again checks for the LR that has previous at that time our cursor it at this position and before one we don't have anything else. That's why it just terminated the loop. That's why we won't get anything else instead of one. Here. Got it. How? This has previous and previous works. Now let's try to explore the list iterator with index. So here I want to start the list at error from the mid to get the mid. We have the formula total size by two. If you do that we can divide the array into half right. So currently if you do this list dot size by two what this list dot size will give. It just returns the number of elements right. So currently how many elements we have 123456789 ten 1112. And if you do the 12 by two, we will get the half of its size. For that I am giving this list dot size by two. So I am passing this index 12 by two means six. Right. So we will passing six as the index here, which means 0123456. Our cursor will be at this position now. Now if I run this I don't want to call this method now because we are currently at mid. Now this will work fine. Now if I run this we need to get these previous values 654321. Right. Before that let me just clear this screen for better understanding. Okay. Now if I run this now you can see 654321 our cursor on starting our cursor at this position. And we got these values printed. Because we are not printing current position, we are just printing previous values. As simple as that. That's why we won't get this six as printed out. And what if instead of hash Has previous. We do this has next. And if you want to print out next values. I would probably see half of these values getting printed out 678759. If I run this again now you can see the these half. So that's what the bidirectionality means. You can iterate over backward and forward in our list iterator. What about the other methods in Listiterator? Let's explore them also. Next index. So instead of elements let's try to print out indexes of them. Next index. Can we get it. So if I run this. Okay okay okay. Sorry. My bad guys. We also need to move forward right. LR dot next. Then only you will get the cursor mode to the next position. Now, if I run this, see six, seven, eight, nine, ten, 11. So these are the indexes of next half, second half of the collection. So if you do this instead a light dot has previous on here we can use previous index to get the previous index values. And instead of next we can perform previous. Got it. If I run this now you will probably see the previous indexes of these from 543210. Now that we have cleared this index methods let's move to the remove method. Let's also explore this remove on our list iterator. So here I want to give a condition. If LR dot previous equal to equal to three Then you just break the while loop. And before that, just remove that particular element LR dot remove or let me keep this element in a variable. Previous value and previous equal to inlay. Dot previous. And instead of calling this LR the previous year let me check. Like this. Previous equal to equal to three. Okay. And here instead of printing LR at previous let me just print out previous value. So if I run this now you would probably see 654 as values 65433. Also getting printed because after printing only we are removing that element. And after that we are just breaking the sequence from that. So here we got the 36543. After printing three, we are just removing that element from the list. So if I print out the list now here short list after removal. So if I print just list now, you would see this three got removed from our list. If I run this now see 12456. There is no three available now. So that's how remove work on list iterator. Now let's move to the next method on our list iterator set. So now instead of removing three I want to set it as 33. Can we do that. Let's see LR dot set here I'm passing 33. As simple as that. If I run this I would probably see 33in between 2 and 4. See. One. Two. Three. Three, four. And here we got up to the three. Just like previous. Because after printing three we are replacing this three with this 33. And after that we are just breaking the sequence. Hope you got the clarity by now how this illustrator works. And here we have another method add. So which means we can also add some elements into it. And to add let me just call LR dot add. And 55. Let me just run this program. And there you have it on this 55 got added to the added before 55. You know why this happened. Because here we are traversing our cursor upon previous values. And we have stopped the cursor at this position. Right. So wherever our cursor present now at that particular position, whatever you add, values will get added to that particular position. So that's what happened here. After printing three and setting 33 our cursor is transferred to this position. And since we are performing previous operation, our new value, when we call this add method, our new value will get added to that values that current values. Previous position 55. Got it. So instead of calling this, let me just, uh, comment out all of these things. And if I run this now, you will probably see 55 is added to the just after the half of the List, because here we are setting our cursor to the mid. That's why it is got added to the half of the list. So let me just remove this one also. Now if I run this program, this 55 will get added to the starting position because our cursor is currently at starting right. It's starting position right. So if I run this you would probably see 55 is added to the added at index zero. What if if we do this if we print out whole list using the li dot hasnext and just simply printing out assert and lay dot next. So after doing this, if we call now, you would probably see this 55 is getting added to the end of the list. Got it. So wherever our current cursor is at that position. This addition will perform as simple as that. So that's all about list iterators, guys. Now let's move to the next method which is sub list. The sub list method allows you to create a smaller list from a larger list. Think of it as taking a slice of a cake. You get a part of cake without changing the rest of it, so if you observe its structure in it, we have two parameters. One is from index and another one is to index. So this from index is nothing but the starting position of this sub list. And here the two index is nothing but the ending position of this sub list. Let's explore it through an example for that. Here I'm creating a new file sub list example sub list. Test dot chop. And let me include all of these imports in this file also. And let me type SVM. And here I'm creating a list of integer numbers. And I have added these elements into it. Got it. Now let me create a sub list for that. I'm creating another list. List of integers. And this one would be sub list equal to. And let me call our sub list method on this uh above number. Above numbers list. So number starts sub list. So here we need to pass the from index and to index. As you can see we are getting suggestions here already. So let me go from index has two and two. Index has five. So what does this do? Is from 3 to 5, it will slice that part and add it to this sublist. So here from index is two right. Which is the third element because indexing starts at zero. That's why we will get a slice from three up to this five. So whatever index you are passing here. So index five would be this right. This is index 5012345. You will get slice up to two index minus one from 3 to 5. Got it. So let me just print out our sublist. I do like this System.out.println. And let me add sublist into it. If I run this now now you can see the sublist has three, four, five. And you might be having a doubt. Now, after performing this operation, does this values affect our previous list? Does this part get removed from this actual list or not? So to verify that, let me just print out our numbers list also. So if I print our numbers list now here you can see our number list remains unchanged. As I said that before, this sub list method will not affect the actual list. It just copies this part from index two to index and just add it to the separate list. That's it. It won't do anything else. So as we have cleared our sub list method, now let's move to the next method which is split return. So this split return in Java is a special type of iterator designed for parallel processing. Both the list and collection interfaces provide a split radiator method, but there are some differences in their usage and behavior. So there are some common features like parallel processing traversal characteristics. So when it comes to parallel processing, both list and collections iterators support parallel processing, making them useful for working with larger datasets. And when you consider traversal, both can traverse elements in a collection, but they do so in a way that supports parallelism. And these both split radiators can have characteristics like ordered, distinct, sorted, sized, etc.. And the main difference here would be the list. Split radiator maintains the order of elements as they appear in the list. This is important for the lists where the order of elements matters, and in list we have indexed access. Since lists are ordered collections with indexed access, the list split traitor can effectively split the list into sublists for parallel processing, whereas in collections split data, which is more general and can be used within any type of collection, whereas this list split traitor only used with list collections. Collection split traitor would be used on all collections such as lists, sets, queues, etc. and ordering. In collections, split data is not guaranteed unless the collection itself maintains an order like a list or linked hash set. We'll discuss more about linked hash set when we explore the topic in our further course. Don't worry about that. So those are the main differences. This split rate around the actual collection split later and usage would be same as we have explored before, because this split method also contains the same methods as previous, like a thread once for each remaining because it belongs to the same interface split later, right? That's why try split estimate size, get exact size if known, etc., which we have already explored in our previous video. Now let's explore our next method which is off. So here you can see we have a lot of overloaded off methods starting from no parameters to ending with n number of parameters. If you see here we have split operator for n number of parameters in half. Got it. And along with this we have half with ten parameters nine parameters Six, five, four and up to one C. So what exactly this half operator would do? So here the half method in the list interface is a static factory method. See every half method you will be having static before it. So it's a static factory method introduced in Java nine. So before Java nine you won't find this off method on our collection framework or in this list interface. So it allows you to create an immutable list with a fixed set of elements. Immutable means unchangeable, right? So by using this off method we can create immutability in our list like in arrays. In arrays. Also we can't add new elements and remove elements right. So we can implement that functionality on our list using this off method, which means the off method is used to create list of list with a specific set of elements, and the list created by this method is immutable, meaning you cannot add, remove, or change elements after the list is created. As simple as that. Now let's explore through an example. Here I'm creating a new file for this of test dot Java. And let me include these things in here also. And we don't need these two because we are directly creating using off method. And let me type public static void main PS vm. And now let's try to create our fruits list using off method. List string fruits equal to. So since our off method is a static, we can make use like this list dot off right? So if you do this now, it will just create an empty list and you cannot add any value to that particular list. If I print this list. Routes I would probably see the list we are getting is empty. And if you try to add something into it, we'll just put start add doing this. Let me go low there. And if I, if I try to add something into it now we will probably getting unsupported operation exception because it doesn't support this operation. Since our list is immutable, we can't add new things into it. So if I run this program you can see unsupported operation exception you can't add new things to our list are removed, but inside the list. So this is the empty list. Now let's try to create a list with some values. Okay, so here I'm adding some values into it like apple banana, cherry. Now if I run this you'd probably see these elements available in our list. Even now if we try to add something into it or remove something like fruits dot add date to date. And if I run this now it probably getting unsupported operation exception. Let's try to remove one fruits dot remove. I'm doing this. I'm trying to remove apple from it. Now if I run this you would see the same exception. Unsupported operation exception because we can't add or remove elements from it or to it. And if you try to add null value here instead of this, or let me just remove this here, let's try to give null. So since this off method doesn't accept null values, if you try to add null values into it, you would. It just will throw a null pointer. Exception. If I run this now, it probably is showing a null pointer exception. See null pointer exception. So it means you can't add null value into it. It must be a something. It must be something other than null. Then only it takes that particular value into its list. So here we are currently adding three elements right. So when you use this off method and pass three elements then you would probably going to use this method. This off method. Got it. This off method with three elements. And when you try to add four values into it, then you're probably going to use off method with four elements. So no matter how many elements you pass here, it reflects that particular off method in our test interface. And what if if it is more than ten elements you are passing into it, then you're probably going to use this off method with the spread operator. It just returns Unmodifiable list containing an arbitrary number of elements, which means you don't know how many elements you are passing. In such case, you would take this off method. So in summary to our off method, I would say the off method in the list Is a handy way to create immutable lists with a fixed set of elements. It's simple to use and ensures that the list cannot be modified after creation. Making it useful for creating constant lists. So that's all about our method, guys. Now let's move to our next method and final one which is copy of. So as you can see here this copy of method, also a static method. And this method introduced in Java ten when version after introducing of methods. Got it. So this method actually introduced in Java ten. And it allows you to create an Unmodifiable copy of an existing collection. So if you have a list collection which is modifiable, and if you do this copy of on that list, it will create another collection on that newly created collection would be unmodifiable which means immutable. So if you want to create a list and you want to make it immutable by starting, then you can make use of this off method. And if you want to create a list which is changeable in starting and often when you want to make it unchangeable while implementing further in your program, then you can make use of this copy of method for creating an immutable version of that particular list. So basically this copy of method is used to create a new list that contains the same elements as the given collection. And the new list is unmodifiable, meaning you cannot change its contents after it is created. As simple as that. So let me demonstrate that to you. Let me create a new file for this copy of test Dot Java and let me import our list. So I'm importing these imports and SVM and let me create a list first. Normal list. List of integers. List equal to new ArrayList. And here I'm passing some values into it. Restart as list. 123456. Okay, so I want to create a immutable version of this actual list. Then you can use of copy of method. Right. So for that list of integers and here I'm giving the name as copied list equal to our interface name dot copy of the actual list which we are gonna copy which would be list right. So if you observe carefully here it's accepting a collection. That is why we are passing our list collection into it so that we can copy and create a immutable version of it. Now, if you try to add some values into lists, that would be possible, like list dot add ten. This is possible, but if you do this copy list dot ten and ten then you would probably get an exception. See what you got. Unsupported operation exception. And let me just comment this out. Now it is possible. See it has added and it doesn't give any exception. So if I print this list now insert list now it probably is saying the ten also added to the list. See? Whereas if you try to add something into this list or remove something into it, you would probably getting unsupported operation exception. And just like a half method. This copy of method also doesn't support any null values. Let's try to add a null value into it. Okay, now if I run this program now see you will get a null pointer exception because in this list we are having null. In normal list it won't matter if you are having null or not. So if I have committed this out, if I run this now, it probably able to see that null value also included in our list. But when you're creating copy of, you should make note of that. You can't pass null values into the copy of method. If this particular collection containing null values, then this copy of method would likely throw null pointer exception. So, based on this example, you can say that the original list is a mutable list containing 1 to 6 elements, and this copy of method is used to create a unmodifiable copy of this list and stored in this copied list. Any attempt to modify this copied list now will result in an exception such as unsupported operation exception. But and if you try to add null values into your original list, and then you try to make a copy of that particular list, then you'd probably getting null pointer exception. So that's all about our copy of Method Guys. Hope you got the clarity by now since we don't have any further methods in our list collection, and as of now, we have explored each and every method available in our list interface. So that's it for this video guys. Let's meet up in the next lecture. Up till then, stay happy and stay secure.