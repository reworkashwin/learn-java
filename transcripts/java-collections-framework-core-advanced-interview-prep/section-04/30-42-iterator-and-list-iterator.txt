Hello everyone. Welcome back to our Java Collections Framework course. Today in this video we are going to explore iterators and list iterators. So these are the key interfaces that allows you to traverse collections like list and sets and work with the elements within them. Whether you want to move through elements one by one, or remove elements during iteration, or even modify them. Iterators. Unlisted iterators have got you covered. Let's start with the iterator interface. So an iterator allows us to traverse through collection in a forward only direction. What is mean by that? So throughout our collection we can move out from our elements only in forward only direction. We can do backward direction like this. We can't access elements in a backward direction. That's what it means. It only access elements in a forward only direction, and it is most commonly used with lists, sets, and other collections that implement the collection interface. So whatever collection, no matter what it is, if it implements the collection interface, then it probably gonna have access to the iterator and listiterator both. Now let's take a look at the basic structure of an iterator. It provides three key methods such as Hasnext next method and then remove method. So these are the three key methods provided by an iterator interface. So what exactly this Hasnext do is it checks if there is another element in this collection. Let's suppose we have this collection. We have elements in such manner 234, five and start. Our cursor will be here. So whenever you call Hasnext method on our iterator interface object, then it immediately checks for the element which is present after the cursor. Note this point it only checks the element which is after the cursor, not at the cursor which is currently at position. Focus on our point. It only checks for the element if it has or not next to our cursor. So if our cursor at this position for it checks for the has next element or not, so it checks for the element five. So it returns true if there is a element and it returns false if we have reached the end. So if we have reached the end. So on our final element, if we have done this Hasnext method, then we probably get a false result since we don't have any element next to our last element. So that's the reason why we get false as a result when we reached the end. Okay, then we have this next method. So it probably returns next element in the iteration. So from the cursor it will just returns the next element. So unstart. If you do the if our cursor is at this position, if you call the next method on our iterator interface object, then it probably returns true. So if our cursor at this position, if you do the next then it will just return five. As simple as that. And it throws no such element exception. If there is no more elements so well at this position, if you call the next method, then it probably going to return no such element, since after five we don't have any elements, and then we have remove method to remove elements from our collection. So whenever you call this remove, it just removes the last element written by the iterator. So it on last it has written the three whatever elements it has written previous. Let's suppose if our cursor is at this position, which means on previous it has returned this three, right? So if you call remove method on our iterator now, then it probably removes this number three. And here you have to note that this remove must be called after next so directly you can't call remove without calling next. If you do that, it will just throw Illegalstateexception. So let's suppose if our cursor is at this position and if you call remove method. So before our cursor we don't have any elements to remove. In such case it just throws illegalstateexception. Now let's explore a basic example with our understanding on iterator interface. Okay, for that let me open our VSCode. And here I'm creating iterator example dot Java. And here let me do the necessary things. First let me import our iterator import Java dot util dot iterator. Then let me import a collection so that we can test this iterator upon Java dot. For that I am importing ArrayList and let me also import list also just in case Java.util dot list. Okay, so far I have added these three imports from our collection framework. And let me call our public static void main method and PS VM shorthand for public static void main in our vs code. So here let me create a list object. List of string. And let me give names here names equals to new and ArrayList object. Okay, now we can add names into our names object by doing names dot add. First of all, let me add Alice and then let me add Bob. And then here I'm going to add Charlie. So I have added these three elements into our list collection. Now let us perform iterator operation on our names collection okay. For that let me create a iterator object iterator of string. Since our names collection is a string, that's why this one also going to be string and iterator equals to. I have given name as iterator for our iterator object. And here let me call names dot iterator method. We all know where does this iterator method lives. So if you press Ctrl and click on this iterator, it will take you to its origin. So currently this method is being used which is present in our list dot class since we have used a list as a collection type. So if you use set here then it probably gonna use set iterator. However, ultimately this iterator method is inherited from our collection interface. However, if you want to prove just press Ctrl and click on this collection. And if you scroll down a bit here you can see this iterator method is declared in our collection interface. So we all know that we can traverse through collection in a forward only direction using this iterator object. So let's do it. Let's achieve that implementation for that. Here I am Calling while and let me set condition as iterator dot hasnext. So currently we are having elements in our collection as Alice, Bob and Charlie, right? So whenever you do iterator dot hasnext currently our cursor at this position. So it checks for the next element which is this Alice. So if this method found any element next to cursor it will just returns true. Right. So if true means then only our while condition will run. That's the reason why we are setting condition like this for our while loop. So after if our while condition got true, then I want to store the name in a separate variable which would be string name equals to. So to get the next name here we have to call the iterator dot next method right. So currently our cursor at this position Disposition. When you do this, it will just return the next name to the cursor. So currently on the first iteration it would be Alice. So Alice will get stored in this name on first iteration. And then let me just print out our name so that we can see it clearly. So if I run this program now now you can probably see each name getting printed. See Alice, Bob, Charlie. So after first iteration, our cursor moved to this position. Whenever you do this next method, it just returns this Alice. And then it sets this cursor to this position. And now our cursor at this position. Then again it checks for the condition hasnext. So it found the next element Bob. So that's why again this condition will return true. So since this condition is true our while loop will run. So next here again we are calling next method and storing that particular name in this name variable. A next name would be Bob, right? Since our cursor at this position, our next name would be Bob. So Bob will store in our name variable and it will just printing that name. So on third iteration it again checking for the condition has next. So since our cursor at this position after returning the bob it will just move this cursor to this position. Right. So since our cursor at this position it checks for the hash. Next again. So we have the element next to our cursor. Then just it will return the true. As a result, since our condition is true all while loop will again run and then this operation will again happen. Iterator dot next. Then it will just send our cursor to this position and it just returns the Charlie as a result. So on that Charlie will be stored in our name variable. And here we are just simply printing that name again Charlie. And after third iteration now it will check for the condition again at this time has next will return false. Since after cursor we don't have any elements, right? So it will just simply return false. That's why our while loop got terminated and after our program will stop running. So that's how we got these three elements in our output. So what if if I want to remove an element I wanted to remove this pop when it occurs in our cursor. So if we found Bob in our collection I just want to remove it. So what can we do that to remove this pop. Since we are moving from one element to another element in a forward only direction, whenever we reach Bob, whenever our cursor at this position and after returning the bob, we can check if the element has bob or not. If it is Bob, you can just call iterator dot remove method to remove the element before our cursor. So to remove this element our cursor has to be at this position. For that let me do this here. I'm checking if name dot equals sorry equals Bob. Whenever we found this element, this bob element in our name let's just simply call iterator dot remove method. Okay. So we are getting name means we have called this next method right. So it just returned that particular name and it has moved cursor to further and start our cursor at this position. So after returning Bob it has moved our cursor to this position. So that's why whenever we call remove method now it is removing the element before the cursor. So whenever we do this it is just removing the element before the cursor which is Bob. So here we are. Just make sure of it. We are only removing the element. Bob okay, if you don't keep this condition, it will just remove the every each element other than just removing the bob. Let me prove this to you. First, let's run this program. If I run this now, now you can see, uh, Bob got removed, but we don't see any output yet since we need to print our collection after this operation. Right. Let me just call s out here and let me print our names. Let me keep message as updated names. Let's just paste our name. So if I run this, I can probably see updated names as Alice and Charlie. Only since we have removed the Bob from our collection. So what if if you want to remove each and every element after processing. In such case you don't need to make use of it. Make use of this condition. You can just simply call iterator dot remove after printing each element. We are also removing the printed element. So if I run this now, you can probably see updated list updated name as empty since we have removed each and every element after processing it. So that's how our iterator works. And one more important thing to remember when working with iterator is that they are fail fast. This means if the structure of collection is modified outside the iterator, for example, by adding or removing elements, the iterator will throw a concurrent modification exception. Let me show you what happens if you modify the list directly while iterating through it instead of removing. Here I am setting a condition. If name equals Charlie, then I would just want to add a name to our list, not to our iterator. Here I am adding name to our list instead. So here I want to add a name David. This method add string is undefined for the type string. Sorry, not name names. Okay, now if I run this program here, you can probably see it is printing these three. But while triggering Charlie after printing Charlie, it just triggered Charlie right on our last iteration. Then it tries to add names that add to our collection, not to our iterator, but it tries to add this element to directly to our names collection. So that's why we got this exception Concurrentmodificationexception. As you can see, adding an element directly to the list during iteration causes a concurrent modification exception. Iterators are designed to be fail fast to prevent unpredictable behavior when a collection is structurally modified during iteration. Now let's move to the list iterator interface, which extends iterator. So Listiterator allows us to traverse a list in both directions forward and backward. It also gives us additional capabilities like modifying elements during iteration, and Listiterator is available for any class that implements list interface, such as ArrayList. Unlink Unlinked list. These two implements list interface. Right. So that's why you will be having support of Listiterator to these two implementations of list interface. You will also be seeing this list iterator availability on since stack and vector also the implementations of list. So here are some key methods of Listiterator. They are Hasnext and next. So just like iterator these move the iterator forward. So these are used to move forward in our collection. Whereas next will just returns an element and moves the cursor one place further right. Whereas Hasnext is used to check for the next element. If it found the element, then it will just return true and while at the end while our cursor at the end, if it calls the hasnext again, then it won't find the any element here. In such case it just returns false. Just like these two we have. Net has previous and previous method. So these are basically traverse our list in backward direction where has previous is used to check for the previous element. If our cursor at this position, it is used to check for the previous elements. If it found one element then it will just return true or if if the cursor is starting. If you call has previous on that element, then before our first element we won't find any element there. In such case it just returns false, whereas previous method is basically used to move previous in our collection. Let's suppose we have this collection elements currently our cursor at this position. Let's assume if you call next now, then cursor will probably move to this position and it will just return this element. And we also have next index and previous index methods. These methods are used to return the index of the next and previous elements. Let's suppose we have this collection 12345. For our list collection, we will be having index based access. Right. So let's suppose we have this index based access for these elements 01234. And currently our cursor at this position. If we call next index now then you will probably get the result as two. Since this elements index is two. And if you call previous index method on this position while cursor at this position, then you will probably get the result as one. Since previous to this cursor, we will be having this element to which index is one. So that is how we will be getting one in such case. And after that we have this set method which is used to replace the last element. Remember this point which is used to replace the last element returned by the next or previous with the specified element, whatever element you passed here, so it just replaces the last element which is returned by the next method or previous method, and then we have one more method add, which inserts the specified element. Whatever element you passed here, it will insert that specified element at the current position of the iterator. Wherever cursor is present, it will just add that element. So if we call add method and pass the 22, in such case it will add that element. In between these two, two and three we will be having 22 in between these two. So that's how our add method works. Now let's explore it through an example. For that let me open our VS code. So currently on our collection wheel we are having these three elements Alice, Bob Charlie. And instead of iterator let me call list iterator and let me change this to Listiterator. So here we need to call method write any small now will be no error gone. Okay, so if you press control and open this. We are currently calling this method which returns a list iterator over the elements in the list. Let's get back to our example here. Let me just do forward traversal. Let me keep comment here as forward to Vercel. And also let me just print this assert message as forward traversal. Let me just remove this thing. And here everything is fine. And we are calling iterator does has next to check if we have the next element or not. And then we are just moving our cursor one element further and storing previous element in this name variable, and simply printing out that name value to the output. And let me just print backward traversal here. Let me copy paste this while loop over the while loop here also for our backward traversal. So instead of hash next let's call hash. Previous okay. Not just previous hash previous. Now it's fine here instead of next let's call previous method. Now this will print elements in backward direction. And let me comment this thing out. We don't need to see updated once since we are not changing anything here. So here we are just traversing our list collection which is names in forward and backward directions. Let me run this program now. Now you can probably see elements as printed out in Alice Bob, Charlie order, which is forward traversal and backward traversal such as Charlie, Bob, Alice. Got it. So basically in this example we use a Listiterator to traverse the list in both directions. First, we move forward through the list using Hasnext and next methods. Then we reverse the direction and traverse backward using hash. Has previous method and previous methods, and one of the powerful features of Listiterator is that it allows us to modify elements during iteration. Let's take a look at how we can do that. So in this position, let me just do this. If name dot equals Charlie okay. Then I would just want to replace it with Charlie. For that let me call our iterator dot set. Let me pass Charlie. Okay. And one more thing here I want to do is if name dot equals Bob, then I would just want to replace it with character dot set. Here I want to set it as David. And let me comment this part here. I'm doing it on forward traversal after modifying. Let me just print out our updated values on our names collection. Okay. So here I'm running it. You can probably see we have done a forward traversal. After that, we got elements as Alice, David and Charles instead of Alice, Bob, Charlie. So here we have modified these two elements while iteration. So that's the benefit of using Listiterator. So here basically we use a set method to replace the name Charlie with Charles. And here at the pub we have just replaced that Bob with this David. Or if you want to just add David without replacing Bob, then you may use add method. So this will let me run this. Now you can see. Oh sorry. Got to debug. Let me run this okay. Now here you can see Alice, Bob David Charles. So it just inserted a new element David after Bob. As you can see, Listdata is incredibly flexible, allowing us to modify the list during iteration without causing errors. Now let's take things a step further with a more complex example. Imagine you are developing a task management system where you want to iterate through a. List of tasks. Mark some as completed. And add new tasks dynamically. Listiterator makes these things easy. Let me create a class with the name task class task and here let me give name, string name and boolean is complete. Let me set constructor as task of string name. Okay, so here I'm just setting this name to the name which have passed the user. And here I'm setting. By default this start is complete to false. Okay. And for every class, we'll be having a override method. toString. We'll be having that method. We can override it according to our need. So let me override it. Public string toString. It is throwing some error since here we need to return a string until that it will throw that error. So for that let me just return string as. Here I'm giving name concatenating with this message name plus. And here I'm setting a condition is complete. If it is complete then question mark. In such case let me print message as completed and if not in such case I'm just returning an empty string. Okay, so if it is completed then we are just returning name plus completed or if it not, we are just returning this name with empty string concatenated. That's it. Now here instead of names, Let me just give tasks and let me remove this thing. And here, let me give. Tasks. Dot listiterator and let me keep it as a short form for listiterator. And I will be doing everything after it. Okay, before this and here instead of string here, I want a task type of list collection in that we can include these two. Name and is complete status. And for this listiterator also let me give it as task. Okay. Now here I want to add three elements into our tasks collection which is of list type. So here I have added these things. Tasks dot add new task of task one. Here I have passed. This task name as task one. Then task two. Then task three. Okay, now let me call while loop. While here, let me set condition as la Dot has. Next. And let me create a task object. Task. Task equals to la dot next. So whatever task we find next we will just storing the task into this task object. As simple as that. Okay. Now here I want to mark task two as complete okay. For that here I'm setting condition as if task dot name dot equals. If we do just task dot equals then it won't work right. So here we need to access that particular task name by calling task dot name. Then here I'm checking the name with task two. Task two. In such case here I want to set task status as complete. I mean true task dot is complete to true. And now I want to add a new task after task one. For that, let me set condition as task dot name dot. Equals to task one okay. Now here I just want to add like dot add new task of. Let me pass the task name as task 1.1 okay. So after everything let me just print. Out our tasks. So for that let me use for and here I'm using for each task. Task colon tasks okay. Here I'm just simply printing out each task. Now if I run this program now you can probably see we have task one. Then we have added task 1.1. And for task two we have set the status as completed. And we got task three. Basically in this scenario we are working with a task class. We use list iterator to traverse through a list of tasks. Mark specific task as completed and even dynamically add new task during iteration. This shows how powerful list iterator can be in real world applications. So to summarize. Iterator is perfect for simple forward traversal when you just need to read or remove elements. On the other hand, list iterator provides much more power, allowing you to move the collection in both directions, modify elements also, and dynamically change the structure of your list. As you can continue working with Java collections, you will find both these tools invaluable. That's all for now. On Iterator and List iterators. In the next section, we will dive deeper into the difference between these two interfaces and explore more advanced collections concepts. So up till then, stay tuned.