Hello everyone. Welcome back to Java Collections Framework course. Today in this video we are going to explore Concurrent collections, a powerful set of tools for working with collections in multithreaded environments. Unlike traditional synchronized collections, concurrent collections are designed to handle high level of concurrency without compromising the performance. So what exactly are concurrent collections in Java? Java's concurrent collections are part of Java.util.concurrent package, and these collections are specifically designed to be used in highly concurrent and multi-threaded environments. They differ from traditional synchronized collections in that they allow for much better scalability and performance, even when accessed by multiple threads simultaneously, and synchronized collections like those created using collections dot synchronized a list or collections, dot, synchronized map, etc. use coarse grained logs which can lead to performance bottlenecks when main threads are trying to access or modify the collection at a time. In contrast, concurrent collections use techniques like log stripping, non-blocking algorithms, and optimistic concurrency to minimize contention and improve the throughput. Now let's talk about key types of concurrent collections. Basically, Java provides several types of concurrent collections, each designed to meet the specific concurrency needs. Let's explore some of the most commonly used ones, such as concurrent hash map. It is one of the most widely used concurrent collection concurrent hash map. A concurrent hash map is a thread safe version of hash map. It achieves this by dividing with much better performance in concurrent tournaments. It achieves this by dividing the map into. Segments and locking only individual segments during updates, rather than locking the entire map. This allows multiple threads to operate on different parts of the map simultaneously. Let's explore it through an example. For that, let me open our VS code, and this time let me open a different folder with the name Concurrent Collections. Let me select this folder. Let's create a file with the name concurrent Hash map test dot Java. Okay, let's minimize it and let me do the necessary things. Public class concurrent hash map test PVM short for public static void main. And on top of that let me import some classes. So we all know that where does this concurrent hash map Presets. It's obviously it resides in Java. Dot util.com current package Concurrenthashmap. Okay, now let's try to create a hash map collection using this concurrent hash map. For that we need to define type first which is of concurrent hash map. And then first one would be string. Key would be string type and value would be integer type. And let me give its name as map equals to new concurrent hash map. As simple as that. Just like creation of a normal collection and to add elements on map, we use the method known as put right here. Also we can use that method map dot put. And here key would be Alice and his age would be 23. Age or value whatever you may call map dot put then key would be next thing Bob and his value would be 21. And again, let me add one more element Charlie with value 24. Okay. So to retrieve element, just like normal collection, we may pass it in over s out. We may use map dot get method just like a normal map. So here I'm keeping message as age of Alice plus map dot get Alice. So by using key we can get its value right. So that's what we are doing here. Let me just remove this extra column. We don't need it and that's it. If I run this program now now you can probably see the age of Alice as 23. See. So what is the advantage of using this concurrent hash map? Now if you want this type of operation, if you want to perform this type of operation, we might have used a normal hash map instead of using this concurrenthashmap. Right. So it is used in concurrent environments. So let's try to add new elements concurrently. Adding new elements concurrently which means let me call runnable and let me give its object name as task1. Equals to. Here I'm using lambda expression map dot put. This time I'm concurrently adding elements into our map David and its value as 24. And then let me try to add one more element concurrently. Runnable task two. This time equals to lambda expression map dot put eve and its age as 25. And this Charlie's age would be 22. Okay, now if I start these two threads, let me also create two threads before starting thread red one equals to New thread in which. Let's pass task one. If you know multi-threading concept, then you can easily understand what is going on here. If you don't getting it, just don't worry. I will explain it later after processing everything. And here I'm creating a new thread with the name thread two equal to new thread in which here we need to pass task two. Wait a minute. Task two. Right. Okay. Now fine. Now we can start our two threads by calling the method upon it. Start just like thread one dot start and then thread two dot start to start thread two. We should ensure both threads finish the execution. For that, let me join these threads in a try catch block since they throw interrupted exception, that's why I'm keeping it in. Try catch block. Thread one dot join. And then here also I'm joining the thread 2.2. join. An exception would be interrupted exception, and let me handle that exception by just printing a dot print stack trace. And finally, let me just print out our final map final map colon. Then here I'm just printing our map. So basically in this example we are demonstrating how to use a concurrent hash map in Java, which is thread safe variant of hash map that allows concurrent access and updates by multiple threads without needing to explicitly synchronize blocks of code. So at this line, we are actually creating a concurrent hash map. A concurrent hash map named map is created where the keys are of type string and values of type integer. Unlike HashMap, concurrent HashMap can handle concurrent modifications by multiple threads without throwing a concurrent modification error. And here we are just simply added these three elements. Alice, Bob, Charlie as keys and their values also. And then at this position we are just retrieving the age of Alice by calling Map.get method, which retrieves the value as 23 for us. And here we have created two tasks task one and task two, which are defined using lambda expression. Each task inserts a new key value pair into the map. Task one inserts David with the age 24 and task two inserts Eve with the age 24. Sorry 25. Then we have created these two threads. Thread one and thread two are created to run the two tasks concurrently simultaneously. This simulates a situation where two threads are trying to modify the concurrent HashMap at the same time, And then we are just starting this through threads by calling the method upon them start. And at this part we are ensuring that these both threads finish. So here the join method ensures that the main thread waits for thread one and thread two to complete before proceeding. This guarantees that both concurrent write operations are finished before we print the final map. Then we are just simply printing the map with the message final map. Finally, the map will get printed to the console showing all the key value pairs, including the ones added by two threads. Also, the output should log. So if I run this program now, now you can probably see the output as final map. Then Bob 21 if 25 lies 23, Charlie 22 and David 24. So you can see these elements in order since hash map doesn't follow any order, right? So the key points here is thread safety. Concurrent Concurrenthashmap allows multiple threads to access and modify the map concurrently, without causing concurrency issues or needing manual synchronization and concurrent modification. Threads like thread one, thread two run concurrently and modify the map by adding new entries. Since we use concurrent HashMap. These modifications are handled safely and efficiently, and one more key thing here is no explicit synchronization, unlike HashMap where you would need to manually synchronize, read and write operations if used in a multi-threaded environment. Concurrent hash map. Whereas it takes care of the synchronization internally, this makes it a great choice for situations where you need to perform concurrent modifications in multi-threaded program. Now let's explore copy on write array also. And next we are having copy on write ArrayList, which is another useful concurrent collection in Java.util.concurrent Concurrent package. It's a thread safe version of ArrayList where every modification, like adding or removing elements, creates a new copy of the underlying array. This means the copy on write. ArrayList is particularly suited for scenarios where you have many reads and few writes. Note this point many reads and few writes. You particularly use copy on write. ArrayList. In scenarios such as reading how many reads on few writes. Here the reads can be happen concurrently without any locks, while writes involve copying the entire array, which ensures thread safety. Now let's also explore copy on write. ArrayList through an example. For that, let's create a new file with the name copy on write. ArrayList. Demo dot Java. And here also let me do the necessary things. Here I'm importing this to java.util.list and Java.util.concurrent. Copy on write ArrayList and let me do PS VM. Public static void main. And here let's create a list of type list. But the object called as copy on write ArrayList. Let me do this. List of string object name would be list new. Copy on write ArrayList and let me add two elements in it. Let's start add Alice and list dot. Add Bob and to read elements in list. Let me print initial list here. Initial list column. Let me call list here. So here I'm just printing the elements you may call or reading the elements. Okay then let's try to add elements concurrently just like we did it in our concurrent HashMap by using the runnable task one equals to using lambda expression list dot add. Charlie. Then again runnable task two. This time equals to lambda expression list start. Add. This time let's add David. Okay. Then let's create two threads. Thread thread one equals to new thread in which we are passing task one. Then thread thread two equals to new thread in which. Let's pass task two. Then let me start our threads by calling start method on our threads. Thread one dot start. Then thread two dot start. And let's ensure that both threads finish execution by calling thread one dot join in our try try catch block. Thread one dot join. Then thread two dot join Chain and it throws interruptedexception. And let's handle our exception. A dot print stack trace. And finally, let's print out our updated list. Updated list. Column list. In this example, we use a copy on write array list to add new elements concurrently, since the list is copied on every write operation. Reads are thread safe and don't require any synchronization. If you run this program now, it would run just as expected without throwing any concurrent modification errors. See. Initial list would be Bob, Alice, Bob, and after updating and adding these two elements concurrently, our list got updated to Alice, Bob, Charlie and David. So that's how our concurrent collections work. And we have another important concurrent collection which is blocking queue. It is an interface which is commonly used in producer consumer scenarios. Use a blocking cube blocks the producer when the queue is full and blocks the consumer when the queue is empty. When queue is empty, it blocks the consumer trying to enter in it, and when our queue is full, it blocks the producer, and when queue is empty, it blocks the consumer. Providing built in synchronization. So basically Java provides several implementations of blocking queue including array blocking. Queue linked blocking queue reactive blocking queue list. Let me list all of these here. Array blocking queue. Linked blocking queue and priority blocking queue. So let's explore it through an example. For that, let me open our VS code and let me create a new file for our blocking queue demo. Let me give it name as blocking queue demo dot Java and let me do the necessary things. PS VM and here I'm importing. So first of all let me import these two array blocking queue interface and blocking queue interface and array blocking queue implementation of this blocking queue interface. Now let's create a blocking queue with a capacity of three elements. Blocking queue of string queue equals to new array blocking queue. Here we need to pass capacity. This example we have already explored in our previous file exploring interfaces module two. So if you watched that video carefully, this example will even easy for you to understand. Okay so what this capacity is basically we have set this size to three to this queue. If we add more elements than three let's try to do this. Queue dot add low queue dot add I q dot add. So here I'm adding four elements. If I run this now you can probably see we'll get an exception. Illegal state exception. Since this capacity is set to three we can't add more than three elements into it. So if I remove this one element then it just adds those elements into our queue. But in this concurrent collections example, let's try to add elements concurrently instead of just randomly adding it. So here I'm creating a producer thread using runnable. Runnable producer equals to calling lambda expression again. And in lambda expression let's use try catch block on try. Let's try to add three elements. Since we are having the capacity of three q dot. Put item one and let me copy paste this two times unmodified two, three and two Okay. So we have added three elements. Now let me just print out a message, Rubio said. All items. Okay. Then it throws interrupted exception. Let's handle that exception. E dot print stack trace. And here we need to keep semicolon to ignore that error. And now let's produce consumer threads using the same runnable. And this time it would be consumer. Let's keep lambda expression again. And here let's just, uh sleep our thread for 1000 milliseconds by calling Thread.sleep method, in which we need to pass those in as milliseconds. And then System.out.println. Here I'm printing a message consumer. Plus here I'm just calling you dot take method. So what does this tech method actually do? If you hover on it, you can read its description. It retrieves and removes the head of the queue, waiting if necessary until elements becomes available. So as we have our head or blocking queue blocks the producer when the queue is full, which means here we have mentioned capacity. If we add more than elements, here more than capacity. If we add elements more than its capacity, it just blocks the queue, which is nothing, but it blocks the producer to produce more element. And also it blocks the consumer when the queue is empty. So after removing each and every element by using the take method, just block the consumer to keep removing the elements even if the queue is empty. So if queue is empty, we can't remove anything, right? So it just stops the blocks the consumer. So that's the meaning of it. So here basically we are just retrieving and removing elements and printing messages consumed the whatever element. We have removed. We are just printing that element's name. It would be. Item one. Item two. Item three. Okay, let me just print this for three times again. And I forgot one thing here. We need to keep it in a try catch block since it also throws interruptedexception. Let me keep in. Try catch block. Okay, let me set its indentation properly. Now let's handle this exception and drop dead exception on e.printstacktrace. And let's keep semicolon here to avoid that error. Now let's try to start producer and consumer threads by creating these threads. Thread producer red equals to new thread in which we need to pass our producer and then thread. This time consumer thread equals to new thread in which this time we need to pass our consumer thread to start our threads. We need to do this and let's start our threads. Producer. Threads. Producer thread dot start and then consumer thread dot start and wait for both threads to finish their job. And we can join them by calling producer dot join or consumer thread dot join. So basically what does this join method do is okay I forgot one thing here. It just throws an interrupted exception. Since we need to keep this thing in a try catch block. Let's do that. Try catch and absorption as interrupted exception. And let me handle this E.printstacktrace. Okay, so in this example we use an array blocking queue to demonstrate a producer consumer pattern. This producer adds items to the queue and consumer retrieves them. If the queue is full, the producer will wait until space is available, and if the queue is empty, the consumer will wait until an item is available. Working queue handles all the synchronization behind the scenes. To run this program, it would run as we expected. See it produced all items, then it just consumed each item at a time. Item one. Item to item three. So what are the advantages of concurrent collections? So let's summarize some of the key advantages of concurrent collections one by one. So first one here is better scalability. So it keeps better scalability by reducing lock contention. Concurrent collections can handle high number of threads efficiently. So that is why we are calling it as better scalability. Then it has Non-blocking rates. What exactly mean by that? So collections like concurrent hash map and copy on write ArrayList allow multiple threads to read concurrently without blocking. Right. So that's why we are considering non-blocking reads as an advantage in our concurrent collections. And we have one more advantage which is optimized for concurrency. So the concurrent collections use techniques like log stripping. We all know that a non-blocking also and optimistic concurrency to reduce bottlenecks in multi-threaded environments. So that's what it means by optimized concurrency, which is an advantage of our concurrent collections. So because of these advantages, concurrent collections are ideal for applications with a high level of parallelism, such as web servers, real time systems, or any application where multiple threads need to work with shared data. Now you might be having a question when to use concurrent collection. So answer to this is use concurrent collections when you expect a high number of threads accessing or modifying the collection simultaneously. For example, if you are building a web application with many users accessing the same data, concurrent hash map is a great choice at that situation and use copy on write ArrayList. When your application has many reads and few writes, the overhead of copying the array on every write is justified when read, performance is critical. So when you explore a news website, then users are not writing on that. You would be having organization to write a news on that website. So there are more reads and fewer waits for a for an article. You you might be having billions of audience reading it. So in such situations you might be weary. You might use this copy and write a list instead, and you should use blocking cues for producer consumer patterns, where you need to build in thread synchronization to manage a shared queue between producers and consumers. So best example for this is e-commerce platforms such as Amazon, Flipkart, etc. so in summary, concurrent collections provide a powerful, efficient way to handle shared data in multi-threaded environments. Whether you are using a concurrent HashMap for high performance key value storage and copy on write ArrayList for concurrent reads or blocking queue for producer consumer patterns. These collections are designed to scale with your application's needs. So that's all for this video guys. Hope you got the clarity by now. What exactly are concurrent collections and why do we use them and where do we use them? So that's it for this video. Let's meet up in the next lecture. Up till then stay happy and stay secure.