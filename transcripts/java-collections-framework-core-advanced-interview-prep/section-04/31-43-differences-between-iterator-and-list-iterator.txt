Hello everyone. Welcome back to our Java Collections Framework course. In this segment we are going to talk about the key differences between iterator and list iterator. Although both are used to traverse collections, each of these interfaces serves a different purpose and understanding the difference will help you use the right tool for the right job. So let's start with the fundamental distinction basic difference, which is collection type supported iterator can be used to traverse any type of collection. It might be list, show set or even map. It's a more generic interface for navigating through the collection. On the other hand, list iterator is designed specifically for the traversing the list collection list types such as array list, link list, vector stack, etc. and it cannot be used with sets, SKUs or any other collection which are non-list collections, you cannot use Listiterator with them, so let's explore it through an example. Now, if you observe this example, in this example, we are using an iterator to traverse set fruits. This is something that Listiterator cannot do. Since set is a Non-list type set queue map. These are Non-list types, so you can't use Listiterator on them. Since iterator is the method of collection, which is the parent for all the collections, you can use iterator on any collection in our collection framework. It will either be list, set, queue, maps, etc.. Let me prove this to you. If I run this program now, you can probably see this set is being iterated and printing all elements apple, cherry, banana which we have added into it. So instead of this, let's try to access list iterator on fruits. List iterator. Let me also import it on instead of iterator. Let me call list iterator method. And here you can see the method list iterator is undefined for the type, since on fruit you can't see the Listiterator. That's why we are getting this method as undefined, but if it is list instead of set. Let me change this to list on here. Let me change this to queue. Sorry ArrayList. Here we need to also import ArrayList. Let me import that one and list also. Okay. Now here you can see here we got rid of that error which we previously facing. So other than list, if you give any collection implementation here you would probably get error. Since Listiterator can be accessed on other collections. Let's try with Q let me give priority. Q. Here, let me import them also. So now you can probably see Will again facing the error, since Q is a non list type. So next reference will be having is direction of traversal one way versus two way. What exactly mean by that. Let's explore it here. Iterator allows you to move only in one direction. Either it might be on list, it might be on Q, or it might be on set. You can move in only one direction from the beginning of the collection to the end. There is no going back once you call next, you can't go back to the previous element. In contrast, Listiterator allows bidirectional traversal, meaning you can move forward and backward also throughout the list collections. Only you can't see this bidirectional access on other collections such as Q and set and map. Also, you can see this on these collections. Let's explore it with an example. So here we have created a list collection of names. And here we have added three names Alice, Bob, Charlie. It is the same example which previously explored while exploring the iterator and Listiterator. Okay. And here we are calling Listiterator on our list collection, which is names. And first of all, here we are doing the forward traversal using Hasnext and next methods. And then we are doing the backward traversal using the hash previous and previous methods. So if I run this program now you can probably see the result as Alice, Bob, Charlie. And on backward traversal you will be seeing this Charlie, Bob, Alice. See which proves our point. We can move in both directions. Either it might be forward or it might be backward. In this example we use Listiterator to move forward and then backward through the list. Iterator, on the other hand, does not offer this kind of flexibility and only allows forward wind. So let's get back to our presentation and explore the third difference, which is ability to modify the collection. Limited versus full control when it comes to modifying the collection during iteration. The iterator provides limited control. It can only remove an element using the remove method, and this can only be done right after calling. Next. Any other modification will throw an exception, however. List iterator gives you full control. You can move. You can remove, add and replace elements while iterating. This makes it far more flexible when working with lists. Let's verify it using an example. For that, let me open our VS code. And here we have this example just like previous one. Here we have created a names collection and in which we are adding these three elements. Alice. Bob. Charlie. And here we have called Listiterator on it. And in this while loop here we are modifying the collection. See, whenever we find Bob in our name, we are just adding a David element after Bob. So adding new element after Bob, which is David. And then at this condition we are checking whenever we found Charlie, then we are just modifying the current element. We are replacing current Charlie with Charles. Okay. And then we are finally printing out the updated list. So when we run this program you will probably see Alice, Bob and after Bob we have added David. Right. David. And then instead of Charlie, we will be having Charles. Since we have replaced Charlie with Charles. Now let me run this program. If I run this Java program now, now you can probably see the same output. Updated list as Alice. Bob. David. Charles. See this Charlie place? We have replaced it with Charles and we have added David after Bob. In this example we have seen how list iterator allows you to add a new element after Bob using add method, and also modify the Charlie to Charles using set method and iterator does not provide such advanced features and it is limited to the just removing elements. So on iterator you can only remove element, whereas in Listiterator you can remove add as well as replace two. Now let's move to the next difference which is index access. It is having the ability to work with element indexes. Iterator doesn't provide access to index of the current element. It only allows moving forward through the collection without knowledge of the element's position. In contrast, list iterator gives you access to the index of the next and previous elements through the next index method, and for previous element index, we will be having previous index method. Throughout these methods, we can access the index of the next and previous elements in our list collection. Let me verify it through an example. For that let me open our VS code. And here let's focus on this example. So if you observe here here we have created a names collection which is of list type. And here we have added these three elements Alice, Bob Charlie again. And here we are having and here we are calling Listiterator on our collection. And here we are calling the next index. And we are storing it in index. And then we are storing the next elements name in our name variable. And then we are just printing out the element at index. Plus we are just printing the index. And then we have given colon and then name. So our collection currently is like this, right? Alice. Bob. Charlie. Double quote. Okay. Now currently our cursor at this position and we will be having indexes like 012. So whenever we have called this one iterator dot next index just returns the next element's index which would be Alice index which is nothing but zero. Whatever element we are having after our cursor, it will just that element's index when we call this next index method. And then we are just storing moving cursor to one element further and returning this element to the name. So since here we are printing out and finally our output would be like element at index. And then index would be zero. And with colon and name would be Alice. So to prove this to you let me run this program. And here you can see the output element at index zero. We are having Alice. So after next iteration we will be having element int index one, which is pop, and element at index two, which is surely like this. We got the three elements indexes and their names. Now let's access indexes of elements while moving backward. For that, let me just copy paste this while loop instead of hasnext. Let me call has previous and to get previous index we need to call previous index. And then instead of moving forward we need to move backward using the method previous. And that's it. Now if I run this program now you can probably see this is the forward traversal which we have traversed using this while loop. And this one is backward traversal which is happened using this while loop. And you can see the indexes like 210. So which proves our point. Next index is used to get the index of next element whereas previous index is used to get the index of previous elements. And finally we are having one more difference which is collection type restriction. Finally, another important difference is that the iterator can be used with any collection that implements the collection interface, including sets, queues, and lists, which we have already discussed on our first difference. There is a slight difference between these two Namings or first difference is collection types supported. Here we are discussing about the support of the collections, and here we are talking about the restriction of the collection. So since iterator interface implements the collection interface. So that's why it will be having no restrictions on any collection. Whereas however list iterator is restricted to list only such as array list, linked list and other classes like vector stack that implements the list interface and it cannot be used with other types of collections. So that's all about differences. So these are the basic differences which you might consider while exploring the iterator and list iterator. In conclusion, iterator is a great for simple forward only traversal when you don't need to modify elements or track their positions. It works with all types of collections, making it more general tool. On the other hand, list iterator is the preferred choice when working with lists where you need more advanced functionality, such as modifying elements or traversing the list in both directions. Remember, Listiterator only works with lists, so use it accordingly. So that's it for the differences between iterator and Listiterator. Guys, in the next section we will dive into fail Fast and fail safe iterators and explore how they handle concurrent modifications. So up till then, stay tuned.