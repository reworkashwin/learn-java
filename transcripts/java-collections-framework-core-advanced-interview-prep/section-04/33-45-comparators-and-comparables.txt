Hello everyone. Welcome back to our Java Collections Framework course. Today in this video we are going to discuss two powerful tools for sorting objects in Java which are comparators and comparables. These interfaces allows you to control how objects are compared and sorted in collections, which is particularly useful when you are working with custom objects. Let's start our session by understanding what is sorting. Sorting is an essential operation when working with collections. Whether you are sorting numbers in ascending order or arranging a list of objects based on specific attributes, then understanding how to sort efficiently is crucial. In Java, there are two main ways to define custom sorting behavior through the comparable interface and through the comparator interface. These are the two ways to define custom sorting behavior in Java. So these two provide a mechanism for comparing objects, but they serve slightly different purposes and are used in different situations. So to understand these situations and these purposes let's try to understand comparable interface first. So let's start with the comparable interface. When a class implements comparable it indicates that its objects have natural ordering. The comparable interface is a part of Java.lang package, and it contains only one method, which is compare to. Let me verify this to you. For that, let's open our VS code and for our practice, let me select a different folder. Let me create a new folder here and the name comparator and comparables. Okay let me select this folder. And here let me create a new file with the name comparable test dot Java. Okay. So in languages we have this comparable. Let me open and see what's inside it. Okay I haven't opened this comparable interface. And if you scroll down a bit now you can see we have only one method which is compared to init. So the compare two method defines how two objects of the same class are compared to each other. It returns an integer which is negative if the current object is less than the specified object. So it compares two objects, right? It compares each object with other, right. So on that process it will return negative if the current object is less than the specified object, and it returns zero if they are equal and it returns positive if the current object is greater than the specified object. As simple as that, it returns positive if the current object is greater than the specified object, and this allows collections of objects to. Be sorted using methods like Collections.sort or Array.sort. Now let's look at an example where we implement comparable in custom class. Okay, for that, let me open our VS code. And here first of all I wanted to. Create a list list of students. For that I need to mention student class first. Let me create a student class first. Then I will try to create a list of students. Class students implements and this is going to implement or comparable interface. Let me pass student in it. And now it is throwing an error such as the type student must implement the inherited abstract method comparable dot Compareto. So until we don't implement this compare two method, we will be seeing error, this error on our class. So let's fix that. Let me click on this add unimplemented methods and then boom we have overrided our comparative method which is of this comparable interface. So instead of this random thing let's implement this in a separate way. So here I want to compare the student by age. So we will be having two things in our student. Let me mention that first okay. So one is string. Obviously it would be name and then age. So I have given these two properties for our student class name and age. Let me also define constructor also for this class student. Let me define it public student. Whenever we pass name and student we need to store it to these variables string name comma int age H. Then I'm doing this dot name equals to name and this dot h equals to h. Now whatever name and age we pass it will get stored in this current variables name and age. Now let's focus on this compare to method. So since it is a inter type method we need to return an integer right? For that. Or let me call compare method of integer class. Integer dot compare. And here let me pass this dot age and other dot age. Whatever object we are getting here we are comparing the other students age with the current student age. That is what we are doing in this method. O dot age. Okay, let me put semicolon here. So what this compare method doing. We all know that this compare to Compareto method, which we have provided, is a part of comparable interface in Java, and this method is used to define the natural ordering of objects of the class that implements the interface. So since our student implementing this comparable interface, which means this method is used to define the natural ordering of objects of our student class. And here this line integer dot compare and in which we are passing the start age and dot age. This line basically compares the age of the current student object, this dot age, with the age of the student object passed as a parameter, which is o dot h. Other dot h and this integer dot compare method returns a negative integer if the age is less than o dot h. Now let me open this implementation. Now here you can see x less than y. Then it returns minus one a negative value and it returns zero if x equal to equal to y. If both are equal, then it returns zero or a positive integer. If this dot age is greater than the other dot age, so x is greater than y, then it should return a positive integer, right? So that's what's happening here. Let's go back to our example. So basically the comparative method allows student objects to be compared based on their age attribute. This is useful for sorting a collection of student objects by age in ascending order. Now let's override our toString method. Public string to string. And let's give our student objects message as return name. Plus, let me keep this bracket and then age and then this closed bracket. And that's it. Now let's try to add some students into our list collection. Okay, for that, let me define this list of student and students equals to new ArrayList. Okay, this ArrayList got imported automatically. And let me add students dot add in which we need to pass student like this new student name would be Alice and age would be 23 for this man okay and student start add new student and this time name would be Bob and his age would be 21. And then student start add. Let me add one more student with the name Charlie and his age would be 22. So currently this is in alphabetical order. But I want to sort this collection by the age instead of the sorting it by name. So we can do that by calling collections dot sort method and passing students in it. So currently we are not getting any error because since the students objects class is implementing comparable interface. So in this Collections.sort method we can only pass the class which implements comparable. See. All elements in the list must implement the comparable. If it not the case then you should see an error. Let me remove this and here you can probably see the error. This method sort is a type of collections is not acceptable for the arguments. And here you can see sort sort the specified list into ascending order according to the natural ordering of its elements. All elements in the list must implement the comparable interface. Furthermore, all elements in the list must mutually comparable, such as even that compare to of e two like that. So to override of this error, we must implement the student class with comparable. And here let me also after sorting, let me just print our students collection with the name sorted students by age column. Let me call names students here. Okay. Let's just run our program. So if I run this program now, you can probably see sorted student by age such as Bob 21, Charlie 22, Alice 23 C 21 2223. We have successfully sorted our students based on their age rather than sorting them based on their name. So basically in this example, we have a student class that implements comparable and the comparator two method compares students based on their age. See it compares students based on their age. When we call Collections.sort method, the students are automatically sorted by age in ascending order, so that's what's happening in it. Now let's talk about limitations of comparable. While comparable works well for defining a single natural ordering, it has limitations. For instance, if you want to sort the same class in multiple ways, save by name and age by both, you can do that with comparable alone. You need a separate method to define the different sorting criteria. Let me prove this to you. So with this we are only sorting it by age if we want to sort it by name. Let me keep Charlie here. Okay? Now let's sort it by name instead of by age. Here let me call this dot name dot compare 210. name if I do this now, the elements are sorted in by their name in their natural order. Alice. Bob. Charlie. C so either we can do it by name or by age, we can keep multiple sortings incomparable. So that's why we are seeing this comparable interface is limited to single sorting which is natural order. So that's where comparator kicks in. Unlike comparable which defines natural ordering inside the class comparator is a separate interface used to define multiple sorting orders for the same class. What is mean by that? So just like comparable comparator interface is also the part of Java.util package and provides two key methods such as compare and reversed. So here compare method which compares two objects and returns results based on their relative order. Whereas reversed method returns a comparator that sorts in reverse order, and this comparator allows you to write external classes or even use lambda expression to define sorting logic outside the object itself. What is mean by that? We're going to explore it in this video itself. If you don't know about lambda expression, you will be getting to know about it in short. So let's see how can we use comparator to sort this same student class. But this time we will sort by name and age both. For that, let me open our VS code. And here let's just remove this part. And we don't need this method also which is of comparable. And currently in this student class we are having this these two properties. And we are setting name and age here by this constructor. And then we are formatting it. It's true string method. And now let's create two different comparators, one to compare it by name, one to compare it by age. Okay, for that let me create different classes. Class name compare later implements comparator of student. So currently this class is throwing an error. Why is this throwing an error. If you open this comparator it has this compare Overridable method. So until and unless you override this method you will be having this error. So let's solve it. If you hover on this error and click on Quick fix you will see add unimplemented methods. So if you click on that see it just overrided this compare method which is of our comparator interface. So in this name comparator we are supposed to sort our collection by name. So here we are getting the names. Here we are getting the student objects object one. Object two. Let me change this to S1, which means student one and this one to S2, which means student two. Okay, now here we need to return an integer. Right. So for that let me just return S1 dot name. Here I'm comparing it with. Let me call compare to method. And here we need to pass S2 dot name. So here basically we are comparing student ones name with student twos name. So what it does return is if this S1 name has is lesser than the s s2 dot name then it just returns negative value. If I open this compare to here, we have this compare to method with string attribute. So it just compares those two strings. It will return zero if the argument string is equal to this given string, and a value less than zero, which is negative value if this string is lexicographically Less than this string argument and a value greater than zero, which means positive value. If the string is lexicographically greater than the string argument. Okay, now let's continue our example. So here we are just returning a sorting this with the name. Okay. Now let's do the same with our h comparator. Let's create a h comparator class. H comparator implements comparator of student. And until we implement this Overridable method we'll be seeing that error right. So let's click on this add unimplemented methods. And boom here we have this compare method. So here also let's just return an integer return here. Also let's give it as s1 and S2 instead. And here now let's compare using integer dot compare method. Here we need to pass two objects. Obviously it would be s1 and s2 dot h. Right now this comparator will sort the collection based on H. So as we have these two comparators now let's try to add new elements instead of this. Let me add students dot add Alice first okay. Sorry here we need to pass this new student right. And name would be Alice. And his age would be 23. And students dot add new student. This time this person's name would be Alice. Again this is actually second Alice and his age would be 21. And then let me add students dot add new student. This time name would be Bob and his age would be 24. And then let me add students dot add new student. Let me give you name as Charlie and age as 22. And now again, let me add student Dot. Add new student Bob, let me give your age as 20. And let me again add one more student with the name Alice again. Okay, this time let's keep age as 50. Pretty much old Alice. Okay, now our task is to sort this collection with the name first. So at this time, if you do the name comparator, you will get this Alice with age 23. Alice with age 21. Then again, Alice with age 50. And then Bob with age 24, then Bob with age 20, then Charlie with age 22. If you just sort this collection with the name, you will be getting the result as in this format, But here we have the same names with different ages, right. So for this you want to sort this thing with age again. I mean you want to sort this list with smaller age. Will first allies with second smallest would be with second position, which would be 23. And then allies with the big age at the last. And then we have Bob. Bob with smallest age will be first, and Bob with highest age would be the last, and then Charlie with 22. So if you sort this thing in this format, this type of sorting is possible with comparators. Let's see how can we do that. So since the students no more implement comparable that's why this sort method is giving us error. So instead of just passing the students we can also pass comparator after it. So for that comparator let's create a new comparator object. First, let me give a comparator of student obviously. And then let's create its object with name then age comparator. Okay. This would be our object name. Okay then new here we need to call our comparator which would be name comparator. And at this position if you want to do another comparison on this comparison then you can call then comparing method comparing. And here we need to pass another comparator which would be new age comparator. Got it. So like this we can do the two comparisons at a time using comparator. Since here we need to pass a comparator. Right. So let me just pass our newly created comparator which is name then age comparator. Now this error gone. This time we are using a slightly different sort method which will be accepting a list, then a comparator. So as this comparator we have passed a name, then h comparator c. And instead of just printing this students list let me format it using our for loop for student student colon students. So for each student we'll be storing each student in the student. And then print out that student to the output. Okay. If I run this program now, now you can probably see see, as we have expected, we got the result Alice with age 21, first, Alice with age 23 at second position and Alice with age 50 at last, and then Bob with 20, Bob with 24, then Charlie. See here we have done two sorting on same collection. So this type of sorting is not even available on comparable. So that's why. Comparator is preferable in such conditions. So basically we have added students with the same name but different ages Alice and Bob. So the sorting by age will be more visible. And the sorting first arranges the students by name alphabetically. And then if the names are equal, it sort those names by their ages. You can see it here. Now while exploring our comparators theory, we have read a line here. This allows you to write external classes or even use lambda expressions to define sorting logic outside the object itself. We have already explored what is lambda while exploring our interfaces in our module two. Now let's explore Lambda on this comparator. How can we start our code using this lambda expression? So basically this lambda expression is used to shorten our code. Right. That's where we are going to do now. So here we don't need to mention these classes separately. Let me just remove these classes and add this name. Then age comparator. Let me do this instead. Here I'm calling comparator then dot comparing. And in this comparing let me pass. Let me do the anonymous call student s. Here I'm using lambda expression as start name. This is equivalent to name comparator. And then comparing. And let me call student s with lambda expression again s dot h this time okay. Let me put semicolon here. Let me do this instead. Let me enter. And here also let me enter okay. Now it's pretty much visible clearly okay. Now if I run this we will probably see the same results as previous list 21 2350. Then, Bob. 2024. Then Charlie 22. This is what it means. We can use lambda expression by this line that we can even use lambda expression to define sorting logic outside the object itself. So basically this lambda expression replaces the need for separate new comparator and h comparator classes. And here this comparator dot comparing method is used to define the primary sorting criterion. Sorting by name c and then comparing method is used to define the secondary criterion, which would be sorting by h and the rest logic, such as sorting the list and printing the results remains the same, so you might be having question when to use comparable and when to use comparator, so we can use comparable. If you want to define a single natural ordering for objects of your class. This is useful when there is one obvious way to sort your objects, such as sorting integers or strings in ascending order. And we can use comparator for multiple ways to sort the same class. For example, you might want to sort a list of employees by name, then age and then salary. So with comparator you can define as many sorting criteria as you like without modifying the original class. So in conclusion, comparable and comparator are both powerful tools for sorting collections in Java. Comparable allows you to define a single natural ordering where comparator allows and offers flexibility for creating multiple sorting orders without changing the class. You can even chain multiple comparators together for more complex sorting needs. So that's it for this video guys. In the next section, we'll continue exploring advanced sorting techniques using comparators and lambda expressions. So see you there. Until then, stay happy and stay secure.