Hello everyone. Welcome back to our Java Collections Framework course. In this video we are going to dive into synchronized collections. If you are working in a multi-threaded environment where multiple threads might be accessing or modifying the same collection, synchronization is crucial for preventing data inconsistency and race conditions. So why do we exactly need synchronized collections? It is because in a multi-threaded application, it is common for multiple threads to access shared resources, including collections like lists, sets and maps. If two or more threads modify the same collection at the same time, it can lead to unpredictable results or even corrupt data. So this is where synchronized collections comes into play. A synchronized collection ensures that only one thread can access or modify the collection at a time, making it a thread safe. So now you might be having a question how Java provides this synchronized collections. So basically Java provides two main ways to create synchronized collections, such as using the collections dot synchronized ZK methods. So what exactly are they. So Java provides static methods in the collections class to create synchronized versions of various collection types such as lists, set and map. And another way is using collection classes from Java dot util dot concurrent package. So these collections like concurrent hash map and copy on write ArrayList, which we have already explored in our previous videos, are designed specifically for concurrency and offer better performance in multi-threaded environments. We will focus on these in our future video. Don't worry about them. So currently let's start by looking at how we can create synchronized collections using the collections dot start synchronized methods. For that. Let me open our VSCode. And for this practical, let me create a new folder synchronized collections. And let me select this folder. And in this folder let me create a file with the name synchronized collection demo dot Java. And let me do the necessary things. Public class synchronized collections demo dot synchronized collections demo. So in this let me create a ArrayList. First let me zoom in a bit okay. List of string list equal to new ArrayList. So here let me add elements into it. Let's start add alias and then list dot add bob and then list dot add Charlie. So here we have this list collection. If I want to synchronize this list version to a synchronized collection we can do that like this. So first we need to call the type list string. And then let me give a name as synchronized list and equal to. Let me call collections dot synchronized list. See we have a lot of synchronized methods here. Synchronized list. Synchronized collection. Synchronized map. Synchronized navigable uh map I think. And then we have synchronized navigable set. Then we have synchronized set synchronized sorted map like that. We have a lot of methods in it. So here in this example we are going to use this synchronized list. And add this. We need to pass our actual collection which we want to convert it into synchronized which is nothing but our actual list. And that's it. Now our normal list is converted to synchronized list which is thread safe now. But this one is not thread safe if you perform operations on thread safe operations on this list. Synchronized list. Then it can handle them without corrupting the data. So basically in this example we have created a regular array list, then converted it into synchronized list using collections dot synchronized list and we have passed the list in it. Our actual list. So this ensures that any access to the list will be thread safe. And here we have one important consideration to note when using synchronized collections is that while methods like add, remove, get are automatically synchronized, iteration over synchronized collections require manual synchronization. What exactly mean by that? Let me open our example and explain it to you. When iterating over this collection this needs a manual synchronization. So basically when you call this synchronized list method and converted an object, all the methods which are add, remove, set, etc. all are converted to synchronized methods, but however, while iterating the each element in it, you need to do the manual synchronization like this by calling synchronized upon. You need to pass this synchronized list object and then you can normally iterate through it. Unlike string name column synchronized list then assert name. So iteration would take manual synchronization. So if you run this it will work. So basically in this example we manually synchronize the list during iteration using a synchronized block. This prevents other threads from modifying the collection while we are iterating over it, avoiding potential concurrent modification exception errors. Just like we can create synchronized lists, we can also create synchronized versions of sets and maps using collections dot synchronized set and for map we have collections dot synchronized map. So let's explore this also through practically for better understanding. For that let me copy paste this collection and all. Let me just modify this part if you want set here I want to change this list to set. And let me keep set here. Let me call hash set here okay. Sorry we are getting error because we haven't imported our set Java.util.set okay. Now this error gone. So instead of calling synchronized list here we can call collections dot synchronized set. See. And here we need to pass our set okay. Here also we need to change this to set. And that's why we are getting error okay. Now it's gone on instead of synchronized list. Here let me keep synchronized a set. And here also we need to change this to set. Now it's fine. Now, if I run this, this will also work exactly like our previous synchronized list collection. And if you want a synchronized map, then also you can simply create such implementation. You can call collections dot synchronized map to create this synchronized map. So here in this example we are trying to add these two elements into our synchronized map list Bob using this put method. And finally we are printing it. So basically what I mean to say is our raw collections like hash set array list. These collections are not thread safe. If you want the thread safe versions of it, you can convert them by calling this method collections dot synchronized that particular collection name. If you want set, then set. If you want map, then map, whatever it may be, okay. And passing that particular type of collection into it, you can create a thread safer version of that collection. So in this example, basically we have used a collections dot synchronized set to create a synchronized set and collections dot synchronized map to create a synchronized map. These collections are now thread safe, meaning that multiple threads can safely interact with them. So after exploring this synchronized collection, you might be having a question in your brain which is when to use this synchronized collections. So if you got this question in your brain then you are on a right track. So basically this synchronized collections are useful when multiple threads need to access or modify a collection. However, they come with some performance overhead due to locking mechanism for other threads. Every time a thread accesses the collection, it must acquire a lock, which can lead to contention and reduced performance in highly concurrent environments. Therefore, in cases where high concurrency is expected. It might be better to use the concurrent collection from Java dot util dot concurrent package, which are optimized for performance in multithreaded applications. In summary. Synchronized collections provide a simple and effective way to ensure thread safety when working with collections in multithreaded environments. Java offers several methods to easily create synchronized versions of lists. Set Unmap using collections dot synchronized x axis, whatever it might be that it should be set. Map anything. However, keep in mind that manual synchronization is required when iterating over synchronized collections and for performance critical applications. It's worth exploring more advanced concurrent collections from the Java.util.concurrent package. So that's all for this video, guys. Thank you for watching this video and I will see you in the next one.