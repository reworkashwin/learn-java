Hello everyone. Welcome back to our Java Collections Framework course. Today in this video we'll be talking about immutable collections. While we have discussed immutable collections that allow modification which are nothing but normal collections, list, set, map, etc., there are times when you need collections that can be changed after they are created. These are called immutable collections. So by definition, what exactly are immutable collections in Java? An immutable collection is a collection whose elements cannot be modified. Once the collection is created, its elements can be modified. So that's what a immutable collection means. You cannot add, remove or update elements in an immutable collection. This makes them very useful in situations where you need to ensure that the data in collection remains constant throughout the lifecycle of your application. Immutable collections are especially useful for read only data because you don't bother to modify them on constants, or when you want to share data across multiple threads safely without worrying about the synchronization. So these are the cases where you don't even bother about modifying the data. So it is especially useful in these scenarios. So while we're talking about its usefulness let's explore some advantages of using immutable collections. So first one here is thread safety. Immutable collections are inherently thread safe because they can't be modified. This eliminates the need for synchronization when accessing the collection from multiple threads. And another advantage of using it is data integrity. Since immutable collections can't be changed, they help ensure that your data remains consistent and doesn't get modified accidentally or maliciously. So that's what data integrity means. It ensures the clarity and data which it can be modified whatever you do. And another advantage of using immutable collections is memory efficiency. As immutable collections are often shared between different parts of the application, which reduces memory usage because you don't need to create copies of the collection by making this collection memory efficient. And we have one more advantage of using immutable collections, which is simplicity. So immutable collections simplify code because you don't need to handle edge cases involving modifications during the iteration or other concurrent operations. So that's why these immutable collections are known to be their simplicity. Now everything is said. How can we create these immutable collections? So to understand it, let's focus on creating immutable collections in Java. So Java provides several ways to create immutable collections. The easiest way to create an immutable collection is using factory methods introduced in Java nine. So before versions of Java nine you can see these factory methods. Starting with Java nine. The list, set and map interfaces provide static factory methods to create immutable collections. These methods include list dot of for creating immutable collections on list using this off factory method. And then we have set that off for creating immutable collections for set and we have map that off for creating immutable collections for map. Let's explore and create some immutable collections through our Java. For that, let me open our VS code and let me select a different folder. This time let me create a folder with the name Immutable Collections. Let's select this folder. And here I'm creating a Java file with the name immutable test dot Java. And here let me do the necessary things since we are going to create immutable collections for list map set. Let me import these packages, these interfaces and let me give class as. Public class immutable test PS VM. And let me create here a immutable list. Creating immutable list. So just like normal collection here also we need to. Define its type string. So here I want to create immutable list of string. Let me give its name as immutable list equals to list dot of and in this. Of method. In this factory method we can pass our list. Alice. Bob. Charlie. So this is our immutable list which can be changed. So let's try to modify it. By calling immutable list dot add method. And let's pass. Hello. And let's run this program. Now you got unsupported operation exception since you can't add or modify our immutable list. So that's why we are getting this exception unsupported operation exception. Here we are. We have tried to add a element into our immutable list which proves our point. We can't modify this immutable collection. So similarly let's create immutable set. Now set string immutable set equals to set dot of Alice, Bob and Charlie. Similarly, let's create a immutable map. Also map of string and key as integer immutable map equals to map dot off. Here let's give our key value pairs Alice and his value as 23 and Bob key and its value as 21, and then key as Charlie and its value as 22. That's it. Let's end our map. So this is the way how we can create immutable collections in Java. Since we know trying to modify an immutable collection will throw an unsupported operation exception. Let's keep our addition functionality in a try catch block. And here let me add immutable list dot add. And let me add David into it. And let me keep exception as unsupported operation. Exception. Okay. Let me handle this exception by simply printing out cannot add to immutable list. And finally let me just print out our immutable lists A sort immutable list column. Immutable. Uh, sorry. List here. And let me repeat these things for three more times. Set. And here it would be set. Wait a minute. Why? It's giving error. Okay. Here we missed E. It's fine now. Then here. Immutable map. Map. And here also. Let me keep map. It's fine. Now, if I try to print. Now if I try to run our program now, you can probably see cannot add immutable list. Since we have tried to add some elements into our list. And we got this exception. And finally we have printed our collections and we have tried to add David to list. Right. That you can see on our list, which proves our point. These are immutable. So since we have tried to add one element to our list, let's try to add one element to set. Now also you can see the difference. You will again get the cannot add to immutable set message. And if you try to modify our immutable map also, then also you will be getting the same message whatever you have presented here. And instead of just printing this message, if you do e dot print. Wait a minute E dot print stack trace. If I run this program now, you can probably see this unsupported operation exception. Then this got printed and you can see the update on our immutable set. So basically in this example we use the list dot off, set dot off and map dot off methods to create immutable collections. If you try to modify any of these collections, such as adding an element, you will get an unsupported operation exception and you can also create immutable collections using collections. Dot Unmodifiable Xk6 methods. So for versions of Java prior to nine, before versions of Java such as Java eight, Java seven. You can use the collections dot Unmodifiable zk methods to create immutable collections. These methods wrap a mutable collection and return an immutable view of it. What exactly I mean by that? Let's also explore it through an example. For that, let me open our VS code again. At this time, let me just remove everything here. Firstly let me create a list string. And here I'm creating a normal mutable collection mutable list equals to new ArrayList. And let me add some elements into it. Mutable list dot add Alice. Mutable list add Bob then mutable list dot add Charlie. Charlie. Now let's try to create an immutable view of the mutable list. List of string. immutable list equals to collections dot here and also need to import our collections. Import Java dot util dot collections. Okay then collections dot Unmodifiable list. See we have Unmodifiable list Unmodifiable collection Unmodifiable map Unmodifiable navigable map Unmodifiable set, even Unmodifiable sorted map, sorted set, etc.. So let's use Unmodifiable list. And here we need to pass our mutable list which will further be converted to immutable list. Now on this immutable list we can perform operations. This is constant. Now however, you can perform operations on it actual mutable list. So if I print out our immutable list, you can see the elements as present in our mutable list. If I run this program now. So here you can see list Alice, Bob, Charlie as elements results in a mutable list. So now let's try to modify our immutable list, which will throw an unsupported operation exception. Let's try to do it. Since it throws an exception. Let's keep it in a try catch block. And here I'm calling add method on our immutable list. And here I'm passing David as an element. Okay now let's set here exception as unsupported operation exception. And let's handle this exception a dot print stack trace. Okay. Now if I run this program, let me clear the screen for better understanding here. Now, if I run this program now, you can probably see the exception. First it will print out our immutable list. Alice. Bob. Charlie. Then we got this exception. Unsupported operation exception. Now let's try to modify the original list which will reflect in our immutable view. See here I'm adding mutable start add. Let me pass David into it and let me just print out modified original list column. Here, let me just print out mutable list. Then again a sort immutable list after origin. Original modification after modifying the original list. Let's also print out our immutable list. So if I run this program now, first of all, we have printed our Alice Bob Charlie Immutable collection, and then we have tried to add David into it. That's why we got this unsupported operation exception. Then we have again tried to add David into our mutable list, not into immutable list. So that's why after adding it, then we have printed just our mutable collection, which is mutable list. Modified original list, say Modified original list as Alice, Bob, Charlie and David. After modifying the original list, then we have printed out our immutable list. So see immutable list after original modification as Alice, Bob, Charlie, David. So it reflected the original collection. However, we haven't modified the immutable list, it just reflected the original collection. So you can perform operation on original collections. So here it is. The difference between the complete immutable collections. And these are whatever methods we are calling Unmodifiable access methods are partial immutable collection which makes sense right? So here you can modify the original collection. But whereas in our previous immutable collections which we have created using factory method, let's start off set dot off, map dot off, etc. we don't have any original collection. It is original collection and it is immutable. You can't achieve this type of partial. This type of partial immutability on those factory methods which are complete immutable list, immutable set, and immutable map. So that's the main difference between this and Unmodifiable Xk6 methods and factory methods. So basically in this example we use collections dot Unmodifiable list to create immutable view of a mutable list. While immutable view can't be modified, changes to the original list will still be reflected in the mutable view, which is something to keep in mind when using this approach. So when should you use immutable collections? Then you can use immutable collections when you have data that should remain constant once initialized. This can include configuring settings, constants or any data that should not change during the lifetime of your application. And you can use immutable collections in multithreaded environments where many threads need to read from a shared collection, but no threads need to modify it. This eliminates the need for synchronization and reduces the risk of concurrency issues, and you can use immutable collections to improve code readability and maintainability since the collection cannot be changed. You don't have to worry about the accidental modifications, which can make your code simpler and more predictable. Now let's try to explore the main difference between immutable and unmodifiable collections, because it's important to understand the difference between immutable and unmodifiable collections. An immutable collection is one that cannot be changed after it's created in constant. An unmodifiable collection is a wrapper around a mutable collection that prevents the direct modification, but still reflects changes made to the original collection as we have explored. If you want to get true immutability for true immutability, you must use Listof sets of or maps of method. These factory methods, which ensures that neither the collection nor the underlying data can be changed. If you want to immutability, you can use them, and if you want partial immutability, then you can use Unmodifiable access methods here. ZK is nothing, but if you want Unmodifiable list then you will keep a list here. If you want Unmodifiable set then you can keep set here. So it's nothing but a placeholder for our ongoing collections. So in conclusion, immutable collections are a powerful tool in Java, providing thread safety, data integrity and simplicity. Whether you are using them for read only data or shared data in multi-threaded environments, mutable collections offer a safe and efficient way to manage your data. So that's all for this video guys. Hope you got the clarity by now on Immutable Collections. So that's it for this video. Thank you for watching and I will see you in the next one.