Hello everyone. Welcome back to our Java Collections Framework course. In this segment we are going to explore two very important types of iterators which are fail fast and fail safe iterators. These terms describe how iterators behave when a collection is modified while being iterated over, which is a critical concept to understand when working with collections in Java. Let's begin with fail first, iterators first. So what exactly are fail fast iterators? These iterators throw a concurrent modification exception if the collection is structurally modified while you are iterating through it. Here, structural modification refers to operations like adding elements into a collection while iterating, removing elements from our collection that change the size of collection. Whenever you add, you increase collection by one, right? If you add one element, then you will increase the collection size by one. And whenever you are removing an element, then you are decreasing the collection size by minus one. So that's why we are referring the structural modification term to this operations. This is default behavior for most of the iterators provided by Java Collections framework, including those used in classes like ArrayList, Hashset and HashMap. And the reason they call fail fast is because they detect modification failures as soon as possible, preventing unpredictable behavior or corrupted data. Now let's take a look at an example of a fail fast iterator. For that, let me open our VS code. Let me select this folder. Okay. Here let me create a file with the name fail Fast demo dot Java. And let me do the necessary things. So here we have this example. So in this example you can see we have created a ArrayList object with the name names. And here we have added these three elements Alice, Bob, Charlie. And we have called iterator method upon this collection. And here we are iterating through it. So in our iteration here when our names is equal to Charlie then we are just trying to add this element name started debit to our collection. We are trying to add this debit element to our names collection while the iteration in progress. So a concurrent modification exception is thrown. This is because the iterator is fail fast, meaning it cannot handle structural changes to the collection while iterating. So that's the reason you will be seeing this exception when we run this program. Okay. Let's try to run it. So here I'm running our Java program. Now you can probably see it has printed all of these elements. And whenever a triggered Charlie. Here it is. Checking right here. We have set it this condition. Whenever Charlie is equal to the name which we got here, then we are just trying to add this element. So whenever we try to modify the element, whenever we try to do the structural modification, then we got this exception Concurrentmodificationexception C, which proves our point that it cannot handle structural changes to the collection while iterating. After exploring it, you may be having a question, but why do we have fail fast iterators? So the primary reason is to avoid issues with the concurrent modification without this fail fast behavior, if one thread modifies a collection while another thread is iterating over it, the iterator could end up in an inconsistent state. This could lead to unpredictable results Or even data corruption. So that's the main reason. Why do we need fail fast iterators and fail fast iterators ensures that if any structural modification occurs during iteration, the process halts immediately by throwing an exception such as concurrent modification exception, preventing the program from running with corrupted data. So, as we have explored the fail fast iterators, let's try to explore fail safe iterators now. So what exactly are fail safe iterators? So these iterators don't throw an exception if the collection is modified while iterating. Instead, they work on clone or copy of collection, which means any changes made to the collection during iteration are not reflected in the iteration process itself. What exactly mean by that? Fail safe iterators are typically used in concurrent collections such as copy on write, ArrayList and concurrent. HashMap and other classes found in the Java.util.concurrent package. All of them will be exploring in our further course videos, so don't worry about them, so fail fast. Iterators are typically used in concurrent collections such as copy on write ArrayList, concurrent HashMap, and other classes found in the Java Dateutil concurrent package. These iterators are useful in multi-threaded environments where modifications to the collection may occur simultaneously, while another thread is iterating over it. In such case, these iterators fail safe. Iterators are more useful. Now let's try to explore it through an example for better understanding. For that, let me open our VS code. And here let me create a new file with the name Failsafe demo dot Java. And let me paste our example. So this is our example for a fail fast. If you observe this example carefully instead of creating an ArrayList, here we have created a copy on write ArrayList collection. And in this we have added these three elements Alice, Bob, Charlie. And then we are calling iterator method on this collection. And then we are doing the iteration. So just like previous here we are trying to achieve the same operation. Here you can see whenever we get the Charlie in our name we are trying to add David after it. So as of now this will not trigger the concurrent modification exception because we are working on copy on write ArrayList, which is a which resides in Java.util.concurrent package. So the implementations present in this concurrent package are built for this type of situations. These are specifically built for fail safe operations, such as. When we do this structural modification during iteration, we won't be getting the concurrent modification exception. Let me prove this by running our Java program here. I'm running our Java program. Now. You can probably see Alice, Bob, Charlie here we have done the iteration. And then after iteration here you can see the updated list Alice, Bob, Charlie and David, which proves our point that this copy on write list is fail safe, whereas the other collections like ArrayList linked list, these are not fail safe, those are fail fast. So in this example we are using this copy on write ArrayList which provides a fail safe iterator. As you can see, we modify the list during iteration, adding David. This time there is no concurrent modification thrown and the program runs smoothly. However, it's important to note that the newly added element, David is not printed during the iteration. See, you can see David here because the iterator is working on a snapshot of the list taken at the start of the iteration. That's why you can't see the David here it forward traversal. Now, while fail safe iterators may seem like a more convenient option, especially in concurrent environments, there are performance tradeoffs. So let's explore the performance considerations for both fail fast and fail safe iterators. Now, while fail safe iterators may seem like a more convenient option, especially in concurrent environments, there are performance tradeoffs. Since these iterators work on a copy of the collection, any modification made during iteration require additional memory. Furthermore, the cloning process can be costly if the collection is large. This makes failsafe iterators slower compared to fail fast iterators, which directly work on the original collection, and that's why you will typically see fail safe iterators being used in specialised concurrent collections where consistency and thread safety are more important than performance. So let's summarize the difference between fail fast and fail safe iterators. So here we are considering these three. We are comparing these fail fast iterator and fail safe iterator based on these features. Okay, so when we talk about structural modification fail fast iterators throws concurrent modification exception if the collection is modified during iteration right. Whereas fail safe iterators allow structural modification during iteration and fail fast First iterators. Works on original collection such as ArrayList, Hashset, HashMap, etc. whereas fail safe iterators works on cloned copy of the collection such as copy on write ArrayList concurrent HashMap. These are generally used to copy the original collection, and when we talk about performance, fail fast. Iterators are more efficient, but it can throw exceptions if structural modification happens and whereas fail safe iterators less efficient due to the cloning, but no exceptions. So you can say that these two have their own advantages and their own disadvantages. So whenever you want efficiency and you can bear the exceptions, then in such case you in such case you should use fail fast iterators rather than using fail safe. And when your concern is more about the exceptions, you don't want to get exception. And here you know you are doing the structural modification and you want to safely work on your data, then you must use the fail safe iterators. So as you can see, the fail fast iterators commonly used on these collections. ArrayList, Hashset HashMap, etc.. Whereas the common collections of fail safe iterator are. Copy on write ArrayList. Concurrent HashMap. And the other collections which are present in Java.util.concurrent package. So in summary. Fail fast. Iterators are more efficient but require careful handling of structural modification. They are typically used in single threaded environments or situations where concurrent modification is not expected. On the other hand, fail safe iterators are designed for concurrent environments where multiple threads may modify a collection during iteration. They avoid the concurrent modification exception, but come at cost, but come at a cost of performance due to due to the overhead of working on a clonal collection. So understanding these differences will help you choose the right iterator depending on your use case. If you are working in a multi-threaded application, you will likely want to use fail safe iterators to ensure smooth and consistent iteration in other scenarios. Fail fast iterators offer better performance as long as you are careful with modification. So that's it for this video guys. Hope you got the clarity by now on. Fail fast and fail on what is fail fast and what is fail safe iterators. In the next video, we will dive deeper into the comparators and comparables, which are key components in sorting collections in Java. So thank you for watching this video. I will meet you in the next one.