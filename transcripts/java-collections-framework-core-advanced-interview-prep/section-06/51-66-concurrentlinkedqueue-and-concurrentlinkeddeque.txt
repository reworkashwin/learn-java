Hello everyone. Welcome back to our Java Collections Framework course. Today in this video we are going to explore two important concrete data structures such as concurrent linked queue and concurrent linked queue. These are non-blocking log free queues designed for high performance and for thread safe operations in multi threaded environments. So let's start with concurrent linked queue which is a thread safe non-blocking queue. Unlike blocking queue implementations, it doesn't block when performing operations. Instead, it uses a lock free algorithm allowing multiple threads to add and remove elements without locking the entire structure. This makes it highly scalable and efficient for scenarios where many threads access the queue simultaneously. You can imagine it as a self-checkout line. At a grocery store, multiple customers can leave the line and go to different checkout stations at the same time. There is no need for anyone to stop and wait for others. Everyone moves along freely. This is similar to how a concurrent linked queue works. Threads can add or remove items from the queue without being blocked or waiting on each other. And concurrent linked queue is unbounded, meaning it can grow dynamically. It's based on a linked node structure, and internally it uses a technique called compare and swap CAS to manage concurrent access. This allows the queue to be modified by multiple threads without needing explicit locks, reducing the contention and improving performance in high concurrency environments. Now let's explore its example through VS code. For that, let me create a new file here with the name concurrent linked queue demo dot Java. So first of all, here I'm creating a concurrent linked queue object and let me import it before creating its object. Concurrent linked queue from concurrent package. Concurrent linked queue of string queue equals to new concurrent linked queue. Okay, now it's fine. Now let's add some elements into it. Q dot offer task one q dot offer. Task two Q dot offer task three. And let me try to simulate multiple threads accessing the queue, new thread passing lambda expression in it, and just printing out a sort in this thread I'm just removing task one here, setting the message as thread one removed. And here I'm calling pull method on our queue, which is nothing, but this method retrieves and removes the head of this queue. When we call it for first time, it will remove this task one and if we call second time then our queue head will be task two. So task two will get removed. And when we call this pull method on our queue third time then on final and head element would be task three. So at that time task three will get removed. And let me just start this thread by calling Start method on it. Let me copy paste this for two times and slightly modified this thread to and this one is thread three. So in this example there are three tasks are added to the concurrent linked queue. Three different threads access these queues concurrently, removing elements one by one. Since the queue is lock free, no thread blocks the other. Each thread independently removes an item without any contention. So if I run this here, we are getting the order of these messages in a slightly different way. Thread two remote task two and then thread one, remote task one and then thread three remote task one. However, you are getting these messages in such order, but the elements got removed from this queue often. First task one will get removed by the thread one, then task two will get removed by the thread two. Then task three will get removed by the thread three. However, here this poll operation also performs two operations. One is removing and one is retrieving. Here, removing operation happens in an order like first one will we'll get to more is task one, then task two, then task three. Remove operations. Order will be the as expected, but the return operation you won't expect in which order we get these elements since these threads are running concurrently. So that's why we can see this output at one execution. If you execute this again, this time you can see thread one removed, then thread two, then thread three. If you run this again, this output may change or may not. Executions. Let me run this again. Now the execution got changed. See, you can depend on what you see here. Removal will be in the same order as they added. Whatever the element at the head, that element will get removed, but returning will delay a bit. So that's why based on the delay here threads getting printed, it will remove the elements as expected. But whatever pull method which returns the element first will get printed here first. So based on this example, you can say that concurrent linked queue is ideal for situations where many threads need to share queue without blocking one another. Here you can see no thread is blocking other thread. They are just happening concurrently. That's it. So that's the basic about concurrent linked queue. Now let's move further and explore concurrent linked queue. So concurrent linked Dick is an extension of concurrent linked queue. But it provides a double ended queue or dick. This means you can insert or remove elements from the both head and the tail of this queue, making it more flexible than concurrent linked queue and concurrent linked queue is non-blocking. Log 3D queue. Unlike concurrent linked queue, it uses a linked node structure. The main difference is that it allows for operations on both ends of the queue. You can add elements to the front or back using our first method, and our last method to add elements at the front, you use our first method and to add elements at the end, you use our last method such as just like that to remove elements from the front and back. Use pull first and pull last methods to remove elements from the front. Use pull first method and to remove elements from the back you use pull last method. Let's also explore it through an example. So here let me create a new file for our concurrent linked queue. Concurrent linked dequeue. Practice. Concurrent linked dequeue. Demo dot Java. So first of all let me import our concurrent linked dequeue. Import Java dot util dot concurrent. Link concurrent. Linked dequeue SVM. And first of all, let me create a dequeue object in it. Concurrent linked Dqz DQ object. Concurrent linked D queue of string dequeue equals to new concurrent linked d queue. And to add elements at the front we use the method offer. First let me add front task one. and to add elements. At last we use our last method write back. Task one. And just like that, let me add. Task two is also front and back DQ of our first front. Task two DQ of last back. Task two. And let me remove elements just like we did it in our previous video using our concurrent threads thread in which I'm calling Lambda expression. And in our lambda expression, let me just simply print out thread one removed from front as DQ dot pool first. And let me just start this thread. And again let me copy paste this for four times since we are having four elements in our DQ. And here let me change this to thread two. And here let me call pull last. This one is thread three. And this one is thread four on thread four. Let me call poll last. It's fine to poll first and two poll last. Here we have one extra parenthesis. Let me remove it. Okay. Now it's works fine. So in this example elements are added to the both front and back of the concurrent linked dequeue. And then multiple threads concurrently remove elements from both ends showcasing how the dequeue allows for more flexible operations compared to the regular queue. Since this poll list and poll first methods also returns and removes elements, see retrieves and removes elements. Here two operations are happening, so here remove will happen as expected. First one this poll first will get executed, which means this front task one will get removed first, then back task one will get removed second, then front task two will get removed by thread three and then back task two will get removed by thread four. However, returning may happen in any order, so that's why you can see the specific order in our output. So let me run this program. And here you can clearly see. So first of all thread two got returned as thread two removed from front. Wait a minute here. I also need to change two things here. This one. These things are back right here we are just removing from back. Now if I run this program we are getting a totally unexpected result. See thread three removing the front task one and thread four removing the back task one. So here what is happening is we have this queue elements in this order. First on head we are having this on tails head at rear end we are having this element back. Task two okay so first here we will be having two tables. Let me draw it like this for better understanding one for tracking removal order and one for tracking retrieving order. So as you can see this is the retaining order we are getting. So first of all thread three got returned. Then thread four. Then thread two. Then thread one okay 3421. This is the retrieving order we got. But removal order would be. So first thing here front task one got removed. Let me just simply specify it as FT1 okay. For better clarity front task one got removed. Then back task two got removed. Front task one got removed by thread three and back task two got removed by thread two. Let me specify it as BT two got removed by thread two. And here we can't specify which one got removed first from back task one and front task two. Since thread four is coming first, let's assume BT one got removed first. Thread four removed from back. Task one since this one got removed and thread one removed. Front task two feet two. Okay, we can assume this is the order of retrieval and removal happening in our concurrent linked DCS background. It is hard to track their order since these threads are running concurrently. So when you start multiple threads, Java virtual machine JVM does not guarantee the order in which the threads will execute. The order in which each thread access and removes elements from the queue is determined by the JVM thread scheduler, which can vary between executions. On first execution, we got this order and if you run this again, then this order will may or may not change, so you can't say in which order they will get. In our case the four threads. Thread one. Thread two. Thread three. Thread four. All are independently removing elements from the queue, and since concurrent linked dequeue is thread safe, the threads can operate in any sequence, removing elements as soon as they get the chance to do so. You may have a question why does this order of thread change on every execution? Because thread scheduling is managed by the operating system and the JVM, so different threads are giving CPU time at different points. Note this point different threads having CPU time at different points. So JVM decides which thread to run next based on the available resource and other factors like thread priority if specified. So even though you start thread one before thread three and thread two before thread four, there is no guarantee that they will execute in the exact same order every time. So if you want to control the order, expect a strict order. For example, thread one should always run before thread three or thread four should always run last. You would need to manage the threads explicitly using synchronization mechanism, such as calling join method upon them, or by introducing some form of synchronization like locks to ensure one thread completes before another thread. But here the entire advantage of using concurrent linked DK is for not having locks, right? So if you ensure locks on this concurrent linked DK, which doesn't make any sense, then why should you use concurrent linked DK? You should have used other concurrent collections which are available in our concurrent package. So here you are using this concurrent linked DK so that you won't have locks on our collection. So that's why you are using concurrent link. And if you synchronize them or ensure locks upon them then which didn't make any sense. If you are enforcing locks on concurrent linked DK. So that's it for this video. I hope you got the clarity by now why we are getting these unexpected results in output on how these concurrent linked DK and concurrent linked queue is working. So that's it for this video. Let's meet up in the next lecture. Up till then, stay happy and stay secure.