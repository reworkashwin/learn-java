Hey everyone, welcome back to our Java Collections Framework course. Today in this video we are going to look at the copy on write ArrayList. It's a really cool list that helps you work with lots of threads, especially when most of them are just reading data and not modifying it. By definition, copy on write ArrayList is like an ArrayList, a normal ArrayList, but with a twist when you make any changes to it to copy on write ArrayList, like adding, removing or updating an item, it doesn't change the list right away. Instead, it creates a whole new copy of the list with your changes and then swaps the old one out. This way, if other threads are reading from the list while you are making changes on copy of that ArrayList, they won't get interrupted, the other threads won't get interrupted or see incomplete data. Now imagine you own a busy restaurant and you got a memo that your customers look every day. Most of the time your customers just read menu, pick what they want. But every now and then you need to update the menu. You need to remove some items. You need to add new items into your menu if you want. Maybe you add a new dish or change the price of an item. So instead of taking all the menus back from customers and printing new ones and making everyone wait until you give the updated menus, what if you just made a new copy of the menu while letting customers continue using the old ones? And when the new menu is ready, you quietly swap the old ones with new ones. This way, customers keep ordering without waiting, and once the new menu is ready, they get the updated version. This is exactly how copy and paste works. It allows threads to keep reading the old list while changes are happening in the background. Now let's explore an example through Java. For better understanding for that, let me open our VS code and let me select a new folder. For our practice. In module six, I'm creating a new folder with the folder name practice. And in this let me create a new file with the name copy on write demo dot Java and public class copy on write demo SVM. Let me zoom in a bit. Now here. First of all, let us create a copy on write ArrayList collection. For that, let me do this copy on write ArrayList and it automatically got imported from Java.util.concurrent dot copy on write list from concurrent package. Okay. And here I want to add a string collection into it. For that, let me give string type here and let us create a menu equals to new copy and write ArrayList. So we have successfully created a collection and let's add some menus into it. Okay menu dot add burger menu dot add pizza and menu dot add pasta. Now let's print out our initial menu. Initial menu colon. Our menu okay. Let us suppose currently our restaurant is having these menu these items in our menu. Now customers can easily access read menu items through your menu okay so if I run this menu you can easily see this. You will be having menu as burger, pizza, pasta and then you just want to add a new menu. Here you can just add a new item to your menu. Whatever it is just like add. So here I'm just showing it to you the basic example as much as possible. So here we have added sushi and let me print our updated menu. Colon menu. So in this code, you got a copy and returning list that store the restaurant menu. Customer threads can keep reading the menu while you quietly add new items. However, we haven't created any threads here, but assume just like threads accessing them. Okay, so here threads can keep reading the menu while quietly add new items. After you add sushi, the new menu is ready and customers get the updated version without waiting for the list. So if I run this now, now sushi got added to our menu and customers can see the updated item in our menu. This is how copy and write ArrayList ensures that reads are fast and uninterrupted while the list is being modified. Let's take it a step further with a more complex example. Imagine you are building a chart application where users can join chart rooms. When a user joins a chatroom. Their actions, like sending messages, are handled by the listeners. These listeners need to keep track of what's happening in real time. Most of the time, listeners are just reading the data like checking who sent what message. Occasionally, a new listener is added when a user joins in a room. So currently we are having four users here, which means we have four listeners. One listener for each one. This way. Now, instead of locking the entire listener list and making everyone wait while adding a new listener, you can use a copy on variety list to store these listeners. This way, while one thread is adding a new listener, the rest can keep reading messages in real time without interruption. When a new person, David, got added to this chart. While David is getting added to this chatroom here, we are not supposed to stop there. The remaining person's communication. Instead, the remaining persons keep communicating as they want. If new users get added to the chatroom, then they will see the updated person if he sends the message to this chatroom. So here whole major thing is when a new person is getting added to the chatroom, we don't need to stop communication to other persons here. We don't need to update the list first. Here we can update the user list whoever got added to the chatroom later. But here main focus is on communication. Here communication won't stop the other threads accessing the content. The messages of other persons won't stop. Now let's simulate this chatroom example through a code. For that, let's open our VS code. And here let me create a new file with the name chatroom dot Java. And here in this chatroom, let me create a copy on write ArrayList which will be private in this class. Let's create that private copy and create ArrayList. Private copy on write ArrayList list. Uh nurse listeners equals to new. Copy on write ArrayList. Okay, let me zoom out a bit for better clarity. And let me close this terminal and let me minimize this one also. Okay. Now let's create a method add listener to add a new listener to our chart room simulating a new user joining. Okay, let's do it. Public void add listener string. Listener. None. Let me add this password listener to our listeners by listeners. Dot. Add. And let me pass our listener in it. Okay. By doing this, we can simply add the past listener to our actual listeners collection. And after adding let me also just print out a message with listener, added colon and the listener name. So that's the add listener method for us. Now let's create a method to notify the all listeners simulating a message being sent. Okay. Notify all listeners simulating a message being sent. Let us create a method now. Public void notify listeners. String then. Message. And for message let us loop through it for messages per string. Listener. Colon listeners. So here basically we are just iterating through each listener. And let us just print out listener plus receive received message colon plus message. Okay here we are getting error because here I haven't specified string for our listener type. Let us specify string here also. And now this error is gone okay, now let us call our public static void main method by SVM. Let's create a chart room. Object chart room. Chart room equals to new chart room and chart room dot add listener. Let us add user one and let me add user two also in it. User two. So here we have added two listeners, meaning two users into our copy and error list. And let us notify all user listeners notify listeners chart room dot by calling this method notify listeners wait a minute. Notify listeners as. Hello everyone. Now let's add a new listener while others are still receiving messages as chart room dot add listener. User three Chartroom dot notify listeners. User three has joined the chart. So here we are not doing this concurrently, but here our motive is to understand what if this type of situation occurs in concurrent enrollment. So in this example copy and variety really stores the list of listeners in a chatroom. Even though we add new listeners in the middle of a conversation, other users keep receiving messages without any delay or problems. This is perfect for situations where reads checking messages happen a lot more often than writes. However, through this example, we are not doing it concurrently. Now this is the basic implementation. Now let's implement our concurrent implementation through this basic implementation. Okay. So for concurrent implementation we need to work with threads for that. Here let us get thread name also instead of just printing listener added letters q thread, dot current thread, dot get name and let me concatenate it with the message added. Then we are just printing out our listener. Okay. And here in this notify method also instead of this received message let me change this to send to and here instead of just listener let me print our thread name red dot current thread dot get name and we have concatenated it with our message. And then we are just printing the message. So before printing message let me also give over print our listener send to then listener then plus then concatenate with colon with our actual message which is passed through this method. Now let's create a thread which simulate multiple users joining and sending messages. Okay, So we need to have this chat room object. And for adding user one, let me just remove it and update it newly. First of all, let's create a thread. Thread one equals to new thread. And in this thread let me give a lambda expression. And in this lambda let me add chat room dot add listener. User one. And let me notify users also by calling chat room dot notify listeners and passing hello from user one in it. And let me just close this thread one. And similarly let me do the same thing for thread two also. Let me copy paste it. Let me change this to thread two. Okay. And here user two. And here also from user one to user two. Now hello from user two will be passed to notify listeners. And let me add two more threads. Thread three and three for user three and user four. And let us simulate this kind of situation as we are already having user one and user two in our chat room. And user three has recently joined so that let us inform our chat with message like user three has joined. So it happens concurrently. We do not know which thread will come first. They randomly chatting in our chat application. User one and user two. They are just sending messages like hello from user one and user two also sends the message as hello from user two. And we do not know when this user three will join our chat room since it is happening in concurrent enrollment. We do not know which is going to happen first, which thread is going to executed first? Okay, and on thread four, let me just print message as user four is here. And that's it. Now let's start our threads. Let me remove these things. We don't need it to start threads, we can simply call start methods on our threads. Let me start these threads. Thread one dot start thread. 2.8.33. start. And 34. start. And let's wait for all threads to finish their job by joining all the threads. Thread one dot join. Thread two dot join on thread three dot join on thread four dot join also. And this will throw an exception. Interrupted exception. And let me handle this exception by just printing out a dot print stack trace. As simple as that. Now finally, let us just notify all the listeners that global message has been sent as global message to all users. A final message in our chatroom application. So let us break down this code once again. So first thing we are having here is concurrency with threads. So here we have created four threads. Thread one, thread two. Thread three, thread four which are created, each representing a user joining the chatroom and sending message. Each thread adds a new listener to the copy on write array list and notifies all the listeners with a message. Okay, and second thing we are having here is thread safety. Since the list is copy on write ArrayList, multiple threads can modify the list, add listeners and read from it, and send messages concurrently without any issue. The internal mechanism of copy and variety released ensures that modifications like adding listeners happen without blocking reads. And here we have join threads. So here this join method ensures that main thread waits for all the user threads to finish before sending the global message. So after each thread sending this global message, our main thread will get terminated. Now let's run this program. If I run this Java program now, you can probably see first of all thread one, added user two and thread two added user three and thread three added user four and thread zero. Added user one okay. Thread three sent to user one. User four is here and then thread one sent to user one. Hello from user two okay. Since thread one means user two, thread zero means user one okay. Don't get confused here and thread zero sent user one. Hello from user one. Like this. Something can happen and here you are three got joined into our ArrayList and here chart didn't get interrupted even while user three joining the chart room through the thread two. So here multiple threads are adding listeners and notifying them at the time, but this list is copied when modified. No threads blocks another thread from reading the list. The final global message. Here you can see all threads sent. Global message main send to user one global message to all users. So here main thread. Send this global message to every user, user one, user two, user three and user four not in any particular order anyway. It send the final global message to all users after all user threads have completed. So basically this example shows how copy and write ArrayList allow multiple threads to interact with the list without causing race conditions or requiring external synchronization, but working with copy on write ArrayList. What we need to remember is this copy on write ArrayList comes with a cost. Just like printing new copies of a restaurant menu every time you make small changes. Copy on write ArrayList creates a new list every time you modify it. This is fine when modifications are rare, but it becomes slow and wasteful if you are constantly making changes. So if you are building an app where changes to a list happens often, like adding a lot of users to a chat room. You might want to look for a different data structure, like a concurrent hash map or blocking queue. So to summarize, copy and write a list is awesome. When you have a lot of read operations and not many modifications. It's great for event listeners, caches, or even live systems where the data changes occasionally, but you want your users to keep reading without interruptions. In short, you need to use it when you read a lot and write rarely. So let's copy and write a list. A smart way to handle data in highly concurrent systems where reads are more frequent than writes. Whether you are managing a restaurant menu, a chat room, or an event listeners in a game. This list. Copy on write. ArrayList can help keep your system running smoothly without slowing down your reads. So that's it for this video. Thank you for watching and I will see you in the next one.