Hello everyone. Welcome back to our Java Collections Framework course. Today in this video we are diving into the blocking queue interface, which is a fundamental tool in Java for building multithreaded applications. We will explore how blocking queue works and look at four different implementations of it such as array blocking queue, a linked blocking queue, priority blocking queue and delay queue. So let's jump in and start our session by introducing blocking queue interface. So a blocking queue is a type of queue that is specifically designed for use in multithreaded environments. It's called a blocking queue because it blocks the operations of adding and removing elements under certain conditions. So what are these conditions for? Suppose if the queue is full, trying to add elements, trying to add an element will block until space is available. Similarly, if the queue is empty, trying to remove an element will block until an element is available in our queue. You can think of a blocking queue like a ticket booth, where there is a limited number of tickets available. Customers such as threads come to the booth to either buy tickets. Let's assume this scenario. Here, customers will come to this ticket booth either to buy tickets or to sell. So here, buying ticket operations refer to removing elements from our ticket booth and selling tickets. Operation refers to adding elements to the booth. So if the booth is full, customers can't add more tickets into it. It blocks the queue by taking more tickets into Booth. Customers can't add more tickets until someone buys one. If there are no tickets left, which means now queue is empty. In such situations, customers can't buy more tickets until more tickets are added to the queue. Simply blocking the buying operation, which means removing operation. So why should this blocking queue matters much? Because blocking queue is especially useful in procedure customer scenarios. A procedure thread adds element to the queue, while a customer thread removes elements from the queue, which we have already explored in our previous module, module four. So by using a blocking queue, the threads can coordinate automatically. If procedure works too fast, it will pass until customer catches up and vice versa, which means if customers works too fast, it will pass until the procedure catches up. So here the threads can coordinate automatically according to the add and remove operations by blocking one until other catches up. So there are some key methods in blocking queue which includes put take over, pull. So here put is used to insert an element and it will block. If the queue is full this operation gets blocked. If the queue is full and we have take to retrieve and remove an element which retrieves and removes an element blocking. If the queue is empty. If the queue is empty, then obviously this operation will get blocked. And similarly to put, we have offer which tries to add an element but returns false if the queue is full instead of getting blocked and we have pull which retrieves and removes an element, but returns null if the queue is empty. Similar to take, but where take is get blocked where it tries to remove an element, whereas in pull, if you use pull, you will get an element if the queue is empty. Now let's explore four popular implementations of blocking queue interface, which are first one. Among them is array blocking queue. So this array blocking queue is a bounded queue, which means it has fixed size and it's backed by an array, and the size of the queue must be specified when you create it. So specifying the size of the queue is a must here. Imagine doctor waiting in a room with fixed number of chairs. When room is full, no one can enter until someone leaves. Similarly, when you use an array blocking queue, once the queue reaches its capacity, no more items can be added until space is freed up. So let's explore this analogy using an example. So here let me create a new file with the name array blocking queue demo dot Java. Public static void main. And let me close this terminal. We don't need it as of now. And let me zoom it a bit. And here let's create a object of our array blocking queue. And here we need to import it. Java.util.concurrent dot array blocking queue. And let me specify its type as integer and object name as. Let me give it as q equals to new array blocking queue. And here capacity is must. If you don't give capacity then it just throws an error. So here we need to pass. Capacity here. Until then you will be getting error. So let me specify. Capacity as three here. No error can see. Now let's add some elements in our analogy. We'll consider it as producer adding elements. Since our blocking queue is based on producer and customer scenario. Right. So let's suppose let me give comment as producer adds elements q dot put one, q dot put two and q dot put three. And here we are getting this error because if you hover on it here, you can see unhandled exception type interrupted exception. Since this operation throws an exception, you can keep it in try catch block or just here just give this keyword throws interrupted exception. Now these errors are gone. See now using a thread, let us add a one more element which is new thread in which. Let us pass a lambda expression. In this lambda expression. Let us specify a try catch block since it throws an exception. Adding you put. Let's add one more integer for. If it happens, let us just put some message added code to the queue. Okay. And let me handle this exception. Interruptedexception e dot print stack trace. As simple as that. Now we can start this thread here itself by calling Start on it. Okay, so here we have created a thread and started it also. So this is a anonymous objects type. Here we have defined it like an anonymous object. Now let's define a one more thread new thread to simulate removing behavior okay here also let us pass a lambda expression and try catch block since this will throw an interrupted exception remaining also. And here let us just sleep this thread for some time. Thread dot sleep 2000 milliseconds after sleeping. Let us just print out System.out.println. Removed and let us call our remove operation queue dot. Take just returns and removes an element right? But if the queue is empty it will just get blocked. And here also let me handle our exception, which is interrupted exception a dot print stack trace. And let me start this thread also by calling start method on it. So here the procedure adds three elements to the queue. And when it tries to add fourth element it blocks because the queue is full already. Full right after a short delay, the consumer removes an element, freeing up space so the procedure can add the fourth element for on this program, after removing one element, this element four is got added to the queue. If I print our queue now a short queue. Let me run this. Okay, here I'm adding element to two times since this one got printed first. That's why we are not getting the four in our list. So instead of printing it here after removing, let me just print out our actual queue. Our final queue okay. Final cue is cue. If I run this now, you can probably see the final cue as two, two, four. What if if we don't remove it instead of removing, we just try to add one element into it. Since we are already having three elements in our cue, if we try to add one more element now, you can probably see what happens here. And let me also print out updated cue here. After adding the salt updated cue and let me run this and it taking some time. And here you can see it got delayed. It got blocked. Operation got blocked. Until you remove an element from this queue here you can see anything. It just got stopped. So here you need to remove an element concurrently. Then only this operation will get performed until one element got removed from this queue. It just get halted and just waiting. Some elements to be get removed so the thread only get executed only if the queue is having one more space to add an element. Let me just stop this operation. And here, let me unblock this and let me rerun this again. So here we are removing after removing. Since this thread also happening concurrently, our fourth element got inserted into the queue and one got removed simultaneously. This occurs simultaneously at the same time. So that is why we are getting messages added four to the queue. Removed one element and queue updated queue as 234. If you see here we are printing this updated queue after this hurry mode. So if you observe carefully here we are printing this updated queue in this thread. Right? But added four to the queue printed first here, then it just printed this and after removed and returning the remote element, our control got back to this line again. Updated. Just printing the updated queue. Now we can say that these two things happening concurrently at the same time. So here what is happening here is first of all our cursor come to this thread no matter it comes to which thread. If this thread got executed first then here it will come to this queue output. Since our queue is full, it just waits until one element get eliminated from our queue and then simultaneously new thread. This thread also get executed. Then it will wait for some time, some milliseconds. Here we have given 2000 milliseconds and after that it will just remove the thread. It will do two operations right? It removes and retrieves. It just removes an element and Immediately. It won't return the remote element. Okay, before. After removing an element, that particular element from our queue immediately this thread will get executed. In this thread. Queue output will get executed since we have removed one element. So after getting printed. After getting added. After adding this element four to the queue, immediately this normal message will get printed, added four to the queue, and after that this thing happening simultaneously. So after printing this message, the whatever element this queue dot take method removed it, just returning that element as with this message as removed colon one. And then we are getting the updated queue again from our first thread. Updated queue as the queue 234. And then we are again printing out this System.out.println message. Final queue is queue. Don't get confused by the order of output we are getting here. So here one thing for certain is only after removing an element, then only this put operation get executed. And here these things can happen randomly. And this time here you can see updated queue got first 234. And then we got the message as removed. So it happens concurrently. We don't confirm that which output is getting printed first. But it is certain that remove will happen first. So this take method having two operations. One is remove and another is returning. Here. Return can happen anywhere after adding an element or before adding an element, but remove will happen before adding an element. Hope you got the clarity by now. Now let's move to the next implementation of our blocking queue which is linked blocking queue. So unlike array blocking queue it's typically unbounded meaning it can grow indefinitely unless a maximum size is specified. So if you don't specify a maximum size, it can grow indefinitely, so it's backed by a linked list rather than an normal array. So it's ideal for situations where you don't know how many items you will be dealing with. You can think of a linked block in queue as a conveyor belt that can keep growing as more items are added. There's no fixed limit unless you specify one. Let's also explore our linked blocking queue through an example. For that, here, let me create a new file with the name linked blocking queue demo dot Java. And here let me do the necessary things SVM. And let me also import our linked blocking queue from concurrent package import Java dot util dot concurrent linked blocking queue. And here in our SVM let me create a linked blocking queue object which is of type string. Q equals to new linked blocking q producer thread. Let me create a producer thread. New thread. Let me keep it in a try. Catch block. Try. Catch. Q dot. Put. Task one. Task one. Task two and task three. And after adding these elements using our producer thread, let me just print out a message, added tasks to Q, and then let me just handle our Interruptedexception E dot print stack trace. And finally let me start our thread. So in this example, basically what we are doing is your tasks are added to the queue by one thread while another thread processed them. Since linked blocking queue is unbounded by default, the producers can keep adding task as long as there's demand. So if I run this, it will keep adding these elements. And finally we got the message added tasks to the queue. And here you can add as much elements you want. But what if if you specify a maximum size here as ten, then it will just act like an array blocking queue with capacity of ten. So here let me add ten tasks nine and ten. So this will get executed as expected. But what if if we add one more task here I'm adding 11th task also. So if I run this now, this operation will get halted until you remove one element from our queue. So to remove an element we also need to process a thread. For that, let me process a new thread to remove one new thread. Let me pass lambda expression in it and try catch block. In try catch block. Let me just print the removed element in s out removed colon plus Is q dot take. And here, after removing an element let me just print out our final q final q is plus Q. And that's it. Now let me run this program. Now you can probably see wait a minute. It's just throwing an error. Why? Okay okay. Sorry I haven't started this thread. My bad. Dot, start. Now let me run this program. Still be having an error. Let's see. What is it? Okay, so I have missed one thing here. I need to close this main methods block. Yeah, it's fine now. Let me run this. Now, you can probably see I had a task to the Q message after adding all tasks and remove the task one since it follows the order of element. So if you given 100 here, then 100 will get removed first. Let me run this again and show it to you and see. Remote task is 100. And finally Q is you can see task one or task two. Task three. Task four. Task five up to task 11. You don't see task 100 here. And that's it. As simple as that. If you don't specify capacity here, then it will just add a number of elements as much as possible without halting the adding operation. And if you want to specify a maximum capacity to the linked blocking queue also, then you can specify like this. Here you can set it to millions 10,000,100 million. If you are working on a big application in your company and according to your customer base, only when you don't know how many tasks are getting added to the queue, and one thing you ensure is that it will no more add more than 100 million tasks. So if you know that in certain case you can give the maximum capacity and even if you are not sure, it can add more tasks than that capacity, in such case, you can just leave it as empty. Don't pass capacity in it. In such case, it can add as much elements as possible without worrying about the capacity. So now let's move further and explore priority blocking Q. Priority blocking Q is a little bit different from others. It's an unbounded queue where elements are ordered according to their natural ordering or a custom comparator. It's perfect for cases where certain tasks need to be prioritized over others. We can think of it as an airport security queue, where VIPs get to skip ahead of regular passengers. Similarly, a priority blocking queue allows high priority tasks to be processed before lower priority ones. Let's also explore this through an example for that. Here I have opened our VS code. Here, let me do the necessary things. Public class priority blocking queue demo PS VM and let me import our priority blocking Q class from Java.util dot concurrent priority blocking Q. Now let's create an object for it. Priority blocking q of integer q equals to new priority blocking q. Now let me create a producer thread and add some elements to this producer thread. New thread may give a lambda expression here and in our lambda expression. Let me give a try catch block so that our q dot put method throws Interruptedexception q put. Let me add three first, and then let me add two more elements one and two. After adding elements into it, let me also print out a message such as added task to the queue. Here also we can add as much as elements we want. Let me handle this exception. A dot print stack trace. And finally, let's start our red calling dot start method on it. So now let's also create a consumer method. Because to see the priority how the elements in our collection is getting removed. So I have said here our priority blocking Q by default it will remove elements in their natural order elements natural ordering. Which means if we create our consumer thread and remove elements. Let me create it. Consumer thread lambda expression. And in our lambda expression let's call our try catch block. Since remove method I mean take method which is used to remove elements from our queue also throws Interruptedexception. That's why. So here first of all, let me create a task variable of integer and store our task in it q dot take. So it will just remove that particular high priority element and store it in a task. Okay. And then here I'm just printing it out with the message processing task with priority. In other words, we are removing we have removed that particular task. Let me handle this exception. E.printstacktrace. And then finally, let me also start this thread by calling start method on it. So if I run this program you can probably see the elements getting removed. Okay, we have just removed this for one time. So first it removed the element which having higher priority see processing task with priority one. Since we have added numbers in the order 312. However first it removed the element one which proves our point. This follows the natural ordering. Let me verify this in a clear way instead of just doing this for one time. Let me put it in while loop. While true, this loop will run for infinite if we constantly adding elements into it. However, it will just get blocked after removing these three elements. So after removing these three elements, our queue will be empty, so it will just wait. This q dot method will halt its operation until producer adds some elements into our queue. Let me verify it to you. So if I run this program now, now you can probably see processing task with priority one, then two, then three. And it got halted. So that's the basic about our priority blocking queue. Now let's move further and explore delay queue. So delay queue is another specialized queue where elements can only be taken after a certain delay has passed. It's also unbounded and stores elements that implement the delayed interface, which means the queue orders elements based on their delay expiration time. You can think of this as a package delivery system where certain packages are held for a certain amount of time before they can be delivered. A delay queue works the same way it holds on to one element until the delay has expired. Let's also explore it through an example. For this to practice, let me create a new file with the name delay queue demo dot Java. So first of all, let me do the necessary things such as PS vm and let me also import our delay queue from concurrent package. After that let me create a object for it. Delay queue. So one more thing here is I have already said here you can read it. It's an unbounded and stores elements that implement the delayed interface. So that's why let's create a separate class which implements this delayed interface. Let's do it. And then we will going to specify the class here. Let me just do it. Class delayed task which implements delayed. And this just throws an error because it needs to implement its abstract methods such as get delay and compare to methods which are available on our delayed interface. So here you can see here we have this get delay. And one more method which is compare to is available under this comparable method which is also abstract one. So we need to import these two methods to avoid this error. Let me just do this. First of all let me create two private variables one private string task name and one first start time private long start time. Then public delete task string, task name and long delay. This task name equals to task name and this start start time would be delay. We need to pass these two values while adding elements into our delete task collection. And let me just add these elements by clicking on this add unimplemented methods here. And here you can see we got these two methods this Compareto method from our comparable interface. And this get delay method from our delayed interface. So let me process this get delay first. So here I am going to create a long diff variable start time minus system dot current time millis. It would be difference. And let me just return unit dot convert. And let me pass diff. Then time unit dot m l I s e c o n d s millisecond. So here I have just converted this to a proper time. And now let me handle this compare to method. So here let me return long dot compare. And here let me pass this dot start time and let me pass or delete task class of o object of delete task. So by using this I am calling start time. And that's it. And let's format its toString method to its name to process and return its name public string to a string return task name. And now let's create a directed queue collection of type delete task delete task queue equals to new delete queue. New delete queue. And let me add some task with delays okay q dot put new delete task of. Let me pass task one and its delay time as By thousand, which means five seconds delay. And let me also put new delete task. Task two. And here I would want to give it a 10s. That's why I am passing 10,000 here. And let me also add cue dot put new delete task task three with two two seconds. And finally let me create a consumer thread with new thread. Let me pass lambda expression in it and now lambda expression in try catch block. Let me just give a while loop. Let me set it to true. Let me just remove elements based on their delay. Delay task. Task. Let me store that a remote element in this task variable task object delete task task equal to q dot. Take and let me just print out Processing plus task. And when I let me handle this exception also e.printstacktrace. And let me start this thread by calling start method on it. So that's it. So basically in this example tasks are added to the delay queue with different delay times. The queue holds onto them until the delay expires, at which point they are taken and processed. This is useful in systems where tasks need to be delayed, such as unscheduled tasks or retry mechanisms. Let me run this program now to see how it get processed. See here we got processing task three first, since task three has the delay time of two seconds only. That's why it got processed first. Then task one which has the second largest delay time. So that's why task one got printed processed second. And the task two which with the highest delay time got processed last. So that's how our delay cue works. It processes its elements based on their delay time. So to summarize array blocking cue is fixed size cue backed by an array blocking producer when cue is full or blocking consumer when cue is empty, whereas linked blocking cue is typically unbounded. Cue backed by a linked list grid for flexible and dynamic sizes, whereas prior to blocking cue orders elements by their priority, ensuring that high priority items are processed first. And finally, our delay cue holds elements until the delay expires, perfect for scheduling tasks. Each of these blocking cue implementations has its specific use case, and understanding their strength will help you build more robust and efficient multi-threaded applications. So that's it for this video. Thank you for watching and I will see you in the next one.