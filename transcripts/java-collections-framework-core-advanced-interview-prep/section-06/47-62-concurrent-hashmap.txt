Hello everyone. Welcome back to our Java Collections Framework course. Today we are exploring one of the most powerful concurrent collections provided by Java, which is the concurrent HashMap. Let's understand what makes this data structure so crucial for developing robust multithreaded applications. So we all know that the concurrent HashMap is a part of Java Collections framework, which allows for concurrent modifications by the multiple threads without the need for synchronization on each retrieval or update operations. This makes it a vital tool for developers when building high performance concurrent applications. And here, one of the main reason why should we consider concurrent HashMap is traditional hash tables are synchronized maps lock the entire data structure for every operation, which can severely hinder performance when many threads are trying to read from or write to the map, whereas the concurrent hash map. Addresses this by allowing concurrent reads and segmented logs for writes, which significantly reduces the contention and improves the scalability. So basically what is mean by this is let us understand this example through a real life example similar to the one which we have explored in our previous video. So now imagine you have a big jar of candies. Let's suppose these are candies and you and your friends, you are here and these are your friends. You and your friends want to grab candies at the same time from this jar. If one person can open the jar at a time. So here we have lock on this. If one person can open this jar at a time, everyone has to wait. So if this person is opening this jar, then all other persons has to wait until he takes the candy and release this resource, which can really slow and frustrating, right? So traditional hash tables are synchronized. Maps work like the jar here. They log the whole thing whenever someone wants to take or add a candy. So only one person can do anything at a time. Now think of concurrent hash map as a jar with multiple compartments. Each compartment can be opened separately. So in this compartment we are having candies. And these compartments can be opened separately. So while one friend is taking candies from one compartment, let's suppose this friend is taking candies from this compartment. Another friend here can take candies from a different compartment, which is not contained by any one else. Okay. This way everyone can get their candies faster and without stepping on each other's compartments. This makes the whole process much smoother and quicker, especially when lots of friends are involved. So basically, what I mean to say that is by using this concurrent hash map, we can create a collection with different compartments and we can store elements in it. So whenever we are working on multi-threaded applications, we can give access to multiple threads at a time without making them wait, so they can easily come and retrieve and modify the collection easily. So here we have mentioned hash table before, right? So why we are mentioning hash table here is because hash tables are common way to implement maps. Normal maps. We have map collection right. So it is developed using this hash table. And by using hash table we can also create dictionaries in programming. So traditional hash tables can become slow when many peoples or threads try to access them at the same time because they lock the entire structure of each operation, no matter how many threads are there, only one thread can access that collection created by hash tables. Basically, here we are talking about the map general map. We don't have different compartments like concurrent hash map to allow multiple threads to access in it. And here one thing to remember is here. If it is this normal hash table, normal maps are locking. This means we have synchronized them manually. If you didn't synchronize it, then multiple persons can access this hash map easily. But there is a cache. If you do the concurrent accessing on a regular hash map, it can lead to the data corruption and unpredictable behavior. And concurrent hash map, on the other hand, uses a segment based locking. Remember, this concurrent hash map uses a segment based locking mechanism where the map is divided into segments like this, each of which is controlled by a separate log. So here each compartment has a separate log. So when a thread wants to perform a write operation on a part of the map on a segment of the map, it only locks that relevant segment. So here you are taking candies from this segment. Then while taking and modifying it when doing operation on this segment, you just log this segment so that no one can access this segment when you are accessing it. So by allowing other threads to access the rest of the map. So while accessing this person is accessing this segment, if you also want to access this concurrent hash map and by accidentally you went to this segment, you will see this segment has already logged. Then you just move on to another segment to work on to take candies from it, or to add more candies into it. You can do whatever operation you want if you get access to any one of these segments. And when we take a look at performance consideration of concurrent hash map, the concurrent hash map greatly improves performance in multi-threaded environment. In multi-threaded environment it is good, but it's crucial to understand the trade offs. However, on Concurrenthashmap read operations are generally very fast and happens concurrently with write. Operations require lock acquisition on segments, which can become a bottleneck in highly write intensive applications, so concurrent hash map is best used in scenarios where you expect high concurrency and mostly read operations. With moderate writes, it is ideal for caching data where you frequently read and occasionally update entries. I am saying occasionally update entries, because every time we try to write operations on our concurrent hash map, it needs to acquire the lock acquisition on these segments, which can significantly reduce the performance of our collection, so it is best to use when you only need fewer rights. And here you can perform reads as much as you want, so more reads when you want more reads. So in this situation only you consider concurrent hash map. If your applications having more rights, then you shouldn't consider this concurrent hash map. To sum up, Concurrenthashmap is powerful tool for managing a map in concurrent environment efficiently. Its ability to handle concurrent data operations without locking the entire map makes it an excellent choice for high performance applications. So that's it for this video on Concurrenthashmap. I hope you got the clarity by now. Why should we use this concurrent hash map in our Java applications? So thank you for watching and I will meet you in the next one.