Hello everyone. Welcome back to our Java Collections Framework course. Today in this video we are going to dive into another Threadsafe collection which is copy on write Array set. If you have already seen our previous video which is on copy on write array list, this will feel familiar because these two collections work in similar way. But there is an important difference. We're gonna cover that in this video. So first of all, let's try to understand what exactly is this copy on write array set. So copy on write array set is a part of Java dot util dot concurrent package. We all know that. And it's designed for situations where you need a set like behavior a collection that only holds unique items here. Why I am saying set like behavior. Because set is a collection that only holds unique items. So we need in concurrent enrollment. Also, we need a collection which holds unique items. So that's why we are having copy and variety reset in our concurrent package that can be read safely by multiple threads without needing lock or synchronization, like the copy on write array list. We need uniqueness and the behavior of exactly like copy on write array list also, but in copy and write ArrayList we can have duplicates, whereas in our copy on write array list you won't see duplicate elements. So whenever this set is modified, a new copy of entire set is created, just like copy on write array list. This means that reads happen without blocking, but modifications are more expensive because of the copying, which is also just like in copy on write list. So the main thing here we can see is uniqueness. Now let me prove these points through a code. For that let me open our VS code. And this time let me create a new file and let me keep its name as copy on write set demo dot Java. And just like that, let me declare our public static void main. And let me close this terminal. And let's create a copy on write array set collection copy on write array set. It didn't get imported automatically so let me do this to import it. Import Java dot util dot concurrent dot copy on write set second one. Now copy on write array set of string on name would be. Let us improve an example of unique guests at a party. Let's think of a copy on write array set like an organizing a guest list for a party. You want to make sure everyone can see the guest list, but you also want to make sure nobody's name appears more than once. So as new people coming to the party, you add their names to the guest list. But here's the thing. If someone tries to come again, let us assume his own variant tries to enter that party again. In such case, their name won't be added twice at the same time, all your guests are threads can check the list as many times as they want without waiting for it to update. So first of all, let us implement set like behavior through our copy and varied array set. For that, let me create a object of our copy and write array set which contains strings, and let me give this object's name as guest list equals to new copy on. Write a set just like we did it on copy on write array list. And let's add some guests to the set guest list dot add Alice guest list dot add Bob and guest list dot add Charlie. And now let's try to duplicate as guest list dot add list again. And finally let us print our copy and brighter asset collection which is guest list. And let me give message as before printing it and let me give a message as current guest list. Okay, now let me run our program. Now you can probably see we are having only these three elements in our set. And if you have noticed that here we have added a list twice, but only one instance of LA is shown up in the guest list. This is how sets work. They ensure that no duplicates are allowed. Now let's think about a more complex situation. Imagine you are building a system for online user registration where multiple users are signing up at the same time. You want to make sure every user gets added to the system exactly once. There are no duplicates of this own person. Again, even if some users try to register at the same time. You can use a copy on write array set to manage this kind of situation. Since this set automatically handles duplicates, you don't have to worry about two threads accidentally adding the same user twice, and because this set is copied in the background during overwrites, other users can still view the current list of registered users without being blocked. Now let us explore a concurrent example of this kind of situation. For that, let me create a new file with the name User registration dot Java. And here let me do the necessary things. SVM. And before our main in our user registration class, let me create a private variable or private copy and write array set object. Let me create it. Write copy on write array set. Let me also import it. Import show dot util dot concurrent list. Wait a minute. Copy on. Write a set of string disturbed users equals to new copy on write array set. Now let me create a method to restore users. Okay. Let's simulate adding a user to the system. So here I'm creating a public void register user method which is having one parameter which would be string user. And here we are setting a condition if registered users in our registered users collection add user. It adds that user to our collection, which means it is a unique element. If it didn't add that element to collection means the user already present in our collection, in which case we can handle that kind of situation in our else block. So first, let us handle our logic for the true case. So if user is unique, then assert. Here I am just printing out the threads current name current threads name, thread dot current thread dot get name and let me concatenate it with the registered method registered and the user who got registered and if if user already present in our set registered user set, then if block will get terminated after checking this condition, it doesn't go and print this, it just come to the else block. So let's handle else System.out.println. Here also let us just print our current threads name current thread dot get name and let me just concatenate with the message. Write to register and press and let me give user. And here also concatenate with. But they were already registered. Okay, let me zoom out a bit. Now it's fine. Let's keep it like this. Now in our main method, let's create a object for our current class. User registration. User registration registration system equals to new user registration. And let us create some threads here to simulate users trying to register. So thread thread one equals to new thread and in thread let us pass lambda expression registration system dot register. Let us call our register user method. Register user and let me pass Alice in it. And let me remove this semicolon here and let me add semicolon at last. Now, just like this, let me create a new thread. Thread at three equals to a new thread the lambda expression registration system dot register user of Bob. And let me copy paste this for one more time. Sorry, I have named it three rates. Let me change it to two thread. Two and thread three. And third time it will be Alice. So here I'm trying to add Alice two times. See? Now let me start these threads by calling start method on our thread objects. Thread one dot start at two dot start and thread three dot start. And let us join them also. Thread one dot join 32. join and On 33. join and here we will be having interruptedexception. Let me handle it through okay. Dot print stack trace. And that's it. Now finally let us print a final list of registered users and sets out a final list of registered users. Colon registration system dot registered users. That's it. Right. So in this example, multiple threads are trying to register users. The system ensures that only one instance of Alice is registered, even though two different threads try to add the user Alice. So copy an array set automatically handles thread safety, so we don't need any extra synchronization for it. Just like with the copy on write array list, the main advantage of copy on write array set is that it reads happen quickly and concurrently without locking. However, the trade offs is that modification. Just like in copy and list adding. Removing can be slower because every modification creates a new copy of the set. If you expect a lot of reads and only few modifications, then you might choose any one between them. Copy and write ArrayList as well as copy on write array set. But if you expect lot of reads and only few modifications along with uniqueness, then copy on write array set is the best choice for you. Now let me run this program. So now here you can see first of all thread zero registered Alice and thread one registered Bob and thread two third time. Also, we are trying to add the same user who already present in our copy and write array set collection. So that's why it's throwing thread two tried to register but they were already registered. third and final list of registered users as Alice and Bob. So to sum up, copy and write array set is perfect for cases where you need fast reads, fast reads unique entries and you don't expect too many writes. It's great for managing user registrations, even listeners, or any scenario where you need to guarantee that no duplicates exist in your collection, and also you want to avoid thread contention. So in conclusion, copy on write array set combines the power of set, ensuring unique elements with the thread safety and fast reads. Whether you are managing unique users in a system or keeping track of events in multi-threaded environments, this collection is a solid choice when you expect a lot of reads, but not a lot of changes. So thanks for watching this video. I will see you in the next one. Up till then, stay happy and stay secure.