Hey there everyone! Welcome back to our Java Collections Framework course. Today in this video we are going to dive into know how task scheduling works using Priority Queue in Java. Course. Task scheduling is an important aspect of managing jobs that need to be done, and the periodic is the perfect tool to handle it efficiently. So let's break it down. First, let's understand what a priority queue actually is. So it's a queue where elements are processed based on their priority rather than the order they were added. In Java, the priority queue class is implemented using a min heap, meaning the elements with the smallest priority is served first. By default, the elements are ordered in natural order, but we can define custom orders by using comparators. This makes it a great fit for the scheduling tasks, because we can assign priorities to tasks and ensure that the most important or urgent task gets processed first. Now, imagine you have several tasks that need to be executed, each with a different priority. For example, let's say we have a list. We have a task list like this. Task A with priority three means low priority, and task B with priority one with high priority. And we have another task C with priority two which is medium priority. So here we want to make sure that highest priority task is executed first which is nothing. But task B has to be get executed first. So we are going to apply this kind of functionality in our application, which we are going to build now in this video. So overall in our application first step, we are going to allow users to input a task task name or priority both. First of all we are going to take this input from user. Then we are going to add task to the priority queue. And third step would be we have to show tasks in the order of their priority in order of their priorities. We are going to show tasks. Then finally we are going to let user continue adding tasks until they decide to stop. So these four steps we are going to build in our application. So before going to complete this video I want you to stop this video as of now and you already know how Priority Key works and how to use Priority queue as we have already explored in our previous module. So first of all, go through with that video once again and then try to implement these four steps using Priority Queue in your way. If you don't get it, then it's okay. But first of all, let's try by yourself. Then come to watch this video and compare your solution with the solution which I have developed here. Here I'm not going to build a very complex application here. I'm going to keep it as as simple as possible. So don't worry for everyone. It will be easy to understand. So for that, first of all, let me open our VSCode. And here let me create a file with the name Task Scheduling using Priority queue dot Java. So first of all here I want to create a class which implements a comparable class. So here I'm going to create a class task. Class task not public one which implement comparable. So here we are getting error because we need to override a Compareto method which is residing under this comparable interface. So if you open it here we are having a single method which is compared to. We need to override it to get rid of this error. So here I'm clicking on this bulb icon and adding unimplemented methods. And now we have this method. Let me remove it as of now. First of all here let me implement this class. In this class I'm going to have two fields one for name and one for priority. And these are going to be private private string name private int priority. Let me construct a constructor public task string name int priority. This dot name equals to name. And this dot priority equals to priority. That's it. Now to get name and priority let's also set getters for these two properties to get name, I'm going to create a method which returns a string with the name get name. And here I'm just going to return our name. Likewise let me also build our get priority method which will return int get priority. Return priority as simple as that. And in this Compareto method we are going to pass our task object. Let me change this object type to task and let me keep it as oh, and here I'm gonna what I'm gonna do is return integer dot compare. I'm calling this compare method which is residing under this integer class. And here I need to pass this start priority and other task which is getting passed into this Compareto method the task priority. And you already know what we are going to get in this method. Okay, to get rid of this error. This method compares two tasks. Of type task must override or implement a Supertype method. So to get rid of that error, we need to add our comparable type as task. You need to set it like this. And now here you can see we got rid of the terror. And let's back to our code. Let me also override our tasks our task class toString method. Public string toString. And let me format it as task colon. Let me concatenate it with our name followed by and let me give pipe symbol here. Then priority as priority. That's it. So now in our main public class let me call our PS VM. Public static void main. And here I'm going to create a priority queue collection of type task priority priority queue of task. Let me import it. Import Java dot util dot priority queue. Task queue equals to new priority queue. And let me also create a scanner object. Scanner is equal to new scanner system dot in. And let me also import over scanner class Java.util.scanner. And here I'm going to create a lock variable and naming it as keep going. By default I'm setting it as true. And here I'm starting my while loop. If keep going then assert. Here I'm going to ask user to enter the task which you want to add. Enter task name. Then user is going to enter the task and I'm going to store the task in this task name variable SC dot next line and then let me print out a message. Enter task priority and let me mention as lower number equals to higher priority. Hope you got it. Now what I'm gonna hinting user to enter input here int task priority Whatever user enters priority, it is going to store in this and it will be integer which will be get stored into this task priority. Then let me start a next line. Clear the newline character since we are taking integer input after taking string input. If you don't do this the next time when user entering a task name again, then he will gonna face some issues while storing input in this task name. Here let me simply add task queue dot add. Let me say task. After taking input from user, then I'm going to add our task, task name and task priority which will gonna get stored into our priority queue task queue collection. And after adding a task on priority to our queue here, I'm gonna ask user do you want to add another task is or no. And here I'm gonna take input from user, which will be string response equals to. I'm going to store the response in this response variable. Let's say dot next line. And here if response dot equals and let me set it as ignorecase however he enters either it will be in uppercase or lowercase. If it is yes or no we're gonna process the response based on it. Let me set it to no. If response is no then we gonna set keep going equal to false, since we no need to run this loop again and again. And if it is yes, then this loop gonna run for infinite time until this keep going becomes false. And then here, let me print out a message with the extra newline. Scheduled task in order of priority. While not task queue dot is empty. If our task queue is not empty, then only we are gonna miss It's out and process our Q element based on their priority. Here I'm calling poll method. So here basically we are retrieving and removing that particular element from our queue, meaning that the element with the least priority will get processed first. And finally it is best practice to close our scanner C dot close. And that's it. Now let me run our program. And here you can see it is asking for us to enter task name. Let me add a task name as task A. And now it is asking for us to enter priority lower number equal to. Higher priority means lower number having higher priority. So let me give its priority as three. Now it is asking for do you want to add another task or not. So let me give s here and it is asking for us to enter the task name which you want to add. Let me give it as task B and let me give its priority as one. And then it is asking again, let me give s then let me again add one more task. Task C and let me set its priority as two. Now it's again asking for us to do we want to add another task. Let me set it as no. And that's it. Scheduled task in order. Okay. I have printed this two times. It's okay. In order of priority. Now here you can see task B has the highest priority. That's why it got printed first. Then we are having task two with the second highest priority. And then we are having task A with the lowest priority. So that's how we can implement task scheduling using priority queue. You can create more complex versions of it. So go ahead and try it out. That will bring good practice for your hands. And that's how you can implement task scheduling using priority queue in Java. It's a simple and effective way to ensure tasks are handled based on their priority levels. So that's it for this video guys. Hope you got the clarity by now. Thank you for watching and I will meet you in the next one.