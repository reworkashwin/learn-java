Hey there everyone! Welcome back to our Java Collections Framework course. Today we are diving into a super practical use case managing unique user sessions. So have you ever wondered how websites track you when you are logged in? Or how they make sure you aren't logged in twice on the same account? Well, today we are going to explore how we can handle that kind of implementation using sets in Java. So in the digital world, managing users is a critical task, whether it's a social media site or an e-commerce platform or an e-learning portal, one thing is for sure each user needs their own session and each session needs to be unique. You don't want to be accidentally have two active sessions for the same user, right? That's where sets come in handy. So what makes sets to go to choice for maintaining unique sessions? Let's start with a problem. Say you are building an app that tracks user sessions. Imagine users can log in from multiple devices. We need to make sure we aren't creating duplicate sessions for the same user. This is exactly where set implementations, because sets in Java doesn't allow duplicates. That's the main reason. Which means if you try to add a duplicate session, it won't happen. Whether it's a hash set link, the hash set, or a tree set. Whatever implementation it is, they all enforce uniqueness, making them perfect for one use case, which is uniqueness. So let's jump into code and explore our example by creating a hash set to store our sessions. A hash set. Here I'm going to use hash set because because it's fast. And for most session management cases speed is crucial. So that's why here I'm going to use a hash set. So first of all let me import two things here which are java.util dot asset and set java.util.set. So here I have imported these two things. Then here let me create a class. User session manager. User session manager. In this, let me create an active sessions object of set private set of string active session active sessions equal to new hash set. So here I have successfully created a Active Sessions object to store different sessions coming from different users. Okay. And to add sessions into this active sessions let me create a public method. Since we can't access this active sessions object directly on our application. So that's why we need a medium. That's the reason here I'm creating a public method. Public boolean method to add sessions into it. Add session string session id. And here we need to return a boolean. So let me just add that particular session ID whatever. Pass it to this method here to our active sessions by simply calling active sessions dot add and passing session ID into it. That's it. It will return false if session already exists in our active sessions ID. But this session ID is unique, then it probably returns true since this session ID will get added to our active sessions. And when user log outs, we need to remove session ID from active sessions, right? So for that, let me create another folder to remove that particular session ID whenever whoever logs out will simply calling this method. Public boolean remove session of string session id. And here also we need to return a boolean in which I'm calling active sessions dot remove and passing our session ID into it. So here this method will remove session when user logs out. And here let me create a one more method to check if the session is active or not. Let me create another method. Public boolean is session active string session id. And 91 here. Also, we need to return a boolean active session start contains so in active sessions, if session ID contains, which means our session is still active. So here's the magic by using this Hashset. Every time we try to add a session, it will automatically ensure that there are no duplicates. If the user tries to login again from the same device, the set will return false, meaning the session already exists. So now let's test this out with some logins and logouts calling it in our main method PS VM. And to access those public methods in our what is it User Sessions Manager class. Let me create a object of it user session manager. Session manager equals to new user session manager assert user logs in with session id a b, C123. Now let's try to add a session ID to our active sessions by calling this add session method through this Session Manager object. Let's try to do that Session manager dot add session and let me pass our ID as ABC 123. Okay it's throwing error. Invalid character constant. Let me keep it in double quotes. Okay now it's fine. Now let me do this again. User logs in with session ID logs in again with session ID ABC 123. When we do this, we'll probably gonna get result as false here. Okay. And after it let's check if ABC 123 session is active or not. Let me search this message as is ABC 123. Session still. Session active. And let me call our each session active method through our session manager object is session active. And let me pass our ABC 123. So if you still present in our active session, it will return true. Now let's log out the user. Assert user logs out with session id ABC123. Got it. And let me take us to log out. We need to call our session or session manager. Object through session over and through session manager object. Here we need to call remove session method. And in which we need to pass our session ID which we want to remove. So by doing this you can simulate the behavior of a log out from session IDs and assert. Now let's again check if after logging user out after removing our session ID from the active sessions. Let's check out our session is still active or not is a, BC123. Session still active. To check it session manager dot is session active. Let me pass a, b, c, one, two, three in it and that's it. Now let me run this program. So here you can see first user logs in successfully. First time when you call this session manager dot add session. And when we do this again when we try to add the session again. Here you can see the messages user logs in again with session id ABC 123 colon false. So by implementing this kind of implementation, we can ensure that not a single account will be active at two positions. And when we check is ABC 123 session is active or not. We got true. And after some time we have logged out that particular user ABC 123 from our session active sessions. See user logs out with session ID. When we remove this session manager dot remove session, we got status as true and when we check again is ABC 123 session still active? After removing that particular session from active sessions, we got the result as follows. So that's how we can implement and maintain unique user sessions with sets. Now let's talk about edge cases. Session expiry and management. In real applications you would want sessions to expire after a certain period and maybe even use sets to manage this. For instance, you could pair a scheduled executor service with set to remove sessions after a timeout. But that's a more advanced topic for another time. So the key takeaway here is that sets help you enforce unique sessions effortlessly. And that's how you can maintain unique user sessions using set implementations in Java. Whether you are tracking logins for a small app or managing thousands of users across multiple devices, using sets ensures you never have duplicate sessions and that each session is uniquely managed. So that's it for this video. Thank you for watching and I will meet you in the next one.