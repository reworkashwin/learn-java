Hey there everyone! Welcome back to our Java Collections Framework course. Today in this video we are going to explore common pitfalls and best practices which you should consider while exploring collections in Java. So as per this video, we are going to focus on mostly three topics. One of them which is avoiding concurrent modification exception. One of the most common exceptions encountered when working with collections in Java is the concurrent modification exception. This typically happens when you try to modify a collection while iterating through it. For instance, removing or adding elements during iteration using a standard loop can trigger this exception. To avoid this, always use iterators. Remove method when you need to delete elements during iteration. Alternatively, you can use the copy on write ArrayList or concurrent HashMap. These classes which will allow safe concurrent modifications in multi-threaded environment. But if you don't want to use this. Copy and rewrite ArrayList or concrete HashMap. In such case, you should consider removing elements through iterator method. Remove method. Let's explore an example here. Let me create a list of string list equal to new ArrayList in which. Let me pass array start as list a comma b comma c. Let me also import our list from util package to avoid this error. So after doing this, let me iterate over our list in for loop. Here. Let me create a iterator of string. It equals to. Let me call this list dot iterator method. And let me also import our iterator over here. On here we are gonna iterate over this loop. If we have the next element it dot has next. And that's it. These two conditions only we are having since we don't need to increment in our iterator. So that's why here I am continuing our loop. And here I'm going to create a item variable to store each item item equal to Itnext. So when we do this, it automatically moves to the next element on each iteration. On first iteration, we will be getting this a into this item. On second iteration we will be getting B into this item. Likewise C and then further elements. Currently we don't have anything beyond C, so we can assume that this loop will iterate for three times. And to safely remove elements from our list, we can use this technique. If you want to remove B, then we can set condition like this. Whenever we reach B and it is equals item. In such case, we are going to remove that element from our iterator, which will probably going to reflect upon our actual list. So after removing it, let me just simply print our list out. Let me run this program. So here you can probably see we have successfully removed Ailment without facing the exception. Concrete modification. Exception. What if. If you don't do like this, instead of doing like that, if you randomly remove a element from your list like list dot remove and you have passed an object which you want to remove, if you do like this, you'll probably gonna face concurrent modification exception. And second thing you should consider while working with collections is properly implementing hashCode and equals methods. Because implementing hashCode and equals methods correctly is crucial for ensuring proper behavior when using objects in collections like HashMap or Hashset. When two objects are considered equal according to their equals method. When you use objects in collections like HashMap, Hashset, or hash tables, these collections rely solely on the hashCode and equals method to determine the uniqueness of objects and to manage them efficiently. Here equals method. This method checks if two objects are considered equal or not, whereas hashCode. This method returns an integer representing of the object, which is used by hash based collection to quickly locate bucket where the object might be stored. For example. Here let me create a employee class employee in which. Let me create a two private properties. Private string name and its name. And another would be their id private int id. And here let me create our equals method. So here this equals method is overridden to provide a way to compare two employees. Obviously objects based on their ID and the name fields. If this equal to equal to zero means. Here it is checking if both references point to the same object or not. And here if code equal to equal to null or getclass not equal to oh dot getclass. It ensures that the object is not null and is of the same class And here it is equal to equal to employee.id. I'm pressing tab insert objects. Dot equals name. Employee dot name. Name is current objects name. And this employee dot name would be the employee which has been. This object passed to this equals method so that objects dot employee name. If they're both IDs are equal, then we will probably gonna return true, meaning that the both objects are equal. So you can implement our equals method like this. Whereas overriding hashCode. Let me override this one also public int hashCode. So here we need to generate a hashCode based on their name and id which would be unique to each and every object. Let me do that. Return on objects. We have a method called as. To do such implementation in which we can pass our name and ID, which are nothing but our class properties to create that to generate a unique hash code. So here object dot hash name comma id which is supposed to generate a hash code using name and id fields. It ensures that if two employees objects are equal as per the equals method, they will have the same hash code. Then only they are said to be properly equal. To prove this to you, let me show you an example here. First of all, let me create a set of employees employees equal to new as set here. We need to import our set. Let me do that set. It would work now okay. Now here let me create two employees to employee object. Employee employee one equals to new employee of in which let me pass J-o-h-n John Doe and let me pass his ID as 123. So here I'm getting this error because we need to create a constructor for this. Let me create a constructor on our employee class by clicking on it. We got this employee constructor in which we need to do two things, which is this dot name equals to string, and this.id equals to I. As simple as that. Whatever password here we need to set it to our current object. Now we got rid of that error. Now let me copy and paste this and let me change this to employee two. And second time also here I am creating the same object with the same name and ID. Now in employees let me add one employee. Employee start. Add employee one okay. And in short let me do this. Employee start contains. Let me check it with employee two. Since they are having they both MP one and MP two are having John Doe and 123. Right. So before doing this let me just simply print out our employee set. is certainly not a scholar. Let me print out our employees. Okay. Here. We need to handle this. Let me comment out this part and let me run this again. And since we are storing it like an object, we are getting some random text. So to format it, we also need to override our toString method in it. It will be public void toString or public string toString. Return error one or return employee name name plus colon. Let me set id colon before name. Let me also concatenate it with the name colon, then id colon or id as simple as that. Now let me run this program again and we will be having John Doe name as John Doe and ID with one, two, three in between. Let me also put comma there for better clarity. See now we are having this in our employee. Now can it be equal to this employee two That's the question of it. Now let's run this. If it is SQL employee two and employee one is equal, then we should get result as true. Let me run this and we got true. And it also has to return true for this hashCode also. First of all let me prove this to you. So let me call employee one dot hashcode. When I do this I will be getting some hashCode. This 562773047. And when I do this again MP two dot hashCode. Even when I do this, if I get the same hashcode as MP one dot hashcode, then we can say that these two objects are both are equal. Let me run this. See we are getting the same result. Now we can say that these two MP one and MP two are both are equal entities. So that is how you can implement a hashCode and equals method into your code. Now let's move to our third thing which we should consider while working with collections, which is nothing but dealing with null values in collections, which is the most important thing because collections in Java such as HashMap. ArrayList. These can store null values, but this can often lead to subtle bugs if not handled carefully. For instance, when using a HashMap, calling contains key of null is valid, but you need to mindful that only one null key is allowed. In general, avoid storing null values in collections unless it's explicitly necessary. Instead, consider using optional or default values to handle the absence of data more safely. Let's explore how can we handle null values? First of all, let me create a map of string comma string. Map equals to new HashMap. So here let me try to put a null value using map dot. Put null if null place. Let me add some value as value so it is possible in map to have one. Okay, let me print out this map. And if I run this, you can probably see null as key and it's value as value. To avoid confusion, let me comment this out. Okay. Now let's continue our null handling. So here is it possible to put null value at value place instead of at key. Can we do that. Let's explore it. Map dot put. Let me set it as key one and let me give its value as null. So is it possible. Let's run and test it out. So before going to do that let me also print it out. So map let me run this. And here you have it. We can have a null value which verifies our point. And my question here is we can't have two null keys. Let me repeat this thing here again. And here let me add value to. And when I do this this value will be replaced with value two. Let me prove this to you. Now here you can see this null value is replaced with value two, which verifies our point that we can't have two null keys, but we can have two null values. Is that possible? Let's also verify it. Let me copy paste this thing. Let me change this Q1 to Q2 and let me run this again. So if I run this again now here you can clearly see we are having three elements now Q1 equal to null and key two also equal to null. Which proves our point. We can have multiple null values, not keys in a map. And in collections we have a method known as Containskey. By using this method, we can check if a work map having null keys or not. Let me show this to you. If I do this, if map dot contains key and if I pass null here, it will check for null keys. If it found, it will just return true. And here we can. Let me set this message as map contains a null key. let me run this program. Now, here you can clearly see since we are having null key in our map, we got this message printed out map containing null key. Similarly, like this, we can also check for null values in a map using the contents value method. If map dot contains value, let me first null here. If I do this. If it returns true means we are having null value in our map. In such case, let me print out map contains a null value. Got it. Let's prove this. If I run this now, here you can clearly see map contains a null value. Since we are having a null value in our map. As I said that having null values is not a good thing in some cases. That's why we can use optional to handle null values. How we can do that is we have of nullable value method in optional, which wraps the value in a optional object which can handle null values safely. Let's also explore it using optional to handle null values safely. First of all, let me create a value equals to map dot get key one. Sorry equals two here. So when I do this on key one we are having a null value. We are storing that null value in this value. And here let me create a optional object. Optional of string. Optional value equals to optional dot. Here we are getting error. Let me fix this. We have to import optional. Let me import this one. Import optional. Okay. Now we got rid of that error. Optional dot. We are having this off nullable method. So here I can pass our value which is of null. And then to check these optional values we are having a method in optional which is if present or else method. Let's use it on our optional value. Object. Optional value. Dot if present or else. Method. Let me use it if present or else. Since here we need to pass an action. Let me use Lambda. Let me give value for v. Lambda expression. System.out.println. Value for key one colon. And let me print out that value. V and then let me also print out System.out.println. No value found for e one. For better clarity let me enter here. Let me also bring this part down to a new line for a better view. Now it's good. Okay so this if present or else method executes a lambda expression. If the value is present, otherwise executes an alternative action which is no value found for key one. Let me run this and show this to you. What we are getting here. So here you can see instead of having printing the value for key one, we are printing no value found for key one. If you do this normally, you will probably seeing the value as null if you do it directly using a map that contains key or contains value method, you are getting null value instead. If you do this, if you use if present or else method, you safely handling the null values in only cases when you don't want to consider null values as a actual value. In such case, you should implement this type of implementation as we have done here. Let's suppose at key one, let me add some value instead of null value as hello there or v one. And now let me run this. Now instead of printing this message no value found for key one. Now we got value for key one as v one. Got it. How to handle null values in collections. So by doing this we can best handle all values in collections. So best practices for this is we should avoid storing null values in first place. Whenever possible, avoid storing null values in collections. Use optional or default values to handle absence of data like this, and you can also perform null checks. If you are concerned about not having null values. You can always perform null checks when dealing with collections that may contain null values to prevent null pointer exception. By following these practices, you can handle null values in collections more safely and avoid common pitfalls. So that's all about this video guys. Hope you got the clarity by now. What are the common pitfalls and best practices you should consider while dealing with collections? So these are the major ones. There are lot more things which you should consider while dealing with collections. So thank you for watching this video and I will be seeing you in the next one.