Hello everyone. Welcome back to our Java Collections Framework course. Today we will dive into a very practical and powerful concept in software development, which is caching using maps. So caching can significantly improve performance by reducing the time taken to access data that frequently used. Let's see how can we implement this using map? So first of all, let's start with what exactly is caching by definition? Simply put, caching is the process of storing a computed result so that next time you need it, you don't have to compute it again. Instead, you retrieve it from the cache, which is faster. So overall you can say that caching equals to storing computed results for quick access. Now where do maps fit into this? A map in Java stores key value pairs, right? The key is often the request or input to a method, and value is the result you want to cache. Whenever you need a need result, you first cache the map to see if the result is already available. If it is available, we return the cached value. If not, we calculate the result and store it in map and return the fresh value. So here. What I mean to say is, if we have already calculated a calculation for particular value, we will be storing that values result in our map. Whenever user want to perform the same operation on that same value, then we don't need to perform that whole action. Instead, we will grab that result from the result which already available on our map. So let's visualize key value pairs in a map with example inputs and outputs. Now here let's implement a basic caching mechanism using a hash map. Let's suppose we have a function that performs an expensive operations, like finding Fibonacci numbers. For that, let me change this class name to Fibonacci Cache, which will best fit into it Fibonacci cache okay. And here let me import two things. One is HashMap and another is map from our little package Java.util dot map and Java.util dot HashMap. Now let's try to create a private map collection in our Fibonacci cache to store the calculated results. Private map key would be integer and value also would be integer. Since to find a Fibonacci number, we are supposed to give a number, right? And let me give this collections name as cache. Cache equal to new HashMap. Okay, here this is giving error illegal modifier for parameter cache only final is permitted. Okay, here I am defining it in our main method. That's why we are getting error. Let me just remove it and let me define it in our class. Okay. Now error gone. So here I want to create a method which would do our Fibonacci operation. Public int Abenaki of Indian. We are supposed to pass an init, and if n value is less than or equal to one, then it probably gonna return the n. If you pass zero into our application, then it gonna probably return zero as a result. Or if you pass one in our Fibonacci method, then it probably return one. And if cache dot contains key contains n, then we don't need to calculate that Fibonacci result since we are already having the result in our map, right? So in such case, we don't need to perform the whole Fibonacci operation and we can simply return already available result such as System.out.println. Fetching from cache per. Let me give n here and let me just simply return our cache dot get of n result. And then if we can't find the result in our map, then we need to perform the action. So for that to perform the key operation, let's do this. Enter result equals to key of. Here I'm doing the recursion with key of n minus one plus Fibonacci of n minus two. And then after performing the operation, let me put this result in into our map such as n and the value as the result. If we do this, and if we want to get the result again, then we don't need to perform the whole operation. We can simply take the result from our map. So that's the idea. Let me return our result. If we are not having our Fibonacci result in our map, which is nothing but cash, then we'll probably perform the whole operation and return the result. And if we found the Fibonacci result in our map, then we'll probably return the result results from our cache map using this cache target method. So here let me create a Fibonacci object Fibonacci cash fib equals to new Fibonacci cache. And first of all, let me access the result of Fibonacci of ten. Now key of ten by calling fib, by calling fib dot Fibonacci method and passing ten into it. So first of all, we don't have any cache. Our current cache is empty. So when we do this operation, we are putting this Fibonacci of ten result into our cache map. Then we are just simply returning it. So when we call this for first time, we are actually performing the whole operation. We are not taking it from cache. Okay, now when I call this Fibonacci of ten, it will get passed to this Fibonacci function and it will check in less than or equal to one, which is false, right? Ten not less than or equal to one, which is false. That's why it will check this condition again. Cache dot contains n, which means it's checking for key as a ten in our cache map since our cache is empty. So this if block also get terminated since this condition also false. Now our cursor will come to this. It will just simply perform this recursion operation. Fibonacci of n minus one will also check for Fibonacci of n minus two. So here fib Nokia of n minus one, which will be nine will be passed to this function nine also returns false. And here also we don't have any nine in it, so it will get also false. Then again this operation happens for nine and also get passed to this. Let me run this for better clarity. How we are getting the result here. You can see we got this as result. So like this. It will happen for up to our n value will get reduced it to three minus one. So when three minus one will get passed to this, which would be two. This again will get false. And here also we will get false for first time. And we are going to store our two Fibonacci result in our map. And we are going to do this Fibonacci of n minus one again which will be two minus one and one will get. Pass it to this and this. Now we will get true when we pass one and it simply returns one. And then when while backtracking you know about recursion backtracking right. So when backtracking we are storing two result into our cache c cache that put n comma result. And again when this function tries to access our two here we are doing this n minus one plus Nokia of n minus two. And this function backtracking tries to access for Fibonacci of two. Then will probably returning the result of two which is already got stored in our map. So that's why we are getting this result. Fetching from cache for two. And then it just simply put the result like that for three. Also we are storing it up to eight we have stored. So overall what I meant to say is one time this function will process this whole result, this operation and store it in our cache. When this function tries to access the result for the same element, such as n of two, n of three n of four, it will get the result from cache instead of doing this operation again and again. But at this time. Finally we got the Fibonacci of ten from this return, since we haven't accessed ten again, that's why we aren't get that ten Fibonacci result from cache. If you pass Fibonacci of ten again, then you will probably gonna get that Fibonacci result from cache. Let me prove this to you. Let's say we aren't having here output as fetching from cache for ten. Instead we are getting Nokia of ten. Colon 55. Now let me pass with Nokia of ten. Again, let me just simply copy paste this line. Okay. Here I have simply copy pasted this line. Now let me run this again. Now you can clearly see this ten we got from cache instead of doing this operation. So one time you need to do the operation. And then no matter after performing one time operation you can take the result from cache a number of times. You don't need to perform the whole operation again and again. So that's the whole idea of it. It will just simply reduce the number of operations we do, and it will probably gonna save you time and save memory in our programming applications. So here, without caching, our Fibonacci function would take exponential time due to the repeated calculations. But with caching we only compute each Fibonacci number once, reducing the time complexity to O of N. This is a huge improvement in Fibonacci. Without caching. Will probably get the time complexity of O of two power n. However, in some scenarios we need to limit the size of cache to avoid using too much memory. One popular solution is to use an LRU, which is nothing but the least recently used cache. Java's linked HashMap is perfect for this, because it can remove the oldest entry when the cache gets too large. So to implement this LRU cache, let's create a new file with the name LRU cache dot Java here. First of all, we need to define capacity how many elements our cache going to store. We define it in private private final int capacity. And here let me set its constructor. Public LRU cache of int capacity. This is a constructor for LRU cache class. It takes an integer capacity as a parameter. And here we are doing this super of in which I am going to pass capacity and then 0.7575. F one comma two. So here what I am doing is this line calls constructor of the linked hash map class with three parameters. To call it, I need to extend our linked hash map. Also here extends linked hash map. Let me import it before extending it. Import Java dot util dot linked hash map. Now it's fine. And here you can see the hints load factor as 0.75 and access order as true. So what exactly is this? What is 0.75 F here? Capacity would be initial capacity of linked hash map and 0.75 F is the load factor, which measures of how full the hash table is allowed to get before its capacity is automatically increased. Automatically increased. Here true. Specifies that the order of elements should be based on axis order. Which makes last. Recently accessed items are removed first, so by extending Linkedhashmap with the access order, the LRU cache class leverages the built in functionality of Linkedhashmap to maintain the order of elements based on their access. This is useful for implementing our last recently used cache, with the least recently accessed items are removed first when the cache reaches its capacity. Once our cache reaches its capacity, then only we are removing the least recently used accessed items here. I'm not saying recently accessed items here, I'm saying the least recently accessed items, which means the items which we haven't interacted much, those are going to get removed first. And then here I'm going to set our capacity with the password capacity into this constructor. By doing this, start capacity equals to capacity which is passed into this constructor. And here we need to override a method remove list entry to remove list recently accessed elements. Let me override it and let override protected boolean. Remove list entry. Map dot entry of integer. Let me also import our map. Integer. Integer. List. Okay, we are still getting error because we need to also define type here as Linkedhashmap of integer. Integer. Now it's gone and here we need to return a boolean return size greater than capacity. And I'm calling our SVM. Public static void main. Now we are going to create a LRU cache object and later cache. Cache equals to new LRU cache of capacity. Let us Q3 here and let me put three key value pairs. Hash dot. Put one comma one and let me again cache that. Put two comma two and cache dot put three comma three. And let me just simply print out our cache as cache colon. Our cache. Now if I run this will probably having the result as one equal to one to equal to two and three equal to three. Got it. Okay. Here I have given c h s h e c s h. Okay. Now here let me add one more element. Since we have reached our capacity now least recently used element will get removed. Let me pass four comma four here. After doing this operation once again let me just print out our cache c s colon plus cache. Let me run this again. And here you have it C one equal to one got removed. And our updated cache has two equal to two three equal to three and four equal to four. So this is how our ideology works. In this code, we have created a LRU cache with a maximum capacity of three. When we add the fourth entry, the least recently used entry is automatically removed to maintain the size limit. So caching is commonly used in many real world applications, whether it's storing web page data to avoid fetching the same information repeatedly, or optimizing database queries by caching the results. Understanding how to use maps for caching is a powerful skill for any developer. So that's the basic here we have explored. If you want to explore. There is a lot more on this topic if you go to depth. So that's it for today's session on caching mechanisms using maps. By leveraging caching in your applications, you can dramatically improve performance, especially when dealing with expensive computations or slow data sources. So that's it for this video. Thanks for watching and I will see you in the next one.