Hey there everyone! Welcome back to our Java Collections Framework course today. Basically in this video we are going to solve our question how does linked has set maintenance order. Now? How does it differ from actual hash set. So by the end of this video you will know exactly how these two sets are different and when to use each one. So let's start with basics. Both linked Hashset and hash set are implementations of set interface, which means they do not allow duplicate elements. However, there is a key difference between these two that impacts how they maintain elements internally. First off, let's explore linked has set. Linked has set maintains the order in which elements are inserted. It maintains the same order it does by using doubly linked list along with a hash table. This means when you iterate over a linked hash set, you will get the elements back in the same order they were inserted, so this makes linked hash it useful. When you need to preserve the sequence of elements in which you have added, like if you are processing a collection where order matters. For example, keeping track of unique names in the order they appear in the order they are inserted in such cases. Linked hash it is useful. On the other hand, Hashset does not guarantee any specific order. It uses simple hash table internally to store items, and the order of iteration depends on entirely on the hash codes and how they are distributed across the hash table. This is why if you iterate over a hash set, the order can seem almost random. This means Hashset is generally more efficient than linked hashset, as it does not have the overhead of maintaining insertion order. So if you don't care about the order and you are focused purely on performance, hashset is the way to go. So if you want to summarize the difference between these two, we have three steps to go through which we can compare both linked Hashset and hash set what exactly and how they differ from. So first difference is order of maintenance. We all know linked has set maintenance insertion order, while Hashset does not guarantee any specific order. And second thing we should consider is performance. Obviously hashset is performance since it does not take overhead of keeping elements in an insertion order or any specific order. That is why Hashset is slightly better compared to linked hash set, because due to its insertion order, linked has it slightly slower than has said due to added overhead of maintaining order. And third main difference we should consider is memory usage. LinkedIn has said it uses more memory because it has to store a doubly linked list along with hash table, whereas hash it only needs the hash table. So overall, if you consider order maintenance, then linked hash set is the winner. And when you consider performance, then hash set is the winner. And when you consider memory usage then also hash set is the winner. So on these two cases hash set is the best choice. Whereas if you want order maintenance in your collection and you want uniqueness among your elements, then you might consider this linked hash set, because in such case you won't see any order on hash set, you can only implement it through this linked hash set. And there you have it. If you need a set where insertion order matters, use linked hashset. If order doesn't matter and you want maximum performance, then you should go for hash set. So I hope this cleared up the differences between linked Hashset and hash it for you. So that's it for this video. Hope you got the clarity by now. Thank you for watching and I will be seeing you in the next one.