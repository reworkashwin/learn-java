Hey there everyone! Welcome back to our Java Collections Framework course. Today we are diving into an interesting and often misunderstood part of Java Collections framework, which is the weak hash map. By the end of this video, you will not only understand what a weak hash map is, but also how weak references affect garbage collection in Java. So let's get started with the question what exactly is a weak hash map is? Well, it's a type of map in Java that works almost like a regular hash map. You can store key value pairs in it just like any other map. But here's the twist. In a weak hash map, the keys are stored as weak references. Now you might be wondering what are weak references. To understand that, let's first talk about how regular objects work with garbage collection. In Java, objects are typically referenced by strong references, which means that as long as your program holds a reference to an object, it will not be eligible for garbage collection. As long as your program holds a reference to an object, it will not be eligible for garbage collection, but weak references are different. If an object only being referenced by weak reference, the garbage collector is free to collect it when it runs out of memory, or it also be collected at the object's discretion. So in a weak hash map, the keys are weakly referenced by a weak reference. That means if a key has no other strong reference elsewhere in your application, it becomes eligible for garbage collection. Once the garbage collector reclaims that key, the entire entry, including a value including its value, is automatically removed from the map. Now, you might ask, why would I want my keys to disappear like that? That's a great question. Here is an example scenario. Imagine you have a cache where objects are stored temporarily, you want the cache to automatically remove these entries when the keys are no longer used by other parts of your application, instead of manually checking for stale keys. You can use a weak hash map as soon as a key is no longer referenced elsewhere, it will be collected by garbage Collector. Cleaning up your map for you. Let's suppose this element is not referenced anymore or anywhere else. It will get automatically handled by garbage collector. Now let's get a bit deeper into how weak references affect garbage collection. So in a regular hash map, even if key is no longer used anywhere in your application, it still stays in memory because it's strongly referenced by map. So the garbage collector won't remove this. So here the map can grow unnecessarily. But whereas a weak hash map, if the only reference to key is the weak reference inside the map, so the garbage collector here is free to collect it, or the garbage collector is free to clean it up whenever memory is needed, which helps in automatically managing memory usage. So this is why weak hash map is ideal for seniors like memory sensitive caches. Now let me show you a quick code example to bring this to life. For that, let me open our VS code and let's open our module nine. So here let me create a file with the name weak hash map dot Java weak hash map test dot Java okay. And here let me do the necessary things PS vm. And to use weak hash map we need to import it also. For that let me import it. Import Java dot util dot weak hash map. So first of all here let me create a weak hash map object. So it is a map. This one also contains key value pairs. Weak hash map of. Let me give key as object, Object type and value as string. And let me define Weakmap. Equals to new weak HashMap. And here let me create two keys first. Of type object. Object key one equals to new object. So we have created one key. And then let me also create our second key. Object key two equals to new object. So now we have two keys. Now let's pass this key one key two along with values in our Weakmap using put method Weakmap dot put key one. And let me set its value as val one. And like this. Similarly, let's also put Key2 with value val two. So currently these keys are having strong reference. Let me remove a strong reference to key one. We can do that by nulling it. Let me show it to you how we can do that. Remove strong reference to key one. By doing this, key one equals to null. So by doing this we have removed strong reference to this key one. Now, if we hint our garbage collector to remove unnecessary spaces, we can hint our garbage collector like this system dot GC. If you call this, our garbage collector will run. So after calling it, we will wait for the GC to clear the weekly reference data object and then let me print out our map after GC and null link key one. Let me print out our week map and before Nulling key. Also, let me also print out our week map for better clarity. Let me also print it this here. Now if I run this here, you can clearly see the output. So before null in case we have this two and key key two with a strong reference having value two and key one with strong reference having value one. And after nulling key one we are only having key two with strong reference having the value two. Our garbage collector has freed up this key one with the weak reference from our cache. So basically here we put two keys into weak hashmap using Weakmap dot put method. When we set key one to null and triggered garbage collector, you will notice that the entry for key one gets removed automatically. Pretty cool right? So to wrap things up, a weak hash map helps you automatically manage memory by allowing the garbage collector to remove entries when their keys are no longer in use, and when their keys are weakly referenced. This can be super helpful when you are dealing with things like caches or large data structures. When you don't want to manually manage every entry. When you don't want to manually manage every entry. In such cases, you must use a weak hash map to remove the weak references which are no longer used in our application. So that's it for this video guys. Hope you got the clarity by now how weak references works in Java, and how weak hash map on garbage collector handles them. So that's it for this video. Thank you for watching and I will be seeing you in the next one.