Hey there everyone! Welcome back to our Java Collections Framework course. Today in this video, we'll be working on a popular challenge from Hackerrank that helps us understand one of the core implementation of set interface, which is the hash set. So the problem we are solving today is called the Java hash set of Hackerrank. So let's explore it and see how we can solve this challenge. So in this problem we are given a number of pairs of strings as here you can see on first line we are supposed to enter the n number of inputs. And based on this input here we are going to give number of pairs. And here our task is to find how many unique pairs there are. So here we have pairs. Let's name space. Here we have John Tom John Mary and then again John Tom. Here we have duplication and Mary Anna and Mary Anna. So here we need to find out how many unique pairs we have. So this is where Hashset becomes particularly useful because Hashset stores only unique elements, making it a perfect fit for this challenge. So before we jump right into our solution, let's take a moment to talk about Hashset. A Hashset in Java is a part of Java Collections framework and implements the set interface. We all know that it stores only unique elements, meaning if we add duplicate elements they will not be added again. This make it ideal for solutions like this where we need to handle unique values. Now let's break down our problem step by step what we are going to do. So here we are given a multiple strings pairs and we need to count how many unique pairs we have. That's the first step we are going to do. And each pair of these two strings first name and last name. Our job is to store these pairs in data structure that only keeps unique ones. So naturally we will use hash set. So this challenges a solution approach would be. First we will create a hash set that stores each pair of strings like John Tom. We add this one and then this one, this one, this one, this one to hash map. So whenever it reappears, it will just ignore it. It will consider it only once. Right. And here we can do like this. So that's how we are going to add. So if you look at the available code in our code editor this is Unmodifiable code we have here. Here we need to only implement this part of our solution. So first here they are taking an input in T. And based on this T they are looping and storing this first name and last name in these pairs. Here they have created a pair left array and pair right array. They are storing pair left array in this pair left. So they are storing pair lifts left pairs like these ones John, John, John, Mary, Mary, these in this left pair and the pairs. This one. Tom. Mary. Tom. Anna. Anna. They are storing these in this pair. Right. Okay. So here basically we have two arrays pair left and pair right. And we need to combine them by first element and pair writes first element. We need to concatenate those both. And then we need to store it in our hash set like this. We have to do t times. And then finally we will be having our unique pairs in our hash set. And then finally after each insertion, we will output the size of the hash set to show how many unique pairs have been added so far. So if you see the sample output here on first iteration, you can see whenever we add this pair into our hash set, we will be having our hash set size as one, right. So whenever we add first element it would have set size would be one. That's why we are getting output here as one. And whenever we add one more element it will see is this pair is unique or not. So on first element we have join term and second element will be having join Mary. So these two are unique. So that is why our hash set size will get incremented by adding this pair also to our hash set. Now our hash set size would be two. Since our hash set size would be two. We are going to print this out. And then our third iteration on our third iteration will be adding join term. And we are having this join term already in our hash set. So it just ignores it on our current size would be the same as two. So it will just print out that size again. And then when we add this Mariana which is a unique element. So it will add that element to our hash set and it will just simply increase its size. So now our size of hash set would be three. So that's why we are getting three here. And then we are again adding Mariana. So it is already present in our hash set. Since it is already present, we don't need to add one more element again to cue uniqueness in Hashset. So again, on this iteration our hash set size should be the three again. So that's why we are getting three here also. So that's how it works. And if you have a bit of confusion here, you can always read this explanation for better clarification. See after taking first input you have only one pair. Join Tom. That's why we our size is one and after taking second input you have two pairs. Join Tom and John Mary. So that's why our size is two. And then after taking the third input you still have two unique page because we already have this join Tom in our hash set. So that's a or size is B two here again. And after taking the fourth input you have three unique pairs John, Tom, John, Mary and John or Mariana. So that's why our hash set Hazard side would be three. Now taking fifth input. You will still have the unique pairs because Marion already present in our asset. That's why our size would be three again here. So that's the explanation. Now let's dive into our solution and implement our further code in solution. So let's dive in. So first of all what we need to do here is here I need to create a hash set hash set of string. We need to go in. We are going to store strings right. So that's why a set of string let me give its name as unique pairs equals to new hash set. So here I have created a hash set object. Now let's loop through these arrays. And on each iteration let's get the elements of pair lift and pair rate based on I int I equal to zero. And I would be less than t I plus plus. And based on this, first of all let's combine these two pairs, a left and a right. Then we're gonna store that into our hash set that let me create a string full pair equals to. Let's combine it. First of all pair left of I. And with this space then we're gonna combine it with pair right. Got it. Pair rate of I. So after combining these two I left and pair rate elements. Now we need to add it to our hash set which would be new page start add pull pair. Got it. Like this is like this. We're gonna add our each string pair into our unique pairs. And after getting it after doing this we need to print out the size of our unique pairs. Or we can do that by simply calling unique page dot size method. And we're gonna print out its size. So that's it. That's all we have to do here. If I run this program now, now here you can clearly see it will get executed. And let me submit this code. And boom, there you have it. All test cases passed. So overall in this code we have created a hashset to store unique page strings. And then we will loop through the pairs. We iterate over pairs, concatenate the pair left with pair right into a single string known as full pair. And then we have added that full pair to our hashset. After adding each pair, we print the size of the hash set, which gives us the count of unique pairs so far. So that's it for this video guys. Go ahead and try out this solution, or even you can try out your own solution for this challenge. It is a very easy challenge. If you see difficulty here, it is set to easy and you will be getting ten points for doing this challenge. So that's it guys. That's it for this video. Thanks for watching and I will be seeing you in the next one.