Hey there everyone! Welcome back to our Java Collections Framework course. Today I am going to talk to you about a topic that is crucial when working in multithreaded environments. How does concurrent hash map achieve thread safety without locking the entire map? In a typical multithreaded application, ensuring thread safe operations on shared data structure is a common challenge. Concurrent HashMap is one of the most commonly used class for handling this efficiency, and its design is quite interesting. We know that in a basic hash map, multiple threads can lead to data corruption if they access and modify the map simultaneously. But using a hash map with global lock hash table, for example, can cause performance issues because it blocks other threads from accessing the hash map collection, it allows only one thread at a time, so concurrent HashMap solves this problem using clever techniques to ensure minimal contention and high throughput. First, let's sit back and talk about cloaking for a second. In multi-threaded environment, if you have to lock the entire map for every operation like inserting, deleting, or updating, it leads to a bottleneck. For example, imagine a group of workers need to access to a shared table, and if only one person can access to this table at a time and everyone else has to wait here, the overall process slows down. That's what happened when we use locks to control access data. Cause while using locks, only one person can access the shared resource at a time, while others are waiting until accessing thread completes its work. So that's what locking means. So how does Concurrenthashmap handles this? So concurrenthashmap, instead of locking the entire map, Concurrenthashmap divides the map into smaller chunks, which before Java eight were called segments. These segments can act as independent hash tables. This one is a independent hash table, and this one is an independent hash table, and this one is an independent hash table. This means that different threads. Let's suppose these are threads. Let's suppose we have some more threads some more chunks of our hash map here. So here different threads can modify different segments simultaneously without causing any conflict, significantly reducing the contention. While this thread accessing this segment this thread can access this one and this one, this and this one this. By dividing this whole work to different threads, we can complete our task pretty much fast, as well as we can reduce the contention of colliding different threads onto a same task, onto a same chunk. In Java eight or beyond versions, I mean further versions segments were removed to simplify the design, but the idea remains the same. Instead of locking the entire map, it locks only part of the map, and in many cases it doesn't need to lock at all. So the core concepts behind Concurrenthashmap are mainly we have three concepts here. One is partitioning the map. It divides the map internally, allowing parallel access such as this. And another concept it depends upon is non-blocking techniques like cache, which is nothing but compare and swap. Instead of locking during updates, it uses a technique called compare and swap, where it checks if a value is what it expects and only then updates it. This reduces the need for locking in many cases, and third, technique it depends upon is optimized internal structure. When the number of elements in a bucket gets too high, it switches to a tree structure like red black tree, which ensures that lookups are always fast, even when many elements have access to the same bucket. So these three are the main concepts which our concurrent hash map depends upon. Let me show you an example to make this clear. Let's imagine we have a map of stock prices, and you want to update the price of a particular stock in a concurrent hash map. If multiple threads try to update different stock prices at the same time, they won't block each other such as this. Here, let's suppose we have these two elements in our concurrent hash map. For AAPL key we have value 150. And for George key we have value 200. And here we are updating. Let's suppose these are two threads which are happening concurrently. And here we are updating these two different parts of our hash map concurrently at the same time. This will work. So here if the multiple threads try to update different stock prices at the same time. They won't block each other because they are working on different parts of our map. As simple as that. Here in this case, both updates happen concurrently because they are operating on different keys. They are stored in different parts of the map. That's why. And for more complex operations like incrementing the value for a key, concurrent HashMap uses a cache mechanism. This ensures that even two threads try to update the same key. Let me try to update a key. The same key concurrently. So here I'm up this. Let's suppose this is thread three. And here we are updating AAPL which is already being accessed by this thread one. So here we are currently updating 150 to 160. Concurrently we are updating 150 to 151 value plus one. Okay so here either one of them either this one or this one. one of them will be succeed and the other will reiterate successfully update the map so only one of them will succeed at a time and other will retry. Let's suppose this thread got succeed. Then this has to wait until it completes the task and after completing after updation of this thread, then it will retry the updation again on our map until it successfully updates the map. So here this compute method ensures that operation is atomic, meaning no other thread can interface with the computation until it's complete. And another significant improvement of Java it's concurrent hash map is the use of red block trees in earlier versions. In earlier versions, when too many elements hashed it to the same bucket, performance obviously degrades because it essentially becomes a linked list. Java eight improves this by red block trees when number of elements in a bucket cross threshold. Red-black trees ensure that lookup, insertion and deletion operations take logarithmic time, even when many elements are stored in the same bucket. For example, imagine you have a thousands of elements that hash to the same bucket. Instead of traversing through a long list of items, the map switches to read block trees, making lookups much faster. And one final point to mention is how iteration is handled. We know that concurrent hash map doesn't block entire map during iteration, unlike hash table. It uses failsafe iteration strategy, which allows you to iterate through the map even while other threads are modifying it. It provides a weekly consistent view of data, meaning you may see changes made during iteration, but it won't throw concurrent modification exception. Here is an example of how you can do it while this iteration is happening. Other threads can still modify the map. An iteration will reflect a snapshot of a current state. So to wrap up, Concurrenthashmap is a powerful tool in multithreaded environments because it allows concurrent reads and writes without locking the entire map by dividing the map internally using non-blocking algorithms like CAS, and optimizing its structure with techniques like read block trees. It strikes a balance between thread safety and performance. So if you are working on an application where multiple threads need to access a shared map, Concurrenthashmap is a great choice. It's designed to handle the complexity of concurrency efficiently, allowing your application to run faster while still maintaining the thread safety. So that's it for this video guys. Hope you got clarity by this question. How does Concurrenthashmap achieve thread safety without locking the entire map? So that's it for this video. Thank you for watching and I will meet you in the next one.