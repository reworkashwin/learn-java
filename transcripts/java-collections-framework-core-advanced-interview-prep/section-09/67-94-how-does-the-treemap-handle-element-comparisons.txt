Hey there everyone! Welcome back to our Java Collections Framework course. Today in this video we are going to explore our next question. How does the tree map handle element comparisons and what are its performance implications. So to understand it, let's reconsider. What exactly is this tree map? Tree map is a key implementation in Java that maintains elements in a sorted order. And obviously it is a implementation of map interface. And just like Linkedhashmap, our tree map also uses red black tree, which is a type of self-balancing binary search tree. And in a tree map, elements are ordered either by their natural ordering or by custom comparator. So when you insert an element into a tree map, it performs comparisons to determine where the element should be placed in a tree, and these comparisons based on either these two either natural order or custom comparator. Here, natural ordering means the natural order of the keys. If they implement comparable interface, for example, strings or integers, or normally ordered alphabetically or numerically. If strings, they are ordered alphabetically. If numbers there are ordered numerically starting from low to high in ascending order. Whereas a custom comparator that you provide. This allows you to define a specific order, such as reverse order or sorting based on a particular field of an object, and for each insertion and lookup or deletion. Here, the tree ensures that it remains balanced through the rules of red black tree. So what are these rules? Let's explore the rules of this red black tree. So here we have some rules which were tree map follows while inserting elements, inserting, deleting or updating its elements. So here we have the first rule. Every node is either red or block. What exactly I mean by this. So here each node in the tree has a color attribute either red or black. This color property plays a crucial role in maintaining the tree's balance. So here the main thing is the colors help you guide the tree when and how to balance itself during insertions and deletions. Let's suppose we insert a node into a red black tree. Initially, every new node is colored red. So here just let me insert like this. So initially every node is denoted with red color. And after the insertion depending on the position of the other elements, other red or black nodes, the tree may need to change the color of newly inserted node or other surrounding nodes to maintain balance. And second rule. Here you can see the root of the tree is always black, so whatever elements you add on start it will be read. But the root of our tree would be always black. This helps you reduce violation in the highest priority from the start. And black ones can have red childrens, but red ones cannot have red childrens. And we have this rule to prevent the tree from having long chains of red nodes, which could lead to imbalance. For example, consider a inserting a new node into tree. So here we have this node. If you want to insert a red node under another red node this creates a red red violation. Red red violation. The tree. While they need to fix this by performing recoloring changing one of the nodes color to black. And next rule we have here is every path from node to its descendants. Leaf nodes must have the same number of nodes. This ensures that paths across the trees are somewhat balanced, contributing to efficient lookups and modifications. What it means is, it means that if you start any node and follow any path to descendant leaf nodes, you will encounter the same number of black nodes no matter which path you take. This rule ensures the tree remains balanced in terms of height, preventing some paths from being much longer than others. Consider a tree where one side has more black nodes than other nodes. Let me construct a tree. So here we have 20 left to it. We have ten and right to it we have 30 and right to 30. We have 40 which would be black. So here on the left path of this tree we have two black nodes 20 and ten. And the right path here you can see we have two black nodes. And here also we have two black nodes. And here we have two black nodes and one red node. If one path had more black nodes than other, it would mean one side of the tree is heavier. Currently it is balanced on each path. We have two black nodes and two black nodes. So we can say that this tree is balanced, right? And let's suppose here we have another black node which is having value 50. Now this is imbalanced because now we have three black nodes on right side. Whereas on left side we have only two black nodes. So we can consider it as imbalanced tree. So that's our rule. Every path from node to its descendant leaf nodes must have the same number of black nodes. This ensures that path across the tree are somewhat balanced, contributing to efficient lookups and modifications. And next, our red black trees fifth rule is when violation of any of these rules occurs due to insertion or deletion, the tree rebalances itself through rotations and recoloring. For example, if red red violation happens, let's suppose you inserted a red node. Here we have a black node with the value 20. And here you have inserted a red node to this tree with the value 30. And then you have added one more red node here 40. So if you do this, if you insert a red node under a red parent violating the red red rule violation. So here currently we are having red red violation between 30 and 40. So if you perform left rotation on 30 it would rearrange the nodes like this. First we would be having 20. Then we will be having 40 and left to it. We will be having 30. Got it. So this is what rotation and recoloring means. So why all of these rules matters? It's because the rules ensures that a red black tree remains balanced at all times. Without these rules, the tree could become unbalanced, making operations like searching, inserting, or deleting take much longer than they should be if tree is imbalanced. So by keeping the tree balanced, red black tree ensures that their height is always O of log n, which will eventually lead to efficient operations. Now let's get back to our topic. So due to the balancing mechanism of red black tree in tree map, operations are efficient with a time complexity of O of log N for all major operations such as put means insertion, we have JIT for lookup or searching whatever you may call, and we have remove method to delete deletion. However, it's important to note that this is slightly slower than HashMap, which typically has an average time complexity of O of N. For these operations, HashMap have time complexity of O of n, whereas on tree map we are having time complexity of O of log n, but you won't see features like Treemap in HashMap. So that's the main difference. So in contrast, Treemap needs to maintain order which involves additional overhead from the tree balancing operations like read block tree. It has to follow all of these red black tree rules, which makes it slower than HashMap for purely random access patterns. So here Treemap is ideal when you need to maintain a sorted order of elements, such as in situations where you need to iterate over keys in sorted order or perform range queries. In such case, you should consider treemap. However, if order doesn't matter and you are prioritizing performance rather than the order of getting elements, in such case, you should consider HashMap. It would be a more efficient choice, since it is having only one of one complex of one time complexity. So in summary, while Treemap ensures ordering with logarithmic performance, it comes with the cost of being slower than hash based structures like HashMap, especially for insertions and lookups. So the decision between using Treemap or hash map depends on whether the sorted order is crucial for you or not. If it is, then you should go with Treemap, which is backed by red black tree rules. Or you should consider hash map if you don't want to depend on order for better and efficient case. So that's it guys. Hope you got the clarity by now. How does tree map handle element comparisons? And we have also explored the how these two collections performs with their term time complexities. And we also explored their time complexities. So it's up to you which one to use them on which conditions you are. So that's it for this video. Thank you for watching and I will see you in the next one.