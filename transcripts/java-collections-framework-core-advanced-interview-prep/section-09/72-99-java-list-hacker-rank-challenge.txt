Hey there everyone! Welcome back to our Java Collections Framework course. From today onwards, we are going to solve some hackerrank challenges on collections to strengthen our grip on Java Collections framework. Okay, so first we are going to start from Java List Challenge on Hackerrank. So here you can see for this problem we have two types of queries we have to perform on list such as insert and delete. We need to insert y element at index x. And here on delete we need to delete the element at index x. We have given a list L of n integers. We need to perform q queries on the list. Once all queries are completed, print the modified list as a single line of space separated integers. Hope you got it now. And here you can see the input format. First line contains an integer. So here they have given a sample input. Here they have entered five define, which means the first line contains an integer n, the initial number of elements in L, it represents the number of elements in our list. See here they have entered five. That's why here they have entered five elements in the list one, two, three, four, five. Got it. And next the second line n contains space separated integers describing l. So here based on this number we need to enter the number of elements. That would be the second line of. Then third line contains integer q the number of queries. Here they have given two. That's why they have performed insert and delete two queries. And if the line of query contains the string insert, then second line contains two space separated integers x, y and value y must be inserted into L at the index x. So here we need to give a element followed by its index where we need to insert that element. Got it. So here here you can see at index five we need to insert element 23. And then we need to perform a delete operation. If the line of query contains the string delete then we need to perform delete operation. Then the second line contains index six where we need to remove that element. We need to give its index whose element must be deleted from list L. So here they have given zero. That's why this tool got removed from our list. See we are having now elements from zero one, seven, eight, 12 and 23. So this is overall concept pretty much easy. We can build this solution right away. So first of all what we need to do we need to take integer input for that. Here we need to import scanner they have imported already. So let's create a scanner object. First scanner is equal to. Let me set it over screen like this scanner is equal to new scanner system dot. And let's take integer n t n equal to c dot text n. And to store elements in our list, let's create a list. List of integer list equals to new. Let's take array list and based on the input. Here we need to add elements into our list and these elements are entered by the user. So here we need to enter these elements in a loop. For that let me create a for loop for int I equal to zero I less than n I plus plus. As simple as that. Now let us try to add the elements into our list by calling list dot add and in which we need to pass SC dot next int. And that's it. We have completed our second line of input also. The third line contains integer q, the number of queries, and here we need to take the number of queries. So as we have already used this n, we can take input into this same n if we want. And if you want to create another variable known as query for this you can also create. But here I want to use this same variable instead of creating a separate variable for it. Since we don't have the use of this anymore, let's use it to store query n equal to SC dot nextint. Now here whatever input given by user stored in this n considered as the number of queries. Okay, so our third line also completed. Now we need to take queries to subsequent lines describing the queries. And each query is described as over two lines on insert and delete. So let's start another for loop for int I equal to zero, I less than n obviously and I plus plus. And here we need to take query string q equals to SC dot next. So in which either we are having insert or delete. So don't need to take next line. And based on this query let me compare it. If q dot we can use equals we can use compare two method to check either the query having the content insert or not. And here we can also use compare to ignore case. Let me use compare to method here compare to insert and if it returns zero, which means the query insert is matched, then what we need to do is we need to take two inputs. One is index, one is element. So first one would be index. Let me do this list dot add. And here we need to pass to. We are going to use this add of int index int integer element. So let's use it. So first index would be taken from user right. SC dot next int then element also SC dot next int. That's it. And the query is not insert. Which means on the next iteration it would be delete right. So let's keep it in else block list dot remove SC dot next int. So here we are passing index on. Based on that index we are removing an element at that particular index which we have entered here. So based on this index we are just removing that element from our list. So finally we have done all operation. But our last operation would be we need to print the modified list as a single line of space separated integers. Let's do that for int I call on List, and we can also use for each list dot for each. And here let me pass a As system dot out dot print ln. Or we need a single line right now space separated integers I s a space. That's it. We are done. Now first of all, let me run this and check if it is correct or not. It is processing and boom, it is correct. Now let's submit this code and see if all test cases are passed or not. See we have passed our all test cases. So that's it guys this is for this video. You can follow your own approach to solve this challenge. I hope you got the concept. Now how to solve this type of challenges in Hackerrank. So that's it for this video. Thank you for watching and I will meet you in the next one.