Hey there everyone! Welcome back to our Java Collections Framework course. Today in this video, we are tackling an interesting challenge on Hackerrank that will test your knowledge of the DK interface in Java. The challenge is called Java DK. So without further delay, let's dive in and explore what exactly is this challenge about. So here the problem statement here is straightforward. You are given a sequence of integers. First we have the n number of elements n as input. Based on this input we further going to add the elements. So since here we have entered six. That's why here we are having six elements. See these six elements. And followed by this n value. We are having a window size. You need to identify the. So overall here you need to identify the maximum number of unique integers in each subarray of size m from this sequence. So to solve this challenge efficiently we'll be using a double ended queue, which is nothing but DK, one of the most versatile data structure in Java Collections framework. So if you still didn't get what exactly you have to do in this challenge, you can see this explanation for this sample input and output. In this sample test cases, there are four sub arrays of continuous numbers. Say we have window size as three. Since you can consider these three as one sub array and these three as another sub array. And again these three as one more sub array. And finally these three as our final subarray. Like this we have four sub 535352523 and 25232. Got it. So on first sub array we are having two unique elements which are five and three. Here I have duplicated one more time right. So that's why it has two unique unique numbers. And on sub array two we are having three unique numbers 523 and s three. Again we are having three unique numbers five, two, three. And finally on S4 our fourth subarray, we are having two unique numbers, which are two and three. In these subarrays there are two, three, two unique numbers. Got it? And respectively. The maximum amount of unique numbers among all possible continuous subarray is three, right? So that is why we are simply outputting that maximum unique subarray number. So before we jump into this solution, let's quickly revisit what DQ interface is. To find more about this, you can click on this here. It will take you to this page. And from this Oracle documentation about DQ you can gather more information. So basically DQ stands for Double ended Queue and allows elements to be added or removed from both the front and back. So let's say we are having different kinds of method to add elements at first to remove element at first, basically to do operations at starting of the head of the DQ, and to perform operations at the end of the DQ. To perform first we have these methods add first, Remove first, get first, and to perform operations at last. We have these methods. At last. Remove last at last. So basically these add last over last remove first pull first. Get first, peek first. These methods are equivalent of these Q methods and offer remove poll element peek. Since our deque is extending the q interface when d q is used as q Fifo, first in first out behavior results, elements are added at the end of the d q and removed from the beginning. To do this, we have this equivalent q methods in our dqs. Add last over, last, remove first, poll first, get first, and peek first. These methods and to implement stack like behavior which is last in first out we have these methods. Add first. Remove first. Peek first. So overall Java provides two implementations of the DQ interface, which are nothing but a DQ and linked list. For this challenge, we'll be focusing mostly on Arraydeque, which is a good choice because of its performance characteristics Statistics, and it's more efficient in most compared to linked list. Now that we know the basics of dequeue, let's break down the problem solving approach. Let's come back to our challenge. So basically here we are sliding a window of size m across the array so that we can keep track of elements within the window and their unique count. So we'll use a DQ to maintain the elements of the current window and a Hashset to keep track of unique members. And each time we slide window to the right, we remove the leftmost element since. So basically we are doing this we have 535232. On our first window size we'll be having 535. So here we will be counting its unique elements on. First we have two unique elements. And then we are removing leftmost element from our subarray which would be five. So while removing now our window size now in our window. We'll be having three, five and two. Then again, we'll be counting the unique elements here. Now we have three unique elements and we are keeping its count. And then again we are removing leftmost element. Now we will be having five two and three. So here we have unique elements again three. And then we'll be having. We'll be removing this five. Then we'll be having two three and two. And here we will be having unique elements two. So since maximum is three we are just printing it out. All right let's dive into code and see how we can implement this solution. So here they have already created a deck for us using Array deck. And to keep track of unique elements here I'm going to use a Hashset of integer. Let me give it a set equals to new Hashset. So here we have a deck and a headset. And here we are taking in as an input. And here we have created a input object n and using that I n we are taking n number of elements on m window size as inputs. And here let me also create an array to store our subarrays int r equals to new int of n. And here let me create an array to store our elements int r equals to new int of n. And here let me simply change this to error of I equals to I n dot next int. So here we are taking all our inputs in our r array. After doing this let me set our max unique as zero first int max unique equals to zero. And let's perform our sliding window approach. Now for int I equal to zero I less than n I plus plus. And here let me add our arrays ith element to our dequeue q by doing d q dot add rr of I. And let me also add set of rr of I, and then let me remove the element that are out of the current window, which would be the leftmost element, right. We need to remove it. If d q dot size greater than m, then we are simply. Let me first store the remote element into remote. Equals to d q dot. Remove first. When you do this, we are simply removing our first element from d q and we are going to store it in this remote. Okay. This is showing us type mismatch cannot convert from object to int. So let me just set it as integer okay. We need to convert this object to integer. Now int also works fine okay. And here if d q dot contains removed then we will probably gonna remove that element from our set. Also set dot Remove. Removed. After that, we are going to track the maximum number of unique elements for that max. Unique. Equals to. Here we have function math dot max of max unique and set dot size. By doing this I can keep track of maximum number of unique elements, and after that I can simply print out our Max unique. Max unique. Let me run this code and see if it works fine or not. It's taking a bit of time. Now you can see. Congratulations! You have passed the simple test case. Now let's submit our code and see if it passes all of our test cases or not. And there you have it. We have successfully passed all of our test cases. And this challenge difficulty is set to max medium. You can handle it pretty much easily. I am sure of that. Now let's talk through this code again. So here we use a DQ to maintain the current window of elements and a hash set to track the unique numbers. Got it. For each element in the sequence, we add it to the DQ and hash set both. If the window exceeds the size M, then we remove the leftmost element from the DQ and potentially from the hash set also. If the element no longer in the DQ, then we have to remove it from our hash set also, right. So that's why we are doing this. And then we calculate the maximum number of unique elements in each window and update our max Q. And finally we are just simply printing out our maximum unique. So that's how we have tackled our challenge. So as you can see in this example, the maximum number of unique elements in a window of size three is three. So overall this challenge is a great way to practice sliding window techniques using the DQ and hash set from the Java Collections Framework. I hope this walkthrough was helpful. So that's it for this video. Thank you for watching and I will be seeing you in the next one.