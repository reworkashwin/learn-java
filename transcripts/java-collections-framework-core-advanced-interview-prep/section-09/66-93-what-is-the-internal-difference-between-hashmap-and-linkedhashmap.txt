Hey there everyone. Welcome back to our. Java Collections Framework course. Today in this video we are going to explore our next question which you might be facing on an interview, which is what is internal difference between a hash map and a linked hash map? We all know that these hash map and linked HashMap both are part of Java Dateutil package, and both provides key value pairs, but they have significant internal difference, particularly when it comes to how they handle the order of their entries. Now you might be wondering if both hash map and linked hash map store key value pairs. Why use one over the other? The key difference is that linked hash map maintains a doubly linked list, which allows it to preserve the order of elements in which they were added, which is nothing but insertion order. It can preserve the order of elements either by insertion order or access order, while hash map does not have any order. It does not maintain any order at all. And today I am going to explain exactly how this works. So let's start with the basics of a HashMap. First, we all know that a HashMap is a data structure that stores key value pairs, where each key is hashed to generate an index in an internal array. This hashing allows for fast access time on average over one time complexity. Specifically for get and put methods. These operations you will be having O of one time complexity on HashMap. Since each key in our HashMap is hatched to generate an index in an internal array, because once this hash is calculated, the key value pair can be directly accessed. However, a key point about HashMap is that it does not maintain any particular order of its entries. So when you iterate over a HashMap, the order in which you get the key value pairs may not match the order in which you inserted them. Now let's take a quick look at this example. Here we have created a HashMap and then we. Here we have inserted three elements into our HashMap. Three key value pairs. Apple with value three, banana with value two and cherry with value five. And here we are iterating over it. And if I run this program now here you can clearly see it doesn't match with our insertion order. We have added apple banana cherry and we got apple cherry banana. So the order of iteration here will likely be unpredictable. The HashMap does not guarantee any ordering. So you might see banana first followed by apple or cherry. So this insertion order is unpredictable. So that's the basic about HashMap. Now let's move further and talk about linked HashMap. So linked HashMap is a similar to HashMap. This one also stores key value pairs and also it relies on hashing to distribute the keys across an internal array. However, it adds an important feature. A doubly linked list that runs through all its entries. So this linked list allows Linkedhashmap to maintain the order of entries. You can configure it to preserve either insertion order, meaning the order in which you add the elements, or access order where recently accessed elements moved to the end of the list. By default, Linkedhashmap uses insertion order, but if you want to track the access order, you can set a special flag in its constructor. Let's explore this example. Let me close this terminal. So here I have added this example. Let's take a quick look at this. So only HashMap. The iteration order here will match the insertion order as apple banana. Cherry. If I run this let me comment this part out. And here we have it. See? Apple. Banana. Cherry. So how exactly this maintains order is internal, it keeps doubly linked list in addition to hash tables, as we already know. And each entry in linked hash map has pointer. Let's suppose we have these entries, and each entry in a linked hash map has a pointer to its previous and next elements. So last will be having null on first elements. Previous pointer would be null and this element's next pointer would be pointing to this node. And this would be pointing to this. And this would be pointing to this. And this pointer will be pointing to this. So each entry in linked hash map has pointers to its previous and next entry in the linked list. This is where difference comes in. Hash map only has an array of buckets and for collision resolution uses linked list or trees within those buckets. Whereas linked HashMap, in contrast, adds an extra layer of structure which is doubly linked list that connects all entries in either insertion or access order. So this link list provides predictable iteration behavior, but at the cost of slightly higher memory usage than a simple hash map, and it is having overhead due to maintaining the extra references. These pointers the pointers to the next and previous. To understand this further, let me break down the impact in a hash map in a linked hash map, each bucket also holds a reference to the previous and next nodes. These additional references allows it to remember the insertion order of elements, even as new ones are added or removed. To give you more precise example, let's modify the behavior of our linked hash map to preserve access order. We can do this by using the collector that takes a Boolean flag for access order, such as. Let me close this and let me comment out this part. And here let me add our example. Here we have it. So if you look at this example here we are passing a true flag which defines access order. And here we are accessing an element. Now if I run this program and here you have it Apple colon three. Since we have only accessed one element, that's why we are getting Apple colon three. In this case after accessing banana the iteration order will change. We will now see apple, cherry and then banana. The reason for this is that Linkedhashmap moves the most recently accessed element to the end of the list, when it's in access order mode. Let me verify this to you and sort access order map. Let me run this. And here you can see banana set to the last. Now if I access cherry again here access order dot get cherry now. Now cherry will be the last element. Let me run this. And here you can clearly see banana is the second element now and cherry is the last element instead of cherry. What if if you accessed apple here. Now if I run this again here you can clearly see the order as cherry, then banana, then apple. We got cherry first because we haven't accessed cherry at all in our linked hash map. That's why it got to the top and the accessed elements got to the end of the our linked hash map. So that's what access order means in linked hash map. So this feature is only available in linked HashMap. You can see this type of functionality in a general hash map. This type of behavior is extremely useful for certain use cases, like creating a least recently used cache, which is also known as LRU cache, which we have already explored in our previous videos in our previous module. I mean, where you want to evict the oldest or least recently accessed elements. For instance, when the cache reaches its limits, you can remove the head of the list, which represents the least recently accessed element. Now let's talk about the performance considerations between hash map and linked hash map. So of course there is a tradeoff between hash map and linked HashMap. The hash map is slightly faster in terms of raw performance, because it does not have the overhead of maintaining a linked list. However, this difference is minimal for most use cases, and the benefits of maintaining order in linked hash map often outweigh the costs, especially when you need order insertion. I mean, when you want to keep the insertion order and you need faster access than linked HashMap, then you might choose a hash map collection for your data to store. For example, if you are implementing a LRU cache or need to process elements in a specific sequence, a linked hash map is the better choice. It's also worth noting that Linkedhashmap has slightly higher memory footprint, because of the extra pointer needed to maintain the doubly linked list. Each entry requires additional space to store the links to the previous and next elements. So if memory is a major concern and you don't need ordering in that case only you might choose HashMap because HashMap would be more efficient in such cases than a linked HashMap. So in conclusion, while HashMap unlinked HashMap may seem very similar on the surface, their internal structure differs in a key way. HashMap offers fast access and retrieval with no regret for the order of elements, whereas the linked HashMap adds a doubly linked list to preserve the order of entries, whether by insertion or access order. So that's all about the difference between HashMap and Linkedhashmap guys. That's it for this video. Thank you for watching and I will meet you in the next one.