Hey there everyone! Welcome back to our Java Collections Framework course. Today we are diving into something that's super important when working in multithreaded environments, which we have already explored in our previous videos. It is nothing but thread safety and in this video itself, we'll be comparing two key players for explaining it, which are copy and write ArrayList and a regular ArrayList. And by the end of this video you will know exactly when to use each one. So let's get into it. So first up we have a classic ArrayList. Most of you have probably used this a ton in your projects because it's fast efficient. But there's one catch. It's not thread safe. What does that mean? Well, in multithreaded environments, if two or more threads tries to access the same ArrayList at the same time, we could end up with corrupted data or unexpected behavior. Not good. Right? Now you can make an ArrayList thread safe by synchronizing it. So what exactly mean by that? Synchronizing means it's like putting a lock on the door so that only one thread can access our ArrayList at a time. For example, you can do something like this. So for our ArrayList we can synchronize it like this by calling collections dot synchronized list and passing whatever list implementation we want. In this collection dot synchronized list format, you can here pass an ArrayList, a linked list, or a stack or vector, whatever you want, but there is a trade off. So here we are locking this entire ArrayList by synchronizing it right? So that only one thread can access this ArrayList at a time. So here locking means slower performance because every time a thread tries to access list, it has to wait its turn. So when this thread accessing this ArrayList, these all threads has to wait. And in real world application this can become a bottleneck. So what do we do if we need to handle lots of threads like millions of threads but don't want all these locking hassle? We want all threads to access our data structure, not like only one thread can access it at time while it accessing others has to it not. Not like that. If you do such things it will degrade our application's performance. So we need to go for better ones. We need to choose better data structures for it. So what do we do if we need to handle lots of threads but don't want all the locking hazards? So this is where copy on write error list comes into picture. So here this entire copy on write array list this one built specifically for multi-threaded environments. And here's the cool part. It doesn't need any external synchronization to stay thread safe. So how does it work? Well, whenever a write operation like adding removing this all comes under write operations or updating an element happens. Let's suppose this is our copy on write array list. So whenever a write operation like adding or removing an element happens, it creates a brand new of the array. It will create brand new array. You can think of it like taking a snapshot. Whenever you do you are doing operation. So here each modification works on a new snapshot. So whenever you are adding something into it, it will create a new separate list and it will do that operation on this newly created list of this exact clone of our actual original array list on simultaneously. When you are performing remove. It will create another snapshot of it and it will do this remove operation also simultaneously according to this ad, while this ad operation is performing on this newly created snapshot, whereas this remove operation is also performing at the same time on the one more newly created snapshot of our copy on write array list. And since our original list isn't changed so multiple threads can access this, our original array list and these threads can read from it at the same time without locking anything. Got it? Sounds pretty amazing, right? But it's just like with everything there is a flip side. So creating a new copy of the list every time you modify it can change and can get it can get expensive, especially if you are doing a lot of writes. So copy and write ArrayList shines when you have lots of reads and few writes, it's perfect for read heavy applications. Let's say you are building a chart application where most of the times users are reading messages, and occasionally someone sends a new message. In that case, copy on write ArrayList is great fit because reads are fast and don't require any locking. But if you are frequently updating the list, the overhead of copying might slow things down. So here's the takeaway. You can use ArrayList when you are in a single threaded environment, or you handle your own synchronization. So in such case you can use a normal ArrayList and you can use copy on write ArrayList. When you have a multi-threaded application with more rights than writes. So it's built exactly for that kind of situation. When you have more reads, then it will work perfectly there. So all right, that's it for today's session. I hope you now have a clear understanding of difference between ArrayList and copy override ArrayList, and more importantly, when to use them. So thanks for watching this video and I will be seeing you in the next one.