Hey there everyone! Welcome back to our Java Collections Framework course. Today in this video, we will dive into two essential methods working with collections frequency and disjoint methods from collections utility class. These methods make it super easy to analyze and compare data in your collections. Let's see how they work. First up, let's explore the frequency method. This method counts how many times a specific element appears in a collection. It's really useful when you need to find duplicates or see how common a value is. Let's explore it through an example. First, let me create a list. List of strings names equal to. Let me use arrays dot as list and in which. Let me pass some elements. Alice, Bob and let me pass Alice again. John and let me pass Alice again. Third time. So here you can see we have entered Alice three times in this list. So to find out the frequency of this list element in our list, let's use this frequency method. Here it is giving error. You need to. Import Java.util.list interface also to get rid of that. And here you can see we get rid of that error. Now to find out frequency let me store it in a frequency variable. And to get frequency we need to call frequency method from our collections. Collections. Make sure you enter collections here not just collection. Collection. Start frequency in which we need to pass two things. Our list collection and the name which we want to find out. The frequency for the element which we want to find out its frequency in a particular collection. So first one would be collection and second one would be element. And let me pass list. And that's it. Now let's just print out the frequency of allies in our names collection. Let me set its message as frequency of allies Cola. And let me just simply print out our frequency over here. So when I run this program now here you can clearly see the frequency of allies in our collection as three. So in this example you have just created a list of names. and we want to know how often Alice appears using the frequency method, which does all the work for us. In this case, it prints out three, since Alice appears three times in our collection. So as you can see, the method quickly returns the result. You don't need to loop through the list yourself to find out the number of times Alice's arrival in our collection. If you want to do this manually, then you need to create a loop. Then you have to iterate over all collection to find out how many times Alice appeared in our collection, which will get you the O of N time complexity, whereas this one will work on simply on O of one time complexity. Now let's talk about disjoint method. This method checks if two collections have no elements in common. Think of it like a quick way to check if two groups are entirely different or not. Let's also explore it through an example. So here to check disjoint we need to create two lists for that. Here let me create two list list one and list two. List of integer. First of all, list one equals to new. Let me use arrays not as list. And let me add some numbers in it. One, two, three and four. And again let me also create a list of list of integer. List two equals to array. Start as list in which. Let me pass five, six and seven. Five, six and seven. So if you combine the list one and list two even at that time, also you will be having unique elements, which means our list one and list two two have completely different elements. Now to verify it, let me create a boolean or descent variable or descent equals to. So our descent method also resides in the collections. So to access it we need to call collections first collections dot disjoint in which we need to pass again two arguments which are nothing but we need to pass two lists list one and list two. And that's it. So to test it out let me call our assert here. Let me set our message as are the list disjoint with a question mark. And let's see its status by printing or disjoint. Now let me run this. So basically here we have two lists. List one and list two. And here we have used the disjoint method to see if there are any common elements. Since there aren't any it will return true. Let me run this. And here we got true if we have any common element between it. Let me add two here. Now if I run this you will probably get false as a result. See? We got false. That's how it works. This method confirms that the two lists have no overlapping elements. When it have overlapping elements, it just returns false. When it doesn't find any overlapping elements, then it simply returns true. So to wrap it up, frequency helps you count occurrences of a specific element in a collection, whereas disjoint method checks if two collections have nothing in common. Both of these methods are powerful tools that save you from writing extra loops and conditions. So that's it for this video. Thank you for watching and I will meet you in the next one.