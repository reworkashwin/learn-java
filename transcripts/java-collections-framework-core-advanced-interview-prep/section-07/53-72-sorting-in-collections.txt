Hello and welcome back everyone. Today in this video we will be discussing sorting in Java collection. So sorting is a common operation which we perform on data. And Java makes it straightforward with its built in utilities. We will cover two types of sorting. One is natural order sorting and second one is custom sorting. So first of all, let's start with natural order sorting. So basically natural order sorting refers to sorting elements in the default logical order based on how they implement comparable interface. For example, numeric data types will sort in ascending orders and strings will sort alphabetically. So this is the default nature of these types. For nums it would be ascending order, and for strings they will get sorted by default in their alphabetical order. And you can also reverse this order using comparable using comparable interface. You can reverse this order, or you may even create your own custom sorting methods using this comparable, which we have already explored in our previous module, what is comparable and comparator interfaces, how they are useful. So here I would suggest you to go and please watch that video once again for a better understanding on sorting. So generally sort elements in collections or Java collections framework provide Collections.sort method for sorting lists. Let's explore a quick example of it. So first of all, let me create a collection. Here I'm creating a collection of list of integers, numbers or Numskulls to restart as list. And let me pass some numbers in it five, one, four, three and two. Okay, now to sort them in an ascending order, we need to pass this collection in our Collections.sort method. Let me do this. Collections dot sort. So in which let me pass our nums. And here you have it. So after calling it let me just print out our nums. And if I run this program, you can clearly see our collection got sorted. See? One. Two. Three. Four. Five. To its natural order, which is ascending order. So what exactly is this collection? Is it interface or class? To verify it, let me open this implementation. And here you can see this collections is a class. And if you want to find this sort method let me just type out sort here. So here we found 19 results. Let us go through every one of it. So here we are having actual sort method which is accepting a list object. So in this position we have used this method. If I open it see we have used this method sort which is accepting a list. And here we can also sort other types of objects like list of strings. Let us explore this one also. List of names equals to array. Start as list and let me pass some names in it such as Sean, Alice and Bob. So let me just print out before sorting our collection names, and let me give a message here as names before sorting. And then let me just call our sort method and pass this names in it. Collections dot sort of names. And let me just simply copy paste this names after sorting. Okay. Let me run this. Now you can clearly see names before sorting as Sean, Alice, Bob and after passing our names collection into our Collections.sort method, we got our collection sorted to Alice Bob John to their natural order, which is alphabetical order. Now let's move on to the custom sorting demo dot Java. And here custom sorting allows you to define your own order by using a comparator. This is particularly useful when you want to sort based on non-natural criteria. Or or you want to sort based on an object in your collection. For instance, imagine you have a list of objects like person, and you want to sort them by their age instead of their name, which we have already done in our previous videos. Serious. However, let us explore it again for a better understanding. So to do this you can create a comparator. So to explore this comparator example, first of all, let me create my own class, the name person. And in this I'm going to give two fields. Obviously one would be name and another would be age string. Yeah this is person's name and int age. And let me build a constructor for this to store our objects name and age into our person collection public person string name comma int age. And this dot name equals to the name which is passed by this person constructor, and this dot age equal to the name which got passed to this person constructor. And that's it. Now let's override our string method to format this public string toString return name with a hyphen and then age. So here I'm just formatting this name and age combined so that if we print our person's collection so that it will just print its element like other collections do. So now, before creating our custom comparator, let us create a collection list of persons. List of person people equals to arrays dot as list. And in that let me add some persons person of John with the age 25 comma new person of price with the age 50 and another person with the name Bob and with age 20. Don't need to put semicolon here, just need to put comma. And finally here put semicolon. And now it's time to create our comparator comparator of person by age. I am going to compare by age. That's fine. Now given its name as by age new comparator of person and in which we need to override a method. So if you click on it and click on this icon or it doesn't showing anything here, it should be showing add unimplemented methods. Okay, let's do it manually. We'll be having a compare method which we need to implement that method. Override that method to get rid of this error. So let me do that. Override public int compare person 1P1 comma person p two. And that's it. Now we need to return an integer or that. Let me just return integer dot. Compare. Let me call this method which is available on our integer class and in which. Let me pass p one dot h. So here we are comparing our current h with the first h, which should be p two dot h. Now if you call and pass this people on the comparator in Collections.sort method like this collection dot sort the collection which we want to sort upon, which would be people Obviously on the comparator, our custom comparator, which we have developed so that we can sort our collection by its age. So which is by age. Let me pass it. And if I print out this collection, people, now you can probably see all the elements in it are sorted by their age instead of their natural order. See Bob with 20. John with 25 and Alice with 50. So highest aged person got the last place in our feeble collection. So here we have created a person class. So this is the class which represents a person with the name and age. And then we have created a people collection by using this person's collection implementation. And we have added some persons into it with their name and age combined. And then we have created our custom comparator so that we can sort this collection by the age in it. Then we have passed our collection and the comparator which we have created custom one and then we have finally printed the result after the sorting. Now we got this output as expected, sorted by the age. So here, instead of writing all this code, you can sort it using lambda expression. How can we do that? So here we all know that if you open this comparator interface it is a functional interface which means we can perform lambda expression on it. So let's just take advantage of it, the functional interface and specify it like lambda expression instead of doing all of this. So let me just remove this. And here instead of by age let me just do this. Let me pass. First of all, what is the syntax of our lambda expression. We need to pass parameters which should be P1P2. And then we need to pass two parameters here. So if you open this comparator interface and if you scroll down a bit here we have this comparator method in which we need to pass two objects. Right. So that's why here parameter list I am just passing those two objects. P1 and P2 and lambda expression. I'm calling and here I'm going to use the integer dot compare method. And here I'm going to pass p1 dot h and p2 dot h. Now this will do the same thing. Let me verify it to you. So if I run this program now you can clearly see the same output Bob as 20 John 25 Alice 50. So this does the exact same thing as our previous example, but in cleaner and more concise way. And sometimes you want to sort in reverse order so that Java provide the collections dot reverse order method, which reverse the natural order or any custom order you define. So here, if you want to print this collection in reverse order by the age, you can do that. So after calling Collections.sort and passing people in it and here we need to do the thing. Let me just cut this part. And here let me call our collections dot reverse order again. Collections. Thought reverse order in which we need to pass our comparator. Right. So for that comparator here, let us pass our lambda expression which should be p1 p2 integer dot. Compare p1 dot is p2 dot h. And that's it. Now we will get this collection in a reverse order sorted by their age. If I run this here, you can clearly see Alice with the age 50 got first, then second highest age John got second, and then Bob got the last days because it is having the minimum age in this collection. So that's why. So overall sorting in Java is highly versatile, whether you are using the natural order or customizing your own with comparators, the flexibility is what makes Java sorting utilities so powerful. I hope you got the clarity by now how this sorting works. And in the next video we will dive into searching and binary search within collections. So see you there. Thank you for watching and I will see you in the next one.