Hey there everyone! Welcome back to our Java Collections Framework course. Today we are diving into a crucial part of Java Collections framework, which is the arrays class. We all know that arrays are at the core of almost every Java program, and understanding how to work with them, manipulate them, and integrate them with Java collections framework will open up a whole new level of coding possibilities for you. All right, so let's begin with the basics. Arrays. Arrays in Java, as you probably know, are used to store multiple elements of same type in a single variable, unlike regular variables that holds just one value. Arrays let you work with collections of values, all indexed for easy access. You can think of arrays as the most basic form of collections, which is why they are so important in broader Java collections framework. But here's the thing working with arrays isn't just about sorting elements, right? They're sorting, searching, converting them to other data types also, and much more. And this is where Java really shines. Thanks to the utility methods provided by Java.util.arrays class, there are much more we can do before we dive into more details. According to this Java.util.arrays class, let's understand why should we care so much about arrays in context of collections? Well, even though more dynamic collections like ArrayList Hashset are often preferred because they are more flexible. But arrays are faster when it comes to accessing elements by their index, and they require less memory than the actual collections like ArrayList, Hashset, whatever it may be, sometimes you need that extra performance boost to your program. Arrays also serves as a foundation for many Java's data structures. Even classes like ArrayList are actually backed by arrays internally, so mastering arrays means you are already halfway to understanding other, more complex structures in Java. Let's start by talking about the sorting. First, sorting is probably one of the most common operations you will perform on arrays. Thankfully, Java's array class makes sorting super easy because you don't need to do anything here. You don't want to create a separate sorting function or implementation by yourself, because this Java Start Arrays class will provide you a sort method, which is a static one. You can call it like array, sort like that and upon which you need to pass your collection. Let's suppose if you want to sort out this array, we can do it by calling arrays sort method. So say we have different sort methods here. We're gonna explore about them shortly since we need to sort an integer type array. So here we are going to use this one sort of int and two sort long array. We have this sort of long. And here we have sort of short two to sort out a short array, whereas for different types of arrays we have different types of sorts here. So currently we are having an integer type array. So let's use this one sort of. It will automatically fetch it out based on the array you are passing in this sort method. So here I am passing numbers. So since this numbers is an int now it will probably gonna make use of this sort method of int. If I hover upon it you can see void Java.util.arrays sort of int a. If I open it now here you can see its actual implementation and details regarding this sort method in more detail. So it sorts the specified array into ascending numerical order. And here we have a implementation. Note the sorting algorithm is dual pivot dual pivot quicksort which is developed by Vladimir, some Russian developer, along with John Wortley and Joshua Block. So this is developed by these three developers, and this algorithm offers O of n log n time complexity performance on many data sets that cause other quicksort to degrade to quadratic performance, and is typically faster than traditional quicksort implementations. So that's the reason why should we use this sort method instead of creating our own sorting method. So here we have sorted this right now. After sorting, our array will look like this. Let me verify it to you. Let's use for each method int I column numbers. Upon which let me just print out our I sort. Let me remove this ln print. I let me concatenate it with a comma and a space and that's it. Now if I run this program you can clearly see our elements of array in in ascending order. Got it. So here's the cool part. Java doesn't just sort primitive types like integers or doubles. You can also sort objects And if you want to sort objects like strings or even custom objects like employee objects based on their names or salary, you can use comparators, which we have already explored in our previous videos. How to use comparator and comparable. So here, pretty much what I meant to say is we can sort everything. Either it may be objects or either it might be primitive type, whatever it might be. For example, let's create a employee class and try to sort its objects based on its name and salary. Okay, let's try to do it again here in this code. So first of all let me create a class employee. Class employee in which I'm going to create two private properties. One would be name private string name and another would be salary private int. Now let me define it as double salary. And let me construct a constructor for this public employee of string name Come on. Double salary. This dot name equals to name. And this dot salary equals to salary. Let me override its toString method. Public string toString or return one. Let me set is its output as. Employee name equals to. Let me set it to this employee name equal to name plus salary equal to plus salary. And in our main let me go comment as sorting objects. First of all let me create a employee object and in which let me add three new employees okay. Along with their salaries employee. So here I'm creating employee array employees equals to in which let me add new employee of. First of all let me add John with the salary ₹50,000. And let me repeat this for three times. And here on second time let me add Jane with 60,000 salary and our third employee would be Jack with 40,000 salary. Or let me give ya, 40 is good with 40,000 salary. And let me put semicolon here. So if you observe here we have implemented a array like object for this employee. And if you want to sort this employees by their salary you can do like this arrays dot sort of. First of all we need to pass employees. Then a custom comparator after employees. We need to pass our custom comparator which would be comparator dot. Let me import this one first comparator comparator dot comparing employee by their salary we need to get salary. So here we need to create that public method to get this salary. Since it is private, we can't access it directly. So for that, let me create a public double. Get salary method which will simply returns our salary. Now it will work. Wait a minute. Where did I pass this? Here. Not. We need to do that here. Okay, let me remove this one. So in this Array.sort method, we need to pass this employee. And with a custom comparator. And in this comparator we are comparing it with employees salary. And after that let me just simply print out our employees before calling this. Let's see what its actual order is for employee colon employees just as out employee. And if I run this now here you can clearly see. And one more thing here is here. After this let me call a empty assert. Otherwise our output will start from this line. Okay, let me run this program. Now, here you can clearly see we are having employees elements order in this way. Employee name equal to John first, then Jane, then Jack. Okay, now, if you want to sort it by their salaries in an ascending order, let me just simply print out after our sorting by their salary for each employee. Employee column employees as sort employee. Let me run this program. Now. Now here you can clearly see. Oh, wait a minute. Let me also after this, let me just simply print out a message. Employees after sorting by their salary. Okay. Now let me run this. Okay. This is before sorting with their salary and after sorting by their salary. This is the output 40,000. Jack. Then John 50,000, then Jane 60,000. So that's how you can work with objects to perform a sort. See how much simpler it is in just one line? You have sorted complex objects based on one field of your choice. You can do it by salary, or even you can do it by their name. Also, let me show it to you how you can sort it by their name. For that, let me set a getter to get its name string get name. And here let me just simply return our name. Okay. And instead of calling get salary. Here let me set get name instead. Okay. Now if I run this you'll probably see it is sorted out in an order. First we have Jack, then we have Jane, then we have John. And for better clarity, let me put this as Alice, or let me keep it as Sean. And here, let me put this person as Alice and this person as Martin. Okay. Now, if I run this here, you can clearly see this collection is sorted out by their name. First Alice, then John, then Martin. Got it. So that's how simple it is. So as we have explored sorting, now it's time to explore searching. We can also perform searching using arrays. Dot binary search method. We are having a binary search method in our arrays class. Let's explore it. This is our array numbers. And here we need to search out which is sorted here we have sorted it right since binary search only accepts sorted array. If you are not sorted this array, you have to sort it or you pass the elements in a sorted order in binary search. Okay, since our numbers are sorted now, our array is like this 12359. Now if I want to search a number in it, let's search for five element. Let me set target as int target equal to five. I want to search this number in our number set as we have already explored our array start binary search method will return an index where it found the element right. So let's keep that index in an index variable. Int index equal to restart binary search in which we need to pass two things. One is our array which is numbers. And second thing is the element which you want to search for which is the target. And this binary search method will return a negative number if it can't found that element. So as we know that. So let's set up a condition if index greater than or equal to zero which means we found the element because in index we don't have a negative value which means we found the number's index. That's the only reason this if condition will get true okay. So here let me just simply print it out element found at index. And let me simply print out our index. And if it is not zero or greater than zero it is not positive. Then you need to simply print out. Otherwise it would be negative right? If we get negative value means this binary search is unsuccessful. Our binary search won't find the target value in the array which we have provided. So in such case, let me just simply print out we can't find the target element in numbers array. Okay. Now let me run this. And we will be having five in our numbers array. At what position we are having we are having 012, three and third index we are having. That's why we have got this element found at index three. Got it. So that's how this binary search operation works. And let me give some other element which we can find in our numbers array. So since our numbers array don't have 67. Now if I run this program again now we'll probably get. We can't find the target element in the numbers array. Got it. How it works. So now let's explore our arrays class even more, because Java's array class doesn't stop at sorting and searching, it comes back with other useful methods also to make working with arrays easier. For example, you can fill arrays with specific value using arrays. Dot fill method. Let me define our new array as numbers two or nums two. Now let me give nums. And here let me pass nums. Now here we can fill this nums with complete sevenths since it is having size ten. Now after doing this operation it will have 10/7 in nums array. Let me simply print it out using our for each assert I. And if I run this, you can probably see ten sevens or for better view. Let me set it like this with a comma and a space and let me remove this cell. Now here you can clearly see the ten sevens. 123456789. Ten. Got it. It works. Not only filling. You have methods like rest dot copy of. You can also copy erase using restart copy of or even compare two arrays to see if they are equal using a restart equal method at some point. We have already explored those methods in our Java Collections Framework course. Now let's talk about converting arrays to collections, because often in real world applications, you will want to convert arrays into a more flexible data structures like list or set. You can easily do that using restart as list method, which is we have already explored in a lot of examples previously. However, let's explore it once more. So here. First of all, let me create a string of names equals to in which. Let me pass some names here. Alice, Bob and Charlie. And to convert these names to a list, we need to create a list first. Okay, wait a minute. Here. We need to declare it as array. Now it's for now. It's perfect. Now let me create a list of. Since our base array is string here we need to keep the. Type as string here also. And here we need to import our list. Also. Let me import our list. It's fine. Now name list equals to arrays dot as list. And let me pass our names in it. See this will work. Now our names array is converted to name list. However, we can still access this names array. And in this names list we are having the same copy of this names array. So basically this method converts your array into a list. But here's a quick caveat. The list you get from Arrays.aslist is fixed size. You can't add or remove elements from this list because it is just a wrapper around the original array. Now you can't do name list dot, add on it and let me add Jack here. And if I do this now, we will be having this unsupported operation exception. If you do like this, if you convert array like this, you will be having this fixed size problem. You can't add or remove elements from this list because it's just a wrapper around the original array. If you need fully modifiable list, you will need to create a new one like this. Let me show it to you like list of string modifiable list equals to new array list. Let me also import this one. So here After importing a released. We got rid of that error. Let's see. Yeah, it's fine now. Now in this we need to pass a list as list. And let's pass name set. Now this modifiable list is modifiable. Now we have a list that we can change however we like. Now let's try to first of all let me comment out this one. And here in this modifiable list let me try to add Jack into our list. Modifiable list dot add jack. And if I run this we won't see any errors in it. Got it. We have successfully added Jack into our modifiable list. Now what about we know that how to convert arrays to a collection. Now what about converting collections to arrays? Is it possible going the other way? Converting a collection back into an array. It's just as easy as converting an array to a collection like Arrays.aslist, which converts a normal array to a collection We have two array method which will convert a collection back to array. Let me show it to you. So here we have this modifiable list. And we need to convert this modifiable list to an array. We can do that by knowing first of all we need to create an array of type of that particular list which we want to convert to. Okay here I have taken string since our modifiable list having string collection. And here let me give names. Array equals to name. Let me give modifiable list dot two array in which we need to pass the type new string of zero. By doing this we have successfully created a list collection into a string array. This is especially handy when you are working with API's or libraries that expect data in an array form, but your data is in a list or set form. In such case, you can convert these collections to arrays like this. So all right, let's switch gears for a second and talk about performance. We already know that arrays give us constant time access O of one to any element, which is great. But they have some downsides too which is arrays are fixed size. Once you declare an array with, say, ten elements, you can't add 11th element to it. You either have to create a new array or use a dynamic data structure like array list. And another thing is memory. If you ever overestimate the size of array, you are wasting memory. If you underestimate, you will have to go through the trouble of resizing it. So while arrays are fast for lookups but mindful of other fixed size limitations and memory overhead. So if you are having this in mind, you will get to know which type of data structure might be perfect for you. Now let's quickly recap what we have covered today. So first thing we have learned is arrays are fundamental, both as collections on their own and as building blocks for other data. Structures like ArrayList and Java's arrays class provide essential utilities like sorting and searching. On searching we have binary search and sorting. We have sort method so we will get utilities like this. Filling, copying etc. which makes our programming journey a lot more easier. And we can convert arrays to collections and vice versa easily using Arrays.aslist and to array method. So arrays give you a great performance when it comes to accessing elements, but comes with limitations like fixed size. So when next time you work on with arrays, remember that you have got a whole toolbox of methods in the arrays class to make your life more easier. If you want to explore more about arrays class, press control and click on these arrays. It will take you to its implementation. Now here you can explore even more about this arrays class. There are a lot of method which we haven't covered yet. C. Public static double copy of range. We have boolean copy of range. There are a lot of methods, a lot of methods which we haven't covered yet C Indexof we have hashCode method, different types of hash codes. We have and we have deep hashCode method also. And not only equals we have deep equals. Also select this. We have lot more methods in our race class. So I will I would suggest you to go and explore all these operations for your better understanding. So overall that's it for this video guys. Hope you got the clarity by now how these arrays start class method works. How does this arrays class works and what are the things we can perform using this class? We have explored searching, sorting and converting arrays to collections and collections to arrays. We have explored both. So overall that's it for this video. Thank you for watching and I will meet you in the next one.