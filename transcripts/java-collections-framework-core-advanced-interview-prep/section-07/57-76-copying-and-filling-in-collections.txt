Hey there everyone! Welcome back to our Java Collections Framework course. Today in this video we are going to cover something super useful which is copying and filling in collections. Whether you are organizing data or managing lists, these methods are going to make your life so much easier. Let's dive in. First of all, let's start with copying collection. So what exactly is this? Imagine you have got a list. For example, let's suppose this is our list. We have this source list in which we have Java, Python, C plus, plus. And you want to create an exact copy of this list without messing with the original list. So without messing with this source list, we want to create an exact copy of this list. That's where the collection Start copy method comes in. Let's explore it. So to copy this list into another list, let me create one more list here. List of string. Let me name it as DST list equals to new array. List of arrays. is not as list in which I'm going to add three empty elements. Here I have created a destination list with three empty elements, and now I want to copy this source list into destination list so we can achieve this by calling collections dot copy method. Let's call it collections dot copy, in which we need to pass two things. One would be the destination list where the copied elements are getting stored into. In our case it is destination list. And then we need to pass a source list the elements where we are copying from, which would be source list. Okay, so upon doing this we are creating a copy exact copy of this source list to this destination list. If I print this out our destination list now is out okay so it's out System.out.println. Let me give message as copied list colon. And finally let me print out our destination list. Now if I run this program, you can clearly see our destination list containing the elements of our source list Java, Python, C plus, plus. Now this is the exact copy of our source list. So what does this collections dot copy method do? It copies all the elements from the source list into the destination list. And just like that, boom, we got the complete clone of our actual list source list. So this is perfect when you want to work on a backup copy without altering the original list. So if you observe this example a bit, let me close this output and here why we are creating this new array list. Then passing a restart as list instead of doing this. Why we want just create a destination list empty destination list and tries to copy these elements into it. If you do this now, if you run our program, you can be able to copy the actual list into this destination list. Let me show it to you. If I run this here, you can clearly see we will be getting an exception which would be index out of bound. Exception source does not fit in the destination, so you need to make sure that when you are copying one collection into another collection using this collection copy method, you have to make sure that if we open this copy method and here we can read its documentation, see, it copies all of the elements from one list into another. After the operation, the index of each copied element in the destination list will be identical to its index in the source list. So that's why size matters. The destination list size must be greater than or equal to the source list size. So here's the thing. It must be greater than or equal. Then only it works. It's not like that. It has to be the exact same size as our source list, but it can also have the extra size. Sorry if I confused you if I run this program now, this one will also work. C after C plus plus we have a empty element there. Let me add one more thing here. Something like this. And if I run this now, you will be seeing our extra elements also in the destination list. What if this extra element is in the middle of this? What happens then? Let me just remove this and let me just paste this here. So what happens now? Let me run this now. Here you can clearly see the middle element from list got replaced by this. But if you want the exact copy of the source list then your destination collection size would matter. It won't be greater than the actual list. Copying works. But now this array list. This destination list is not equal to this source list because of its size variation. Now we can say this destination is a clone of the source list. It is called clone only when all of this destination list properties matches to this source list, it only matches when its size is equal and both are having the similar elements. If I remove this now, these two are clones. If I run this program now, our copied list having Java, Python, C plus plus as well as our source list also having Java, Python, C plus plus. Now we can call this destination list as clone of our source list. That's why size matters. However, this copy method will work even if destination list size is bigger than the actual source list, but it can't be the lesser size than the actual source list. So if I do this, I will be getting an exception which is obviously index out of bound exception. Hope you got the clarity by now. How does this copy method works? Now let's move to our next topic which is filling. So what exactly is this? Imagine you got an empty list. Let me create that empty list. Now here we have it. And you want to fill it with the same element in collections dot fill method. We are supposed to pass our collection along with the element which you want to replace the whole list with. Let me show it to you. Collections dot fill in which you need to pass to first one would be my list, and second one would be the element which you want to replace it with. Simply put, fill it with. And here I want to fill our all my list with the Java element. Okay. Now at each element's place we will be seeing Java here. So to prove this to you, let me just simply print out a sort my list and let me run this program. And here you have it Java Java Java. At each element's place we will be having Java in it. So if you want to do it manually then you need to replace each element with Java Java Java. Right. Let's suppose we have some elements here. Let me just give some random elements. And if I run this even at this situation also we are just replacing each and every element with our Java filling element. So if you want to do it manually then you have to go and you have to call set method on this my list three times for these three elements like my list dot set. Then you need to pass this element and the element which you want to replace it with. You have to do this like three times. But if you use this fill method, which is present in collections, you can do it in a single step. So that's the whole use of it. Now you might be having a question when might we use these collections copying and filling. So here copying this collection is great for backing up data or working on different versions of a list. While filling. This collection is perfect for initializing values or prepping a list with placeholders, simply put. Super useful right? That's the main reason. Why should we use this copying and filling? Now you know how to efficiently copy and fill your collections in Java. These tricks will save you tons of time. Whether you are ripping data for processing or working on multiple versions of a list, then these two methods copy and fill. These two will help you explore your collections in a more effective way. So that's it for this video. Thank you for watching and I will be seeing you in the next one.