Hello and welcome back everyone! In this video, we'll be exploring the concept of searching in Java's collections. Whether we are looking for an element in a list or finding a specific value in an array. Java provides tools to search through data. Overall, in this video, we are going to focus on two types of searches, such as linear search and binary search, and will also analyze the efficiency of these algorithms. So first of all let's start with linear search. So linear search is the most basic form of searching where we go through. Let's suppose this is the collection we are having to be get searched upon. And in this collection we have elements 123, four. So if you perform linear search where we go through each element of a collection one by one until we found the target, let's suppose our target is four. So here we will traverse through each element one by one looking at the value we are getting. And finally, at the last position at index three we will get our search item. So this is how linear search works. It's a simple and intuitive, but it can be defined inefficient for a larger data sets. If we are having millions of elements in our collection, and if our search element would be at the end of our collection, in such case, we have to take the overload of going through each element. It is very difficult if we have the larger data set to perform linear search upon. So let's explore it through an example. For that, let me open our VS code and let me select our practice folder in module seven. And let's create a new file with the name Linear Search test dot Java. And here let me create a collection of list list of integer numbers equals to array start as list and let me pass numbers in it. Some random numbers and let me import list also. List. And here let us give target as our target as int target equals to four. And let us also create a Boolean variable. Boolean pond equals to. Let us by default keep it as false. And let me run through our collection in a loop for num colon numbers. If num equals to equals to target, then we would simply return our set pond equal to true. And after finding our element, we don't need to search for the already found element in our collection. So we just need to break it, break our loop from making it further. Iterate. And finally, let me just print out our pond result with number found. Then will be returning its status. True or false? Let me run this. So since we are having four in our collection, it will just return true. Here number found equal to true. Let's explore it. So here you can see number found equal to true. What if if I give 44. Since we do not have 44 in our collection it will just returns false. As simple as that. So this is what linear search is. Now let's break down the efficiency of this linear search. So here time complexity would be what if our target is at the starting position. So what if if 44 was present in our collection it is starting at the zeroth index. Then what type of time complexity we are having on our algorithm here. So in such case which would be best time complexity which is denoted by omega of. Here time complexity would be one, right? If it is at zeroth position then we will be having that element in a constant time. So that's why best time complexity would be omega of n 44 element present at the end, or whatever it doesn't found on our collection. In such case, we need to go through each element in our collection. Which would you o of n o time complexity. So for this algorithm, we will be having less time complexity of omega of one and worst time complexity of O of n. Now let's talk about binary search. Binary search is much more efficient, but can only be used on sorted collections. So here the idea is simple. Instead of searching through every element, we repeatedly divide the collection into half and compare the middle element with the target. Let's suppose our target is target equal to four again, and we will be having these elements in our collection. Let me add some random elements 528349 okay, so our binary search won't work on this random collection. It would be sorted collection. As mentioned here we can only perform binary search on sorted collection. So first of all, what we need to do is we need to sort this collection. So after sorting we will be having two, three, four, then five, eight and nine. Now we can perform binary search on this collection. So here the idea is simple. Instead of searching through every element like a linear search and finding element at particular index, here we repeatedly divide the collection in a half. So first of all, we will get the size of this collection and divide it by two. So the size of collection is what 123456 right. So if we do this six by two divided by two then what. The result we can get here. Three. Right. It will be three. So here we divide the collection into two parts. Up to three will be at the left part as left and right parts of our collection. We will going to check here our target element where it lies. So here we can check our target element with any of these either five, either four. So first of all, what we do is if target greater than whatever collection we are having here collection dot get of three the half of the size we are comparing. So here we are. Compare. This target is greater than this collection dot get element or not. So this collection dot get element. So here this would be searching at index three right. Which would be element five. So here target is four. So here target is not greater than the collection dot get element which would be five. So if block will get terminated so on else block we will be having we are going to check this part. This element is equal to equal to the current element or not current target or not. If this target is not greater than this collection dot get element of three means we won't see the target in this part of array, since it is the. Our target is the smallest than this element. Obviously we will be finding our target in the left part of our collection. So that is why we just ignore this part and we will only work on this half of part. So that is why in else we will. First going to do is we will check for the. If target is equal to equal to collection dot get of the half of the size minus one, which should be the three minus one. So here what we pass is size minus one, size minus one in this get method. So if it equal then we will just return true. Or the index of the element. We can do whatever we want. What about if this target is three. Then we can find at this part. Right? Then what we will do is. Then again we will divide this half of the array into two parts. So currently we are having three by two which will become one point something. So this collection will get divided into these two parts. So here again we will check if the target is greater than collection dot get of size of size would be the one right after point. values. Whatever we are having, we don't consider it. We will consider this position only since we are getting the size as one, so zero and one. We will be having three here so we can set target greater than or equal to. Here we will be combining two conditions here. So if it is greater than or equal to this position. So currently our target is three. So target is greater than or equal to collection target of one. Here our size is one right. So we will be getting three here. So three greater than or equal to three condition is true. So that is why we will just here we can set our status to true. Or we can just return the index of this position which would be one in Java binary collection we will be getting index of the element where we found that element. We will be getting that as a result in our Java binary search algorithm. Even if you don't found this element, let's suppose here we are searching for two. You can find that on this position, then you need to divide the entire array. You need to forget this. And here we can furthermore divide. So that's why we will check if this element is our target or not. A target equals to equals to two. Target equals to equals to collections dot get of zero. If it is equal then we'll just return the index zero. Or we can set it to true status. And if you can't find here also two it is. Let's suppose it is something else such as it is one. Here we are searching for two. If we can't find our element we can just simply put message as the element. Whichever you are searching for, not found on our collection. You can mention whatever you want. So this is how a binary search works. So since Java provides a binary collection algorithm so you don't need to worry about this all, you can just use a predefined method to do this type of searching. So that's the basic of Java's collection framework. You will be having a lot more helper methods like this. Instead of going through all this overhead, you can just simply use the predefined methods which are already available on our collection. Let's explore it through an example. So here, let me create a new file with the name Binarysearch test dot Java svn and let me close this terminal. Don't need it and let me close this explorer. Also let's zoom in a bit. And here first of all we need a collection and which would be sorted if your collection is not sorted. In such case you can sort that collection first. Then only you have to perform binary search on that collection. For that, let me create a list of integers. Let me give you the name as numbers equals. To restart as list, let me add elements in a sorted way to reduce the steps I'm doing it. If your collection is not sorted, then you can make use of Collections.sort method to sort these elements to ascending order. One. Two. Three. Four. Five. Okay. And let me give the target here int target equals to four. Since we know that our collection start binary search method will return an index. So to store that index let me create an index variable. Index int index equals to collections dot c binary search. And here we need to pass two values. One would be the collection. And second element would be the key which will be the target which you want to search for. First of all, here let me pass our list as numbers and our target as target. And that's it. If our target found on this numbers collection, it will just return that index to this index variable. And we can check it here. Here I'm defining a simple condition. If index greater than zero. If we can't find this target on our collection then this method will just returns a negative value minus one. So that's why here I'm setting condition as if index greater than zero, which means we found the element. So let me just simply print out message as. Or we can set it to zero equal to zero. Also since we are also having the index zero in our collection, we don't give zero here. Then this binary search returns zero. If the target found on at zeroth position then that would be message since we are having elements from zero index. So that's why we need to specify this index greater than or equal to zero. So that's why here I'm specifying it index greater than or equal to zero. In such case we found the number number found at index plus. Just simply print our index. What if we don't find index? In such case we just simply print out number not found. So let me run this. Since we are having four on our target, we will be getting the index as 0123. Right. Let me run this and verify if you are getting three or not. See? Number found at index three. What if if we give 40 or 45 here. Let me run this. Now you can probably see number not found. And let me see the index value also here. If you have not found the element. And index value is. Let me give colon and let me just print out our index. So if we can't find the element in our collection then we will be getting a negative value. See we got negative six here. So that's how our binary search method works in our Java collections framework. This method is a part of collections class. And let me open this methods implementation by simply clicking on it. Here we have reached to this position and here we can clearly see what is going on. So first we are getting the key and the list. And if you observe this binary search method we will be having two binary search here. And here we are having indexed binary search. So first of all we are setting storing low as zero. Since our elements starts from zero in our collection and high as list dot size minus one. So since elements start from zero so our last elements index would be the list dot size which will be in this example that would be five. Right. So we'll be having from 0 to 4 indexes. That's why here we are storing list dot size minus one. So high will be four in our example. And here they have running a while loop until low is less than or equal to high. What this means. So here this loop continues as long as low is less than or equal to the high, it ensures that search space is valid and then it just calculating mid value. So here the mid is calculated using bitwise right shift which is equivalent to the dividing by two okay Divide it by two. So to avoid this overflow potential issues, it is using this right shift operator which is equivalent to low plus high divided by two operation. And the value at the midpoint index will be retrieved midway. And the comparable here comparable of super ensures that the value can be compared to each key, and at this line, the comparative method is used to compare the midpoint value, which is mid val with the key. So here the result will be negative if the mid value less than the key. Let me just specify it as minus one minus one. Here it would be zero mid val is equal to key. And let me specify it like this. Mid val is equal to k. It will be zero and it will be positive if mid vowel greater than key. So that's why here if the cmp is less than zero, which means the key is in the upper half which means right side. So that's why this low is adjusted to mid plus one. Or if comparison is greater than zero. In such case the key is in the lower half which means left part. So that's why here it is set to high is set to mid minus one, so that we can only focus on the part of the array which our key is available in. And if the comparison cmp equal to equal to zero, which is probably nothing, but the key is found at the index mid. So that's why we are just returning mid which is nothing. But the final index will be getting in our this method called collections dot binary search method call. Got it. And what if we not found the key on our collection? If we don't find the target in our collection, then we are just setting this value to minus one. Here we are specifying low and we are adding plus one to the low, and then we are just specifying minus to it so that it can return a negative value if it can't find the target in the collection. So that's why we got the minus six as a result. When we run our code. So basically this algorithm efficiently narrows down the search space by half with each iteration making it much faster than a linear search, especially for the larger sorted lists. And this binary search time complexity would be O of log n. This is logarithmic time complexity, which means that even for larger collection, the search is very fast. Since we are not looping through every element in our collection, we are just dividing the entire collection into half. Then we are finding out the which part of this collection we are going to find that element. So if it is lower part, then we just ignore the upper part of our collection and then we can further calculate this, this part of collection instead of going through each element in this collection. So that's why the time complexity here is logarithmic. So to summarize the difference between these two search algorithms. Linear search works on any collection, whatever it might be, but has a time complexity of O of N since it need to go through each and every element in the collection, whereas binary search requires the collection to be sorted, but it has a time complexity of O of log n, making it much faster for a larger collection. In conclusion, both linear and binary search have their place in programming for small or unsorted collections. Linear search is sufficient. However, when working with larger data sets or sorted collections, binary search is much more efficient. That wraps up to our discussion on searching in collections. So that's it for this video. Thank you for watching and I will see you in the next one.