Hello everyone. Welcome to this video on Introduction to Streams. In this video, we'll be taking a closer look at one of the most powerful feature introduced in Java eight, which is the stream API. Streams have revolutionized how we work with collections by providing a functional, declarative approach to processing data. They allow us to perform complex operations like filtering, mapping, and reducing data in a clean and efficient way. So what exactly is a term stream means? A stream is not a data structure, but a sequence of elements that can be processed in parallel or sequentially. Note these two terms parallel and sequential. It provides a way to work with collections in a functional programming style. So unlike traditional collections which store data in a stream, does not modify the original collection, instead, it processes the data in a pipeline of operations, transforming it without affecting the underlying data structure. So that's what stream means. And stream allows us to express complex data processing in a declarative way. What exactly this term means declarative, which means you specify what you want to do rather than how to do it. You just specify what you want to do rather than how to do it. This leads to more readable and concise code. And why should we use streams? Why should we bother to use this stream? So the introduction of stream API in Java eight was a game changer for working with collections. Here are some of the key reasons why streams are so powerful, such as they provide concise and readable code. Streams allow you to perform complex operations with fewer lines of code. Note this point it allows you to perform complex operations with fewer lines of code improving readability, and another reason is functional programming. The stream API encourages a functional programming style, allowing you to pass behavior like lambdas to methods and parallel processing. One of the main reasons among them streams can be processed in parallel. Leveraging multi-core processors to boost performance. And one more key reason of using stream API is non modifying operations performed on streams. Do not modify the original collection, maintaining the data integrity. So that's the reason why should we use streams? Now let's take a look at how stream works. The stream pipeline works. So basically the stream pipeline consists of three parts such as source intermediate operations and terminal operations. So here source means the collection or data source from which the stream is created. For example, list set map can serve as a source for a string. And here we have intermediate operations. What exactly mean by this is these are the operations that transform the stream, such as filter map. These are the intermediate operations. And these operations are lazy meaning they do not execute until terminal operations are called. Until these operations are called, they don't invoke. So what exactly terminal operation here is the final operation in the pipeline, which triggers the execution of entire string terminal operations include for each collect, reduce, etc. methods. Now let's take a look at basic example where we use stream to filter and sort a list of names. To explore that example, let me open our VS code and let me select a specific folder here in module five, let me create a folder with the name practicals. Let me select this folder. And here let me create a file with the name stream demo dot Java. First of all, let me import these two things. Here is a list. And here let me do the necessary things. Public class. Stream demo. Public static. Void main. Let me create a list here. List of strings with the name names equals to arrays. Dot as list. Let me pass some elements into it, such as Alice. Bob. Charlie. David. Here where it's giving error. Okay. We need to use array start as list. Okay. Now let's use a stream to filter and sort names using a string. Let me just keep this command to filter and sort names okay. Here let me again create a another, uh, list so that we can filter these names list and perform stream operations. Then we store the result in another list. For that let us create another list. String filtered and sorted. List sorted names. Okay. Equals to name start. Let me call our stream method on it stream. Then here upon it let's call filter method in which I want to pass a lambda expression name as lambda name. Dot start starts with capital A or name dot starts with. Let me keep it like this instead for a better view. Name dot starts with B, so this is the filter dot sorted with. Is this unsorted? Let's call our sorted dot collector start to list. So here we also need to import this java.util dot stream dot collectors. We have given an extra parenthesis there. No. It's fine. Let me keep it like this. So it is more readable. So basically what I did is in this example I have created a stream for list of names. We then I then applied two intermediate operations such as filter unsorted. So what this filter method actually do is this filter method filters the names, keeping only those that start with A or B. Whatever elements we have here, whichever starting with capital A or capital B, it just only keeps those elements. And this sorted method sorts the filter, the names, whatever elements which are filtered, it just sort those elements in an alphabetical order. Then finally we use the collect method, which is a terminal operation, to gather the results into a new list. So here the stream API handled it for us in a clear and readable way. Before going to run our program, let me just print out our a filtered unsorted names sort filtered and sorted names column. Let me just print this out here. If I run this program now. Now, you can probably see the filtered and sorted elements as Alice and Bob. So what if if Bob comes first? Bob comes first and Alice comes next? Since we are doing the sorted operation here. Wait a minute. Why? It's giving error. Okay, now it's gone. Since we are doing sorted operation here, we will again get the same results. Alice. Bob, in alphabetical order, see Alice. Bob. What if. If we don't do this? Let me just remove it. And if I run this program now, again, probably seeing the elements as Bob and Alice since we haven't sorted our resulted collection. Now let's take a look on stream types sequential versus parallel streams. So streams in Java can be processed into two modes, such as sequential and parallel. Here, sequential stream means in a sequential stream, the operations are processed in a single thread, one element at a time. Got it. That's why its name is set to sequential. And this is the default behavior for streams. Whereas parallel streams in parallel stream the operations are processed using multiple threads, allowing for better performance. On multi-core processors, you can convert a sequential stream into a parallel stream by calling parallel method on the stream. So by calling this method, you can convert a sequential stream into a parallel stream, making it accessible by making it processed by multiple threads. We will cover parallel streams in more detail in an upcoming video, but for now, just remember that parallel streams are great for improving performance when working with large data sets. Now let's take a look on stream Operations, such as intermediate versus terminal. As we mentioned earlier, stream operate using a pipeline of intermediate and terminal operations. So here intermediate operations include these methods. Filter map distinct. Unsorted. We have explored these two methods in our previous example. So intermediate operations are lazy mean. They do not perform any computation until a terminal operation is invoked. So the terminal operation here include for each collect, reduce and count. In our previous example we have used this collect so the terminal operations are eager meaning they execute the entire stream pipeline immediately when they called. And one of the key benefits of stream is that they are non-destructive. This means that the original collection remains unchanged after applying the stream operations Instead, stream, process the data and return a new result, leaving the original collection intact. It just reads the original collection. That's it. I don't modify the original collection keeping it intact. Now let's take a look at another example where I play stream operation without modifying it. Let me just use some other operations here instead of color filter. Let me do the necessary things here. So here I have imported these two arrays and list. Now let me create a list. List of strings names equals to array. Start as list. Alice. Bob. Charlie. Okay. Now let me call stream method on our names directly without storing it in any other separate list. Okay, let me just do it. Names. Dot stream dot map of string to uppercase. So here I'm just converting whatever the elements in our collection. I'm just converting them to uppercase and dot. Let me call for each method on it. For each system dot out dot. Let me just do this. Until then, let me iterate over each and every element in our collection. Let me make it more readable. Okay. Now it's more readable. Now let me just print out our names. Original list. Now, what do you think? Does this stream operation modified the actual names collection or it remains same? Does any changes happen to our names collection here? Names list. Let's verify it. If I run this program now, let me clean our terminal. Let me run it again. Now you can see we got Alice. Bob, Charlie. Because we are printing that out here. Because of this, for each loop, we are just seeing this Alice, Bob, Charlie in uppercase. But here we have this original list printed as Alice, Bob, Charlie. So basically in this example, we convert each name to uppercase using map and printed the results using for each method. However, the original list of names here you can see remains unchanged, demonstrating that non-destructive nature of strings. Which proves our point right? Our stream API is non-destructive, so in summary, streams provide a powerful and flexible way to process data in Java. They allow us to write concise, functional and non-destructive code that's easy to read and maintain. Whether you are filtering, sorting, or mapping data streams enable you to work with collections in a more intuitive and declarative way. So that's it for this video. In the next video, I will be dive deeper into intermediate and terminal operations and explore how to build more complex stream pipelines. So see you there.