Hello everyone. Welcome back to our Java Collections Framework course. Today in this video we will explore the first method introduced in Java eight. This method is an enhancement for iterating through collections in a more concise and readable way using lambda expressions. By the end of this video, you will know how to use for each to streamline your code when working with collections. So let's understand what exactly is this for each method. So the for each method here is a default method which you can found it on iterable interface. Why I'm saying it as default method because if you open our iterable collections implementation, let me open its implementation. So if you open this implementation now here you can clearly see we are having for each implementation in this interface. And it is set to default. Its access modifier is set to default. So that's why I'm saying it as a default method. So basically this method allows us to perform an action on each element in the collection. So here the action typically defined using lambda expression, making the code much cleaner. Let's explore it through an example. So here let me create a traditional for loop which are mostly used on collections which is nothing but fur. So before going to create our loop, let me first create a list of string. Let me keep its object name as names, since we are going to store names in this list, and I'm going to use arrays dot as list to manually add elements into our list. So here we need to import these two arrays and list to get rid of these errors. Let me import them. Import Java.util dot list and import Java.util dot arrays okay here it is. Now we can simply pass our elements into it. Here I'm adding Alice, and then Bob and Charlie. And then let me add Diana. Okay. I have added these four elements into our list. Now let's explore a traditional for loop which would be string name column names. So here we are clearly iterating over each element in our names collection. Sorry for the spelling mistake names. It's okay now and to print each element into it. Here, let me pass a sort and in our sort let me pass our name. So here what. Basically in this loop doing is just taking one element per iteration from this name, starting from zeroth element and just taking one element per iteration and printing them out. So this is what a traditional for each traditional for loop. Okay. Now let's explore for each how it look like. So let me keep comment as using for h with lambda. So as I have already said in our for each, we'll be having a lambda on the action, right? So instead of passing names in our for loop, here we are calling for each method on or collection. So here first we need to give names. Then we need to call our for each method on it. See this one. And in this for each we need to pass a consumer which would be asking for an action see consumer action object. So for this action we can include our lambda expression and such as name arrow. And the action would be System.out.println. So here I'm just printing whatever element we got in this name. We are just simply printing out in this action. So first of all let me comment this out. Let's explore this one first. And it's saying build failed. I don't know why it's saying that. Wait a minute. We have some errors in our code. Let me verify it. Okay. I haven't mentioned semicolon here, so that's why I got error. Now let me rerun this program. Now you can probably see we are getting elements as Alice, Bob. Charlie, Dina. As expected in a traditional for loop. Now let me comment this thing out. And let me uncomment our first method. Okay. Now let me try to run this program. Now you can probably see we are getting the same result. Now you can clearly see that how much simpler it is to use our for each loop. In this way, iterate elements using for each loop rather than just using the traditional for loop. So here we have just a reduced number of lines and code readability also enhanced. So basically in this example the for each method replaces the traditional for loop. And we pass a lambda expression to for each which takes each element and performs an action, in this case printing the elements to the output. So now you might be having a question if you still have a question on doubt. Why should we use this for each method? So here are some key points you should remember which are cleaner syntax. One of the main reason why we should use for each. So it removes the need for boilerplate code like defining an index or using an iterator like that. And it also gives readability. This one also is one of the main reasons why should we use for each instead of a normal traditional loop? So here readability means using lambda expressions with for each makes the code more readable, especially for simple operations such as printing elements one by one. We can wrap up whole code in one line instead of creating this whole mess. And one more reason why should we use is. It promotes functional programming style which is more concise and expressive. And let's also explore one more example. Now this time let me create a list of integer. And let me give our object's name as numbers equals to array. Start as list. Let me pass some numbers into it one, two, three, four, five, six, seven and eight. Okay, now let's use this method to double the number and print them one by one. For that, we can call for each method on our numbers for each. And let me give a lambda expression here on System.out.println as here. Let me pass this in into two. If I run this program now, you can probably see each number got doubled and printed out. So here if you just want to print the elements without doing any action, then instead of this lambda expression you should use, you should print elements like this also by using System.out colon colon println. So if we use this here also, you can probably see the elements are printed out one by one as it is. So instead of ln here if you use print, then these elements are printed out side by side instead of lane by lane. If I run this program now, now you can probably see these all elements are printed out in a line balanced manner. So if you just want to print elements as it is in our collection, then you might use this type of declaration in our for each System.out colon colon print. And if you want to specify a certain condition and if you want to modify the output, you don't want to print as it is from the collection. In such case, you might use the lambda expression like this. So here it is nothing but a temporary element name of the elements present in our collection. On every iteration, one element at a time is allotted to this end, and we are just simply performing our action on that end to manipulate our collections. Let me do a sort here. So here on this end, we can perform whatever operation we want. If you want to do n minus n, you can do. And if you have any specific expression in your brain, you can apply it as well. So if I run this program now after performing this action on each element, we got the zeros as result. So here I have randomly picked up one expression. So that's why we are getting it. Let me just remove this part and let me do n into n. And here n plus one. So here whatever action you want to perform you can perform. See. So this is basically lambda expression. But this way of printing our elements in for each we call it as method reference. Keep that in mind. It's called method reference which is a shorthand for lambda expression. So basically this makes our code even more concise and readable. Now let's use a stream to combine this for each loop on our stream. Okay, let's try to do that. For that, let me create a stream here. So as we have already created a numbers, let's just perform our stream operation on our numbers collection. So for that for better understanding, let me just remove this part on here. Let me also remove this. Okay. Now let's perform stream operation on our numbers. Number start stream dot filter to filter out elements. So here I just want to do n mod two. If n mod two equal to equal to zero which is the logic for finding even numbers. So then only we are doing the for each on each element we are just printing out using our lambda. So let me just pass in here and that's it. So this is the way to use our for each loop with streams. So basically here we have used our stream to filter out even numbers and then apply for each method to print them. So this approach combines the power of streams with simplicity of for each. So if I run this program now so here you can clearly see we have filtered out even numbers from our numbers collection. And then we just simply printed using for each loop. One thing to note is that the for each method cannot handle concurrent modifications to the collection. If you try to add or remove elements while iterating, it will throw a concurrent modification exception. Let me just prove this to you. So here we are having names collection, right? Let me call for each method on this names collection names for each. And here let me just pass name lambda expression. And let me give the condition as if names. Sorry name not equals Charlie. What I want to do here is I just want to remove that name from our collection in Strathmore. Charlie. Okay. I just want to remove that particular object from our list. So if I try to do this, if I run this program now, it either gives us concurrentmodificationexception or an unsupported operation exception. Let me see it. So after running it, we got unsupported operation exception, which proves our point. We can't remove while iterating in for each. Not only remove, we can't add also. So always be cautious when modifying the collection you are iterating over using for each. For such operations, consider using iterators or strings with specific terminal operations. To sum up, the for each method offers a more concise and readable way to iterate over collections. It can be paired with strings using with method references and allows you to write cleaner code. However, remember to avoid concurrent modification issues while using it.