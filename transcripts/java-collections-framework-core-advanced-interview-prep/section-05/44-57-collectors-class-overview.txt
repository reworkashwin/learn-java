Hello everyone. Welcome back to our Java Collections Framework course. Today in this video we are going to explore and look at a overview of our collectors class in Java. So in Java the collectors class is a utility in Java.util dot stream package, which provides a set of predefined collectors that can be used to transform or process data in streams. Collectors are typically used in conjunction with collect terminal method of stream class to accumulate elements from a stream into a collection, such as to list, to set or to map, or to perform aggregate operations like grouping, partitioning and summarizing. Let's explore it through an example. Let me create a new file here with the name Collectors Overview dot Java, and let me import our collectors class and let me do the necessary things here PS vm. And here I have created a sample list. List of names. If you open this collectors class implementation. Now here you can see we have a lot of methods in it. Let's look at a public ones. So basically we will be having a two list method. Two list. See this returns a collector that accumulates the input elements into a new list which we have already explored. Let's explore it once again. So to use this two list method, we should call a terminal operation on our stream. We have to store the result in a separate list object. For that, let me create a list object for the result list. String res list equals to name. Start stream. And now let me call collect method on it. And we need to pass a collector object in it. For that let me call collector start to list. So whatever elements we are getting in our stream, we are just converting them to a list and then we are storing into our result list. Now if I print our result list this out collected list column, let me concatenate with our actual list which is result list. And let me run this program now to see the result. And here you got it. We have successfully collected the collection the list of names into our result list. What if if this is a set instead of list? What if if this collection names collection is a set? And let me also import this set here. Okay, now let me add some elements into it. Name start. Add list. Name. Start. Add Bob. Name. Start. Add Charlie. Name. Start. Add David okay now also this names collection will be converted to list and get stored in our actual list since we are converting it to two list. See you got it! So basically here we are converted this set collection to a list using our stream API. Combined with this terminal method collect instead of converting it to list. If you want to store it in a set, then we are going to use two set method to collect elements into a set which will remove duplicates. So currently we are having a list, right? Let me add a list again into our ideas. Let's start add list. So here I'm duplicating these elements. And let me also add Charlie into our areas list Charlie. Right okay. Now let me create a set object to store the result set string arrays. Set equals to areas. List dot. Stream. Dot collect. Collectors dot two set. And now let me print this out. Collector set. Colon res set. Okay. Now, if I run this program now, you can probably see we are just removing the duplicates from our collected list. See in our collector list we are having, which is nothing but a result list. We are having a list twice and Charlie also twice. After converting it to two set and storing it in an array set. Now you can see we have removed those duplicates and stored this collection into a set collected set called Bob list. Charlie, David. And in our collectors we are having a joining method. Let's explore it. J y n I n g. Where is it? Yeah. Here we have this. So it basically returns a collector that concatenates the input elements into a string. It basically joins strings together with a delimiter. Let's explore it through an example. So we are having a collector start joining method which is static. That's why we can use this. These methods joining are two list or two set with the class name. See static mentioned here. So if these methods are not static then we have to use a object to call them object of collector to call them. Since these methods are static, that's the only reason we are using calling these methods like this using class names. So here to store results, join the names. Let me create a string with the name joined names equals to. Now let's perform string method on it. String method on our names names. Dot. Stream. Dot collect. Collectors. Dot. Joining. Joining in which. Let me just pass this comma and space. This method joining is undefined. While it's showing that okay collectors start joining. So here I have added this delimiter. So after each name it will just add this comma and a space. Let me just print out our result. Assert. Join the names colon. Let me call our join names variable here. Now if I run this program you can probably see the result as joined names. Colon. Bob. Comma. Alice. Comma. Charlie. Comma. David. So between each element it just added this delimiter. So what if if you want to include ampersand between them. So if you specify and present then you would probably get an unpleasant in this format. See Bob. Ampersand Eliza present Brother Charlie. And if you want a space between them, then you can give space to clearly see and hear. You got it. So basically here you can specify whatever you want as telemeter. See. As of now, let me keep comma here, which makes some sense. Now let's explore some more methods present in our collectors class. Now our next method would be grouping by grouping by. Let me search for our grouping by. Where is it? Okay. Here we have this grouping by which basically groups elements by a classifier function. For example grouping by length of names. Let's explore it through an example. So we can hear you is collector. Start grouping by method to group elements by a classifier function to store result. Let me create a map of integer and string, which is a comma string. Or let me just, uh, create a list of string here instead of just a string. List of string. And let me give you the. Let me also import our map here. Okay. Now let me give our object name as grouped by map, grouped by length equals to names dot stream, dot collect collector. Start grouping by and as per classifier function. Let me pass string, colon, colon and length. Okay let's put semicolon there. Now let's just simply print our group by length object with a message grouped by name length. Then concatenating it with grouped by length object length collection. Okay, if I run this now, now you can probably see the result as. So first of all we got three and we are having Bob as a list in it since we are having only one element with length three. That's why we are having this as a complete list in this map and as five we are having two elements with length five. That's why we are getting this. Alice and David combined into a one list with length five, and we are having only one element with the length seven. What if I add one more element with length seven instead of Charlie Charles? Let me add. Now if I run this program, you can probably see we are going to have two elements in our list with seven as their elements length. Hope you got the clarity by now. What does this grouping by method would do? So here we are just grouping the elements by their length. And here is a task for you. Go and search out for what are the other things we can do with this group by method. One way is we can Our group elements by their length. Instead of this length. Go and search out for what are the other things we can pass in this group by. Okay. That's the task for you. Hope you will search out for the other features provided by Group by. Now let's move to the next method which is partitioning by. So here we are having this method in our collector class, which probably returns a collector which partitions the input element according to a predicate. And it produces values in each partition according to another collector and organize them into a map of Boolean comma, a specific type whose values are the result of the downstream reduction. So basically it just partition the elements into two groups. Let's explore it and try to partition elements into two groups by whether the name starts with a. For that let me create a map of Boolean. And here, whatever type you want to give here, I want to give the collection type as list of string. And let me give our object name as partition by starts with a okay equals to name start, stream dot collect and let us call collectors dot partitioning by name lambda expression name dot starts with capital n and that's it. So if I print our result collection now, I sort with a message partitioned by names starting with a colon. Then let me just pass our partition by. Starts with a object collection. Okay, now let me run this program. Now here you can clearly see the elements starting with a are separated into a different collection. And we got true as a Boolean result. And whatever elements which are starting with other characters like C, B, C, d are grouped into a separate collection. And for this collection we will be having boolean result as false. Got it. So if we are having one more element which starts with a, then we'll be finding it in this collection true collection. So we are basically getting two collections in it. One is false collection and one is true collection. According to the condition here, whatever elements which matches this condition will be stored into a true collection, and all other elements will be stored into a false collection if they don't match the condition. Here, let me add one more element. So here I have added this atom. Now if I run this program now again, Let me run this. Run our program. So here you can see Adam and Alice, which are stored into a true collection. What if if you do this, if you want to group elements, partition elements by whatever elements which are starting with C, then you will probably see Charlie and Charles in true collection. Here you can probably see the partition by the name starting with C as false as all other elements Adam, Bob, Alice, David, and True as Charles and Charlie. Hope you got the clarity by now. What does this partitioning by method would do? Now let's explore next method which would be counting. Let me search for our counting method in our collectors class. And here we have it starting counting, which should probably return a collector accepting elements of type T that counts the number of input elements. If no elements are present, then it just gives the result as zero. So it basically count the number of elements in the stream. Let's explore it. Our sixth method would be collector start counting, which counts the number of elements in the stream. For that it will return a long. So if you see here we are having a long type. So let's store the result in a long name count equals to name start stream. Let me call collect method on this collect collector start counting. And here we don't need to pass anything in accounting. It just returns the number of elements. As a result let's just print out count of names as name count. Now let me run this program. Now here you can see we are having six names in it. That's why this count is set to six. Now let's explore another static method of our collectors which would be happening. So this static method would probably returns accepts a collector. Here we need to pass a collector accepting elements of type U to one. Accepting elements of type T by applying a mapping function to each input element before accumulation. So what exactly mean by that. So this method basically apply a mapping function and then collect convert names to uppercase, lowercase etc. which we have already explored in our previous videos. Let me just, uh, show this to you again. Our seventh method is collector start mapping, which apply a mapping function and then collect convert names into. Let's do uppercase. Now to store the list, let me create a list object. List of string per case name is equals to name. Start. Stream dot Collect. Let us pass collectors dot mapping. And here we need to pass two things. One is the action which would be a string to uppercase C. We are having a different ones to carry it to lowercase to lowercase local to string to uppercase, and to uppercase local. Let us use this to uppercase as of now. And let me just, uh, set it like this. Now let me just remove this terminal for a better view. And here we need to pass a collector. Like if you want to convert the result into a list, then you just need to pass collector star to list. If you want to set, then you just need to pass collector star to set. Since we are storing it in a list, let us use collector start to list method. Okay, so this one is after converting every element to uppercase and it will just convert that result into a list. Then we are storing that result into this uppercase names. Let us just print our uppercase names S out and put it to uppercase colon uppercase names. Now if I run this program, you can probably see the result as all the elements are converted to uppercase and stored into a list. Adam, Charles. Bob. Alice. Charlie. David. All are converted to uppercase and stored in uppercase names. So that's what our mapping method would do. And if you want all these elements in a lowercase then you should pass two lowercase there. Let us also verify it. Let me change this to lowercase lowercase names. And here let me just let us just pass instead of name start stream. Let us pass uppercase name start stream here two lowercase. And that's it. Set. Now here. Map it to lower case. Let us pass lower case object here. Now if I run this you can clearly see all these elements are converted again to a lower case and stored in a lower case names collection which is of type list. Now let us move to the another method which we can use with collectors. So next method would be something in which probably returns a collector that produces the sum of a integer valued function applied to the input elements. If no elements are present, then it just returns result as zero. So basically it just sums the length of all the names. If we call summing int on our names collection. There are a lot you can do with this method. Let us just explore an example on remaining things. I will leave up to you. Go and search for them and learn them by yourself, then only you will get to know what exactly how this method working as a part of this video, let me just show you an example how to use it. Okay. And we are going to explore an example which sums the length of all the names using this summoning int method to store a result. Let me just give int total length total length equal to name start stream, then dot collect and let us pass collectors dot summing int and let me give a string colon colon length. And let me just print out our length total length of our collection. By summing each element's length with other elements, we will get total sum as. Let me give a message also here. Sum of name lengths colon total length. Let me run this program now. and here you can probably see some of name lengths has 31 since we are having. If you add all of these elements length, you will be having 31 characters. See one two, three. So here we have added them with their length. Right? Three plus four, seven plus five into 210. Seven plus ten would be 1717 plus seven into two, 1417 plus 14 which would be 31. Now let us explore another method available on our collectors which would be max by. So here we are having this method which will probably return, say collector that produces the maximal element according to a given comparator, which is a optional method in our collectors class. By using this method, now, we are going to find the longest name in our names collection, since it is an optional. Let us store it in a optional collection. Why? Because the result is an optional here. This method's result would be optional because this max by method might return nothing if the stream is empty. So we use optional dot if present to print the longest name only if it is present. So that's why here I'm storing it in optional. Let us go our object name as longest name. Longest name equals to names dot stream dot collect collectors dot max by. Let us pass comparator dot comparing method. Comparing int and let us pass string colon colon length. Since we are focusing on its length, let me remove this tab spaces for better way. Okay, now let me just print out our longest name S out. Longest name. Colon. Longest name. Let me run this program now. Now we are having longest name as optional of Charles, but we are having Charlie also in it, right? Why? We are getting Charles as a result. It because here the Charlie is not considered as the longest name, even though it has the same length as Charles is. T2 collectors dot Max Pi method works in Java. In Java streams, Max Pi finds the maximum element based on the comparator you provide, but it only returns one element even if multiple elements have the same maximum value. In this case, since both Charles and Charlie both having the same length seven characters, only one of them will be returned as the longest name, depending on their order in the stream, and here the stream order would be stream process. In our list we are having Adam. Charles. Bob. Alice. Charlie. David. Like this. In this order where Charles appears before Charlie C. Charles B appears before Charlie. So when collectors start, Max B encounters Charles as the first name with the maximum length of seven. It considers that as the current maximum, even though Charlie also has the length of seven max, which keeps Charles because it appeared first in this stream. So here comparator provided a comparator dot comparing int in which we have passed a string colon colon length. It is based only on the length of the strings. Since Charles and Charlie have the same length, the comparator does not differentiate between them. As a result, whichever name comes first with the maximum length is returned by the max way. So that's the basic about Max by now. Let's move on and explore one more method which is available on our collectors class, which is two map. So it basically returns a collector that accumulates elements into a map whose keys and values are the result of applying the provided mapping function to the input elements. So basically by using this method, we are going to collect elements into a map name as key and the length as value. Okay, so here I have created this map of string comma integer and I have set it our objects name as name length map and name start string. Let me call collect method here dot collect and let us pass collectors dot two map. First of all let us pass function dot identify not this function dot identify okay. And as the next argument, let us pass string colon colon length. Since we need to store links in our map. Okay. Now, as the third element, let us pass. Old value. Comma. New value. Old value. Comma. New value. And let me give a lambda expression on it. Old value. Let me return old value. So here this old value and new value come into play when there is collision in the case while collecting the elements into a map. In other words, when two elements in this stream have the same key, we need to decide which value should be used in the resulting map. This is handled by providing a merge function in the collector start to map method. So here we are just merging the duplicate keys old value and new value. We are just returning the old value since the both values having the same key, and we don't have duplicate keys in our map collection. That's why we are doing this. Okay, hope you got the clarity by now. Now let us just print our name length map sort collected to map name, then length. Okay, let's skip column then space. Now here. Let me call our name length object. Okay. Now if I run this program here, you can clearly see the name as key and the length as value. Since Adam has four characters. Charles has seven characters, Bob has three characters, and Alice has five characters, and Charlie has seven, and David has five. So this is the basic about collector start class. There is a lot more. If you can explore this class, we're gonna explore even more complex examples while exploring our course in Java Collections framework. So that's it for this video guys. Thank you for watching and I will see you in the next one.