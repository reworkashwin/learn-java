Hello everyone. Welcome back to Java Collections Framework course. Today in this video, we'll be diving deeper into how streams work by exploring intermediate and terminal operations. These are the core building blocks for working with streams and allow you to create powerful flexible pipelines. Process data in collections. So let's re-explore what exactly are these intermediate and terminal operations in Java? A stream in Java is processed in two stages intermediate operations and terminal operations. Each has specific role in how streams process data. Let's start with intermediate operations. These are operations that transform a stream into another stream. For example, you might want to filter certain elements and map one type of data to another, or sort the elements in the stream. In those types of situations, you use intermediate Immediate operations. However, intermediate operations are lazy. This means that they don't actually do anything until a terminal operation is invoked. They simply build up a description of what needs to happen, but nothing is executed it until the terminal operations are invoked. They don't do anything. Let's explore some common intermediate operations which includes filter map sorted. Distinct limit. In our previous videos example, we have already explored these three things. Filter map sorted. So basically the filter method filters the elements of the stream based on a condition, as we have filtered elements based on the condition which starts with A or B, right. We have a filtered elements, whatever. Starting with these letters will get stored in a new list. And on our next example we have used map which transforms each element of stream into another form, like to uppercase to lowercase, etc. and we have used sorted also, which sorts the elements of the string by default to their natural order. Along with these three, we will also have these two distinct and limit. So distinct here removes duplicate elements from the stream, whereas limit limits the number of elements in the stream. Let's explore it through an example. For that. Let me open our VS code. Let me select a new folder for this, and let me create a new file for our intermediate operations demo dot Java. And here also let me import arrays, lists and characters Java.util.arrays Java.util.list and Java.util dot stream dot characters. So here I am including this characters from stream because while collecting while we have collect operation also right. Which is a terminal operation and to store the processed elements to a specific type of collection, we need to pass the tool static method which is results in this collector. If you want to process it to a list collection, then you might be using two list method, which is results in this collectors method. And if you want to process it to a set, then you should be using two set method which also results in this collector method. So that's why I'm including collector here to store results in a different collection. And here let us call public static void main SVM. Let me create a list here which is of string array uh names equals to array start as list. Let me pass some list here. Alice. Bob. Charlie. Then, David, let me also keep one more Bob. Basically, I have duplicated the elements here. Now let's use intermediate operations and to store the results, let me also create one more list of string again, and let me give our variable name as result equals to names dot. Let us call stream api on it, then dot instead of keeping dot. Here let me just do this dot filter on filter. Let's give condition as uh let me pass this lambda name. Name dot starts with b dot map to string to uppercase. So whatever elements starting with b we are converting them to uppercase. Let me call our string again string colon colon. If you give two then you can see lot of methods here. We can convert it to two carrier to lowercase to lowercase local to string to uppercase to uppercase local. So here we are using two uppercase okay to uppercase. And let me call distinct method on it unsorted also. And then collect and let me pass collectors dot two list. See when we press dot here we have two list two set a lot of method here to map to concurrent map to unmodifiable list. See we have lot of options. Currently we are using this two list. Let me close this parenthesis and let me keep semicolon here. So that's it. Now let's print out our result Processador me uh, keep it in double quotes process ID names colon plus result. Let me just print out result. So if I run this program now. So basically let's understand what's going in it before running it. In this example we start with the stream of names. And then we have called this filter method in which we are going to keep only the names which starts with b. So currently we have two bobs. And then we are using this map to convert the remaining names to uppercase. And then we are using this distinct method to remove the duplicates from the stream. Then we are using sorted to sort elements alphabetically. And that's it. This color is a terminal operation will be which is used to convert our result into two list collection and which makes easier to store that result in our list result. So this operations define the pipeline, but nothing happens until terminal operation is invoked which is this collect. So if I run this program now now you can probably see Bob, which is turned all into capital since we are having two duplicate bobs. One got removed because of this distinction. Intermediate operation. Instead of only keeping the elements which are starting with B as Bob. Let me also include some more elements which are starting with B, such as let me give some random names, Baha, ram, bob the boat, piano, etc. I just randomly picked these names from internet and now if I run this program, I can probably see the process of the names as see. All the elements which are starting with B are converted to uppercase and these all are sorted. So if you see bob is added first then these elements are added after Bob. But on our process the results Bobby said last since its second character having O, which is greater than A. That's why it is at the end position, which is something it showed by this sorted intermediate operation. Now let's move on to the terminal operations. So these are the operations that actually trigger the execution of this stream pipeline and produce a result. Once a terminal operation is called, the entire pipeline of intermediate operations is executed after calling the terminal operation. Some of the common terminal operations include for each collect, reduce, count, find first. In our previous examples, we have already explored these two for each and collect. So here for each basically performs an action for each element in the stream, whereas collect collects the elements of the stream into a collection or other data structure in which we are passing a collector method, right? Such as collectors dot two list to convert the result into list and collectors dot two set to convert the result into set. And then we are having reduce method, which basically reduces the elements of the stream to a single result. And then we are having count which counts the number of elements in the stream. And finally we are having find first method which is used to find the first element in the stream. So unlike intermediate operations, terminal operations are eager. This means they execute the entire pipeline and produce result. Whether it is a collection of elements, a single value, or even just performing an action like printing the elements. Let's also explore it through an example. Let me create a new file for this terminal operations demo dot show. Let me copy paste some of the things from our previous example. This thing, we paste it right here. Okay. Now this part also let me just copy paste it in PS VM okay. And here let me just remove this thing distinct unsorted. Let me keep distinct. And let me keep remove this thing also. I just want to let us keep it. It's okay. And instead of collect let us keep dot count here. And this is giving us some error words. Giving error distinct. Sorted. Okay. I forgot one thing here. It returns count rate which is a. It might be int or long like that. Let me keep it as int here. And instead of result let me keep count. Which makes sense now. Okay. This count returning the long. That's why we need to keep it long type here. Now our error gone. So instead of printing now, let me give processor to count as message. And let me just give count here. So if I run this program now, now you can probably see our output as processor to count five since we are having five elements. Right. So this is our previous files output. We are having five elements in it. So we are just counting those processed elements and storing it in a count variable to print it out in our program. As simple as that. Let's also explore for each once more for better understanding. So instead of storing this in a variable like we did before, let me just here I want to filter it with this condition. And then I want to map this to uppercase. Then I just want to remove duplicates. Then I want elements to be sorted. Then instead of count, I just want to print out those elements here. Using for each. For each. Here I can use a lambda expression name system dot out dot print ln processor the names column. If you want to give message then I should be using this name. And here we don't need to include this anymore okay. So if I run this now see processor the name here I should be keeping name for better logic explanation. So now see processor. Rename this 1115 times. We got this and we just want to print the names. We don't want this message. In such case you might use this thing instead. System dot out colon colon print ln okay now if I run this you will be getting all the result printed one by one. See? So that's how our fridge works. And we are having reduce also. Right. Which reduces the elements to the stream to a single result. What exactly mean by that. So on the same operation instead of for each let me use reduce. And here let me pass empty string then partial string comma element and lambda expression partial string plus. Don't worry if you didn't getting it what I'm doing here. Just bear for a moment, okay? And let's store this in a result variable of string type string result equals to name start stream, then filter, then map, then distinct, then sorted. Then we are reducing. So in this example the reduce method is used to concatenate all the strings. All the names that start with b into a single string Separated by spaces. So if you want space, then you should give the space. Here the initial value is an empty string. So that is why we are keeping these two double quotes with without any space there. And the accumulator function concatenates each element to a to the partial result. Now let's just print out our result now given wrong spelling. Here, let me correct it. Result okay. Result. Now it's fine if I run this program now before going to run, let me just clear our terminal. Now let's try to run it. Okay. Now you can see all these are strings. All the output is got converted to single element. And if you don't want space there then you can just remove that space by removing here. So if I run this now I can probably see all the elements are concatenated without space. What if if we keep something here what happens then? So if I run this now, you would be seeing that question mark as a initial character here. So it is nothing but the initial value empty string. Then we are doing this operation on it. We are just concatenating everything into it. So here one of the key feature of streams is laziness. Intermediate operation like filter map are lazy, which means they only execute when terminal operation is called. This allows us to stream to be processed efficiently, avoiding unnecessary computations. For example, if you use limit operation to restrict the stream to the first three elements, only those three elements are processed, even if there are hundreds of elements in the original collection. Let's also try out this in an example. For that, let me keep its name as lazy stream demo dot Java and let me do the necessary things. Here I want to import a JSON list. Right. Let me copy paste it. Okay. Observe here. And then let me create a list of string names. Equals to rest dot as list Alice, Bob, Charlie, David. And then a and let me give names dot frame dot filter in which let me give name. And then lambda. Let me call lambda like this. In lambda expression. Let me just print out filtering and then name filtering corner and then name. And let me just return name. Dot starts with a or name dot starts with b. And after this let me call limit. Here I want to limit the filtering to the two operations only and then for each. And let me just print out our result. System dot out dot colon. Colon println. Now if I run this we got this as result. It printed each processor element. So which prove our point. After getting invoked by Farage our filter starts its operation. So that's why we got filtering and printed alias result got stored in stream as alias since we are just returning it here. This name dot starts with a whatever element starts with A or starts with B, we are just returning it to the stream. So from all our collection we got these two elements returned to our stream. Since we have limited our stream to two elements only. That's why it just checked for two elements, and then it successfully got those two elements into this condition. That's why it just printing, filtering and filtering. Bob. So basically in this example, the limit method ensures that only the first two elements that pass the filter are processed, even though the list contains more elements. This is an example of lazy evaluation, where the stream process only what is necessary. In summary, intermediate operations define the transformation of the data in a stream, while terminal operations trigger the execution of the stream pipeline and produce a result. Understanding how to build and execute stream pipelines is key to leveraging the full power of the stream API. So that's it for this video. In the next video, we'll explore specific intermediate operations like filter map Reduce in more details showing you how to use them to process your data efficiently. So that's it for this video. Thank you for watching and I will see you in the next one.