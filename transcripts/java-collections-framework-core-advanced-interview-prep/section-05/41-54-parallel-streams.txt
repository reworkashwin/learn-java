Hello everyone. Welcome back to Java Collections Framework course. Today in this video we are going to talk about more exciting topic which is parallel stream introduced in Java eight. If you have ever wanted to make your data processing faster by using multiple threads, then this is the topic for you in our previous video, as we have already converted streams and how they help us process collections of data in a more functional and streamlined way. But what if I told you that we could take this concept further and make our streams process data in parallel across multiple threads? This is what exactly streams do. They split the data into different parts, allowing different parts of the collection to be processed simultaneously by multiple threads, giving us a boost in performance on multi-core systems. Creating a parallel stream is super easy. You can convert any existing stream steam into parallel one by calling the parallel method. Let's explore it through an example. For that, let me open our VS code. Let me create a new file with parallel frame demo dot Java. And here let me create a list of integer. Let me give a name as numbers equals to restart as list. And let me go 123456789 ten. Okay. Now we call our number start stream. So we have created a stream now upon which I just want to call parallel to process this data in a multi-threaded environment. And for that let me use for each. And here let me just system dot out dot print each element of it using println. Yeah that's it. Now in this code this parallel method converts the stream into parallel stream. And now the. For each method, this operation will be done on multiple threads. You will notice that the output may come in any order since the operations are happening in concurrently. You won't see a specific order of elements getting printed here. So if I run this program now and there you have it. We haven't got the order since the thread which is working on data seven has got priority first while printing these elements and then eight, then three and ten got last priority. So that's why we are having the data in this format. If I run this again now, now you will probably see a different result. See now the element, the thread which is working on one got the least priority. That's why you are seeing this one getting printed out in the last. So now you might be having a question why might we use parallel strings? So the key advantage of parallel stream is that they leverage the fork or join framework, meaning the automatically dividing the work into tasks. Let's suppose we have this work, and it is dividing this work into different tasks such as T1, T2, T3, etc. and distributing this work across multiple threads. For to handle task one, we are having a different thread to process it, and for T2 we have different thread, and for T3 we have different thread, and for T4 we have different threads like this. It will go up to n based on the parts which our work is divided into. This can significantly reduce time taken to process large data sets. You will only see the parallel stream effects on large data sets if you consider the time here. If you are playing parallel stream on a small data set like this, it will be taking even more time to process it, since on parallel we are having a more number of operations than a normal sequential stream, and it's particularly useful in CPU intensive operations where the workload can be split evenly. However, it's important to keep in mind that not all operations benefit from parallelism. For instance, tasks that involve IO operations or they rely on order like for each order, may not seem much improvement and might even run slower. And before we jump into parallel streams, we should consider some performance considerations such as task size, hardware, and stateful operations. So if the tasks are too small like we have performed on like in this example here, the overhead of managing multiple threads May outweighed the benefits, and parallel streams are ideal for multicore processors. On a single core machine, you won't see any performance gains, and we should avoid using stateful operations like reduce, modify or modifying the shared data as it can lead to the race conditions. Now let's explore a real example of parallel streams. For that, let me just open our example. And here let's take a larger data set. Now for that to create a larger data set I'm going to use int stream dot range closed, on which I want to pass from 1 to 100 million 000. Let me give one more zero here and three zeros here. Okay. And let me call about set method on it. And upon it let me call our collect method to convert the result into a list by passing collector. Collector. Start to list method. Okay, and one thing to notice here is these imports are automatically got imported while I'm doing this. So if you don't get this please import them manually. Now let me create a sequential stream for that. Let me create an instance object name as start sequential equals to instant dot now method which probably return the current instance using the system clock. And let me use long sum sequential equals to number start stream dot map to long. Let me use this method map to long. Since we are working on a large data set num lambda function num into num. Let me do this multiplication operation and let me sum this result and let me just create another instant object which belongs to Java.time package. And let me give this object name as end sequential equals to same as before. And standard now method. And let me keep the duration type elapsed for sequential linear object of duration duration time elapsed sequential equals to duration dot between start sequential and end sequential okay. And after that let me just print out sequential stream some as some sequential. So here we are printing our operation which we have performed using our sequential stream. Got it. We are just printing out the result. Then let me also just print out the time taken to do this operation in milliseconds. For that, let me use sequential stream time as time elapsed dot two millis. And after that, let me just keep the units as miss milliseconds. And after that let me start our parallel stream operation now. For that here also let me store our starting time using instant object start parallel equals to instant dot norm. Now learn some parallel equals to numbers dot stream. Or we can also call our parallel stream method directly upon it. Or you might be just call stream method first stream, upon which you can call dot parallel method and dot map to long and let me go numb. Same lambda expression as before. Numb into numb. Then let me just sum the result. Now let's store instant of and parallel time equals to instant dot norm method. So it's giving error. Now it's fine. And here also let me give you duration. Time elapsed. Parallel equals to duration dot between or start parallel and end parallel. Got it. Let me just print out parallel. Stream some as some parallel and parallel stream time. As time elapsed parallel. So if I run this program now this program basically does is it creates a list of integer ranging from 1 to 100 million. And it is having error. Here. Let me just remove this extra brace. It's fine now. And it calculates the sum of squares of these numbers 1 to 100 million, using a sequential stream on start and then parallel stream after it, and it compares time taken to complete the task for both sequential and parallel streams. Let me run this program. Finally, it just printing out those sum and times we run this program. Now it is taking a little bit time to process everything. Now here you can see sequential stream sum has this number. It would be the same result at both places. And one thing I forgot to do is here. Here I forgot to convert this to millis. And let me also keep units here as milliseconds. Now let me rerun this program. Now here you can clearly see on a sequential stream it took 259 milliseconds. Whereas on parallel stream it just took 137 milliseconds. See the difference on same team operation, we are exactly having the exactly doubling the performance and parallel stream on such large data sets. So here, instead of doing this on parallel stream stream calling stream, then calling parallel, we should have done this directly dot parallel stream. If I run this program now, now also we will be having the similar result here. I am not saying same results but similar result this time. You can clearly see sequential stream took 293 milliseconds for the same operation, whereas parallel stream even reduced the time. Say it just took 115 milliseconds. So that is the difference between a normal sequential stream and a parallel stream. So basically in this example we have typically see that the parallel stream finishes the task faster through. It depends on the hardware right. That's why this is because it uses multiple threads to split the work and compute the sum of squares in parallel using multiple threads. So finally, while parallel streams can be powerful, they aren't always the best choice. You should avoid parallel streams if you are working with a small data set, because as the overhead might be more than the speedup if you perform parallel stream on a small operation small data set, instead of reducing time, you end up increasing the time. So on smaller data sets, always use sequential stream only on the situations like cases like when you want to work on the larger data sets where you are having the data in millions, millions of millions of data, then only consider the usage of parallel stream and you should also avoid your operations are if your operations are I o bound input output bound, such as network calls or reading from disks. In such situations, don't use parallel, and if you need to maintain the order of elements in the stream, then also you shouldn't use the parallel. Since we have explored here, it doesn't maintain the order, since each part of data is processed by a different thread, and whichever thread who finished the task will get the order first. Since here in this situation the thread working on seven got the screen time first. So that's why if you need to maintain the order of elements in the stream, then you should not consider the parallel stream. So that's an overview of parallel stream in Java eight. There can be huge performance boost when used in the right context, but always remember to evaluate whether the overhead is worth it for your specific use case. So that's it for this video. Thank you for watching and I will see you in the next one.