Hello everyone. Welcome back to our Java Collections Framework course. Today in this video we are going to talk about one of the most exciting feature introduced in Java eight which is lambda expression. So what are lambda expression? A lambda expression is essentially a way to represent an instance of a functional interface. Note at this point, a lambda expression is essentially a way to represent an instance of functional interface using a short and concise syntax. You can think of them as anonymous methods, meaning they don't have a name and can be passed around as if they were objects. So here we are not at this point as lambda expression, as the instance of a functional interface. So what exactly is this functional interface. So a functional interface is any interface that contains exactly one abstract method no matter It might be user defined interface or predefined interface. If an interface having exactly one abstract method which makes it a functional interface. So a functional interface is any interface that contains exactly one abstract method. And this is where lambda expression comes in handy. We are going to create lambda expression as an instance of these functional interfaces. Only on basic syntax of lambda expression would be. We will be having three things in our syntax. The first one is the parameter list which is enclosed in parenthesis. So in between you can give a number of parameters according to the method defined in your functional interface okay. It might be having a number of parameters according to your need if you have defined it. So this is the first thing which will be having in our syntax of a lambda expression, it would be empty. It would be with a number of parameters starting from one to n, and next to it we will be having an arrow which separates the parameters from the body. Next we are going to declare is the functional body. So to separate these two we will be having an arrow operator between them. And next thing which will be coming is body which is the code you want to execute. Code or action, whatever you may call, you will be declaring those actions or logic at this place. So whatever you want to do using this lambda expression, you can define that logic here at this place body. Now let's explore an example. Let me create a new file for it with the name Lambda test dot Java. And let me close this terminal. So here I have created a list of integers and I have passed one, two, three, four, five in our numbers. If you want to loop through, iterate through this collection. Traditionally, we might have used this traditional loop to iterate over it, but if you want to iterate through this using lambda expression, then you might declare a ferry turnover collection number. Start for each and see this for each is expecting a consumer object. What exactly this consumer class do? Let me first declare our lambda expression and we're going to explore. So I have already said that our lambda expression is an instance of a functional interface. So here as parameter I'm just passing in a number. Let me pass it as a parameter and an arrow on our code. This is the body in which I'm just going to call our Dot println. And here I just want to print our number. As simple as that. And if you declare parentheses then you must put semicolon here and here. If you're having only one parameter you don't need to declare this parenthesis. See this one will also work. And here if you want to declare all it in one line then here you don't need to declare these parentheses all also in the code. So if you don't declare parenthesis then you don't need to put this semicolon here. Extra semicolon okay. So this code also works. So as I said our lambda expression only works on functional interfaces. But how this is working at this place in our forage. It is because here our forage expecting a consumer object. And this consumer object coming from Java.util dot function dot consumer C Java.util dot function dot consumer. If you open this consumer, this is a functional interface which is having only one abstract method, which is accept and it is accepting an element of type T only one element. So that's the reason we are passing one element here as number. If you do this let me try to pass two parameters number one comma number two. Now this will throw an error. Lambda expressions signature does not match the signature of the functional interface method, which is accept of only one integer. But you are passing two parameters here. That's why it just throwing the error. Since our functional interface consumer only accepting one method in its abstract method which is void accept. Thus, here we can't pass two or more elements into this method, so which proves our point. Here Lambda expression only works on the functional interfaces. All right. So why should we care about lambda expressions anyway? There are a few reasons. Why should we care? Such as? First one would be conciseness. Here conciseness is nothing but lambda expressions. They reduce amount of code we write. If you don't want to use lambda expression then you have to declare it like this. Here we are doing it in three lines. But if you use lambda expression, you can concise it to a one line, which reduces our code and makes it even readable. So the next reason is readability. The code is more readable because it's straight to the point. Avoiding boilerplate code. And third reason would be functional programming. So the Lambda spring functional programming concept to Java allowing us to treat behavior as data. So here we are treating this behavior as data and parsing it in our consumers action object as object. So here we are passing this behavior as an object in this consumers action object. So these are the main reasons why should we consider lambda expressions instead of doing it in a traditional way. As of now we have explored how to use this lambda expressions with collections. But what about if you want to create our own functional interfaces and use lambda expressions on them? Can we create? Yes, of course we can create. Since I have already told you functional interfaces would be any interface which is having only one abstract method. So let's try to create a functional interface. So to define a functional interface in Java you have to simply create an interface. Public interface. So here I have created this inner lambda test. You can give a name as your wish. I want to give my functional interface. That's also fine. So you can give whatever you want. And our one and only rule is it would have only one abstract method. So let's create that abstract method. Void my method. Okay. Sorry I don't have to declare this public here because here you can see after declaring public to this interface, it just my file name is automatically turned to this name, my functional interface. Okay. So I have added an extension. So whatever public we find it will just turn that class or interface to the that particular name. Whichever coming first. So here first we are declaring interface and I have put it public to it. That's why I just renamed our file name to this. So to avoid this let me just remove this oblique in front of our interface. Now it just got back to Lambda test or Java. Now let's create our function my method. So first of all I'm not using any parameters in it. So this would be an empty method without parameters. And we all know that by default the methods in our interface will be abstract. So this is our functional interface. So here we are having only one method which is abstract in our interface which makes it a functional interface. So can we declare lambda method on it. Obviously yes we can apply. Let me do this. My function interface Instance equal to. You are free to give naming as your wish. So here I'm just keeping it as instance equal to. And here I'm declaring an empty parenthesis since our method doesn't accept any parameters in it. So that's why I'm declaring it as empty and an arrow. Then the behavior is out and here I'm just printing out a message. Hello from my method. So does this work? So here we have parsed this data into our method. But if you want to print that hello from my method we have to use this instance dot my method. We need to call it if I run this program now. Now you'll probably see below from my method, since we have passed this into our main method and we have stored that instance in this instance object. So that's how we can use it. Like this. Now what if if I declare some other methods in it? Does this also work? If we do that, let's verify it. Let me also give one more method. Void work. Now here it doesn't giving any error. But here you got the error because the target type of the expression must be functional interface. But here you are using two methods which doesn't make it a functional interface. So if you want to get error while working on functional interface, then you might use this declaration about our functional interface to avoid doing such things in our interface. If you want to declare it a functional interface, then you just need to mention at the rate functional interface. Now we'll be having an error because invalid functional interface annotation. My function interface is not a functional interface because we have declared two methods in it. Let me remove one method. Now this error is gone which satisfies our functional interface requirement. So here you can see its description. This functional interface is an informative annotation type used to indicate that an interface type declaration is intended to be a functional interface, as defined by the Java Language specification. Conceptually, a functional interface has exactly one abstract method. Since default methods have an implementation, they are not abstract. If an interface declares an abstract method overriding one of the public methods of Java.lang.object that also does not count towards the interface abstract method call. So that's it. So in our previous videos example also while we exploring collectors class. So here we have used our lambda expression or method reference a lot of places. See here. We have used it in our partitioning by. So why this lambda expression working here. Because if you open this partitioning by it just accepting a predicate. So here this predicate will be a predefined functional interface. See functional interface. That's why lambda expression is working on this. So here we would be having some interfaces which are predefined such as consumer and supplier predicate which we have just explored now. And we will be having by function also etc. we will be having some functional interfaces which are predefined in Java. So we can work with lambda expressions on these functional interfaces. So that's the basic about lambda expression guys. Hope you got the clarity by now how to create a your own user defined functional interface, and how to use the predefined functional interfaces to work with lambda expressions. So that's it for this video. Thank you for watching and I will see you in the next one.