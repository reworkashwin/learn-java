Hey there everyone! Welcome back to our Java Collections Framework course. Today in this video we are going to discuss about internal versus external iteration. So in Java internal iteration and external iteration refer to the two different ways of traversing collections. Internal iteration is often associated with functional programming and stream processing, where the control of iteration is handled by the library, such as stream API, whereas external iteration involves traditional loops where the user controls the iteration. Let's take a look at an example. What exactly is this external iteration? First of all, let me create a collection of list of type integer. Let me give numbers equals to arrays dot as list and let me pass some numbers into it 2345. Let me import our list also. Now let me do the external iteration, which is nothing but a traditional way of iterating collection. And before iterating over it, let me also print out a message. External iteration using for each loop for integer num column numbers and sort number colon plus number. Sorry num. It's fine now. So this is what an external iteration looks like. So here we controls the user controls the iteration. And another form of external iteration would be. Let me show it to you using an iterator. Let me also print out the message external iteration using iterator iterator of integer it equals to number start iterator. Now using. While we can simply iterate over our collection right dot has next Integer num equals to itnext assault number colon. You're just simply printing our number. So this is one more form of external iteration. Now let's explore internal iteration. We have already explored all of these such as external iteration versus internal iteration already in our previous videos. However, these terms are new to us. Let's look at what an internal iteration looks like. So, as I have said already, internal iteration is a type of iteration where library handles everything such as stream API. Now let's handle our internal iteration using stream API. And for each loop. And here let me just print out this message. Internal iteration using streams. Now let's iterate over our nums collection num start stream dot for each lambda expression with action. System.out.println. Number column. Let me keep it in. Double quotes. Number column plus num. As simple as that. Now let's take a look at internal iteration with other stream operations such as filter, map, etc.. Okay now number start. Stream. Dot. Filter. Here we need to specify a condition. So here I want to grab even numbers from our collection. So for that we can specify this logic. NUM mod two equals to equals to zero. So if it satisfies the condition it will get stored into our stream API. And let me map also like num event number column plus num. Then we can just simply iterate over it using for each. For each. System dot out colon colon Rintelen. Got it. Now let me run this program. So basically, first of all, this is the traditional way of iterating over a collection. This is how we got its output. This traditional way of for each loop output, just simply printing out number one number colon two three up to five. And then here we have used another form of external iteration using iterator interface. And here also we got the same output as expected. And then here we are using an internal iteration using a stream API library which is introduced in Java eight. Basically this streams abstract the iteration logic. So here user specifies what should be done. Example printing each element. But the iteration is handled internally by the library. And here we have also done an internal iteration with additional operations such as filtering and mapping then printing it. So here you can perform operations like filtering, mapping, reducing, limiting etc. whatever you want. So here we have filtered only even numbers. And then we have mapped it with this message even number colon. And then we are just simply printing out that even number. So here the main key difference between external iteration and internal iteration is in external iteration, you as the programmer controls the flow of iteration using basic loops like for, for each while, do, while, etc. or in iterator, whereas in internal iteration the library handles the iteration for you, often leading to more concise and readable code, especially when combined with other functional style operations. So that's it for this video guys. Hope you got the clarity on what exactly external iteration and internal iteration is. Thank you for watching and I will see you in the next one.