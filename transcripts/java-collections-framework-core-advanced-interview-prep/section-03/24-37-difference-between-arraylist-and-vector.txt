Hello everyone. Welcome back to our Java Collections Framework course. In our today's video, we are going to talk about the difference between two important classes such as ArrayList and vector. And we all know that these two are both list implementations in Java, but they have some key differences. So to explore them, let's dive into our video. And first of all, let's quickly recap what ArrayList and vector are. Both ArrayList and vector are part of list interface and provide dynamic arrays that grow in size as needed. The elements in both can be accessed using an index, which means if you want to access an element using index, that is possible in both. So this is how you can access an element of ArrayList using index. So currently I have passed a zero as index here. If I run it now, you can see the element at zeroth position is getting printed here in output. Let's create a vector object instead of a list. Let me give vector. So here also if I do this vector dot get of index zero, then you can probably see we are getting the same result and both ArrayList and vector allows duplicate values. So why do we have two different classes if these two classes are so similar? So here the answer lies between four main differences such as synchronization, performance, growth, size, and legacy versus modern use. So these are the main differences between ArrayList and vector. So one of the most significant differences between these two classes is synchronization. So when we talk about ArrayList it is not synchronized which means it is not thread safe. If multiple threads access ArrayList shared resource access simultaneously, you might run into issue unless you manually synchronize it, whereas vector it is synchronized by default, every method in vector is thread safe. If you are working in multi-threaded environment and want a synchronized collection, vector might seem like a better choice. However, keep in mind that making every method synchronized can introduce some performance overhead. This led us to the next big difference performance. Since ArrayList is not synchronized, ArrayList tend to perform better in single threaded environments. It doesn't have any synchronization overhead, which makes it faster for general use cases where thread safety isn't a concern, whereas in vector, because of synchronization, vector can be slower than the ArrayList, especially in single threaded environments. This is one reason why ArrayList is generally preferred when you don't need synchronization. And the next difference is how both these collections grow when they run out of space. So growth. Size in ArrayList it grows by 50% of its current size when more capacity is needed. So if you have a ArrayList list with ten elements. If it runs out of space, it will increase its capacity to 15, which means it add extra 50% of its current size, which is five, right? So it adds five more element space to our ArrayList. In contrast, vector doubles its size, which means vector doubles its size every time it runs out of capacity. If you have a vector of ten elements, its size will increase to 20 if needed. That made vector might consume more memory in some cases compared to ArrayList. And another important thing is to note is that vector is a legacy class. So legacy versus modern use ArrayList. Introduced in Java 1.2. Right as a part of collection framework at the time, ArrayList is considered more modern and is generally preferred for new applications, whereas vector. It has been around since the early days of Java since Java 1.0, And it is a part of old free collection framework. It is there when collection framework don't even introduced. So after introducing collection framework, while it is still supported and usable, its use is discouraged in favor of more newer and efficient classes like ArrayList or copy and write ArrayList for Threadsafe needs. So to wrap things up, if you are working in a single threaded environment and don't need synchronization. ArrayList is generally the better choice because of its performance on modern design, but if you need thread safety out of the box, vector could be an option. Even then, there are better alternatives like copy on write ArrayList. In most cases. I hope you know how a clear understanding of the difference between ArrayList and vector. If you have any questions, you can reach out to me and I will clarify you the doubts you are having. Okay, so that's it for this video guys. Let's meet up in the next lecture. Up till then, stay happy and stay secure.