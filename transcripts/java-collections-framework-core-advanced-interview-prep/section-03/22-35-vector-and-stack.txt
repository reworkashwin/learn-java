Hello everyone. Welcome back to our Java Collections Framework course. In our previous videos we have covered ArrayList linked list and today we are diving into two more important classes such as Vector and Stack. Vector and stack will not as commonly used in modern development. These classes still hold significance, especially when we talk about the synchronization and legacy code. So without further delay, let's get started and learn more details about classes vector and stack. So first let's talk about vector class. If you have already worked with ArrayList, you might find vector quite similar because they both implement the list interface and function in nearly the same way. But what is the main difference here? The main difference is synchronization. Vector is synchronized, meaning it's thread safe When multiple threads access a vector object, you don't need to worry about inconsistent state because only one thread can manipulate it at a time. But here's the trade off. Synchronization adds overhead and slows down operation compared to the non synchronized ArrayList. In terms of structure, vector uses a dynamic array internally. So just like an ArrayList, the elements are stored in a contiguous block of memory and it grows as needed when more elements are added. The key difference here again is the vector synchronizes its methods, making it safer in multithreaded environment. Now let's see the simple example of using a vector in Java. So here you can see I have created an vector object using vector class. Now I have added three elements apple banana, cherry into our vector. And then simply here I am just printing out those elements. And then I am removing one element from our vector list, which is banana getting removed, and after that removing. I am just printing out the final vector output. Okay, so if I run this now, you can probably see the result as we have expected. So it just works like an array list. But as you can see we add elements, remove them and the operations are almost identical to ArrayList. So when should you use vector. That's the main question. So if you are working in a multi-threaded environment and you need synchronization, vector might still be a valid choice. But for most use cases ArrayList is the preferred because it's faster. Vector is a bit slower than ArrayList because of its synchronization. Okay, so that's the basic about vector. Now let's move further into our video and explore Basic about this tag class. So Java's tag class extends vector. So here all the threadsafe characteristics apply here to Stackless two. However, the stack class also introduces new methods specific to stack behavior, like push to add an element, pop to remove and return the top element, and peek to view the top element without removing it. In practical terms, stacks are useful in scenarios like parsing expressions and new features in the text editors or implementing recursive algorithms iteratively. Now let's look at a quick example of stack in action. So here you can see I have created a stack object. And if you open the stack implementation here you can see our stack class implementing the vector class as I have said. Okay now let's get back to our example. So if you observe here we are pushing integers onto the stack. Then using the pop to remove the top element. And if you notice here we also picking the top element here without modifying the stack. So if I run this now you can probably see here by using this push method we have added these three elements. And then here we are picking the top element using the pick method say top element as 30 because stack follows last in first out right. So whatever element you added the last is on the top of the stack. So that's why it's returning the 30 as element. And then you can see when we call this pop method it just removes that top element popped element as 30. Here you can see and after removing we are just printing this updated stack. Now you can see we have only these two elements left in our stack 1020 so these are the basic operations which you can perform on our stack class object. And if you want to explore more operations which we can perform on stack, you can still explore this stack class implementation here. Here we have push method. Synchronized pop method. Synchronized pick method. Empty method. Here we can also perform such operation on our stack. So these are the operations which we can perform on our stack object. So to sum up both vector and stack are legacy classes in Java that are thread safe but tend to be slower than their modern counterparts, while vector is very similar to ArrayList but synchronized, whereas stack provides a classically for data structure built on top of vector. For modern applications, you might want to explore alternatives like concurrent linked dequeue or dequeue for stack behavior and copy on write ArrayList for thread safe list. So that's all for today's video. In the next one, we will be exploring the topic of synchronization in Vector and Stack. So thanks for watching and I will see you in the next one.