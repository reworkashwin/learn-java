Hello everyone. Welcome back to our Java Collections Framework course. In this video we are going to explore DK implementations, specifically the array DK and the linked list. And we'll walk through their key features advantages and when to use each one. So without further delay, let's get started with a small question. What exactly is DK? So basically in Java DK stands for double ended queue. And as the name suggests, it is a data structure that allows us to add or remove elements from both ends. Let's suppose this is our DK. So here we can add elements to the end. And also we can remove elements from the ends. Either it might be from front or either it might be from rear. This makes it super flexible for variety of use cases. Let's explore it through an example. Let me open VS code for that. And here let me create a file with the name DQ test dot Java. Wait a minute, I have created a folder. Let me delete that one and let me create a file. Now dq test dot Java. And let me do necessary things. PS vm and let me also import our dq from Java dot util package. And now let's try to create an object for it dq string dq. And we all know that in Java, DQ is an interface which extends Q interface, and it allows us to perform operations at both ends of the queue. So you can think of it as a queue that can also behave like a stack. For that we have methods like add first to add elements at the front and add last to add elements at the rear end. And also we have methods like remove first to remove elements from the start and remove last two elements from the to remove elements from the list like that. We have a lot of operations which can perform operations at both ends like pole first, pole last. Also the operations which can which are easy to perform at the both ends of our queue. See these are used to retrieve and remove the first and last elements from our queue. To retrieve elements from both ends, we have this. Get first and get last methods also and to retrieve just without removing, we have these ones peak first and peak last methods. And just like Indexof and last Indexof, we have remove first occurrence method to remove the first occurrence of the specified element from the dequeue, and we have remove last occurrence to remove the last occurrence of a specified element from our dequeue. Other than this, we can also add elements normally. So using this method add. Just insert the specified element into the queue represented by this dequeue. In other words, you may call. This operation would add elements at the tail of the cube, and we can also use this remove method to just retrieve and remove the head of the cube represented by the d cube. So basically we can do all of the operations which we can perform on a normal cube. And addition to that, we also have some new methods in it to perform operations at both ends, like add front end and add rear end, such as the above methods which we have explored like remove first remove last pole, first pull last offer first offer last, add first and add last. Now, first of all, let's talk about the array deck. This is the one of the most popular implementation of the deck interface. It's backed by the dynamically resizable array, making it super fast for operations like adding or removing elements from both ends. Let's explore it. So here I have created a deck object. And here let me call array d q. And to use it we also have to import it trait. For that, let's also import our Arraydeque. Okay, now we have successfully created our Arraydeque. Let's try to add element. First of all, let me just try to add some random elements using add method dk dot add apple dk dot add banana dk dot add cherry. Let me keep it at the top so that we can prove the point which this collection will store elements in an insertion order. Okay, so if I print it, let me use assert and let me just print DK. As of now, if I run this program now, you can see it just maintains the insertion order as we have expected. Now to add elements. At the first we have a method add first method. Right. So let me just use our add first method to add elements at the head or front position. So here I'm am adding mango and the first. And let me also use array or let me also use dk dot add last method to add elements at the rear at the end of our dk, and let me add at the end grapes. Now if I run this and before going to run this, let me also print out our DK again and let me run this program now. Now you can probably see here we have successfully added mango at the front of our DK, and also we have successfully added our grapes element at the end of the queue using add last method, which proves our point that it will operate at both ends. And now let's try to remove the elements at both ends using remove first and remove last methods. Okay, for that let me use dk dot remove first. Here we have this and we can use dk dot remove. Last. And let me print our DK again. And if I run this program now, now you can probably see mango and grapes got removed. So remove first is to remove the starting element, whereas remove last is used to remove the last element in our dequeue. So basically in this example we have initialized an array dequeue and added two elements first at the front of the our dequeue and second one at the end of our dequeue. Before these two we have added some random elements cherry, apple, banana and after that we have tried to add these ones at the front end. And then we have successfully removed those two elements at the ends using remove first and remove last methods. Now let's try to imagine a scenario where we are managing a task queue. So on queue, high priority tasks should be processed first, right? While regular tasks are added to the back of the queue. Here's how you can handle that using a radical. Let me create a new object. D q frame task q. Let me name it as task q equals new array d q. So task q dot add first. So whatever your priority task is, you can make use of add first method such that you can add priority once at the starting priority task. And to add regular tasks to our queue can add them at the last of our queue using add last method regular task. So you can always add your priority elements or priority tasks at the starting of our queue using add first method, and you can add regular ones using add last method. So as you can see, priority task is added at the front and regular task is added at the back. When we process tasks, the higher priority one will always come first. Now let's move on to the another d q implementation which is linked list. So unlike array d Q which uses a resizable array. Linked list stores elements in a doubly linked list. Let me use linked list here so it stores. Elements in a doubly linked list. This means each element is linked to both its previous. And last elements. So you can do all of these operations on our linked list. Also, let me also set it to linked list. So if I run this you will see the output exactly like previous one. See. So in this case also we are using linked list to implement a queue just like array d q. We can use methods like add first to add elements at the first position and add last to add elements at the rear. So basically we can add elements at both sides and remove elements at both sides in our linked list. Also, just like in Iraq, now that we have explored both array dequeue and linked list, let's compare these two. Which one should you use. So basically, Eric is generally faster when you are adding or removing elements from either ends. Either it may be at first or last array d q is generally faster because it's backed by a dynamic array, and linked list, on the other hand, might be slower due to the extra memory extra memory overhead of storing node pointers, so it stores node pointers right of next element and previous element, which makes our linked list a bit slower than our d q, but it offers flexibility when you need to access elements in the middle. So when you need when your primary focus on middle rather than ends, you might select linked list. But whereas your primary focus on ends rather than middle part, then you might select array d q as your primary collection. So basically the array d q. Best for stack and q operations when you only care about adding Removing from the ends. It's faster and more memory efficient if you do this and use linked list when you need more flexibility, like frequently inserting or removing elements from the middle of the list. And when you are dealing with larger data sets where random access isn't required. In such cases, you might select the linked list as your primary collection. So in this demo, we have added elements to both ends of array d, q and then removed them. As you can see, d q gives us a lot of flexibility in managing our data structure. And that's it for today's video guys. We have covered both Array Deck and linked list as d q implementations. And you know now when to use each, how they work and why they are such powerful tools in your Java toolkit. So that's it for this video guys. Thank you for watching and I will see you in the next one.