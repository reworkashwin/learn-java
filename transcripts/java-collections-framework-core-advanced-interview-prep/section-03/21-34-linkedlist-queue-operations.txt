Everyone, welcome back to our Java Collections Framework course. Today in this video we are going to explore the queue operations on linked list. So without further delay let's get started. So as we have explored all of the other methods of our linked list collection implementation, now let's explore the queue operations and in queue operations. First of all we have this method peek. So what does this method actually do? Is basically this method is used to retrieve but not to remove the first element of our linked list. So basically this peek method will return the value stored in the first node of the linked list without removing it, or it returns null if list is empty. Let's explore it. Let me remove all of this. Just keep this thing. If I do this fruits dot peek. Now we'll probably get this first element if I run this now. Now you can see we got Apple and if the collection is empty. Then we'll going to get null as a result. If you do this operation on a empty linked list. Okay. Next method we have here is element method. So this element method in the linked list class is very similar to the peek method, but with one key difference. It throws an exception if the list is empty, such as no such element exception, whereas this peek method will just returns a null if the list is empty. So that's the main difference. There is not much variation between those, so basically the element method returns the first element of the list, and if the list is empty, it just throws a no such element exception. So currently we have empty list right. So if I do element instead of peek now we are gonna get a exception such as no such element exception. And if we have elements on our linked list and if we perform this operation now, now you can probably see we'll get the Topmost on mean first element of our linked list. Then we have this poll method. So what this method basically do is this poll method in the linked list class is used to retrieve and remove the first element of the list, where these two are basically used to only retrieve, not to remove the element. But this poll method reduces and removes the head of the linked list. Or you may call the first element of the linked list. Let's explore it instead of element. If I do poll after operation, let me just print this fruits array to see the changes. So if I do this. So if I run our program now, now you can probably see we got Apple because it returns the first element and then it removes the first element. And you can see here we don't have the apple. Now first apple got removed which means it successfully removed the head of our linked list. So after poll we have this method remove method. So this method also use it to retrieve and remove the head element of our linked list. So exactly. Just like pole method. But here the difference is it throws no such element exception if the list is empty. So if we do this now, if the list is empty and if we do pole operation, we will get null as a result. Instead of this pole we just use this remove method. Then you can probably facing the exceptions such as no such element exception. So that's what this remove do. And next we have this method offer. So basically the offer method in the linked list class is used to add an element to the end to the tail of the linked list. It's method. It's a method commonly associated with the queue. Let's explore it. If we do this let me uncomment these things out. Now if I add new element fruits dot offer. And here I'm giving grapes. So just adds a new element at the tail of our linked list. So if I run this and let me comment this thing out. Now, if I run this, you can probably see grapes added to our end of the list. See, that's what our offer method would do. And next we have offer first. So this method basically used to insert a specified element at the front of the list. It just sets the header of the list. If I do this offer first method. And now you can probably see this grip is added to the before apple. Now you can see grapes Apple like that. We have our list. So that's what offer first would do. Then we have this offer list. So this method basically inserts a specified element at the end of the list. Now you might be wondering then what is the difference between this offer list and this offer method. So basically the difference between offer and offer list lies primarily in their usage context and explicit intent, although they perform the same operation in the linked list class, adding an element to the end of the linked list. Or you may call adding an element to the tail of the linked list. Both methods add an element to the tail of the list, and basically offer is commonly used when treating a linked list as a queue and offer. List is used when treating a linked list as a DK. See here at offer list we have this DK as mentioned, whereas here you can see over here. Queue. As mentioned, whenever you are dealing with queue, you will probably use this. And whenever you are dealing with three queue then you would probably use this offer list. Okay, just keep that thing in mind. Next we have this method pick first. So what does this do. So basically what it does is retrieves the element but it doesn't remove the element. And it only retrieves the elements at first position at the head. That's why first is mentioned here. And if the list is empty then you can probably see the result as null. Now if you do this fruit start peak first, we will be getting grapes as a result. And if we comment these things out, and if you run this program now, now you can probably see none as a result since our list is empty. Now let's move to the next method pick last. So by reading its name you can say that what does this method would do. So pick last means it just retrieves but does not remove the last element of the list and it just returns null if the list is empty. So pick last right? Just replace this first with last. Okay, now if I run this, we will be probably getting null as result since our list is empty. And let me uncomment these things out. And if I run this now, you can probably see Apple as a result since Apple is it at the end of our linked list. Okay, that's why we got the apple. And then we have full first method. So what does this do. Is it just retrieves and removes the first element of the linked list. Whereas our pick first just retrieves but does not remove the element. Here this first method will reduce as well as remove that particular element and it removes the first element. That's why here first is mentioned first. So if I do this, let me uncomment this thing out and let me call our pull first method. Here our grapes element would get removed okay. Now after printing it you can see we have the elements such as apple, banana, apple, cherry, apple and just return the removed element. Also, that's how our pull first method works. Then we have pull last method. So basically opposite to pull first it just removes the retrieves and removes the last element of our linked list. Now if I run this now, you can probably see cherry has our last element since Apple got removed. Okay, so after pull last we have this method. The push method in linked list class adds an element to the front, I mean to the head of the linked list, and it is meant to be used when treating the linked list as a stack where elements are pushed on to the top of the stack, right. The front of the list in this case. Now, if I do this instead, push grapes and let me comment this thing out. And you can probably see grapes at the starting position. So whenever we're dealing with the stacks then we'll probably gonna use this method push. Okay. Next we have this pop method. So basically this method just pops an element from this stack represented by the list. In other words, it just removes and returns the first elements of the list. Whereas this push only adds an element, pushes an element to the front of the list, whereas this one just removes the element in front of the list which is present at the first position on our list. Okay, so here you can see it just using the method remove first method, which is an internal method of our linked list class. Right. Just using this particular method to remove the first element of our linked list. So if you do this pop instead of push here. So if I run this now you can see grapes got removed and returned. Also, let me show this to you. Let me call pop here. Here. Let me just only print the fruits after removing. Now you can see grapes got removed. Since grape is at our first position, right after removing grapes, it just returned that grapes. And after that you can see we don't have grapes on our linked list. That's how it works. And then we have another method such as remove. First occurrence by reading this method's name can say that it is used to remove the first occurrence of our object on our linked list. So here we have Apple repeated times, right? So if we just want to remove its first occurrence it just removes this element okay let me run this. Currently we have grapes apple banana apple cherry apple right. So if I do this fruits dot remove first occurrence and let me pass the object. As Apple now can probably see, this apple will get removed and it just returns the result as true since it is a boolean operation C boolean and just returns true after returning true. Let's just print our fruit set. Now. If I run this now, you can probably see grapes, banana, apple, cherry, apple. So first occurrence of our apple got removed successfully. So just like that we have remove last occurrence also. So if we use remove last occurrence instead. Now you can probably see after calling this method this apple will get removed. Remove last occurrence. Right. And let me run this program. Now I can probably see grapes, apple, banana, apple, cherry and you won't find the last apple in our output since it got removed by this method. Remove last occurrence and you can see the result as true since it has successfully removed that element from our linked list. Okay, so after that we have listed traitor. So basically this list iterator method in our linked list class returns a list iterator Illustrator object, you can move forward and backwards. Add or remove elements and retrieve the current position of the iterator using this method. Let's suppose we have these elements on our linked list right now. If you want to create a Listiterator starting at the second element banana, you can do the following list iterator since these elements are string, right? So here I'm giving it a string and it equal to root start list iterator. And here I'm passing one. Since here I want to start it from one right. So that's why here I have passed one. If you don't mention anything here it just takes from start. Okay. Let me call while here. While it has name you can call a sort. And to get next element we can use Itnext method. Right. So if I do this now you can probably see elements are getting printed from banana, apple cherry apple. Then these methods got printed out. So like Hasnext. And next we also have has previous also. Right. So if I start index from like so we have total five elements. Right. Since if I pass four here then we can use here as previous instead to move backward in our list on it that we can use just previous method. Okay. So if I do this now you can see these elements are getting printed in backward cherry apple banana apple. So here we need to pass five to get all elements okay. Now you can see apple cherry apple cherry apple banana apple apple banana apple. Got it. And there are a lot of operations we can perform with our Listiterator. If you open this Listiterator interface here you can see we have Hasnext next as previous previous method we can get next index. We can get previous index. We can remove the element from the list using this remove method. And you can set event I mean replace elements. And we can also add an element to our list. Iterate. After this method we have a list item class which just implements our list iterator and implements these things like a next method as previous method. Those methods implementations implemented in this class. You can see here there are. Whereas in our list iterator interface we just have only the references. Right. And in this linked list class we have this list iterator subclass. And in this subclass we have these implementations of our list iterator methods such as hash. Next implementation and previous implementation. Previous index implementation. See all of these method implementations are designed. And then we have this another static class node in which we just have an item next and previous references. And since it is a double linked list now you can see we are setting element to this dot item okay. And next reference to this task next, which is nothing but this next. And here we are setting previous to this dot. Previous. Okay. That's how this node class is implemented. And this is getting used everywhere while we are performing operations on our linked list. See here we are just using that class here. Also on pull list we are using the node class. Wherever you are calling that node, you are actually using this node class. Okay. Make sure of that. And after all of this we have this method descending iterator. And it is available on our linked list class since Java 1.6. So basically this descending iterator method in the linked list class returns an iterator that traverse the list in reverse order. This means it starts from the last element of the list and moves backward towards the first element. If you open this, uh, descending iterator constructor here, you can see we have this separate class for descending iterator. Boolean has. Next. Then it is just using the has previous method to iterate back. Okay. And if you call remove it just removes the element previous element. Since this descending iterator class is a private, so you can't use it directly. So just ignore it. And finally we have a public method split data. So basically this split method in the linked list class returns a split data for the elements in the list. A split editor is a special kind of iterator introduced in Java eight, designed for parallel processing of elements in collection, supporting both sequential and parallel traversal. As of now, just keep this definition in mind or let me write this definition here. Okay, the split return method in the linked list class returns a split data for the elements in the list. So iterator is a special kind of iterator introduced in Java eight, designed for parallel processing of elements in a collection supporting both sequential and parallel traversal. We're going to explore more about this method once we have cleared the concept of multithreading in our collection framework. Okay, just remember this definition until we explore this split later method once again. As of now, you can use this split later method such as advance for each remaining, which we have already explored in our previous videos while exploring collection interface. Right, so that's all guys. That's all about our linked list class implementations. So in our next video we'll probably gonna explore the vector and stack class implementations. Okay. So that's it for this video guys. Thank you for watching this video. See you in the next one.