Hello everyone. Welcome back to Java Collections Framework course. In today's video we will explore Treeset, which is another member of this set interface family. If you are familiar with Hashset unlinked Hashset, Treeset offers some unique characteristics that make it suitable for a specific use case. So let's explore it with a simple question what is Treeset? So a Treeset is a class in Java that implements navigable interface. And this navigable interface extends this sorted set. And then this sorted sets further extends set interface. So indirectly our tree set class implementing this set interface and this class using a tree map internally. Unlike Hashset which is unordered, tree set ensures that elements are stored in a sorted order. This storing happens in natural order that is, ascending order or according to a custom comparator you can provide here. Comparator means. First of all, let's say this example. Here we have added these elements 2010 4030. Then here we are printing that tree set values. Okay. Let's explore that one. First let me comment out all of these here I'm running this. Now you can see we got sorted set 1020 3040 right. So what if we want these elements printed out in reverse order. Then we can just pass a simple comparator in here using comparator dot by calling this reverse order. Okay. Now after running this program you will see the output as 4030 2010. Okay. So here I'm running it. Now you can see 4030 2010. And if you want elements in another specific order you can create your custom comparator Better. Also, and other than this reverse order, we also have a natural order. If you do this now, you can see the elements are printed out in the format which we got in first. So if you don't pass this natural order also by default it is considering that natural order. So run this again. Now you can see the similar output. So if you open this this is a static method C public static comparator natural order. And just returning a comparator. And above this we have this reverse order method. Also this one also static. That's why we are calling this method using the class name. This is possible because these methods are static like this. We have a lot of methods. We're going to explore them in this course only in our further videos okay. So don't worry about them. So by default this storing happens in a natural order that is ascending order. Or if you want, you may use a different comparator to print them. So here trace. It also offers many of these same functionalities as Hashset, but with the added benefit of maintaining the order of elements. So that's the difference between Treeset and Hashset. However, there are some key characteristics of Treeset you may concern, such as. Let me mention them here. Key characteristics of Treeset. First one is sorted order as we have explored it already, right? So sorted order is one of its main key characteristics. Here elements are automatically sorted in ascending order by default and if you want a specific order then you can mention it. Like if you want reverse order then you can pass comparator in the Treeset constructor call just like comparator dot reverse order. Right. So you can do this too. And next key characteristic is no duplicates means here. Also if you try to add one more element with the same value. So here I'm adding 32 times. Now if I run this now you can probably see only four elements just like Hashset and linked Hashset. Here also you see the uniqueness between elements can see the duplicate elements in our set. And another key characteristics is navigable set interface. So here tree set implements navigable set interface right which provides methods to navigate through the set. So if I open this tree set now you can see here it is implementing the navigable set interface. Thus if you open this navigable set interface now you can see there are lot of methods like lower which just returns the greatest element less than the object which we have parsed, or it just returns null if there is no such element. Okay. Let's suppose if we pass numbers dot lower and here let me pass 40. And if I run this now, you can see the result as null. Why we are getting this. Let's just add 20 here. Now let's run it again. Now you can see the tree set value as 30 because in previous 40 is the greatest one. Right. And next time we have passed 20. So after 20 the next great one is 30. That's why we are getting 30. So if we pass 30, if I run this now you can probably see the 40 as result. And if you pass ten then you can probably see the next greater object which will be 20. So here I am passing ten. If I run this now you can see the output as 20. So that's how this lower method works which is of navigable set interface. And next method we have in our navigable set interface which is flow method. So it just returns the greatest element in this set less than or equal to the given element. What is mean by that. Let us just also explore it. So here let's pause. Let me set it as floor and let's keep it in here. If I run this now we got the element as ten and let's pass 20. If I run this now we got the element as 20 again. So what if if we pass something else like 19. Now if I do run this again now we got the output as 20 again. Let's pass 35. So 35 is between 40 and 30 right. So which element it choose either 40 or 30. Let's see it 35. If I run this it chose 40. So biggest element if you pass 34 here now you can see it has just used 40 again. Which means it just looking for only the next biggest element. Whatever element you pass, the next biggest element which is present in our set. So if you pass the correct element which already present inside, it just returns that element. Or if you pass the other element like we have 20 and 30, right? So if I pass 21, if I run this again now, you can probably see the result as 30 because nest biggest element which we have is 30. Right. So that's how this floor method works. So if I pass 11 here now you can probably see the 20 as result. See 20. And if you pass ten we'll gonna get the ten as a result. Since ten is already present in our set. And if you pass 20 then also we are gonna get the result as 20. And if you pass 19, then we are gonna get 20 as result. So that's how this floor method works. And next we have ceiling method. So basically this ceiling method in navigable set interface used to find the smallest element in the set that is greater than or equal to the specified element. If no such element exists it just returns null. Let's also explore. So instead of floor flour, let me call ceiling. Let's just keep 11 and let's try to run it. Now you can see the nearest element to 11 in our set is ten. That's why we got the answer as ten. So if I pass 19 here then you can probably see the result as. So it looks for the smallest element right. Then also you will not get answer as ten because. So below 19 we have ten. That's why we got the result as ten. Now if we pass 25 here now you can probably see the result as 20. Since the 20 is the smallest element, then 25 we can find immediately in our set. Even if you pass 29 here also, then you will gonna get 20 as result. See 29. So that's how our ceiling works, just quite opposite to our floor method. And then we have higher method. So let's pass. Let's call higher method in that let's pass 30 and we got the result as 20. Let's call 40. Then you can probably going to get 30. And if you call 39 then also you will gonna get 30. As result. If you call 41 then you will be going to get the result as 40. So this higher method just returning the list element in the set strictly greater than the given element. So whatever element you pass here it just checks for list element then the current element and it just returns that element. So currently we have passed 41. It checks for the list element. Then 41 it finds 40. So it just returns that 40 as result. And there are lot of methods here we have pole first pole list. These are used to retrieve and remove the last and first elements of our set. And there is iterator method also to iterate over our set collection. And there is descending set, which is probably used to reverse order the elements contained in this set, and we have the descending iterator to iterate over the elements in the set in the descending order, and then we have subset method. Also like this. We have a lot of methods in our navigable set interface. We're gonna explore it once more in our further videos. So don't worry about them. So let's get back to our lecture. So that's one of the key feature key characteristic of tree set using navigable interface. And then fourth characteristic is null elements entry set. Adding null elements is not allowed as it tries to compare null with the other elements and throws a null pointer exception. So if you try to add a null element into it, let me pass null value here. Now if I run this now, you can see the null pointer exception. We are not getting any error here. We'll be adding the null value to our tree set. Still, we are getting an exception such as null pointer exception. But if you do this in Hashset you won't get any error. So in Hashset you can include one null element in your set, since how many null elements you can add into Hashset and just takes one unique element, right? So in Hashset, adding null element is possible, but in tree set it just gives throws this null pointer exception. And when we talk about performance of tree set, tree set is not as fast as hash set when it comes to adding, removing or searching for elements because of O of log n complexity, adding, removing or searching operations on tree set gives O of log n time complexity, where hash set operates in constant time O of one time complexity for these operations, such as adding, removing or searching. However, tree set is more appropriate when you need sorted data or need to navigate the set in a particular order. In such case, tree set is more preferable rather than just using a hash set. If you want to compare tree set with hash set on the term ordering tree set stores elements in sorted order as we have already explored, and while Hashset does not maintain any order. We all know that right then the null values tree set does not allow null values, whereas Hashset allows one null value. And when we talk about performance, hash set is faster for basic operations and it can take up to constant time o of one, while tree set has O of log n time complexity due to its internal tree structure, and as we have already explored, the tree set provides various methods that help navigate through the elements. Let's explore some of them here. Let me keep this example instead. So in this example here you can see first I have created a set. Then I have added these elements apple banana, orange, mango. And here mainly here I am using two methods headset and tail set. What are these methods. And these methods belongs to which class? Let's explore it. If you open this headset method now you can see these methods are methods of tree set class itself. And if you open this tail set also see this also belongs to tree set. What are these methods are capable of doing? So first of all here we are just printing out the original set. Then we have this message head set less than mango. And then we have just called this headset method on our set and pass the mango in it. So basically this headset returns a view of the portion of the set whose elements are strictly less than the given element. Let's run it. Now we can see we got the headset less than mango as apple and banana here. Why we didn't get orange as a result in this headset. Because here O comes after m, right? M n o p. So it just given the bottom ones of M in ordering, you can say apple and banana comes before m a and B comes before m. That's why we got apple and banana in our headset. Whereas in tail set tail set also we are passing the from mango onwards. And then we have just called tail set. Now you can see tail set includes whatever element you pass its first character. It starts from its first character. Just include all elements greater than the first character of positive element. So basically tails. It returns a view of portion of the set whose elements are greater than or equal to the given element. So these are the important methods you might learn while exploring the tree set. That's why I have included it. Other than this, there is one more important method which I want to include in this lecture, which is subset. Now here you can see subset banana to orange. So here I want to include elements in my subset from banana to orange. So that's why here I have passed the elements as banana from element as banana and then two elements as orange. So if I run this I can probably see only banana and mango. So this is the original set right. Apple banana mango orange. So banana to orange means we got mango also in it. So it just ignores the last element means it considers the from element and it just ignores the two element. So before two elements it will take the elements as its subset. So here that's why you can see the elements banana and mango only in our subset. So if you pass apple here apple to orange means just as apple banana then mango just ignores the orange. If I run this I can probably see apple, banana, mango. So what if I do this instead of orange here? If I do mango, then you can probably see the result as apple and banana. See apple and banana. So that's an overview of tree set guys. While tree set may not be the go to choice for all scenarios due to its performance cost, Tree Set shines when you need sorted data. So in the next video we'll continue and probably explore the priority queue. So that's it for this video guys. Thank you for watching and I will see you in the next one.