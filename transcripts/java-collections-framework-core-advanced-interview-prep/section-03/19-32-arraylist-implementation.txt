Hello everyone. Welcome back to our Java Collections Framework course. In this video, we are going to dive into one of the most commonly used collection class which is the ArrayList. So what is an ArrayList and why is it so popular among Java developers. So to understand it, let's explore it through a definition first. So by definition an ArrayList is a resizable array implementation of the list interface in Java. Unlike regular arrays which have fixed size, ArrayList can dynamically grow and shrink as elements are added or removed. This makes it incredibly flexible for various programming scenarios, and when you want to know about how ArrayList stores elements in its collection or in its data structure may call. So an ArrayList stores elements in a contiguous block of memory, just like an array. However, when it runs out of space, it automatically allocates a larger block of memory and copies the existing elements over. This resizing operation is seamless and handled internally by the ArrayList. Now let's look at some key features of our ArrayList that make it a preferred choice for many developers, such as. First one is dynamic resizing. As mentioned earlier, the size of an array can change dynamically, right? This eliminates the need to guess how much space is required at the start, and then we have random access, which means ArrayList allows for fast random access to elements using their index. This means you can quickly retrieve and modify elements at specific positions. This is possible because it's index based access, so due to its index based access, we can insert elements anywhere easily as we want, like it start at end, at middle at specified position. We can do this operation very quickly and easily and retrieving also simple due to its index based access and replacing, also makes it simple due to its index based access. And next we have ordering. So an ArrayList maintains the insertion order of elements. The first element added will be the first element in the list, and so on. Like however you add elements no matter in which order you have added elements into our ArrayList, the order you have inserted elements into our ArrayList will reset this same and next feature we have flexibility. It can store elements of any type, including custom objects, as long as they are not primitive types. For primitive types, Java provides a wrapper classes like a integer class, double class, and Boolean class. So these are the wrapper classes of primitive types. So what are the primitive types we have int float double right. So for int int int we have wrapper class, integer class, and for double we have double with capital D class, and for boolean we have boolean with capital B class b boolean. So these are the wrapper classes. Now let's explore a practical and see how to create and use an ArrayList in Java. However, we have explored it through lot of examples previously. Still, let's explore it once more. For that, let me create a new folder in our module three. Let me create a folder with the name practicals and in this let me keep it as ArrayList practical. Let me select this folder. Yes, I trust the other cause I am the other here. Now let me create a file ArrayList test dot Java and let me do the necessary things. So first you need to import the ArrayList class from the Java.util package, right? So let me do this. Also import Java dot util dot ArrayList. So in an ArrayList, let me create its object ArrayList string. Let me give fruits equal to new ArrayList. So to explore what are the operations we can perform on ArrayList, let's open its implementation. ArrayList implementation. Now see, we have lots of methods already defined here. So we can perform all of these operations which we will going to explore each one of them in this video. So let's start from start. So on start we have these constructors. We need to understand these constructors first. ArrayList. We have an ArrayList constructor with initial capacity parameter. And here we have an ArrayList with in which we are passing a collection. So what exactly These constructors doing why I'm calling these as constructors. Because. So these are the method calls which having the same name as our class name see ArrayList. ArrayList. And you can see also here ArrayList. So this is the constructor with the empty arguments. And this is with a single argument. And this is with collection. So these are the overloaded constructor methods of this ArrayList class. So first explore this one public ArrayList with empty parameters. So what actually this do. So it basically constructs an empty list with an initial capacity capacity of ten elements. So if you add more elements into it it eventually gonna explore its size. So basically it's a creates an empty list which can contain up to ten elements. So here we have done this right ArrayList. Here we have called empty constructor. When we do this you just create an empty list of fruits which can hold up to ten elements. Let me add ten elements now because since it's, it can hold up to ten elements, right? Let me do this. Fruits dot add apple. And let me call this for 10 or 9 more times 23456789 ten. Okay I'll call it like ten times. So if I run this. So before going to run let me just print out our fruits ArrayList. Our fruits ArrayList object. So if I run this you'll probably gonna see Apple printed out for ten times 123456789 ten. But now what if if I add one more, which makes its 11th element, right? So if I run this now, it probably included one more apple. So our fruits array list size got increased to store one more element. So this is what resizable means. So what if if you use this constructor instead? While creating our ArrayList objects with initial capacity, let me do this. So instead here let me pass ten. So here I have given initial capacity ten. Now since I am adding 11th element it doesn't giving any error. Let me run this and see what is going to happen now. So if I run this Java code you can probably see it doesn't give me any error. Instead it just included that element. Apple. Apple. Apple like 11 times. Got it. So here you can see its implementation details. Constructs an empty list with the specified initial capacity, the initial capacity of the list in which we are passing the initial capacity parameter. And it just throws an illegal argument exception if the specified initial capacity is negative. So if you pass negative value here instead of ten. If I pass minus ten here, it probably going to face that illegal argument exception. So here we only be able to pass positive elements. So basically here I have given. Let me just remove this minus. Now here I have given initial capacity as ten. Whereas in if I don't pass this ten. So on our empty call also it just initiating a empty list of ten capacity. So both are same. What is the difference between these two. So instead of giving initial capacity as ten, if you want to assign initial capacity more like a 2030, then you would probably using this constructor with initial capacity. In such case, like if you want to give initial capacity as 20, you would just use ArrayList constructor like this. Or if you want to just give initial capacity as five, then also it makes sense like if you give five here, but if you want ten, then you just don't give any initial capacity because on start it will be having initial capacity of ten. If you don't pass anything here. So that's the main difference. So basically these three constructors ArrayList constructor with initial capacity without any parameter and with collection. So these three constructors for ArrayList class in Java serve different purpose for initializing an ArrayList. Basically, this constructor creates an empty array list with a specified initial capacity, as I have said earlier. So here this initial capacity defines the initial size of the underlying array, that is, the number of elements it can hold before needing it to resize. And why do we need to specify this parameter instead of calling it as like empty ArrayList. So specifying an initial capacity can improve performance. If you know in advance the approximate number of elements the list will need to hold. This minimizes the number of times the internal array needs to reset as elements are added. So that's where initial capacity comes into action and improves the performance. If you know how many elements which our list is going to contain in advance. And it is also useful in exception handling, like if initial capacity is negative, it throws an illegal argument exception. Right? So that's the main reason we use ArrayList with initial capacity. And we have default constructor ArrayList with empty parameters. So this constructor creates an empty array list with a default initial capacity typically set to ten. So default capacity means. Here. Internally, this constructor sets the initial capacity to ten by default when elements are added for the first time, this means the internal array does not actually allocate space until the first element is added, thus optimizing the memory usage. And why do we need to use this constructor instead of using this with the initial capacity? Because this is the most commonly used constructor when you don't have a specific size in mind. It provides balance between memory usage and performance without needing any parameters. That's the reason why we use this default constructor without parameters. And we have this constructor also with the parameter as a collection. So what does this actually do? So this constructor creates an array list containing the elements of the specified collection in the order they are returned by the collection iterator. So basically this constructor allows for creating an array list directly from another collection like set list, etc. copying all the elements from the collection into the new array list, and it also provides efficiency. If the input collection is already an array list, it directly uses the internal array of this existing array list, which is more efficient as it avoids copying elements. If it's not in a list, it copies the elements into a new array list, and this one also helpful in exception handling, because if the specified collection is null, then it just throws an exception such as null pointer exception. Now let's explore this example also. Let me create a set here. So set with string. Let me give it as f of t is equal to new hashset. And let me give f of t s dot. Add grapes and let me do this one. Also let me add banana also. So I have added these two elements in our efforts. Now if I do this like uh, so here while I'm calling, let me pass this collection efforts instead. Now if I run this you can probably see grapes banana added at the start of the ArrayList. So what it does is it just created a new array list and added these two elements first. Then it has. Added all of these other elements into our array list. So what if. Instead of this set, we have an array list of a separate array list. Itself array list instead of hash set. Let me call array list again. Now let me remove these extra things. Okay. Now this is an array list. So if we pass array list object. Here instead of creating a new array list it just utilizes its space. Now it directly uses the internal array of the existing array list which is. Efforts. So which makes it more efficient as it avoids copying elements. So if not an array list in such case a in our previous case like we have given set right. In such case it copies elements into a new array. So that's the difference. When you are passing here the collection of same type, which is array list when passing the collection which is of different types such as set. Okay, so as we have explored these three constructors default ArrayList and ArrayList with collection and ArrayList with initial capacity parameter. Now let's explore other methods of our ArrayList implementation. Okay, so next we have this method trim to size. So what does this method actually do. So basically the primary purpose of this trim to size method is to reduce the capacity of the ArrayList to its current number of elements. You can say this by reading its name itself, right. Trim to size C. So by default an ArrayList may have additional unused space or unused capacity to accommodate future additions without resizing. As we have already explored in this ArrayList with initial capacity, right? So in such case. So. However, if the application no longer needs that extra capacity, for example, after most of the elements have been removed. Calling trim to size method. Calling this method can free up this unused memory. Whatever extra capacity which is remaining in our array list, it will be freed up from the memory. Let's suppose. Let me remove this. So currently we have capacity as ten, right? If you won't pass anything here, it would be ten, right. So let's suppose we have passed only three elements. And in this fruits array list we only want to include these three elements. And we know after this we won't add anything else in this array list in such case. Calling this fruits dot trim to size method free of that. Uh sorry. Fruits, right? Now, this call would free up that extra size, whatever the extra size. After adding these three elements we are having in this fruits array list, which is ten minus three seven. Right. So that extra seven space will be removed from our fruit surrealist after calling this trim to size. So using this method have some use cases such as. One is memory optimization, because when an array list is known to be, it's in final form. No more elements will be added or removed. Trimming to size can save memory by releasing unused airspace. So that's why we are considering this memory optimization as it's one of the use cases. And next use case we have here is final state preparation. So what this actually means. So before saving an array list to disk or sending it to our network, trimming it to size can reduce the amount of data that needs to be serialized or transferred. And we have one more use case, such as memory constrained environments. It is useful for memory constrained environments, such as in applications where memory usage is critical. Calling trim to size method on large lists that have shrunk in size can help reduce the application's memory footprint, so these are the use cases of using trim to size method. So despite of being this much helpful, this method also has some downsides such as performance impact. So calling trim to size method has the time complexity of order of n because it involves copying the entire array if an array list is frequently modified. Calling this method repeatedly can lead to performance issues due to repeated array copying, and it also has one more downside, such as relocation overhead. What exactly mean by that? It means if the array list is trimmed and later need to be grow again, it will incur the overhead of relocating the copied array to a new large size. This can lead to performance degradation if not used judiciously. So basically the trim to size method is useful tool for optimizing memory usage in ArrayList. It reduces the capacity of the internal array to match the number of elements, freeing up any unused memory. While this method can save memory, it's important to consider its performance applications such as especially in scenarios where ArrayList may continue to grow after trimming, which might cause some degradation issues. Now let's move to our further implementations of our ArrayList class. So after trim to size we have this ensure capacity method. So here the sewer capacity method in which we are passing minimum capacity in the ArrayList class is used to increase the capacity of ArrayList. If necessary, this ensures that it can hold at least the number of elements specified by minimum capacity. This method is useful for optimizing performance by reducing the number of times the internal array needs to be resized when adding elements. So basically, the primary purpose of this method is to ensure that the array list has enough capacity to accommodate a specified number of elements, such as minimum capacity. This method is typically used to avoid frequent resizing operations when the array list is expected to grow significantly. By preemptively increasing the capacity, this method can help improve performance by minimizing the need for expensive array resizing operations. Suppose you are creating an array list when you know our array list, at least store 1000 elements. In such case, you ensure capacity on that list you have created now. So in our case, we have created this fruits list and we know we are going to include at least 1000 fruits in our array list object. In such case, what you would do is you call insert capacity size on our array list, such as 1000. Here I'm passing min capacity as 1000. So this method ensures now you will be having 1000 capacity on our fruits. So by calling this insert capacity of 1000, you instruct the array list to allocate enough memory to hold at least 1000 elements. This prevents the list from having to resize multiple times as elements are added, improving the performance. So basically this insert capacity method is a performance optimization tool in the ArrayList class. It ensures that the internal array has enough capacity to accommodate a specified number of elements, minimizing the need for resizing operations by preemptively increasing the capacity. Insert capacity helps improve performance, particularly when dealing with large datasets or when the list is expected to grow significantly. However, it should be used judiciously as unnecessarily increasing the capacity can lead to wasted memory. Obviously. Right. So that's what inside capacity means. So next we have this grow method in which also we are passing min capacity. So if you read its implementation details here, it is used to increase the capacity to ensure that it can hold at least the number of elements specified by the minimum capacity argument. So it is similar to what we have read on our insert capacity. Right. So this one also uses to increase the capacity. So what exactly the difference between these two. So here the grow int method the grow of min capacity and ensure capacity of min capacity. These two methods in ArrayList class both relate to increasing the internal capacity of our ArrayList to accommodate more elements, but they serve different purposes and are used in different contexts, such as. This grow method is a private method specifically designed to perform the actual resizing of the array lists internal array to ensure it can hold at least number of elements specified by the min capacity. So you can't call this grow method from our array list objects. So if you try to do this fruits dot grow. See you can't see that method. It is showing min capacity but it is giving error because this method is private. You can't access like this. So if you observe this inside capacities implementation here you can see this method is actually calling this grow method. So whenever you call this ensure capacity. Whenever you use this inside capacity method you are definitely going to use this grow method. So ultimately this method is calling this grow method to increase the actual capacity of our array list. So this method here Directly modifies the internal storage array by creating a new, larger array and copying the existing elements into it. So what actually does this grow of min capacity would do? Is this method calculates the new capacity using a helper method. Typically new capacity of min capacity, which determines a suitable size that is larger than the current capacity, but not optimized for memory usage. It then creates a new array of the larger capacity and copies the current elements into this new array. And next we have this grow method. This one is overloaded method of our actual grow and capacity method. So in this grow method we aren't having any min capacity like that. So what it does is so it increases the capacity of ArrayList to ensure that it can hold at least one more element. So here you can see in its implementation it's doing size plus one. It's just utilizing the this grow method so as min capacity just passing this one actual size plus one, which means it increases the capacity of the ArrayList to ensure that it can hold at least one more element than the current size. This is a convenience method used internally, not externally, because this one also is a private. It is used internally when adding a new element to the ArrayList. Let me prove this to you. So while we are adding a new element to our array here, we are actually calling this grow method. See, in add method, we are actually calling this grow method so that while adding new element to our ArrayList here we are ensuring that our ArrayList must have the corresponding size to add that particular element into our ArrayList. So that is why it is calling grow method, so that it can increase one more element space into our ArrayList. So that's all about grow guys. Now let's explore these two methods new capacity and huge capacity. So if you have observed our previous methods code in this group, we are actually calling new capacity while copying our array, right? So whenever you call this method ensure capacity we are indirectly calling new capacity method also. So what this method new capacity actually do and what kind of output it is returning to this grow method. So the method new capacity and huge capacity are part of the internal implementation of the ArrayList class. So if you see for both methods it has private keyword mentioned, which means you can't access these methods through objects directly. And these are responsible for calculating new capacity when the internal array needs to grow. These methods ensures that the ArrayList has enough space to accommodate additional elements while managing memory efficiently and preventing overflows. So basically this new capacity method calculates a new array size for the array list based on the desired minimum capacity, typically increasing the current capacity by 50% to balance memory usage and performance. If this calculated size exceeds the minimum allowable array size, which is nothing but this max array size, it causes the huge capacity method. The huge capacity method handles cases where an extremely large capacity is needed, returning either max array size or integer dot max underscore value to prevent the overflow. Together, these methods ensure efficient resizing of the internal array while preventing excessive memory allocation or overflow errors. So that's what these methods actually do. Now let's explore our next method size. So as you can see it just returns the number of elements in the list. Let me prove this to you. So currently we are having three elements on our list. And let me do this fruit start size. So basically what it returns is it returns an integer. Let me comment this thing also. And if I run this now you can probably see the answer as three since we are having only three elements in our list. So that's what size two. And next we have Isempty method. So it basically returns if this list contains no elements. If it is empty then it just returns true. Or if it is having some elements then it probably returns false. So currently we are having elements right? So if we call isempty on it then you will probably getting result as false. And here we got it. Now let's move to the next method which is contents. So it probably checks for an object on our collection. So whatever object we have passed here if it finds that objects in our collection, then it probably returns true since it is boolean type method. So if that particular object it won't find on a collection, then it just returns false. Let's also explore it. So contains and let me pass object as apple. So if I run this you probably gonna see answer as true since Apple is resides in our fruits. So instead of Apple, let me pass something else. Banana. I run this now it probably get result as false since banana object is not present on our collection as of now. Now let's move to our next method which is Indexof. So it probably returns the first occurrence of the specified element in the list. So currently we are having only Apple. Let me add before Apple, let me add some more elements to it. Start add banana. Let me copy paste this thing also and let me mix these elements. Now if you see on index one we are having first occurrence of our apple. Right. So when we do this instead of contents let me give method as index of. And here I'm passing Apple as an object. Now it probably now it should probably return the first occurrence of this apple. So first occurrence is at index one. Right. So it probably returns one as a result. If I run this I can probably see one as a result. So what does this actually do is so while returning it just calls index of range method in which it just pass the particular object. And then it specifies the range for starting index. It is giving us zero. And for ending index it is just mentioning this size size of that particular collection which means the end index. So that's why this method checks for the complete range of the array. Next we have the last index of method. So what does this method actually do? Is it just returns last occurrence of specified element in the list. So just opposite to our index of method. So it just returns the first occurrence. Whereas this last index of returns the last occurrence. So if we see this example again so here what is the size of it. 1234567. Right now the last occurrence of it would be the six. If we call last index of since our index starting from zero. That's why now we are going to get the result as six. See we got the six. As a result it just return the last occurrence of our Apple object. Now let's move to the next method which is clone. So here the clone method in the ArrayList class creates and returns a shallow copy of current array list instance. So what I mean to say is let me just prove this to you practically for better understanding. So currently we are having this array list right fruits object. Now if I want to clone this array list let me just do it like this. Array list string. Clone the list equal to. If I do this ArrayList string and if I call our fruits array list. Then upon this. Here I'm going to call clone method. So if I do this, this clone method is called. On this fruits array list and clone released are printed initially. They both contain the same elements as banana, apple, banana, apple, banana and apple. So if I print cloned list now, you can probably see the result as same for both. So we are having these elements right now if I print our fruits ArrayList. Now also you'll probably gonna see the same result. So currently these two containing the same elements. It just cloned the fruits array into cloned list array. So basically the contents of both fruits and cloned lists are printed are containing the same elements. So after creating cloned list, if we add some elements into it like a cloned list dot add if I pass cherry in it. So let me print cloned list. And here let me print my cloned list. And after this let me also print our fruits list plus fruits. Okay, now if I run, this cherry is getting added to our cloned list, but it doesn't get added to the fruits list because specifying that these two are separate array lists. Now, modifying contents of this cloned list won't affect on fruits list. So this example shows how clone method can be used to create shallow copy of an ArrayList, allowing modifications to the cloned list without affecting the original one. So that's the use of clone method in our ArrayList implementation for our ArrayList collections. So as we have explored previously, this method to array. So you just use it to convert our actual ArrayList collection to the normal array. So as we have already know that I'm just skipping it. And then we have two array with some parameter. Just another type of doing converting our array which also we have already explored. And here we have get. So it basically returns the element at the specified position in the list. So if we do this getting element at index two and upon it let me just call dot get two. So at index two we are having banana. So now we can probably get banana as our result. See? So that's what get do. Now we have set method. It is probably used to replace the element at the specified position. So in which we are going to pass two arguments one is index and one is actual element which we want to replace it with. So if we do like this as of now let me remove all of these things. And here at this position at index six I want to just replace it with salary. Or here we are containing these two bananas one after another. Right. Let me just replace this banana at index five to the cherry. Let me do this. Fruits dot set at index five. I am just want to replace the element with cherry. Now if I print our fruits again let me before going to run this let me clear the screen. And if I run this now you can probably see it. Index five cherry got added. Now let's move to our next method which is add method. So it probably adds an element where using this method since we have started this course. So don't need to explain it again. So for that let me just skip that explanation. And here we have these two add methods. One which basically adds an element and one which adds an element at particular index. So since our list having index based access so we can add particular element at particular index. And next we have this remove method. It probably removes element at particular index. Whatever index you have passed. It just removes the element at that particular index. And we are having one more remove method to remove a particular object. Instead of removing the element at particular index, see this one is remove with object. So we are having two remove methods one with index, one with object. And next we are having equals method. So it probably checks for the equality as we have also explored this one in our previous examples. So if you have already watched our previous examples, you might already know what this method would do would actually do. It just checks for equality between two objects. And this equal range is helper method for this actual equals method. So here you can see here this equals method at this position calling the this equals range method. And we can directly access this equals range method. So let's just ignore it. And this equals array also a helper method for our equals method. So here we are calling that equal. So here it is mentioned equal array method in our equals method. Since it is private we can't access this method also directly. And this check for modification method is helper method for this equals ArrayList method and this one also private. So basically all the helper methods you will seeing them as set it as private, which you won't be able to access them through actual ArrayList objects. And then we have this hashCode method. So what does this actually do. Is it just returns the hashcode of a particular collection. Whatever collection you have called upon, it just returns that particular hashcode and it is inherited from our collection interface. You might probably using this hashCode method for the consistency to check consistency with equals method. So basically the hashCode method is overridden in ArrayList to ensure consistency with equal method. This means that if two array objects are considered equal according to the equals method, they must return the same hashcode. This is a fundamental contract of the hashCode method, which is crucial for the correct functioning of hashCode based on collections like HashMap and Hashset. So if you see in equals method and next we have hashCode range method, which is probably a helper method for this hashCode, just like other helper methods we have explored previously and then remove with object, we have already explored it in our previous videos, so don't need to worry about it now since we know what it does actually do. And next we have the first remove method which is new one. So it just basically and it is a helper method for our I mean remove with object method. What it does is it is used to remove an element at specified index I from the array lists internal array. It performs the removal operation more efficiently by directly manipulating the array without performing any bounds checking, such as verifying if the index I is valid or returning the removed remote element. This method is typically used internally by the other ArrayList methods, such as remove method that have already performed the necessary checks before calling first remove. So these are those checks I am talking about. Next we have this clear method. We all know what it does by reading its name. You can say that it removes all of the elements from the list. The list will be empty after this call returns. So no matter what collection you call this clear method upon, it just removes that entire collection. So actually it doesn't remove the entire collection, just removes the all elements from that particular collection. Finally, by keeping our collection as empty and then we have add all method to add collection to our current ArrayList, and then add all with index to add all of the to add a particular collection at particular index. This one. Also we have explored in our previous videos while exploring list interface. And next. We have a lot of helper methods here. Let me check for public ones. So here we have this public boolean removal. So basically this removal method removes all the elements which you are passing here from the collection which you have called this removal method upon. Unlike clear where clear just removes everything from collection. Whereas removal taking a collection and checking all the elements which are present in the past collection and then checking upon the same elements on the call the collection. So if there are matched elements, then it just removes those matched elements. Basically it just does the intersection. Rather it does not actually the intersection. It just mimics that behavior. Like whatever elements are common, it just removes those elements from our current collection. And unlike removal. Here we have the retain all method. It just retains the elements in the list that are contained in the specified collection. It just retains those particular elements. Those are common in these two collections. Let me show this to you. Let me create an another array list with the name Fruit Store. And in fruit store let me add some elements. Fruit store. Dot add grapes. Fruit star two dot add banana fruits two dot. Add mango okay. And fruit store dot add cherry. So what are the common elements we have. Banana and cherry right. So after calling retain all it probably having fruit start retain all fruits two. Now if we print fruits now you can probably see cherry and banana as result. Sorry I have called removal here. I need to call. Okay. However, here you can see it just removed the common elements like a banana and cherry. These are the common elements, right? It has removed those elements and just printed out apple. So instead remove all let me call return all. In such case you will probably see banana and cherry as the result. So here we got banana three times since we are having banana three times in our previous list. And cherry. So these are the two common elements. It just retains those common elements instead of removing just removes the other elements. That's what return all do. Just quite opposite to removal. Next we have the batch remove method, which is a helper method for both return and removal method. So in both cases we are using this method itself batch remove. And next we have another private method right object. And then let me go to the public method only. Okay, here we have listed traitor, which we have already explored in our previous video, which just returns the list iterator over the elements in the list starting at the specified position in the list. Since it has having index, and this list iterator only works on the list implementations such as linked list, array, list vector, etc.. So here we have empty list iterator method, overloaded method of this list iterator which is containing the index. And then we have iterator inherited from our collection interface. It just returns an iterator over the elements in the list in a proper sequence. Since our list having the index based access. That is why it returns elements in the proper sequence, whereas in other interfaces implementations such as Hashset, you won't see the sequence for iterator, and then we have for each remaining. It is a overridden method from our list interface. We are also having this in actual collection interface itself while exploring those interfaces. We have already seen example of this for each remaining, so don't need to explain it again. So that's it guys. So these are the methods we are having from this point. We are having separate class so don't need to explore it. So since we are not gonna use them directly let's just ignore as of now. So these are the methods we are having as of now in our ArrayList class. We might be having some more new implementations in future. So as of now, currently at 2024, these are the updated methods in Java list class. So that's it for our introduction to ArrayList guys. In the next video, we'll explore the linked list class and see how it differs from ArrayList. So thanks for watching. I will see you in the next one. Bye.