Hello everyone. Welcome back to our Java Collections Framework course. In the last video we have explored vector and stack classes. Today we will be diving deeper into a key concept that sets these classes apart from the other classes such as ArrayList, linked list, etc. and we're going to discuss how synchronization works in both vector and stack and its advantages and some of performance tradeoffs. So let's jump right in and explore with the question what is synchronization. So in Java, synchronization ensures that when multiple threads access a shared resource. Let's suppose this is the shared resource. It might be a vector or stack. When multiple threads trying to access these this specific shared resource only one thread any one of them. It may be so. Only one thread can modify the resource at any given time. So at a time only one thread can modify this resource. This type of functionality prevents data inconsistencies that can arise in multi-threaded environment. When multiple threads try to add or write to same data simultaneously. That's what synchronization means, and you may think of synchronization as a traffic light system. So if this traffic light, this signal we have multiple ways of routes okay. Now we can only allow one side to pass to other side at this collision point. Here we can only allow one way vehicles to pass to other side. Let's suppose this is the junction. So here we have vehicles at this position and everywhere like this. Let's suppose this is a two way road. So here we allow first only these vehicles to pass to other side. Either it may go to this side or it can go to straight or it can go to this roads, this side. Okay. We can't allow vehicles on each road to go at a time. It may cause collision at this point. You may see accidents if you do that. So here on traffic light system also we are following this synchronization rule. Only one thread can access the shared resource at a time. Only one side of vehicles can access this road at a time. After clearing this road we can allow other side to go. After that we can allow next step to go on. So that's how traffic light system works. Just like that synchronization also in our Java works in similar way. So here we have vector class right. So this vector class if I open its implementation this vector class is synchronized by default. How can you say that. So if you see each and every method in our vector class. Other than constructors, you can see the. Each and every method in our vector class is synchronized. See. Copy into is synchronized. Trim to size is synchronized as well as ensure capacity also synchronized. You can see synchronized and private ones since you can't access it from outside wall. So don't worry about them. So all the public methods are synchronized in our vector class. This means that if one thread is adding an element in such case, no other thread can modify the vector until the first operation completes. Let's suppose here you are using this method set size. So when you are setting this size to our vector, it ensures that here we have synchronized keyword right. So it ensures that at that particular time when one element is when you are setting a size on a collection. When you utilizing this vector resource, then no other thread can perform this set operation and at that time it can do any other operation on that particular resource. Got it may see every method that modifies the content of a vector like add remove set is wrapped in a synchronized block. This ensures that only one thread can execute these methods at a time. Now let's look at how this works internally. For that here I have created an example file vector synchronization example. So here I have created a vector object with the name shared vector okay. And now let me create two threads so that these threads can access this common shared resource at a time. And we will see is they really accessing this shared resource at a time or our vector just taking one thread at a time. We're gonna see it. So for that, let me just create two threads here. So to create those two threads I'm going to create a worker class first. For that let me create a worker class here. Class worker which extends thread class. Now our worker class will get thread class properties. And under this worker class I'm creating a private vector of integer type. So here I have created a vector object. And let me create a constructor for this which will be public worker of. Here I'm passing. Here I'm accepting an integer vector object along with a string with thread name. So after getting these two values from our worker object, I'm just calling super method. And in this super method it will just call our thread class constructor. Right? And you do super, it will just call this one public thread. Here it is accepting a string name right? So here I am just passing this thread name as string to this thread constructor. If you don't get this, don't worry, you will eventually get the concept what we are doing here once after finishing this whole code and then here, I'm just simply using this operator to set our current vector object value, which will be integer to this class vector object. And here I am overriding run method of thread class. Now public void run. So if you see in thread class we have this run method already mentioned in it. So here we are just overriding this read method into our example. And then we are implementing this run method according to our need. So here I'm calling for loop here and I equal to zero I less than five. I want to repeat this loop for five times. On each iteration I'm incrementing the I value. So that's the basic. Now I'm doing this calling synchronizer of vector passing the vector object in it. And in this. Now I'm trying to get the current time to see when each thread is accessing the vector. For that we can use current time millis method. Long current time equals to system dot current time millis to get the current time in milliseconds. And here I'm just printing out system dot out dot println. First of all I'm getting the current thread's name Current thread dot get name. So we have already set our name to thread right? Here you can see here we are setting our name to thread. If you see string name. So whatever thread we are currently using, that thread's name will be updated to our third class. So by using thread class get name method. So if you do this get name here we have this get name method. It just returning the name which we have set by calling this super constructor. So it just basically returns our current thread name which is currently accessing our shared resource. Okay then I am just printing a message is adding here I want to add a value to our vector at current time. So whatever time we have captured and print just printing that current time. So after printing it and simply adding element to the vector by using vector dot add method. See here I'm just passing a value. So after adding let me again print the time. Let's print the current state of the vector to demonstrate that only one thread is modifying the resource which we have shared at a time. For that, also, let me call assert and hit dot current thread dot get name first to see the current thread's name, then updated vector. Let me also print our updated vector values we saw at the time. So this is the time after updation okay, current time here. By doing this, we can see how much time it took to add that particular element. So whenever a thread access this shared resource we are capturing the time when it access the current thread and after doing operation add operation. Then also we are capturing the current time to see when that particular thread leaving the or stop using the current resource. We are just capturing the time, leaving time. Since we all know that only one thread can access our vector shared resource. Because of that reason, you can see different timings on each thread. If multiple threads can access that particular shared resource at a time, then you will see the same current time, right? But here you can see the different current timings of accessing that resource. After running this program, you will get the proof. Why I am saying that. Okay, so after printing all of this lets slip our thread for certain time. For that here I am going to use the Thread.sleep method. Since our thread Thread.sleep method throws an exception known as interrupted exception. That's why I'm keeping this in a try catch block sleep to simulate delay. Here I'm using a dot sleep method. Sorry. Sleep right. And let me pass value as 1000. Passing 1000 milliseconds. So after doing this operation and printing the timings of entering and leaving, just wait for or it just halts the thread use for 1000 milliseconds. Let me pass exception here as interrupted exception. And let me just print out a dot print stack trace. So that's it. Now here we can create two threads. Now of worker. Class worker. Worker one equals to new worker. And in this let me pass our shared resource which is shared vector. Right? And let me pass its name as worker one. And let me repeat this thing again. Let me give its name as worker two now. And here also name as worker two. So we have successfully created two threads now. Now we can start our threads by using the start method. Worker one dot start. Worker two dot start. So after starting we can wait for both threads to finish jobs by joining worker one dot. Join on worker two dot join. And finally we are just printing out the updated shared vector. So if I run this now see running it as Java. Now you can see first it accepts the worker two, which means first this thread is accessing the over shared resource which is nothing but shared vector at the time. five. Five. Seven. Okay. And it is updated. The vector added the zero c and after printing here you can see our updated vector as zero. And it just took no time to add it. You can see the difference between these two. If you remove on this time, you can see the difference how much time it took to add that particular element. Okay. And then after leaving worker two from Shared Resource. Now worker one is accessing our shared vector. And here you can see it is adding at the time 617. And this one also took no time to add the element on this one. Worker one also added the zero and after some time now worker two again adding the element one at the specified time 618 and this one. This operation also took no time to add that element. See 001. Now Our updated vector after it. Our worker. Worker one. Also adding one at 618. So it is happened. This operation happened in no time. That's why this one also accessing the same time. But once after leaving worker two. Worker one access it at that time added that particular element. Currently I'm using a high end laptop. That's why you can see these timing in with small variations. So if you are using a low end device, you can clearly see the difference between one thread accessing a shared resource and then other thread accessing the shared resource again. Okay. And then you can see worker one updated vector 0011 at no time all. Let me just pause it for 2000 seconds delay. Then you will probably see the variation. I hope so, let me run it. Yeah. Now you can see the variation clearly. So this took five five. And now worker two is accessing A2011 now. Worker one is accessing it 012 then worker two again accessing our shared resource with 013. Now like this, you can see the variation in timing, which proves our point that only one thread can access our vector or access our shared resource at a time. So after leaving one thread, the other thread can make use of that shared resource. So that's how synchronization works in our vector. Now you can see the final vector content as 0011223344. Now do the same operation using the stack. So that's the work for you. Go and change vector to stack here since our stack also extending our vector class. It also have some synchronized val methods such as Pop Synchronized Peak. So go and perform these operations and explore how stack is performing the same operation if you do. So to wrap things up, while Vector and Stack offer built in synchronization, making them thread safe, the performance tradeoff can be significant, especially in high concurrency environments. In today's multithreaded world, you might want to consider alternatives from the Java.util.concurrent package for better performance and scalability. That said, understanding how these legacy classes works is still important, especially if you are working with older codebases or need simple synchronization out of the box. So in next video, we will dive into the more modern alternatives in Java.util.concurrent package and see how these compare to Vector and Stack. So thanks for watching and I will see you in the next one.