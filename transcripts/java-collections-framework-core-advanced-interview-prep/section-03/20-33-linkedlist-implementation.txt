Hello everyone. Welcome back to our Java Collections Framework course. In today's video we are diving into another core collection linked list. So linked List is a powerful data structure that offers several advantages over other collection types like ArrayList, insert and scenario. So by the end of this video you will understand what is linked. List the key differences between linked list and ArrayList, when and why to use a linked list and how to work with linked list in Java. So let's jump right in with a simple question. What is a linked list in Java? Linked list is a doubly linked list that implements both list and Dick. So here in this hierarchy you can see it is implementing both list and Dick. And it is a part of Java Dateutil package. We all know that an it functions as both list and queue, which gives it more versatility than a typical array based structure. But what does being doubly linked list mean? Well, each element or node you can say in a linked list contains three parts, such as a reference to previous node, the actual data. It is nothing but the value of the current node, the value of the node, and the reference to the next node, a reference to the previous node, actual data, and the reference to the next node. This is what doubly linked list means. So because of these links between nodes, you can navigate both forward and backward throughout the list or throughout the queue. This provides flexibility when it comes to operations like insertion and deletion. Now you might be having question. So tell me why Java developers consider. Doubly linked list for our linked list collection class. Don't we have singly linked list in Java? So in Java, the decision to implement linked list as a doubly linked list rather than a singly linked list comes down to performance, flexibility and functionality. So these are the reasons why they consider doubly linked list for our Java collection class. While three isn't built in singly linked list, however, there isn't any built in singly linked list in Java collections framework. Let's explore the reasons why Java linked list is doubly linked list. So first reason is d, q and q operations. S, d, q and q operations. Java's linked list implements both list and decorate. Both list and dequeue interfaces, which means it functions as both list and queue. A doubly linked list is ideal for supporting efficient bidirectional operations. BI directional means forward and backward, which is possible in doubly linked list, but not in singly linked list. We all know that singly linked list follows linear direction, and this bi directional operations required by these interfaces such as dequeue and queue, such as dequeue operations like add first, pull first, pull last. These operations requires constant time. I mean order of one access to both front and back of the list in a single link list. Accessing the last element is costly because as you have to traverse the entire list to reach the end of the list, whereas in doubly linked list it provides a quick access to both ends, its start and it end for both ends. Our doubly linked list provides quick access, making it more suited for implementing queues. And next reason is efficient insertions and deletions. So in linked list operations like add a particular index or remove it index must be efficient of any index, not just the head such as in linked list. So in singly linked list doing these operations head only queues the constant time. If you want to do these operations at the end or middle, it just takes some time. Like it can have up to time complexity of o of n order of n, which is a linear time complexity, whereas a double linked list, on the other hand, allows efficient insertion and deletion from both ends and any position within the list. Because each node has a reference to both its previous and next nodes, right? So this makes linked list more versatile for scenarios where elements are inserted or removed frequently, Both from the front, back or middle of the list. And one more reason is obviously bidirectional traversal, because Java's linked list allows bidirectional traversal using iterators such as listiterator, which we have already explored while exploring our list interface. Right, so this can move both forward and backwards. This is enabled by the doubly linked list. This is working on our list only because of doubly linked list. If you have implemented singly linked list, then this bidirectional access might not be as responsive as in doubly linked list here. In such case, a singly linked list only supports forward traversal, since each node only contains a reference to the next node. Whether. Whereas bidirectional traversal is essential for operations like reversing a list, backtracking, or efficiently implementing algorithms that require moving in both directions. So that's the one more reason why they have considered doubly linked list as forever linked list. Java collection class. And you might be having a question where there is no built in singly linked list in Java collection. So while it doesn't, while Java doesn't provide a built in singly linked list, it's fairly easy to implement a custom version of your own forward traversal and create simpler operations on it. However, this would come at the cost of flexibility. You won't find flexibility, such as you can find the flexibility on a doubly linked list. So in a singly linked list, while adding or removing it, the head is efficient. Operations at the tail are at an arbitrary index request. Linear time traversal, which means it requires O of n time complexity, whereas you can perform similar operations with the time complexity of O of one in doubly linked list, so Java designers opted for a more versatile data structure in the linked list class to cover a broader set of use cases where both forward and backward navigation, efficient dequeue operations, and frequent insertions and deletions are required. So that's why they have chosen doubly linked list for our linked List collection class. So why they do not consider both? So the designers of Java Collections framework likely to choose not to implement built in singly linked list because a doubly linked list can do everything a singly linked list can do, plus more with relatively minimal overhead. And the trade off in memory usage means storing an extra reference to the previous node is often worth the increased efficiency in handling more operations, so Java's decision to choose a doubly linked list for the linked list class ensures that it can efficiently support dequeue and queue operations, with fast access to both ends, and bidirectional traversal and frequent insertions and deletions from both ends and arbitrary positions here. Arbitrary positions means any position in between the collection, so if you only need the functionality of a single linked list. Example. Simple forward traversal. You can implement it yourself, but Java Standard Library opts for the more versatile doubly linked list. So before we move further, let's compare the linked list with ArrayList, something which we have covered in our previous video. So our first reference is on its underlying data structure. Its structure. Where ArrayList uses a dynamic array, it means an ArrayList internally uses a resizable array to store elements. A dynamic array grows and shrinks in size as necessary. When the array's capacity is exceeded, a new, larger array is created and the elements from the older array copy it to the newly created array with more size. And when you add elements to the array list, it may not immediately create a new array if there is available space. However, if the array is full, a resizing operation occurs, copying all of the elements to a new array, whereas list uses a node's connected via pointers or references we may call. A linked list is made up of individual nodes, where each node contains the data value stored at this node, reference or pointer to previous node and reference or pointer to the next node. Because it is a doubly linked list, these nodes are not stored continuously in memory, but are spread out in different locations linked together by these pointers. And when we talk about memory difference in array and linked list array lists store elements in a contiguous block of memory. We all know that which can make resizing costly in a list. All elements are stored sequentially in a memory in a single block of memory. Mean contiguous blocks of memory. When the array runs out of space, it needs to grow. So Java allocates a new block of memory, typically larger, and then copies all the elements from the older array into the new array. This resizing operations takes O of n time because each element needs to be copied. This continuous memory allocation provides fast random access, but makes resizing costly because of the copying involved. Whereas in a linked list, since nodes in a linked list can be located anywhere in the memory and are linked by the references, so here references means these links are pointers, so these nodes might be anywhere in the memory location, not contiguous and linked by these references. So here you don't need to allocate a larger contiguous block of memory. The linked list can grow and shrink dynamically without need for resizing operations like any released. However, while linked lists avoid the cost of resizing, they come with memory overhead due to the need to store two references next and previous for each node. This means that in terms of memory usage, linked lists are less efficient per node than ArrayList when it comes to overhead associated with each element. Now let's talk about the difference between access time between both ArrayList and linked list. So ArrayList provides o of n time complexity for random access. Here O of one random access means you can directly access any element in the array in constant time. This is because elements in ArrayList are stored in continuously, and Java can compute the memory addresses of any element by performing a simple calculation such as starting address plus. Index into size of element. Starting index plus. Index into size of element. For example, if you have done this ArrayList dot get of five. This will immediately retrieve the sixth element as the index is directly translated into the memory address. Whereas in a linked list you can jump directly to an element because the nodes are not stored continuously in memory. If you want to access an element at index five, for example, you need to start at the head or tail, maybe of the list, and traverse the nodes one by one until you reach the fifth node. This traversal requires O of n linear time complexity. In the worst case, you might have to visit every node in the list because you might have to visit every node in the list before finding the one you need. So an access time array list provides the better performance than linked list. So now what about insertion and deletion? We all know that for insertion and deletion, ArrayList have O of n time complexity for inserting and deleting an element at any position other than the start and end while in between at any position, it needs to have the O of N time complexity for insertion and deletion. When you insert or delete element in the middle of an array, list all the elements after the insertion or deletion point need to be shifted. For example, inserting at index three means all elements from index three onward must be moved to one position to the right. Got it? Similarly, deleting an element requires shifting all elements left by one position. This shift takes O of n time complexity, where n is the number of elements after this insertion or deletion point, whereas in linked list. Linked list can insert or delete elements at the beginning, middle, or even at the end with the time complexity of O of one. In a linked list, inserting or deleting an element is efficient. Once you are at the desired node, you do not need to shift elements. Instead, you simply adjust the next and previous pointers of surrounding nodes. Let us suppose we have these three nodes and these are linked. Now if if you want to remove this node now you can just adjust these links. Instead of pointing this to here you can point this new link to this position. Let me provide it in a different color. So this is the new link. You have just removed this link and point it to this link. And here also we just removed this link and pointed this link to this node. And here removed this link also and this link. So by doing this you can simply remove that particular element. This makes insertion and deletion at any position in the list, such as beginning, middle or end, an O of one operation or of one operation if you already have a reference. What if if you don't have any reference so the time complexity becomes O of N? If you don't have the reference and need to traverse the list to find node from start. So now let's recall our overall points on underlying data structures. ArrayList uses a dynamic array, while Linkedlist uses a nodes with pointers. Unveil memory. ArrayList uses contiguous memory location with resizing overhead, while linked list uses non-contiguous memory with extra overhead for storing pointers while on access time. ArrayList provides O of one time complexity. I mean fast random access, whereas linked list provides O of N time complexity or O of n access time so that it requires traversal. So when we talk about insertion and deletion, ArrayList have O of n insertion or deletion time, whereas linked list has O of one insertion or deletion. If the node is known, if you do not have the reference, then here. Also you might have to face O of n time complexity. So these all are the main differences between ArrayList and linked list in our collection. Let's now talk about when you would want to use a linked list or a ArrayList. So when frequent insertions and deletions are needed, if your application involves adding and removing elements frequently, especially from the middle or start of the list. Linked list is a good choice because you don't need to shift elements around like you would do with an array list. And when you don't need random access, if you rarely need to access elements by index and instead perform operations like iterating over the list or accessing elements from the head or tail linked list can be more efficient. And when you want queue like behavior. Since linked List implements the dequeue interface, it is often used for queue and dequeue, which is nothing but double ended queue implementations. This allows you to use methods like add first, add last, remove first and remove last to manage elements in a queue like fashion. Now let's jump into some code to see how can you work with linked list in Java? For that, let me open my VS code and let me choose a different folder for our practice. Here I'm creating a folder with the name linked list practical. Let me select this folder. And here let me create a new file with the name linked list test dot Java. And let me do the necessary things here, such as creating class linked list test and in it public static void main. And before all of this we need to import our. So here small I right now it's perfect. Now here I need to import our link list class from util package Java dot util dot link list. So here let me create an object for our linked list link list string root equals to new linked list okay so if I open this class implementation. So first of all what you would see here is it is implementing our list interface and also dequeue interface which is then further implements queue interface. Right. And if you scroll down a bit here we have size variable. And here we have these two references such as first and last. So it is basically a pointer to first node. This one is pointer to first node and this one is pointer to last node. And if you scroll down a bit here we have constructor which we have used here. Here you can see here we have used an empty constructor to create this fruits object. And after that here we have another constructor linked list constructor with parameter collection c. So it is basically used to convert any given collection to linked list okay. Then we have this method link first method and it is a private one. What does this actually do. So basically this link first method is a part of internal implementation of linked list. It is a helper method used to insert new element at the beginning. So if you see insert method or here this methods just calling this link first method. So basically it is a helper method used to insert a new element at the beginning or head of the linked list. Whenever you call add first method on our linked list objects and pass some element into it. It just insert that specified element at the beginning of the list. That's what it do. And if you scroll down a bit here we have similar method link list. So what does this actually do is so whenever you call this add last method. So here it is add first right. And if you call add last method here we are actually using this link list method. So this link list method basically another internal helper function in the linked list class that is used to add a new element at the end. Or you may call it the tail of the linked list, just opposite to what this add first and linked first would do. So here these are used to insert elements beginning at position, whereas these are used to append specified element to the end of the list. So that's it. Now let's move further. If you scroll down a bit here we have this link before method. So this link before method is designed to insert new element before an existing node in a link list. It works by adjusting the pointer of the existing node around the insertion point to maintain the integrity of doubly linked list structure. So here this line. This commented out line ensures that the asuc which is short form for successor, it is a successor node. Whatever it is, it is not null. It ensures that it is not null. It's an internal check to guarantee that this method is only called with a valid node as insertion target that is not the tail of the list or any empty list. Since this check is commented out, it won't be enforced in the current state, but it's implied that this method should never be called with successor being null. And then we have this final node. It is nothing but a predecessor. This is successor and this is predecessor. So predecessor equal to. So basically the predecessor variable holds a reference to the node that comes before successor such as successor dot previous is a pointer to the predecessor of the node successor. If the successor is the first node then predecessor will be null, right? So before first node we don't have anything. That's why if the successor is first node, then predecessor must be null. Don't worry if you are not getting what is going on here, just focus on these lines and try to understand what is really happening here in this method. If you are still not getting then don't worry. Just I will be explaining an example. After that, try to imply those operations on these lines. Then you will definitely going to understand what is going on in this. Okay, let me summarize all of these operations going on here in simple words. So this link before method inserts a new node containing E before the node successor, which is already present on our linked list. And this is this element would be the new element which we are, which we want to add into our linked list. So here we want to add this e element before this successor. Okay. So this method adjusts the pointers both previous and next. So here it is setting successors previous to new node because we are adding before right. So this is this new node is nothing but a node which is containing this element E. So if you see here, here we are passing this new node equal to new node of. And we are setting this as predecessor. And here we are passing this one and we are passing successor. And here we are just setting predecessor to successor dot previous. Here we are just setting the link right. What if successor is the first node before successor. We don't have any node. Then the new node becomes the first node. If successor is the first node then predecessor would be null, right? So if that predecessor is the null, then we are just setting this new node to first okay. If successor was not the first node then we are just setting the link predecessors dot next to new node and the successors. Here we have set this right. Previous successors dot previous node linking to our new node. And here predecessors. Next node is linking to our new node. That's completely adds our new node to the linked list because here we have successfully set the link and after that we are just updating the list count such as size and mod count. We are just increasing it to one. Let me explain it through an example. Let's suppose let me open notepad. Okay. Now let's suppose we have these nodes a, b, c. And here a is the first node right on previous of a is null. Is that correct. So before A we don't have anything. So previous of A is null as of now. And then we have b node which is second node. And after that we have node c which is the last node. And next of c is null. So after c we don't have anything which makes it null. Now if you call link before method and pass x as new element and pass B as successor. Let's try to compare these steps with these steps which are present in our link before method. Okay, let's try to compare it and try to understand what is going on here. Okay. So here we are passing this E as x and successor as b. So here B is already present on our linked list right. So which is successor. So here by using this method we just want to add this new node x node between a and b. So before b we just want to add that link before. So here we are adding before this B successor. So when you call this this is what happens. The PR previous pointer of B is updated to point to new node. Previous of B is updated to C successor that previous is updated to point the new node which is set here and then the next pointer of A. So A is predecessor, right? Currently at this position predecessor is not null. So this step is going to be executed. Now at this example predecessor dot next set to new node. Next pointer of a a is nothing but predecessor is updated to point to new node. Let me mention new node like this okay just like here. So after doing this finally list would look like this a x b c. What if if we don't have a before it. So this is the example. What is going to happen in such case. In such case B is the first node and C is the last node. Previous of B would be null and next of C would be null. Okay. So currently uh successor is B right. So successors previous node is null right. So we are going to update that null to the new node. So successor dot previous equal to new node. This step is going to happen anyway. Okay. After setting successors previous node to new node we are just checking here if the is this successor is first node which means is predecessor is set to null as of now. If it is null, then we are just setting our new node as first first equal to new node. And that's it. By doing this so we have only one link on our first node right. One is null and one is this successor which is already set successor dot previous equal to null. So let's make our point. And after doing this we are just increasing this size for one more element. And we are just increasing the mod count in this case after updating everything. Our final output would be this. Instead of a here we will be having x right and x previous link would be null and x. Next link would be b and B is, which is not nothing but successors. Previous link would be X and successors. Next node which is already set to C. Hope you got the clarity by now how this method is going to perform when we call add method at particular index. So basically this link before method is a helper method for add I mean add it particular index. And also for normal add. So after link before we have these three methods unlink first, unlink last and unlink. So what exactly these methods do. So basically these three methods are part of the internal working of linked list. So these three methods you can't access them directly through an object because these are internal implementations. See these two are set to private. And this is not even public. So what does these three methods do. So let's start by this method first link. First. So the purpose of this method is to remove the first node in the linked list. If the link list is not null, then our primary purpose here is to remove that first node from the linked list. That's what this unlink first do. So here are the steps. So first this method receives the node f as input. Okay. So this method is probably called by. Let me show it to you. Remove first from remove first method. We are when we call remove first. This remove first method is going to call this method on link first. And then in pull. Also we are calling this method unlink first. And if we use pull first method also, then we are supposed to call this unlink first method. Okay. So these three are the places where we call unlink first method. So while calling this methods in pole or in pole first or in remove first, we are passing f, so this method is used the first node f as input, and it stores the value of the first node so that it can return later. And then it updates the first reference to point the next node in the list, moving the first node one step forward. So basically it is setting the next node as the first node okay. So if there was only one node in the list which means next is already set to null, right? Then it sets the last to null, making the list empty. If there is a new first node, its previous pointer is set to null because it is no longer has a predecessor, right? And then it reduces the size since we are removing one element. That's why here. Here we are just removing the size of that linked list and then incrementing the mod count, which is nothing but a counter that tracks changes to the list structure. Whatever changes we have done on linked List, we can track the number of changes happen to that linked list using the mod count. So on every linked list operation you would say this mod, you would see this mod count set to plus one. I mean call like this mod count plus plus and then it just returns the value that was stored in the old first node. So previous which is getting removed that elements that nodes value just returns. Okay here it is storing that value right. And just returns that node's value. So simply put this method removes the first element of the list and makes the next node the new first element. That's what. Unlike first method would do. Okay, now let's move to the next method unlink last. Just like link first, the purpose of this method also is to remove the last node in the linked list. You can say it by reading its name itself. And the steps here are. The method takes the last node as L, and just like our previous node method, it also stores the value of the last node to return it later, and then it updates the last reference to point to the previous node. Moving the last node one step backward on previous, it is set to moving the first node one step forward, right? Whereas this is set to moving the last node one step backward. And then if there was only one node in the list which means previous is null, right? It sets the first to null as well, making the list empty. If this new last node, its next pointer is set to null, it is no longer has a successor, right? So then after doing this, you just reduces the size because here also we are just removing one element. And after that we are just incrementing the mod count which keeps track of the number of changes happened to our linked list. And then finally we are returning the element which we have stored here old node's value which is got removed. So simply put, this method removes the last element in the list and makes this second to last node the new last element. And then we have this method unlink. So purpose of this method is to remove a node x from any position. So this method basically takes a node as x as input which could be anywhere in the list. We do not know the exact position. It stores the value of the x node to return it later. Just like previous methods x start item see element equal to x dot item. And then if the node x has a previous node and previous is not null, which means it has next node means previous is not null, right? It updates the next pointer of the previous node to skip over x and point to the x next node, so it just removing its previous node link set to this node. Right. So just removing that link and it is setting that link to its next node. So it just connecting the both sides node to each other. Okay. And if you access the first node which means previous is already null right. So it updates the first pointer to point the next node. And if x is the first node, and if the node x has the next node, then next is not null, right? It updates the previous pointer of the next node to skip over x and point to the x previous. If x is the last node, then next node is null right x next node is null. If x is the last node, it updates the last pointer to point to the previous node, and then it breaks the links to X from surrounding nodes by setting the previous and next pointer to null and clearing the stored value, and then it reduces the size of the list by one, and then it increments the mod count. And finally it returns the value which was stored in starting, which is nothing but the node value which is getting removed. So simply put, this method removes a node from any position in the list by adjusting the links of the nodes before and after it to skip over the node being removed. So basically unlinking is the process of removing a node from a linked list. Since link list relies on differences or links, you may call between nodes to form the structure, and linking means here breaking those references so the node is no longer part of the list. So the unlink method is this case. Handle the process of breaking those connections and updating neighboring nodes pointers accordingly. So after exploring a lot of private methods, now we have this public method get first. So what does this method would do is just returns the first element in the list. If the list is empty, then it just throws an exception such as no such element exception. So let's explore it through an example. So here we have this right. Since this throws an exception let me just keep this thing in a try catch block. And what is the exception. Throws no such element exception right. Let me copy it. Let me paste it here. Let me call root start. What was that okay. Get first right. Get first. Now this one throws an exception. Let me keep exception instead. No E dot print stack trace okay. Now if I run this we should probably get no such element exception. And we got it. See? And before this, let me add some elements into it like fruits dot add. Apple rootstock. Add banana and fruit. Start at cherry and let me call this in. So to print the element which we are getting assert to start get first. So if I call it I will be getting the first node which is Apple right at head position will be having Apple. So if you call this you will be getting Apple as result. And we got the apple. Now if I print our fruits how it looks like, let's just print our fruits linked list and see its structure. So it's printing. So if I print this just returns like a list. Just like an array okay. You won't see any specific structure here while printing. It just looks like an array. But here each elements has links to its previous and next elements. And here this apples previous is set to null and this chair is next is set to null. And here Bananas Press is set to Apple. Bananas next is set to cherry. So that's how our linked list will form. Okay so as we have explored the get first method. Now just move to the next one which is get list. So just like get first get last method would give the last element get last. I run this now it probably getting cherry as a result. So cherry now let's move to the next one. And here this one also throws no such element exception if the list is empty. Keep that in mind. And next we have this one. Remove first method which is ultimately calling the unlink first method to remove the first node from our linked list. Okay, so basically it removes and returns the first element from the list. Removes and returns. Okay, so if I call it remove first. Okay. And after this I'm printing our fruits. So if I run this now you can probably see it just removes the apple and brings the banana cherry and while removing, just returns the removed element also, right. So that's why we are getting removed elements value removed node's value which is Apple right? That's we are getting here. Now after that, if we print the fruits now you can see apple successfully got removed. I mean first element from our linked list is successfully got removed, which is nothing but head of the list. And then we have the remove last. Similarly, we can just remove the last element from our linked list. So if I do this, it just returns cherry and removes the cherry from our linked list, since it is the last. Since it is the tail of our linked list. And here you can see cherry and from the list cherry got removed. And next method we have here is add first. So this method basically inserts the specified element at the beginning of the list. So just utilize the method link first method to add element to the starting position. So what does this link method would do? We have explored it right. Just add a element node to the beginning. So if I do this add first. And if I pass grapes as element and it is throwing an error because it is void, right. So since it is void we can't make it in System.out.println. Let me remove it and remove this too okay. So add first right. Okay. Now if I run this now you can probably see a new element is added at the beginning of the list. So if I run this you can see grapes apple banana cherry. So it basically added an element at head. Similarly we have add last method also. So what does this do. Is it just adds an element to the last position of our linked list. Simply say it just adds an element at the tail add last. So if I do this this grapes will get added to the tale of our linked list. Let's verify it. If I run this now, you can see grapes is added at the end. And next we have contains method. So this method is similar to other collections which we have explored on. So it basically returns true if this object contains in our linked list. Let's suppose we have grapes on our linked list right. So if we do this assert fruits contain grapes. So at this point it just returns the result as true. And we got that result as true because we have grapes in our list. So if I comment this out, if I run this again now you can probably see the result as false since we have not added these grapes to our list. Right? So that's how it works. Next we have the size method. Obviously it returns the number of elements on our list right. So instead of calling this contents method Let me just do the fruit start size. And if I run this now, now you can probably see we are getting result as three of. We include this method also this element also. Then we can probably see the result as four right? See. Thus we have four elements in our linked list. Now let's move to the next one which is add method. So it basically adds an element to our linked list. So this add method basically uses the linked list method which adds the each element at the end of the list. So after adding Apple it won't add next element before the apple, it just adds next element after the apple which is banana. And after adding banana, the next element which is getting added to the after banana, which is nothing. But whatever element we add it, just add a detail. See appends the specified element to the end of this list. So that is what going on here. And after that we have the next method as remove. So whatever object we pass in this remove method just removes that particular element from our linked list. And since it is a boolean and just also returns true or false, if it successfully removes that element, it just returns true or false. Let's try this instead of size. Let me call remove and pass the object as Apple. Now if I run, this just returns the true and removes the apple. And after that you can can see Apple on our list. See if I run this I can probably see it just return true. And we got Apple removed from our list. Now let's move to the next one which is add method. To add a collection to our list, we can make use of this add method. Let's suppose we have an another linked list. Let's suppose this one is fruits two. Let me keep Mickey two. Also here. And here I want to add all of these elements to our fruits. Okay. Add all. And let me pass collection here instead of apple. Let me pass fruits two. Okay. Forgot to mention two here while adding. Let me keep two here also. Okay. Now if I run this, this will just returns true. And add all of these fruits, two elements to our fruits linked list. Okay, we run this now. You can probably see it. See? It returns true. And then after adding all of these, it just added apple to a banana two and cherry two to our linked list. So that's how our add method works. Now after add all we have our next method which is add a particular index. So instead of adding at the end if you want to add after 012. So if you want to add other elements after two after cherry then we can use it index. So here I'm passing the index as two and then passing fruits two. So if I run this now you can probably see apple banana cherry. Wait a minute. Apple banana. Then we got apple two okay. Here we need to pass three. So if you pass three then it should be adding it from this three. So we need to replace grapes and then add apple banana cherry. And after that we need to add grapes again okay. Now if I run this now you can probably see apple banana cherry then apple two banana two cherry. Then we got grapes. So that's how this works. Now let's move to the next one which is clear method. So it probably use it to remove all of the elements from the list. And since it is white we can print it like this. So here let me call routes dot clear okay. Now, if I run this after. True, it just prints this empty list. Since we have cleared our list and after clear we have get method. So it just probably returns index of the element. So it probably returns whatever index you pass. It returns the element at that particular index. So if I do this instead you let me call get and let me just pass three. If I do this now, we can probably get result as grapes. So if I run this now you can see grapes. And then we got this empty list since we are clearing it. Right. So it just returns the element at this particular index. So after get we have set method. So it probably replaces the element at the specified position which we have already explored in our previous collection videos. Let me show this to you again set. So here we need to pass an index and then element. Okay. Here at index three instead of grapes I want to add new grapes. I think we can't make use of this new as an element. Let me grow new grapes instead. Like this. Okay. This will work. So if I run this. Wait a minute. Let me comment out this clear method. Now, if I run this, I can probably see it. The place of grapes. We have just replaced it with new grapes. So that's how our set method works. So if you pass the index, which is, uh, greater than the linked list size, then you can just probably get this index out of bound exception. So if I do this, if I pass ten here now if I run this you can probably see index out of bound exception. Got it. So you need to keep this index according to your linked list. So next method we have here is add a particular index so we can make use of this add method. Special add method also. So if I do this add at index three. Here I want to add this new grapes element. Since this is a void we can't make it in assert. So let me just remove it. Okay. Now if I do this now you can probably see before grapes we got new grapes and then we got grapes. Again we just added the over new element at the specified index. So that's what add with index would do. And after add with index we have remove at index method. So what does this do. Is it just removes the element at the specified position in the list. So whatever index value passed here since here we are passing a index three. Right. Let me just remove all of these things. And instead of adding let me just give remove. So in previous we have seen remove with object. And this is not exactly the same. This one is remove with index. So if I do this now, you can see at current position we are having a grape set for right at index three. Right now it will just get removed and it will just print apple banana cherry as simple as that. Okay. Now after remove we have each element index. So this method would probably tell us if the argument is the index of the existing element. And this one is not a public one. So it is the internal method which you can't access through our objects directly. So this method is a simple utility function used internally in the linked list to check if the given index correspond to a valid existing element in the list. So here we don't need to go through this much. So just ignore these things is position index is element at index. We don't need to go through all of this. Let's search for public ones as of now. Yeah. Here we have one public method which is index of. So what does this do? Is it just returns the first occurrence of the object which we are passing here, which we have already explored in our previous videos. Right. So let's also explore in our linked list. Also for that let me repeat this appeal. Okay here we have Apple three times. Now if I run this now you can probably get our first occurrence of Apple at index zero. For that let me just give let me remove everything for better clarity. Okay. This one also we don't need. Let me call a sort. And in a sort let me go. Fruits dot index of and let me pass Apple. Now it will give the first occurrence of our apple which is at zero right now it probably returns zero. And we got zero. Similarly we will be having last index of also. See it just returns the last occurrence. So instead of index of if we give last index of now we can probably see the result as 123401234. Right. So we'll be getting result as four. And we got that four as result. So this is the method which will return the last occurrence of an object. Okay. Next we have this method. So this is basically the queue operations. So as of now we have explored the list interface operations. And now we'll be seeing the queue operations on our linked list. Or let's make a separate video for this. Let's end this video as of now so that we can understand these methods separately. Once after exploring the queue interface implementations. So that's it for this video guys. In next video we'll explore more advanced collections. So let's end this video here will be covering queue operations in a separate video. So that's it for this video. Let's meet up in the next lecture. Up till then stay happy and stay secure.