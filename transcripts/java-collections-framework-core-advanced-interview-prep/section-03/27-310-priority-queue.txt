Hello everyone. Welcome back to our Java Collections Framework course. Today in this video we are going to dive into another very useful class from Java Collection framework which is Priority Queue. If you look at the term priority queue, priority queues are an abstract data type in any language you can take that operates similarly to a regular queue, but with one significant difference each element in the queue has a priority associated with it. The elements are dequeued based on their priority rather than their insertion order. So what exactly is a priority queue in Java? The priority queue class in Java implements the queue interface and stores elements in accordance with their natural ordering, or according to a custom comparator. You can define. Just like in our previous video, we have passed a custom comparator rather than using default one, which is comparator dot reverse order, right? If you want elements in reverse order, then you can pass comparator dot reverse order. And also you can create your own comparator and pass that comparator into it. So you can think of it as a queue where elements are removed based on their priority rather than just their order of arrival. So the element with highest priority is always at the head of the queue. So let's break down and expand each sentence of this explanation clearly with example for better understanding. So here first thing to understand is that this priority queue is a type of queue. If you open it now here you can see this priority queue extends abstract queue. And this abstract queue further implements this queue interface which makes our periodic queue class also ease the implementation of the parent queue interface. So this queue which is an interface in Java collections Collection framework, and this queue interface represents a collection designed for holding elements before processing, and a queue works on Fifo order, right? First in first out principle, where the first element added is the first to be removed. However, priority queue changes this behavior slightly by focusing on not on their order of insertion, but on the priority of the elements, so elements with the higher priority are processed first, even if they were added later. So to prove you that I have this example. So here I'm just using a normal queue linked list. And here I'm adding elements like this b a, c. So if I remove an element from our queue so whatever element at the head will be removed. So first we have added b. That's why b is the element which is present on head right. So before this let me just print out actual Q so if I print this now, you can see we have elements like b, a, c, and in this if I remove an element using poll method, it will just remove the element at head right. See we got B removed from our queue. Let me also print the queue after removing an element. Okay, so if I print this again now you can probably see we have now only left with a and C, B got removed since B is the first element which is inserted, that's why it got removed. But if you see the same example in priority queue. So here I have just changed this implementation linked list to priority queue. Now we are adding b a, c. So if it follows insertion order then it must remove b first. Right. But now you can see if I run this now we will see it stored elements in ascending order. By default, it will be storing elements in ascending order, and on ascending order the priority will be set to the lowest element, right. So here the lowest element is a. So that's why A got removed. A has the highest priority in this queue. That's why the A is at head. B has the second most priority after A. That is why it is after A in our queue. And if you want to set the priority in descending order then you can pass the comparator here using comparator comparator dot reverse order. I will probably see C is getting removed since C has the highest priority in this queue now, which proves our point. Elements with higher priority are processed first, even if they were added later. You can see here we are adding C in last, but still C is getting removed. Whereas in a normal queue A was deleted first, so it's the first to be removed. But in a priority queue, the element with the highest priority is removed first, regardless of their insertion order and priority queue stores elements in accordance with their natural ordering. This means that if you are using a priority queue with elements that have natural order, like numbers or strings, the queue will sort these elements based on this order. Like we have seen for alphabets natural order like ABC. That's why on first they are sorted to their natural order instead of elements. If you use one, two, three. Let me make it integer instead and let me add three first. And these elements are sorted to their natural order. So for numbers, natural order means ascending order. That is, smaller numbers have higher priority. For strings, natural order means lexicographical order. Alphabets are. You can say alphabetical order. So now let's see this example. If I run this now, now you can probably see it is sorted to the ascending order. So since the smallest element will have the higher priority. That's why one got removed. Or this can store elements according to a custom comparator. You can define you can define a custom comparator of your own. Or you may use a different comparator just like comparator dot for ordering. We have these two methods natural order and reverse order. So we can do reverse order also. So if we do this now the greatest elements will have the highest priority. So if I run this now you can see three got removed first. So by defining your own custom comparator you can change how priority queue orders its elements. A custom comparator allows you to specify custom rules for how elements should be prioritized. We haven't created a custom comparator for ourselves as of now. We will be seeing that in our future videos, and you can think of it as a queue where each elements are removed based on their priority rather than just their order of arrival, and we have read this term. The element with higher priority is always at the head of the queue, which means in a priority queue, you always have access to the element with the highest priority without needing to traverse the entire queue. The head of the queue. The first element is always the one with the higher priority. That's the meaning of it. See, while we do reverse order, the higher priority is set to three, right? So that's why the three is at the head of the queue. Now let's look at some of the key features of a priority queue. So first one here is ordering as we have seen. By default the elements are ordered in ascending order meaning the smallest element is at the front. However, if you provide a custom comparator, you can order elements in a custom way you like, and next feature is implementation. Under the hood, priority queue uses a binary heap structure to maintain the ordering of its elements, so the binary heap helps the priority queue perform efficient insertion and removal operations. So there are two main types of binary heaps. Min heap. Max heap. In a min heap, the smallest element is the root, and every parent node is smaller than its children, whereas in max heap the largest element is the root and every parent node is larger than its children. So basically Java priority queue uses a min heap, which means the smallest element is always at the top of the heap, and that's how it keeps the elements with the highest priority queue smallest element at the front of the queue. Whenever you use a reverse order, then you would probably see the max heap in work. Now you might be wondering how the binary heap works in a priority queue. So the answer to this is insertion add method. When a new element is added, let me open add method in priority queue. So when the new element is added to the priority queue. It is placed in the last available position in the heap. To maintain the completeness of the tree, then the heap property is restored by comparing the new element with its parent and swapping them if the new element is smaller. Let's suppose when we add three, one, two. First of all, it's placed three at this position, and whenever you add one, it just compares one with three. If the new element is smaller, it just swaps the positions. This type of swapping is done in min heap. If it max heap then it don't do the swapping if the new element is smaller. If the it only does the swapping if the new element is max bigger than the already present element. While the time of removal when the element with the highest priority smallest in min. Let us consider min heap only as of now. Smallest in Minheap is removed, which means the element is. This position will get removed. So on Minheap one will be there, right? Let me redraw this structure. So after adding these three elements, our heap with elements will look like this one, two and three. So the smallest will get removed when we do the pole method on our priority queue, and the element at the root is replaced by the last element in the heap, and the heap property is restored by comparing the new root with its children and swapping it with the smallest shell again, and this process is called down heap. Both insertion and removal operations have a time complexity of O of log n, due to the binary heap structure when n is the number of elements in the heap. And. Now let me show you the proof why I am saying it uses the binary heap structure. Let me show this to you if you open priority queue implementation here, we will be having a method known as offer. See, while we are adding an element into our priority queue, we are just passing that past element to add method to this offer method. So if you open this offer method now, you can see the shift of operation. And if you open up that shift of operation and here you can clearly see it is first checking comparator is not null. If it is not null, then it just passes that comparator whatever comparator we have passed. And if it is null, which means just use the basic comparator shift up to pass the value which we have passed, which is x. So it basically doing two operations here. If comparator is passed then it just shifting up using comparator. But just calling this shift up using comparator method and passing comparator and the element and the whole queue, which is nothing but the updated queue as of now, and K, which is the position to fill and X is nothing but the item to insert. And if you open up this shift of using comparator, then here you can probably see at this position it is doing the comparison and shifting elements swapping. And if you pass it without comparator then you can probably do this shift of comparable operation at this time. It just does the ascending operation. Now let's get back to our queue example. So here basically the shift up and shift down. There are two methods. If you see shift down whenever you do pull whenever you remove it particular i'th position. You would probably use this shift down method. So first of all these methods shift up and shift down. These methods are responsible for maintaining the binary heap structure. Basically the shift of this method is basically used during insertion to place the element in the correct position, while the shift down is used during removal to restore the heap property after the root element is removed, so the operations are based on the comparing elements, either using their natural ordering like numbers or strings, or custom comparator. Now let's take a priority queue with the following sequence of numbers being added, such as ten, 30, 20, and five. Okay, so here's what the heap look like internally. So first of all whenever you add ten. So here the only one element no need to reorder. So it just adds the ten. And when you insert 30 here also no need to reorder since ten is less than 30. So it just adds that 30. And whenever you insert 20 here we need to reorder the elements. So before reordering if we have elements like this ten, 30 and 20 on heap. After reordering, we can see elements as ten, 20 and 30 and when we enter five. So here also we need to reorder elements. So at this position five moves up to become the root because it's the smallest. And then after complete reordering now you can see the elements in this order 510, 20 and 30. And when you call the pole method to remove the element five will get removed, since it is the root, and the heap will recognize itself by putting the last element ten at the root. So that's all about its implementation guys. Now let's look at its another key feature. No null elements means. One thing to note here is priority queue does not allow null elements. So make sure your queue does not contain any null values if you try to add one. And if you run this program now, you can probably see the null pointer exception. So make sure you don't do this mistake while dealing with priority queue. And another one is Non-synchronized. It is Non-synchronized collection, similar to many other collection classes in Java. Java is not a thread safe. If you want to use it in multi-threaded environment, you should either wrap it with collections dot synchronized queue or use a thread safe alternative. And another feature which you should consider is unbounded. Yes, the priority queue is unbounded queue, meaning it can grow as needed. However, it will still be constrained by the heap memory available. And when you talk about time complexity, insertion operation will contain O of log n, as we have already discussed. And deletion also will give the time complexity of of log n, whereas peak operation will give the result in O of one at constant time because no elements need to be rearranged. It just acts as the top element which is available at the head. And when you talk about the use cases, some of the use cases for priority queue include task scheduling when certain tasks have higher priority over others and should be executed first. It is useful in Dijkstra's algorithm. It is heavily used in algorithms like Dijkstra's for finding the shortest path in a graph, and it is also useful in event driven simulations where events need to be processed based on their time of occurrence. So that's all about priority queue. Guys, we have covered how it works, and we have also explored some examples on when it can be useful. Priority queue is an excellent tool for efficiently managing collections of objects where priority matters more than order of insertion. So that's it for this video guys. Thank you for watching and I will see you in the next one.