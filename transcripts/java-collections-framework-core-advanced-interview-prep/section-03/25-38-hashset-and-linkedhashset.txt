Hello everyone. Welcome back to our Java Collections Framework course. In today's video, we are diving into two important classes from collection framework such as Hashset unlinked Hashset. These are part of set interface as you can see here Hashset unlinked Hashset are implementing. Or you can call these as implementations of our set interface, which means this store unique elements and don't allow duplicates, but they have their own distinct behaviors, has set to its own separate behavior, and linked hashset have its own separate behavior that make them useful in different situations. Let's take a look. First of the Hashset. Hashset is probably the most commonly used implementation of the set interface. It uses a hash table for storage, meaning that the elements are stored based on their hash codes. What this translates to is superfast access time. That's what Hashset used for superfast access times. But the order of the elements isn't guaranteed in Hashset. Here is a simple example of a hashset in action here. Notice how we added string Apple twice. So first here also we have added Apple and on last you can see second time we have again added the apple. But because a set doesn't allow duplicates it only stores once. So no matter how many times you try to add Apple, it only appears once in this set. Not only Apple, it may be anything. How many times you add a particular element, it will consider as only once in Hashset or any set in implementations. Now let's talk about the hashset behavior. So behavior of hashset when it comes to the ordering. First we have added apple then banana, then cherry, then again apple. Right. So when it comes to ordering hash, it makes no guarantees about the order of elements. The order might seem random, and that's totally fine because the primary focus here is on fast access. If you have focus on order then you might not choose hash set. You are choosing hash set because it allows faster access than any other class in collection framework. This makes the hash set a great choice when you only care about uniqueness, not order. So here if I print our hash set now using S out hash set. If I run this now you can probably see in our output we have banana apple, cherry which proves our point. It does not maintain order and it is also having uniqueness among its elements. See we have added Apple twice right. But you can't see Apple twice in our output since it is implementation of our set interface. Next up we have linked hash set, which is an extension of hash set with one key difference which is it maintains the insertion order. So if you want the unique property of set but also need to preserve the order in which elements were added then linked, Hashset is the way to go. Because if you look at this example, let me just replace this hash set with linked hash set. So now if you look at this example just like hash set, linked hash set also does not allow duplicates but the order in which they were added, the order of these elements is maintained. Order in which the elements were added is maintained. So if we print this elements now if we print this linked hash has set, the elements will appear in the order they were added. Now let's see how linked list behave compared to Hashset. As you can see, when we print out the linked Hashset using Qsort. Let me hide these things. Okay, now if I run this program now. Now you can probably see apple, banana, cherry. The elements are printed in the order they were added. And also you can see the duplication since we have added Apple twice. You can see Apple twice in our linked Hashset set collection. Hence the difference clearly laid out. Hashset does not preserve any order while linked Hashset does. This makes linked Hashset useful when you need it is useful when you need predictable iteration order but still want the efficiency of set, then this linked hashset might be useful to you. Now let's take a quick look at this performance table. So based on this performance table. So first of all order of elements in hash set is unpredictable which means get the elements in random order. Whereas in linked hash set it preserves the insertion order, which means you get the elements in the order in which you are added, or you were inserted the elements into linked hash set. Okay. So when it comes to performance, since because of its unpredictability, has set is more faster than the linked hash set. So hash it gives the elements in the order of one in constant time, where linked has set is slightly slower than the hash set. It is also well performed, but it is slightly slower than the hash set in. Performance and use. Case of using hash set is when you want faster access and you don't want elements in particular order, then you might use the hash set. Whereas in linked hash set, when you have and when you want the elements in a specific order and you want the uniqueness also, then you might use the linked hash set as your collection. So based on all of this information, you can say hash set is generally faster than the linked hash set because it doesn't have to maintain the insertion order. However, if the order is most important to your application, linked hash set is greater choice and still offers reasonable good performance. So to wrap things up, if you need fast access and don't care about the order, go with hash set. If you need to preserve the insertion order, then you might choose Linked hash it. Both are part of set interfaces and both ensures that only unique elements are stored. Which one to use depends on entirely on your specific needs. So that's it for today guys. If you have any questions you can reach out to us. Let's meet up in the next lecture. Up till then, stay happy and stay secure. Bye bye.