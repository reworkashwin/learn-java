I hope you had a very good break. Since right now we are into the new section, I'm going to create a new project which is specific to these new sections so that all the code changes that we're going to discuss inside this section we can do in this new project. As of now, I have created a demo project under the section1 folder. Instead, this time what I'll do is, I'll try to create a new project. This time also, I'm going to give the name as demo followed by the folder I'm going to mention as section2. You can find all the code that we're going to discuss inside this section under the folder section2 inside the GitHub repo. Build system will be Maven, JDK will be 25 under the advanced settings, I'm going to have this group ID and artifact ID as soon as I click on this create, I'm going to get an option to open this new project in this window or a new window. I'll select this new window so that we are going to have two different tabs. So this is the new project under the section2, and this is the project under the section1. So what I'll do is, I'll try to copy that dependency related to spring framework into this pom.xml as well. So first I'll go to the other demo project and open this pom.xml. So here I have dependency with the name spring-context. Let me copy the same and paste in the new project. So the new project is present here under the section2. Here the pom.xml is already present. Let me paste the dependency details here. Once the dependencies are pasted, you need to click on this sync maven changes. With this now we have converted this maven project into a spring based project. So let me close this pom.xml. Next I'm going to define a package under the Java folder. The package will be com.eazybytes.ex1. So ex1 means example one package. Throughout this section, we're going to discuss various examples covering the spring core concepts. For each of the example, I'm going to maintain a separate package so that it is going to be super clear for you whenever you want to refer the code. Let me press enter followed by I'll try to copy the code from this project into the new project. Let me copy these beans config and demo main class. So here I'll try to paste all the content. Next I'm going to rename these file to example1. So let me type example1. Before that, I need to fix some compilation issues. So here I need to fix the import statements. So this Vehicle class we need to import from the right package first let me delete these invalid import statements. So now I'm going to import the Vehicle from the correct package. The same applies for project to config as well. Once the import statement issues is fixed, we need to rename the class name as well to example1 so that all the compilation issues will get resolved. So now I can close the demo project from the section1. Since we copied everything, we should be good and close the same. As a next step, I want to show you one of the most famous exception that you may receive while you are dealing with the spring beans. This exception is no unique bean definition exception. When multiple beans of same type are defined in the spring context, and if you try to attempt to retrieve a bean by type, spring cannot determine which instance or which bean to provide. For example, here I have three different methods returning the bean of type Vehicle. Off course, the name of the beans are different. From the first method, the name of the bean will be vehicle1 followed by vehicle2 and at last we are going to have vehicle3 bean. So in this kind of setup, if you ask the spring context to provide the bean of type vehicle, it is going to have confusion because there are three different beans of same data type available. In this kind of scenarios, we are going to get a no unique bean definition exception. Let's try to see the demo of this, followed by we can try to understand how to fix this exception. What I'll do is, under this ex1 package, we have ProjectConfig file. Let me open the same here we have import issue. Let me delete this and import this Vehicle again from the correct package. Next what I'll do is, I'm going to get rid of this string and integer related beans. Instead, I'll try to copy the same method two more times and rename the method names to vehicle1(), vehicle2() and vehicle3(). Under all these three methods, I'll try to provide different names for the vehicle. Under the first method, I'm going to mention the vehicle name as Audi followed by Honda under the second method and at last, I'm going to mention Ferrari. In total my spring context, it is going to have three different vehicle beans with the different names with this kind of setup if you ask the spring context to provide the bean of Vehicle data type, since there are three different beans, it is going to simply throw the NoUniqueBeanDefinitionException. I can show you the demo as well. Let me go to example1 here i'm going to get rid of all these unnecessary code that we discussed previously. Once the code is deleted what I'll do is, if I try to execute this code I'm going to get an exception. So here I'm asking the spring context to provide a bean of type Vehicle, but there are three different vehicle beans. That's why the spring framework, it is going to throw an exception. The exception name is NoUniqueBeanDefinitionException. Under the exception details, we have these information. No qualifying bean of type Vehicle, expected single matching bean but found three beans with the names vehicle1, vehicle2 and vehicle3. Whenever you're facing this exception, you need to clear the ambiguity that spring framework has. So how to do that? It's very easy when you are invoking the get() method, you can pass both bean name and vehicle data type or you can simply pass the bean name as well. So this time I'm going to invoke the getBean() method, which accepts two input parameters. The first one is name of the bean followed by data type of the bean. So under the name of the bean, I'll mention the bean name as vehicle1 followed by data type of the bean, which is Vehicle.class. So with this now, I should not be getting any exception instead I will get the output as Audi. So if I execute this program as you can see, we got an output with the vehicle name as Audi. I hope you understand on how to solve the NoUniqueBeanDefinitionException. As a developer, you need to make sure you are providing enough information to the spring framework or to the spring context so that it is not going to have any ambiguity while it is retrieving the bean from the context. So here my spring context has three different beans of same data type. So while invoking the getBean() method, if I pass the bean name along with the bean data type, it is not going to have any ambiguity. You can also invoke getBean() method by simply passing the bean name as well. Let me show you that demo as well. What I'll do is, I'll try to copy these two lines one more time. This time I'm going to mention the variable name as vehicle instead of mentioning the vehicle1. I'm going to mention the vehicle2 bean here, and I'm not going to provide any data type. Since there is only a single bean inside the spring context with the name vehicle2, i'm not going to get any exception since we are trying to get the bean by using name alone. We need to perform the typecasting manually by mentioning Vehicle here. So with this now, if I try to execute this program after performing the build, I'm going to get the output Audi and Honda as expected.