So far, we focused on creating the Spring Beans. As of now, in all the previous lectures, we only created the beans by using various annotations. From now onwards, we are going to understand how to establish the dependencies between multiple beans while using Spring Framework. Dependency injection is another basic concept inside the Spring Framework. If you are already aware about this dependency injection, wiring, and auto wiring, feel free to skip all the lectures around the wiring of the beans. Usually, in enterprise applications, as I said before, we are going to write the logic in multiple layers. Let's imagine, I have written three different classes like VehicleController, VehicleService, and VehicleRepository. If I created beans for all these classes, they are going to stay like this inside the Spring context. As you can see, there is no dependency between them. With this kind of setup, what is going to happen, when the controller layer, it want to invoke the logic inside the service layer, it cannot invoke because it has no dependency on the service bean or on the repository bean. The Spring context does not aware about the dependencies. So to establish these dependency hierarchy, we need to tell to the Spring context or the Spring Framework to establish that dependencies as well between the beans while these beans are getting created by the framework. Establishing these dependencies is also called as Bean wiring. It is a process of connecting these dependent beans within the Spring IOC container. Manually managing these dependencies can become complex. So Spring provides both automatic style of wiring and manual style of wiring. First I'm going to show you some manual style of wiring followed by, we're also going to explore the options around the Autowiring as well. So when you perform the wiring between these beans properly, this is how the Spring IOC container is going to maintain all these beans. When it is creating the vehicle control bean, it is going to make sure to create the dependent beans as well. So in this case, the dependent bean is VehicleService bean and this VehicleService bean, it again has dependency on the VehicleRepository bean. Spring IOC container will make sure to create all these beans in a proper order, followed by it is going to establish the dependencies between them as well. Let me show you some code snippets on how to perform these wiring manuals, followed by we can see that demo as well. First I'm going to show you some no wiring scenario. As part of this scenario, we're going to simply create two different beans and we're not going to establish the dependencies between them from the Spring IOC container perspective. As you can see here, I have a Person class and Vehicle class. The Vehicle class, it has a single field name called name. Coming to the Person class it has two different fields like name and vehicle. This vehicle field is of type of Vehicle. From this code, it is very clear that this Person object or class, it has a dependency on the Vehicle. Usually in real life also, a vehicle will be owned by a person and the person is going to dependent on the vehicle to travel from one place to other place. So that's why I have taken this simple scenario, but in real applications, we're going to have the dependencies in these style like controller bean it is going to dependent on the service bean. We're going to explore these examples and standards when we are building an enterprise backend application. For now, let's consider the simple person and vehicle example. Once I created these Java classes, I'm going to create the beans of these classes by using the @Bean annotation. So there is a vehicle() Java method inside this method I have created a new object of Vehicle and into this, I have set the name as Toyota. With this now the Vehicle bean is ready inside the Spring context. Now coming to the Person bean, I have created a Person object by using the new operator followed by I have set a name to the person with the value Lucy, but I have not written any code related to the vehicle. I'm not setting any Vehicle object into this field with that the Person bean, it is going to have the null value against to these Vehicle object. So here, what I'm doing, I'm simply creating two different beans independently. And the Spring framework, it does not know the dependency between the Person and the Vehicle. That's why it is going to treat these two beans as two different independent beans. If someone is trying to access these vehicle field by using the Person bean, they're going to get the null as an output. Now let's try to understand how to establish the wiring manually in this scenario, primarily we have two different wiring options. The first one is by using a method call. This time, what I'm going to do is, while I'm creating the Person bean, I'm going to invoke the setVehicle() method and to this method, I'm going to assign the output that we are going to get from the vehicle() method. So here I'm invoking the vehicle() method. So what is the output that we are going to get from the vehicle() method? We are going to get the Vehicle bean. The same I'm trying to set into these vehicle field of Person bean. With this now, the Spring framework or the Spring context, it knows that the Person bean is dependent on the Vehicle bean. And internally, it is going to perform the wiring between these two beans. Here how I'm trying to achieve the bean wiring by using the method call. So with this setup, if I try to execute this code, where I'm trying to get the Person bean and Vehicle bean from the context, followed by, if I try to execute the person.name, vehicle.name and person.vehicle, this time from this last statement, I'm going to get an output saying that so-and-so person has so-and-so vehicle with the name Toyota. We're not going to get the null value. If something is not clear, don't worry, we're going to see this demo in few minutes. Let me show you the next approach as well. This time we are going to perform the wiring of the beans manually by using method parameters. To the person method, I'm going to pass the vehicle as an input parameter, followed by the same input to method parameter, I'm going to set by invoking the setVehicle() method. This time I'm not invoking this method directly, instead I'm passing this vehicle as an dependency to this person() method and the same parameter I'm trying to assign to the setVehicle() method. When we have this kind of setup behind the scenes, what spring framework it is going to do is when it is trying to create the bean of Person, it is going to see that this person method, it has a dependency on the Vehicle object or Vehicle bean. So it is going to check, are there any configurations inside the spring context related to Vehicle bean? If you already have such configurations, first it will make sure to create the Vehicle bean. The same Vehicle bean will be injected as a dependency by the spring context behind the scenes. So by the time we are passing these veh variable as an input to the setVehicle(), we are setting the Vehicle bean itself as an input to this vehicle field of Person. And with that, the wiring between these two beans is going to happen and we are going to get the similar output that we saw in the previous slide. I hope you're clear on what is wiring and how to perform these wiring while we are using @Bean annotation. Let's see the demo of these examples in the next lecture.