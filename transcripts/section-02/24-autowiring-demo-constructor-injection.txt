Let's see the demo of Constructor Injection. For the same what I'll do is, I'm going to update these Constructor that we have here. So into this Constructor, I'm going to add a Constructor parameter with the name Engine followed by inside this constructor, I'm going to write a logic this.engine = engine. So let me remove the @autowiredannotation on top of the setter() method. Now I'm going to mention the @Autowired annotation on top of the constructor. So this is called Constructor Injection. So this Car class has a constructor and to this constructor, we are mentioning the required dependencies since this class has only one dependency with the name Engine, I have mentioned single input parameter. In the scenarios where the bean has multiple dependencies, we can define all of them by using multiple constructor input parameters. With this code changes during the startup of my application, the spring framework it will try to create the bean of Car by invoking this constructor. But while it is trying to create the bean of Car, it will see that this car has a dependency on the Engine bean. That's why it will make sure to create the Engine bean before the Car bean. By the time this Car constructor is getting invoked, this engine bean will be created and whatever bean that we have here, that will be assigned to the Java field that we have defined here. So I can try to bring these fields above the constructor. This code is going to look better. Let me execute the example 4 and see what is going to happen behind the scenes. So this time, if you see engine bean is created first, because during the Car bean creation, the spring framework it realizes it has a dependency on the Engine. That's why first this Engine bean is created, followed by Car bean. Coming to the output, we got the expected output. So and so Engine is owned by the Car. So what is that engine name? It is V8. We have one very good advantage when we are using Constructor Injection. The advantage is, if your class has only a single constructor, then mentioning this @Autowired annotation is completely optional. If I save these changes and execute the same example 4, I'm going to get the same output. First Engine bean is getting created, followed by Car bean and the output also is displayed as expected. So this is the beauty of Constructor Injection, but I'm going to mention this @Autowired annotation just for your reference. But as we progress inside this course, if there is only a single constructor in my class, I'm not going to explicitly mention this @Autowired annotation, because behind the scenes, the spring framework is smart enough to perform the auto wiring based upon the single constructor and the parameters that I pass to the constructor. Now let's try to understand how the Constructor Injection is going to help us in overcoming the disadvantages that we have discussed in the previous lecture. So while we are using Field Injection or the Setter Injection, I said the unit testing is going to be tough. Whereas with the Constructor Injection, it is going to simple to mark the dependencies. Since the dependencies are accepted as an input to the constructor. So inside your unit testing code, while you are trying to create the bean or object of these Car class, you can pass all the required dependencies to the constructor itself. And behind the scenes, the spring framework is going to take care of injecting the given beans into your Car bean. The next advantage is, when you are using the Constructor Injection you can mark the fields as final. Here, even I have mentioned the final just before the Engine, which is the dependency that I'm trying to inject by using Constructor Injection, still, I'm not getting any compilation error. Because before this Car object or bean is created, first these Engine bean is going to be created. So with this advantage, now we should be able to create the immutable Car object. In case if I want to make sure that no one should be able to change the engine dependency once the Car bean is created, I can simply mark that field as final. Whereas if I want to have a flexibility to overwrite this dependency at later point of time, I can mark this field as non-final. When we are using Field Injection and Setter Injection, these options we are not going to have. The next advantage is the readability of our code is going to be increased. Without opening my Car class, anyone should be able to understand what are the dependencies that my Car class has. For example, if I go to this example 4 and if I try to create the object of Car, here it is going to show me that this Car constructor has a dependency on the Engine. There is no need for me to open this Car class and try to understand all the java fields and setter() methods to understand what are the dependent beans. We can also avoid the Null Pointer Exceptions when we are using the Constructor Injection. Let me show you a demo around the same. Let's imagine this Engine bean is not available inside the Spring Context. So I'll go to this Engine class and comment this @Component annotation with this now the Engine bean is not going to be created inside the spring context. So now if I try to execute this example 4, I'm going to get an Runtime Exception and the bean of Car also is not created because the required dependencies are not available to perform the auto wiring. Whereas in the scenario of Field Injection and Setter Injection, the Car object or the Car bean is going to be created without any issues. When someone is trying to access the Engine field, they are going to get a Null Pointer Exception. This is one more advantage that we have with the Constructor Injection. Let me uncomment this code, I hope you are clear about the Constructor Injection. Please try to use this Constructor Injection inside your production code. If there are some special scenarios where you don't bother about unit testing or about the immutability of the bean, then you are free to use Field Injection or Setter Injection as well. Before I try to close this lecture, I want to share you about an exception that you may face while you are using Constructor Injection. When you are using the Constructor Injection, if you define the wiring in a circular manner, you're going to get an exception with the name Unsatisfied Dependency Exception. Let me show you what do I mean by circular dependency. We know this Car has a dependency on the Engine bean. That's what we have defined here by using the Car constructor. If I try to do the vice versa for the Engine constructor, then I'm going to create a circular reference. Let me show you an example. On top of this Engine constructor, I'm going to mention auto wired followed by to this Engine constructor I'm going to inject the bean of Car as a dependency. Inside the class also, I'm going to create a field with the name Car and the same field I'm going to update by using these, this operator. So this, what is going to happen while the Car bean is getting created the spring framework, it is going to realize that this Car has a dependency on the Engine. So it will go to the Engine, and while it is trying to create the bean of Engine by using this constructor, it again sees that this Engine has a dependency on the Car. This way, the Spring Context or the Spring Framework, it will keep circulating between Car class and Engine class, and after a few seconds, it is going to give up and throw an exception. Let me show you the demo as well. I'm going to execute this example4 class, as you can see, we got an exception. The exception name is UnsatisfiedDependencyException. Here, the exception also clearly highlighted. Is there an undissolvable circular reference or an asynchronous initialization dependency? That's why you need to be very careful while you are trying to perform Constructor Injection. So please make sure you are not creating the circular dependency. There is no other workaround to resolve this problem. So I'm going to revert all these changes inside the Engine class so that we can get rid of the circular reference that we have created. I'll save this and execute this example 4, and I'm going to get expected output. Whatever we discussed inside this lecture, I have mentioned those details in these slides. I also highlighted that the Constructor Injection is the recommended approach. This is the preferred approach in modern spring applications because it works well with the immutability and unit testing. Inside the next slide, I provided the details around circular dependency and how to avoid it. To avoid this circular dependency as a developer, we need to perform some analysis so that there is no circular dependency between the beans that we are trying to create.