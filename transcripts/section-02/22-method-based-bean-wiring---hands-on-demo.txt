I'm going to create a new sub-package under the com.ezbytes with the name as ex4, ex4 means example 4. Into this package, I'm going to copy the code from example3 package and paste it here, followed by I'm going to rename this example3 to example4. After renaming this file, I'm going to open the beans package and here we have a Vehicle java class. Let me get rid of all these @Component because we are planning to use the @Bean annotation for our demo. Later on, I'm going to show you the demo with the at component as well. Let me remove these methods like sayHello(), destroy(), afterPropertySet(), everything I'm going to delete. It is going to have simply a name field along with the getter and setter. Apart from this code, I'm also going to define toString() method. Let me click on the command and shortcut and here I'm going to select toString(), which is going to generate a toString() method. The reason why I'm defining this toString() method here is, I'm going to write some logic to print these Vehicle object. Under such scenarios, I want the output to be printed by using this format. So that's a whole reason why I'm defining this method. Next I'm going to create one more java class with the name Person. This person is also going to have name field followed by is also going to have vehicle as the dependency to these fields. I'm going to generate the getter and setter. Using these getters and setters only we are going to access these fields from the Person object. Next I'll go to the config package and open this ProjectConfig. And here I'll get rid of these components can followed by I'm going to create a java method with the name vehicle, which is going to take care of generating the bean of Vehicle. So here, please make sure you are importing the vehicle from the example4 package followed by, I'm also going to import the @Bean annotation. In the similar lines, I'm also going to create another java method, which is going to create the bean of Person. So let me type @Bean followed by this method is going to return the object of Person. Let me mention the method name as person() itself and inside this method, I'm going to create the object of Person. Once the object is created, I'm going to assign some name to this person by invoking the setName() to the name, I'm going to pass the value as Lucy. And at last, I'm going to return the Person object. With this code, there won't be any wiring between these two beans inside the spring context, Person bean and Vehicle bean, they are going to be maintained as two different independent beans. I can confirm the same as well by going to this example4. And here, what I'll do is, I'll try to get the bean of Vehicle and Person. We already have code to vehicle. Let me fetch Person bean as well with a variable name as person and here I'm going to update the variable name to Vehicle, followed by I'm going to get rid of this code and paste to three new lines of code. By using these three lines, first, we are trying to print the name of the person inside the spring context, followed by vehicle name. Whereas by using the last statement, we are trying to print a statement vehicle that person own is person.getVehicle(). We know as of now, there is no wiring between the Vehicle bean and Person bean. That's why we are going to get the null as an output. Let me execute this example4 as soon as I execute, like we expected, we got the null as an output. But coming to the person name and vehicle name, we have the correct output. This gives a confirmation both beans are created, but they are created in an independent manner there is no dependency between them. Now let's try to establish that dependency between them by using wiring concept. So what I'll do is, I'll go to this ProjectConfig and here just before returning the Person object, I'm going to invoke the person.setVehicle(). And to this method, I'm going to assign the output that I'm going to get from these vehicle methods. So here I'm trying to invoke the vehicle() method. If I save this and execute this class, I'm going to get this time the output saying that vehicle that person own is vehicle with the name as Toyota. So from where this is coming, this is coming from the toString() method available inside the Vehicle class. Since we are trying to print the Vehicle object, so the logic that we have written inside this toString() will be used to display the content of the Vehicle object. This is one option of wiring. So here we are trying to wire the beans by using the method invocation. Instead of method invocation, we can also perform the wiring by using the method parameters as well. Let me show you that approach as well. I'm going to copy this code and paste again. Let me comment this code for your reference. Now to this method, I'm going to pass Vehicle as a dependency. So whatever method input parameter that I receive, the same I'm trying to pass as an input to the setVehicle() method. With this, now we are not directly invoking the vehicle() method, which is going to give us the bean of the Vehicle. Instead the Spring framework is going to invoke the vehicle() method and inject that Vehicle bean as a dependency into this person() method. That's why by the time this line of code is executed, the Vehicle bean is going to be injected as a dependency into this Person bean. So with this setup, first the Vehicle bean is going to be created, followed by the Person bean. Now the Person bean has a dependency on the Vehicle bean. We can confirm this behavior as well by having a System.out.println() inside the constructor. So what I'll do is, I'll try to create a constructor by mentioning public Vehicle and inside this constructor, I'm going to have System.out.println() with a statement Vehicle Bean Created. Let me do the same for person as well. Inside the Person class, I'm going to create a constructor. Let me update this to Person and here also I'm going to mention as Person Bean Created. If I do the build and start this Example4 class, as you can see, first Vehicle bean is created, followed by Person bean and the output also is a valid output. Instead of null, we are getting the name of the vehicle because both these two beans are wired together by using method parameters approach. These are the approaches that you need to follow while you are creating the beans by using at @Bean annotation. To establish the dependencies between the beans you need to perform the wiring by manually writing this kind of code, but there are better alternatives as well. From the next lecture, I'm going to show you how to perform the auto wiring when we are using stereotype annotations like @Component annotation. When we are using stereotype annotations, the wiring is going to be simple.