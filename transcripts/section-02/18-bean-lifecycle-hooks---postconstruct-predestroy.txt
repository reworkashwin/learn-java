Let's try to understand on how to populate some data into the bean that is created by the spring framework using @Component annotation. Right now, the developer don't have any opportunity to populate some data. Off course, to this name we can assign some default value, but the same default value will be considered for all the beans or the objects that is created from these Vehicle class. So this is not the recommended approach. Instead what we have to do is, inside the same class where we are using this stereotype annotation, we need to create a method here I'm trying to create a method public void and the method name I'm trying to give as initialize(). So this method name can be anything inside this method we can write any logic to populate data into the Vehicle Bean object. But to make this work, we need to tell to the spring framework to invoke this method once the bean is created or once the object of the vehicle is created. Once the bean is ready, then only we can populate the data inside it. So to convey the same to the spring framework on top of this method, we need to mention an annotation PostConstruct, but this annotation is not from the spring framework library instead it is from the Jakarta Enterprise Edition library. As I said before, spring framework is inspired from the Jakarta Enterprise Edition or the Java Enterprise Edition. That's why few of the specifications which are good inside the Jakarta Enterprise Edition, we should be able to use them inside the Java framework without making any changes. So let's try to use this annotation. This annotation is at the @PostConstruct, but to use this annotation first we need to add a library from the Jakarta Enterprise Edition. To know the details of this library, I came to the maven repository website. Inside this website, you can search for Jakarta annotation. In the search results, we are going to have this result with the name Jakarta Annotations API. Please click on the same as of today, this is the latest version 3.0.0. I'm going to click on the same and copy these maven dependency details. So in case if you are using other build tool like Gradle or any other tool, you can click on the respective tab and copy the dependency details. Now I need to go to the pom.xml file that I have inside this project. Here just after the spring context dependency details, I'm going to add the new dependency details that we have copied. Whenever you add new dependencies into the pom.xml, this icon is going to appear to sync the maven changes. Please make sure to click on the same, which is going to take care of downloading the new libraries into your local repository. Now on top of this method, I'm going to mention @PostConstruct annotation. With this annotation, we're telling to the spring framework, please execute this method once the bean is created. Whenever this method is getting executed, I want to assign a value to the name variable. So this indicate the current bean object using the same I'm invoking the name field and I'm trying to set a value called Audi. So now if I save this and do the build this time, if I execute the example3 program again, I'm going to get the expected output. This time here instead of null, we have the Audi value. This way, whenever you want to have some control, once the bean is created, you can use this @PostConstruct annotation. Please remember you need to use this only when you are using the stereotype annotation like @Component. In case if you don't want to use these @PostConstruct annotation, which is from the Jakarta library, spring framework also provide another alternative to show you that alternative I'm going to comment this code followed by I'll go to the top of the class and here this class, it needs to implement an interface with the name initialize bean. When we are implementing this interface, we need to implement a method with the name after property set. Let me click on the same. Now inside this method, I can have the same code like this . name is equal to maybe this time instead of Audi, I'm going to define as Tesla. With these changes now, if I try to execute the example3 code, this time I'm going to get the output as Tesla. So this is the spring provided approach, but I feel this is a lengthy approach like you have to implement the initialize bean and you had to overwrite the method. Whereas using the @PostConstruct annotation is a pretty easy one. While using the @Bean annotation as well, we can try to define some initialization method as well. For example, if you go to the ProjectConfig under the example2 here, while we are using the Bean() method, we can invoke a parameter with the name initMethod to this initMethod we can pass the name of the method that spring framework can invoke once the Bean of this class is created. But usually we don't use this approach because anyway, we have enough flexibility inside this method to populate the initialized data. But you can use this approach in the scenarios where if this vehicle1() method is calling some other third party method and it is providing the return object of the vehicle and before you transfer that as a Bean, if you want to populate some data then you can define some initMethod by providing some method name like initialize(). So whatever method name that you have mentioned here, you have to define a method with the same name inside the Vehicle class. If you click on this initMethod inside the documentation also, they clearly highlighted this is an optional parameter, not commonly used given that the method may be called programmatically directly within the body of a Bean-annotated method. What they mean with this statement is, instead of mentioning the initMethod here, the developer can directly call that method by using this Vehicle object inside the body of the method. That's why this approach is less commonly used. Let me delete the same and go to this Vehicle class. So this is about @PostConstruct. That means once the bean is created or initialized so these methods like after property set or this initialize method will be invoked by the framework. And these methods will only be invoked only once in the bean lifecycle and when is that is going to happen? Once a bean is successfully created and the dependencies are injected into this bean. Once this operation is completed, then only these methods will be invoked by the framework. Just like how we have a chance to invoke our business logic once the bean is created, we also have a flexibility to invoke our business logic just before the bean is getting destroyed. Usually when the application is getting shut down or when the program is being terminated, the spring beans, they're going to be destroyed from the spring IOC container. So just before that destroying process, if you want to have some flexibility to execute some code, like you may want to execute some cleanup code, or you may want to close some of the connections or the resources hold by the bean. So all such operations you can also perform for the same you need to use an annotation with the name @PreDestroy. So just like how we have these initialize method, we can create any other method. Let me create the method as public void destroy(). So this method name can be anything. So inside this method, I'm simply having a statement saying that Destroying the Vehicle Bean. On top of this method, I'm going to mention the annotation @PreDestroy. So with this now, whenever your bean is getting destroyed by the spring IOC container, just before the destroying process, this method is going to be executed. I can show you a demo as well. Let me go to the example3 to simulate the destroying process of the IOC container what I'm going to do is, by using Context object I'm going to invoke the close() method. So this is going to trigger the destroying process. So let me save this, do the build once the build is completed, if I start this Java class, you will be able to see I am seeing a statement Destroying Vehicle Bean. This gives a confirmation to us that our method is getting invoked just before the destroying of the bean inside the spring context or inside the spring IOC container. In case if you don't want to use the annotation from the Jakarta enterprise edition, you also have a flexibility from the spring framework. Let me comment this code. This time you have to implement a interface with the name Disposable Bean. So let me click on this implement methods. We need to implement an abstract method destroy() inside this method I can have the same system.out.println() with the value Destroying Vehicle Bean. So let me save this, do the build. Now, if I go and execute the example3 program, I'm going to have the same output. @Bean annotation also have an option to execute some business logic before destroying the bean. So let me go to these ProjectConfig. So here, just like how we have initMethod, we also have destroy() method to this destroy() method we can pass the method name, which we have defined under the Vehicle class. This approach is also not commonly used because we can execute whatever logic that we want to execute inside the method body() itself, or we can invoke the same destroy() method from the method business logic. But I'm trying to share this information so that you are aware about all these three options. Whatever we discussed inside this lecture, I documented in these four slides for your reference. Please spare a minute and read all this information so that now you better understand the purpose of these annotations are the approaches that we have discussed.