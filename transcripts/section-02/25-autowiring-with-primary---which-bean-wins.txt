So far, we explored on how to perform auto wiring between the Spring beans. As the next step, let me show you some advanced or complex scenarios. In the same process, we are going to face some challenges, and I'm going to show you the solutions for them as well. I'm going to create a new sub package with the name ex5, ex5 means example 5. Inside this package, I'm going to create another package with the name beans. Inside this beans package, I'm going to create an interface with the name Coffee. So this is an interface and this interface, it is going to have an abstract method. The abstract method name is makeCoffee and this Coffee, it is going to return String as an output. Now let me create the implementation classes for these Coffee interface. I'm going to create a java class with the name Cappuccino. This class, it implements Coffee interface. Since it is implementing, it has to implement the abstract method with the name makeCoffee. So from this makeCoffee abstract method, I'm going to simply return Cappuccino coffee as an output. In the similar lines, I'm also going to create one more implementation class with the name Espresso. This class also implements Coffee interface, let me implement the abstract method from the abstract method I'm going to return Espresso Coffee as an output. So there is an interface with the name Coffee and this interface has two implementation classes. Next, I'm going to create one more java class with the name CoffeeShop. This CoffeeShop has a dependency on the Coffee interface. That's why I'm going to create a java field of type Coffee inside the java class, I'm going to mention @Component annotation on top of this class, since this CoffeeShop has a dependency on this Coffee, we need to make sure we are performing the Constructor Injection. So I'm going to create a constructor and inside this constructor, I'm going to have a logic this.coffee = coffee. Let me create a getter() method for this Coffee variable. Since it is a final variable, we can't create the setter() method that's why I simply created a getter() method alone. So now I'll go to the Coffee interface and this interface has a couple of implementations on top of these two classes, I'm going to mention the @Component annotation. So with these, both these Cappuccino and Espresso, they're going to be treated as Spring beans. With this setup, let's try to understand what is going to happen. So the CoffeeShop has a dependency on the bean of type Coffee. So by looking at this Constructor Injection, the spring framework, it will try to inject the bean that implements the Coffee interface, but it is going to find two beans inside the spring context and with that, it is going to have the ambiguity and due to this ambiguity, the auto wiring is going to fail and the framework is going to throw an exception. Let's try to see the demo of this. I didn't mention @Autowired annotation on top of these constructor, because we have a single constructor inside this class, this is completely optional, but I'm trying to mention the same for your reference, since you started learning auto wiring just now for the next few lectures, I'm going to mention this @Autowired annotation, but once we make some progress inside the course, I'm not going to mention these @Optional annotation, let me create a new package with the name Config inside this package, I'm going to create a new java class with the name Projectconfig on top of this class, I'm going to mention @Configuration annotation, followed by @ComponentScan annotation by using this annotation, I'm going to invoke the base packages and to the base packages, I'm going to pass the package name where I have defined all my beans. I defined all my beans here, let me copy the same and mention here. Now I'll try to create a main class, what I can do is, I can try to copy this example4 and paste it here and rename this to example5. Let me clear some code here I'm going to get rid of all this code what I'll do is, from this context I'll try to get a bean of type CoffeeShop on the left-hand side, I'm going to mention the variable name as coffeeShop. Once the CoffeeShop bean is retrieved by using the same, I'm going to invoke the getter() method that we have inside the CoffeeShop class, this getter() method. It is going to give you the implementation class of the Coffee interface. That's what I'm trying to accept by using the Coffee variable. Next, I'm going to have a system.out.println() and to this println() statement, I'm going to pass the output that I'm going to get when I'm invoking the makeCoffee() method by using these Coffee variable. If this code if you try to execute, obviously we're going to get an exception here, we are getting the exception as NoSuchBeanDefinitionException. This is because my IntelliJ, it is trying to do a smart job here. It is detecting the problem at the compilation time itself. So while we are doing the auto wiring using Constructor Injection, it is throwing a compilation error saying that I cannot perform the auto wiring because there is more than one bean of Coffee type. What are those beans? Cappuccino and Espresso. Due to this compilation error, the auto wiring is not happening, but if you use some other ideas like Eclipse or visual studio code, I'm sure there is a good chance that they may not find this auto wiring problem during the compilation problem and with that, you're going to get an exception with the name NoUniqueBeanDefinitionException. We already saw the demo of that exception in the previous lectures to solve this kind of problems during the auto wiring. There are multiple approaches. The first approach is you can mark any of these beans as primary. For example, if I mark this Espresso as the primary bean, then the compilation problem is going to be solved. If I go to the CoffeeShop right now, the compilation issue is resolved because right now the spring framework has enough clarity on which bean it has to inject as a dependency into this CoffeeShop. Though there are two beans of type Coffee, the spring framework it is going to construct this Espresso for the dependency injection since we mark this as primary. So what is the output that we can expect? We can expect Espresso Coffee as the output when we execute this example 5. I'm still getting that NoSuchBeanDefinitionException for the CoffeeShop. I know what is the problem. If you go to the example 5, it imported the ProjectConfig from the example 4 package. So let me get rid of these and import these ProjectConfig from the example 5 package. Let me save this, do the build once the build is completed, I'm going to execute the example 5 one more time, and this time we got the output as Espresso Coffee. Let me try removing these primary and see what is going to happen. The compilation is failed inside the Coffee Shop. So if I still try to execute this example 5, I am going to get NoUniqueBeanDefinitionException. Here, whatever error that we're seeing from the IntelliJ, it is a kind of warning it is giving more details to the developer, but it is not stopping the developer from executing these example 5. But anyway, we are going to have enough details inside the console and to resolve these NoUniqueBeanDefinitionException, you know what to do. We have to mention @Primary annotation on top of one of the implementation. Now, this time I'm going to get the expected output. Just like @Primary annotation, we can also use @Qualifier annotation as well to resolve the ambiguity that Spring IOC container is going to have. Let's see the demo of Qualifier in the next lecture.