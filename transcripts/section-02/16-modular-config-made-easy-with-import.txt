So far, we are defining all our beans in a single configuration class with the name ProjectConfig. Here you may have a doubt, is it mandatory to define all the beans definition in a single class? Off course, it is not mandatory you can create any number of configuration files. Let me show you the demo around this as well. So under this config package, this time I'm going to create one more Java class with the name as AnotherProjectConfig. On top of this class also, I'm going to mention @Configurationannotation followed by i'll try to create a bean of type String. I'll try to define a method which is going to return String object. The method name I'm going to mention as helloWorld() and from this method, I'm trying to return a String object. In the similar lines, we can create any number of beans. Maybe I'll try to create a bean of type Integer. So let me define a method which is going to return Integer with the method name as LuckyNumber(). From this method, I'm going to return a number 16. If you see right now, we have two different configurations where we have provided the details of multiple beans. So this way you can create any number of configuration files, but you need to make sure to provide all these configuration details to the Spring context. So inside the example 2, we're trying to invoke the constructor of AnnotationConfigApplicationContext to the same so far we have provided a single configuration class, but this constructor it is going to accept any number of configuration class. So here if you see, there is a spread operator. So whenever you see this spread operator inside Java, it's a confirmation to you that so-and-so method or constructor, it is going to accept any number of parameters as an input. So what we can do is, along with the ProjectConfig.class, we can also provide AnotherProjectConfig.class as an input. With this now, we should be able to fetch the beans that we have defined in this new configuration class as well. So what I can do is, I can copy this code and paste it here. So this time, I'll try to fetch the bean of type String. Let me mention the variable name as helloWorld. The same, I'll try to pass as an input to this System.out.println(). So here instead of vehicle name, I'll just mention String value from Spring context is so-and-so value. So let me save this, do the build once the build is completed, if I execute this program I'm going to get the HelloWorld as expected. Instead of defining all your configuration class like this, there is also a better alternative. In real applications, we're going to have more than 20 configuration files or sometimes even 50 configuration files as well. So mentioning all those configuration details as an input to this constructor is a very cumbersome process. Under such scenarios, we can also use another recommended approach. The recommended approach is, by using @Import annotation. So what we can do is, on top of these any one of these configuration class, we can mention @Import annotation to this @Import annotation, under these curly braces, we can mention another configuration classes like AnotherProjectConfig.class. Since I have a single class, I'm trying to mention the same. In case if I have multiple classes, either I can mention all of them here, or I can also make sure that all these configuration classes they are following some inheritance hierarchy, just like how we have inside Java. So with this now, I don't have to mention another ProjectConfig.class here. If I remove these, still my code is going to work because we are passing only ProjectConfig.class here. When Spring Framework look at this configuration class, it also sees an import statement here from this import statement, it will go to this class if this configuration also has another import, it will go to that import class as well. So this way, the Spring Framework will make sure to load all the configuration classes along with the beans that you have configured inside them. So if I save all these changes and do the build and execute the program, this time also I'm going to get the same output. Whatever we discussed around the @Import annotation, I mentioned all those details and example in this slide for your reference. Always use this annotation when you have multiple configuration files inside your project.