So far, we are creating the spring beans by using @Bean annotation. Inside this lecture, I'm going to show you another alternative approach. So this approach is most commonly used by the real developers as well. For our demo purpose, I'm going to create a new package with the name ex3, ex3 means example 3. Into this package, I'm going to copy all the code from ex2 package. Once the code is copied, first we need to rename this example2 with example3 so that we will not have any confusion. Next I'll go to the config package. Here I'm going to delete this AnotherProjectConfig and go to this ProjectConfig. So here I'm going to delete these @Import annotation followed by I'm going to delete all the code that we have written to create the beans by using @Bean annotation. Because we are going to use a different and best approach. Now I'll go to the Vehicle class. On top of this class, I'm going to mention an annotation with the name @Component. When we are keeping these annotations on top of a Java class behind the scenes, the spring framework it is going to create the bean of this class. So now the responsibility of creating the object of this class by using the new operator is going to be with the spring framework. After mentioning this annotation, we need to go to the ProjectConfig file and on top of these file, we need to mention @ComponentScan annotation. Since real applications are going to have thousands of Java classes, we can't expect the spring framework to scan each and every Java class and look for these annotations so that it can create a bean. That is going to create serious performance issues. So that's why as a Java developer, we need to give enough clues to the spring framework about the packages or the classes where we have defined these annotation @Component. By mentioning @ComponentScan annotation, I'm going to give those clues to the framework. Now I need to type base packages. So to these base packages, we need to provide the list of packages where we have defined the beans. So first, let me mention curly braces followed by I'm going to mention this package name com.eazybytes.ex3.beans. We need to mention the package name as a String. That's why I have surrounded this package name with a double quote. Now with these during the startup of my application, the spring framework is going to look for the configuration classes like ProjectConfig. On top of this class since we have mentioned the @ComponentScan annotation, it is going to visit all these packages and scan for the classes which are marked with @Component. And behind the scenes, the bean of this class will be created. So now I can go to these example3, and here I'm going to get rid of all these code that we have written in the previous lectures. Since we are going to have a single bean of type vehicle, I can use context.getBean() by passing vehicle data type and here I have mentioned that ProjectConfig class as an input to these AnnotationConfigApplicationContext. So now if I try to execute this program, you're going to have a surprise. So let me execute this example3. As you can see, inside the output we are seeing the name of the vehicle as null. Though the bean is available inside the context, but this bean object it is not carrying any data related to this vehicle. This is because nowhere we have initialized the name of the vehicle. Whereas when we are using the @Bean annotation, the developers are going to have enough flexibility to create the object on their own and populate any data into this object. So this is one of the major difference between @Bean annotation and @Component annotation. Whenever you're looking to have complete control on how your object is getting created along with the populated data, then this is the most recommended approach. Otherwise you can simply follow with the @Component annotation approach. The reason why in real applications, most of the developers that are going to use this @Component annotation is usually in an enterprise application we will try to create the spring beans for Java classes, which holds the business logic inside the Java methods for simple POJO classes or data carrier classes, which transfer data from one place to other place for such a simple POJO objects, we are never going to create them as beans because those objects they are developed for the purpose of data transferring and these data will keep changing from request to request. Whereas the Java classes where we have written all our business logic so those Java classes and their objects are going to remain for all kinds of requests that the application is processing. Because for everyone, the business logic is going to be the same. Just for our simple understanding purpose, while we are discussing the spring core concepts I have taken this simple vehicle class. But when we start building a real enterprise application, I'm going to show you what are the best practices that you need to consider to create a bean. For now, focus on learning the various annotations and syntaxes that you need to follow to create a bean successfully. And these @Component bean, it is also called a stereotype annotation. Inside spring, there are many other stereotype annotations, which we are going to discuss in the coming lectures. The reason why this annotation it is called a stereotype annotation is, in English stereotype means commonly accepted pattern or approach. Since mentioning the annotation on top of the class is a commonly accepted approach inside the spring framework, these annotations they are called as stereotype annotations. There are many other advantages with these stereotype annotations, which we are going to discuss in the coming lectures. In this vehicle class, I can also create one more method with the name sayHello(). So whenever someone invoke this sayHello(), this statement is going to be printed. The statement is printing hello from component vehicle bean. So what I can do is, I'll go to the example3after this statement is printed, I'm going to invoke the sayHello()method by using vehicle bean. So as soon as I press ., you'll be able to see I got an option to invoke the sayHello() method. If the bean or the object is not created, I'm going to get null pointer exception. Since the bean is getting created by the spring framework, we are going to get the output when we invoke this sayHello(). If I execute this program you'll be able to see, I got the expected output saying that printing hello from component Vehicle Bean. By looking at all this information, you might be inclined to use @Bean annotation because it is giving you enough flexibility to populate the data. But as I said in real applications, there'll be very rare scenarios where you may want to populate some data into the object to bean that is going to be created. Under such scenarios, you can happily use this approach. We should also be able to populate some data into the object bean when we are using the @Component annotation as well. I'm going to show you those approaches in the next lecture. For now, I'm assuming you are clear on how to create a spring beans by using @Component annotation. When you are using this annotation, please make sure you are mentioning the @ComponentScanAnnotation along with the package details on top of the Configuration class.