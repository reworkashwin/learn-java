Let me comment the @Primary annotation that we have mentioned on top of the Espresso java class. With this now if I try to execute the example 5, I'm going to get the exception which we already know the exception is NoUniqueBeanDefinitionException. Just like how we solve this problem by using primary, we can also solve the problem by using @Qualifier annotation. Before we try to use the @Qualifier annotation, we need to know what are the names of our bean. We know behind the seens  Spring is going to create the bean of this class and Cappuccino, by default those bean names will be class name itself. If you want to give your own bean name, you can do that by passing the name to the @Component annotation. Now, whatever name that you're trying to pass, the same will be considered as a bean name. I'm trying to give the same default name but in case if you want to change this, you can change based upon your requirements. Let me do the same for Espresso. So for the Espresso also, the bean name I'll try to mention espresso itself with complete to small letters. So now I can go to this CoffeeShop here under the constructor of CoffeeShop we have a compilation error because the auto wiring is going to fail. Since we have a couple of beans that implements Coffee interface. Just before this Coffee constructor input parameter, we can mention @Qualifier annotation and to this @Qualifier annotation, we can mention the name of the bean. I'll try to mention the name of the bean as Cappuccino here. So with this, what is going to happen? The spring framework, it is going to search inside the spring context for the bean with the exact name as cappuccino and the same bean will be injected into this CoffeeShop. Now if I try to execute the example 5, I'm going to get the Cappuccino Coffee as an output. It is also completely possible to use the combination of Qualifier and Primary as well. Let me mark @Primary on top of the Espresso again. This time still my spring IOC container, it is going to consider Cappuccino bean to perform the dependency injection. If I try to execute this example 5, we are still going to get the Cappuccino Coffee. That means when the primary and the qualifier both are defined, the highest preference will be given to the bean, which we have mentioned by using the @Qualifier annotation. You may have a question ? Under which scenarios we are going to use both @Qualifier and @Primary annotation. Let's consider a scenario where in majority of the business scenarios, you want to consider the espresso bean, but under certain business scenarios, you want to use Cappuccino bean. To handle these kind of special scenarios, you can simply use @Qualifier annotation in the classes where you are writing special business logic. Whatever we discussed around the @Primary annotation and the @Qualifier annotation, I have mentioned all those details in the slides that I have provided. In this slide, I have taken a couple of examples. First here, I have taken a Car along with the Engine implementation beans. There is an Engine interface and this interface is implemented by a couple of beans with the name petrolEngine and dieselEngine. If we are not marking this bean as a primary, we know what is going to happen we are going to get an exception. To resolve that exception we need to use this @Primary annotation and with that, the dieselEngine Bean will be injected as a dependency into this Car Bean inside the spring context. Just like @Primary annotation, we can also use @Qualifier annotation as well. When we are using the @Qualifier annotation, we can mention the bean name that we want to inject as a dependency into this Car class. So these @Qualifier annotation and the @Primary annotation, they are going to work for all kinds of auto wirings. Here in the slides, I have used the Field Injection, but regardless of Field Injection or Constructor Injection or Setter Injection, in all type of injections, you can use both these annotations. I know there'll be a confusion between @Qualifier and @Primary annotation. Let's try to identify the differences between them. You're going to use the @Primary annotation when you want to have a default bean that should be used in most of the scenarios. Spring automatically selects the @Primary bean if no specific qualifier is provided by the developer. Whereas you're going to use a @Qualifier annotation when you want to explicitly specify which bean to use for the auto wiring process. So this @Qualifier annotation, it is going to override the @Primary behavior and allows selecting a specific bean when multiple beans are present. Inside the slides, I also provided the Coffee and Coffee Shop example. You can consider these @Primary as a Default Choice. If you take an example, like you walked into your Coffee Shop and you ask this guy to give a Coffee, but you didn't provide any of your preferences and you said you're fine with any of the Coffee and this guy in this kind of scenarios, he's going to provide Espresso as the default coffee for these kinds of customers. But there is another example where one of the customer, he walked in and asked these guys specifically that I want the Cappuccino. So with the @Qualifier annotation, we're going to perform the Explicit Selection. When you are mentioning the exact coffee type, this guy is going to give that exact coffee to the customer. The same is going to happen with the spring IOC container. Before I try to close this lecture, I want to share an another important annotation that you're going to use very less inside the real projects. This annotation is @DependsOn. We know during the constructor injection auto wiring, that dependent beans will be created first followed by the parent bean. But what if you want to achieve the same behavior, even though two different beans they directly don't dependent on each other. Under such scenarios, you can use the @DependsOn annotation. So here I have an example, there is a method with the name engine() and vehicle(). Both these methods are going to create the Engine bean and the Vehicle bean. This Vehicle class does not use this Engine class or Engine bean anywhere directly inside its java class. There is no java field inside the vehicle with the type as Engine. So in this scenario, both Vehicle and Engine they are independent to beans and the order of these beans creation is going to be random. So sometimes Engine bean is going to be created first and other times vehicle bean is going to be created first. But if you have a scenario where you want to make sure that Engine bean is created first before the vehicle bean, then you can use these @DependsOn annotation to this annotation you can mention the bean name. So here the bean name you have mentioned is engine with this setup, the spring IOC container, it will make sure to create the engine bean first before the vehicle bean. Here I'm showing an example on how to use this depends on along with the @Bean annotation, but you can follow the same syntax and same strategy when you are using the stereotype annotations as well. But please make sure to use this annotation only when necessary. Otherwise you're going to bring the tight coupling between these two beans during the startup sequence. I'm not showing the demo of these beans since it is very simple I'm assuming you are clear in case if you want to try a demo, feel free to try this example in case if you are facing any problem, please shoot me a question inside the Q&A.