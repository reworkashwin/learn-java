So far, we set up to MySQL database inside our local system by using Docker. Now let's spend a few minutes understanding the difference between the MySQL database running inside our local system and the one that runs inside a production environment. Usually enterprise applications, they're going to set up the MySQL database in the cloud environment like AWS. So when they set up a MySQL database server using AWS, these database server, it is going to store all the tables data, columns data, and the records data that we store inside them inside the server hard disk. So every MySQL database server, it is going to have some hard disk space as well assigned like one terabyte or 500 GB based upon the project requirements. So when that data is getting stored inside the file disk or inside the hard disk, what is going to happen? Even when you restart your database server inside the cloud environment, your data is not going to last because the data stays in the hard disk. And typically enterprise applications, what they are going to do is they're going to take the backup of the MySQL data into some other locations as well. So that if something happens to the database server and the hard disk also lost, they can easily get their data back by using the backup. But coming to our local system, all the data that right now storing inside our database server, it will be stored within this container. If you open this container and go to this files section inside this page, look for the var folder. Under this var folder, there is a lib folder. Under this lib folder, there'll be again MySQL folder. Under this MySQL folder, all the data related to our tables, records, everything is getting stored. So with this kind of setup, what is going to happen? If I delete this container, whatever data that we stored inside our MySQL database, everything will get lost. I can show you a demo as well. But before that, let me stop the container and restart again. When you are stopping and starting the container again, your container is not going anywhere. It is still here, but it is in the stop mode even the data inside it, it will not get lost. So here I have done only stopping of the container. Now I'm trying to start again. Once the container started again, if you invoke the REST API, you are going to get the same response. That means we are not going to lose the data when you restart the container or stop the container. But there is a good chance sometimes you may delete the container. For example, I can delete my container by using this delete option and now along with the container, all the data that I stored inside the container is also lost. I can try to recreate the container, but I will not get that data again. Let me create the container again. Here I'm going to run the same command one more time. A new container is created. If I send the request to the REST API, I'm not going to get any response because there is no database tables present. That's why I'm getting this error. So all the tables that we stored and all the data that we stored is everything lost. So this is a typical problem that you're going to get when you're working with the Docker containers. And this is going to happen with any product. It can happen with MySQL or RabbitMQ or Kafka. So to overcome these kinds of challenges, you can use the Docker volumes concept. So by using the Docker volumes, we can mount a local system folder to the container so that the container can start storing the data inside a local system folder that we have provided. I'll explain you on how to configure the volumes while creating the container but before that, let me delete these container that we have created. This time also, I'm going to run the same command docker run-p along with the port mapping container name and with these environment variables, apart from all these data, I'm also going to give some volume related configurations. So just after the environment variables, I'm going to type - v ,v indicates volumes. So after these volume flag, I'm going to provide some volume configurations. So first I'm going to provide the local folder location. So under the users, I'm going to have a folder like eazybytes desktop. Under the desktop, there is a folder with the name jobportal-data. This is my local folder inside my system. And I'm using these unique style of syntax because I'm using Mac operating system. In case if you are using windows operating system, you need to provide the folder path by using the windows format, like C drive, followed by users, followed by your username after the username desktop and the folder name. Under my desktop, there is a jobportal-data folder is created. As of now, it is empty. So this is my local folder name. Next, we have to mention colon followed by we need to mention the container folder path where the MySQL data is being stored. So by default, we know it is going to be stored under the var folder, followed by lib, followed by mysql. So with these volumes configuration, what we're telling to the Docker container is, from now onwards store all the data inside this local system folder, which I'm trying to mount as a USB drive to this container. We know by default, the MySQL database server, it is going to write the data into this folder. So whatever data that is being written into this folder, the same will be written into these local folder as well. And let's imagine you have provided this volume mapping and you created a container and you started saving some data. And after a few days, let's imagine you deleted the container and this folder is still retained in your local system. And in that kind of scenario, if you try to run the same command by using the same volumes configuration this time during the startup, the MySQL database container, it is going to replicate the data present inside the jobportal data into these var, lib, mysql. And that is how we are going to retain the data, even if the container is getting deleted. As long as you have the jobportal data inside your local system, if you try to create the Docker container, even after one year, you are going to get the same data that you have stored. I'm trying to execute this command. Don't worry about this command I'm going to mention this inside the GitHub repo under the readme file. As soon as I press enter, a container will be created. Under the jobportal-data also, you'll be able to see a lot of data is being saved. The same will be leveraged by that database container. So whatever database container that we have created right now, it will not have any tables data and the company's data because we created a brand new database, so let me execute these scripts one more time. If you are not using IntelliJ premium version, please use sqlectron client application to execute all these statements. Now, let me go to the data and here also, I'm going to execute all these insert statements. Now from the postman, if I invoke the REST API, I'm going to get the data. Now let's see the beauty of volumes configuration this time, I'm deleting the entire container and I'm trying to recreate the container with the same command. When I say with the same command, with the same volume configuration. So I had to mention the same local folder name. As soon as I press enter, the container will be created. Since this time we have used the volume configurations to store the data permanently. If I invoke this API, I'm going to get the same company's response. And this is how you can store the data permanently inside a Docker container by using volumes concept. As a developer, you should be familiar with all these basic concepts of Docker. That's why I'm trying to take some time and explain them in detail. The details of the command that we have used, I have mentioned in this slide for your reference. So please take a minute and read these couple of slides so that you are comfortable with the Docker run command that we have used.