By default, the framework it is going to look for compose.yml file. But in case if you want to use different name for your docker compose file, you should be able to achieve that by overriding that default name. You have to use a property with the name spring.docker.compose.file. To this property, we can provide any custom name, you can mention like jobportal-compose.yml. From now onwards, the framework is going to look for this file. Let me stop my application and start it again. This time, the application did not started successfully,because there is no file with this name. I'll go with that default name itself, but I'll keep this property name and value in a commented style, just for your reference. Let's explore a few more properties under the spring.docker.compose. So there is a lifecycle management property. To this property, there are three different values that we can pass. The default one is start and stop. With the default value what is going to happen is, whenever you are starting your application, the containers they are going to be started. And when you stopping the application, the containers will be stopped. But in case, if you don't want the spring boot application to not start or stop the containers, you can set the value none, or if you are looking only to start the containers, but not stop the containers during the stopping of the application, then you can go with the start only. But I recommend you going with the default option start-and-stop. With this, we know what is going to happen. A few seconds back, we stopped our application. That's why the container also stopped behind the scenes by the framework. These are the most important two properties under the spring.docker.compose. Similarly, you will be able to find many other properties. If you type spring.docker.compose, these are all the supported properties. Please explore all these properties from the official documentation. But I want to highlight about these stop.command and start.command. So if you type start, so there is a start.command by default for these up is going to be the value. Inside the docker ecosystem, just like how we have docker run command, we also have some docker compose command like docker compose up, docker compose down and docker compose start and docker compose stop. There is a good difference between these four docker compose commands. When you are using docker compose up behind the scenes, based upon the docker compose file that you have provided, if required, the framework is going to prepare the docker images, based upon the docker images, it is going to create the docker containers from scratch. Whereas with the docker compose start command, only the existing stopped containers will be stopped. So that's the difference between up and start coming to the down and stop. When we are using the down command, all the containers, they're going to be deleted when we are running the down command. Whereas with the stop command, the containers, they are not going to be deleted, instead, they will simply stop. So by default for this property, up is the default value. I recommend you using the same so that you don't have to take care of creating the containers manually by using the docker run command for the first time. In the similar lines, we also have stop command as well. If I type stop.command by default for this, the value is stop. I recommend using stop itself instead of down. If you use the down, every time the container is going to be deleted when you are stopping your application. Unless you want to start with a brand new container, every time you start the container, I don't recommend you changing this default value, always go with the stop. All that we discussed around the spring boot docker compose support, I have documented them in these three slides. In the very first slide, I have given an introduction around what is docker compose and how to define our docker container dependencies by using a file called compose.yml. We already discussed about these configurations. Inside the next slide, I mentioned on the dependency that we need to add into our spring boot application to integrate our application with the docker. When you are adding this dependency, what is going to happen? The spring boot framework, it is going to detect your compose.yml, which is present inside the root project to folder. When you're starting your application, the docker compose up command is going to be executed, which is going to create all the required containers and the required connection beans as well. And when you stop the application, the docker compose stop command is going to be executed. In case if the containers are already running, when you're trying to start your application, there should not be any problems, spring boot understands that and it will try to connect to them directly without restarting. Inside the next slide, I have mentioned all the important properties that we have discussed. Please make sure you are reading all these slides one more time so that you are clear with all the concepts that we have discussed so far. I'm going to end this section. Before that, let's try to test the API from the UI app as well. If I access the localhost 5173, I'm going to get the homepage and inside the homepage under the companies, I'm not seeing any company's data. Seems I have stopped my spring boot application, let me start this and also please make sure your UI application is running based upon the commands that we have discussed in the previous section. So now if you see under the companies, we're able to see all the company's details if you click on this company's tab as well, here also we're able to see all the 30 companies details. This confirms our UI application is properly talking with the spring boot application, and from spring boot application, we are interact with the MySQL database, which is running as a Docker container. And how the communication is happening between the database and the spring boot, by using the spring data JPA. I hope you remember all those concepts. By the time you come to the next section, please make sure you are revising all the concepts that we have discussed so far. You just have to quickly revise all the slides that I'm providing to you. Please make sure you are stopping your UI application before you wind up this section to stop the UI application. You just have to press control C with that your UI application will be stopped. In the similar lines, please make sure to stop your spring boot application, which is going to stop the dependent containers as well. Inside the Docker desktop, I'm able to confirm the same.