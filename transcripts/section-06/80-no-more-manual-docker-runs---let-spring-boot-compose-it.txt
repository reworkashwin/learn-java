Let's imagine a developer is done for the day and he want to stop all the running applications. So what developer is going to do first He's going to stop that database container Manually here by coming to the docker desktop and similarly the spring boot application also will be start by clicking on the stop button and let's consider a scenario where tomorrow the developer may add more dependencies like a rabbit MQ server or a Kafka or a Redis cache server. So when your spring boot application is depending on more number of products or software components the management of the database containers is going to be somewhat Inconvenience to the developer. The developer has to manually stop the containers and start the containers based upon whenever he want to test the spring boot application. Since it is not giving good to developer experience What spring boot team they have done is, they have introduced docker compose support under the spring boot. We just have to add a dependency inside the pom.xml, and with that while we are trying to start the spring boot application the framework will look what are the dependent . docker containers that this application has, and accordingly it is going to bootstrap all the dependent containers during the startup of my spring boot application. The same applies for the shutdown process as well when I am trying to stop the spring boot application all the dependent containers will also be stopped. The developer don't have to manually start or stop the containers. Let me show you very quickly on how to do that. We need to look for a dependency with the name Docker Compose Support. I'll explain you what is docker compose in few minutes. It is not exactly same as docker It is an advanced feature of docker. You need to select this dependency and click on the explore option and with that inside the pom.xml, you will see this dependency added. Let me copy the same now under the pom.xml just after the mysql dependency, I'm going to add this spring boot docker compose related dependency We need to make sure to click on this maven refresh option, which is going to take care of downloading all the required dependencies Now this time after adding this dependency if I try to start my spring boot application It is going to look for a file with the name compose.yml. My startup of the application is failed, because there is no docker compose file found in my project directory. So just under the project directory, we have to create a file, so this is my project directory so under these directory, I need to create a new file the name compose.yml Please make sure you are mentioning the same name by default, this is the name that framework is going to look for, in few minutes I'm also going to explain on how to use different file name as well by overriding these default value. So this is a simple yml file and inside these we need to provide some details on what are the database containers that my Spring boot application has a dependency. But before we try to define those details, we need to understand what is docker compose. previously, we saw by using the docker run command, we are able to create a database container. So whenever we want to create multiple containers, which belongs to multiple products, or multiple docker images, we need to run the docker run command individually one by one. And this is going to be a very cumbersome process right, in the real enterprise applications a spring boot application can easily dependent on four or five containers. Running all those commands, every time we want to set up the containers is a cumbersome process. In these kind of scenarios, where we want to start multiple containers at a time we can use the docker compose concept. All the docker container details we have to mention inside a file like compose.yml. So here, first I have to type services since we are looking to set up multiple services, we need to mention services as a parent property. Followed by colon, next we have to press enter, now i'm going to paste few lines of configurations. You can get the same from github repo Understanding these configurations is very easy. The first information that i'm trying to provide is, a name to my service So here the name that I have given is db service, so db means database. So this way if you have multiple services, you have to repeat these kind of configurations at multiple times inside the yml file. Under the service name i'm trying to provide the image, to this image I am mentioning the docker image name What is the docker image name that we want to use to set up the mysql container? It is the mysql itself followed by colon next i'm mentioning the tag name as latest. Even if you don't mention this latest, by default the docker it is going to construct the latest docker image, but it is a good practice to always mention the tag name if you are mentioning latest always the latest version of the database will be used. Next, we are trying to give the name to the container by using this container_ name. What is the name? It is jobportaldb after the container name, we are providing port mapping details to provide the port mapping details, we have to mention this ports colon followed by enter since this ports mapping it is going to accept a list of ports mapping details, inside the yml whenever you want to define a element under a list we need to mention the hyphen. After the hyphen I have mentioned  3306, 3306 which are the port mapping this way if I want to provide another port mapping for the same database container. I can press enter followed by hyphen, next inside the hyphen I can mention like 3307 colon 3306. This is how you can provide multiple port mapping, but for mysql database, it is not required. But few of the software products they are going to expose multiple functionalities at multiple ports. For example, if you take the rabbit MQ, which is the product that we use for asynchronous communication it is going to expose two different functionalities. One is the queue functionality and the other one is administrative functionality. In these kind of scenarios, you need to mention multiple port mappings. That's why the ports is allowed here, with a list of elements. Next we want to mention the environment variable details, that's why we need to mention environment the first environment variable that I want to mention is, password and followed by, database name the database name is jobportal and at last i'm trying to mention volumes configurations under the volumes also, we can provide multiple volume configuration. That's why here we have to mention hyphen followed by what is the local folder name after the folder name colon followed by container folder name. If you understand our docker run command most of the stuff here also is self-explanatory. But please make sure here we are following the yml file syntax. Under the yml, the positioning of the property keys and value is very important. if you see under the services, I have a single service with the name db service. In case if I want to create one more service, what I have to do, I have to make sure that the second service detail is, starting exactly at the same position, so here I have to go to the line where the db service is present and mention one more service. Here I'm getting a compilation error because i'm trying to create the service with the same name. Maybe I can change this to dbservice1. Let's imagine this dbservice1 is going to create some other container maybe related to kafka Or rabbit MQ or redis. Now if you see this dbservice is service1, and this is the another service. So this way you can define any number of service so that during the startup of your application all the dependent service they are going to be started by the framework. And under the each service, we have to mention similar details like what is the image? So this image since this belongs to the dbservice, we are mentioning these image details under the dbservice. We have to give some tab space and mention the image followed by colon, followed by space and the value. So after the colon the space is very important. Otherwise the yml syntax it is going to complain. I have a dedicated yml course as well. It is a short course, with two hours in case if you are very serious about yml, you can enroll into that and try to learn everything about yml. Yml is very important if you want to learn the cloud concepts, docker or kubernetes concepts, to some extent I also explained in detail about yml inside my microservice course as well. After the image we have mentioned the container name and since ports it is going to accept multiple values. we have to make sure under the ports, we are mentioning the port details by using hyphen. So if you see here the hyphen is starting under the ports, but not at the same location where the p started. So here there is a tab space followed by we have mentioned the value, next under the environment, since it is going to have multiple values under the environment also we need to mention under the environment but not at the same position of the environment. So all these image container name ports,environment,. volumes. They should be present under the service name at the same position. Whereas the values under them should be under the respective property. for example under the environment we have these two environment variables. Please make sure you are copying this compose.yml file content from the github repo, don't try to type it, because when you start typing even if you forget to add a space or if you forget mentioning a tab space, it is going to create some issues. Let me delete these dbservice1 related data. So far, our spring boot application has a dependency on a single container. You can save these changes, do the build, once the build is completed what i'll do is, I will delete this database container that we have and start my application. So as soon as I start my application, you will be able to see inside the console logs the framework, it tried to create a container with the name jobportaldatabase. It went through various stages, like Creating,Created,Starting,Started, Waiting and Healthy. Once the container became healthy, the actual spring boot application is going to start. So now my application started, and the container also started.  This time the container is started, under the name jobportal. So the reason why we are seeing that job portal name is, since the project name is job portal under this folder we have a compose.yml, it is taking this jobportal as a parent folder, and at this folder, it has a single container with the name jobportaldatabase. In case if you have defined multiple dependencies, all of them will be present under this jobportal. If I invoke this REST API i'm going to get the same response. Did you see this time even though we have not created the tables data and records data, still the rest api is working fine, because in this compose.yml also, we have provided the same local folder name. Where we have saved the previous container data. The same is leveraged by the new container that is created by this compose.yml. I hope you are clear, on how to make our life easy by using spring docker compose support dependency inside of a spring boot application. I want to share a few more details around this. Let's continue this discussion in the next lecture as well.