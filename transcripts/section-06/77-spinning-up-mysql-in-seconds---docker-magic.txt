We just have to run a single docker command, which is going to set up mysql database inside our local system within 1 or 2 minutes. Inside this docker website, under these products, you will be able to see there is a product with the name Docker Hub. Inside this Docker Hub, we are going to have various docker images. To access this Docker Hub, you need to go to the website hub.docker.com. Inside this Docker Hub, lot of AI models, docker official images of various products are available. For example, if you scroll down, there are some Gen-A models like qwen3, hylang, and similarly, if you are looking to set up some products inside your local system, like memcached, nginx, neo4j, postgres, mysql, mongo, this way you should be able to find the docker image of any software product or software tool. If you use docker, you don't have to manually install and set up any software product. Let me show you the magic of docker by setting up mysql database. Here we have mysql related docker image. There is a confirmation also that it is a docker official image. You can either click on this or you can go to the top and here you can search for mysql and press enter. And inside the results, you're going to have mysql with the docker official image tag you need to click on the same. All the docker images which are officially published by the developer team, they are going to have this docker official image tag. We know at the end of the day, some developers, they have to develop the mysql database related application logic. So what the mysql developers they have done is, they have packaged all the required components to set up the mysql database into this docker image. We as other developers, we just have to run a docker command to set up this database. Here you'll be able to see that this docker image is downloaded by more than 1 billion times. So that should give a confirmation to you how frequently this image is being used by developers inside the industry. If you scroll down here, you'll be able to see some commands. This is the command that we need to use to set up that database inside our local system. Let me copy the same and paste inside my terminal. So I'm using a terminal with the name war. You can go to this warp.dev website and download the terminal based upon your operating system. This warp terminal is an intelligent terminal. It has a good integration with all the GenAI models. So at any point of time, if you're not clear about any command, or if you want to ask some question directly from your terminal, you should be able to do that very easily. On top of that, this terminal, it is also going to do a good job by providing some recommendations as you're typing the command. Apart from these intelligent features, it has good UI as well. Once you log into this terminal, under your user logo, you will have settings. From these settings, you can go to appearance and you can choose any of the theme that you like. Right now, I'm using a theme with the name Pink City. Apart from theme, you should be able to change many other settings as well. Please explore this page and update the settings based upon your requirements. Let me close this, and this is the command that we found inside the Docker Hub page. With this command, we're telling to run a Docker container. So what is a Docker container? It is a running instance of our Docker image. That's what we're trying to convey by using this docker run command and to this command, we can provide various other details as well. By mentioning these hyphen hyphen name, we're trying to give a name to the container. If I don't give a name to the container, the Docker itself, it is going to generate a random name to my container, followed by I can provide some environment variables using which I can set up some password. We can provide these details at a later point of time. Next, there is a flag with the name -d. So d means detached mode. When we are using this -d, the container is going to start in a detached style. That means my terminal will not be busy until the container starts I should be able to run other commands and use my terminal as usual, even after this container is created. If I don't use this -d, then this terminal is going to be blocked so that I can't type other commands. I can show you the demo as well. Let me remove this -d. After that Docker run, I'm mentioning that Docker image name. So every Docker image name, it is going to have a name. So this Docker image name is MySQL. The same I'm trying to mention, followed by colon. Next we have to mention the tag value. Using the tag, we can represent which version of MySQL database we want to install. And mentioning this tag is completely optional. If you're not mentioning the tag and the colon, by default, the Docker, it is going to create the container by using the latest version of MySQL Docker image. So if I press enter, I'm getting an error. I use this dollar. I don't have to use a dollar. I just have to type Docker run MySQL and say the official documentation, they have mentioned this dollar representing that this is a terminal, but we don't have to use that dollar symbol. We just have to copy the command and run here. So as soon as I press enter after typing this Docker run MySQL and getting an error saying that I should definitely set an password to that database by using an environment variable with any of these variables. This is how the developers can enforce the other developers to provide some mandatory information to start a container. Since password is a sensitive information, the MySQL team, they are expecting to provide a password by the developer by using this environment variable, or we can use this environment variable like MySQL to allow the empty password, but I recommend you to provide your own password. So what we can do is, we can change the command this time to Docker run and -e by using this -e,  I'm telling that I'm trying to provide an environment variable. So the environment variable name is MySQL_ROOT_PASSWORD, to this I'm passing the password value as root followed by the image name. The image name is MySQL. So let me press enter, this time you'll be able to see the MySQL database is getting started inside my system as a Docker container. So first, what is going to happen is, the Docker image will be downloaded from the Docker Hub into your local system. Once a Docker image is downloaded, the Docker container by using that image will be created. Since I have started the image by not using the detached mode, my terminal is busy I can't run any other commands from this terminal. I had to open another window of the terminal, inside the Docker desktop under the images, you will be able to see there'll be an image with the name mysql. So all the Docker images that are downloaded into your local system, all of them, you should be able to see them under this images tab. Next if you go to the containers, you will see there is a container started by using the MySQL image and the name of this container is elegant_tu. Since we have not provided any container name, the Docker is giving some random name and there is no port mapping also defined for this container. If you click on this container name, you will be able to see the logs of the MySQL database server. . So the MySQL database server, it might have started at a port. For example, if I search for 3306, so here they clearly highlighted port 3306 is used to start this database server. But I can't access this database right now from my local system because this container will start in its own network. In order to access these database from outside of this Docker network, which means from our local system, we need to mention the Port Mapping so that using the host port number, we should be able to access that database server. Every container, it is going to have a container ID as well. Since we can't remember these random generated container ID, it is always recommended to give a container name. So far, we have given the responsibility of generating a random name of the container to the Docker. But the best practice is the developer has to define the name. What I'll do is, from my terminal, I'm going to press control C two times and with that, the container is going to be stopped. As a next step, we can try to provide our own container name and port mapping as well. So let me delete this container. So here you need to check these and select these delete option. So this is going to delete the entire container. Deleting the container should free your terminal, if not, please press the control C couple of times so that your terminal will get free. So this time I'll try to run the same command, but I'm going to run with the -d flag. That means I'm trying to start the container in a detached mode. So the locks of the container starting will not be displayed here. Only the container ID will be displayed and the terminal will be free to execute the other commands. Apart from that, I'm also going to give some name to my container. So how to give that, you just have to mention hyphen hyphen name. And to this, we can mention some container name. Let me mention the container name as jobportaldb. And next, we also want to provide port mapping. So to provide the port mapping, we have to use the flag -p followed by space and here we need to mention two ports by following the syntax like P1 colon P2. P2 is the port number which represents the container port where it has started within the docker network. So where it started, it started at 3306 and P1 is a port using which a developer want to expose the docker container traffic to the host mission or to the local system. So here typically we are going to mention the same port number like 3306. That means my Spring Boot application, if it want to communicate with this container, it has to mention the port number as 3306. In case if I mentioning here as 3307, then this 3307, it has to mention, it should not mention the second port number because the second port number is internal to the docker network. Whereas the first port number represents the host port number where the container is running. Using these host port number only the docker container, it is going to accept the traffic. So when we send the traffic to this port number internally, it will be forwarded to the 3306. Let me mention 3306 in both places, which is the recommendation or the standard. After port mapping, I also want to provide another environment variable, just like how we are providing the password by using this environment variable. By using another environment variable with the name MySQL_DATABASE, I want to create a database schema inside that database server. When we start the container by using MySQL docker image, a database server will be created and inside this database server, by default, no database will be created. Since we are looking to use this database server to have some database schema and tables created inside them, we need to make sure we are mentioning what is the database that needs to be created inside this container. If you're not mentioning these environment variable, we may have to manually run some SQL scripts to create the database schema. Just to avoid that, I'm trying to provide this environment variable to this I'm going to mention the value as jobportal. So we're going to have a database server and inside that we are going to have a database schema with the name jobportal. The container name will be jobportal database. As soon as I press enter this time, since we ran the command using detached mode, the terminal is free to execute other commands. This is the container ID, which is printed onto the terminal. Under the docker desktop, we should be able to see there is a container created with this name jobportal database, and this time we are able to see the port mapping details. From our local system, we should be able to access this database by using HTTP localhost 3306. With this, our mysql database is ready. In the next lecture, let's try to connect to this database from our Spring Boot application.