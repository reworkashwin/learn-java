I'm trying to use different background for the slides. I keep doing this i'll try to change the background of the slides for every few sections. This I'm doing intentionally to avoid some boredom for you. I want to keep you engaged as much as possible. That's a whole intention. Now coming to the Docker. Docker is a tool that helps to package an application and run it anywhere. So we can think Docker as a magic box that contains an application related logic. The application can be anything. It can be a backend application, or it can be on front end application, or it can be a software application like database or message queue, regardless of what is the type of the application by using Docker, we should be able to package everything. And inside the package, we're going to have application code or application logic. Along with that, all the softwares are dependencies that an application needs. Apart from that dependencies, the Docker package, it is also going to have all the configurations that are required to start an application very quickly and easily. Once we package an application, the same can be used to run the application anywhere. Inside your local system or inside a cloud environment or inside your friend system, regardless of where you're trying to run this package in all the places, the application it is going to work exactly the same because the dependencies, the versions and the configurations, everything we are using the exactly same setup. Docker is like a shipping container for a software, just like inside shipping industry, how containers revolutionized cargo transport. The same has been done by the Docker as well for the software applications. We know how the ships they are going to transfer the cargoes from one place to another place. Inside the ships, we are going to load the containers and inside these containers, we're going to keep all the cargo material. The beauty of this container is, the same container is going to fit in all kinds of places. Even if you want to send the same container into other ship, or if you want to transfer it by using train or truck, regardless of what is the mode of transfer that you're trying to use, the same container can be leveraged. Before containers, the shipping industry, they used to face a lot of problems. Like one customer, they're going to bring their cargo in a square box the other customer, they're going to bring their cargo in a rectangular box, similarly someone will bring their cargo in a triangle shape container. Just to avoid all these problems, the shipping industry, they migrated to a standard container shape and size. Docker also has done the same for the software applications as well. Docker packages, an application with everything it needs to run. With that, the application, it is going to become portable, works the same on any computer where the Docker is installed. Before Docker, developers, they used to struggle a lot. Many developers, they used to complain that the application is working perfectly in my system. I'm not sure why it is not working in the QA environment or in the UAT environment or in the production environment. Inside the different environments, the applications, they used to behave differently. The reason is, in order to set up these applications before Docker, someone has to manually install that dependencies, when I say dependencies, these dependencies can be frameworks or libraries or the software components or any software products. The server admins, they need to take care of installing all these dependencies manually. When someone is taking care of these manually, there is a good chance that they may miss making some configuration or they may install some other version instead of the recommended version. With all these challenges, the applications, they used to behave differently in different environments or in different systems. Even it is a painful process for the new developers as well to set up an application in their local system, because they have to follow a lot of documentation to install all the dependencies manually by making sure to follow the correct configurations and correct versions. If the developers screw up even a single step, the application will not work as expected. The application servers also, they used to crash a lot due to mismatched software versions. But Docker solves all these problems by providing consistency, isolation, easy setup, portability and faster deployment. How Docker is providing all these advantages? It's by following the principle of packaging an entire application along with its dependencies and configurations into a single magic box. You can carry that box wherever you want, the application, it is going to work exactly the same way. I have a scenario as well. What used to happen before Docker and after Docker. Let's imagine you're trying to set up a Spring Boot application, and usually a Spring Boot application requires a JDK version, a build tool like Maven or Gradle, a database server, caching related server, messaging related application. And it also requires some configurations around the database connections, ports and environment variables. Before Docker, if a new developer joins the team, the developer has to make sure to install all these dependencies in a specific order with a given specific versions along with a given specific configurations. The same needs to be followed when a server admin trying to deploy this Spring Boot application across various environments like dev, QA and production. But with the Docker, we just have to run a single Docker command like docker-compose-app. And with that, the entire application, it is going to be set up with all the required dependencies. Behind the scenes, what is going to happen is, a developer is going to prepare some instructions on how to package this application. Once the packaging of the application is completed, the same package can be given to other developers or to the server admins so that the deployment can be taken care in the higher environments. As long as if someone is using the same package, the behavior of the application is going to be very similar across multiple environments or across multiple systems. Within a couple of minutes, the entire application is going to be set up with all the required dependencies. If the same application has to be set up manually, a developer will typically take at least 30 minutes of time. So I hope you're clear on why we should use Docker. When we start seeing the demo of this Docker, it is going to be more clear for you. And here you may have a question saying that Madan, you keep saying that Docker is going to package an application. How Docker will know what are the dependencies that my application has? Let's try to answer the same. But before that, I need to give some introduction about some jargons that Docker has. So inside Docker, you're going to listen various jargons like image, container, volumes, port mapping, and so on. Let's try to understand them one by one. The first one is Docker image. So this Docker image represents the Blueprint of an application. In other words, this Docker image is nothing but the package that Docker is going to prepare for your application. Someone who is good at Docker concept, like a developer or a DevOps team member, what they're going to do is, they're going to prepare a file called Docker file. And inside this Docker file, they're going to mention all the instructions that this is my application name, and it has so and so dependencies, and it has to use so and so configuration, so and so environment variables. So once this Docker file is created, a Docker build command is going to be executed by the developer or by the DevOps team person. So the output of this Docker build command is this Docker image. The input to the Docker build command is the Docker file, and this Docker file, it is going to have all the instructions on how to package an application. So based upon these instructions, the Docker build, it is going to package the application and the output of this Docker build, we call it as Docker image. So this Docker image, it is a blueprint or template of an application, it contains all the application code or application logic, all the runtime dependencies. For example, in case if you are preparing a Java based application, then it requires JDK to be installed to run the application. In the similar lines, if you are trying to package and JavaScript application, then it requires Node as a runtime dependency. So all the runtime dependencies along with the required libraries, OS level dependencies, configurations, everything will be present inside these Docker image. We can think Docker image as a cake recipe with all the ingredients pre-measured and included. It's a read-only template. Once the Docker image is created, we can't modify the content of that Docker image, but the Docker image can be shared with the other developers or with the other team members. This is very similar to on how we can share the cake recipe with others by sharing a photo. Docker ecosystem has a product called Docker Hub under this Docker Hub, all the Docker images can be uploaded from where the other developers and team members can download the Docker image. So once that Docker image is generated by using the same, we want to start our application or set up an application or deploy an application. So that's where Docker container is going to come into picture. So what is a Docker container? It represents a running instance of an Docker image. If the Docker image is the recipe, container is the actual cake you baked based upon the recipe. You can create multiple containers from one image, like baking multiple cakes from one recipe. So what are these Docker containers? They are running applications based upon the Docker image. By using a single Docker image, we should be able to create multiple running instances of an image. These Docker containers are lightweight, fast to start, isolated from other like applications running in separate bubbles. We should be able to stop or delete the containers anytime very quickly. The next jargon is Docker volumes. Using these Docker volumes concept, we're going to provide the permanent storage for the containers. By default containers, they are not going to keep that data permanently. If you restart the container or delete a container, the data that it stores is going to be deleted. In case if you want to persist the data, regardless of container is being restarted or deleted, we need to use that Docker volumes concept. By using these Docker volumes, we're going to provide the permanent storage to the containers. And with that, the data is going to survive container restarts and the multiple containers, they can share the same data as well. In simple language, we can think of these Docker volumes as a USB drive connected to the containers. So these are the major important jargons, Docker image, Docker container, and Docker volume. Apart from that, there are some basic jargons as well. We already discussed about Docker file. Docker file is nothing but a text file with instructions on how to build and Docker image. We also discussed about Docker Hub. Docker Hub is like an app store for Docker images. So under this Docker Hub, we can publish our Docker images from where the other developers or other team members, they should be able to download the Docker images and set up the applications. Next, we have Port Mapping. Previously, I was saying by using a Docker image, we can create any number of containers. Let's imagine I prepared a Docker image for a spring boot application. And this spring boot application, it is going to start at the port 8080. So using these Docker image, I should be able to run multiple Docker containers. So this will be the App1 representing my spring boot application. This will be App2, and this is 3, and this is 4, and this is 5. This way I can create any number of running instances representing my spring boot application. And all these applications, they're going to start in their own bubble, they're going to have their own network separate folders, since they are going to start in their own networks. So all these applications are going to start at the default port that we have configured, which is 8080. But in order to send the traffic to these applications, we need to expose a port outside of this container to our local system. That exposing of the port, we call it as Port Mapping. So the containers, they're going to start in their own internal network to access the applications inside these containers from a host mission. So we must map the ports, for example, if my container is started at 80, I can map that port to 8080 inside my laptop. And with that, what is going to happen whenever I want to talk with this container, I have to send the traffic to 8080 port and internally, it will redirect the traffic to the actual container application. If something is not clear, don't worry, I'm going to explain it again in the next lecture. So basically, by using Port Mapping, we are giving the container a door so that the outside world can enter into the container network. Next we have docker-compose. Docker-compose is like a tool to run multiple containers together at a time. Instead of creating a container manually, one by one, by using docker-compose, we should be able to start multiple containers with a single command. We're going to see the demo of this docker-compose as well. Next we have Registry. Registry is nothing but storage location for images. So in the scenario of docker, we have Docker Hub, which is going to accept as a public registry, just like Docker Hub, every company, they are going to maintain their own registry as well. For example, AWS has their own registry where all the docker images can be stored. And these registries can be of public or private. Usually the enterprise organizations are going to maintain their own private Registry where they are going to store all their company specific docker images. Outsiders cannot access them or download them. So these are the jargons. I know you may have some questions, but don't worry when we start setting up some applications by using docker, it is going to be make more sense to you. In the next lecture, let's try to set up MySQL database by using Docker.