Since we are into the new section, I'm going to create a new folder with the name section6, and into this folder, I will copy the backend code from the previous section. I'm not going to copy the UI code because we're not going to require a separate new UI code for this section. Whatever we have under the previous section, we can use the same to test the backend integration. I'm going to open the jobportal project inside my IntelliJ. This is my new project under the section 6. All the MySQL database related changes we're going to make in this project. You can find them under the GitHub repo as well. So far, inside our Spring Boot backend application, we are using the H2 dependency. Since we are looking to replace this with MySQL database, we need to remove the two dependencies related to the H2 database. In the same place, we need to mention the MySQL related dependency. Inside the start.spring.io website, after clicking on the add dependencies, you can look for the dependency with the name MySQL driver. You need to add this to the dependency and click on the explore button. And with that, under the pom.xml, you will be able to see this dependency. You can copy the same or in case if you are using IntelliJ premium version, you can click on this add starters button, which is going to show you a pop-up where you can search for MySQL as well. That's a shortcut if you are using premium version. Since we already copied from the start.spring.io website, I'm pasting the same here. So this is the dependency. The group ID is com.mysql and this is the artifact ID and the scope is runtime. After adding this dependency, please make sure you are syncing the maven changes by clicking on this refresh button. Next we need to go to the application.properties file because here we have good amount of h2 related properties. So let me delete these two properties. I'm going to require data source.url, data source.username, password, and I don't require these let me delete the same. This way I'm deleting all the not required properties for the mysql. Under this data source.url, we need to provide the connection details of our mysql database. Let me replace this String with the new value. You can get this value from the GitHub repo. So the value is jdbc colon mysql. So this time, since we are using mysql database, we need to mention this value followed by we need to mention colon, two forward slashes and next by using the environment variable syntax, I'm trying to define the hostname. The default hostname will be localhost. But in case if you want to override these localhost hostname with some other higher environment hostname value, we should be able to inject a dynamic value by providing an environment variable with the name DATABASE_HOST. So previously we discussed on how to inject an environment variable with a new value. In case if you want to provide this environment variable, you need to click on these three dots followed by Edit, inside this page, you need to select these modify options and select the option of environment variables, which is going to show you this new text box, and here you need to mention the variable name along with the new hostname like app.mysql.com. So whatever value you want to mention here, you can mention and end the value by mentioning semi colon. When you are providing this environment variable, then this value will be considered as a hostname. Otherwise, the default localhost value will be considered. So for now, I'm good with the localhost. That's why I'm trying to delete this value. Let me click on apply, ok. In the similar lines, I'm also trying to inject the port value also by using the environment variable syntax. . So in between the hostname and the port number, we need to mention colon followed by dollar with curly braces. The port number environment variable is DATABASE_PORT. This variable name can be anything, but I'm trying to use something very relevant to the value that we are trying to inject. So the default value is 3306. Here the port number value should be the same as the value that you have mentioned as the very first port number inside the docker command. In case if you have mentioned 3307, you need to mention the same here followed by forward slash next I'm trying to provide the DATABASE_NAME. So while we are starting the mysql database container, we provided an environment variable with the name MySQL_DATABASE to this environment variable I have provided a value jobportal. So with this value, a database schema will be created behind the scenes during the startup. The same value I have to provide as a DATABASE_NAME. So here this is the default name that I have provided in case if you want to change this value to some other database name in higher environments, then you can create an environment variable with this name and provide an alternate value. So this is that data source URL. Now coming to the username, the default username is root. Instead of mentioning the root value directly, I will follow the environment variable syntax because this is the standard. So default username will be root. In case if I want to change this to different value in the higher environments, I can change it by using these environment variable. Let me do the same for password as well. For the password, we have set the password value as root by using this environment variable. So I have to use the same password value for the connection to successful. I'm trying to do the same in case if you are using different password across different environments, you should be able to inject different values by using this environment variable. Next, I want to retain this property because regardless of which database we are trying to use, the responsibility of generating the sql statements based upon the jpa methods will be with the jpa framework. Since I want to look at those statements inside my console, I will continue using this property. When we are using this property alone, the SQL statements are going to be printed without any format. Understanding the unformatted SQL statements will be tough. That's why what we can do is, we can tell to the Spring Data JPA framework to print the SQL statements onto the console in a understandable format. There is a property with the name spring.jpa.properties. Using this property, we need to invoke hibernate.formatsql. To this format sql, we need to provide a boolean value true, but instead of directly providing the value, I'm trying to follow this environment variable syntax. So inside local environment, I want to construct these true default boolean values. But in higher environments, we should not print the SQL statements. That's why what we can do, we can change the values to this property by using these environment variables. And that's how we can control the printing and formatting of the SQL statements. So after providing these details, you can do the build once the build is completed, you can try to start your application. Once the application is started, we can go to the postman, inside the postman if we try to invoke this API, we're not going to get any response. Let's see the console. Inside the console, there is an exception saying that there is no companies table. So far, we just created a brand new MySQL database with the schema as job portal. But we have not created any table and we also didn't insert any data into this table. So that's why as the next step, we need to execute the SQL statements that we have under the SQL folder. This will be a one time activity, but to execute these statements, I need to connect to the MySQL database as a client. There are multiple approaches on how we can do that. In case if you are using the IntelliJ premium version, you're going to have these database symbol just before your data source.url. If you click on the same, you will land it onto this database menu, and here by default, the connection will be created to your database. In case if you're not seeing this, you can also create the connection manually. Let me show you that. Before that, I'm going to delete this by selecting this remove data source and this is how it is going to look like. If there is no database connection created by IntelliJ, you need to click on this plus symbol and select data source and MySQL option. So there'll be a MySQL option. You need to select the same and a new window will open. But instead of manually entering all this data, what you can do is, you can click on this symbol, which is going to populate the host details, username and password details based upon the property values that you have mentioned. If you are trying to create the connection for the very first time, here you might see an error or warning to download the MySQL driver. Please make sure you are completing that step. Followed by you can click on this test connection, which is going to confirm whether the connection is successful or not. Once the connection is successful, you can click on apply, ok. And with that, you're going to get the database connection here. But please note that this feature is only available under the IntelliJ premium version. And that's a behavior I see while I'm recording this course, in case if you are using the free version of the IntelliJ, don't worry, I'm going to show you another alternative in few seconds. Let me open this connection and this is the database schema that we have created jobportal under this, we don't have any tables created. So to create the tables, what we have to do, we just have to right click on this and select this new query console. Let's go to the jobportal-schema.sql. You can either copy this enter create statements into this console and execute, or you can directly select the entire statement from this file and click on this execute symbol as well, anything is fine. So far, I created that table that's why I'm able to see a table with the name companies. As a next step, we need to populate the data into this table. Let me open them jobportal-data.sql. Here you can select all the scripts and click on this execute button, and it is going to ask you which schema it has to use. You can confirm this option, use default schema. And with that, all the companies details will be inserted into the companies table. We can confirm the same. If you double click on this companies table, you will be able to see all the records that are inserted into the table. Now if I invoke this REST API, this time I got the successful output. So this is the process that you need to follow if you are using IntelliJ premium version. Otherwise, you need to come to this sqlectron.github.io website. This is a UI application using which we should be able to connect to any database. Here there is an option to Download GUI. If you click on that, you will be redirected to the GitHub page. From this page, based upon your operating system, please download the installation file and install this sqlectron. Once the sqlectron is installed, if you open, this is how it is going to look like. Off course, I have too many connections here, but in your case, there won't be any connections displayed. You can straight away click on this add button and here I'm giving the name jobportal database and database type we have to select it as MySQL. Coming to the hostname, it has to be localhost and the port number is 3306. Username will be root, password is root and initial database name is jobportal. After entering all these details, you can click on this test, which is going to confirm you whether the connection is successful, followed by you can click on the save button, and once the connection is created, this is how it is going to look like. If you click on this connect button, the query console is going to open. As you can see, there is a company's table and if you query this table, these are the records that we have inserted so far. In your case, if you are not using IntelliJ premium version, the table might have not created. Please make sure to copy the schema SQL and data SQL into this query console and executed one after other. I hope you are clear on how we set up a MySQL database container and connected it from our Spring Boot application. We just have to add a required dependency in the pom.xml, followed by we have to define all these properties.