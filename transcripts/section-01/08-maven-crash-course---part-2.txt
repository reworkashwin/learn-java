Let's try to validate the points that we have discussed in the previous lecture by practically exploring them using IntelliJ IDEA. Inside the previous lectures, we have created a maven demo project with the name demo itself. Inside this project, we have defined all the required maven dependencies under the pom.xml. Whenever you are creating a maven project, regardless whether you're using spring or spring boot or any other libraries inside your project, this is how the default structure is going to look like. We are going to discuss in detail about this maven project structure in few minutes. For now, let's try to understand what do we have inside these pom.xml. Under these pom.xml, we have defined a group ID, artifact ID and version with our own values. Under the group ID, we have mentioned the value as com.eazybytes coming to the artifact ID, we have mentioned demo and this is the version for our project. All these details like group ID, artifact ID and version number, they are going to be leveraged by the maven when we are trying to package our application. At the end of the day, all the maven applications that we develop, we want to deploy them into some cloud server. To continue with that deployment, first we need to package our application as a jar file during the package process, all these details will be leveraged. Once we package the maven project, we can either deploy that into a cloud server or we can also share the maven project as a dependent library for other developers or for other companies as well. If required, we can also publish this entire project into the maven central repository as well. Anyone who wants to use our project as a dependency inside their respective projects, they have to define the same group ID and the artifact ID along with the version number. So here, if you scroll down here under the dependencies we are trying to use some other library as a dependency, and this library is developed by the Spring Framework team. So just like how we are using the group ID, artifact ID and the version number, very similarly, these details that we are defining for our project will be used by others when they want to use our project as a dependency in their projects. After these details, we have properties under these properties by using these properties, we are configuring which Java version we want to use for our maven project. And accordingly, maven is going to use the JDK version. After the properties, we have dependencies. Inside these dependencies, we can define any number of maven dependencies. As of now, I have defined only one dependency with this group ID, artifact ID and version number. Based upon the dependencies that I have added inside my pom.xml, behind the scenes, the maven build tool, it will try to download the required libraries from the central repository. Once the download from the central repository is completed, the same libraries will be maintained locally inside my system under a maven local repository so that the same library can be reused multiple times across multiple projects. Every time we don't have to download the same dependencies again and again from the central repository. We can confirm these points as well. If you go to these maven window here under the demo project, we have repositories. Here we have two repositories. One is with the name local. This is the folder location where my maven local repository is going to be maintained inside my local system. For windows, obviously you're going to have different folder location. In case a given dependency is not present inside the local repository, then maven build tool, it will try to download the dependent libraries from the central repository. So this is the central repository details. Here you may have a question. Madan, you're saying maven build tool, it's going to download all the required dependencies into my local repository. Can you show me where these dependent charts are being saved inside my local system? This can be your question. There are two ways that we can use to confirm this. The first one is we can directly navigate to this path otherwise We can go to these external libraries option let me open these and these external libraries, you will be able to see all the libraries that are being used by our maven project. As of now, we are using JDK 25 version, all the JDK related libraries they're being referred from this location. Coming to the other external dependencies we know we have added only one dependency with the name spring-context, but these dependency it again has dependency on many other libraries as well. So that's why we are able to see many other dependency details here. We simply added only spring-context into our dependency, but it has many other dependencies. We can also confirm the same if you hover on this spring-context and press command button inside Mac, it is going to show you an option to open the pom.xml of this dependency. Inside windows, you need to press the control button. This is the pom.xml of the spring-context dependency. Here again, it has a dependency on many other libraries like spring-aop, spring-beans, spring-core, spring-expression. Again these libraries, they are going to have dependency on many other libraries. Based upon this dependency graph, all the required libraries are downloaded by the maven into the local repository. If you open any of these external library, you'll be able to see there is a jar, which is downloaded into your local system you can right click on this and here select an option open in Finder inside Mac, whereas in windows, it will show some explorer option. Please click on the same, which is going to show you the location where your dependent jar is saved. Let me navigate up. If I keep navigating upwards, I will be landed onto the place where my local repository is being maintained. So there is an .m2 hidden folder inside my home folder, under that I have repository under this repository, all the maven dependencies are being maintained. Since I'm using maven build tool inside my system from last many years, the maven end up maintaining all these libraries inside my local repository. In order to navigate to the spring context related jar, you have to navigate to the folder org.springframework. So here, first let's look for org followed by spring framework. Under the spring framework, we need to look for spring-context under the spring context again, we need to look for the version number that we are using which is 7.0.3. And under this version folder, you're going to have the respective library jar. I hope you got some clarity around maven. Now let's try to understand what are the advantages that we are going to have by using maven. The first and major advantage is we can avoid all the manual setup that we discussed previously. Apart from the developer advantage, we're also going to have teamwork advantage as well. When you share your code with someone else, they don't need to hunt for the required dependent jars since all the required dependencies are already defined inside the pom.xml they can simply run a maven command with the name mvn install, which is going to download all the dependents automatically into their local system. So this makes your projects portable, Easy to share and Less error-prone. When you are sharing the projects or when you are uploading these projects into the GitHub repo, we are never going to upload all the dependent jars. So that is not a recommended standard. If you upload all the dependent jars as well, every project, it is going to have gigabytes of data. Instead what we are going to do, we are going to upload only the project source code along with the pom.xml, which will take the size to some megabytes or some kilobytes as well. Apart from teamwork advantage, we're also going to have advantages around the Build Automation. Think like once the development of the web application is completed, we want to deploy our code into some higher environments like development, QA, UAT or production. In all those scenarios, when you are moving your code from one environment to environment, the maven is going to help us. Apart from the dependency management, maven is also going to help us in compiling our Java code, running unit tests, packaging the application into a jar or war file, creating reports or deploying artifacts. All this can be achieved by using commands like mvn clean install, mvn package. Each of these commands, they have a meaning and different purpose. I'm going to discuss about these maven commands in the next lecture. Coming to the maven project structure, this is how it is going to look like. Every maven project follows the same structure, just like how every McDonald's looks similar worldwide. Under the project at the root location, we are going to have the pom.xml. Inside this pom.xml, we are going to define all our dependencies. Next we are going to have src folder and target folder. Under this src folder, src means source. Under this source folder, we are going to have a main folder and test folder. All the actual Java code or the business logic that we want to write should be written inside this Java folder present under the main. Any config files or property files, we need to place them under these resources folder. Next under this test folder, we are again going to have Java and resources. Under the Java folder, we can write all our testing related code, especially the unit testing related code we are going to write under this folder. Next under these resources, we can define all the testing related configurations or properties after the src folder, we're also going to have target folder. Under this target folder, the maven build tool, it is going to place all the compiled code. We know inside Java, we write our business logic inside a source file, but eventually that source code has to be compiled into bytecode. So that compilation process is also going to take care by the maven. Once the compilation is completed, all the compiled code will be pasted under this target folder. So this standard structure means any developer can understand any maven project instantly. Even if you are moving to some other organization or some other project, you're always going to have a standard, which is going to help you to understand the project in a more effective manner. Tools And IDEs, they know exactly where to find things which can help us in building some automation. So no more, where do I put this file confusion? Let's try to validate these project structure very quickly inside our demo project. This is the project that we have created. As you can see, we have a pom.xml at the root location. Apart from pom.xml, we have an src folder and a target folder. Under this src folder we have main java. Under this java folder only, we are going to build all the business logic. Under these resources, we are going to place lot many properties when we start leveraging spring boot for our web application. For now, it is going to be empty. Next under this test, we can write all the testing related code. If our testing code requires any configurations or property files, we can create the same resource folder under this test. Next as we discussed under this target folder, we are going to have all the compiled classes. So apart from these src, target and pom.xml, we are able to see some .idea, .mvn and .gitignore. These are all the folders and files that are created by IntelliJ for the effective management of our project. Ideally, the developers they never have to worry about these folders. So under this .mvn folder, the maven is going to maintain all its cache. Similarly, under this .idea folder, the IntelliJ is going to maintain all the details around this project. Next under the gitignore, we are going to mention all the folder details or file details that needs to be ignored while we are uploading this project into the GitHub repository. So all these are hidden folders that's why we are able to see . at the starting of their name. Looking at them is very annoying on day to day basis. So that's why IntelliJ is going to give you an option to hide them as well inside these project view. What we have to do, we have to open the settings of the IntelliJ. Inside these settings, under the editor there is a section with the name file types. Inside these file types, you need to open these ignored files and folders tab and click on this plus symbol. So I'm going to mention .idea under this ignored list, followed by I'm also going to add .mvn. Next I'm also going to add .gitignore. After adding all these details, if I try to apply and click OK, this time you will be able to see I'm strictly seeing the maven structure only. At any point of time, if you want to check gitignore file or any other folder, feel free to go to these file types and ignored files and folders and here you need to remove them. Like if I remove these gitignore, then I'll be able to see the gitignore again here. I hope you're clear. Let me hide these gitignore. I'm going to this tab and clicking on this plus and typing .gitignore, which is going to hide the file that we have under the project. In the next lecture, let's try to discuss all the important Maven commands.