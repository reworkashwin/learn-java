Apart from acting as a dependency manager, maven can also perform lot many other tasks to perform these tasks, we need to use the Maven Commands. These tasks include compiling your code, running unit testing, packaging your application, deploying artifacts, cleaning old builds. So all this can be achieved by using simple command. Let's try to discuss all these commands one by one. The first command is mvn clean. When you run this command, it is going to remove all your old compiled files from the target folder. This command literally it is going to delete the entire target folder. Why do we need to run this command? Sometimes when you are rebuilding your application, you may have some cache issues or some conflicts with older builds to ensure a fresh start, you are going to run this command. Let's see the demo of this command very quickly. Inside IntelliJ, if you click on this Maven command, under these life cycles, you should be able to see all the commands supported by the maven. So the first command that we have is clean. When you run this command, it is going to delete the entire target folder along with its content. You can run this command by using the start button or you can open the terminal as well within your IDE and here also you can run the command mvn space clean. Both of them, they are going to have the same effect. I prefer executing the maven commands by using these buttons that I have here. As soon as I click on this button, you will be able to see the target folder is going to get deleted. Let me click on this button. After a few seconds, the target folder is gone. So if I do the build of my application or the compilation of my application one more time, the target folder is going to come back. We're going to see that in a few minutes. The next command that we can run is mvn validate. When you run this command, it is going to basically check if your project is properly configured. It is going to validate whether your pom.xml is valid XML if you have defined all the required fields, like group ID, artifact ID, version number, is the project structure is correct, are all necessary plugins are available basically the sanity test of your project is going to be performed when you are executing this command. We will see the demo of this as well in a few seconds. After the validate, we have mvn compile, which is going to take care of compiling all your Java source code. When you run this command, it is going to read all your source code from this folder and generate the . class files and place them under the target folder. Next we have mvn test command, which is going to run all your unit testing code. If you write any unit testing code inside the test folder, where do we write the unit testing code? Under the src test java folder. When you trigger this command, it is going to execute all your unit testing code that you might have developed by using JUnit or any other library. Once the execution of the unit testing is completed, this command, it is also going to show you how many unit testing cases are passed, how many of them failed. You're going to have a detailed report. Usually in real applications, while deploying the web application into an higher environment, the deployment tools, they're going to run this command just to make sure the entire application is working properly. After the mvn test, we have mvn package command. When you run this package command, it is going to package the project into a jar file or a War file based upon your configurations inside the pom.xml. So whatever package that got generated, it is going to be placed under that target folder. We can use this jar to deploy our application into a higher environment server like development, uat or production. Next we have mvn verify command. It ensures that the build is ready for the final installation or the deployment. If you run this command, it is going to compile your application, perform the unit testing and verifies everything is working without actually deploying it. If something fails like unit testing fails, it will let you know so that you can fix it. Now the next command is mvn install. This is the most important or commonly used command. So when you are running this mvn install command, it is going to take care of compiling your application, triggering the unit testing, packaging and verifying your application. Once the verification is successful, it is going to install the generated jar file or War file into the local maven repository so that any other projects on your mission, they can reuse this application as a dependent jar. Next we have mvn deploy command. There is a small difference between mvn install and mvn deploy command. When you are running the mvn deploy command, it is going to perform all the previous steps like compiling, triggering unit testing, packaging, verifying and generating the jar once the jar is generated, this time it is going to publish the jar into a remote maven repository so that the same can be used by other developers or other projects or by third party vendors as well. So basically when you are running this command, it is not going to deploy your application into some server. Instead the jar of your application or War of your application will be deployed as an artifact into the remote repository like maven repository or any other repository that you have configured so that the other organizations can reuse your library. Next we have mvn site command. By using this command, we can generate the project documentation website for a maven project automatically. This command, it takes the information from our pom.xml and creates a HTML documentation site which we can see inside a browser. So these are all the important commands that we have under the maven and this is the complete flow. The clean command and the site command, they are completely optional you don't have to execute them unless you need them. But these are all the other remaining commands that are going to be leveraged by maven during its lifecycle. For example, if you are running the install command, the maven is going to take care of first running the validate command, followed by compile command, followed by test, package and verify. After this verify is completed, it is going to execute the install command, which is going to take care of publishing the jar into the local repository. In case if you are executing the deploy command, then all the previous commands will be executed before running this deploy command where it is going to take care of uploading the jar into the remote repository. In case if you are executing only the package command, then the maven is going to execute only the previous commands like validate, compile, test along with the package. So during the package only, the jar will be generated. I have mentioned this create jar here, but this should be under the package, which I'm going to correct in the slides that I'm going to share. So this is the shortcut that you need to consider. You never have to run these commands separately. For example, if you run the MVN install, it is going to take care of executing all the previous commands sequentially. We know clean is optional. So that's why clean is never going to be triggered by the maven lifecycle. If you want, you can change this command to something like MVN space clean space installed. So with this, what is going to happen? The maven is going to first execute the clean command followed by install command as part of this install command. All these previous commands will be executed by the maven. I have a real world analogy as well to better understand these maven commands. Imagine you want to bake a cake and these are the steps that you follow. First, you will make sure to validate all the ingredients are available or not. This is very similar to validate command. Next, you're going to mix all the ingredients. This is very similar to compile. Next, you're going to taste the batter. This is same as test command. Next, you're going to do the baking of the cake, which is like packaging the application. Next, you're going to place the cake inside the local fridge, just like how you're running the install command. At last, you're going to deliver this to bakery or to an end user. This is very similar to the deploy command. So these steps must follow the order. You can't bake before mixing. Maven works exactly the same. Let's see the demo of few of these commands very quickly. I'm going to run the validate command, which is going to perform the validation of my project. If my pom.xml is correct or the project structure is correct. If everything is correct, it is going to print these build success. In case if there is a problem for example, inside the pom.xml, if I don't mention the artifact ID and run these validate command, I'm going to get some error along with the details. In the error, it clearly highlighted the artifact ID is missing. So let me place the artifact ID and run the validate command again. This time, the build is successful. Next we can run this compile command, which is going to take care of compiling all our Java source code. As soon as I execute this command, you will see the target folder is going to come back. So if I click on this, after a few seconds, the target folder came under this target folder we have all the compiled classes. Next we have test command. If I click on this test command, it is going to perform the unit testing since I don't have any unit testing code, the build is successful. If I have some unit testing code, it is going to show me the detailed results, like how many test cases are successful, how many of them are failed. After that text, we have package. When we run the package command, it is going to package my application as a jar file. Before executing this package command, let me execute these clean commands so that the target folder is going to be deleted. So now if I execute the package command, all the previous steps will be completed before generating the final jar file of my application. If I trigger this package behind the scenes, it is going to take care of validating, compiling, testing and package. Now if I go to the target folder, apart from the compiled classes, I also have a jar file generated. In case if I want to package my application as a War file, I should be able to achieve that by mentioning details inside the pom.xml. Here inside the pom.xml after the version, I can mention packaging. Under this packaging, I can either mention jar or War or any other packaging style that I want to use. The most famous packaging formats are jar and War. So this time, after mentioning these details and after refreshing these maven changes, if I execute the same package command, I'm going to have a var file as well. As you can see, I have a var file. I also have a jar file as well. I'm not sure if the jar is generated as part of this execution or previous execution. Maybe what I can do is, I can run this clean command followed by I will try to run these package command, which is going to give some confirmation to us whether the jar is generated or not when we are mentioning the War packaging, this time you will be able to see only the war is present. So whatever jar that we have seen before that was generated when we are not mentioning the packaging as war. But as I said before, jar is most famous compared to war. That's why I'm removing these and refresh the maven changes, followed by I'm running the clean command after the clean command, I'm going to execute the package command, which is going to generate the jar file inside my target folder. Now if I try to run these verify command, it is going to perform all the sanity testing if everything is working fine, I'm going to get the success message. Next, if I run this install command, the jar that got generated, it will be deployed into my local repository as a dependent jar. So that other projects inside my system they can use. We can also validate where this jar is installed inside my local system. If you can scroll towards the right side. So here there is a statement that the jar is copied to so-and-so location inside my system. So under the maven local repository, we will be having folders like com, ezbytes, demo and under these version number, we're going to have this jar file. You can validate the same in your local as well. Next after the install, we have the site command. This is going to generate the documentation about your maven application. As soon as I execute this site command under the target folder, you will see some HTML code that got generated. Let me execute this command. Once this command is successful, we are going to have a site folder under this site folder we have various HTML files. Let's open this index.html by using open in browser and selecting Chrome. So this is the documentation that got generated. So it has some details around my project. What are the dependencies that it use along with the summary. So the more details that you mentioned inside the pom.xml, all of them will be extracted and will be provided in a readable format by using these HTML documentation. Usually the enterprise applications who are building libraries are for the third party developers they're going to take care of providing the nf documentation to the third party developers. How they are going to do by using the site command. Next when you run this deploy command, the jar is going to be deployed into the maven central repository or any other internet repository that you have configured in the pom.xml. As of now, we have not configured any repository inside our pom.xml. That's why if you run this command, you're going to get an error. This is an advanced topic. I don't want to discuss about these as part of this course. In case if you are interested, you can explore more about these by looking at that documentation of the maven. The command is failed because we have not configured any remote repository details. So there is a website called mvnrepository.com. This is the place from where the maven is going to pull all the dependencies. Ideally, this will be the place where you have to publish your libraries. If you want other developers to use your library, maven will also give you flexibility to have your own private repository that is specific to your own organization so all those are advanced topics, but I'm trying to give some directions around publishing your jar into the central repositories. So inside these maven repository, you can search for any dependency. For example if you search for spring, it is going to show you all the projects under the spring. As of now, we have mentioned this spring-context as a dependency inside our pom.xml. With this, I'm concluding the discussion around the maven. So those details are good enough for you to transform into a successful Java backend developer. At any point of time, if you are looking for any advanced details around maven, you can refer its official documentation.