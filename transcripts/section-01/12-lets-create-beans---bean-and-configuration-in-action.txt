I am very excited. Finally, we are going to see some demos of Spring Framework. The most fundamental action that we can perform by using Spring Framework is bean creation. Once we create the beans, then only we can leverage the advanced features of Spring Framework or Spring Boot. Whether you want to perform the dependency injection or whether you want to perform the autowiring or any of the advanced concepts, first, you need to understand what are the various different approaches that we have to create a bean by using the Spring Framework. We know beans are nothing but normal Java objects. When you create a Java object with a plain new operator directly as you can see here, then your Spring context or Spring IOC container, they will never have any clue of this object. So that's why if you consider this container as a Spring context container or Spring IOC container, you will see this is empty and it is not aware about the object that we have created here. So here, what is Spring context ? It is an IOC container, which is an implementation of BeanFactoryAndApplicationContext. Instead of following this basic approach, we need to use certain annotations to tell to the Spring context or Spring Framework to create a bean from a given Java class. So this is one of the approach that we are going to explore first. In the similar lines, we also have many other approaches to create a bean inside the Spring Framework. All the approaches we are going to discuss in the coming lectures. First, let's try to understand this approach. On top of a Java class, you need to mention @Configuration annotation. So there is a Java class here with the name ProjectConfig. The name can be anything. On top of this class, I have mentioned @Configuration annotation. By using this Spring annotation, we are marking a class as a source of bean definitions. So we are telling to the Spring Framework, Hey buddy, inside this class, I have defined some beans related configurations. So while you are trying to initialize the Spring context during the startup of the application, please make sure to visit this class and visit all the configurations that I have defined inside this class. So this kind of message that we are trying to give to the Spring Framework. Once we have defined this annotation on top of this class, inside this class, I have created a method with the name vehicle() and the return type of this method is Vehicle. Inside this method, I have written a logic where I'm trying to create the object of Vehicle by using new operator. And to this object, I'm trying to set a name as Tesla. And at last, I'm trying to return this Vehicle object. Here you may have a question, which is Madan here, you are using the same new operator again, which means you are trying to create the object manually. Then what is the whole purpose of using the beans? So this can be a question. Let me try to answer the same. As I said, this is one of the approach that we can use to create the beans. There are other approaches also where we don't have to use new operator at all. When you create a bean from an Java object, we are going to have various advantages. This object will be maintained by the spring framework. At any place inside your business logic, you can simply ask the spring context to give you the bean or the object. And it is going to give you the same Vehicle object. Let's imagine you have 20 different classes. In all these classes, you have some business logic where you require these Vehicle object or Vehicle bean. So instead of defining the objects in all those 20 places by using this new operator, you can simply ask the spring context to give you the bean that is created here. So that all those 20 places, they're going to get the same bean or same object. In future, if you want to change the name of these vehicle, the change needs to be done only in this place. Whereas if you follow this basic approach, you have to revisit all those 20 places and change the vehicle name. So this is one of the advantages, but as I said before, we are also going to explore on how to avoid manually creating the beans without using the new operator. From this method, we are trying to return the Vehicle object. Unless you keep an annotation on top of this method, which is @Bean, this object will not be converted as a bean by the spring framework. So if you don't mention this @Bean annotation on top of this method, this vehicle will be a normal Java object. Since we want this object to be maintained by the spring IOC container, we need to mention this @Bean annotation on top of this method and with that, the spring IOC container or the spring context is going to take care of these Vehicle object as a bean. That's why this time inside the spring context, we have a Vehicle object, which is a spring bean and the name of this vehicle is Tesla. Let's see the demo of this example. Inside our demo project under this package, I'm going to create one more sub package with the name config. So inside this config, I'm going to create all my configurations. Let me create a new Java class with the name ProjectConfig. On top of this class, I need to mention @Configuration annotation. We already discussed about the purpose of this annotation, but I'm going to mention some Java comments for your reference. Please read these comments for more details around the configuration. Now inside this configuration class, we want to create beans by writing a Java method. But before that, we need to make sure to create a class of Vehicle because we want to create an object of Vehicle class, which eventually needs to be converted or handled as a spring bean. So that's why first we need to create a Vehicle class as well. So what I'll do is, I'll try to create one more package with the name beans. Inside this package, I'll try to create a new Java class with the name Vehicle. Inside this class, I'm going to create a Java field of type String with the variable name as name itself. Next I'm going to create getter and setter method for this field. Now my Vehicle class is ready let me go to the ProjectConfig and here I'm going to create a method which is going to return Vehicle object. The method name, I'm going to keep it as vehicle() and this method, it is not going to accept anything as an input. So let me import this Vehicle inside this method, I'm going to first create the object of Vehicle followed by I'm going to populate the name field by using the Vehicle object. So let me invoke set name and to this, I'll pass value as Tesla. Maybe I'll try to change this variable name to veh, I have shortened this just to avoid some confusion. There are so many vehicles here. So once we have the Vehicle object is ready, I'm trying to return the same from this method. To convert the Java object as a bean on top of this method, we need to mention @Bean annotation. Let me mention some details around @Bean annotation as well. I'm providing these comments for your reference. This way I can create any number of methods inside this configuration class and all these methods can return various kinds of beans. For example, I can add two more methods. Like I have added a method with the name, hello(), which is going to return String object from this method. I'm simply returning the hello world String object, on top of this method I have mentioned @Bean annotation. Similarly, I also created one more method with the name luckyNumber() and this method is going to return an object of Integer. Inside this Integer object, I'm storing a value 16 on top of this method also I have mentioned @Bean annotation. With this, inside our project, in total, we have three different beans. As a next step, we need to initialize the spring IOC container or spring context during the startup of our application. To achieve the same, we need to go to these demo main class where we have a main() method. So here, first, what I'll do is I'll try to create an Vehicle object manually by using new operator using these Vehicle object. I'm trying to invoke the setName() method here I'm mentioning the vehicle name as Audi. So this is a normal Java object, which is created manually by me and spring context. It is not tracking these Vehicle object because spring context does not have any clue. Next to these two lines, I'm going to add System.out.println() where I'm going to print a statement saying that vehicle name from non spring context. So when this code is executed, we are going to get the output as Audi. So if you try to execute this program, we are going to get simply Audi as an output. Now let's try to initialize the spring context or spring IOC container. As I said before, there is an interface with the name BeanFactory inside the spring framework code. So this is the interface. You can click on these download sources so that you can see the source code. If you want to avoid clicking on the download source for each and every file that you open inside the framework, what you can do is you can go to this Maven tab and here there is an option to download sources and documentation for all the dependency libraries that you are using. So BeanFactory is an interface and for this interface, there are many implementation classes. One such implementation interface is ApplicationContext. So ApplicationContext is again an another interface, and this is extending ListableFactory. And this ListableFactory is again extending the BeanFactory. So if you go for the implementation of these BeanFactory, you're only going to get the basic functionality of the spring IOC container. Since we are looking for all the advanced features as well, we need to choose one of the implementation class of these ApplicationContext. So if you look for the implementation classes, so these are all the implementation classes and annotations. Since we are using the annotation style of configuration, we need to use this class with the name AnnotationConfigApplicationContext. So this is the class which is going to take care of creating the beans and maintaining them inside the spring context or inside the spring IOC container. So I need to create the object of this class by going into this demo main class. So what I'll do is I'll create a var variable with the name context. And on the right hand side, I'm going to create the object of AnnotationConfigApplicationContext. So I need to import this class followed by to this constructor I need to mention where I have defined all the project configuration. So we have defined all our bean configurations under the file ProjectConfig.class. With this now during the startup or during the bootstrap of our application, what is going to happen is this class, which is acting as a spring IOC container or spring context, it is going to look for the configurations that we have defined inside this class and accordingly, it is going to create the beans of Vehicle, String and Integer. We can also validate if the beans are actually created inside the spring context or not. But the same, what we have to do using these context, we need to invoke the getBean() method. And to this getBean() method, we need to provide what is the data type of our bean. The data type of the bean that I'm trying to retrieve is of type Vehicle that's why I'm mentioning Vehicle.class here. On the right hand side, I can try to catch the output by using a variable name like veh. So now I'm going to have one more System.out.println() where I'm going to print the name of the vehicle. Here the statement is vehicle name from spring context. By the time we print this statement, we should get the output as Tesla because this is the object or the bean that is being maintained by the spring context. In the similar lines, we can also retrieve other beans as well. Just to save some time, let me paste few lines of code. You can find all this code in the GitHub repo as well. So here, if you see by invoking the same getBean() method on top of the context, this time I'm trying to get a bean of data type String. So the same, I'm trying to print here. Next I'm also trying to get the bean of type Integer. The value inside the Integer I'm trying to print here. So whenever you are invoking this getBean() method, you don't need to perform any explicit casting. The spring framework is smart enough to look for a bean of type you requested in its context. If such a bean does not exist, spring will throw an exception. So let me save all these changes, do the build. Once the build is completed, I'm going to start my application by clicking on this debug option. So this time you'll be able to see, we have various outputs inside the console. The vehicle name in the spring context is Tesla. The String value from spring context is hello world. And the integer value from spring context is 16. So this gives a confirmation to us, all our beans are properly getting registered inside the spring context. And we are able to read them without any issue. Let's imagine you're trying to be smart and you're asking the spring context to provide the bean that does not exist. Under such scenarios, we are going to get a RuntimeException. Maybe what I can do is, this time I can try to ask the bean of type Double, just like Integer. We also have Double class. When this line is executed, we are going to get a RuntimeException. Let me do the build. All the shortcuts that I'm using, you'll be able to see in the down. So this is the shortcut that I have used to trigger the build in macOS. And this is the window on Linux shortcut. So once the build is completed, I'm going to start my application. I got an exception saying that NoSuchBeanDefinitionException. There is no qualifying bean of type java.lang.Double. So this is the console. If you don't want to scroll onto the right hand side, there is a soft wrap option available. If you click on the same, your entire console text will be wrapped properly so that you can see the content without scrolling onto the right hand side or onto the left hand side. So this is the exception. I hope you are clear. Let me comment this code so that we won't get any exception. Before we try to close this lecture, I want to share some important information. Usually inside Java, the standard for method naming convention is to follow the verbs instead of nouns. A verb represent an action, whereas noun represents a name. Here I have mentioned the method names by using noun like vehicle, hello and lucky number. But ideally Java recommendation is to mention the method names by using verbs like createVehicle(), sayHello(), luckyNumber(). So that's a standard that we need to follow. But when you are using a method to create a bean, you can ignore that standard and mention the method names by using noun standard. The reason is whatever method name that you have given here, the same name will be given as a bean name inside the spring context. Inside the DemoMainClass, as of now, we're trying to fetch the bean by using the data type. Instead of that, we can also fetch the bean by using the bean name as well. So let me copy these two lines one more time. This time, instead of invoking the getBean(), I'm going to invoke the method, which is going to accept the bean name. So here I'll mention the bean name. What is a bean name ? Bean name will be the method name itself. It is hello(). Let me mention the same here and rename this variable name to hello one, the same I'm going to mention here. When you are passing the bean name, you need to perform that typecasting. Let me perform the typecasting here by simply mentioning string because my spring framework does not have any clue on what is the data type of these bean at compilation time. Whereas with this syntax, since we are providing the data type as an input, the compiler know about the output during the compilation time itself. I hope you understand the difference here on why we need to perform the explicit casting here. So now if I try to execute this program, I got the same output towards the end, but this time the last output we are trying to print by providing the bean name as an input to the getBean() method. Since these method names are going to act as the bean names, it is perfectly fine to follow the noun standard instead of verb standard. Let me mention the same details here on top of these method. I hope now you understand on how to add the beans into the spring context. As I said, this is one of the approach. There are multiple other approaches as well, which we are going to discuss in the coming lectures. With this, I want to conclude this section 1. More basic concepts around the spring framework we're going to discuss in the next section as well. Before you try to come to the next section, I have a couple of requests. The first one is try to revise what we have discussed so far inside this section. You can simply look at them and quickly revise. And the second request that I have is, please take regular breaks. Don't try to complete this entire course as a marathon, because since all these are the new concepts for you, your brain can't take all of them. And at some point of time, you may feel overwhelmed and ignore this course. I don't want that to happen to you. Since you completed section 1, take a break of one day and revise all those concepts and come back. I hope you are enjoying this course. If yes, please provide your valuable feedback.