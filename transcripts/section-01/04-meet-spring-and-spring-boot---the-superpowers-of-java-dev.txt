Previously, we saw the challenges that we are going to face if we try to build web applications by using code Java alone. I just discussed a few set of problems, but those are not all the problems. You're going to face hundreds of problems, but I try to give most common major problems. Now let's try to understand what is Spring, what is Spring Boot, how they are going to help us in overcoming these challenges. Spring Framework is not a programming language. It is not a replacement for the Java programming language. Instead, it is a framework built on top of Java. In order to become an expert in Spring, first you need to have the knowledge of Java. The same concepts are also used inside the Spring. Like you have to create the Java classes, you have to write the Java methods, you can use the Java collections inside your business logic, all the code we are going to write in Java only. But this Spring which is a framework, it is going to make a back-end developer life easy by defining some set of rules. As long as you follow those rules and start developing the code, you should be able to build the enterprise web application. In simple words, we can say Java gives you the tools like hammer, nails, wood whereas Spring Framework, it is going to give you the blueprint and the power tools to build a house faster and better. Let's try to understand more details about Spring. Spring is a popular Java framework for building web applications, whether you are building a REST API or microservice or a web application in all these kinds of scenarios, you can happily use the Spring Framework. It helps developers to write clean, modular and testable code. It has built-in features for handling databases, security, messaging and more. By using Spring Framework, the back-end Java development will become easy because it is going to help us in reducing the boilerplate code. All the unnecessary boilerplate code will be taken care of by the Spring. We as developers, we can simply focus on the business logic. Spring Framework supports building various kinds of web applications like web apps, microservices, cloud-based applications. Around the objects management also, Spring is going to help us by providing concepts like dependency injection, where the dependence objects will be injected automatically by the Spring Framework based upon the developer configurations. Spring also works well with other technologies like Hibernate, Kafka, RabbitMQ. We're going to explore all these if something is not making sense, don't worry as we are making progress inside this course, we're going to discuss a lot of advanced topics as well. So this Spring Framework, it is introduced around 2003 initially by Rod Johnson as part of his book with the name One-on-One J2 EE Design and Development in October 2002. But the first stable version is released somewhere around 2004 and these are all the other milestone releases. As you can see here even after two decades, the Spring and the Spring Boot they together they are ruling the Java backend development inside the industry. There is no other framework or technology inside the Java ecosystem that is closer to Spring ecosystem. This Spring Framework, it initially emerged to simplify the complexities faced by the developers when they are building web applications by using J2 EE. So apart from core Java, Java also has an enterprise edition by using that enterprise edition, developers should be able to build the web application as part of these enterprise edition only. We have concepts like servlets, EJBs, which means enterprise Java beans, but developing web application by using all these enterprise edition specifications it is a super complex process. Spring was introduced to simplify all these processes. So Spring is not an replacement for the Java enterprise edition. Instead it complements it by integrating only selected features from the Java enterprise ecosystem. Over the decades, based upon the demand from the developers and industry, the Spring Framework it evolved into many other popular sub-projects like Spring Boot, Spring Security, which focus on security, Spring Data, which focus on database logic, Spring Cloud, which focus on microservices, and similarly Spring Batch and Spring AI. For every problem inside the industry, we have a solution in Spring ecosystem. We definitely going to explore all these sub-projects as well throughout this course. Before I try to further explain you about Spring, let me show you a glimpse of what is Java Enterprise Edition. There is a website with the name jakarta.ee. Long back, this enterprise edition it used to be maintained by Oracle team itself. But at later point of time the Oracle team they hand over these enterprise edition maintenance to the open source community with the name Jakarta. That's why these days everyone call the Java Enterprise Edition as Jakarta Enterprise Edition. So inside these website, you should be able to read all the details around the enterprise edition. But as I said before, no one is using these enterprise edition directly in the enterprise applications. 99% of the developers, they are relying on the Spring framework and Spring Boot framework. At any point of time, if you want to learn more details around the Jakarta Enterprise Edition, please feel free to refer this website. Now let's try to understand how Spring actually is going to solve all the problems that we discussed in the previous lecture. At the core of the Spring, it follows concepts like inversion of control and dependency injection. We're going to discuss more details about these basics on a high level let me try to explain what is inversion of control and dependency injection. When we are developing a web application by using core Java, it is the responsibility of the developers to create the objects. But when we are using the Spring framework, the responsibility will be given to the framework not to the developer. The developer has to simply define the requirements or the configurations. And based upon those configurations, the Spring framework is going to take care of creating the objects. That's why this name came in version of control. Once the objects are created by using the concept of dependency injection, the Spring framework is going to inject all the dependent objects into a given object. I have taken the same example of user service inside these user service class we have a method with the name registerUser(). Whenever an user is registered, we want to send a notification. It can be by using an email or SMS or WhatsApp. So if you see here this time into these UserService, the object of notificationService is going to be injected by Spring framework itself. Nowhere the developer has to use the new keyword to create the object manually. Inside the notification service, we have a abstract() method with the name send notification and EmailService is a class which implements these notificationService. When the Spring framework injects these objects into this UserService behind the scenes, the object of EmailService will be injected because Email Service is a class implementation of this interface. Similarly, we will have other implementation service like SmsService or EmailService. So based upon our requirements, we can tell to the Spring framework which of these implementations should be considered to send the notification to the end user. As soon as the sendNotification() method is invoked, if I configured inside my web application to use this EmailService, then the notification will be sent to the end user by using the email logic but not the SMS logic. So this way Spring is going to take care of objects creation, dependency injection and even switching between the dependencies as well. And with that, all our logic will be loosely coupled and we can also easily mock the dependencies during the unit testing as well. Apart from these basic concepts, Spring is also going to take care of managing the database transactions, database connections, logging, properties management and many others, which we are going to explore throughout this course. As I said before over time, the Spring ecosystem evolved into multiple sub-projects like Spring MVC which is going to help us in building monolithic web applications, Spring Data for database related logic, Spring Security for implementing security, Spring Cloud for microservice related, and Spring AI for GenAI related and so on. I hope you got some clarity around Spring. Now, let's try to understand what is Spring Boot? Before Spring Boot, developers they have to manually configure everything by using XML files, dependency jars and long setup steps. Though using Spring Framework is better than CodeJava, but still Java developers has to follow some lengthy approach. For example, if you are trying to create a web application by using traditional Spring Framework, the developer has to configure the dispatcher servlet and it requires around the 50 lines of code inside XML, set up the component scanning inside XML, set up the data source inside the XML the developer has to set up the view resolvers view resolver means which UI page has to be displayed based upon a given path. The developer also should take care of deploying the web application into the Tomcat server, making the server settings. So this is a lengthy process. But once the Spring Boot is released, it simplified all these manual process as well. So Spring Boot, it is going to ask the developers you tell me what kind of web application you're trying to build. And accordingly, I'm going to take care of auto configurations i'm going to provide an embedded server, i'm going to pull the required dependencies and many more features. For example, if you tell to the Spring Boot Framework that I'm trying to build an REST API or a web application, the Spring Boot Framework is going to assume that this fellow requests and server to deploy his application. That's why it is going to provide Tomcat by default and our code is going to automatically deployed into the Tomcat server as well. So this kind of assumption, the Spring Boot is going to consider and it is going to provide some default configurations, which is going to speed up the process of creating the web applications. If the developer is fine with the default configurations provided by Spring Boot, then everyone should be happy. Otherwise, the developer also has an option of overriding those default configurations as well. So Spring Boot, it is going to help the developers from idea to working APIs within minutes. That's why most companies these days, they use Spring Boot because it includes Spring Framework, but makes development 10x faster. So Spring Boot is not a replacement for Spring, but rather it is an extension that makes Spring easier to use. Don't be under the assumption that I will not learn core Java and Spring and I'll straight away learn the Spring Boot, because that is not going to make you a good backend developer because the Spring Boot behind the scenes, it is going to leverage the concepts of Spring Framework it's just that it is abstracting all those complexities from the developer. And similarly the Spring Framework behind the scenes, it is going to use the Java programming language. Let me show you some real life analogies that are going to help you in understanding the difference between Java, Spring and Spring Boot. In the olden days, when people they want to communicate with each other they used to write the postcard and they used to mail them physically. So when they want to send a news to someone, they have to send a letter or postcard and wait for the delivery and the response. So this is very equivalent to using core Java in building web applications. So everything is manual you write and handle every step yourself. Next once the landline telephone is introduced, people started communicating by using it and this makes the communication easier, but still limited to only one location. If someone wants to call another person, they both have to be close to the landline. So this is very similar to using the Spring Framework for the web application development. Though it automates some work like dependency management and object creation, but still it requires some setup configurations that are going to take some effort. And finally we have the most advanced option, which is mobile. So once the mobiles are invented, people started calling each other without any problem. They are always ready, portable and the calls can be made instantly. So this is very similar to using Spring Boot for the web development. So everything is built-in, instant startup, auto- configurations, embedded servers, one click deployment, everything is going to be provided by the Spring Boot framework. I also have another car scenario. You can consider manual car driving scenario equivalent to web development using core Java. Whereas the driving of automatic car is equivalent to using the Spring Framework for the web development. And recently we are seeing the self-driving car, which are more autonomous and more advanced. So this is very similar to using the Spring Boot framework for the web development. I hope you're clear with the Spring and Spring Boot and core Java approaches. If something is not clear around the Spring and Spring Boot, don't worry i know I have used some jargons, which may not make sense to you but as we make progress throughout this course, more and more in detail explanation will be shared with you. Before I try to close this lecture, I want to show you some funny memes related to Spring and Spring Boot. As you can see inside this image, if someone wants to develop a web application by using Java, they may have to write millions of lines of code whereas by using Spring, they just have to write thousands of code for the same web application they use Spring Boot, they just have to write some hundreds of lines of code. I hope this image answers most of your questions. The next image is around the greatness of Spring Boot. If you are already inside the industry or if you are entering into the industry, you will see the Spring Boot is used everywhere inside all kinds of applications. If someone is building backend applications by using Java, then there is a very good chance that Spring Boot is getting used everywhere. Before Spring and Spring Boot as I said, we used to have Java Enterprise Edition. So inside this funny image, someone is claiming that I'm a Java software engineer and these soldiers they're asking, can you work on Java Enterprise Edition along with the Timeleaf? Timeleaf is a UI library by integrating this Timeleaf and Java Enterprise Edition. In the olden days, they used to build the web applications. But as we discussed previously, developing web applications by using Java Enterprise Edition is super complex. That's why the developer is saying instead of that, please shoot me. This is the final image as you can see here inside the Java ecosystem, Spring Framework and Spring Boot Framework, they are the leaders. And if you take any other Java framework, it is not even close to them. That's why you should feel proud and happy for the decision that you have taken to learn the Spring and Spring Boot. They're going to boost your Java career.