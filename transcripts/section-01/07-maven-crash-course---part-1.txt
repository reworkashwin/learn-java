As I said before, we are going to use maven build tool throughout this course to build backend web applications. Since there is a chance that you may be completely new to this maven build tool, I want to spend few minutes explaining you about what is this maven build tool, why we should use this as a java backend developer. If you are already familiar with maven and you know how it is going to work, then feel free to skip all these maven related discussion. This maven discussion is going to be super fast you can assume this as a maven crash course within few minutes. Imagine you are building a house, and to build this house, you may need bricks, cement, windows, doors, and many other tools like hammer, brush, drilling machine. To get all these dependent materials and tools, what you have to do? You have to go to different stores across the city to buy each item separately. That would be exhausting, right? This is exactly what developers faced before maven build tool introduced. Let me share more details on how maven changed everything and how it is making developer life easy. When we are writing code java programs, we usually deal with a very few java classes as long as we install java inside our system, we should be able to write any kind of logic. But when we start building the enterprise applications by using frameworks like spring and spring boot or any other library or frameworks, we're going to rely on hundreds of classes provided by the frameworks, third party libraries, external tools so these are all called dependencies. That means to build a java web application, you are going to depend on other frameworks or libraries. Here, I have given some examples as well. Let's imagine you're trying to build a REST web service. For the same, you're going to depend on the spring web library. In the similar lines, when you want to interact with the MySQL database, you will have dependency on the MySQL driver and coming to the scenario where you want to convert the java objects to JSON and vice versa, you're going to depend on the JSON libraries called Jackson. Since we can't write all the code present inside these libraries, we are going to reuse them inside our web application by adding these dependencies. Next to build a simple web application, developers may need to set up around 50 plus dependencies or 50 plus jar files. So in these kinds of scenarios what developers they used to do before maven is, developer had to manually search the internet for jar files like spring -web jar, mysql- connector jar, Jackson jar, all these dependencies, they need to be downloaded one by one by going to different websites. Once our dependent jars are downloaded, we need to place them under the lib folder and this lib folder we need to manually add to the class path of the web application. Basically class path is a location where your web application is going to look for the dependent classes or dependent libraries. And sometimes one jar may depend on 10 more jars or 10 more libraries. In those scenarios, you can imagine the kind of pain the developer has to go through more and more downloads has to be done manually. And the worst part is, every developer in a team had to repeat these painful steps whenever they want to set up the same web application inside their system. And the same applies when you want to move the project to another mission if you end up missing one of the jar, then your project is going to break. To overcome all these challenges, Maven came into picture Maven is a build tool, which is going to help us as a dependency manager apart from acting as a dependency manager, it is also going to help the developers to compile the application, to deploy the application, to install the application. We're going to discuss a few of the most common Maven commands in a few minutes. But before that, let's try to understand what are the most basic details of Maven. When we are using Maven as a build tool inside a web application, we are going to get a file with the name pom.xml. So what is pom? Project Object Model. Inside these files, we need to define all the dependencies that our project has. For example, if I define these dependencies by mentioning the group ID, artifact ID, and the version number, the Maven is going to take care of downloading these dependencies and setting up them under the classpath. Just like Maven, we have another famous build tool as well with the name Gradle. Gradle is not going to have pom.xml it is going to have another file name since Gradle is out of the scope of this course i'm not discussing about it, but any build tool that you take like Maven or Gradle, they all going to work conceptually in the same style. Maybe the syntax or the file names will keep changing from one build tool to another build tool. As i said, we can define our dependencies inside pom.xml by following the syntax. If you have multiple dependencies, you need to repeat the syntax more number of times. So here we are providing details like group ID, artifact ID, and version. Let me explain the purpose of these properties. Every library or dependency that we want to use, it is going to have an unique address. So just like how our homes has address in the similar line, every dependency or library that we are going to use inside Maven, it is going to have an unique address and at the group ID, we need to mention which company or organization build that dependency or that library or that framework. For example, here I have mentioned the group ID as org.springframework.boot. So this gives a clarity to anyone looking at this group ID. It's a confirmation to them that so-and-so dependency is developed by an organization with the name spring framework under the technology called spring boot or boot. This can be the technology name or framework name or the department name it can be anything. Coming to the artifact ID, it is like the project name. So under the spring boot, we have various dependencies. For example, to develop a web application, we have to mention this artifact ID, which is going to download this dependency from these group ID location. And the last detail that we need to provide is, version. So every framework and dependencies, they are going to evolve from time to time. By mentioning these version number, we can try to use a exact version of a dependency. In simple language, you can assume group ID is like a city name whereas artifact ID is like the street name and the version is like the house number. So if you take the same terminology to the dependency management, so the group ID will be the company name or the organization name and the artifact ID will be the project name and the version will be the specific version that we are trying to use. In case if Google has developed a framework or library, they're going to mention the group ID as com.google. Most of the time, this will be the domain name of their website in the reverse order. Under the project name, they can give any project since Google can develop hundreds of projects so for each project they're going to have a unique name, which can be identified by using this artifact ID. Let's imagine a developer has mentioned that dependency inside the pom.xml. Now let's try to understand what is going to happen behind the scenes. As soon as the dependencies are defined under the pom.xml Maven build tool, it will search for that specific library or dependency. Once the search is completed, it is going to download the dependencies automatically. If the given library is dependent on other libraries, the maven is also going to take care of downloading all the required dependent libraries as well by following the concept called transitive dependencies. Once all the required dependencies are downloaded, maven is going to keep them in the correct class path of your web application. As you can see here, there is no manual effort required. The developer, they just have to define the dependencies that they are planning to use inside their project. Here, you may have a question from where the maven is going to download all these libraries? Does the maven is going to visit all the multiple websites inside the web? Off course not instead Maven, it is going to download the dependencies from a central place on the internet called Maven Central Repository. Any organization or open source community who build a framework or a library, they will make sure to publish their work inside the Central Repository. You can consider this like an Amazon website for Java libraries. So once that downloading of the dependencies is completed, Maven it is going to place all the dependencies inside a Local repository. For example, inside the Mac operating system, the maven it is going to store all the dependent jar files under a path called .m2 / repository. For windows, I'll show you how to check this path in a few minutes. With this, what is going to happen is in future if you end up setting up or developing another project, which requires the same jars then the maven, it is not going to download them again from the Central Repository. Instead, it is going to reuse them, which are present in the Local Repository. With this, what is going to happen? We are going to save a lot of time and network bandwidth. I have given some chat flow on what is going to happen as soon as we define the dependencies under the pom.xml, maven is going to check whether the library is already present inside the Local Repo. If it is not found, it is going to download from the Central Repo, which is from the internet once the download is completed, the same jars will be added to the project class path. So once our dependents are added to the project, we can either build, test, package, or run the application by using various Maven commands. Let's continue this Maven discussion in the next lecture as well.