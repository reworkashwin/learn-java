Before we try to explore the basics of Spring and Spring Boot and how they are going to help you in the Java backend development, first, let's try to answer the very basic question that you may have, which is, why can't I use core Java alone for the backend web development? If you are watching this course, you already know core Java. You understand how to create classes, objects, methods, inheritance collections, and maybe even some multi-threading as well. This is good that's a good start. And with this knowledge, you might be under the assumption, I can develop any kind of web applications or enterprise applications by using Java alone. But in reality, you can't develop a good enterprise applications by using core Java alone. We will definitely need help from the frameworks like Spring and Spring Boot. That's why let's try to spend a few minutes understanding what is Spring and Spring Boot, how they are different from plain core Java, why companies across the globe demand these skills and most importantly, how they will make you a more productive backend developer. Let's imagine you decided to develop a web application for an e-commerce website by using core Java alone. If you are going with this decision, you're going to face a lot of challenges. For example, this web application has to interact with that database. Whenever you want to run a query against your database by using core Java, you may need to write these kind of code where first you need to get the connection of the database by passing the URL, username and password. Once you get the connection, you may need to prepare the SQL statement that you want to execute. Once the statement is prepared, you need to actually execute that query by using the methods like execute query. Once you get the results from the database, you may need to convert that result data into Java objects by writing some mapper logic. Along with all this logic, you should also take care of exception handling, because when you're writing this code, it is going to throw an SQL exception, which is a checked exception. And with that, you as a developer will be forced to handle these exceptions by using a catch block or by throwing to the parent method. And finally, you should also take care of closing the connection that you have opened. So if you see, for a simple database interaction by using the core Java JDBC library, the developer has to go through all these steps and all these process. In reality, the enterprise applications, they are going to have thousands of scenarios where they have to interact with the database. In all those scenarios, you need to repeat these kind of complex code. This is the very first major problem that you're going to face. The next problem that you're going to face is around the object creation and dependencies. In real enterprise applications, you're going to create thousands of classes, thousands of objects. All these objects, they are going to work together to execute a flow or a scenario. In this kind of setup, it is the responsibility of the developer to create all those objects manually by using the new operator. And if an object is dependent on other object, that dependency injection also should be taken care by the core Java developer. Here I have given some pseudo code. Let's imagine there is a main class, which has a main method inside this main method the developer is trying to create an object of UserService class. If you closely observe this code, the object creation is happening manually by using the new operator. So once the object is created, the developer is invoking the registerUser() method. With that, the invocation will go to this method. Inside this method logic, it is using another object as a dependency, which is the EmailService. So to this UserService class, the EmailService is a dependent object. So that's why the developer has to make sure to create the dependent objects as well manually by using the new operator. Once the EmailService object is ready, by using the same, the developer is going to invoke the sendEmail() method, which is going to trigger the method present inside this class. And this method is going to take care of sending the email to the customer. So there are a lot of problems with these core Java approach. As I said, the very first problem is the developer has to manually create the objects every time by using the new operator. So this is a simple scenario, but in reality, you're going to have thousands of lines, thousands of scenarios, creating the objects manually every time using the new operator is a very cumbersome process. On top of that, the developers also, they have to take care of injecting the dependencies. And with this kind of setup, the objects are going to have the tight coupling between them. So here in this scenario, the UserService is tightly coupled with EmailService. In future, if you want to switch with the EmailService with some other communication service like SmsService or WhatsappService, the developer has to revisit all this logic and make sure to replace this EmailService with other services like SmsService  or WhatsappService. That means switching between the dependencies is not a straight and easy process. The developer has to edit and recompile the classes like UserService. And with this, the unit testing is also going to be harder because UserService creates its own dependencies. So during the unit testing, if you want to replace these EmailService with some other dummy service, you can't do that because both UserService and EmailService, they have a tight coupling between them. Moving on to the next set of problems. The next problem is around the configuration management. In real enterprise applications, we may have to deal with a lot of properties that we have defined inside the files like config.properties. So loading all those properties into your business logic and using them, it is going to be a very cumbersome process. And the worst part here is none of these logics that we explored so far, they are not related to business logic. All these are boilerplate code or extra code that the developer has to write in order to execute his or her business logic. The next problem is around the creating REST APIs. So with the plain code Java, in order to build a REST APIs, the developers have to use the servlets. By using these servlets, the developers, they should be able to build the REST APIs. But it's a very complex process where the developers, they have to configure the servlets inside the web.xml, they have to define the URL mappings, manage the servlet lifecycle. And these servlets, they are going to process the request and response in the form of HttpServletRequest and HttpServletResponse. So inside these objects only, you're going to have the payload data that's sent by the client. The developer has to write some logic inside the servlet to manually pass the parameters or the data sent in the payload. The developer should also take care of converting the objects to JSON when sending the response to the client applications. When we're using the HTTP protocol for the communication, the Java objects are not going to make any sense. We need to send the data to the client applications, either in the form of XML or JSON. So that conversion also should be taken care of by the core Java developer. So this way, there are a good amount of problems that developers are going to face when they decide to develop a web application with core Java alone. There'll be too much boilerplate code, manual object management, tight coupling between the objects, configuration nightmare, and reinventing the wheel every time they face a problem. So this is where Spring come into picture. So all these problems will be solved by the frameworks like Spring and Spring Boot. Let's continue this discussion in the next lecture as well.