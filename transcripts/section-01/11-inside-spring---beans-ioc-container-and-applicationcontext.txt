Let's try to understand what are Beans inside Spring ecosystem. As I said before, by using the IOC and dependency injection concepts and principles, the Spring framework is going to take care of creating the objects and establishing the dependencies between them. So when a given Java object is being created and maintained by a Spring IOC container, such objects we call them as Spring Beans. So they are normal Java classes that are instantiated and managed by the Spring IOC container. These Spring Beans form the backbone of a Spring application, representing its key components or services. Here you may have a question, why can't we simply call them as Java objects or objects? What is the need to create one more jargon with the name Beans? Let me answer the same. When we are developing a web application by using Spring or Spring Boot, you're going to have thousands of classes not all of them will be maintained by the Spring framework. Few of the classes and their objects, you may want to manage them by manually creating their objects by using new operator. So since there is a possibility of having two different kinds of Java objects, one is being maintained by the developer and the other one being maintained by the Spring framework. So since we have two different kinds of Java objects, to avoid the confusion to the developers, the Spring team, they came with this new jargon called Beans. So when someone is saying Spring Bean or Bean, they are referring to a Java object that is being maintained by the Spring IOC container. Here you may have another question, how my Spring IOC container or Spring framework will know for which classes it has to create by itself ? Because I may have thousands of classes and you are saying, and not for all of them, Spring is going to create the objects. So how do we communicate that information to the Spring framework for which classes it has to create the beans? Let me answer the same. There are various approaches inside the Spring framework. Developers they can make some configurations by using XML, but this is a legacy approach which used to follow a decade back these days, no one is providing the configurations by using XML format. So that's why we are not going to discuss about this approach inside this course. Instead, we are going to discuss more advanced approach. So what are those approaches? By using annotations and Java based configurations, we should be able to communicate the information about the beans to the Spring framework. So there are certain annotations like @Component, @Service, @Configuration, @Bean. By using these annotations, we should be able to provide some meaningful information to the Spring framework and by reading these configurations, the Spring framework will have super clarity for which classes it has to create and maintain the objects or beans. We're going to explore all these annotations in the coming lectures when we are seeing the demo. Every bean that is being created by the Spring IOC container, it is going to have some lifecycle. That entire lifecycle is going to be maintained by the Spring IOC container. When I say lifecycle, it includes various stages like creation, initialization, defining the scope and destructing these beans when we don't require them. All these lifecycle stages will be taken care by the Spring framework itself based upon our inputs. And as we discussed previously, the Spring IOC container, it is also going to take care of injecting the required dependencies into the bean as needed. After the Spring bean, we have another important concept with the name Spring IOC container. We're saying some component inside the Spring framework is going to create the beans outside of the Java classes. What is that component which is responsible to create the beans by following the principles of IOC and dependency injection? It is Spring IOC Container. IOC means inversion of control. So there is a software component inside the Spring framework, which is taking care of implementing IOC concept, dependency injection concept by creating the beans. That software component, we are calling it as Spring IOC container. Its main job is to manage the objects also known as Spring beans in our application it is responsible for dependency injection, bean management and lifecycle management. As I said before, IOC container is a software component inside the Spring framework. So this is represented by an interface called BeanFactory, which provides a basic functionality for managing beans. So there are good amount of implementation classes for this interface. So this interface is present inside this package. We're going to explore more details about this BeanFactory when we are seeing the demo of Spring core concepts in the coming lectures. So this BeanFactory is the simplest IOC container in Spring. It provides a basic functionality for managing the Spring beans. When I say basic functionality, it includes beans creation, dependencies injection, managing the lifecycle of the beans. So this bean factory IOC container, it is suitable for lightweight applications where performance is critical and a few features are needed. But in real applications, no one is going to use these basic IOC container. Instead, they are going to use Spring context. The Spring context is a more advanced container that extends all the functionality of BeanFactory on top of that, it also provides some enterprise features. So these features include Internalization support. When I say Internalization, multiple languages can be supported inside your application. Similarly, it also supports application events like it is capable of publishing and listening to the events inside the Spring. It is capable of loading the resources from classpath file system or URL. It is also capable of reading the properties from properties file, activating the profiles, it can perform the auto scanning of the annotations, it also supports aspect oriented programming. So most of the jargons, it may not make sense to you right now, but as we are making progress throughout this course, all these advanced concepts we are going to discuss in detail. But the information that I want you to understand here is, Spring context is an advanced container that inherits all the basic functionality of IOC container from the BeanFactory. On top of that, it also supports advanced features that are required by the enterprise applications. So ApplicationContext is an another interface available inside the Spring framework under this package. This interface is also implemented by many other classes. We're going to discuss about them in the coming lectures. As you can see, this ApplicationContext extends BeanFactory, which is the basic IOC container. If you ask me for a real life analogy to understand the differences between the Spring IOC container and Spring context, I would say IOC container is like a coffee mission engine. It makes coffee. So just like how BeanFactory is capable of creating beans and managing them, similarly coffee mission is only capable of doing some basic functionality of making coffee. Whereas if you consider the full vending mission, this is very similar to Spring context. By using vending mission, apart from making coffee, we can also provide milk, sugar, snacks option. We can show some messages, accept coins, gives, receives. In the similar lines, we can provide more and more features. I hope you got some clarity. Now, let me show you an image, which help you to understand what happens at the core of the Spring. So we, as a Java developers, we have to provide some configurations to the Spring IOC container on how the beans has to be created. For example, if you're writing 10 different Java classes inside your application, for nine of them, you may want them to be managed by the Spring IOC container, whereas one of the class you want to maintain by yourself. So for these nine classes, you need to provide those configurations by using various annotations that we are going to explore in the coming lectures. So all those annotations information will be given as an input to the Spring IOC container. So whatever objects that are being maintained by the Spring IOC container, they are called as beans. So this will be one input to the IOC container. On top of that, we also going to provide some configuration metadata or context information, which is going to have all the required details for my IOC container to understand on how to build my Spring web application. So this IOC container, it can be a BeanFactory or ApplicationContext that we have discussed. ApplicationContext container is a more advanced container. That's why in reality, all the developers, they are going to use ApplicationContext as a Spring IOC container. So if you provide these two information to the Spring IOC container, finally, it is going to produce a ready to use web application. I hope you are clear about the key components of Spring Core. These are the five components that we have discussed. I'm going to show you the demo of all of them in the coming lectures.