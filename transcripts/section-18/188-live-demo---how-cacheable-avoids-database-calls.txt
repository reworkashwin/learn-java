To get started with the caching inside our Spring Boot backend application, we need to add a new starter project as a dependency. Under the start.spring.io, if you try to look for a dependency with the name cache, you are going to get a result with the name Spring-Cache-Abstraction. You need to select these and click on explore. Inside the pom.xml file, you need to look for this dependency with the artifact id as spring-boot-starter-cache. Let me copy this dependency and paste inside my pom.xml. Since we are into the new section, I have copied the previous section code into this new section folder. You can find all the code that we are going to discuss inside this section under this section folder. Let me add the new dependency followed by I am going to click on this sync maven changes. First, let me show you the caching by using the default cache mechanism inside the spring boot application. . By default, the spring boot application, it will try to store this cache data into the memory of the application. But for the advanced use cases, we need to rely on the products like Redis, Caffeine. So there are a good amount of cache related products, regardless of which underlying product that you are trying to use, the way you use the @Caching annotations is always going to be the same. First inside this lecture, let's see the demo by using that default behavior. As of now, I have not added any caching product related dependency into our backend application. With these, the spring boot application will try to store the cache data inside the memory of the application. So whenever you restart your application, the cache data will get wiped off. Let's try to identify a few of the scenarios where we can use the caching. First, I'll go to the controller with the name, AuthController inside this AuthController, we know whenever someone is trying to register for the very first time by invoking this register related API, first what we are going to do is inside this business logic, we are going to give that new user a default role of job seeker. To assign this role to the registered user, we are invoking this method, findRoleByName() which is present inside the RoleRepository interface. But we know inside the roles table, the data is not going to change frequently. We are going to always have these three records only inside these database table. So that's why what I'll do is, on top of these method, I'm going to mention @Cacheable annotation with the name as roles. So please make sure you are importing the @Cacheable annotation from the org.springframework.cache.annotation package. If this changes, what is going to happen is, whenever this method is being invoked for a given role, at that point of time, a database query is going to be invoked, whereas for any other request, which is invoking the same method with the same name value, the data will be fetched from the cache data. This is one scenario where I want to use caching. Now let's go to the another controller with the name CompanyController. Inside this method, we have a rest mapping, which is going to be invoked by the admin user to fetch all the company details from the database. So company's data also is a kind of static data. They are not going to change very frequently. We're not going to onboard a new company very frequently, whereas the jobs data is very dynamic because the whole purpose of job portal is to create the new jobs, update them, delete them based upon the employer requirements. That's why caching these company's data for admin user will be a good idea. Let me navigate to the method present inside the service class here, on top of this service method, I'm going to mention @Cacheable with the name as companies. So for this method, since we are not providing any input parameters, so behind the scenes, what framework will do is, it will try to store the company's related cache data with the key as empty value. So the key will be like empty value against to that empty value, this entire list of company DTO will be stored as a value. Always the method input parameters that are going to be considered by the framework to create the keys for the cache data. I can show you all that code as well. If you go to the framework class with the name ConcurrentMapCacheManager. So by default, when we are not using any of the cache related products inside our application, I said that the cache data will be stored inside the memory of the application. So this is that class which is responsible to store the cache data inside a ConcurrentMap. Inside this class, you will see all the methods which are responsible for the caching related logic. So here there is a method with the name getCache(). So to this getCache() method, if you pass the cache name, it is going to provide you the cache. The cache data is represented by this cache interface. If you go to this cache here, you're going to have all the methods related to fetching the data and putting the cache data into this cache object. Coming to the cache key strategy, there is a class with the name SimpleKeyGenerator inside the framework. So this class is going to be used by the framework to generate the key for your cache data. So there is a generate() method, which is going to accept the target object method name and the parameters. So based upon the parameters, the key is going to be generated. here, they are again internally invoking this generateKey() method. To this generateKey() method, the params are going to be passed as an input. If the method input params length is zero, this generateKey() method is going to return an empty key. In case if the params length is one, here it is for checking if it is not an array, it is going to simply return the param name as an return value. Otherwise, all the param details will be passed to this simple key constructor. So this constructor will be invoked if the number of parameters are greater than one or if the single parameter is of type array. If you open this SimpleKey constructor, here there is again logic to calculate the hash value based upon the number of input parameters. So this is all the internal logic that framework is going to leverage to calculate the key value for the cache data that you are trying to store. So this is all internals. In case if you want to understand in depth, you can try to debug all this code after implementing the caching related logic. So to this method, since there is no method input parameter, this company's data will be stored against to an empty key. Under the RoleRepository, since here we are passing a single input parameter to the method, this input parameter value will be considered as a key when this role entity object is being stored into the cache data. After mentioning this @Cacheable annotation at the required methods, we should also mention another main annotation to enable the caching inside our Spring Boot backend application. I'll go to our main class, which is JobPortalApplication. On top of these class, we need to mention an annotation, @EnableCaching. So this is very important, otherwise the caching is not going to work inside Spring Boot application. After making all these changes, we need to restart our backend application because we have added a brand new dependency into our application. So now to show you that demo of caching inside JobPortal UI application, I'll try to login as an admin. You can try to test this scenario from Postman as well. So under the admin menu, we have a option with the name company management. So when I click on this link, we know behind the scenes, the companies will be loaded from the database and displayed to the admin. Since we are doing this very first time after restarting our application, there won't be any company's cache data. So that's why actual database query is going to be fired and the output will be cached by the framework. So let me clean the logs inside the console. Now I'll click on this company management, which will display the list of companies to the admin user. If you try to understand the logs that we have inside the console, you will see there is an SQL statement fired against to the companies table, which fetched all the company's data for the admin user. So next time, if I try to access the same page, this SQL query should not get fired. Instead, the data should get fetched from the cache. To confirm that we can try to delete these console logs. Now, if I go back and come back to this company management, this time you will see there is no SQL statement inside my console logs. And in fact, if you see the total execution time is zero milliseconds. That means within some microseconds, that data is fetched from the cache and returned to the client application. The same kind of behavior we can see when someone is invoking these findRoleByName. So what I'll do is, I'll try to register a brand new user. And when I'm doing this very first time, this query is going to be fired. Whereas for all the subsequent new user registration, the DB query is not going to be fired. This time I'll show you the demo by using the postman. So first I need to get a CSRF token. So this is my CSRF token. The same I need to mention under the register headers. Under the body, I'm mentioning the email as testing@gmail.com with this mobile number. If I click on this send button, a new user is created. Let's check the console logs. If you see the logs, there is an SQL query fired that selects the role record based upon a role name. So this is the SQL query. Let me clean all these logs. This time I'll try to register a different user with the email as testing1@gmail.com and mobile number as 90. If I click on this send button, this time also the user is created. But the beauty this time is there is no select query fired to fetch the role record from the database. So this is an select query, which fetch the data from the job portal user table based upon the email number and mobile number. This we are doing for validation. The next query is directly in set query. In between, there is no select query that fetches the role record object. So this time, what happened? The framework fetched the data from the cached data. I hope you are clear with the small demo around the @Cacheable annotation. As a next step, we should also worry about the scenarios which may update the data inside the database. For example, as of now, we are simply caching the company's data here whenever someone is invoking this method very first time. But there are also other methods which are going to delete the company data based upon the ID, similarly, there are some methods which are going to update the company's data. So whenever those methods are getting invoked, we need to tell to the framework to update these cached data. Let's try to discuss about this scenario in the next lecture.