As of now, inside the RoleRepository interface, on top of this method, we are using this @Cacheable annotation. For the roles cache data, nowhere we have used @Cache put or @CacheEvict annotation because there are no scenarios inside our backend application where the roles data may get updated or may get created. So with that, we are going to have a disadvantage, which is these roles cache data, it is never going to be refreshed. Let's imagine if there is a scenario where the application server is not restarted for a month or for two months. And with that, these roles cache data, it is going to stay inside the memory of the server for more than a month or two months. This is usually a not acceptable approach in an enterprise application because in real applications, there'll be some hundreds of scenarios where these roles data may get externally by using database scripts or by some other applications. That's why as a best practice, whenever we are trying to create a cache, we should also configure an expiration time or an refresh time. For example, if I configure 10 minutes as an expiration time, after 10 minutes, the cache will get automatically invalidated by the framework so that after the 10 minutes, the very first request that comes to the backend application will fetch from the database and the cache will be refreshed with the latest data. To implement all these kinds of advanced use cases, we can't rely on the default in-memory cache that is provided by the ConcurrentMapCacheManager. Instead, we need to rely on a good caching related product. If you go to the SpringBoot official documentation here, you can search for Cache and there is a dedicated section with the name Caching. So this is present under the SpringBoot reference IO caching. Inside this page, if you try to read the documentation, the SpringBoot caching mechanism, it supports all these cache providers. So these are all the famous cache providers inside the industry like Redis, Couchbase, Infispan, Hazelcast, Caffeine and so on. Inside real enterprise application, the developer has to develop the caching mechanism by using any of these providers. Most of the enterprise applications, they rely on Redis or Hazelcast, Couchbase, Infispan or Caffeine. Inside our course, I'm going to use this Caffeine option. The reason why I'm using this CaffeineCache provider is, it does not require any separate installations. Whereas if you are going with other providers like Redis, it expects you to set up a Redis inside your local system. Just like how we set up MySQL database, in the similar style, we need to install and set up Redis as well. Since I'm trying to avoid all those operational stuff, I'm trying to use this Caffeine option. Let's try to understand how to use this Caffeine as a cache provider inside our SpringBoot application. As I said before, we should always try to implement caching with some expiration time or refresh time. So this concept is called as TTL,so TTL means Time To Live. In other words, this time to live is nothing but expiration time to our caching. So to control the expiration, we need a real cache provider like Caffeine or Redis, because the default in-memory cache does not support TTL. To use Caffeine cache provider, which is a lightweight and in-memory fast provider, we have to add this dependency inside our pom.xml. Caffeine is a high-performance in-memory caching library, which is developed by the makers of Guava library. This is designed as a modern replacement for concurrent HashMap or EhCache provider. Caffeine is ideal for caching read-heavy, frequently accessed data scenarios. Once we add this dependency, as a next step, we need to define a Bean of type CacheManager. When we are creating this Bean with the help of a Java method, we need to write this kind of logic. First, by using the constructor of the Caffeine cache class, I need to provide a cache name followed by what is the expiration time. So here I'm providing 10 minutes as an expiration time. So this expiration time, I can set by invoking expireAfterWrite() method, which is available under this Caffeine.newBuilder(). So with these configurations, I'm telling to the framework, once the cache is returned, please set the expiration as 10 minutes. And by using maximum size, I'm telling to the framework, under these companies cache, you can only store up to 500 different key and values data. And towards the end, we need to invoke the build() method. So this is going to give me an object of type CaffeineCache for the same I have provided this name. In the similar styles, I need to define all the caches that I want to create inside my application. Under the second configurations, I'm trying to create another cache with the name jobs using these jobs cache, I'm going to store all the jobs related data, which is going to be automatically refreshed after 10 minutes with the maximum size as 5000 records. And towards the end, I'm trying to create the roles related cache as well, for the roles, I have set that expiration time as one day. Since inside my application, roles are going to be updated very rarely, that's why I have increased the expiration time to one day, so this expiration time can be completely modified based upon your business requirements. You can also define a cache for a single minute or 30 seconds 20 seconds based upon your business requirements. Once all the required cache objects are created, these objects we need to provide as an input to the set caches method. This set caches method can be invoked on top of the SimpleCacheManager object. So first I have created SimpleCacheManager object by using this new operator followed by I'm trying to invoke the set caches method to this set caches method, I'm passing all my cache related objects as an array. And towards the end, we need to return this manager, which is of type cache manager. So all these code we need to write inside a configuration file, so once we have written these configurations, this is how the framework is going to create your caches behind the scenes. So these are the cache names with their TTL duration and with their maximum entries. It is also possible to define these cache configurations by using properties as well. For example, by using spring.cache.type, I can mention to the framework, what is the cache provider that right now I'm trying to use. So here I have set the caffeine followed by I can create any number of caches by using the spring.cache.cache names. Here I have provided the cache names like jobs, comma roles. If I want to create many more caches, all of them, I need to mention by using comma as a separator. For all these caches, we can provide specifications by using spring.cache.caffeine.spec. To this, I'm providing maximum size as 5000, expire after access as 600 seconds. So all these specifications has to be separated by using comma as a separator. But there is a disadvantage with these properties, whatever specifications that you provide, they are going to be applicable for all your caches. Whereas with this approach, you're going to have an opportunity to define different configurations for different caches. As part of the next lecture, let's try to update our caching mechanism by using CaffeineCacheProvider.