Instead of manually creating this constant in each of the class by providing this class name and invoking this method, we can simply use one of the @Lombok annotation. The annotation is @Slf4j Whenever we are adding this annotation, inside your compiled code, a constant with the name log is going to be generated by the Lombok. So this is the constant variable name with all small case letters. The same I can use inside my business logic to perform the logging. Now I can comment this constant that I have created manually, followed by here we need to use the variable name that is provided by the Lombok library. What is the variable name? It is log. Let me use the same in all the remaining lines as well. So do you see how simple it is from now onwards, whenever you want to perform the logging inside a given class, simply use this @Lombok annotation. Now let's try to understand the next set of advanced properties that we can use whenever we want to perform logging inside a backend application. As of now, I explained on how to set the LOG_LEVEL for a given package or for a given class name by using this property. We can also define log level for a group of packages or for a group of classes as well. For example here, I'm going to invoke a property with the name logging.group. To this group, I can provide a name, for example I'm mentioning the name as jobportal_error. To this group, I can assign the set of packages that I want to tag to this group. So let me mention com.eazybytes.jobportal.company followed by I'm also going to mention com.eazybytes.jobportal.security. Next I'm also mentioning com.eazybytes.jobportal.contact. So this way I can assign any number of packages by using comma as a separator. Now to this entire group of packages, I can assign a log level, for that we need to use one more property. We just have to type logging.level.jobportal error. So here we need to mention the group name, the group name that I have given the same I'm trying to use inside the second property as well for this group by default, I'm assigning the category as error, but with this setting, there is a conflict with the above properties, with the above property, we're telling for all the classes under these job portal package, please use the info log level, whereas with these grouping related property, we are telling for so-and-so packages use the error log level. So we have this LoggingController under the company package. So this company related package, we also mentioned here as well under this group. So there is a conflict here. Let's try to understand which one is going to be considered by the Spring Boot framework. I will try to clean my console from the postman I will invoke this API and here I'm able to see only the error related log message. That means the Spring Boot is giving more priority to the group configurations that we have done here. If I remove these company related package name, this time, this info level will be considered for these LoggingController. Let me clean the console and invoke this REST API. This time we're able to see info, warning and error as expected. Always use these grouping related option as well. Sometimes in real enterprise applications, you're going to have a requirement where for controller layer and security and service layer, you may want to consider only errors, but for other packages, you may want to set other log levels like warning info based upon the business requirements. Right now, inside our application, the logs are getting printed onto the console by default, but we also have an option of saving the logs into a log file as well. And that's how real enterprise applications, they are going to operate as well. We should always try to store the log messages into your log file as well. So that in higher environments, whenever an issue is identified, we can go and open these log files to understand if there are any exceptions occurred at a given point of time. So to save the logs into a file, we just have to use a property with the name logging.file.name. To this, we can provide the file name. So the file name I'm mentioning as jobportal.log. So let me save these changes, do the build once the build is completed, I will try to restart my application. This time, along with the logs being printed onto the console, the same logs will also be stored into the file as well. So if I open my workspace, there is a log file name generated with the name jobportal.log. Inside this log file, we have all the log messages saved as well. But here the log messages format is something different from what we are seeing on the console. That is because by using this property, we only set the log pattern for the console. In the similar lines, we also have another property for file as well. So we just have to invoke logging.pattern.file. To this, we can mention what is the log format that we want to consider while saving the logs into the log file. Don't try to use this format because this format does not have date and time. You should definitely catch the date and time values inside the file. Because in higher environments, since you are going to save the logs for all the previous days, including the current date, unless you have the date, you can't differentiate when an issue happened. Moreover, while we are trying to save the logs into the file, we should not use traditional log format. We should use JSON format. The reason on why we should use JSON is, usually all the production logs that get generated for a backend application, they will get feed to products like ELK, Splunk, AWS CloudWatch, all these products, they can understand your logs in a better way if you're logging the logs by using the JSON format. The JSON format also is going to give flexibility for you to search for a log for a given timestamp or for a given search criteria. So these are the advantages, because with JSON, you will end up having a structured logging. Always these products, they can expect your log messages in a given structured predefined log JSON format. Let me mention the pattern for the file here. The pattern is first, I need to mention the curly braces at the starting and at the end, because we know inside JSON, everything we are going to put as a object inside the curly braces. So inside that JSON object, I'm going to have a timestamp key for this key I want to assign the value based upon this format. So the format is year, month, date, and time. After the timestamp, the next property I want to populate is level followed by logger. And at last, I'm also going to print the message as well. This way you can customize this JSON format based upon your business requirements. I'm not trying to inject the value by using environment variable because for all the environments, I want to use the same JSON format. In case if you want to store this file in a given location, along with the file name, you can also give the folder name as well. For example here this time, if I mentioned the path as logs, this time you will see the jobportal.log will get created under the logs folder inside my workspace. If you give an absolute entire path, then your log file will get saved into your path that you have configured. Let me save these changes. Now, before I try to restart my application, I will delete these existing log file that got generated. This time, if I restart my application, you will be able to see a logs folder is created inside my workspace. Under this, I have this log file. Inside this log file, all the logs, they are stored by using the JSON format. But if you keep saving your logs into this log file, at some point of time, this log file will have data of GB. Like it may have 10 GB, 100 GB or 1 TB of data as well in a single text document. And opening such document or processing such document is going to be super difficult. That's why these logging libraries, they also give the developers an option to rotate the log file names based upon some predefined criteria. For example, you can ask the framework to create a new log file for every day or based upon the size as well. If you define the size as 10 MB, as soon as the size of the file reached to 10 MB, the libraries will create a new file and will start writing into that file. All the old log files can be moved into some archived folder as well. So this kind of advanced setup is also possible. Let's try to explore the same in the next lecture.