I copied the Job Portal backend application code from the previous section folder 2 into the new folder that is specific to this section. The same I have opened using IntelliJ as well. If you try to see the logs that we have inside the console, this is the pattern that right now my application is using. This is not the default pattern. I customized this format by using a property inside the application.properties. We have done this long back. If you look for a property with the name logging.pattern.console, by using this itself, we have configured the logging format that we are seeing inside the console. By default, as I said Spring Boot will consider info log level to print the log statements onto the console. That's why if you try to understand the log statement severity inside the console, you will only see either INFO, WARN, or ERROR. So all these are info log messages. Let me scroll down,scroll down. There is a warning one here. If I keep scrolling, keep scrolling, there is another warning. Seems I don't have any error related logs, but if there is any exception happening in our application, those will be printed onto the console. So this is that default behavior. Now let's try to understand how to change these default log level inside a Spring Boot application. Again, this is very easy. We just have to use a property. Let me mention the property name. The property name is logging inside Spring Boot. All the logging related properties, they are going to start with the prefix as logging. Next I need to type level. After this level, I need to mention root. To this property, I can provide a value. So here I'm trying to use this Environment variable syntax. I should be able to inject the log level by using the Environment variable. Otherwise, it is going to construct this INFO default value. Let me change this INFO to TRACE. Now I'll save these changes, do the build, and I'm going to restart my application. This time, you are going to see too many logs inside your console. And this is going to delay your startup of our application as well. So that's why, don't try to enable trace or debugging log level unless you want to really DEBUG a issue inside your code. My application is still starting, starting because during the startup, we know Spring Boot is going to do a lot of magic around the auto configurations, setting up of the required beans, everything that is happening behind the scenes. The framework is now printing onto the console because I have enabled that trace logging level. And since trace is the lowest severity level, everything about trace is also going to be printed. My application is still starting. We don't need to wait for this to complete because it will take another 5, 10 minutes. Let me stop my application. Since we have used root here, we are telling to the Spring Boot framework for all the packages and classes that we have inside the Spring Boot and inside my application for every one of them I'm trying to set the log level as TRACE. We also have an option of setting this log level at a package level or at a class level as well. I'll show you that in few minutes. Let me revert back this trace to info and I'm also going to comment this property as well because using these log level.root is not recommended for production application. Unless you want to debug something on the framework code, don't try to use this property. To show you that demo on how to perform the logging inside our business application, I will try to create a dummy controller. Let me go to any of the package like company and at this company, we have the controller package inside this, I'll try to create a dummy controller with the name LoggingController. On top of this, we need to mention @RestController followed by I'm also going to mention @RequestMapping with a path as logging. Inside this controller, I'll try to create a method that is going to return String as a response output. . The method name is testLogging() on top of this method let me use @GetMapping and to this, I will try to provide version details with the value as 1.0. From this method, I'll try to return the successful message saying that logging tested successfully. So this is the message that we are going to return when someone invoked this REST API. Now let's imagine inside this Java method, I want to write a log statement. Whenever you want to write a log statement by using the logging framework, first you need to create a private static final variable by using the logger from the slf4j library. Let me type private static final and here I'm going to accept this recommended code. So this is the complete code that we need to write. So let me remove these package details. These package details we can mention as part of the import statement. So this logger is an interface with the same name we have many other files as well in many other packages, but please make sure you are importing the interface from the slf4j. And next we have the logger factory, which is a class from the slf4j, the same you need to import. Using this logger factory, we need to invoke the getLogger() method. To this getLogger() method, we need to pass the class name where this constant is created. That's why we need to mention the class name as LoggingController.class. I'll explain you the reason on why we need to mention this class name in few minutes. So this getLogger() method, it is going to provide us an instance of logger, the same we're trying to accept as an output by using this constant name. The constant name is LOGGER, which I have mentioned in complete capital letters, because this constant is of type static and final. Now by using these LOGGER variable name, I should be able to invoke various methods. For example, I can invoke info method if I want to log a message by using the info log severity. Similarly, we are going to have error methods, warning methods, debug methods, and trace methods. Each of these log level methods, they are going to support multiple overloaded methods as well. Most of the times, the developers, they will end up using the first one, which is going to accept a simple String as an input. But for production enterprise applications, I recommend using the trace methods that accept String format as an input along with the argument values. Let me mention few lines of log statements here. You can get this code from the GitHub repo as well. I'm trying to do this to show you the demo of logging. First by invoking the trace, I'm passing this message, trace. This is a very detailed trace log used for tracking execution flow. Next, by using the debug, I'm passing another log statement. The same I have done for info, warn, and error. So let me save these changes to the build once a build is completed, I'll try to restart my application. Make sure inside the application.properties, you have commented these logging.level.root. We should also make sure to configure the new path under the public path so that we can invoke this REST API without any security. So let me take this and here, first I have to mention logging followed by, I will copy the API and copy it here. I think since this is a public path, we can also try to mention public to be consistent with the other APIs. Let me add the path to these @GetMapping as well. Here I need to invoke the path property to the same I need to provide the path as public. Now from the postman, I'll try to send a request by using the HTTP GET method. So under the security folder, you will see a request with the name TestLogging. Please use the same. Before I try to click on the send button, let me clean my console so that we can see the log messages that are being printed. If I click on the send button, I'm going to get this response. But inside the console, you will see the INFO related statement, WARNING related statement and ERROR statements they are printed from where these statements are being printed ? They are being printed from a class with the name LoggingController that are present under this package. Since the package name is a very lengthy one, the framework, it will try to do abbreviation of it. So com.eazybytes.jobportal.company.controller.LoggingController. So this is the class name. From this class name, these statements are being printed. How my framework know about the class name? If you closely look at the logic that we have written, we have created a logger constant by providing the class name. By using the same constant, we are trying to invoke that trace, debug, info, warn and error methods. So this is how my framework will know the class name when I am trying to log a message by using any of these methods. This is the reason that we need to make sure to create a constant in each and every class that we have inside our application, and while creating this class, we should also provide the class name as an input. Otherwise, if we don't have this concept, then the developers, they always going to scratch their head to understand from where a given log message is being printed. So right now we are able to see under the console, info, warning and error messages are being printed. Why? By default, as we discussed the spring boot framework, it is going to set the log level as info. That's why we're able to see info and above severity levels onto the console. If I configure to trace, then we should be able to see all the log statements. Let me show you on how to set the log level for a given package or for a given class. We need to go to the application.properties here. We need to type logging.level followed by, we can type the package name. So these are all the package names that I can use, but I don't want to print any of the framework related logs. Instead, I want to print my own log messages that have returned inside my application code. So what is a root package name for all our classes? It is com.eazybytes.jobportal. So if you go and check our package structure, this is the root package name that we are using the same. I'm trying to mention after the job portal, I can mention the log level. I can pass the log level value or I can use this environment variable syntax as well. So this time I will mention trace. With these configurations from now onwards, any class present under this package or sub packages are going to have the log level as trace. If you want to configure log level for a given specific class, you need to mention the complete class name. So after the job portal, you need to mention company.controller.LoggingController. But I don't recommend you configuring log levels at the class level. You can always use this property where you can try to configure the log level by using your application root package. Let me save these changes do the build and I will clean the console. This time, if I invoke the same REST API, you will be able to see the log statements related to the trace, debug, info, warn and error are being printed. This is the great advantage that we have when we implement a log statement by using the logging framework. This kind of flexibility you are not going to have when you are using system.out.ptln(). As a developer, I just have to provide the log message to the logging methods. Other details like timestamp, logging info, what is the thread name, package name, class name, everything will be taken care by the framework. Whereas if I'm using the system.out.ptln(), I need to provide this entire String including the date time as well. Because system.out.ptln() methods, they simply print whatever I'm giving them as an input. That's why I never try to use system.out.ptln() inside your production applications for your local testing, that should be fine. Using system.out.ptln() is completely not acceptable. Let me update this trace value to info so that I can see only the info, error and warning related messages inside my application. I don't want to see any debug or trace related information because that will result into that too much of logging inside my application. In the coming sections, we are going to add more and more loggers to our business logic. Please make sure you are using INFO, otherwise you will end up having too many logs inside your console. We need to discuss a few more details around the Logging. Let's continue this discussion in the next lecture as well.