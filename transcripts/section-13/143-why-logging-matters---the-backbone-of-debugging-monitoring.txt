We are making a very good progress in building Job portal backend application at regular intervals of time. . We're also testing our backend application by using this UI app as well. In the same process, we're also understanding how the end-to-end flow works in an typical enterprise application. We need to develop many more REST APIs that supports various functionalities of various roles that we have inside the job portal application. But we already have a good knowledge on how to build the REST APIs. That's why what I'll do is, before we try to build many other REST APIs, I will try to introduce many other important concepts related to the backend application, so that you also won't get bored with the same kind of REST API information. So far, we already explored few important concepts like auditing, global exception handling, backend validation, spring security. Now as part of this section, we are going to explore on how to perform logging by using the spring and spring boot framework. What is Logging? Let's try to understand the same. Logging is the process of recording important events that are happening inside an application. This logging process, it is going to help the developers in debugging their code, monitoring and troubleshooting any issues that they are identifying. Logs provide various information like errors, warnings, execution flows, and performance insights. So these are the key benefits of logging. But here you may have a question, why should I use an advanced concept like logging when I'm able to print my statement onto the console by using the System.out.println() from the code java library? It's a very good question? Let's try to understand the same. If you're trying to implement logging by using the system.out, it has some serious drawbacks. The first one is, it does not support log levels. For example, you can't differentiate various log messages like errors, warnings, information, debug. Always the System.out.println(), it is going to print each and every log message that we are providing with no log severity details. Next, by using the system.out.println(), we can only log the details onto the console, but not into any files. But in real enterprise production applications, we won't run our application using IDs like IntelliJ. That's why in such scenarios, console logging is not going to help us. Instead, we should do the logging into the files so that whenever an issue identified inside the production, the developers they can open these log files and try to understand what happened at a given time. System.out.println(), it also does not support any formatting of the logs. Always every minute details of your log message needs to be provided as an input to the system.out.println(). That's why using the system.out.println() is not a production recommended approach. In fact, inside real enterprise applications, no team lead or no code reviewer, they're not going to accept a code if some junior developer is using system.out.println() in his code. I hope you are convinced on why we should do the logging. Now let's try to understand how Spring Boot is going to help us in implementing logging inside a backend application. Behind the scenes, Spring Boot, it is going to use SLF4J. SLF4J means Simple Logging Packet for Java. By using this SLF4J, the Spring Boot is going to perform the logging. As a developer, we don't have to add any dependencies. It is going to work out of the box. We just have to develop the log statements inside our business logic. And with that, the log statement will start printing onto the console and onto the files based upon our configurations. So what is this SLF4J? This is a common logging interface that Spring Boot uses to write the logs without caring about which logging framework is being used behind the scenes. Inside Java ecosystem and Spring Boot ecosystem, we can implement logging by using various frameworks like Logback, Log4j2, and JavaUtilLogging. These are the frameworks or the libraries that are responsible to perform the actual logging. But since we have multiple options here, various developers or various projects, they will end up choosing any of these logging libraries. And all these libraries, they are going to have various syntaxes or various methods to implement the logging. Since it is going to create a problem in the future, when someone wants to migrate from one logging to other logging library, they may end up doing a lot of code changes. That's why what Spring Boot does behind the scenes is, it is going to use SLF4J, which is a generic logging API. So this is like an interface. It does not provide any implementation. By using the generic logging API, we should be able to implement logging mechanism. And by default, this SLF4J is going to use Logback as the default logging library. If you want, you can switch to any other libraries as well. The beauty of SLF4J is, regardless of which logging library you are trying to use behind the scenes, the way you write the logging related logic is not going to change. That's why switching to other logging libraries is not going to be a painful process. You just have to add the required dependencies on the pom.xml. So inside the SLF4J, F means Facade. So what is the meaning of Facade inside English? Facade means it's a wrapper or a front door of a building. When we are using this library, it provides one common logging API and internally it forwards the logs to the actual logging framework based upon the which dependency that we have inside our project. By default, SpringBoot is going to use Logback. This is the default log format that you see inside any SpringBoot application. When you start your application inside the console, you're going to see certain logs, and if you have not provided any logging related properties around the customization of the log format, this is the default one that the SpringBoot is going to use. First, it is going to print the date and time, followed by log level. Under the log level, we have various log severities that are supported by the framework. We are going to discuss about them in a few minutes. After the log level, it also displays what is a process ID that is processing your business logic followed by the separator. Next it is going to mention the application name. So this application name you can define by using a property called spring.application.name inside your application.properties. In case if you are also providing application group by using this property, that information is also going to be displayed after the application name. Next it is going to display what is a Thread name that is processing the current business logic. Here the Thread name is main. Next it is going to display any tracing related information by using the correlation ID. It is not shown inside the above console message because tracing and correlation ID are the advanced concepts which I discussed under my microservice course. For a normal monolithic backend application, you don't need to worry about the correlation ID. Next we are going to have the logger name. Under this, it is going to display the actual source class name often abbreviated. So this is my package along with the class name. Next after this class name, it is going to print the actual log message. So this is that default format, but we can customize this by using a property called logging.pattern.console. We are already using this property inside our application.properties file by using this pattern only right now we are printing the logs onto the console by using various colors, which is going to make our local development fun. Before we try to explore about this logging, we need to understand the logging levels that are supported by the Spring Boot framework. Whenever you are trying to log a statement by using the, any of the logging library, these libraries they are going to ask what is the log level or the severity of your logging message. We can use any of these log level. For example, I need to use the error whenever I want to highlight about any of the serious problems that I have inside my application. For example, whenever a runtime exception is happening or a transaction is being failed, these type of issues I need to highlight by using the error log level. By using these warning, I can highlight potential issues inside my logs. For example, if someone is using deprecated API or if a given transaction is resulting into high memory usage, anything that may result into an error, I can highlight it using wARN. WARN means warning. Next, if you try to reduce the severity level, we have other categories like INFO. By using this INFO category, we can print any general application information like Startup messages, business events that are occurring inside our application. After info, we have debug by using this debug, developers they can try to debug their code. For example, they can try to print out what are the variable values, what are the steps that are being executed inside a business logic at runtime. Since developers, they can't debug the code inside the production environment, what they usually do is, whenever they are facing a problem in a given method, they're going to put these debug statements so that they can see what is happening at runtime, what value is coming to a variable, which if conditions are being executed. So everything they can track by using these debug level. Next, we have very lowest severity level trace. By using this trace, we can try to log most detailed information, like which methods are being invoked, which if condition is being invoked. So this is the most detailed log level. Usually developers, they don't use this trace because too much of logging also is going to impact the performance of the backend application. I hope you are clear with this log level. I will share more details around this log level when we are seeing the demo as well. Now let's imagine the developer implemented logging by using these log levels. At some places, the developer might have used either warning or error, whereas inside the actual business logic, the developer might have used INFo, DEBUG, TRACE based upon their requirements. The beauty of these log level and the spring boot logging is, when we implemented logging inside a backend application, we can control what kind of logs should be printed onto the console or onto the file. How to control that? It's very easy. We just have to use a property by using that we need to configure a log level. For example, if I configure the log level as error, then inside my entire backend application, only the log message that are having the log severity with the error are going to be printed onto the console. Whereas if I configure the log level for my application to warning, then both the warning and error related messages, they're going to be logged. Anytime, if you are configuring a log level, all the higher log levels are also going to be constructed by the framework along with the given configure level. For example, for the trace, these are all the higher log severities with this configure log level inside an application, all the logs with the log levels like TRACE,DEBUG,INFO, and WARN and ERROR will be printed onto the console. By default spring boot, it has configured the log level as INFO. That means if you are not customized this log level inside your spring boot application, by default spring boot, it is going to print all the logs with the log level info, warning and error. And this is a decent default choice that I recommend for everyone as well, because if you configure TRACE or DEBUG, you will end up having too many logs and that will impact the performance of your application as well. You need to configure these log levels only when you are trying to debug an issue or identify a bug. So this is a quick introduction about the logging in spring boot. I know you may have too many questions. Don't worry when we implement these inside our backend application, most of your questions will be answered.