So far, we have been discussing a lot of new technical concepts around the spring framework, but right now for a change for the next few lectures, let's focus on enhancing the job portal application, as of now when an admin logged in with his credentials, the admin is able to access only the contact messages page inside the same page we also implemented sorting pagination. And the admin also has capabilities to close the message once it is handled by his team, just like how we have this contact messages functionality under the admin we also have other two functionalities like Company Management and Employer Management. As the next step, we can try to build the supporting back-end rest API's for these two admin related functionalities as well. When the admin navigate to this company management page, all the existing company's details should be displayed to the admin, followed by the admin should have the flexibility to delete a company, to edit the company details or add a new company as well, because adding the company is a very first activity that the admin has to do. Until a company is created, the recruiters, they can't post jobs against to that company. We have to maintain these functionality under the admin so that no one else can create any companies by themselves, because they have to go through some background process where they have to reach out to the sales and marketing team of the job portal application. Once they negotiated with the terms and conditions, pricing, everything, then the admin is going to create a company followed by the recruiters can start posting the job details against to that company. What I'm going to do is inside this lecture, I will show you all the supporting back-end rest APIs that I have developed to behind the scenes. Try to understand what APIs that I have developed with what logic. Once you're clear with all the functionality of this company management, I would request you to build the same rest APIs inside your back-end application as well, by taking my code as a reference. I'm not going to type the code of all these new rest APIs around the company management, because we're not going to discuss any new concepts here. By using the same existing concepts that we have discussed so far, I'm going to build the rest APIs. All the code that we're going to discuss for the next few lectures, you can find it inside the GitHub repo under the respective section folder. Under the Company Controller class, I have created a new rest API that is going to allow the admin user to create a brand new company inside that job portal application. Since this API is going to create a brand new record, I have developed the rest API by using the Post mapping and the path it is going to use is api slash company slash admin with the version 1.0 and this is the method name and this method, it is going to accept the request from the UI application or from the client application in the form of CompanyDto. Inside this CompanyDto, I have added few validations around the name, logo, industry, size, rating, locations, founded, description, employees, and around the website fields. Before this lecture, we always going to send that company's related data from our backend application to the client application. That's why we don't have to maintain the validations. But from this lecture, since we are going to accept the company details as an input from the client applications, we should make sure to enforce validations by using these annotations, all these annotations we already discussed. So please make sure to copy this code from the GitHub repo, or you can look at the code present inside the GitHub repo and try to update your code in the very similar lines. I have used very basic validations like @NotBlank for the rating I have used a @DecimalMin and @DecimalMax because it is of type BigDecimal, but all the String data types I have used to @NotBlank, whereas for the founded, I have used a minimum value as 1900 under this founded, we're going to maintain what is the year that a given company is founded. Next we have employees and these employees also, I have mentioned that minimum value has to be zero. But I think one is going to make sense. So let me update the message to employees must be greater than or equal to one. So next we have website. Since this is a String data type, I have used @NotBlank here. And we're not going to ask the createdAt and the jobs details for the admin. That's why on top of these fields, I have not provided any validations. So now by taking these CompanyDto, the spring boot framework, it is going to perform the validations because I'm using @Valid annotation here. Along with that, I also mentioned @RequestBody annotation by using this CompanyDto, I'm trying to invoke one of the service method that I have created inside the CompanyServiceImpl. So here, if you see from the DTO object, I'm trying to copy that data into an entity object. So this is the new private method that I have introduced. So first I have created a new company object followed by, I'm trying to copy all that data from this DTO into this company object by using these BeanUtil.copyProperties. So this code is going to work only if you are maintaining very similar field names in both the entity and DTO class. I request you to maintain field names same as me so that the UI application will work without any issues. Once a companyEntity object is created, I'm invoking the save method by using the companyRepository. After the record is inserted, the framework is going to provide the updated entity object inside this updated entity object the framework is going to provide what is the primary key ID value. So here I'm checking if the primary key ID value is greater than zero, if yes, that indicates that the record is inserted. Based upon whether the record is inserted or not, we're going to return a simple Boolean value from this method. Since this method is going to create a brand new data, we need to make sure to mention this @Transactional annotation on top of this method. Without this, the transaction will be only readOnly. And with that, you're going to get a runtime exception. So the Boolean value that is returned from the service method is going to be accepted here, and based upon these Boolean value, I'm simply returning whether the request is processed successfully or not. And along with that, I'm providing the status value as either 201 in the success scenario or 500 in the failure scenario. So this is the API, which is going to help the admin to create a brand new company record. Next in the similar lines, I have developed another REST API, which is going to help the admin to load all the existing companies from the database. So this is the REST API. It supports get mapping with the path as admin and version as 1.0. This is the method this method is not going to accept anything as an input. From this method, I'm invoking a service method with the name getAllCompaniesForAdmin(). From this service method, I'm invoking the findAll() method that is provided by the list CrudRepository. We know what this method is going to do behind the scenes, it is going to fetch all the records from the database without any where condition. So this method is going to return me list of company objects, but we want to return the DTO objects from this service method. That's why by using these list, I'm trying to iterate all the elements and during the each iteration, I'm invoking these transform company to DTO for admin. So inside this private method, I'm trying to copy the data from company entity object to company DTO object. So while I'm copying, I'm intentionally not sending any job details. The last field under this company DTO, it represents what are the list of jobs that are associated to this company. Since for the admin, the list of jobs does not matter to him. What I'm doing is, I'm simply sending only the company details, but not any associated jobs. So this is going to bring some performance improvements. Off course, I can use the other method that we have already, which is going to copy both the company and the job details as well, but I don't want to do that because for admin, the job details information is unnecessary. So once our data is transformed to DTO objects, we are returning the list of DTO objects from this method. The same are being accepted inside this method. So whatever list that we received from the service layer, the same we are trying to send under the body of the response. So this REST API is going to help the admin to see what are the list of companies that are already stored inside the database. Once these details are displayed on the UI, the admin has an option to either edit existing company details or delete an existing company record. To accommodate those two operations, I have developed another two REST APIs. This REST API, it is going to support @PutMapping because by using this REST API, the admin can update the details of the company record. So this is the path under the path, the client applications, they have to send what is the primary key value of the company record. So this method, it is going to accept these ID value along with the company DTO object. This time also, since we need to perform the validations, I have mentioned @Valid annotation here, both this ID and the company DTO object, I'm trying to send as an input for this method inside the service layer. So inside this method, we are trying to invoke one of the custom method present inside the CompanyRepository. What is this method? updateCompanyDetails(). . To this method, I'm trying to pass ID value, name, logo, industry, size, rating, locations, founded, description, employee numbers, and website details. Basically, whatever fields that an admin can update, all those fields we're trying to send as an input to this method. So let me go to this method. On top of these method, I have mentioned @Modifying annotation because by using this method, I'm going to execute some update query. To this @Modifying annotation, I'm also providing these configurations like clear automatically and flush automatically with the true value. By using the same method name inside the CompanyEntity class, I have created a @NamedQuery. So here, this is the @Namedquery with the same name, and this is my query that is going to be executed. I'm going to execute this update statement, which is going to take care of updating the company record with the provided data. So in the where condition, I'm using ID value. Here, I'm going with a very simple logic. Since I don't want to write any complex logic to understand which field is updated by the admin, instead, I'm trying to update all the fields without worrying about which field has latest or new value. So this query is going to be executed whenever this method is getting executed. So on top of this method, I have mentioned @Modifying annotation. Let me go to the service layer. Since this method is going to update the data, we need to mention the @Transactional annotataion here. . Once the records are updated, this method is going to return what are the number of records that are updated based upon given data. If the number of updated records are greater than zero, we're going to return true value from this service method. And based upon the Boolean value, we're trying to return either ok or bad request as part of the response. Coming to the last REST API, this is going to help the admin user to delete a given company record completely from the job portal system. So this is the path and this API, it is going to use @DeleteMapping annotation. For this REST API, only company ID is required as an input. So based upon this company ID value, I'm going to invoke this service method, and from this service method, I'm going to invoke the deleteById() method available in the CrudRepository. To this method, I need to provide the primary key ID value. Since this is a data modification operation, we need to mention @Transactional annotation on top of this method. So once that deletion is completed, we're simply returning ok with the message as company record deleted successfully. In total, I have developed four different REST APIs that are going to support all kinds of operations like get, post, put, and delete. By the time you are coming to the next lecture, please make sure that you have developed all these REST APIs with your own hands. So here I have not introduced any new concepts. Take this as an assignment and try to build these REST APIs. While building these REST APIs, if you are facing any challenges, you can always refer the code that I'm sharing inside the GitHub repo, but please make sure you are using the same REST API path, same field names inside the DTO classes and in all other places, because unless you maintain very similar code like me, the UI application, it is not going to work. Apart from defining all this business logic, you should also take care of Spring security changes as well. . Under the PathsConfig, for all the new REST API paths that you're going to build, please make sure to mention under these admin paths so that they can be invoked only by the admin user. Inside the next lecture, let's try to test all these changes by using both the UI application and the Postman application.