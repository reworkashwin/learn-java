The next enhancement that we have under the admin login is, around the Employer Management. The purpose of this feature is, whenever a employer want to onboard into the job portal application, first what they're going to do is, they will try to register as a normal user. We know by default, if someone is registering inside this application, we're going to give them job seeker role. But to post the jobs against to a given company, they need an elevated role, which is employer. That's why what an employer will do is, he or she will try to register as a normal user followed by they're going to work with the admin to elevate their role from the normal job seeker to employer. From this employer management screen, the admin can search an end user email address. And based upon the results, the admin is going to elevate the user from normal job seeker to employer, followed by the admin also can assign that employer to a given company that is registered inside the job portal application, so that when the employer logged in again into the job portal application, they should be able to post the job only against to the company which they are tagged. To support all these admin related features around the employer management, we need to build few REST APIs on the backend side. This time also, I'm going to explain you the changes that I have done behind the scenes by taking those code changes as a reference, please try to build these REST APIs on your own. This is the next assignment that you have as part of the section. This time you need to go to the a class with the name UserController. This is a new controller that I have created behind the scenes. On top of these controller, I have mentioned these @RestController annotation, @RequestMapping annotation. . The prefix that I'm using is users. So first, we have a REST API that supports @GetMapping with the path search admin. By invoking this REST API, an admin user can search an user by providing the email as an input. With the input that is being accepted here as a @RequestParam, I'm trying to invoke the searchUserByEmail() method available in the UserServiceImpl class. So this class implements IUserService interface. These are all the new classes and interfaces that I have created for the employer management related functionality. Inside the UserServiceImpl method, I'm trying to search the users table by invoking a derived query method with the name findJobPortalUserByEmail. If I open this, this is a normal derived query method. Since we are trying to fetch the data from a given single table, the derived query method should be fine. Behind the scenes, when we are invoking this method, the framework, it is going to query the users table based upon the email that we are providing. And if there is an user identified, it is going to return as in the form of UserDto. So the copying of the data from the entity object to the DTO, we are doing inside these private method with the name mapToUserDto. Once the DTO is ready, we are trying to return the same from the service method. So on top of this method, I'm not mentioning the Transactional annotation again, because whatever we have mentioned on top of the class, that should be enough. Now from the controller method, you're going to simply return this UserDto object as a response. So in case if this optional object is empty, we are going to throw this NOT_FOUND Exception with this message. Otherwise we will return the whatever object that we have under this optional by invoking these get() method. Once the users are displayed on this page, based upon the email address provided by the admin, the admin first, he has to elevate the user from a normal job seeker to the employer. So for that, I have another REST API with the name @PatchMapping. Here I have used  @PatchMapping because we are going to update a single column role ID under the users table. So this is the REST API path. As part of the path, I'm trying to accept a path variable, which is going to provide me what is the user primary key ID value. So based upon this user ID value, I'm going to invoke these elevate to employer method inside the service layer. So first I'm trying to fetch the user ID details by invoking these findById() present in the Crud Repository. . So if there is an user identified, we will continue with the logic, otherwise we're going to throw an exception saying that User not found with a given ID. So for the given user, I'm checking if the user is already having employer role inside the application constants, I have created both these constants with the name ROLE_ADMIN and role jobseeker. If the user is already assigned to the employer role, I'm not going to do any update operation. I'm going to simply return these user details to the client application in the form of DTO object. So under this mapToUserDto, we're going to transfer the data from user to DTO object. In case if the user is not an employer, then the next if condition is going to be executed. Here, we're checking if the user is having a admin role. In such scenario, we are going to throw a runtime exception saying that we cannot elevate admin user to an employer role, because that is against our business requirements. If both these if conditions are not satisfied, then towards the end, we're trying to fetch the role entity record based upon the role name ROLE_EMPLOYER. So here also I can use the application constant. Let me use the same and once the role employer record is fetched from the database, I'm trying to set the same to the user entity object by invoking the setRole() method. This is going to override the previous role ID with the new role ID that we are trying to assign. Once the user object is updated, here in an ideal world, we had to invoke the save method by using the userRepository. And to this save method, we need to provide the user object. And with that, the framework is going to take care of updating the record inside the database. But using these line is completely optional because when we are fetching the user record from the database by using the findById at the starting of this method, whatever user object that we loaded, it is a managed entity by the Hibernate framework. Any modifications that you do inside this transaction method, the Hibernate framework, it is going to keep tracking all those changes before the transaction exit from this method. The Hibernate framework, it will try to flush the changes to that database, because as soon as this method logic is completed, the transaction is going to be closed and that's why Hibernate will try to commit any of the changes that you have done on top of these user entity object that is being managed by the Hibernate inside this transaction. That's why before executing these return statement, the Hibernate will take care of committing the transactions. . Invoking the save method against the userRepository with the updatedUser entity object is completely optional. So from this method, we are trying to return the user DTO object, which we are going to accept inside the UserController. And from this controller method, we are going to return the updated user details to the client application. Once the user is elevated to the employer role, as a next step, the admin has to tag that employer to a given company. To support that operation, I have developed another REST API by using @PatchMapping and this is the path. Using this path, I'm accepting both user ID and the company ID values as the path variables. By taking both this user ID and company ID, I'm trying to invoke the assign company to employer method in the UserServiceImpl. So here first, I'm trying to fetch the user details from the database by using this findById(). In case if the user is not identified, we are going to throw a runtime exception. Next we're trying to execute a if condition. Here we are executing a business validation, which is if the current loaded user role is not role employer, then we're going to throw a runtime exception saying that user must be an employer to be assigned to a company. In case if this if condition is not executed, then based upon this company ID, we're going to load the company record again by using this company findby ID method against to the Company Repository. In case if there is no company identified, we're going to throw this runtime exception Otherwise, we're going to simply return a company entity object, whatever company object that we have loaded, we are going to set into the user object by invoking the setCompany() method. And here also invoking the save method by using the userRepository is completely optional. By the time this return statement is being executed, the Hibernate framework is going to take care of committing the updated entity data into the database table. So whatever DTO object that we are returning from this method will be returned to these controller method. From this method, we are returning to the client application. Please take some time and try to understand all these test APIs followed by you can try to develop all this code by yourself. Once you feel you are done with your code changes, please come to the next lecture where we are going to do the testing of this test APIs by using both the UI application and the Postman. I forgot to highlight one more change that we have to do. The changes inside the PathsConfig for all the new REST APIs that we have developed so far, for all of them, you need to mention under these admin paths method, because these REST APIs are supposed to be invoked only for the users who has admin role. Please make sure you are also doing these Spring Security code changes.