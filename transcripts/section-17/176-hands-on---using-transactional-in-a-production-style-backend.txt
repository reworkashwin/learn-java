Let's try opening any of the repositories that we have. For example, there is a repository with the name CompanyRepository. And all our repository interfaces, we know they extend JPARepository interface. Let me open this and what I'll do is, I look for the implementation classes for this interface. If I click on this icon, we should be able to find one class with the name SimpleJpaRepository. All the magic that the Spring Data JPA framework is doing is going to present inside this class. For example, if I invoke findAll() method which is going to return list of entity object, this is the code that it is going to execute behind the scenes. But the important point that I want you to aware here is, on top of this class, if you see, we have annotation @Transactional with the configuration as readOnly as true. That means by default, when a method inside this class is executed, a new transaction is going to be created by the framework with the readOnly configuration as true. And that's why if you invoke any of the find methods like findAll() or findById() method, they don't have any @Transactional annotation again on top of this method, because they are going to inherit whatever transaction configurations that we have defined on top of the class. But if you take a method, which is going to update the record or delete the record like save method, the framework team, they again mentioned that @Transactional annotation. Since the same annotation is mentioned again here, whatever readOnly configurations that are defined on top of the class, they will be overrided and whatever operation that we are going to execute inside this method is going to be considered as a non-read operation or not a read operation. The same kind of behavior you can see for delete() method as well. All these delete() methods, we know they are going to help us to delete the record from the database. Here also, they have overrided the readOnly configuration by mentioning the @Transactional annotation again. . I hope this gives a clarity about all the discussion that we have done in the previous lecture. I said that whenever we are using any of the methods provided by the Spring Data JPA framework, like findAll(), save methods, we don't have to worry about transaction configurations because behind the scenes, the Spring Data JPA framework, it is going to take care of the same for us. But what about the custom methods or derived query methods that we are trying to execute? For example, inside this companyRepository, by using all these methods, we are trying to fetch list of company records along with their associated jobs. In these scenarios, even though if you are not mentioning @Transactional annotation, still your code is going to work because you are only performing the read operation here. Nowhere you are updating or deleting the data by using your custom methods, but it is strongly recommend to always use a @Transactional annotation even for the read operation methods as well. So what I'll do is, I'll go to the CompanyServiceImpl and here on top of this class, I'm going to mention @Transactional annotation. So please make sure to import this annotation from the org.springframework.transaction.annotation To this, I need to provide the readOnly configuration as true. So with this, what is going to happen? By default, all the public methods that I have inside this class, they are going to derive these transactional configurations. For private methods, the transaction configurations are not going to be considered. I'll explain the reason in the coming lectures. Once you define this annotation, you need to revisit all your methods which are going to perform the database operation. For example, this getAllCompanies, we know it is going to invoke these methods inside the repository, which is going to simply perform a read operation. We're not doing anywhere update operation. That's why I'm fine that these methods are inheriting these configurations. Due to that reason, I'm not going to mention the @Transactional annotation again on top of this method. With these configurations, what the framework is going to do is, as soon as someone invokes the service method from the controller layer, a transaction is going to be started. And that transaction, it is going to have the readOnly configuration. In case if there is an exception occurred inside the repository layer or inside these service method logic, then that transaction is going to be rollbacked by the framework automatically. And for readOnly operations, typically there is no need of rollbacking the transaction because these methods, they didn't modify any of the data. Let's see if there are any public methods which are performing database related operations. There are no such methods. So I'm good with this configuration. With these configurations, I don't have to define the @Transactional annotation again on top of these repository interface, because with the configurations that we have defined inside the service layer, a transaction is already started. And we know these methods are going to be invoked from the service methods. But what if there is a scenario where we are invoking one of the methods provided by the Spring Data JPA For example, here I can invoke the findAll() method from the JpaRepository. With this, what is going to happen? We have defined our own transactional configurations here again, inside the SimpleJpaRepository. Here also there are some configurations like transactional with readOnly as true. With this kind of setup as well, there should not be any problem because when this findAll() method is being executed by this SimpleJpaRepository, it is going to reuse the existing transaction that is created by the framework. When these getAllCompanies() method is invoked by the controller layer inside the same transaction, this findAll() method is also going to be executed. I hope you're clear. Let me roll back to the previous method. In the similar lines what I'll do is, I will look for other ServiceImpl classes. So I just have to type Impl and there is an ContactServiceImpl here What I have to do? I have to use the same annotation, which is @Transactional with readOnly as true followed by I need to look for the methods where we are trying to perform the DB operations. So the save contact method, we know it is going to create the new data. That's why I need to make sure to mention the @Transactional annotation without readOnly as true. Let me look for other public methods. So this fetch new contact messages, it is going to only read the data. That's why we should be fine with the default readOnly configurations here this method also, it is going to do the read operation and the last one also, it is going to perform the read operation, coming to the close contact message this method, it is going to update the existing record. That's why here I need to make sure I'm mentioning @Transactional annotation followed by I need to search if there are any other methods. There are no methods. I'm good. Let me go to any other Impl class that I have. So I have AuditAwareImpl, but this is not a typical service implementation class there are no DB operations that are happening inside this class. I'm assuming we are good with the changes that we have done so far. Let me do the build and restart the application because we have made a good amount of configurations. From the JobPortal, what I'll do is, I'll try to first log in as an admin user, followed by I'm going to click on these contact messages, which provided me all the contact details. If I click on this close button, it is going to update that record. The update operation is also working fine. If I go to the homepage here also, I'm able to see 998 jobs along with the company details. This gives a confidence to us that our application is still working fine after making that transactional changes. So this is a strategy that you need to always follow inside your service layer please always start with the transactional as readOnly as true, followed by if you are developing a new method, which is going to perform updation of the data, then you need to override the readOnly configuration by again, mentioning that @Transactional annotation. I know you have a million dollar question on what are the advantages that we are getting by mentioning these readOnly as true. Let's try to answer the same in the next lecture.