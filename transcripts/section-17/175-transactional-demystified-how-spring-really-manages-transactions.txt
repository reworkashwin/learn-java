When an admin user logged in into our JobPortal application, under this menu, there is an option with the name contact messages, which will take him to this page. Inside this page, the admin has an option to close an existing new message. We already saw the demo of this in the previous section. But the reason that I'm trying to show you one more time is, whatever code that we have written to handle this updating of the message to close is not up to the best standard. For example, as soon as the admin click on this close button, this REST API inside the ContactController is going to be invoked. From this controller, we are invoking this method inside the ContactServiceImpl. So here what we are doing, based upon the ID value of the record, first we are trying to fetch the record, followed by we are updating the status by invoking this contact.setStatus() and towards the end, we are providing this updated contact entity object to the save method. And this save method is going to take care of updating the record with the latest status value. But there is a problem with this approach. The problem is, first we are trying to execute the SELECT query followed by here at this point of time, an UPDATE query is going to be executed to just update a column value we are executing unnecessarily two SQL queries. The ideal way or the best approach is, to update the status column by using single UPDATE SQL statement. That's why as a next step, we are going to focus on resolving this issue inside this method. But before that, we need to understand few of the new concepts inside the Spring Data JPA framework. As of now, whenever we are interacting with the database from our Java code, we didn't consider one of the important aspect, which is transactions. Let's forget about our job portal for a few seconds. Imagine we are trying to transfer money from a given saving accounts to a checking account in a bank. And we're trying to achieve these by using a Java code. So whenever we are performing this action, we don't want half the process to happen, like deducting the amount from the saving account, but forgetting to add to checking account, which is going to create some confusion to the end user. So that's where transactions come into picture. They are like all or nothing promise. If we are doing this transaction, when we are performing the transferring of the money, it should be either both these operations are successful or there should not be any changes to both these accounts. So inside database also, a transaction ensures a group of operations like read and writes, either all succeed or all fail together, which is going to keep our data consistent and reliable. This follows ACID principles. ACID principles are the famous database concepts that each and every backend developer should be aware. So what is this ACID? ACID is a shortcut of Atomicity, Consistency, Isolation, and Durability. Let's discuss about them one by one. Atomicity means all or nothing, either you should be executing all the operations or you should not be executing any of the operation. Coming to the consistency, it says rules has to be followed when we are updating the data inside the database. For example, if you take an age column under this age column, whenever we are trying to update some data, it should not have any negative value. Like I should not update the age value to -5 or  -10, which is not valid. Similarly, databases they are going to follow many rules by using unique columns, foreign key columns. All these rules comes under these consistency, because with the help of these rules, the data is going to be maintained in a well consistent manner inside the database. Next we have Isolation. This Isolation recommends that the transaction should not mess with each other. For example inside a application, if two transactions are running parallelly, these two transactions should not interfere with each other. They should be executed in an isolated manner. So whatever partial updates are done by these transaction two should not be reflected to these transaction one. Next we have durability. Durability says, once you commit the transaction into the database, whatever data that got updated, it should stay forever unless someone is trying to delete or change the data. Inside our backend application, we are using Spring Data JPA, which is helping us to handle that database interactions via Java persistence API. The same framework, it also provides an annotation with the name @Transactional. This is going to be our magic wand for managing that database transactions with minimal configurations. No need to write any boilerplate code. For example, this is a sample code snippet that developers they used to write before Spring and Spring Data JPA. So if you closely try to understand that code that has been written here, first the developer has to fetch a connection based upon the data source object. By using this connection object, the developer has to invoke the setAutoCommit() method to this false value has to be provided, with this what we are telling to the database is, don't commit any of the data that I'm providing unless I'm invoking the commit manually. After disabling the auto commit, we can execute any number of SQL statements. Once we are done executing all the SQL statements towards the end, we need to invoke the commit() method. Unless the transaction is committed by using this commit() method, none of the database changes that we have executed by using the SQL statements will not be committed to the database. Let's imagine there is an exception occurred when we are executing the SQL statements. There might be some three SQL statements like 1,2,3. So this is going to insert some record, and this is going to update some column, and this is going to delete some record. So let's imagine there is an exception occurred while deleting the record. So at this point of time, what should happen? Since the third operation failed, the first two operations also should be rolled back. That's why inside the catch block, by using the same connection object, the developer has to invoke the rollback() method so that all the previously executed SQL statements will be rolled back and those changes will not be applied into the database tables. By looking at this code, it is very clear to us that developers, they used to write a lot of boilerplate code, but if we use @Transactional annotation, it is going to make our lives super easy. By the end of this section, you're going to become a master on how to use these @Transactional annotation. In order to use these @Transactional annotation first, we need to understand how to enable this annotation. . So there are multiple approaches based upon the type of the backend application that you're trying to develop. If you are developing a backend application by using only spring, but not the springboot, then you need to make sure to use this annotation @Enable transaction management inside a configuration class. . Whereas with springboot, you don't have to use these annotation as well. You can directly start using the @Transactional annotation, because inside the springboot whenever we want to use a spring data JPA, we are going to add that specific starter dependency inside our pom.xml. And whenever that dependency is present inside our classpath, the springboot auto-configuration, it is going to auto-enable the TransactionManagement by using this annotation behind the scenes. Everything is going to be ready for the developers to use this annotation. Now coming to the question of where to use this annotation? We can put this annotation on top of a class, method or interface based upon our business requirements. If we are mentioning this annotation on top of a class, it is going to be applicable for all the public methods in that class. So typically we're going to mention this annotation on top of the service layer classes. We also have a option of mentioning this annotation on top of a method as well. When we're using this option, whatever class level transaction settings that we are providing by mentioning this annotation on top of the class, those will be overrided. You need to mention the annotation on top of the method only if you are looking for a fine control for a given method. We can also mention on top of the interfaces. Typically these interfaces refer to the repository interfaces that we have inside of our backend application. We know inside repository interfaces, we are going to define many of the abstract methods by using derived query methods and @Query annotation for all of them if we want to feed some transactional related configurations, you can simply use this annotation on top of that repository interface. My recommendation is always to use this annotation inside service layer methods or on top of the service layer classes. Don't try to mention this annotation directly on top of the repository interfaces or repository methods unless you are overriding some defaults provided by the Spring Data JPA framework. Here you may have a question? which is, Madan so far we have written a lot of database related logic by using the methods from the Spring Data JPA and the derived query methods. And nowhere we have used this annotation and our backend application is also working without any issues. Then why should I worry about these @Transactional annotation? This can be your question? Let's try to understand the same. Here comes the tricky part. When we are using the Spring Data JPA provided methods like save, findAll and so on, the transactional related functionality will be provided by the framework itself out of the box. For example, when you are invoking method like findAll, which is going to read the data behind the scenes, the framework is going to use this @Transactional annotation with the configuration as read only as true. When someone is using these configurations, it is an indication to the framework that right now read operation is going to be executed and mentioning these read only as true during the read operation is going to bring some performance improvements. We're going to talk about these configuration in the coming lectures. This is the story around the read methods like findAll. Now coming to the scenario where we might have invoked the save method to update or insert a new record and such scenarios, the framework it is going to use plain @Transactional annotation. I hope you're clear so far, in simple words, we can say whenever a developer is invoking the methods exposed by CrudRepository or JPA repository from the framework, then the developer don't have to worry about mentioning these @Transactional annotations, because behind the scenes, the framework is going to provide these transactional features out of the box. But what about the scenario where a developer has written a derived query method or he's trying to execute a custom query by using @Query annotation. In this scenario also, so far we are executing a lot of business logic by using @Query annotation and derived query methods, but nowhere we have not used to @Transactional annotation, but still our code is working. Our code is working because so far we executed only the read only operations by using the derived query methods or by using @Query annotation. If a read operation is going on, then mentioning that @Transactional annotation is completely optional, because you're not going to do any damage to the data even if a runtime exception happening. Mentioning of the @Transactional annotation is mandatory when you're trying to update or delete the record by using your derived query methods or by using @Query annotation. On top of that @Transactional annotation, you should also take care of mentioning @Modifying annotation as well to convey to the framework that you are trying to modify some data by using your own custom methods. We are going to see a lot of demos around these custom queries on how to use these @Transactional annotation and @Modifying annotation in the coming lectures. Developers they also have an option of overriding the framework provided transactional properties. I said before, whenever a developer is invoking the save method or findAll method from the CrudRepository, they don't have to provide that @Transactional annotation. But what if there is a scenario, the developer want to overridee the default transactional behavior under such scenarios, what the developer has to do is, inside the repository interface, the developer can override the same method like save on top of that method the developer has to mention @Transactional annotation along with the custom configurations. For example, to this @Transactional annotation, we can provide a timeout configuration. Let's imagine the framework is timing out the query after three seconds. If a developer want to update this to five seconds or 10 seconds, what he has to do, the developer has to simply define the same abstract method inside the repository interface on top of that method. He can use the @Transactional annotation along with this timeout and to this timeout property, he can provide value like five seconds, which is going to overwrite that default behavior provided by the Spring Data JPA framework. We're going to see a lot of demos, don't worry if something is not clear. Overall, I would say always try to use these @Transactional annotation inside your service layer. We're going to discuss about the reasons on why we should use these inside the service layer compared to the repository layer in the coming lectures while we are seeing the demos. In case if your service method is only reading the data, but not updating the data under such scenarios, mentioning the readOnly as true is strongly recommended for performance reasons. Inside the next lecture, let's try to understand more details around this readOnly configuration.