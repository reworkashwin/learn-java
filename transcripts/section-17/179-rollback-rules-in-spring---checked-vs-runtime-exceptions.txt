The whole purpose of using @Transactional annotation is, to roll back the transaction when an exception is arising inside our Java code. For example, inside this closeContactMessage method, I may have multiple DB update operations like under this very first operation, I may update some status followed by I may have another transaction which is going to insert some record in another table and there might be another DB transaction to delete a record. Usually, the business requirement will be either all these three operations should be successful or nothing should happen. That is the whole reason on why we are using the @Transactional annotation. If there is a scenario where the first two operations are completed but there is an exception occurred at the third operation, then the framework, it is going to roll back the changes that we have done under this step one and step two. Unless all these three steps are completed successfully, the framework is not going to actually commit the data into the database. As long as your service methods are annotated with this @Transactional annotation, you should be confident around the rollback functionality. You don't have to manually rollback the transactions, but by default, the rollbacking of the transactions will happen only for the runtime exceptions. Using this @Transactional annotation, we should be able to invoke some rollback related properties. So let me open these rollback for and let's read the documentation on top of this method. Here they clearly highlighted by default, a transaction will be rolled back on runtime exception and error, but not on checked exceptions like business exceptions. The reason why this behavior is built inside the framework is, usually inside the real applications checked exceptions or business exceptions should not result any rollback of the data. Unless there is a runtime exception happen like null pointer exception, the transaction should not be rolled back. The same is adopted inside the framework as well. But in case if you want to roll back for all kinds of exceptions, including business exceptions as well, then you need to mention rollback first with the value Exception.class. We know inside the Java, every Exception, including runtime and checked exception, they are going to be jailed elements of this Exception class. But please be very cautious when you are using this Exception.class against to this rollback for, because even in the scenario of any business exception happening inside your code, still the transaction is going to be rollback. Let me show you some demos around this. First, what I'll do is, I will remove these rollback for configurations. And just after invoking these repository method, I'm going to throw an runtime exception. One such runtime exception is null pointer exception. So let me create a new object of null pointer exception and to the constructor, I'll pass a message saying that it is a bad day. And towards the end, I need to comment this return statement. Let me save these changes, do the build. Since null pointer exception is a child of runtime exception, the transaction should get rollback. . So whatever updates that we have done on top of a contact message record based upon this ID value, that status value should be rolled back to the previous new status. Before testing my scenario from the UI, I'm going to keep a breakpoint here. Now I will click on this close button, which is going to update the status of the record. The request stopped at my breakpoint. Let's see what is the ID value. The ID value is 7. Let's remember that I'm releasing the breakpoint. Now I'll go to the database and here under the contacts table, I need to look for the ID with the value 7. For this record, if you see, the status value is still NEW. That means whatever updates that we have done to that data by using this logic, it is rollback because there is a runtime exception occurred. This time, let me throw an checked exception, maybe instead of NullPointerException, I'm going to throw an IO exception. So this IO exception is a checked exception. If you open this exception, you'll be able to see this is extending exception class. So since we are throwing a checked exception, the method has to handle this. So what I'll do is, I will add exception to the method signature and select this option of changing all the methods in the hierarchy. So let me save these changes, do the build once a build is completed, during the build operation, I got a compilation error message inside the controller layer. Here, I need to select this add exception to the method signature. Let me do the build and trigger the update operation. This time I'm clicking on this close and confirming the operation. This time also the ID value is 7. Let me release this breakpoint. Now I'll go to the contacts table and refresh this data. So this time, if you see, the status is closed. That means the rollback didn't happen because this time a checked exception occurred inside our code. So this should give a confirmation to you that the rollback is only going to happen for runtime exceptions by default. But you can change that behavior by using rollback for and providing Exception.class so that for all kinds of Exceptions, the rollback is going to happen. In case if you want to provide a few limited set of exception details, you should be able to provide all those details by using calibrase and inside this calibrase, I can mention details like IOException.class followed by SQLException.class. So all the list of exceptions you can mention in case if you want to go with some rejects pattern as well, you should be able to do that instead of invoking the rollback for, you have to invoke rollback for class name. So to this, if you mention something like Exception, then for all the Exception classes, which are having this Exception in their name, those exceptions will be considered for rollback in the transaction. Just like rollback configurations, we also have noRollback configuration as well. For example, by using these noRollbackFor, if I provide SQLException.class, then the rollback operation is not going to trigger when these Exceptions happen. You can play with these configurations based upon your requirements, but most of the times you should be fine with the default behavior provided by the framework. That's why throughout this course, I'm going to simply use @Transactional annotation and will not explicitly configure any rollback related configurations, but I'm trying to provide this information just for your reference so that you can use this rollback configurations in your real applications based upon your business requirements. Let me remove this breakpoint. I'm also going to delete this code and uncomment this code. Let me delete these throws IOException. I need to go to the interface as well. Inside this interface also, under this method signature, I need to delete these throws. Let me go to the controller. Inside the controller also, I need to delete these throws keyword. All the details that we discussed in this lecture, I documented them in the slides as well that I have shared. Please refer them in case of any questions.