To generate the CSRF token from our backend application, we need to invoke the csrf() method by using this http variable. Let me type csrf(). To this CSRF, we need to pass a lambda expression. Let me mention the lambda input variable name as csrfConfig. Previously, we used to have this lambda expression, where we have disabled the CSRF protection of the Spring security framework completely. Now this time, instead of invoking the disable() method, we need to invoke csrfTokenRepository() method. To this csrfTokenRepository() method, we need to pass the output that we are going to get when we invoke the CookiecsrfTokenRepository.withHttpOnlyFalse(). So there is a meaning for this configuration. First, let's try to understand the purpose of csrfTokenRepository. There is an interface with the name csrfTokenRepository. To this interface, there are a couple of implementations. The first one is CookiecsrfTokenRepository followed by HttpSessioncsrfTokenRepository. Since we are looking to generate the CSRF token by using the cookie, we need to use the very first implementation. This interface is a representation or the template on how the CSRF token needs to be generated and where it has to be stored. Inside this interface, we have various methods like generateToken(), loadDeferredToken(), loadToken(), saveToken(). So all these methods, they are going to help us in managing the csrf token. But as I said, this is an interface, we can't use this. Instead we need to use one of the implementations like CookiecsrfTokenRepository. When we are using this implementation class, by default, the framework it is going to generate the csrf token and send it as part of the cookie with the name XSRF-TOKEN. And when we are sending this token value as part of the Request Header, the framework is going to expect under the Request Header with the name X-XSRF-TOKEN. So all these standards are defined inside this repository, the same standards we are going to follow. Now let's try to understand the purpose of the method withHTTPOnlyFalse(). Whenever a cookie is generated, we have an option to set this HTTP only flag to either false or true. When this value is set to false, then we are giving a flexibility to the client applications to read the cookie value from their JavaScript code. Otherwise, with a true value under this HTTP only, the client applications or the JavaScript code, they can't read the cookie value, even though the JavaScript code is running under the same domain of the cookie. This is a very important configuration, especially when you are integrating your backend code with the SPA applications, like single page applications that are developed by using Angular, React and so on. On top of this class, we have some documentation as well, a csrfTokenRepository implementation that parses the CSRF token in a cookie named XSRF-TOKEN and reads from the header X-XSRF-TOKEN following the conventions of AngularJS. When using with AngularJS, be sure to use withHTTPOnlyFalse(). So just like Angular, we are also using React on the client side. That's why we need to use this configuration. Inside this class, you will be able to see various methods like generateToken(), which is going to take care of generating a token whenever the client application is asking. We have all the logic inside this class. If you're interested, please explore the same. So inside the Spring Security, a CSRF token is represented by this DefaultcsrfToken. This is a class which is implemented by a csrfToken interface. Inside this interface, we have these various methods. All these methods are implemented under these DefaultcsrfToken. I hope you are clear about the first set of settings that we have done inside the JobPortalSecurityConfig. So with these changes, what we are telling to the Spring Security is, please generate the csrfToken by using the cookie style. And when you are generating a cookie, please make sure it is having HTTP only as false. As a next last setting, we should also invoke another method with the name csrfTokenRequestHandler. Spring security never going to generate the CSRF token by itself for each and every request. The client application should ask the spring security framework to generate a CSRF token. Once the CSRF token is generated, it is going to expose the CSRF token value as part of the request attribute. So that's why to this csrfTokenRequestHandler(), we need to pass the object of csrfTokenRequestAttributeHandler class. With this, what we're telling to the Spring Security framework is, when you generate the csrfToken, please expose that token as a RequestAttribute. So if you open this class, here there is a RequestAttribute to define the RequestAttribute name is _csrf. . This is a implementation class of  csrfTokenRequestHandler that is capable of making the csrfToken available as a RequestAttribute and resolving the token value, either a header or a parameter value of the request. I'll come to the point on why we should expose the csrfToken as a Request Attribute in few seconds. So these are the important configurations that you need to be aware when you are implementing the CSRF token solution. First you need to invoke the csrfTokenRepository. By using this method, you're going to tell on how the CSRF token needs to be generated. Is it using cookie or is it using HTTP session? Such information you're going to communicate with this method. By using the second method, which is the csrfTokenRequestHandler(), you need to tell to the Spring Security framework how the token has to be exposed inside the request. So with this class, we are telling to the framework to expose the CSRF token value under the RequestAttribute with the name _csrf. There is also another important method with the name ignoringRequestMatches(). To this method, you can pass any of the REST API path, which is having non-safe HTTP methods like POST, PUT, DELETE, and so on. For example here, if I mention my register-related API or login-related REST API, then for such APIs the Spring security framework, it is not going to provide any CSRF protection. It is an indication to the Spring Security framework, please don't worry about providing CSRF protection for this URL, because I understand the risk and I'm fine you not handling that. That is what we are communicating by using these ignoreRequestMatches(). For now, I'm not going to use these because we don't have any such requirements. For all non-safe methods, we want to use a proper CSRF solution. So these are the configurations that you need to do inside the JobPortalSecurityConfig. Next, you need to invoke a REST API that can be invoked by the client applications to request the Spring Security framework to generate a CSRF token. What I'll do is, under the security package, I'll try to create a new Java class with the name CsrfController. Since this is related to security, I'm trying to create under the security package. On top of this class, I need to mention @RESTController annotation, followed by, I'm going to mention @RequestMapping with the path as csrf-token. Inside this, I will try to have a simple GET REST API. This has to be a GET REST API, otherwise it is not going to make sense because the client applications, before they try to invoke any non-safe HTTP methods, they are going to make sure to get a CSRF token from the backend application. Please import the CsrfToken from the package org.Springframework.security.web.csrf. The path this REST API is going to have is public with the version as 1.0. The client application, they're not going to send any request to this REST API, they're going to simply invoke this REST API. But by the time this REST API is invoked, my Spring Boot framework, it is going to inject this Request object into this Java method. So what is that Request object? It is of type HTTP solid request. Using this request, I'm invoking the getAttribute() method. To this getAttribute() method, I'm passing the  csrfToken.class.getName(). When this code is executed, it is an indication to the framework that a client is asking for a CSRF token. At this point of time, the Spring Security Framework, it is going to generate an actual CSRF token and provide as an output to this method. The output will be a csrfToken object. The same we are trying to typecast and send as a response from this REST API. Here you may have a question ? How do you know that we need to pass this attribute to this getAttribute() method? Let me show you that inside the official documentation. If you go to the JobPortalSecurityConfig here, we told to the Spring Security Framework to use the csrfTokenRequestAttributeHandler to expose the CSRF token inside the request. Here they clearly highlighted whenever this handler is being used, the CSRF token is going to be available as a request attribute named csrfToken.class.getName(). So the same we are trying to use inside our CsrfTokenController as well. I know you have too many questions on how this is going to work end to end. Let's continue this discussion in the next lecture. I'm going to show you a demo and with that it is going to make complete sense to you.