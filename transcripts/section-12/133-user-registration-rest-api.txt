Inside AuthController class, I'm going to create a new REST API that supports registration process. Let me copy the first two lines of this method and paste it here just above the buildErrorResponse() method. I need to update the API path to register slash public and the version will be 1.0. The method name I'm going to mention it as registerUser() and to this method, we're going to accept the input by using RegisterRequestDto. Coming to the ResponseEntity, for now I'll try to return a String value as a response under the response body. At a later point of time, I'm going to change it. We're going to receive all the required details inside these RequestDto. I need to copy the data inside the Dto into my entity class. So here what I'll do is, I'll try to create the JobPortalUser object followed by I will invoke BeanUtils.copyProperties() to this method I'm passing the RegisterRequestDto as the input and the destination is JobPortalUser. Since inside both of these classes, we are maintaining the same field names like name, email, and mobileNumber, those fields will be copied, but password is not going to be copied because inside the entity class for the password field, the field name is passwordHash. Except the password, all the details will be available inside this entity object. Now I want to transfer the password as well, but I can't set the same password that I received inside the Dto class because the end user is going to provide the password in a plain text format. But before I try to store the same into the database, I want to generate the hash value. How to do that? It's very easy. We just have to invoke JobPortalUser.setPasswordHash() method. So to this method, we need to provide the hash value. Into this class, we need to inject the PasswordEncoderBean. Let me type private final PasswordEncoder. We already created BCrypt PasswordEncoder Bean inside the JobPortalSecurityConfig class. The same I'm trying to inject here. Now using this PasswordEncoder Bean, I just have to invoke the encode() method. To this encode() method, I need to pass the plain text value. Plain password text value will be available under the RegisterRequestDto.Password. Let's try to open this encode() method. So this method, it is going to accept the raw password and return a hash represented value. The same we are trying to set into the JobPortalEntity object. As a next step, I can try to save this record into that database but before that, I need to inject that required repository beans as well. So here, let me type private final JobPortalUserRepository. In the similar lines, I'm also going to inject roleRepository as well. By using both these beans, I'll try to save that data into the database. I'll go to the Java method. So here, what I can do is, I can simply invoke JobPortalUserRepository inside this interface. We have a save() method. To this save() method, we can pass the entity object. The entity object is JobPortalUser. And towards the end, I can return the response saying that, user registered successfully with the HTTP status as 201. But do you think this code is going to work? Let's try to test this and see the exceptions so that we can try to resolve it. Let me save these changes and do the build. Before I try to invoke this REST API, I should also update this API path under the PathsConfig file here. Just like how we have mentioned the login path as a public path, very similarly, I need to mention for register as well. From the postman, I'm trying to invoke my REST API. And to this REST API, I'm trying to pass the name, email, mobileNumber, and password. I need to use the HTTP POST. If I click on the send button, I'm not going to get any successful response, but it seems I'm not getting any kind of response. Let me check the console logs. Inside the console, I see that my application is failing to start because to the AuthController, we have injected a bean with the name AuthenticationManager, while creating the bean of AuthenticationManager, the framework is throwing an error because it is expecting an authenticationProvider as an input. Maybe what I'll do is, I'll go to the JobPortalSecurityConfig, and here I'll try to bring back all the previous changes that we have removed. I'll try to keep these changes until we completed the registration process. So I need to save these changes, do the build once the build is completed, this time, if I invoke the same REST API, I am getting a 500 error. So this error response we are getting from the global exception handling logic. Here, it is clearly highlighting for the role column inside the users table, we can't assign a null value because the roles column is a not null column. So to fix this problem, whenever we are trying to save a new user record, we should also attach a role record as well. I should be able to do that very easily. I just have to invoke roleRepository interface and using this, I can invoke findById. So to this findById, I will pass 1L. If there is a record, I want to attach that record to the job portal user by invoking the setRole() method. If you go and check the roles table, under the primaryKeyId value 1, I have a role record with the name ROLE_JOB_SEEKER. So by default for every user that is getting created by using the client application, I want to assign this role. That's why I am trying to fetch the role object by using this primaryKeyId. The framework, it is going to provide me a method with the name findById. To this, I need to provide the primaryKeyValue 1L. Since ID is a long data type, I need to mention 1L here. So the return type from this method will be a role object. The same I'm trying to assign to the JobPortalUser. With this, by the time a new record is being inserted into the users table, it is going to have the primaryKeyId value of the role job seeker record. Off course, this line of code is not a recommended production standard because we're trying to fetch a record by using the primary key number. These primary key numbers, they keep changing for record to record. Instead of finding the record by using the primary key number, we should try to fetch the record by using the role name. But to perform that action, we need to learn a new concept under the JPA, which I'm going to discuss in the coming lectures. For now, let's try to use the findById method provided by the JPA framework. So with these changes, now I should not have any problem. Let me do the build and invoke the same REST API. So this time I got a successful response saying that user registered successfully. Let me go and check the users table. There should be a new record. Yes, there is a record. So this is the name of the person, email of the person, and this is the hash value of the password and this is the mobile number. Coming to the role_id, we have the value as 1. So that means for this end user, the role of job seeker is assigned. I hope you are clear with these changes, but this logic has to be improved. Right now, we're trying to directly insert whatever data that we are receiving. But before we try to save a brand new user, we should first check if there is an existing user with the same email or the mobile number. If there is no user, then only we should allow the registration, otherwise we should throw an error. So that is one optimization we need to take care. The other one is right now we are fetching the role record by using the primary key value, which is not recommended because these primary key numbers, they keep changing from record to record. As of now, I have written all the logic inside the controller layer itself. I have not built any service layer for the authentication logic, because usually the authentication logic is going to have very limited functionality. That's why I didn't build any separate service layer. But in case inside your real enterprise application, if you have some humongous logic around the authentication, then feel free to move all this to a service layer. I hope you are clear with whatever we have done so far. As part of the next lecture, I'm going to introduce one of the beautiful concepts of Spring Data JPA.