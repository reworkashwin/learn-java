With the setup of CSRF token solution that we have so far, the flow should be always the client application first, it is going to ask backend server to generate the CSRF token. Usually most of the client applications, they're going to ask the backend server whenever the homepage is getting loaded. That is one approach. The other approach is the client applications they can ask the backend server to generate a CSRF token before they try to make the very first non-safe method invocation. So that is another approach, either of them is valid. Once the CSRF token is generated, the same will be shared to the UI application in two forms. The first one is as part of the cookie and other one is as part of the Response body of this REST API invocation. So let me type Response body. We know the browser is going to take care of sending the CSRF token under the cookie automatically because the domain names, they're going to match. But coming to the Request Header, it is the responsibility of the client application to read the CSRF token value, either from this cookie or from the Response body and for the every subsequent request that it is going to make to the backend applications, especially regarding the non-safe methods execution, the token value should be present under the Request Header as well. Let's see a demo as well around this. But before that, we need to make a small change inside the PathsConfig to invoke these REST API we need to make sure that these REST API is marked as public. Let me go to PathsConfig and here I need to mention slash api/csrf-token/public. Here you may have a question ? Madan, if the hacker also is able to invoke this REST API, he will also get his own cookie, right? That's correct. So the cookie that he gets will be useful if he is making a request by using the JavaScript code deployed under the same domain. That's why for him, it is useless. Let me save these changes, do the build. From the postman, I will try to invoke this CSRF token REST API it is present under the security folder. So this REST API supports get() method. If I click on the send, you'll see that I received the CSRF token value. So this is the token value. And I need to send this token value again under the header name X-XSRF-TOKEN. So this header name is useful for the SPA applications. In case if you are using timeleaf or JSPs or any server rendering templates for them, you need to send the CSRF token value under the request parameter name with the value_csrf. . So now I have the CSRF token value. A cookie also might have generated. If you click on this cookies, you will see there is a cookie generated with the name XSRF-Token. With this setting, I'll go to the Register and here this time, if I try to send this REST API, let's see what is going to happen. So what I'll do is, I will just mention the name as Test user coming to the email I'll just mention test@gmail.com. I'll mention some random mobile number, and this is the password. So when I'm invoking this REST API, the cookie will be automatically attached. So under the cookies, you will be able to see the XSRF-TOKEN is present. So this cookie will be automatically attached to the request because I'm sending this request to the same local host domain. If I forget to send the token value under the Request Header, the backend is not going to accept. I can show you a proof as well. If I try to send this request right now, I'm still getting the 401. But this time, let's try to send the csrfToken under the header as well. So I'll go to the headers and here I will mention the header with the name X-XSRF-TOKEN. Under this header, I need to mention the value that I have received. So let me copy this and paste it here. If I click on this send, this time hooray, the user is registered successfully. From now onwards, we should be able to trigger any non-safe HTTP methods as well. We just have to make sure we are attaching a valid csrfToken. Let's validate the login as well. We know login also is using POST method. Here I will just mention test@gmail.com. Under the headers as well, I'll make sure I'm passing a valid XSRF-TOKEN. If I click on this send button, you'll be able to see I got a successful response. And if one of the characters is tampered inside the token, for example, if I remove this one and if I try to send this to the backend, I'm going to get the 401. I hope you are seeing the beauty of csrf protection. Now let's try to test both registration and login end-to-end by using the UI application. Please make sure you started your UI application by using the code that I have provided under the section 10. From the browser, you can try accessing the homepage, and as you can see, we are able to see all jobs and company's details. First, we can try to test the existing demo accounts. You can click on this show demo credentials, and first I'll try to login with the Job Seeker. So this is the email and this is the password. As soon as I click on this sign in, the sign in is completed. That's why I'm able to see the name of the user here. The name is John Doe. And if I open this menu, the role is job seeker and this is his email, but the job seeker, we are going to have various options like profile, applied jobs, saved jobs and sign out. For now, all this functionality, they are not going to work. We are going to enhance our backend application in the coming sections. Let me log out. I'll go again to login and this time I'll try to log in as an employer, with the employer the name will be Sanjana and this is the email and the role is employer. For the employer under the menu, we are going to give flexibility to post a new job and to check the existing job postings. Let me click on the sign out. This time I'll try to log in by using the admin for the admin. the name is Admin and this is the email. The role is Admin under the admin menu we are going to have company management, employer management, contact messages and sign out. Now let's try to click on the sign up, which is going to show these registration page here I will try to give my name like Madan Reddy followed by email tutor@eazybytes.com. I'll type some random mobile number coming to the password I'll give the same password Eazybytes@1803. Let me type the same. As soon as I click on create account, I got a successful message saying that user registered successfully. So from this login page, I can try to log in. So what is the email? tutor@eazybytes.com and the password is Eazybytes@1803. As soon as I click on sign in, I'm going to get my name, email, role, along with the job seeker menu. I hope you're happy with this end to end demo. Before I try to close this lecture, I want to ask one of the questions that you may have. In case if you also have some knowledge around the react code or on the UI code, you might be interested from where we are invoking the CSRF token related REST API. Let me show you the same. There is a file with the name httpClient.js. Inside this file, I have a logic to intercept each and every request that is going to the backend server. Inside this interceptor logic, I'm checking if the REST API invocation is related to the safe methods. These are the HTTP safe methods. In case if the request API is related to the non-safe HTTP methods, then I'm checking if there is a cookie already available inside my browser with this name. Under this cookie, if I have a value, I'm going to read that cookie value by using this cookies.get and mention the same under the request headers here. In case if the CSRF token is not available under the cookies, inside this try block, I'm making a request to the csrf-token/public. This is the place where the actual magic is going to happen. When the client application is sending a request to the non-safe methods for the very first time, at that point of time, this block is going to be executed. Otherwise, I'm going to simply attach the CSRF token value into the header. The technical details around our CSRF attack solution, I have documented them in these slides for your reference. So we have made these changes inside the backend application, followed by we also built a Rest API method, which can be invoked by the client application to read the CSRF token. These are the important classes and interfaces inside the Spring security framework that are responsible for the CSRF attack protection. I forgot to highlight about a Csrf Filter. This is one of the Spring security filter, which is responsible to validate whether the token is present inside the request. Whenever the request does not have CSRF token, we are getting 401 error. So that is coming due to the logic defined inside the CsrfFilter. So inside this doFilterInternal, all the logic is written in case if the token is not available or if it is an invalid, it is going to simply throw 401 error.