As of now, we store the end-user credentials inside the memory of the application by using this InMemoryUserDetailsManager. But this is not a production standard we should always store the end-user credentials inside a proper storage system like database. You should also give flexibility to every end-user to register their own username and password. That is what we are going to achieve by the end of this section. Whatever security right now we have inside our application, it is fine for low priority applications or for demo applications. Let me show you some funny memes around the security right now that we have inside our application. Just like how this guy is doing the security check for the namesake, very similarly right now inside our application also, we are authenticating the users by checking the memory of the application. This is another funny image which predicts the current security setup inside our application. If you are a dog lover, you may like this meme and this is the last image that I have and this is my favorite as well. One of the funny developer implemented this login page. A end-user entered his email and the password. Instead of showing invalid credentials message, the developer is showing it seems you entered so-and-so user password maybe your email is joe.smith@gmail.com. So with this, what is happening? We are leaking the other user username and password. So these are the funny memes now let's go to the serious discussion in order to store the user details inside a database first, we need to create the required tables, entity classes, and the repository interfaces. Let's try to do the same. I'll open the job portal schema file inside this file as of now, we have SQL scripts regarding three different tables so the first one is around the companies followed by contacts. next, we have jobs in the similar lines. I'm trying to paste a couple of more create scripts using the very first one. I'm trying to create a table with the name roles inside these roles table only I'm going to maintain all the roles information that an end-user can have inside our job portal application. So this table, it is going to have a primary key ID column followed by name of the role after the name, we are going to have audit related columns as well. The only meaningful column inside this table is name of the role but before I try to execute this script, let me share a note. Since we are into the new section, I created a new folder and copy the backend code from the previous section. The same I have opened inside my IntelliJ as well. Once opened, I started my backend application as well. That's why I'm able to see a database connection here. In case if you're not using the ultimate subscription inside your IntelliJ, please try to execute these scripts by using an SQL client application like SQL Electron. Let me execute this script followed by I'm going to execute the next script as well. Using this create statement, I'm trying to create a brand new table with the name users. So the table name can be anything, but since we are going to save end-user details inside this table, I have constructed this name. You will also see some real enterprise applications they are going to use the table name as customers. Regardless of customers or users, all the concepts are going to remain same. Inside this table, we have various columns like primary key ID column followed by name of the customer, email of the customer, the password of the customer, which we are going to store in a hash format. That's why I have mentioned the column name as password underscore hash. Next we have mobile number. After the mobile number, we have the role ID column. So this role ID column, it is going to have a foreign key reference with the roles table. Inside the roles table, we have an ID column to the same this role ID column. It is going to have a foreign key reference. This role ID column is a not null column, which means every end-user who are trying to register into our application should definitely have an role assigned. After the role ID, I have the company ID. So using this company ID column, we can tag a user to a given company. This company is a nullable column. I'll explain you the purpose of this column in the coming sections. Next, we have four audit related columns as well. Let me select this entire script and execute the query and with this, we have created two brand new tables. So the tables are roles and users. As a next step, I'll try to insert some roles data into the roles table. Let me open the file jobportal-data.sql. Inside this file, as of now, we have insert scripts related to the companies table and jobs table. Towards the end of the file, I am pasting three different insert records. By using these insert statements, I'm trying to create three different roles inside my backend application. So this role will be assigned to every end-user who are trying to register into our application. This will be that default role that we are going to assign from our business logic whenever a new user trying to register. Next, we have a ROLE_EMPLOYER as well. Inside the job portal, typically we need some employer to post the jobs like a HR person from the Amazon company or Google company so first they need to register as a normal user followed by they can work with the admin of the website to get an elevated role of ROLE_EMPLOYER. Anyone who has this role, they should be able to publish new jobs inside the website against to a given company. Whereas a normal job seeker can only apply the existing jobs inside the website. And towards the end, I am trying to create another ROLE_ADMIN. Usually the admin is responsible to maintain the entire website. That's why we are going to give some elevated authorities to the admin. Anyone who has a role of admin, they should be able to upgrade any of the job seeker user to the role employer. Similarly, we're going to have many other admin functionalities inside our job portal application. By the end of this course, under the job portal, we are going to have three different functionalities when someone is logging in with the job seeker role, they should be only apply the jobs. Similarly, for the other roles, we're going to provide a different functionality based upon their role name. We're going to discuss about the role-based access mechanism in the coming sections. For now, I'm assuming you are clear about the purpose of these three roles. Usually inside enterprise applications, you will see hundreds of roles defined, but for our discussion, these three roles should be fine. Let me execute the scripts and with this now inside my roles table, I should have some records. So in total, there are three records created with the primary key ID as 123. I hope you're clear on what we have done inside this lecture please take the scripts that I'm providing and make sure you are also creating these tables and the data using the insert scripts.