Inside the Spring Security internal flow, we know ProviderManager or AuthenticationManager is responsible to handle the entire authentication. This ProviderManager, it is going to rely on the implementation of Authentication Provider to actually perform the authentication. For example by default, DaoAuthenticationProvider is going to be leveraged by the framework. Any AuthenticationProvider implementations like DaoAuthenticationProvider, they expect you to provide an implementation of user detail service. For example, here we are providing the bin of InMemoryUserDetailsManager. Inside this bean, we have various methods like loadUserByUserName(). So this method is going to take care of returning the user from the storage system and the return type from this method is user details. By taking these user details, the AuthenticationProviders, they are going to check whether the passwords are matching by taking the help of PasswordEncoder. This is what happens when you use InMemoryAuthentication or DefaultAuthenticationProvided by the Spring Security Framework. But from now onwards, we want the Spring Security Framework to go and check my users table to load the end user details based upon the email that the end user entered inside the login page. How to communicate that? That's a problem that we are going to solve inside this lecture. We can solve this problem in various ways. The first and basic approach is, we can define our own user detail service implementation. And inside that, we can override this loadUserByName(), which is going to accept username as an input. By taking this username, we can query our own database table and return the user record. While returning the user record, we need to cast it to user details object because the return type from this method is user details. And with that, the DaoAuthenticationProvider that we have, it is going to take care of the remaining magic of comparing the passwords by using the PasswordEncoder. But that's a very basic approach. Instead of that, what I would recommend is, we can build our own custom AuthenticationProvider itself. At the end of the day, AuthenticationProvider implementations, they have to return the output of the authentication mechanism, whether the authentication is successful or failed. So that is what the ProviderManager expects. The same we can try to build inside our own AuthenticationProvider, which will give the complete flexibility to us on how we want to perform the authentication. When you are going with that default AuthenticationProvider like DaoAuthenticationProvider, it is going to simply perform the username and password checks. But what if you have additional requirements inside your organization? For example, you don't want to allow the login operation if the end user entered invalid credentials continuously three times. Or what if you have a requirement like you don't want the end users outside of a given country to log in into your application. This kind of custom authentication requirements you can only build if you are building your own AuthenticationProvider. Let's try to build the same. Inside the security package, I'll try to create a new Java class with the name JobPortalUsernamePasswordAuthenticationProvider. I know the class name is very lengthy. Feel free to shorten it based upon your requirements. This class, it has to implement the AuthenticationProvider. On top of this class, let me mention @Component annotation so that it gets registered as a bean. Followed by, I'm going to mention RequiredArgsConstructor. Now let's try to understand what do we have under the AuthenticationProvider interface. Anyone who are building their own AuthenticationProvider, they have to implement the authenticate() method which is going to accept Authentication object. Inside this Authentication object, we are going to have the username and password entered by the end user. The return type from this method is again an Authentication object. So whatever object that we are returning from our authenticate implementation method, this return object, it should have a confirmation around the authentication, whether it is successful or not. We also have another method with the name supports(). By using these supports(), we need to tell to the ProviderManager, what kind of authentication mechanism that is supported by my AuthenticationProvider. First try to implement these supports. I'll hover on these errors and click on this implement methods. As of now, my first method is returning null followed by the second one is returning false. Let me go to the DaoAuthenticationProvider and inside this, I will look for supports() method. Inside this supports() method, if you see, they are simply checking whether the given authentication object is of type UsernamePasswordAuthenticationToken. If yes, they are going to return true. Otherwise they are going to return false. Let's do the same inside our AuthenticationProvider as well, because we also want to support username and password style of authentication. Let me close these. And here I'm going to paste the line of code. So with that, we are done with the supports() method. Now inside the authenticate() method, first, I'll try to extract the username and password entered by the end user from these Authentication object. I just have to invoke the Name() method and the getCredentials() method. So getCredentials(), it is going to return me the credentials object. The same, I'm trying to convert it to a String so that I'll get the password in a String format. Now inside my hand, I have both my username and password. As a next step, by using this username, I want to load the user details inside the database. Here again, I have two options. The first one is I can directly query my database by using the repository interface that we have. The other option is, we can define our own implementation of UserDetailService. At the end of the day, the responsibility of the UserDetailsService interface is to load the user details from the storage system based upon the username. You have to implement this interface and method and the same can be leveraged inside your provider. But I don't want to go with this option because when you are going with this option, this method, it is always going to return the end user details in the form of user details object. Again, you have to do some typecasting and copying of the data. I don't want to follow all of that. That's why I will directly try to query from my AuthenticationProvider. So in this scenario, I'm not relying on the user detail service implementation. I'm directly querying the database. So to query that database, we need to first inject JobPortalUserRepository as a dependency into this class. So now inside the JobPortalUserRepository, I need to define a derived query method using which I will try to fetch an user record based upon a given username. What is the equivalent column inside the database for the username? It is the email. What I'll do is, I'll try to copy these derived query method one more time. And this time, instead of using this method, I will simply type findBy and as soon as I type findBy my IntelliJ, it is also giving me various recommendations. If I select these findJobPortalUserBy, it is an indication that I'm trying to fetch a single record. Whereas findJobPortalUsersBy, that means I'm trying to fetch a list of records. But based upon our database schema for a given email, we're always going to have a single record. So that's why here I'm going to select to findJobPortalUserBy followed by. Again, my IntelliJ is going to show me various options. So here the IntelliJ is doing very smart job. As soon as I type email, it is giving me all the options that I have around the email. Since I'm looking to use only equal operator, I can simply select the first option. And this method, it should accept a single input parameter String. That's why I'm going to remove the second input parameter. Using these JobPortalUser is completely optional because this is acting as a Infinix. We already discussed about what is Infinix in the previous lecture. In the above method, I'm using user as a Infinix. It's up to you what you want to use. The main purpose of using Infinix is to make our methods readable and descriptive as much as possible. Now I can try to invoke this method from the AuthenticationProvider implementation by using JobPortalUserRepository. I can invoke the new method and to this method, I can pass the username as an input. This is going to give me the record of JobPortalUser. Once I have the user record in my hands, as a next step, I want to compare the given password with the already stored hash password. For that, I need help from the PasswordEncoder. That's why I need to inject PasswordEncoder as a dependency into this class. So what I'll do is, I'll try to paste a few lines of code here so that we can discuss in detail. You can get this code from the GitHub repo. This code is super simple to understand. Here in case if there is no record for a given username, I'm simply throwing an exception with the name UsernameNotFoundException to the client application with the details like user details not found for the user with the given username. In case if there is an user present by using the JobPortalUser, I'm trying to read the roles information. How to read that role information using the JobPortalUser, which is how to invoke the getRole() method, which is going to give us role entity object inside the role entity object. We are going to have the name of the role, the role name that we are storing right now it is of String data type, but inside Spring Security, the roles information should be represented by using simple granted authority. That's why here, whatever role name that I have, the same I'm trying to pass as an input to the constructor of this class, which is going to give me an object of this simple granted authority. The same I'm trying to save into the list of simple granted authority, because at the end of the day, inside Spring Security framework, we need to represent the authorities details as a collection object. Because Spring Security supports multiple roles for a given single user. For our application, since we have a requirement to have a single role, we are going with a single element inside the list. But in case if you have a requirement to maintain multiple roles information, you should be able to achieve that very easily. You just have to populate multiple objects into this list. Once we have the authorities details ready, I'm trying to check whether the passwords are matching by invoking the passwordEncoder.matches() method. To this matches() method, I'm passing the plain password that I received from the end user, along with the hashed password that we stored inside the database. If this matches() method returns false value, I'm throwing a bad credentials exception. Otherwise, I'm creating an object of UsernamePasswordAuthenticationToken by providing the JobPortalUser as an first input parameter followed by credentials I'm setting it as null and at last I'm passing the authorities as well. If you open this constructor, this constructor it is going to accept the principle as a first parameter. So inside this principle, we can store any object. Right now, we're trying to store our entire JobPortalUserEntity object. Under the credentials, we are passing null because it is a standard to avoid any security issues. We need to pass null value. And the third parameter is authorities. The same we have passed as well. The beauty of this constructor is,whenever we are invoking this constructor, behind the scenes, it is also going to set the authenticated as true. By the time this object is created here, within that object, the framework is also storing an information that the authentication is successfully completed. So this Authentication object, we are trying to return as an output from this method. We know this UsernamePasswordAuthenticationToken is a child class of this authentication interface. I hope you are clear with whatever logic that we have written so far, let's continue this discussion in the next lecture as well.