For the two tables that we have created in the previous lecture, let's try to create the entity classes and repository interface. Since I'm using IntelliJ Ultimate subscription, I should be able to generate the entity class from this database window. If you don't have this privilege inside your IDE, you can get the code that is generated from the GitHub repo. Let me first generate the entity class for this roles table, I need to right click on this and select to create JPA entities from DB. Inside this window, I need to provide the target package. The target package, obviously it will be entity,coming to the class name, it will be Role. For the parent, I'll select the base entities because this table also has all the four audit related Columns. Coming to the ID generation, I'm fine with the default identity recommendation. For the name column, it is going to be a normal String data type,and for all the remaining four audit columns, we should be fine with the default recommended values here. Under the references, you will be able to see the JPA buddy, it is recommending us to go with the OneToMany option. For now, let's not check this, we'll try to discuss about the mapping in few minutes. With these values, I'm trying to generate the entity, and this is how it is going to look like. I don't need these attribute overrides related configurations. You need to use this attribute overrides related code, only if you want to override any of the parent provided entity details inside our code, we are using BaseEntity inside the base entity, we have these columns information in case if any of these columns information is not matching with your roles table. You also have a flexibility to override those audit related column details, like making one of the column as nullable instead of not null. Under such scenarios, you need to use the attribute overrides configurations. In our scenario, since the database columns and the BaseEntity configurations are matching exactly, we don't need attribute overrides annotation configurations. That's why I have removed that code. And this is the ID related configuration followed by for the name column, this is the Java field. Now let's try to create the entity class for the users table as well. This time I need to select the users table and click on this place and right click here and select these create JPA entities from database. The class name this time, I will not use the default recommended one, which is User because inside the spring security framework as well, we have a class with the name User. Since there is a good chance that I may get confused between entity user and the spring security provided user, I want to give a different name here. Let me mention the name as JobPortalUser. And to this class, the parent will be BaseEntity coming to the ID generation I'm fine with this default recommendations followed by I have various columns like name, email, password, hash, mobile number. Under the role ID, we have ManyToOne configuration and the data type for this role_id will be RoleEntity,and for the company_id as well, it is recommending ManyToOne, and this is going to have a reference with the CompanyEntity data type. We already discussed about the purpose of ManyToOne relationship in the previous lectures. So here I'm fine with these ManyToOne, because if you look from the JobPortalUser perspective is multiple users can be assigned to a single role inside our backend application, We have only three roles like job seeker, employer, and admin, regardless of how many users I have inside my application, it can be 1000 or 10,000 or 100K or million users, all these users, they should be mapped to any of the three roles that I have. Since many users, they will end up having a same role, we need to use this ManyToOne and I'm fine with this default recommendation the same applies for company as well. I can have multiple employer end users inside my database. So these multiple employer users, they can be mapped to a single company. For example, if you take the Amazon company scenario, I can onboard multiple HR employees of Amazon company. That's why here also I'm going with the many to one coming to the audit related columns. I'm fine with these default recommendations. Let me click on this. Ok, and inside the JobPortalUser, I will get rid of these attribute overrides related configuration because we are fine with whatever we have inside the BaseEntity. So this is the actual table name, and these are all the configurations. Let's go to the role related configuration, so here we have mentioned ManyToOne with the fetch type as LAZY, but I want to change this to EAGER because whenever the user is getting loaded, I also want to load his role details as well. And since this is a not null column, the optional is mentioned as false coming to the joint column, we need to mention the role_id. Why we need to mention joint column inside this entity, because inside these table itself, we have the foreign key column with the name role_id. That's why we need to mention joint column here. We already discussed about these concepts in the previous sections. Next similarly, I have the company related configurations as well. Here also, I will try to change this to EAGER because whenever I'm loading the end-user details, I want to load the associated company record as well. This should not have any performance issues because there'll be only one child record. By using these @OnDelete annotation, we are telling in case if the parent company record is getting deleted from the database inside this table for this record, set the null value under the column company_id. Here also we need to mention joint column because this table has the foreign key column with the name company_id. I hope you are clear with these configurations. Now let's try to generate the repository interfaces as well. When you open these entity class, you're going to have the JPA designer,you need to click on the same and select this plus symbol and here we have an option of generating Spring Data JPA repository. I need to select the entity here,the entity will be JobPortalUser. And with that, the name of the repository will be recommended by the JPA buddy. I'm fine with this name. Coming to the parent, I'm fine with the JPA parent. Under the package, we need to select the repository package. If I click on ok, you will be able to see I got my new JobPortalUserRepository interface. In the similar lines, let's try to generate the repository interface for the Role as well. I need to click on this plus followed by Spring Data JPA repository. Let me select the entity as Role followed by here I need to mention the package name as repository. So this will be the name of my repository and it is going to extend that JpaRepository as a parent. As soon as I click on ok, I got my role repository interface. Before we try to close this lecture, I also want to create a dto class. Let me create a new record class,the record class name will be RegisterRequestDto. Since I'm planning to build a new REST API that allow a end user to register his own account. That's why I'm trying to build this Dto. So using this Dto only, I'm going to accept all the required information from the client application. What kind of information we need from a client application? If you try to check the column names under the users table, you will get to know the value under the ID column we are going to generate by using the Spring Data JPA. We require only name, email, plain password value, mobile number and role_id. We are going to assign inside our business logic. We are not going to ask the end user, which role you want to have. If you give that flexibility to the end user, then everyone will try to have the admin role because with the admin role, they should be able to perform most elevated operations inside the job portal. That's why we're not going to give the flexibility to the end user to provide what role information he is looking. Instead, inside the backend business logic, we are going to assign a default role,and if the end user want an elevated role, you need to work with the admin behind the scenes to get that elevated role. Coming to the company_id also, the end user is not going to share of selecting a given company. The admin of the website is going to verify whether a given employer belongs to a given company. Then only he's going to update that company_id value through the backend. That's why we are also not going to ask the company_id details from the end user. We are only looking for name, email, plain password and mobile number. So the same fields I'll try to mention inside this Dto class. So these are the validations that I want to perform for the mobile number I want to make sure the mobile number has only 10 digit numerical value. Next, I'll go to the JobPortalSecurityConfig, and here I'll get rid of these bean related configurations because we are looking to store the end user details directly inside the database. Here also for now, what I'll do is, I'll remove these two lines and for the constructor of the ProviderManager, I'm not going to pass any value. We will come back and update this method in the coming lectures. Before I try to close this lecture, I want to clarify one of the questions that you may have right now under the RoleEntity we have not provided any JPA relationship configurations, but inside that JobPortalUser entity, we have configured these ManyToOne configurations for the role. In the ideal world, usually we will mention the JPA relationship configurations on both entities. So since here we have ManyToOne on the role side as well, we can mention OneToMany configurations as well. For example, here I can mention OneToMany and using this annotation, I can invoke mapped by and provide the column name under the JobPortalUser entity. So here the column name is Role the same I'm trying to mention here,and just below this annotation, the data type of the column users, I'll mention it as list of JobPortalUser, because for a single role, we can have multiple user records on the user's table. Whenever we are mentioning these JPA relationship configurations on both entities, we call such relationships as bidirectional relationships. For our backend application, I don't want to mention these configurations unnecessarily inside the Roles entity, because inside our backend application, we are never going to have a scenario to load all the user details for a given role. I will never invoke inside my business logic role.get users to understand who are all the users assigned to a given role. If we never have such scenario, then we don't have to maintain the relationship both sides. We can maintain it as unidirectional itself. That's why I'm going to comment these two lines of code and I'll also mention some java comments here for your reference on why I have commented this, so OneToMany on the role side is optional, because I don't have any scenario to fetch all users by role. That's why I'm trying to avoid the heavy bi-directional mapping. I hope you are clear from the next lecture, let's try to build a brand new REST API that supports new user registration.