Let's try to replace this findById() method invocation with our own custom method that is built by using derived query methods. I need to go to the RoleRepository interface because this is the interface which handles the roles table. Here I am going to define a method which is going to return a Role record. The method name I am going to mention it as findByName(). Since I am looking to find a role record based upon a given role name, I need to give this method name. To this method, obviously the input parameter will be a single input String value. Using this input parameter, anyone who want to invoke this method, they have to pass what is the role name that they are looking to consider while fetching a given role record. If you are seeing this icon inside your IntelliJ IDEA, you should be able to test your method as well. I think this is only available under the ultimate subscription. If you click on this icon, it is going to ask to enter the name value for the WHERE condition. So let me enter ROLE_JOB_SEEKER. As soon as I click on this execute, you'll be able to see that a query is executed based upon my derived query. The query is select r from role r where r.name is equal to given name value. And this is the record that fetched by the database. So this way we can always test our derived query methods like what is the query, the framework is generating, what kind of results that we are getting. We also have an option to define these derived query methods by using IntelliJ. Here at the top, you will be able to see there is a option to Add Spring Repository Derived Method. As soon as I click on this, it is going to ask me what you are trying to fetch. Are you trying to find a single instant or a collection of records or count, exist, delete. So let me select the find instant here. And inside this pop-up, we need to select optional as the wrap type for the return value. Under this query conditions section, I can try to define my queries. For example, if I click on this option here, I can provide Attribute name. So these are all the attributes that I have. I can select the name followed by under the condition, I can mention whether I want to use equal operator or Like, NotLike, StartsWith, Contains and so on. I'm fine with the default Is, next I also have an option to check these ignore case, nullability, method prefix also I can select from these options. I can also define OrderBy Attributes as well. In the recent versions of Spring Data JPA, the framework, it is also allowing to define the infix. So what is infix? If you click on this help icon, it has some details. So this is the method name segment that comes between the find prefix and the By condition. It enables concise method naming and can be utilized to return various data types for the same query condition. I'll show you an example as well. What is this infix? If I click on these ok, you'll see I'm getting an error saying that findByName already exists inside your interface. Try to apply the return type name as an infix and use findRoleByName() as a method name. So here under the infix, if I mention role and click ok, I'm going to get a method name, findRoleByName(). So this is also very similar to findByName. The second method, it is giving more information. Using this method, they can find a role record by using the name. So both these method names are fine, but I'm going to use the second one because it is more descriptive and the return type also, I wrapped it by using the optional, because sometimes for a given name value, we may not get any record. That's why we need to give a indication to the invoker methods that there is a chance of getting no record. Since my method is ready, I can go to the Auth Controller here this time, instead of invoking the findById, I can invoke findRoleByName(), and to this, I need to mention the role name. So this role name I can maintain inside the application constants. Let me create a constant here with the name ROLE_JOB_SEEKER. The same, I'll try to pass as an input to this method by invoking the constant that we have created just now, followed by, I'm going to invoke the or Else Throw() method. So using this or Else Throw() method, I'm telling to the framework, if there is no record identified for a given role name, please throw this exception. If there is a role record, it is going to return that as an output, the same, I'm trying to catch it here. So now the role record that we have inside the database with the given role name, it is represented by these role object. The same I can set into the JobPortalUser object. Let me invoke setRole to this I will pass the Role object. This is the perfect way of querying the database by using the derived query methods. We can also add one more logic here. As of now, we're trying to store the given record directly into the database, but we need to perform a check whether a record already present with a given email or mobileNumber. Because both these columns, we define them as unique columns inside the database. That's why this time, what I'll do is, at the starting of the method, by using the jobPortalUserRepository. I can invoke a derived query method using which I can try to fetch an existing record based upon a given email or mobile number. We have to create this new derived query method. Let's go to the jobPortalUserRepository, and here we can either type the derived query method or we can use this option that IntelliJ is providing. As I said before, if you're not seeing all these options inside your IntelliJ, that is completely fine. You can try to type the derived query method name, or you can get the code from the GitHub repo. I'm trying to show you all the features of IntelliJ, so that when you are working in a real enterprise application, in case if the organization is providing you the IntelliJ ultimate subscription, you can explore all these options to make your life easy. Let me click on this and select the find instance. Here also wrap type, I'm going to mention it as optional, followed by under the query condition. First, I will select the email attribute, followed by, I'm going to click on this Add and select the mobile number. Since I'm looking to use the Or operator, I need to select the Or here, coming to the infix, I will mention it as user. This time, just for fun, instead of find, I'm going to select to read. Next, since I don't have any OrderBy requirements, I'm going to click on this, ok, which is going to generate me these new abstract method inside my jobPortalUserRepository. So the method name is readUserByEmailOrMobileNumber(). Let me go to the Auth Controller and invoke the new method. To this method, I need to pass both email and mobile number. The same I can read from the registerRequestDto and pass the output. I will try to catch it by using optional of JobPortalUser. This time, I don't want to throw any exception. If there is no record for this fetch criteria, because that is a good news for me and I want to proceed with that for the business logic. After this code, I'm going to write an if condition using this if condition I'm checking if there is any record present inside this optional object by using these is present. If yes, I'm trying to fetch the object of job portal user by invoking this get() method followed by I'm checking if the given record email is equal to the email that I received. If yes, I'm trying to populate an error hash map with the key as email and the value as Email is already registered. I'm also checking if the mobile numbers are same. If yes, adding these mobile number related error as well into the hash map errors. And towards the end, I'm trying to return the status as bad request and inside the body, I'm trying to send the hash map. With this logic, what I'm trying to achieve is whenever I identify an existing record with a given email or mobile number, I'm sending to the client application that I'm not processing the registration request due to the bad request. Inside the request, I found so-and-so errors. The same I'm trying to pass by using these errors hash map. But with this, we have a problem under the happy path scenario. We are simply sending the string value as an output, whereas inside these error scenario, we're trying to send the hash map. In this kind of scenarios, we don't have to enforce the ResponseEntity to send a given data type. We can simply mention question mark ? And with that, the Spring Boot framework is going to take care of wrapping whatever object that you are trying to provide inside your business logic. Before I try to test this entire code, I'll go to the JobPortalRepository and click on this icon here. It is going to ask me some email. Let me provide the email that I have tested before. So this is the email coming to the mobile number this is the mobile number or you can give any random numbers since we are looking only to test if everything is working fine. Any value should be fine. If I click on this execute, it is going to show me what is the query that it fetched. As you can see inside the WHERE condition, it is using the Or operator. And this is the record that it fetched from the database, because it found a matching database record. This gives a confirmation to us that our method is working without any issues. Now I'll do the build from the postman. I'll try to send the same request. We should get the 400 error. As soon as I click on this send button, you'll be able to see I got a 400 bad request and inside the response body, we have these details like mobile number is already registered or email is already registered. Let me go to the database and delete the existing record under the users table. I just have to select this record and click on this delete row and submit, which will take care of deleting the record. This time we should have successful response. If I click on the send button, yes, I got 201 created. Inside the database also, if you reload this page, we should see a new record with the details that we have provided. Coming to the role_id, it mapped the correct values, but the given name, the primary key ID value is 1. The same is mentioned under these foreign key column. I hope you are clear on how we have leveraged derived query methods concept to enhance our registration process to the production standards.