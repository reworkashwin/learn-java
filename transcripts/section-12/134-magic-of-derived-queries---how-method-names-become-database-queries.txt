If you check the methods present inside the JpaRepository, you will be able to find some methods provided by the framework for various kind of CRUD operations. Save() is a method which we can use to create or update an existing record, and similarly, we have delete related methods, which are going to help us to delete all records or to delete a specific record or delete a record by using the primary key ID value. In the similar lines, we also have find related methods, which are going to help us to fetch all records from a table or to fetch a given record by using a primary key ID value. But what if we have a scenario where we want to fetch or update or delete a record based upon our own custom where condition. For example, inside my database table, I may have many columns. And if my requirement is to perform CRUD related operations by using any of these column names, then whatever methods that are provided by the JpaRepository or by the JPA framework, they are not going to be helpful for me. For example, inside the roles table, we have a name column. Instead of fetching the record by using the primary key ID value by using the findById I want to fetch the record by using the name column. For these kinds of requirements, obviously the default provided methods, they are not going to be helpful because the JPA framework, it can't guess our database columns and provide all the possible methods. It is always the developer responsibility to handle these kinds of custom requirements. With this, you may think Madan, that means do we need to write our own SQL query? Do we need to execute them? Do we need to write a lot of database related logic? This can be a worry, but we don't have to do that. We just have to define an abstract method by following a JPA standard. Let's try to understand what is this concept. There is a concept with the name Derived Query Methods inside the Spring Data JPA. When we define a method by using these Derived Query Methods, the JPA, it is going to auto generated queries based upon the method name. We don't have to write SQL queries manually to execute any of the CRUD operations based upon our customer requirements. We just have to define the abstract methods by using this concept where we have to define them inside the repository interfaces. As of now, the repository interfaces like JobRepository or JobPortalUserRepository, they are empty. They are simply extending the JpaRepository, but inside them, we have never written any logic. So from now onwards, whenever we have a custom requirement to perform the DB operations, we just have to define an abstract method and the name of that abstract method we need to build by using the Derived Query Methods concept. Let's take an example. I want to fetch the list of companies by using a status, inside the status column we may have various values like Active, Draft and Close. So the valid values for the status can be anything, but by using a given status value, I want to fetch list of company records. So for these kinds of requirements, we can't use findById method or findAll() methods that are provided by the Spring Data JPA. Instead, we should define an abstract method inside the CompanyRepository interface. The name of the method should start with the findBy whenever we are looking to read the queries or to select the records from the database, it has to start with the prefix as findBy and the B inside the by has to be capital followed by you can mention the field name that is mapped to the status DB column. If you have considered the Java field name as Status, the same you need to mention here with the S as capital. By looking at this abstract method name, the Spring Data JPA will execute a select query based upon the Status value that we are providing as an input to this method. Anyone who want to fetch the company records based upon the Status, they can simply invoke this method by using the CompanyRepository bean. And when invoking this method, they have to provide the Status value that they want to consider as part of the WHERE condition value. The return type from this method, we can define either optional company or optional of list of company based upon our requirements. Similarly, a derived query method inside the Spring Data JPA consists of two main components separated by the By keyword. So the first one is introducer class. So this class, it is going to specify what is the type of operation that we want to perform on the database, whether we want to perform select operation, update operation or delete operation. So that clue we are going to give to the Spring Data JPA by using these introducer class. Common introducers include find, read, query, count, get. These introducer class, they are going to help us in fetching the records. We can also include distinct to ensure unique results in the query. Next after the introducer class, we are going to have the By keyword and after this By keyword, we are going to have the criteria class. This part begins after the By keyword and defines conditions on entity properties or entity Java fields. Conditions can also be combined by using And,Or keyword. Just a few seconds back, we discussed about this method name findByStatus. So with this, the JPA, it will try to fetch a record by using the Status field. Instead of findByStatus, you can also use readByStatus, queryByStatus. So they're going to have the similar effect. In case if you are using the countByStatus, you are going to get the output as count of the records with a given status. Let me share more examples so that you will be comfortable with these derived query methods concept. In this slide, I have mentioned what are the common words that we use inside that Derived Query Methods. findBy we already discussed whenever we are using findBy, it is going to find the records by a field. So if you consider customers table and inside the customers table, if there is a department column, if I want to fetch all the customers for a department value, I just have to use findByDepartment and to this method, we need to pass what is that department value that needs to be considered inside the SQL query. So for this method behind the scenes, this is the SQL query that is going to be generated by the Spring Data JPA. By using countBy, we can count the records by a field. So here I have mentioned countByDepartment and for this, this is how the SQL statement that is going to be generated. Next by using the existBy, we can check if the record exists for example, here I have mentioned the name as existByName, but this kind of Derived Query Methods, this is the SQL query that is going to be generated. Basically you're going to get a Boolean value true or false as an output from this method. Next for that delete operation, you can use deleteBy or removeBy. For example, if I want to delete a record based upon a name field, then I can define the name of the method as deleteByName and with that, this is how the SQL query is going to be generated. Similarly, I can use removeBy age with this method the records will be deleted from the customers table based upon the given age. Majority of the times we are going to handle the select queries only by using the derived query methods. And very few times we are going to execute that delete queries as well by using these derived query methods. But coming to the update operation, we never have to define any of these abstract methods. We can simply invoke the save() method to the save() method if you pass the updated entity object, the framework is going to figure out what is the associated record based upon the primary key ID value inside the entity object. Once the record is identified, the framework is going to check if there is any mismatch between the entity object and the record object. If yes, then it is going to execute the update operation for the changed values. Inside this very first table, we have considered the scenarios where we are trying to execute the queries based upon a single column. But what if you have a requirement to execute a query by using multiple fields? As I said before, we should be able to do that very easily. We just have to separate the Java field names by using AND, OR logical operators. For example, here I have mentioned the name of the method as findBy name and age. Since we have mentioned two Java fields here, both of them has to be provided as an input to the method. This select query is going to be executed where inside the WHERE condition, it is going to consider both the name and age. In case if you are looking to consider only one of the column value, you just have to use the OR operator and this is how the SQL query is going to be generated. Under the next example, I have used findDistinctByNameAndAge. With this, what I'm telling to the framework, please fetch all the distinct customers with a given name and age value. Let's explore more examples. Sometimes we may have a requirement to fetch the records based upon the greater than, less than or any other criteria. Basically, we're not going to use the exact equal operator and a search scenarios, we can use any of these examples. For example, with the findByAgeGreater than we will fetch the records which are having a age value greater than given value for less than the less than operator is going to be used. So first you need to mention findBy what is a column name followed by LessThan or GreaterThan. We can also use between for the scenarios where we want to fetch the records for age between two values. For the between related methods, we need to pass two inputs. What is the start age and what is the end age? For the String columns or the string Java fields, we can use like operator as well. For example, inside the SQL, we can use a like operator where I can mention if the customer name like MADAN, then it is going to look for the customer names which are having this value in between. For such scenarios, I just have to define this method findByName like to this method. We need to mention what is the pattern of the String that you want to search. We also have NotLike, which is going to do the exact opposite operation of the Like. Next we also have StartingWith, EndingWith, Containing when you're using the StartingWith or EndingWith and Containing. This is how the SQL statements that are going to be generated coming for the sorting related operations, after mentioning the method name like findByDepartment, we can mention what are our sorting requirements. So for sorting inside the SQL statement, we use OrderBy. So that's why after the department, we need to mention OrderBy followed by what is the field name. The field name is Age. After the field name, I can mention ASC, ASC means ascending order. I want to fetch the customer records based upon a given department value OrderByAge in a ascending order. In case if I'm looking to fetch the records based upon the descending order, I just have to mention findByDepartment, OrderBy what is the field name that I want to construct this time I'm considering the name field and after the field, I have to mention Desc, Desc means descending order. I have more examples. Let me share them as well. If you're looking to fetch the limited number of results, you can use find first or find the top three. The method name I have mentioned it as findFirstByOrderByAge ascending order. So here I don't have any WHERE condition requirement. That's why after By, I have not mentioned any Java field name directly I have mentioned OrderByAge ascending. In case if I have a requirement to fetch the very first record based upon a given field name, after the By, you can mention the field name as well, followed by you can mention your ordering requirements. By using findTop3, I'm trying to fetch top three records. This is how the SQL statement is going to be generated behind the scenes. So similarly, I can fetch top 10 records, 7 records or 2 records. I just have to replace these 3 with required number. Coming to the null value examples, if you are looking for the records, which are having department name as null, you just have to first mention findBy what is the field name field name is department IsNull() with this, the JPA framework, it is not going to expect any value as an input to the method because it knows inside the WHERE condition it just have to look for the Null record. Just like IsNull() we also have IsNotNull() where it is going to look for the records, which does not have null values under the department. If you are working with that date columns, you can use something like findByStartDate after. If you consider there is a column with the name start date inside the database table for that, the associated field will be started. So first I'm trying to fetch the records based upon the start date followed by, I have mentioned the after inside the method name to this method name I need to mention a date value as an input. So since I have mentioned after here, inside the WHERE condition, the greater than symbol is going to be used by the framework. Just like after we can also use before in the case of before the less than symbol is going to be used. We can also check multiple values inside the WHERE condition. Usually inside the SQL query, we use IN keyword to check the multiple values at a time. For example, if I am looking to fetch all the customers, which are assigned to the department HR and IT for these kinds of requirements, I can simply use the method name as findByDepartmentIn. To this method, we can provide the list of values that we want to consider under the WHERE condition. Just like IN, we also have NOT IN, which is going to do the exact opposite behavior by using the NOT IN keyword. Next we can also check Boolean values. For example, inside your database table, if there is a column which is only going to store true and false value under such scenarios, you can use these method names findByActiveTrue(). So there is a column with the name active inside the database. That's why I need to mention active here, followed by if I mentioned true, then it is going to look for the records which are having active as true. If I use false inside the where condition, false will be considered. In case if I'm looking to fetch the records by ignoring the case value, I just have to use these ignore case. Here the method name I have mentioned it as findByFirstName, followed by ignore case. If I simply use findByFirstName, then the Spring Data JPA will try to fetch the record which is matching with the exact name that I'm providing. But since we are using ignore case here, so this is the query that it is going to execute. Basically it will ignore the case value and fetch the corresponding records. This way I can give more and more number of examples as well, but since it is not possible to discuss each and every scenario, what I would recommend is, always whenever you have a custom requirement to fetch the records or delete the records or to perform any other operation, please check whether if you can achieve that by using that derived query methods, you can take help from ChatGpt or any other AI tool as well. You just have to give the input to that and ask it if it is possible to achieve that requirement by using the derived query method. It is perfectly possible to build very complex derived query method names as well. Here I try to mix multiple conditions together. So findByDepartmentAndAge. Basically I'm trying to fetch the records based upon the department and age value followed by GreaterThan while fetching for a given age I want to consider GreaterThan symbol for the age column I want to consider the GreaterThan instead of equal symbol and towards the end I'm mentioning OrderByName ascending. So once the record is fetched based upon the given department value and AgeGreaterThan value, I also want to sort the records based upon the name in an ascending order. This way we can define any number of complex requirements by using the derived query method. For this method, this is the SQL query that is going to be executed behind the scenes. In case if your requirement is too complex or if you don't want to define lengthy derived query method names, we also have an alternative approach inside the Spring DataJPA that approaches by using the @Query annotation. By using this @Query annotation, we can give our own custom queries directly to the Spring DataJPA so that whenever someone is executing a given method inside the repository, whatever query that we have provided to this annotation will be executed by the framework. For example, on top of this method name, I can simply mention @Query annotation and to this @Query annotation, I can provide this entire query as an input. When a @Query annotation is present on top of a method inside the repository interface, then the Spring DataJPA, it is not going to construct the method name. It is going to simply execute the query that you have provided on top of the method. We're going to discuss about @Query annotation in detail in the coming sections. For now, I'm assuming you are clear about Derived Query Methods. By using this concept, let's try to enhance our registration REST API.