We built our own AuthenticationProvider. As a next step, we need to provide this provider information to the ProviderManager or AuthenticationManager. Let's go to the JobPortalSecurityConfig here. I can get rid of all these InMemory related configurations, followed by I can try to inject my AuthenticationProvider implementation. Let me type private final AuthenticationProvider. Next I can get rid of all these DaoAuthenticationProvider related logic. To the ProviderManager, I can simply pass my AuthenticationProvider bean. How to do that? To this authenticationManager() method, I will simply pass AuthenticationProvider as an input. Since I'm using bin on top of this method, the spring framework will take care of injecting a bean of type AuthenticationProvider into this method. The same I'm trying to inject into this ProviderManager. The beauty of this ProviderManager is, it is capable of accepting any bins of AuthenticationProvider. Since we have a single AuthenticationProvider, we're directly providing the interface name here. Otherwise we have to mention the exact class name of the AuthenticationProvider to avoid the confusion to the spring dependency injection. So the point that I'm trying to highlight here is, if there is a scenario where you have to support multiple styles of authentication, like one is by using the username and password and the other one by using the OTP and the third one by using a security key and so on. In this kind of scenarios, you need to build various AuthenticationProviders that supports various authentication style. All of them, you can inject them as a dependency into the ProviderManager. So the job of the ProviderManager will be based upon the type of authentication right now happening, it is going to invoke the corresponding AuthenticationProvider. How this ProviderManager will know the correct AuthenticationProvider details? If you check the ProviderManager authenticate() method here, it is trying to iterate all the AuthenticationProvider beans. And the first step that it is going to do is to invoke the supports() method. By invoking the supports() method, the ProviderManager, it is going to ask each AuthenticationProvider. Are you going to support a given style of authentication? If yes, then only it will continue. Otherwise it will simply skip that given AuthenticationProvider. I hope this is all making sense to you. Now let's go to the JobPortalSecurityConfig here. We are good. Next, we need to go to the Auth Controller and here under the login REST API, we need to make certain changes. First this time, when this authenticate() method is invoked, our own AuthenticationProvider logic is going to be executed. So once the authentication is completed, I'm going to get the output in the form of these result Authentication object. The same way we are trying to pass to the generateJobToken() method. Here we need to make one change. When we are invoking the getPrincipal() method, we know the framework, it is going to return an object. So that object we need to type cast based upon what kind of information we stored under the principal. With the changes that we have done under the AuthenticationProvider implementation, once the authentication is completed, we are storing the jobPortalUser object as a principal object. Previously, we type casted to the user because that's a default behavior of the Spring security framework. But right now, since we are storing the JobPortalUserEntity object to the same, I need to type cast. I'm getting an error at the getUsername() because there is no field with the name username inside the JobPortalUser. This time I'll update this claim to name and by invoking the getName(), I'm going to send the name of the end user. In the similar lines, I can send email and mobile number as well. Let me send email and here I need to invoke getEmail() followed by mobile number and here I need to invoke the getMobileNumber(). This way I can send any kind of claims information inside that Jwtoken payload. So here we are good. Let's go to the Authentication Controller. So once the token is generated, as of now, we are simply sending an empty user DTO, but instead we can do a better job here. Let me paste three lines of code. So what I'm doing is, first I'm trying to fetch the principal object by using the result authentication. Since we stored a jobPortalUserEntity object to the same, I'm trying to do the casting. So from these logged in user object, I'm trying to copy all the matching fields to the UserDto by using these copyProperties() method. So what kind of information will be copied? So the name will be copied. Email, mobile number will be copied automatically. Coming to the role name, since inside the Entity object, I'm going to have it in the form of object. What I have to do, I have to manually read the role name and assign it to the role under the UserDto. With this, I'm assuming we are done with all the login changes. Let's try to perform the login operation by using the new accounts that we have created. Under the security folder, we have a login API here under the username, first I'll try to send some invalid values. If I click on the send button, you'll see I'm getting authentication failed message. This time, let me mention a valid username and password. The first one will be john@gmail.com and the password will be EazyBytes@1803. Let me click on the send button, yes Hooray. The login is successful. The message is OK. Company ID, company name for now, it will be null. Under the email, we have the email address. Next we have mobile number, name, role name is ROLE_JOB_SEEKER. User ID is null for now, or we can send the user ID as well. Here, what I have to do, I just have to invoke userDto.setUserId to this, I need to pass loggedInUser.getId. If I save this and do the build, this time if I click on the send button, we're going to have the user ID as well. So this is a JWT token for this user. Let's try to log in as a different user. I will type sanjana@gmail.com for this user the user ID will be 4 and role will be ROLE_EMPLOYER. And this is the name and this is the email. Let's try with admin as well. If I click on the send, this is also working fine as expected. This gives a confidence to us that our both registration and login is working fine without any issues. I prepared some slides on the importance of the authentication provided during the login mechanism, why we should implement our own AuthenticationProvider. Please read this information inside this slide. In the next slide, I highlighted about the abstract methods available inside this interface, why we should implement them. In the next slide I have mentioned all the logic that we have written to perform the login operation by using our own AuthenticationProvider. So once we have defined our own AuthenticationProvider, we need to feed the same to the ProviderManager as well. And towards the end, under the login API logic, we need to leverage the same to perform the actual authentication.