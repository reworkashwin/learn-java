Implementing auditing by using Spring Data JPA involves four different steps. As a very first step, we need to create a class like BaseEntity, and inside this class, we need to make sure to mention all the audit-related fields. On top of this class, we need to mention @MappedSuperClass followed by @EntityListeners. To this annotation, we need to pass AuditingEntityListener.class as an input. This is a class present inside the Spring Data JPA framework code. If you try to read the documentation on top of this class, they clearly highlighted, this class is a JPA entity listener to capture auditing information on persisting and updating entities. To make it work, we need to make sure certain configurations, for example, if you are trying to use inside the Spring Boot application, we need to mention these annotations like @EnabledJPAAuditing. We'll go there, but the important information that I want you to understand is, as a very first step, you need to prepare this entire class along with these @MappedSuperClass and @EntityListeners annotation. To the @EntityListeners annotation, we need to provide this class as an input. The second step we already completed in the previous lecture. As part of this step, we need to make sure all the entity classes, they are extending our superclass. So both Company and the Contact, they are extending the BaseEntity. Now let's try to implement the third step. In order to implement the third step, I'm going to create a new Java package with the name audit. Since I'm looking to define all the auditing related logic in a separate package, I have created this sub package. I'm going to create a Java class with the name AuditorAwareImpl, impl means implementation. Again, the name of this class can be anything. There is a reason why I have given this name. This class, it has to implement an interface with the name AuditAware. Since this class is implementing this interface, I have given this name. Let's open this interface. This is a functional interface since it has a single abstract method with the name getCurrentAuditor(). By writing our logic inside this getCurrentAuditor() method, we are going to give a clue to the framework on who is the current auditor that is trying to insert or update the data. Let me implement the abstract method since inside our application, the current auditor is going to be represented by using the String data type. I have provided the same here. So with that, now our method has to return an optional of string. For now, I'm going to write a logic inside this method to always return a value with the name AnonymousUser. In the coming sections, when we start implementing the security for our application, obviously we are going to update this code to consider the current logged in user as the current auditor. For now, for all the updates and inserts that we are going to do, the Spring Data JPA considers AnonymousUser as the current auditor. On top of this class, we need to mention @Component annotation because we need to create a bean of this class. Now, I'm going to give a name to the bean. The name will be AuditAwareIMPL itself. I will keep the first letter as small case letter. The third step is completed. We can move on to the last step. But before that, here I want to clarify a question that you may have. You may ask me, Madan, we are providing only the current auditor details, but not the current date and time details. The answer is very simple. The Spring Data JPA is capable of identifying the current date and time. We know inside Java, to understand what is the current date and time, we just have to invoke a method like Instant.now() or LocalDateTime.now(), which are going to give the current date and time based upon the location from where the code is running. That's why we're only going to provide the auditor details, but not the audit time details. Moving on to the last step, under the JobPortal application, which is the main class inside our application, here we need to mention an annotation with the name @EnableJPAAuditing. By using this annotation, we need to invoke a property with the name auditorAwareReference. To this property, we need to provide the bean of the AuditAwareImpl. Inside this bean, we have created all the logic to identify who is the current auditor. The bean of this component class, I need to provide as an input to this AuditAwareRef. We are done with all our four steps. Let me do the build. Once the build is completed, I'll restart my application just to make sure to reflect all my code changes properly. From now onwards, we don't have to provide the audit related data from our business logic. I can go to the ContactServiceImpl and here I will comment both these two lines of code. This time, if I invoke the same REST API, the created date and created by will be taken care by the framework. Let me click on this send button as you can see, I got an error seems the code that we have written, it's not working the createdAt is still is going as null value. Let me check what mistake I have done. Under the BaseEntity class, seems I forgot to mention some annotations. On top of the Java fields, we need to mention such annotations. Because by looking at the simple column related configurations, my Spring Data JPA cannot easily understand which field has to be considered for updating the createdAt time and similarly for createdBy, updatedAt and updatedBy. To give that clue to the framework, we have to mention @CreatedDate followed by @CreatedTimestamp. By mentioning both these annotations, we are telling to the framework, please consider the Java field to populate the current date and timestamp while someone is trying to create a new record inside the database. In the similar lines, on top of the @CreatedBy, we need to mention an annotation with the name @CreatedBy. So whenever someone is inserting a new record inside the database table, the framework will populate the current audited details under this column. For the other two columns, we need to mention at last modified date for the date followed by @UpdateTimestamp. By looking at these two annotations, the framework, it is going to have a clear understanding that it has to update this field whenever someone is updating a record. At last, on top of this updated by field, I need to mention an annotation @LastModifiedBy. For this field also, the current audited details will be leveraged to populate this field when someone is trying to update the existing record. With these modifications, right now my framework will have enough clues about the purpose of the Java fields and which columns it has to update in the database based upon whether the update is happening or creation is happening. But there is a small problem with this configuration. By default, the Spring Data JPA framework, it will try to populate all these four fields during both insert and update operation. But we don't want that to happen. We want only createdAt and createdBy should be populated during the creation of a new record. And similarly, when someone is updating the existing record under such scenarios, we want only these two last fields to be updated. We can communicate the same as well to the Spring Data JPA. By using the @Column annotation, I need to invoke the updatable property. To this, I need to mention false. Let me do the same for createdBy as well. Next, for the last two columns, we need to invoke a property with the name insertable and to this, we need to provide false as a value. Let me do the same for last field as well. So with the insertable as false, we are telling to not update these two fields during the insert operation. And similarly, during the update operation, these first two fields should not be updated by the framework. Let me save these changes, do the build. Once a build is completed, I'm going to invoke the same REST API and this time the request is processed successfully. Let's check the new record that is inserted. As you can see for the new record that is inserted, the createdBy column is populated with anonymous user value based upon the logic that we have written. And similarly, the createdAt column also is populated with the current date and time. I hope you are clear with whatever we have discussed around the auditing by using Spring Data JPA. From now onwards, any entity class that we are going to create, we're going to make sure to extend the BaseEntity so that the auditing related functionality will be taken care by the framework itself. All the steps that we have discussed, I have documented them in these slides for your reference. Always feel free to refer to these slides if you want to quickly revise the Spring Boot concepts.