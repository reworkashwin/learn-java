Since we don't have any REST API inside our application that accept query params, what I'm going to do is, I'll try to create a new REST API inside the ContactController. First, I'm going to mention @GetMapping annotation followed by this REST API method it is going to return the response by using String. That's why I'm trying to mention ResponseEntity of String coming to the method name. I'll mention it as fetchOpenContacts() and this method, it is going to accept a @RequestParam. That's why I need to mention @RequestParam followed by String and the param name will be status. Based upon the status that my client application is trying to send, accordingly, I want to return the contact messages from the database. For now, let's not worry about the database business logic. Let's focus on only on the validations. From this method business logic, I want to simply return a String response saying that these are the contacts with the given status. Now let me do the build and invoke this REST API. From the postman, what I'll do is, I'll try to copy this request one more time and update this to FetchOpenContacts. And here I need to mention GET  method and coming to the query param, I need to send it as status. To this status, I'm going to send an empty value. As soon as I click on the send button, you'll be able to see I'm still getting successful response. That means my REST API is accepting even invalid data as well. This is because we have not done any validations. Now let's try to enforce some validations. This time, since we are accepting directly by using the primitive data type like String, we need to mention our @Validation annotation just before this String itself. So I'm going to mention not blank along with the message as well. The validation message that I'm going to mention here is status cannot be blank. Let me bring this code to the next line so that you can see clearly after mentioning these @NotBlank annotation, we should also take care of mentioning @Validated annotation. So by using this annotation, we are telling to the framework, please validate this status field by using the validations that I'm trying to provide. Let me save this, do the build. This time, if I try to send the empty status, I'm going to get an error saying that 400 bad request validation failure. So from where I'm getting this ? So this response I'm getting from the generic exception handling method. If you go to the GlobalExceptionHandler and keep a breakpoint in the first method, the breakpoint is going to stop here. This time, the exception type is HandlerMethodValidationException. If you open these exception details, there are validation result. Let me open the same. And here there is a parameter validation results and under these, there is one validation result. If you open these, you will be able to see on which method parameter the validation failed. Let me open these and here you will be able to see the parameter name is status. So on this parameter, the validation failed. To send a meaningful response to the client applications, we need to define one more exception handling method that handles the exception of type HandlerMethodValidationException. Let me release this breakpoint under the response as of now, we are seeing the 500 internal server error along with this error message. But this is not helping my client application. I need to send a meaningful message. To return our own custom response when the validation fails, we need to define one more method that handles HandlerMethodValidationException. I'm going to paste few lines of code. You can get this code from the GitHub repo as well. As you can see, this new method, it is handling a exception of type HandlerMethodValidationException using this Exception object. I need to invoke the getParameterValidationResults() method, which is going to return me a list of type ParameterValidationResult. So all these results, I'm trying to iterate by using this for each forEach() parameter validation results. I'm trying to execute this logic. So first I'm trying to get the parameter name by invoking these getMethodParameter().getParameterName() followed by to understand what is the validation error message details. I need to invoke these getResolvableErrors(), which is again going to give me a list of error message details. The new list I'm trying to again iterate by using stream and for the each error, I'm trying to read the error message by invoking these getDefaultMessage(). And towards the end, all the error messages that I have received, I'm trying to join them by using comma as a separator. And towards the end, I'm trying to put the parameter name and this combined message into these errors hash map that we're trying to return. Let me say this, do the build, once the build is completed this time, if I invoke the same REST API, this is how I'm going to get the response. The response code is 400 and inside the payload, we're trying to send what is the parameter name followed by status cannot be blank. In case if you want to enforce more number of validations on the same parameter, you should be able to do that. Let me do the same this time I'm going to mention size to this size. I will mention the minimum value as 4 followed by message I will mention it as status length should be of minimum 4 chars. This time, if I invoke the REST API, I'm going to get the two validation error message that are separated by comma. So the first one is status cannot be blank followed by status length should be of minimum 4 chars. If I mention the status with a single char like O, the first validation error message will be gone. So this time you'll be able to see we got only a single validation error message. Let me send a valid value. Maybe I will send it as open. And this time we're going to get a successful response from the REST API. This is how you can enforce any number of validations on top of all the request params and path variables that you're trying to accept. Mentioning this validated is completely optional, but it is a good practice to mention this. If I remove this and do the build, this time also we're going to have the same behavior. Let me remove this open and send. As you can see, we are getting the same response. But I still recommend you mentioning this validated because it is a good practice. While you are implementing these backend validations inside your real enterprise applications, in case if you receive a different exception category, then your code will go into this first method and you will end up returning 500 error. That's why always debug your code and try to identify what is the exception that the framework is trying to throw in your scenario. And accordingly, you need to define your own exception handler method. And inside these methods, you can define your own logic on how you want to send the response body and the response status code. Before we try to close this lecture, let's try to revise what we have done to enforce the backend validations. As a very first step, we added this dependency inside our parm.xml, followed by inside our DTO classes. We need to use these annotations like @NotBlank, @Size, @Pattern, @Email, based upon our business requirements. Once the DTO is updated with these validation annotations inside the REST API, just before the DTO class, we need to mention @Valid annotation. In case if you are looking to validate the query params, then we need to make sure to mention the @Validation annotation directly inside the method signature. As you can see here, while validating query params or path variables, we need to use these @Validated annotation. Always follow these best practices while you want to enforce the backend validations. You need to use DTOs instead of entity classes for request validation. Few developers, they are going to use the JPA entity classes itself to return the response to the client applications. Those developers, they're going to put the validation related annotations inside the entity class itself. That is not a good practice. We need to have separate DTO classes. Always make sure to return meaningful error message to the client. The status code should be 400, which gives a clear indication to the client application that there is an issue with the request data. We should also make sure to centralize all our validation error handling logic by using @RestControllerAdvice. In the next slide, I have given an example on how to enforce GlobalExceptionHandling logic. These are the two methods that we have developed during our discussion.