We, as humans, we always look for opportunities to improve ourselves. In the similar lines, whatever code that we write in enterprise applications, we should always look for the opportunities to improve or optimize the code. That's how we are going to differentiate ourselves from other normal developers. So far inside our application, we have created a couple of entity classes. The first one with the name Company followed by Contact. If you closely look at the code that we have written in both these entity classes, there are four audit related columns or fields that we repeated inside both these entity classes. What are these fields? These are createdAt, createdBy, updatedAt, and updatedBy. We know the purpose of these fields. By using these fields, we are going to perform the auditing of the data, like who created the data or who updated the data, because in real enterprise applications, the production data will be a valuable or secure data. Anyone who are updating to these data, we need to track them. Otherwise there won't be any accountability and it will result into the business damage. But the problem with these approach that we are trying to follow so far is, we are repeating these four fields related code in both the entity classes. By the end of this course, we are going to create 20 more entity classes. And in real enterprise applications, you will have hundreds of entity classes representing all your database tables. Do you think it is a good practice to repeat all these four fields in all the entity classes? Of course not. There has to be a better approach. By taking the help of Spring Data JPA framework, we are going to move these four fields to a parent class. On top of that, we're also going to give the responsibility of populating these audit related columns to the framework itself. As of now, we are manually populating the data into the createdAt and createdBy columns. For example, if you go to the ContactServiceImpl, while I'm inserting a new record into the contacts table, we are populating the createdAt and createdBy manually. But this is again, is a not recommended approach because the framework should be able to know about what is the current time apart from that date and time. We can also give certain clues to the framework about who is the current logged in user and the same current logged in username will be leveraged by the framework to populate the data into the createdBy column. Off course, as of now, we have not implemented security inside our application, but in the coming sections, we are going to implement security by using the Spring Security Framework. And at that point of time, we are going to update our application code to consider the logged in username to populate under these createdBy and updatedBy. So far, we have scenario only to populate createdAt and createdBy, but in the coming sections, we are also going to have some scenarios where we are going to update the existing data under such scenarios, we need to populate the data under the updatedAt and updatedBy columns. Enough of talking Let's see the demo what I'll do is under the entity package, I'll try to create a Java class with the name BaseEntity. This name can be anything. Inside this class, I'm going to mention those four fields. Let me go to Company and here I have four fields. I'm going to cut the code from here and paste under the base entity followed by now I'll go to Contact and here I'm going to delete the code related to createdAt and createdBy. Similarly, let me delete the code related to updatedAt and updatedBy. So now we moved the code into a separate class. On top of this class, I'm going to mention @Getter and @Setter Lombok annotations, which are going to generate the getter and setter method for these fields. As a very important next step on top of this BaseEntity class, I need to mention an annotation with the name@MappedSuperclass. Let's read the documentation of this annotation. So whatever class that we are marking with this annotation, we're telling to the framework that a class itself is not an entity class, but whose mappings are inherited by the entities which extend it. A mapped superclass is not a persistent type and is not mapped to a database table. The persistent fields and properties of a mapped superclass are declared and mapped using the same mapping annotations used to entity class. However, these mappings are interpreted in the context of each entity class, which inherits the mapped superclass. I know this is super complex to understand. Let me explain you in simple language. Whenever we are using this annotation, we're telling to the framework, please don't read these BaseEntity as an entity class. On top of that, we're also telling whatever class which is extending these BaseEntity for that child entity class, please consider these Java fields. For example, if I go to the Company entity class and here, if I extend the BaseEntity class by typing extends BaseEntity, now, whatever fields that I have defined under the BaseEntity, they will be mapped to the Company's entity. So with that framework, what it is going to do? The framework, it is going to look for the columns with the name createdAt, createdBy, updatedAt and updatedBy inside the company's table. Let's do the same inside Contact entity class as well. Here also, I'm going to mention extends ase entity. BThis way, from now onwards, for all the entity classes that we are going to create, we'll make sure that it extends BaseEntity on the database table side as well. We need to make sure that these four metadata are audit related columns are present. And in fact, in real enterprise applications, this is a standard that developers will follow the database administrators. Usually they don't approve to create a table without any audit related columns. Now, even after making these changes, our code should work without any issues. I can save all these changes, do the build once a build is completed. From the postman, I'll try to invoke these API contacts REST API, where I'm trying to send this request data. As soon as I click on this send button, you'll be able to see I got a 201 created response. I can go and check inside the database table as well. In the contacts table, I have this new record inserted. Let me delete the rows which have some invalid data. I'll select these three rows and click on this delete rows option, followed by I will press the submit button. Without all the rows that have some invalid data are deleted. So far with the changes that we have done, we optimized our entity related code by 50%. As a next step, we should also look for an opportunity on how to hand over the populating of these audit columns to the Spring Data JPA itself, so that we can avoid writing these createdAt and createdBy or updatedAt and updatedBy logic inside our business logic. Let's discuss about the same in the next lecture.