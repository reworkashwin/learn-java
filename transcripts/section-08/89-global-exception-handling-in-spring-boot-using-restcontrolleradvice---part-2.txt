I'm going to invoke my REST API one more time this time with these changes. But before that, let me make sure I'm throwing a Runtime Rxception here. Yes, I'm throwing. From the postman, I'm trying to invoke the API this time you will be able to see, we are sending the response properly as a JSON object with the details like what is the apiPath, errorCode, errorMessage, and along with the errorTime. On top of that, we are also sending the overall HTTP status also as 500. From where we are getting this response? From the GlobalExceptionHandler. So whatever logic that you have written here, this is being executed by the framework are you seeing the power of spring boot? With this GlobalExceptionHandling logic, I don't have to worry about the try and catch blocks in any of my controller layer. And in fact, I don't have to worry even inside my service layer and data layer as well. Because whenever an exception occurs at the data layer, it will be elevated to service layer from service layer it will be elevated to the controller layer. Off course, there'll be some valid scenarios where you may want to catch the exceptions and handle them based upon your business logic inside the service layer or in any other layer. In those special scenarios, you can go ahead and add the try and catch blocks manually. This time I'm going to throw the NullPointerException instead of RuntimeException. If I update this to NullPointerException, the logic present inside the second method will be executed. I'm invoking the same REST API you'll be able to see and that the error response I got a message saying that a NullPointerException occurred due to so-and-so exception. This way you can write any number of methods inside this class and these methods, they can handle various kinds of exceptions. You have a scenario where for a given exception, you want to handle the exception in a different way then adding a method and handling corresponding exception is the advisable approach. We can also define these methods that marked with the exception handler within the controller class as well. For example, here, if I keep the method like this inside the controller class itself, then my GlobalExceptionHandler will never get invoked. Maybe I'll show you a demo as well. Let me throw RuntimeException here and from the business logic of the method, I will send the exception message as exception from controller class due to so-and-so exception. If I do the build and trigger the API, this time you'll be able to see I'm getting the exception from the controller class. This should give a confirmation to you that always the controller specific exception handler methods will have highest priority compared to the GlobalExceptionHandling logic. But mentioning these methods inside the controller is not required unless you want to handle the exceptions for a given control in a different or in a unique style. I hope you are clear. Let me remove all these methods and I'm going to revert all the code that we have written inside the controller class. I will remove these through statement as well. From now onwards, anytime an exception occurs inside our backend application, this logic will be executed. I'm going to keep this NullPointerException related method for your reference. Off course it is not required because we're not doing anything uniquely for these two different exceptions. I'm just keeping these for your reference so that you are aware that we can write multiple methods that are capable of handling a specific exception. If you're not handling a specific exception, then this first method will be executed because it handles all kinds of exceptions why because I have mentioned exception, which is the parent of all exceptions inside Java. The details and the annotations that we have discussed around the GlobalExceptionHandling in Spring Boot I have mentioned them in these slides for your reference. Always remember there are two important annotations. The first one is @RestControllerAdvice, followed by @ExceptionHandler annotation. Inside the next slide, I also provided a sample code snippet on how you can write global exception handling logic.