I'm going to create a new package with the name scopes and inside this package I will create a java class with the name RequestScopedBean. On top of this class, to convert this into a Bean, I need to mention @Component annotation followed by to mark this Bean as a request scope, I need to use an annotation with the name @RequestScope. Let me mention @Getter and @Setter as well. Inside this class, I will try to create a String field with the name as username. Next I will try to create a constructor. Inside the constructor, I will try to have a system.out.println() statement. Using this statement, I'm going to print out a message RequestScopedBean created. In the similar lines, I'm also going to create SessionScopedBean as well. Let me copy this class and paste it inside the same package. I will rename the class name to SessionScopedBean. To mark this Bean with a session scope, I need to use @SessionScope annotation. Remaining everything, I'm going to keep it same. Now to show you the demo of both these RequestScope and SessionScope, I'll try to create a REST API. Under the same scopes package, I will create a class with the name ScopeController. On top of this class, I'm going to mention RestController followed by @RequestMapping. To this annotation, I'm going to provide the path as forward slash scope. Next I'm going to mention @RequiredArgsConstructor followed by I'm going to do the auto-wiring of these two beans into this controller. So first let me type private final RequestScopedBean followed by private final SessionScopedBean. So with this now, both these two beans are injected into this controller. Next I'm going to create a REST API by using @GetMapping. To this annotation, I will mention the path as request. Let me define the method as well, the method is going to return Response Entity of String. The method name will be testRequestScope and this method, it is not going to accept anything as an input. Inside this method, I'm going to paste couple of lines of code. First by using this requestScopedBean, I'm trying to invoke a method setUserName using which I'm trying to set this name John Doe. The same I'm trying to return as a response from this API. So inside the body, I'm sending the username details. You know what is the response we are going to get. So this is about the test request scope. In the similar lines, I'm going to create one more REST API, which is going to help me to test the session scope. That's why I'm going to update this path to session followed by I will update this method name to testSessionScope, and here, instead of using RequestScopedBean, I'm going to use the SessionScopedBean. And using the same, I'm going to read the username details and send as part of the response body. Towards the end, I'm going to create one more REST API. If something is not clear, don't worry, when I'm showing you the demo, you will get to know why I'm creating all these REST APIs. To this new REST API, I'm providing the path as test and the method name will be testScope(). And first what I'll do is, by using the RequestScopedBean, inside this new REST API method, I'm not going to populate any user information. Instead, I'm going to simply read them from the beans. So first I'll try to read by using the RequestScopedBean. After making these changes, I'm going to do the build once the build is completed, I'll go to the postman. Here there is a folder with the name WebScopes. Under this folder, please open the TestScope request. Using this request, I'm trying to send HTTP get() method request. The path is /api/scope/test. As soon as I click on this send button, what is going to happen? The logic inside the test method is going to be invoked. So here we're trying to read the username information from the RequestScopedBean. Let me open the console and delete this. As soon as I click on this send button, you will be able to see I received an empty response. But inside the console, we are seeing a statement saying that RequestScopedBean is created. If I try to click on this send button multiple times, each time I'm sending a request, a new bean of RequestScopedBean is getting created. So this gives a confirmation that the bean of this class is getting created for each request. But why I'm not able to read any username information? Because by default, when the bean or object of this class is created, we are not setting any username information into the object. That's why we are getting empty value. But if you try to invoke the RequestRest API, here we are setting the username to JohnDoe and the same we are trying to return from this method. If I go to the RequestScope and here, if I invoke the API by using the HTTP GET, I'm going to get JohnDoe as an output. So that means whatever bean that is getting created when I'm invoking this API to the same bean, I'm trying to populate the username information by using the setter method and the same I'm trying to read at later point of time by using the getter method. Now, if I invoke the testScope, still I'm going to get the null or empty as an output because this is a new or different request. That's why a new RequestScopeBean will be created. And since it is not going to have the username details by default, we are getting the empty response. Now let's see the behavior of sessionScope. So what I'll do here is, inside the testRest API, instead of reading from the RequestScopedBean, I will try to read using the sessionScopedBean. Let me save these changes and do the build. The console also, I'm going to clear it so that we can see what is going to happen. When I'm sending the request to my web application, the sessionWebScopeBean is going to be created when I'm sending the first request as part of my session. For example, here, if I go to the sessionScope and invoke this API by using the HTTP get() method behind the scenes, a Bean of sessionScopedBean is going to be created. Here I need to update the message inside the constructor to sessionScopedBean created. Let me do the build and clean the console as well. I'm sending the request for the first time, and as soon as I click on the send button, you will be able to see a sessionScopeBean is created statement here. On top of that, we are also getting the name value because inside this REST API with the path session, we are populating the username with the John Doe. Now what I'm going to do is, I will try to invoke the test REST API and inside this method, we are using the sessionScopedBean. Do you know what is going to happen behind the scenes? Since we are sending the request as part of the same HTTP session, the same existing SessionBean will be leveraged by the framework and when we invoke this getUsername, we are going to get the John Doe information which we have set while we are processing this REST API logic. If I go to the test scope and try to send the request this time, you will see I'm getting the John Doe as an output. Because I'm sending the request as part of the same session. In case if you want to start a new session inside the Postman, you can click on this cookies and delete this JSESSIONID. Followed by now, if you try to send this test REST API request, you're going to get the null as an output. Because while processing this REST API method, the Bean of session scope is going to be created, but we're not populating any username information. That's why we are going to get the empty value. So if I click on this send, you'll be able to see I'm getting an empty value. But if I invoke the session scope and invoke again test scope without deleting the sessionId I'm going to get the John Doe as an output. So this is giving a confirmation to us. As long as your HTTP request is belongs to a same HTTP session, the Bean is not going to be created again and again. Instead, it is going to use the same Bean. As part of the next lecture, let's try to test the application scope as well.