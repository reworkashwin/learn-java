We, as developers, we have a very bad habit. Do you know what is that? It is overconfident. We always be overconfident that our code is not going to have any bugs and it is going to work in all the scenarios. But that's not how the reality is going to be when you deploy your application into production there can be unlimited remote scenarios that will pop up and may result into runtime exceptions. That's why as a good backend developer, it is our responsibility to handle all the exceptions properly when someone invokes our backend REST APIs, we need to make sure we're handling the exceptions and returning a proper error response so that the client applications, they can understand what is the problem and accordingly they can follow the next steps. So far, we have developed a couple of REST APIs inside our backend application. By the end of this course, we are going to develop at least 50 REST APIs. That's why before we start developing more and more REST APIs in the coming sections, I want to explain you on how to handle the exceptions that are happening inside your REST APIs. Once the exceptions are handled, I'm also going to explain you on how to send a proper error response to the client applications. Since we are into the new section, I need to create a new section folder in my workspace. But before that, let me stop my running backend application and close this project. Inside my workspace, let me create a new folder and into this folder, I'll copy the code from the previous section. I open the new project inside IntelliJ again. Let me open the one of the controller class. Maybe I'll open the ContactController that we have developed in the previous section. Let's imagine my REST API method is throwing some runtime exception. What I'll do is towards the end, I'm going to add throw new RuntimeException and to the constructor, I'll pass a message, it's a bad day. So this is my exception. Since we have the return statements here, this throw block will never reach that's why we are getting this compilation error. Let me comment all this code for a few minutes. Whenever someone invokes my contact REST API, they're going to get this RuntimeException. I'm going to start my backend application followed by from the postman, I'm invoking the contacts REST API. As part of the RequestBody, we are sending a proper JSON object. As soon as I click on the send button, I am getting 404 not found. Somewhere the framework is throwing this unrelated error, which is 404. By looking at this error, what do you think that client developer will think about it? The developer may think that there is no REST API with this path that's why I'm getting 404. But if you look at the backend logs, we are having a runtime exception with the message, it's a bad day. Here, there is a clear mismatch on what is happening on the backend and on what we are communicating to the client. To overcome this problem, we can handle the exceptions in our REST API methods. For example, here I can write a try block and into the try block, I can move all these code followed by, I can have a catch block. So let me delete all this code next I'm going to add the catch block inside the catch block I'm going to return a message saying that request to processing failed due to, and to this, I can add the, whatever exception message that I'm receiving to the catch block. On top of that, we're also sending the status as internal server error, which is going to send 500 as a HTTP status to the client application. So let me do the build once a build is completed, I'm going to trigger the same REST API. This time, if you see, we are sending some meaningful information to the client. The status is 500 internal server error. And in the response body also, we're sending the exception message saying that request processing failed due to it's a bad day. This is one style of handling the exceptions. If you have a hundred different REST API methods, you need to visit all of them and make sure you are handling the exceptions by using the typical try and catch blocks. But do you think it is a viable option to write that try and catch block in all the REST API methods? Of course not in future, if you want to make any change inside the exception handling, you need to visit all the places and make the changes again. It's a very cumbersome process. Instead, we have a better approach as well in the Spring ecosystem. We can write a exception logic that is applicable for all the REST APIs inside our application. Let me show you how to do that. But before that, I will try to revert all the code that we have written inside the ContactController. I'm reverting these, but I'll keep this throw new RuntimeException for a few more minutes because I want to show you the power of Global Exception Handling. To implement the exception handling globally, I'm going to create a new package with the name exception. Inside this package, I will create a new Java class with the name GlobalExceptionHandler. The class name can be anything. On top of this class, I need to mention an annotation @RestControllerAdvice. With the help of this annotation, we are telling to the Spring Boot framework, anytime if there is an exception occurring inside my controller classes or inside my controller layer, please execute the exception handling logic that I have written inside this class. So that's what we are trying to communicate with the framework. If you open these annotations and read the documentation on top of these annotations, the framework team, they clearly highlighted that this annotation is a shortcut annotation that combines @ControllerAdvice with @ResponseBody. So instead of mentioning both these annotations, we can mention the single annotation @RestControllerAdvice. By default, @RestControllerAdvice applies to any controller, including the classes that marked with @Controller and @RestController. The only difference between this @ControllerAdvice and @RestControllerAdvice is, when you are using @RestControllerAdvice, you're going to send the error response by using a JSON format. Whereas, if you are simply using @ControllerAdvice, you have the capability to send the error details by using an HTML code. But since we are developing a pure REST API backend project, we need to use this @RestControllerAdvice. Next inside this class, we need to create a method. But before that, let's try to create a new DTO class using which we can send the error details to the client applications, regardless of how many REST APIs that I'm going to have inside my application, I'm always going to send the error details in a uniformed structured way by using this DTO. So inside that DTO package, I'm going to create a record class with the name ErrorResponseDto. Inside this class, I'm going to have four different fields. The first one is apiPath. Under this field, we're going to send the REST API path details that is invoked by the client application. Next under the error code, we are going to send the HTTP status code followed by error message and when the error happened. So these are the four important details that I'm going to send to my client application using a JSON object by using the object of this ErrorResponseDto. With this, I'm telling to my client applications, anytime inside my application, if there is some runtime exception happens, you can expect the error response in the form of these four fields. Based upon these four fields, they can write the logic on the client side to handle the error responses that my backend is sending. Let's go to the GlobalExceptionHandler here I'm going to define a method which is going to return ResponseEntity of ErrorResponseDto and the method name, I'm going to keep it as handleException(). Again, the method name can be anything. Into these exception object, the framework is going to provide the runtime exception details that occurred. By using the same, I can populate the ErrorResponseDto. But apart from the exception details, I am also going to mention WebRequest as a second parameter, since I'm looking to send the REST API path as well to the client to understand what is the REST API path that is invoked by my client application in the current request, I need to use these WebRequest object. So by the time this method is getting invoked by the framework in the scenarios where the runtime exception happens, the framework will take care of injecting the object of Exception and a WebRequest into this method. Inside the method, I'm going to write a very simple logic as you can see, I have created a object of ErrorResponseDto while creating the object to the constructor. I pass the API path details by invoking the getDescription() method from the WebRequest object. To this method, I'm passing the false because I don't want to include any client information like session ID. I simply want to include what is the request URI. Next I'm trying to pass the HttpStatus as INTERNAL_SERVER_ERROR followed by exception details by invoking the exception.getMessage() method and towards the end, I'm passing the current date and time. Once we have the object ready, I'm trying to send the same by using the ResponseEntity to the ResponseEntity constructor we can pass the DTO class along with the status code. This is one style of sending the ResponseEntity object. If you open the constructor, as you can see, it is accepting the ResponseBody along with the HttpStatusCode. Once we have the method ready, on top of this method, you need to mention a very important annotation. The annotation is @ExceptionHandler. And to this annotation, you need to mention what is the exception data type that you want to handle. In case if you want this method to be executed only during the NullPointerException, then you need to mention NullPointerException.class on top of this method. So with this, what is going to happen? The framework will execute this method in the scenarios of only NullPointerException. But we don't want that to happen we want this method to be invoked for all kinds of exceptions. What is the parent of all exceptions inside of Java ? It is the Exception class itself. That's why here I'm trying to mention Exception.class. This way you can define any number of methods inside this class. But please make sure all these methods, they are handling different type of exceptions. For example, I can create the same method one more time, but this time I will mention it as NullPointerException.class and the method name, I'll mention it as handleNullException(). In the scenarios of NullPointerException, this method is going to be executed to differentiate the response that gets generated from these two methods. Maybe what I can do is, along with the exception message, I'll send a string saying that a Null pointer exception occurred due to so-and-so exception message. I hope you're clear with the changes that we have done so far. Inside the next lecture, let's validate all these code changes.