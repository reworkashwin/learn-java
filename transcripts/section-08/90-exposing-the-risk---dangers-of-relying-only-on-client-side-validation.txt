The REST APIs that we are building on the backend side, they accept a lot of data from the client applications or from the end user. For example, if you consider this contacts related REST API, this API is accepting the data in the form of ContactRequestDto. If you open this DTO, you will be able to see that the client applications are the end user when they want to invoke this REST API, they can send various kinds of information like email, message, name, subject, and user type. Here there is an important best practice that we need to follow when we are accepting data from the client applications. This best practice is around the input data validations. So far, we are accepting whatever data that my client application is sending. But while developing real enterprise applications, we can't accept any junk data or invalid data from the client applications. That's why as a next step, we're going to understand on how to enforce the validations on the data that we accept by using the REST APIs. But before we try to implement the data validations on our REST APIs, first, let me show you some demo on what is happening with the current setup inside our application. There is a REST API with the name contacts from the postman I can invoke these API and send these data. And with that, we are going to get the successful response. But what if there is a scenario, one of the client application or one of the end user, they are trying to send some invalid data or junk data. This they might be doing intentionally or unintentionally, regardless of what is their intent we end up accepting the invalid data. Let me show you an example. Instead of sending all this valid data under name, email, userType, subject, and message, I can send some empty data and with this request, if I click on the send, my request is still processed and I received 201 created. With this, what happened ? We end up saving invalid data or junk data inside our database. If you go to the database and try to query the contacts table, you will be able to see there is a new record inserted with all empty data. So subject is empty, name is empty, message is empty and the email is also empty. This is not acceptable in enterprise applications. Let me show you another flavor of this scenario. Let's imagine one of the client application or one of the end user, they are not sending the entire name field. If I delete these, and if I click on the send button, the client application or the end user, they are going to receive this 500 internal server error. Inside the error message, there is a detail saying that the name attribute under the contact entity is received as a null. But since this is a not null column on the database, that's why this error came. This is what this error message conveys. But do you think this technical information can be easily understood by the end user or by the client applications? Off course not. That's why as a backend developer, it is our responsibility to send with a meaningful error code and error message to the client applications so that they can display the same information to the end user. With the logic right now that we have, we are sending 500 internal server error. Instead, we should send 400, which conveys bad request is received along with the details of the fields which have invalid data. We're going to implement all this logic in a few minutes. Here you may have a question on why should I worry about the data validations as a backend developer? Because there will be always a client application or an UI application which is responsible to accept the data from the end user. I think it would be nice to hand over this data validation job to the client application or to the UI application. This can be your question. I agree with you, but there are some drawbacks if you simply go with the client-side validation. Let me show you a demo around that as well. This is our job portal UI application. From the footer, we can navigate to the contact page and here, if someone is trying to submit this form without filling the data, they are going to get these errors like name is required, email is required, subject is required and a message is required. But there is a drawback with the client-side validation or the UI validation. Any smart technical end user, they should be able to disable these client-side validations. For example, I can press F12 and go to the developer console here under the sources and our workspaces I can search for the file Contact.jsx. So this is the place where I have written all the UI related validations. Any technical person by spending a few minutes on the UI code that they are able to see here, they should be able to disable the validations. Let me scroll down here inside the validate form function I'm checking whether the form data.name.trim is not null or not empty. If it is empty, I'm going to throw this error name is required. Let's imagine I removed these and save this file. And this time, if I click on this send message, my form is not throwing any error around the name because I disabled the validations around the name. That means it is going to accept the empty data for full name as well. I can do the same for email required as well. Let me delete all this email related logic and next I'm going to delete the subject related logic. Let me do the same for message related logic and save all these. This time, if I click on this send message, you'll be able to see the data went to the backend and since the backend is throwing some runtime error, we saw the submission failure error. Why the backend logic throwed an error? Since all these fields are marked as mandatory on the database side, while inserting the null values under these columns, the database throwed an exception and the same is sent to the UI application as well. But what if there is a scenario where we have not marked our database columns as not null? In those scenarios, we will end up accepting all null data or junk data into our applications. On top of that, if we don't enforce the backend validations, we are not giving a good usability behavior to the end user. If they are seeing some RuntimeException in the UI page, that's not going to make any sense. Instead, we should make sure to display some user-friendly messages to the end user. I have some funny memes around the client-side validations. This is what is going to happen when you rely on the client-side data validations in an enterprise application. The client-side validations can easily be disabled or tampered. That's why we should always make sure there is an extra layer of backend validation on the backend side as well. This is another meme of client-side validation. Here the person, he secured the bicycle, but anyone should be able to lift the bicycle and take it away. Let's go to the last image. This image also conveys what is going to happen when we rely on the client-side validations that we built by using JavaScript. I hope you are clear on what is the problem that we are trying to fix. From the next lecture, let's try to make some backend code changes to enforce the data validations.