Inside real enterprise applications, developers, they will end up developing hundreds of REST APIs. And these REST APIs can be invoked by various kinds of people, like third-party API developers or UI app developers or mobile application developers or QA team members who are trying to test these REST APIs. Obviously, all these developers and QA team members, they will not have any clue about your REST API. They may have too many questions like what is the request that I'm supposed to send? What is the response that I'm going to get? What kind of validations that a REST API has? What are the various response status codes that a REST API can send? This way, they may have hundreds of questions around the REST APIs. To solve the doubts or queries of all these people, you may follow a very basic approach. For example, you may organize a meeting and to this meeting, you can invite all these developers and testers where you're going to explain about the REST APIs. The other approach is you can try to document about the REST APIs and the documents can be shared to the other developers or QA team members. While these basic approaches, they are going to help you to avoid questions from the other people, but they are not going to help you in 100% of the scenarios. Many times new people will be onboarded onto the team and a such scenario, you can't schedule a new meeting for all the new members that are joining. Similarly, sometimes you may update your existing REST APIs with the new enhancements. Whenever you're making enhancements to your REST APIs, you can't go and update these documentation every time this is going to be a cumbersome process and some way to make these approaches to work without any issues, you will end up doing a lot of coordination with other team members. Most of the times you will end up answering the questions from the other team members. And with that, you're going to have a feeling like I'm exhausted and I can't take any more questions. At the end of the day, the job of the developer is to focus on the business logic, but not to entertain all this kind of drama. That's why I'm going to teach you a better alternative approach on how to document our REST APIs by adding a simple starter project into the Spring Boot, so that anytime anyone has a question around your REST API, you can simply redirect them to a URL where they can open and understand more details about the REST API. Enough of talking let me show you a demo. There is a website with the name springdoc.org If you scroll down on this website, they are recommending to add a dependency to the Spring Boot application. This is the dependency when we add this dependency behind the scenes, what it is going to do is, it is going to analyze all the REST APIs that you have defined in your backend application. Based upon this analysis, it is going to generate OpenAPI specification. There is a standard called OpenAPI. By using this standard only, all the REST APIs has to be explained in a standard or a consistent approach. If you go to the website with the name Swagger.io, this Swagger team, they have developed a lot many tools around the OpenAPI specification. I'll come to this website at a later point of time. But right now, let's try to understand on how to generate the OpenAPI specifications for our REST API. As a very first step, we need to add this dependency. Inside the pom.xml, I'm going to add the dependency with the name springdoc-openapi-starter-application. Let me save this and sync the maven changes. After adding this dependency, I just have to do the build and restart my application. Inside the console logs, you can search for Swagger and you will be able to see there is a statement saying that swagger-ui.html endpoint is enabled by default. To disable it in production, we need to set a value false to this property. So let's try to access this HTML page. I will type localhost 8080 forward slash swagger-ui.html. As soon as I press enter, I am receiving this error saying that no static resource swagger-ui.html. I know why this error is happening. Long back inside the webconfig.java, we made a code change to append this prefix for all the paths that we have inside our application. The same prefix APA is getting added to this Swagger-UI.html as well by the framework. That's why to make this work, after the port number, we need to mention the prefix api followed by forward slash. Now if I press enter, I'm going to have a beautiful documentation about my REST APIs. If you don't want to follow all that api prefix related drama, you can directly open this URL swagger-ui/index.html. If I go to the new tab and access the same, I'm able to access without any issue. Let's try to understand what do I have inside this page. There are details about my controller classes. For example, there is a ContactController, which is exposing two different APIs with these paths. One supports GET method, the other one supports POST method, followed by there is a CompanyController which is exposing this REST API with the GET method. By looking at this information, anyone should be able to easily understand what are the different REST APIs that my application is exposing. In case if they want to understand more details about any REST API, they can click on the same and here it is going to show them what are the input parameters that this REST API it is accepting and how they can expect the response. So the response they can expect in this form. These details are getting populated from the CompanyDto that we are trying to return as a response from this REST API. If we open this CompanyDto, we have all these fields like Id, name, logo, industry, size, rating and so on. If you see the sample example value here, there is a JSON array is mentioned and inside this array, a object with these fields will be written. So this kind of information we are giving to the other developers or QA members so that they can easily understand what is the response format that they can expect. So here there is an option also to try it out. If someone click on this, it is going to show the execute button. As soon as I click on this, you will see it is going to show the what is the curl command that they can execute, followed by what is the response body and the response headers that the REST API return. So they have an option to download the response as well. So by looking at this response, anyone should be able to easily understand that this is a JSON array and inside this, there are multiple objects which are carrying companies related data and at each Company object, we are going to have details like ID, name, logo, industry and so on. And this is the status code that they can expect. Let's go to the other REST API for example, if I click on these api/contacts that supports GET method here, it's clearly highlighted that these API, it accepts a query param with the name status, and this is a mandatory, whereas coming to the response, they can expect a String value as an output. If I click on this, try it out and it is going to allow me to populate a value. So let me type open here and as soon as I click on execute, you'll be able to see I am getting the expected output. In case if they're not sending some valid data, they are going to get the error saying that value must be at least four characters. So this information is coming based upon the backend validations that we have implemented. Are you seeing the power of OpenAPI and Swagger? OpenAPI is a standard that everyone has to follow to describe their APIs. By providing that OpenAPI to the Swagger tool, we should be able to see the OpenAPI specification details visually inside of a page. I have a separate dedicated course around the OpenAPI specification and Swagger. If you are a developer who works on REST APIs a lot, then my recommendation is to enroll into that course as well, so that you can understand in and out of OpenAPI specification and Swagger tools. With the code changes that we have done so far, your Spring Boot application, it is going to expose this UI page. Anyone who is asking questions about your REST APIs, you can simply ask them to access this URL so that they can try to understand visually about the REST APIs. But what if there is a scenario you can't share this URL outside of your organization? So there might be some firewall restrictions. Outside your organization network, no one can access your application. In this kind of scenarios, there is a workaround. Let's imagine one of the third-party API developer who is working in different organization. You want to know about your REST APIs. To answer their questions, what you have to do is, you have to click on this small hyperlink that you have under this OpenAPI definition. As soon as you click on that, a JSON file is going to be open. You need to save this. I'm going to save it as swagger.json. Once the file is saved, you can share the JSON file to the third-party API developer over an email. Once the JSON file is received, the third-party developer, he can go to the swagger.io website. As I said before, the Swagger team, they have built many UI-related products that help developers in understanding the OpenAPI specifications. The third-party API developer can look for a product with the name Swagger Editor. As of today, if we can scroll on this website, under the footer, we have a link to the products. There is a product with the name Swagger Editor. You can click on the same, which is going to open this page. And here there is an option to try the Swagger Editor. As soon as I click on this, a page will open with the URL editor.swagger.io. So by default here, they are going to show you some REST APIs related to some pet store. What you can do is, you can delete the content that you see on the left-hand side. Instead, you can copy this entire content and paste it here, which is going to show you this warning, whether to convert JSON to YAML. So here you need to click OK, so that the JSON will be converted into a YAML file. And based upon the OpenAPI specification content that you have provided, on the right-hand side, the developer can see the details about your REST API visually. There is an error, we can hide it. So these are all my REST APIs that we have discussed previously. This way, other third-party developers as well, who can't access your application URL directly, they should be able to visualize your REST APIs by pasting the OpenAPI specification that you have provided in this editor.swagger.io website. I hope you are clear on how to document the REST APIs by using SpringDoc OpenAPI library. Using this library in an enterprise setup is mandatory because it is a standard to explain our REST APIs by following the OpenAPI standards. If you are not happy with the default OpenAPI specification generated by this library, you also have an option to further improve the OpenAPI documentation by using various annotations from this library, the annotations like @Schema, @Tag, @Operation, @ApiResponse. This library supports all these annotations. I discussed about these annotations and how to improve the documentation by using these annotations inside my Microservice course. In case if you want to explore further about this topic, please enroll into my Microservice course. In the next slide, I have mentioned the steps that we need to follow. As a developer, we just have to add a single dependency, and that is going to take care of generating the documentation for us. We can access the Swagger UI by accessing this URL in our application scenario. We need to make sure to mention the api prefix, but this is the default path that the developers can access. In case if someone wants to access the OpenAPI specification document, they should be able to access the same by visiting this URL. Otherwise, they also have an option to open this link from the Swagger UI as well.