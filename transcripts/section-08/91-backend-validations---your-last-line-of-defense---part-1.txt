To get started with the backend validations by using the Spring Boot framework, first we need to add a starter project inside our pom.xml. Inside this website, you can search for a starter project with the name Validation. By using these, we should be able to perform some Bean validations with the Hibernate validator. We know inside backend applications, we are going to accept the data by using Java Beans. That's why they have used Bean validation here. Let me select these and click on Explore. Inside the pom.xml, you should be able to see there are two dependencies added, one with the name Spring Boot Starter Validation and the other one is related to the unit testing. I'm going to copy this first dependency, you need to copy the second dependency in case if you are writing some unit testing code. After the Lambok dependency, I'm going to add the new dependency and click on the Sync Maven Changes. With these, the new library related dependencies will be downloaded into my local Maven repo. Now I can go to the ContactController. If you closely look at the code that we have written, the REST API is accepting the data from the client applications by using the ContactRequestDto. So this is acting as a Java Bean object, which is carrying the data from the client application to our business logic. The client applications, they are going to send the data by using formats like JSON and in between the Spring Boot framework, by using the Jackson library, it is going to convert the JSON format data into the Java objects. That's why by the time this REST API is invoked, all the data will be present in this ContactRequestDto. So while transferring the data from JSON to the Java object, we need to enforce the validations. The starter project that we have added previously, it is going to take care of performing the validations, but we need to give some clues to the library on what validations it has to perform. By default, it can't assume some validations and enforce them. The framework always looks for the details from the developers on what kind of validations it has to enforce. That's why as a developer, we have to define some validations. For example, if I want to make this email field as mandatory, I can mention an annotation @NotEmpty, instead of not empty, we can also mention @NotBlank as well. So there is a minor difference between @NotEmpty and @NotBlank. Let me explain the difference. If you open this @NotBlank and click on this download sources, you will be able to see when we are adding this annotation on top of a field, the data inside the field must not be null and must contain at least one non-white space character. Whereas if you look for the documentation of @NotEmpty, you'll be able to see that when we are using this annotation on top of a field, it is only check if the given field data is not null and not empty. That means if an end user is sending some empty white space, then this not empty will not throw any validation because according to this annotation logic, even a white space character is considered as a valid value, but with the not blank, it is not going to consider the white space character as a valid character. That's why I always recommend you using @NotBlank instead of @NotEmpty, because at the end of the day, we don't want to accept only simple white characters from the end users. We want to accept some meaningful character data inside our fields. Apart from mentioning this annotation on top of the email field, we can also define a message attribute to this annotation. Under this message, I'm providing Email cannot be empty. So with this, I'm telling to the framework, whenever someone sends an empty email value or null value under this email field, please send this message as a validation error message to the client applications. What client applications are going to do by taking this message, they're going to simply display the same to the end user. And since this message is end user friendly, the end user can easily understand about the problem and correct the issue on the UI side. In the similar lines, we can mention for the other fields as well. Let me bring all these fields to next line. I'm going to copy this @NotBlank on top of all the fields. After copying, we need to update the message as well. Take this line to next line. So now I need to update the message here it should be message cannot be empty and here it should be name cannot be empty. And here it should be subject cannot be empty, followed by user type cannot be empty. Apart from these @NotBlank annotation, we have many other annotations that we can use based upon our requirements. Let me open this @NotBlank and here on the project window, I'm going to click on the select to open file. And with that, it's going to show me the file along with the package where it is present. So this not blank file is present under the package jakarta.validation under this chart. If you see under the constraints package, we have many other annotations like @Email, @Future, @AssetTrue, @NotNull, @Past, @Pattern, @Size. I request you to explore all these annotations inside the course based upon our business requirements I'm going to use majority of these annotations. So here there is an annotation with the name @Email. If you open these and read the documentation, it says the value under the field has to be a well formatted email address. Since we are accepting some email related data, what I can do is apart from @NotBlank, I can also mention @Email annotation followed by I can mention the message as invalid email address. This is the message that I want to send to the client applications. For the message field, I'm going to mention @Size annotation. So the minimum, I'll mention it as five followed by maximum, but the maximum, I'll mention it as 500 characters for some reason. If someone is not sending a message in between five and 500 characters, I want to return a validation message saying that message must be between five and 500 characters. Let me do the same for name as well. I'm going to copy these and mention here for the name, I'm going to restrict the maximum to 30 characters followed by I need to update the message to name must be between 5 and 30 characters. For the subject also, we can mention the @Size annotation. So let me mention the @Size annotation here for the subject. Maybe I'll mention the max as 150 characters. And here, let me update this to 150 characters along with the field name. So subject must be between five and 150 characters. Coming to the user type, the end user has to select one of these three values. In case if I'm receiving a value other than job seeker, employer, and other, I want to reject the request. For these kinds of scenarios, we have an annotation with the name @Pattern. By using these annotations, we should be able to enforce any reject style of validation patterns. So let me invoke a property with the name regexp to this I'm passing the expression as job seeker, pipe symbol, employer, pipe symbol, other. So with this reg expression, I'm saying the user type field should have any of these three values. In case if the validation fails, I want to return a message saying that user type must be one of job seeker, employer, other. Do you see how easy it is to enforce the validations by using these annotations? Once you define all your validations inside your DTO class, as part of the next step, you need to go to your controller class. Just before the ContactRequestDto, you need to mention one more annotation with the name @Valid. By defining this annotation, we're telling to the framework, when someone invoked this REST API, please make sure to perform the validations that I have defined in these DTO class. So I'm going to communicate with these @Valid annotation. There might be a scenario where you are accepting the data using the same DTO, but you don't want to perform the validations. Under such scenarios, you can simply get rid of these @Valid annotations and with that, the framework is not going to perform any validations. Let me save these changes and do the build once the build is completed, I'm going to restart my application because since we have added a new starter dependency, we need to make sure to restart the entire application. Once the application is restarted from my postman, I'll try to invoke the REST API by sending empty data under all the fields. As soon as I click on send button, you will be able to see I'm getting 500 internal server error. But this time under the error message, we have some good useful details. For example, the bean that is trying to accept the data like ContactRequestDto, it has nine errors. So what are those nine errors? If you read this error message patiently, you can easily identify all the nine errors. For example, for the field userType, the value that received is empty. That's why it is throwing some validations like NotBlank. So here there is a message userType cannot be empty. In the similar lines, you will be able to see all the validation error messages that we have provided. But sending validation error message like this is not going to be helpful to the client applications because for them to understand this error message and extract all the meaningful information from this error message is going to be a super complex process. On top of that, we're trying to send the status as 500 internal server error, which is not a good practice. Instead, we should send 400 error. From where this response is being populated. If you go to the Global Exception Handler and keep a breakpoint here, the request is going to stop here. When we are sending a bad request, let me click on the send button. You'll be able to see the breakpoint stopped here and this time the exception type is MethodArgumentNotValidException. So this gives a confirmation to us whenever some validations are failing the framework is going to throw this exception. That's why what we can do is as a next step, we can define one more exceptionHandler() method which handles this MethodArgumentNotValidException. And as part of this new method, we can build different handling logics so that we can send a clear and meaningful information to the client applications around the validation failures. Let's work on the same in the next lecture.