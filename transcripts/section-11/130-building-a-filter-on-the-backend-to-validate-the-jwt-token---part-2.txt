When we create a filter inside Java, the filter logic is going to be executed for each and every request, because the filter will try to intercept each and every request. But do you think we need to execute these validator logic for each and every request inside our backend application? Off course not because inside our backend application, there are some public APIs as well. When a public API is being triggered, obviously the client applications they are not going to share any Jwt token. Under such scenarios, whatever logic that we have written here will not be executed because we have a null check here. But still executing this method is unnecessary. That's why Spring Framework, what it has done is, it is giving a flexibility to the developers whenever you are creating a filter by extending the OncePerRequestFilter, you can define your logic by using which the filter execution will be skipped if this method is returning boolean value as true. So the method name is shouldNotFilter(). That means whenever we are returning a true value from this method, then the filter will not be executed. So when we want this filter not to be executed, we want this filter not to be executed for the publicPath. So that's why I have injected public paths bean at the top of this class. So this is the bean that I'm trying to use. By using these bean, I'm trying to invoke the stream() method because inside these publicPaths, we are going to have a good amount of public path details. All of them I'm trying to stream(). If any of the path is matching with the given request path, how do I know the given request path ? By using these Request object, I just have to invoke the getRequestUri(). And that is going to give me the current request path that is invoked by the client application. If this path is matching with any of the publicPath that we have defined, then it is going to return true. And with that, my filter is not going to be executed. Whereas for the secured path, this filter, it is going to return false value. And with the false value, my filter logic is going to be executed. I will keep a breakpoint inside these methods so that we can debug line by line when we are seeing the demo. Now let's try to answer the next question that we, you may have on why I'm using these AntPathMatcher object. If you go to the shouldNotFilter() method here, I'm using the AntPathMatcher object and using the same, I'm invoking the match() method. There is a good reason on why we should use the match(). If you go and check the PathsConfig here, sometimes we are defining the paths by using the regex patterns as well. With these kinds of regex public paths, we can't use traditional equals() or contains() methods available under the String. Instead, we need to use the match() method available under the AntPathMatcher. This match() method, it is going to consider the regex pattern that we have under the publicPath. If the regex matcher is matching with the given path, it is going to return true, otherwise it will return false. I hope you're clear with this shouldNotFilter() logic. Now as a next step, we need to inject our filter into the spring security filter chain. For the same, we need to go to the JobPortalSecurityConfig class. Here, in the place where we have defined all our security configurations. We should be able to invoke a method with the name addFilter(). There are multiple methods like addFilterAfter() addFilterBefore(). In our scenario, we want to use the addFilterBefore() method. I'll explain you the reason on why we should use the before method. This method, it is going to accept two filter input parameters. The first one is what is a filter that we want to execute before the second parameter filter. Let me provide the object of our own filter. We can create the object of our filter by using new operator and invoking JwtTokenValidatorFilter. To the constructor we need to provide publicPaths been as a input next as a second input parameter to this method. I'm going to mention BasicAuthenticationFilter.class. I know you have too many questions here. Let me explain what is happening with this line of code inside spring security we know whenever someone is trying to invoke a secure REST API, the request will be interpreted by this filter, which is BasicAuthenticationFilter. Inside this filter, we have a method with the name doFilterInternal(), where it is going to extract the credentials from the request followed by it is going to execute these if condition. So inside this, if condition, if you closely observe, it is checking if the authentication is required or not by using the username field. Let me open this authenticationIsRequired() method inside this method. The framework, it is checking if there is any Authentication object inside the security context. If yes, it is going to return the Boolean true from this method. Otherwise it is going to return false. With the configurations that I have done, what I'm telling to the spring security framework is before you try to execute these BasicAuthenticationFilter, please execute the logic that I have in my JwtTokenValidatorFilter. This is how we can inject our own custom filters into the spring security filter chain. Inside our filter, what we are doing? We are extracting the JWT token received and if the JWT token is valid, we are creating the Authentication object by using the username and roles. The same we are trying to set into the security context. So once after this logic is executed, the filter chain will be moved to the BasicAuthenticationFilter and the logic is going to come into these authenticationIsRequired() method. And this method is going to return the true value. And with that, whatever actual Authentication logic that is going to be triggered by these BasicAuthenticationFilter, it is not going to be triggered. Simply this filter will assume that the authentication is already completed and I need to transfer the request to the controller layer. Are you seeing the power of spring security? Let's try to debug these entire code line by line. I'll try to save these changes, do the build. Once the build is completed, I'll go to the postman. From the postman first, I'll try to get a JWT token by invoking the login public API. When this path is invoked, the validator filter is not going to be executed because this is a public path. If I click on the send button, the request is stopped inside the shouldNotFilter() method of our validator filter. Let's try to debug this line by line under the path we have the complete path api/auth/login/public. Since public paths has a matching given path, this method, it is going to return true as a Boolean value. And with that, the business logic around the validation of the JWT token is not going to be executed. If I release a breakpoint, you'll be able to see the breakpoint stopped inside the auth control, but not inside the validator filter. So here we are trying to generate the JWT token. Let me release the breakpoint. I'm going to copy this entire JWT token value. Now I'll try to invoke one of the secured REST API. I already have one such REST API. I'll go to the Company and here we have GetAllCompanies_public. As of now, inside the backend, we mark this API as a secured API. While I'm invoking the secured API, I'm not going to provide the credentials. This time I will provide the JWT token. So to provide the JWT token, I had to select bearer token here, and I need to simply mention that token value here. After mentioning the token value, if you go to the headers, you'll be able to see there is an authorization header. And if you try to see the value, the bearer space prefix is added before the JWT token value. The postman is doing all the job for me because bearer token is a standard inside the industry. Using the same, It has added this prefix. Now if I click on this send button, the breakpoint is going to again stop inside the shouldNotFilter() method. This time the request path is secured path. So that's why this method, it is going to return false value. And with that, the request is going to be stopped inside that do filter internal method of the validator filter. So here, first we're trying to read the value inside the authorization header. So this is the entire value. From this value we want to extract the JWT token value by using the substring. Let me debug this line by line. So this is my complete JWT token value. I'm going to debug this entire code. If there is no issue, we will end up creating a Authentication object. Let me keep a breakpoint at line number 58 and release the breakpoint. As you can see, we have successfully created an Authentication object, inside this Authentication object we have principle as madan and with the authorities and with authenticated as true. After this validator filter is executed, we know the next filter that is going to be executed, which is BasicAuthenticationFilter. I'll keep a breakpoint inside the doFilterInternal() method. If I release my breakpoint inside the validator filter, the breakpoint right now stopped inside this filter. So let's try to debug these filter line by line. So here the authRequest is null because we are not sending any credentials inside the request. Whenever the auth request is null, simply this filter, it is forwarding the request to the next filter. Even if some reason, if we are sending the credentials inside the request, still this filter is not going to be executed because this authenticationIsRequired() method is going to return false value. I can prove you that as well. Let me release this breakpoint and inside the postman, you will be able to see, I still got the successful response, even though I'm not sharing the credentials. This time, let's try to share both credentials and the bearer token. So I'll go to the auth and here I will select the basic auth and send the username as madan followed by Madan@123. When we add these basic authentication, we are going to have a header with the same name authorization and with the value as basic space encoded value. But with this, we have a problem. We can't send our Jwt token value by using the same header name. Maybe what I'll do is I'll go to the application constants here. Instead of mentioning authorization, I will mention it as authorization one. Let me save these changes, do the build. Once a build is completed under the postman, I will mention a header with the name authorization one. And here coming to the value, I need to mention bearer space, the Jwt token value. This is my Jwt token value. Let me copy this entire value and mention it here. Now I'll click on this send button. Let me release the breakpoint under the shouldNotFilter(). Now the breakpoint stopped inside my validator logic. This time the framework, it will try to read the header value by using this header name. If I debug this line, you'll be able to see I have the value that I shared. Let me release this breakpoint. This time the authRequest object is not a null object. That's why this if condition is not going to be executed. So from the auth request, it is going to fetch the username. The username is madan, with the username, it will try to identify whether the authentication is required. So since the authentication is not required, the if condition is skipped and simply it is going to forward the request to the next filter. If I release the breakpoint inside the postman, you will still see that we got the successful response. I hope you are understanding all the Spring security concepts that I'm trying to explain. Let me revert the changes that I have done here. I need to go to the application constants, here I need to mention authorization followed by under the job portal security config. I will disable the form login. So how to disable that? I just have to pass the lambda expression using which I have invoked the disable() method. Before I try to close this lecture, let me share one more important information. As of now, inside the AuthController, we are generating the JWT token once the authentication is completed. Why we have written this logic here? Because we are triggering the authentication manually. Let's imagine you want to generate the JWT token even when the authentication is triggered by the Spring security framework automatically. In that scenario, what do you have to do? You just have to go to the JobPortalSecurityConfig here you just have to invoke addFilterAfter() method. To this method, you need to provide your own custom filter that you have written to generate the JWT token followed by you need to mention the BasicAuthenticationFilter. Whenever you are using addFilterAfter(), your filter logic will be executed after the BasicAuthenticationFilter logic. When the Spring security framework is performing the authentication automatically, we know the authentication logic is going to be completed inside this BasicAuthenticationFilter. By the time this logic is completed, we're going to have the Authentication object stored inside the security context. From the same security context inside your JWT token generator filter, you need to extract and execute all the logic that we have written inside the JwtUtil So here I'm manually invoking this generate token from the controller. But in the scenario that we are discussing, you need to create this class as a filter and with that, the framework is going to execute all your logic. But for our setup, that is not required. I thought of sharing this information. I discussed about these addFilterAfter(), addFilterBefore() and addFilter() methods in detail inside my Spring security course. Let me remove these and save these changes and do the build. There are no compilation errors and we are good. So far, inside this section, we updated the Spring security logic to store the user credentials inside the memory of the application by using the InMemoryUserDetailsManager, followed by we also created a REST API, using which the client applications can trigger the login API. From this API, we are sending the JWT token that is generated. The same JWT token can be leveraged by the client applications to trigger any other secured API. We have also taken care of validating the JWT token using this filter. As a next enhancement, we should start storing the user details inside the database, followed by we should also build a REST API, which is going to allow the client application to trigger the registration operation. So inside the UI, we are going to have a registration page where the end user will enter all his credentials. From the registration page, our registration API will be invoked and the user details will be saved into the database. Next on the UI, we are also going to have the login page, when the end user entering his own details, the authentication should happen by looking into the database table. So this is our next major enhancement that we are going to work as part of the next section. Once we are done with that enhancement, we can try to test end-to-end flow by using the UI application as well. For now, don't try to test our backend application from the UI code because we have not implemented any registration flow. I hope you are happy with the progress and excited about the upcoming sections. If you are liking this course, please take a minute and provide your valuable feedback.