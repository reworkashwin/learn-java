As of now inside our application, we configured a single user with the name madan by using all these properties. But we can't build a backend application by having a single end-user, instead we should look for the next advanced topics using which we can try to create multiple users. Let me stop my running application. Since we are into the new section, I'm going to create a new folder and into this folder, I will try to copy the backend code from the previous section. We're not going to make any UI related changes. That's why I have copied only the backend project. Let me open the new project using my IntelliJ. Under section 11, I have a project with the name job portal, the same I'm trying to open. First what I'll do is, I'll get rid of all these three properties followed by I'll go to the JobPortalSecurityConfig. Here also, I'm going to get rid of all these commented code. As a next step, I'll try to create multiple users. But before that, we need to configure a PasswordEncoderBean inside our project. We know there is an interface with the name PasswordEncoder and to this, we have various implementations. . As of today, Spring security team, they are recommending to use the BCryptPasswordEncoder. Let me click on these download sources. Inside this class, all the hashing related logic is implemented by using the BCryptHashing algorithm. If you scroll down inside this class, you will see a logic where they are trying to generate the hash value. So this is the method, encodeNonNullPassword. Here if you try to understand the logic, first they are trying to generate a random salt value by invoking this getSalt() method. So once the random salt value is available, behind the scenes they are invoking a hashpassword() method available under the BCrypt library. So to this hash password, they are providing both the raw password that the end user provided during the registration, along with the random salt value. So this is what is going to happen during the registration phase. But coming to the login scenario, this method is going to be executed. This matches non-null method, it is going to accept the rawPassword that is entered by the end user, followed by the encodedPassword, which is stored inside the storage system like database or inside the memory. By using both these values, the framework team, they are calling the checkpassword() method available inside the BCrypt library. To the same, they are passing both the rawPassword and the encodedPassword. So by the time this method executes its logic, it is going to return the boolean value, which conveys whether the authentication is successful or not. Let's try to use this encoder inside our project as well. So what I have to do is, I have to create a bean of type PasswordEncoder. So let me create a method which is going to return bean of PasswordEncoder. The method name is passwordEncoder() itself. From this method, I'm simply returning the object of BCryptPasswordEncoder. So with this, I'm giving instructions to the Spring security framework from now onwards whenever someone is trying to register a new user or whenever someone is trying to log in with the credentials, please leverage the BCryptPasswordEncoder. In case if you are looking to use some other PasswordEncoder, you just have to create the object of those specific classes, like a SCryptPasswordEncoder, but I'll go with the recommended PasswordEncoder. So once we have this PasswordEncoder ready, I'm going to create one more bean. So let me mention @Bean this time, the method that I'm going to create, it is going to return the object of UserDetailsService. So let me mention the method name also as userDetailService(). We know what is the purpose of this UserDetailService, this UserDetailService, it is going to have a single abstract method with the name loadUserByUsername. Using this method only, the framework it is going to retrieve the user details from the storage system by providing the username. So there is also an another child interface for this interface with the name UserDetailsManager. Inside these interface, we have various methods that supports createUser, updateUser, deleteUser, and so on. By creating a bean of type UserDetailService, we are telling to the Spring security framework. From now onwards, whenever you want to fetch the user details, or when you want to create and store the user details into the storage system, please leverage the bean that I'm providing. Now, let me create some users, inside Spring security we have a user class, which represents an end user. Using this, I need to invoke the builder() method followed by username() method, to this username() method I will pass the username as madan, followed by the password to this password() method I need to provide the hash value to provide the hash value What we have to do? We have to invoke the encode() method available inside the PasswordEncoder. Let me invoke the passwordEncoder() method that I have here. So this method is going to return the object of BCryptPasswordEncoder using the same I'm invoking the encode() method to this encode() method I can pass my password like Madan@123. Next I'm going to invoke the roles() method to this roles() method we can provide multiple roles details. If you open these roles() method, you will be able to see it is accepting multiple roles information. Here we are able to see a spread operator. For now, let me mention a single role to this user and towards the end, we need to invoke the build() method. So the output from these code, I will try to assign to a variable user1. In the similar lines, I can create any number of users. So this time I will try to copy the same code and rename the variable to user2. Coming to the username, I will mention it as admin and the password, I'll mention it as admin@123. Let me mention that role as admin. This way we can create any number of users. As of now, I'm good with that two users. But in your project, if you have a requirement to create 10 different users, you can follow this approach. Off course, this approach is not a production standard we need to allow the end user to register themselves by using the registration page. We will eventually go there but before that, I want to show you all the possible options, because sometimes you may build some low priority applications or some demo applications where you want to show the demo by creating some users inside the memory of the application. Under such scenarios, this approach is going to be super helpful for you. Since the required number of users are created, we need to create the object of InMemoryUserDetailsManager. Since right now we are looking to store the user details inside the memory of my application, that's why I have to create the object of this class. And to this class constructor, I can provide any number of user details. Since right now I have only two users, I'm providing both of them. And towards the end, I need to return the object of this class as an output from this method. This object will become the Bean because we are using @Bean annotation here. So with this now, what we have done? We have created a Bean of type UserDetailsService inside our application. From now onwards, whenever we are trying to log in by providing some credentials, the DaoAuthenticationProvider, it is going to leverage our own Bean that we have created. This is going to happen at the step number 5. Coming to the password comparisons, the framework, it is going to use the BCryptPasswordEncoder. And that is going to happen as part of the step 6. I'll also show you that demo as well by debugging the framework code. But before that, let me explain you one of the problems that we have with this code. So with this code, we are hard-coding our passwords directly. That means anyone who has access to our source code, they should be able to know our passwords. So this is not a recommended standard. Instead of providing the plain text value to the encode() method, what you can do is, you can convert this plain text password as a hash value and provide that hash value as an input to the password() method. How to generate a hash value from this plain text value? It's very easy. What I'll do is, I will try to move this code to the above line and I will catch the output as var password1. And I'm also going to print out these password1 onto the console. I'm also going to do the same for second password as well. What is the second password? It is admin@123. Let me update the variable name to password2. Now when we are trying to start our application, definitely these values, they are going to be printed onto the console so that we can take those hash values and provide as an input to the password() method. So right now, I can't start my application here I'm getting a compilation error. Let me provide these as an input to the password() method. Let me do the build once the build is completed, I'm going to start my application. Inside the console, we will be able to see a couple of hash values. So this is my first hash value. Let me copy the same and I'm going to mention as an input to the very first password() method. So this way, we're going to avoid mentioning the direct password values. Even if someone has access to my source code, they can't figure out what is a plain text password. Let me mention the second hash value here as an input to the password() method. So with this, now I can get rid of these above code, let's try to test these code changes in the next lecture.