To test our changes, we should have at least one secure REST API. That's why what I'll do is, I will move these companies related REST API path to secured paths. Let me save these changes, do the build once the build is completed, I will open the DaoAuthenticationProvider. Inside these, we need to look for the methods retrieve user. Let me keep a breakpoint here. Next we also have another method with the name additionalAuthenticationChecks. And here also, I'm going to keep a breakpoint. From the postman, when I'm invoking these API, I'm going to provide my login details like username as madan and password as Madan@123. We know what is going to happen with this setup. Behind the scenes, first, my BasicAuthenticationFilter is going to be invoked. From here, the request will be forwarded to the ProviderManager from the ProviderManager, it will go to the DaoAuthenticationProvider authenticate() method. From DaoAuthenticationProvider authenticate() method, it will go to the retrieve user method. Once the user details are retrieved, the passwords will be compared under these additionalAuthenticationChecks. Let's validate the entire flow. I have removed all the breakpoints, except the two breakpoints that we have under the DaoAuthenticationProvider. As soon as I click on this send button, the breakpoint is going to stop under the retrieve users method. Here from this method, we are going to invoke the loadUserByUsername, which is available under the InMemoryUserDetailsManager. Let me open InMemoryUserDetailsManager. Here we have the method. Let me keep a breakpoint. Now I'm going to release a breakpoint from the retrieve user method. Now let's see what do we have under these users hash map. So if you open this users hash map, there are two users that are saved inside the memory of my application by using this map. So what is the first user? It is madan and the second user is admin. Under the value, you'll be able to see the passwords, but since the passwords are stored using the hash value, no one should be able to make any sense by looking at this value. So to this method, the framework is passing the username as madan. And with that, it will try to fetch the madan related user details from the hash map by passing the username to the get() method. If I try to move to the next line, you will be able to see under these user object. We have a single user with this password and the username is madan. And coming to the authorities, this user has only a single authority with the role as USER. That's what we are able to see here. I'm going to share more details around the authorities and roles in the coming sections. For now, let me release this breakpoint, which will stop the request under the additionalAuthenticationChecks. . So here first it is trying to check whether the authentication.getCredentials is null. Since it is not, the if condition is not going to be executed. Next, let's see what do we have under the presented password. This is the password that is provided by the end user. Both these presented password and the hashed password that we have under this user details object. Both of them, we are going to send to the matches() method available in the BCryptPasswordEncoder. Since we have configured BCryptPasswordEncoder being inside our project, the matches() method present inside the BCryptPassword Encoder will be executed by the framework. I can also show you that. Let me open BCryptPasswordEncoder. Inside this, we have this matches() method. Let me keep a breakpoint here. As soon as I release the breakpoint, the request is going to start inside this method. This checkpassword() method will be called internally and it is going to return a boolean value and if the boolean value is true, the request will be forwarded to the controller layer. And with that, I'm going to get the successful response. Let me test another user as well. What is the other user that we have? It is admin. Let me mention the password as admin@123. If I click on the send button, the breakpoints, they're going to stop. Let me release all of them inside the response this time also, I'm getting successful output. I hope you are clear on how to configure multiple users inside the memory of the application by creating your own Bean of InMemoryUserDetailsManager. While you are creating the Bean of this class, you can provide any number of user details as an input. Please also make sure to create a PasswordEncoder Bean, such as BcryptPasswordEncoder. The sample code snippet that we have discussed, I have mentioned them in this slide for your reference. The hash value that I have inside this screenshot will not be same with your hash value because though we are providing a same plain text value, behind the scenes, the Bcrypt it is going to generate a random salt value. And with that, we are going to get different, different hash values for the same input.