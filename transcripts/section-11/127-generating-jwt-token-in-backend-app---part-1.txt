Whenever a client application is triggering this login API, from the Java method, we are triggering the Spring Security authentication mechanism manually. Once this authentication is completed, as a next step, we need to generate the JWT token, so that the same JWT token we can share to the client applications as part of the response from this REST API. And whatever JWT token that we are sharing, the same will be reused by the client application for all the upcoming requests. There are two important components that you need to build while you are trying to adopt JWT tokens inside a backend application. The first one is generating the JWT token during the login operation. And the second component, which is going to take care of validating the JWT token that is shared by the client application for all the upcoming requests. Unless the JWT token is validated on the backend side, we should not allow the request to the controller layer. So first, let's try to focus on generating the JWT token as part of this login REST API. You might be thinking generating JWT token is a very complex process we need to take care of header, payload, digital signature, hashing, and so on. But believe me, it's very easy. We just have to use a good library. If you go to the mavenrepository.com website, which is the Maven Central Repository website, inside this website, you need to search for the library with the name JWT. So J means Java, Java JWT library. As soon as I press enter, you will be able to see multiple results. To generate and validate the JWT tokens, we need to add three dependencies. The first one is JJWT-API followed by IMPL and Jackson. Don't try to use the second one because this is a legacy one. They also highlighted here as of today, the latest version is 0.13.0. If you click on this, you'll be able to see the Maven related dependency that you need to add. In case if you are using Gradle build, you can click on this tab, which will show you the Gradle related dependency. I'll go to the Maven and I'll copy this value. Let me go to pom.xml. Here just after the validation dependency, I'm pasting the first dependency followed by, I'll go to the second dependency. Here also I need to click on the same version. This is the Maven dependency. Let me copy this. The same I'm trying to paste it here. Let me do the same for third dependency as well. The third dependency is related to Jackson. I'm copying the details and pasting here. After pasting all these three dependencies, you need to sync the Maven changes. Now by using this library, we can try to generate the JWT token. So the logic of generating the JWT token, I can write directly inside these AuthController, but that is not recommended because it is going to make my controller code clumsy. That's why what I'll do is I'll try to create a separate package under the security. Let me create a sub package with the name util. Inside this util package, I will try to create a class with the name JwtUtil. On top of this class, I'm going to mention @Component annotation so that I can inject this class as a dependency into the controller layer. Next I'm also going to mention @RequiredArgs constructor. Now inside this class, let me create a method with the name generateJwtToken(). And this method, it is going to accept the Authentication object as an input because by using the JWT token, we want to pass the user details to the client application, like username, roles details. That's why, unless I have this Authentication object in my hand, I cannot know who is the logged in user. So this Authentication object we are going to get after the login operation. So whatever result authentication variable that we have, it is the Authentication object that is going to be provided by the Swing Security Framework once the authentication is completed. So inside this object, we are going to have all the required details. Let me go to JwtUtil here first I'm going to create a variable with the name JWT token, the same I'll try to return from this method. In between these two lines, I need to write all the logic. So here I'm getting this error. Let me initialize this to null value. Now, before I try to write the core logic of generating the JWT token, I should also create a secret inside my backend application. If you recall our JWT discussion, to generate the signature for our JWT token, we need a secret value that needs to be stored on the backend. So by using the secret only, the hash value will be generated based upon the content of header and payload, the same hash value we are going to send under the signature. That's why first, let's try to create a secret inside our backend application. Secret is nothing but a random secret value, which is known to only your backend application. The secret value should not be shared to the client applications, because if the client applications came to know about the secret value, they should be able to tamper your token based upon their requirements. I'll try to create a new package with the name constant. So let me create the package under the job portal. So the package name will be constants. Inside this package, I will try to create a constant file with the name application constants. I want to use these applications constants file only to store the constant fields, but not any other Java methods or any other business logic. That's why what I'll do is inside this class, I will try to create a private constructor with the name application constants. If someone is trying to create the object of this class, they are going to get this error. This way, I'm trying to stop anyone from creating the Java methods or Java logic inside this class. I'm strictly going to use this class to maintain the constant fields. So this is the standard approach that usually enterprise developers, they follow to enforce other developers from using these constant files for writing any business logic. Here we have two safety mechanisms. The first one is I mark the constructor of this class as a private, so that no one can create the object somehow, if they manage to invoke the constructor by using concepts like Java reflections, still, they can't create the object because from the constructor logic, I'm trying to return an error. Let me mention a couple of constants here. The constants are JWT-SECRET-KEY. This is the constant and this is the constant value. And this is that default secret value that I'm trying to use inside my backend application. For any production applications, we should not maintain the secret value directly inside the Java code. Instead, we need to inject the secret value by using environment variables approach. In case there is no secret value injected by using environment variables, I want to use these default secret value. So that means for lower environment, we are going to use this default value. Otherwise, whatever value that is being injected by using the environment variable, the same we are going to use as a secret to generate the JWT token. Now I'll go to the JwtUtil and here I'm going to paste few lines of code so that we can discuss all of them one by one. You can get the same code from the GitHub repo as well. Let me select this enter method and replace with the new code. As you can see here, first, I'm trying to inject environment bean into the JwtUtil. You need to import this environment from the package with the name org.springframework.core.env. So please make sure you are importing from this package only. There are other environment classes as well available under the Hibernate and other libraries. Please don't import invalid environment. The reason why we are trying to inject these environment bean as a dependency into this class is, as I said before, we should have a flexibility inside our backend application to inject the secret value by using an environment variable. So that's why by using this env, I'm checking if there is any environment variable with the property name as JWT_SECRET, if there is any present, I'm going to consider that. Otherwise, whatever default value that we are maintaining inside the constant, I'm going to consider the same as a secret value for the JWT token generation. So this secret value can be any value, but you need to use the same secret value both for the JWT token generation and the JWT token validation. Don't try to mention simple secret values like 1, 2, 3, 4, 5. Always try to mention a complex string, which is very tough to guess by the client applications. Once the secret value is available under the secret variable, as a next step, we need to generate the secret key object. So to generate the secret key object, we need to invoke hmacShaKeyFor() method under the keys. And to this method, we need to pass the secret value as a Byte[] array. So here, if you see, this is going to accept as a Byte. This keys class is available under the library that we have added at the starting of this lecture. The output from this method will be a secret key object. So this secret key object, we are going to leverage to generate the signature for the JWT token. So next, by using this Authentication object, I'm invoking the getPrinciple() method. So this getPrinciple() method, it is going to give me an object of type User. We know inside Spring Security, by default, the logged in user details will be represented by using the user. The same will be stored by the framework under this principle. And the same I'm trying to catch here by using this typecasting. If you open this getPrinciple(), it is going to allow to store any object. By default, the Spring Security framework, it is going to store the User object. Since the return type from this method is object, I need to do this typecasting here. So from this fetchToUser(), we should be able to know what is the current logged in username by invoking the getUserName() method. The next line is very important so by using the Jwts class, I'm invoking the builder() followed by issuer(). By using this issuer() method, we can define the details of the JWT token issuer. Since we are trying to build a backend for job portal, I'm mentioning the issuer as job portal here. And usually inside the enterprise organizations, they are going to mention the organization name. Coming to the subject, you can mention any value. For now, I'm going with a simple subject, which is JWT token. Now I'm trying to invoke the climb() method. This climb() method, you can invoke any number of times. Each time when you invoke this climb() method, you should be able to store various kinds of information inside the payload of the JWT token. To this climb(), we need to mention what is the key. The key is username. To this username, I'm trying to assign the username value that I'm reading from these fetchedUser object. Apart from username, I also want to send the roles information of the logged in user. Off course, we should not share the password to the client application. That's why I'm not trying to send the password information. After the username, I'm trying to send the roles information by invoking the same climb() method. This time, the key is roles. Under these roles, I want to mention all the roles that a given logged in user has. So inside the Spring security framework, the roles information will be stored under the authorities of the Authentication object. The same I'm trying to invoke by using these getAuthorities() method. Since the logged in user can have any number of authorities, this getAuthorities() method, it is going to give us a collection object. The same way we are trying to stream and for each authority that we are receiving, we're trying to read what is the authority name by using these getAuthority() method available inside the GrantedAuthority. Inside the Spring security framework, the authority and the roles information, they are going to be represented by using the GrantedAuthority. If you click on these getAuthorities() method, it is going to return a collection of GrauntedAuthority object. Inside these, we have a method with the name getAuthority(), which is going to return the name of the authority or the role in a string format. We're going to discuss about authorities and roles in the coming sections. For now, I'm assuming you are clear with these high level information under the map() method when we invoke this getAuthority() method by using the GrantedAuthority object, we're going to simply get the name of the authority in the string format. So all such strings I'm collecting by using this collective method, and eventually I'm joining all the authority names by using comma as a separator. This way we can populate any amount of data inside the payload. As of now, we are only populating the username and roles, but as we are making progress throughout this course, we're going to populate various kinds of information of the user, like email, mobile number, and so on. Next we need to invoke the issuedAt() method. Using this method, we're telling about the date and time at which that JWT token is issued. After the issuedAt(), I'm trying to invoke the expiration() method. By using this expiration() method, we can set an expiration time for the JWT token. So to this expiration method, I need to mention some future date. So if you see first, I'm trying to calculate the current date and time to the same. I'm trying to add 24 hours time. So 24 hours into 60 minutes into 60 seconds into thousand. The reason why I have mentioned this thousand is whenever we want to add time to a existing DateTime object, we need to add the value by using milliseconds. That's why I'm trying to multiply this value with the thousand. So by using these value, I'm trying to add 24 hours time as the expiration time for my token. But in real enterprise application, this expiration time may vary based upon the business requirements. Few projects are going to set it for 60 minutes, few projects, they are going to set it for 24 hours. And similarly, one month, six months, one year, it's all depends upon the business requirements of the application. So once we have defined all these details, we need to invoke the signWith() method to generate the signature of the JWT token. To this signWith() method, we need to pass the secret key object that we have created at the line number 27. And towards the end, we need to invoke the compact() method, which is going to give us the JWT token value that is generated. The same we are trying to return here. I hope you are clear with the logic that we have written inside this JwtUtil. Let's try to integrate this logic into the Log and REST API in the next lecture.