We are going to use the same Hashing concept to store our passwords inside the storage system like database. As a developer who is trying to use the Spring Security framework, we never have to manually hash the passwords, instead we can rely on the password encoder implementation classes. Inside Spring Security, password encoder is a component which takes care of hashing the passwords based upon the hashing algorithm that we have selected. But before we try to use those password encoders, we need to understand certain details around hashing, how they are going to work, what are the loopholes that it has, and how to overcome those loopholes. As I said before, hash functions, it takes any data as an input. The input data can be a plain text value or it can be an image or it can be a video file. You can feed any kind of data with any size, regardless of what you are trying to provide as an input. The hash functions, they always going to produce a unique string of bytes as an output. If you provide the same input to the same hash function, it is always going to reproduce the same string of bytes or same hash value. The output of hash function is often called as digest or HASH. Here I'm trying to give an plain text value as an input to my hashing function. The output from this hashing function will look like this. If I try to give the same input again and again to the same hash function, it is always going to give me the same hash value. A hash function is going to transform input of any size into a consistent fixed length output. For example, 256 bytes in the scenario where we are trying to use SHA-256 hashing algorithm. One of the fundamental characteristic of hash function is, it is irreversible, meaning that it is not possible to derive the input value based upon the output. I can also give one real life example to remember these hashing function. Let's imagine this blender as a hash function to this blender you're trying to provide various fruits as an input. The fruit can be like watermelon, pineapple, apple, orange. You can provide a mix of fruits or you can provide a single fruit as well. When you provide a fruit as an input to this blender, which is like our hash function, it is going to give juice as an output, just like how our hash functions are giving a digest value as an output. Similarly, this blender, it is going to give juice as an output. If I give the same juice as an input to the blender, can it produce the fruits? Off course not. The blender cannot reverse that juice to a original fruit. So you can happily assume this blender concept is also very similar to hashing function. Just like how this blender cannot convert this juice to a fruit. Very similarly, by taking hash value as an input, these hash functions, they cannot derive plain text value. With this knowledge, you might be under the assumption, everything looks great on the hashing let's go ahead and use the hash functions right away. This can be your assumption, but there are certain loopholes that you need to fill when we are trying to use the hash functions. Let's try to understand what are those. In a typical web application flow, what is going to happen is, the user is going to enter the password during the registration process. The same plain text password will feed to the hash functions and the hash value will be stored inside the storage systems like database. During the login operation, what we are going to do, we are going to calculate the hash value again, based upon the value entered by the end user. If the newly calculated hash value is same as the already stored hash value, then it is an indication to us that the end user entered a correct password. Here I have mentioned some examples. There is a user with the name username, his password is 12345. When he provided this password, a hash value is going to be calculated and these hash password value is going to be stored inside the database. Let's imagine some other user also like pavan189 is also trying to use the same password during the registration process. Since the input to the hash function is same, for the pavan also, the hash value is going to look very similar to the johndoe hash password value. The same applies for other users as well. For example, here for mike and saanvi, they're trying to use a plain password with the value as password itself. Since the password values are same, their hash values are also going to be the same. If some hacker hacked my database, they can see the hash password values that we stored inside the database. By just looking at these hash values, they cannot really understand what is the plain text value, but they can try to use some techniques to understand what is the original text value that the end user entered during the registration process. Let's try to understand what are those techniques. One technique is Brute Force attack. As part of this technique, what is going to happen is, the hacker he will try to calculate the hash values for all the combinations of password. For example, he will try to calculate the hash value for a password like 1, 12, 123,1234,12345. . This way, he's going to calculate hash value for every possible combination of letters. At some point of time, he's going to calculate hash value for 1 2 3 4 5. With this, what is going to happen? He's going to get an hash value, which is very similar to the johndoe and pavan. And with that, hacker has a clue saying that that so-and-so users, they are using the password 1 2 3 4 5. Similarly, inside the Brute Force attack, the hacker is going to perform the trail and error. If someone is using simple passwords like 1 2 3 4 5 or password, by using all these most common possible passwords, the hackers, they're going to perform the trail and error. At some point of time, they're going to identify all the weak passwords used by the end user. That's why in critical applications, always what they're going to do is, during the registration process, they're going to enforce certain rules to set the password. Like the end user has to enter at least eight characters. They should be at least one capital letter they should be at least one number they should be at least one special char. By enforcing all these rules, we are going to encourage the end user to choose a complex password. When someone uses a complex password for the hackers to identify the hash value for such passwords, it is going to be really tough. Because one more beauty of hash values is, these hash functions, they're not going to be super quick to give the hash value. They're going to demand some CPU power. If the hacker wants to calculate the hash value for all the possible combinations, they need good amount of investment, like millions of dollars. That's why never try to use plain passwords while you are registering in any website. Apart from brute force attack, there is also a one more type of attack with the name Rainbow table attack or Dictionary table attack. In this scenario, the hacker what he's going to do is, he's going to calculate all the password hash values well ahead and he's going to maintain a table with a key like 12345 and its corresponding hash value. With this rainbow table in his hand when the hacker is able to access our hash to passwords inside the production database, they don't have to calculate the hash values again and again. They're going to simply perform the lookup operation. For example, they'll take this hash value and look into that dictionary table. The dictionary table, it is going to give clue to them that the original text value for so-and-so hash value is 12345. By simply hashing the password values given by the end user, sometimes the database will end up having that duplicate values as you can see inside this table, with these duplicate value, we are making the hacker job very easy. If the hacker can derive the plain text value for these hash password value, then his job is very easy. If any user who has the same hash value for all of them, the plain password is going to be 12345. We need to look for certain options to make the hacker life tough to derive the plain text value based upon the hash password value. Off course, we can't stop the hacker from using these Brute Force attack and Rainbow table attack to derive the plain text value. But by adding some complexity to the hashing process, we can make the hacker life tough. Let's try to understand how to do the same to overcome the challenges of Brute Force attack and Rainbow table attacks. What usually we need to follow is, when we are trying to feed our plain text password as an input to the hash function, along with the plain text password, we should also generate a random salt value. This salt value is nothing but some randomly generated text. For example, it can be Xefjgh. This can be a salt value. Whenever an end user is trying to register into a web application, he's going to provide the plain text password. Along with that, the code it has to generate a random salt value, which is unique to the end user. By using this plain text password, random salt value, along with the hashing algorithm, we're going to have a strong hash value. With this, what is going to happen is, even if thousand different end users, they end up using the same password, like 1 2 3 4 5 their hash value will never be the same because for each end user, the code is trying to generate a different salt value. This salt value is always going to be unique for each user. And with that, we solved a problem, which is maintaining same hash password values inside the database. By adding a simple salt value, we are making sure every user hash password value is unique, though their plain text password is same. Whatever salt value that we have used during the hashing function, the same salt value is also going to be stored inside the hash value at the starting point. The same I also highlighted here. The salt is typically stored at the beginning of the hash string and is used during the password verification process as well. Let's imagine one of the end user. He used a password 1 2 3 4 5 during the registration process. For this password, we are trying to generate a salt value, maybe for simple reasons. Let's assume the salt value is abc. Both these input values we are going to feed to the hash function. And with that, we are going to get an hash value. The hash value can be something like 4defa2f. Along with these hash values, we're also going to store the salt values as well inside the database. This will be the hashed password value that I'm going to store inside the database. Now during the login operation, the end user is going to enter the same password like 1 2 3 4 5. My hashing algorithm, what it is going to do is, it will try to read the salt value, which is abc and the same will be considered during the hashing process. And with that, we're going to get the same hash value, which is 4defa2f. Since these two values are equal, the hashing algorithm will give an output saying that the passwords are matching. In the scenarios where if the end user entered a different password, or if the salt value is different, obviously we're going to get a different hash value. And with that, the login operation is going to fail. When hacker gets hold of this value, he will know what is the salt value, but he needs to guess these passwords for each end user. Unless he provides 1 2 3 4 5 for these salts, he will not get the same hash value. For different end users, since the salt value will be different, you need to start the guessing process again from starting by considering all the possible combinations. This is how we are going to make hacker life tough by adding a random salt value to the plain text password when we are trying to generate a hash value with the help of hashing algorithm. All this process may seem to be very complex to you, but believe me my friend, since we are using Spring Security, this framework, it is going to provide some industry recommended password encoders that are capable of generating random salt value and leverage the password hashing algorithms like bcrypt. As a developer, you never have to worry of generating a salt value or forming the hash function. You just have to invoke the methods provided by these password encoders. I'll share more details of password encoder in few seconds. Before that, let's try to understand how the passwords are going to be validated with the hashed passwords. During the registration process, we know what is going to happen. The end user is going to enter the password like 1 2 3 4 5 to this password. We are going to add some salt value. The combination of plain text password and salt value will be taken as an input and the output hash value we are going to store inside the database. Now, during the login process, the user is going to enter the same password to this password, the password encoders, they are going to add the salt value which we stored inside the database. After adding the salt value, they are going to calculate the hash value again. If the newly calculated hash value and the stored hash value is same, then it is going to convey the login success, otherwise the login is going to fail. Inside the next slide, I have mentioned details around the password encoder. Password encoder is an interface inside the Spring security framework. It has two different abstract methods, which are very important. One is encode. This encode method we need to use during the registration process. When you pass a plain text password as an input to this encode method, it is going to generate a salt value. By using the same salt value, it is going to generate a hash value and the hash value will be the output from this method. The same hash value we are going to store inside the database. There is another method with the name matches. To this matches method, we need to provide two input parameters. The very first one is the raw password, which is entered by the end user and the second parameter is whatever hashed password that we stored inside the database. From this hashed password or encoded password, this matches method, it is going to derive the salt value and the same salt value will be added to the raw password and it's going to calculate the hash value again. If this newly calculated hash value is same as the second parameter value, then it is going to return the boolean as true, otherwise it is going to return false. It is also going to expose a default method with the name upgrade encoding. By default, it is going to return false. If needed, this method can be overrided to return true. When this method is returning the true as an output, the hashing process, it is going to be performed multiple times to make the hash value super tough to guess. Spring security framework also provides various implementation classes for this password encoder interface. The classes are NoOpPasswordEncoder and StandardPasswordEncoder, pbkdf2PasswordEncoder, BcryptPasswordEncoder, SCryptPasswordEncoder, and Argon2PasswordEncoder. The first two password encoders, they are not recommended for production applications. When you are using NoOpPasswordEncoder, your passwords are going to be stored using plain text value. In the similar lines, when someone is using StandardPasswordEncoder, it is going to use some legacy hashing function, which is not recommended based upon the recent advancements happened on the CPUs and GPUs inside the industry. When an application is using this password encoder, they are going to make the hacker's life easy because whatever algorithm that this password encoder uses, it is not super effective when a hacker is using latest CPUs and GPUs to calculate the hash value. Whereas the other password encoders, all of them are recommended password encoders. Whatever name you see like Bcrypt, Scrypt, Argon2 and pbkdf2, all these are the hashing algorithm names. As of today, Spring Security team is recommending to use the BcryptPasswordEncoder. The same we are also going to use inside our application as well. Regardless of which password encoder you're trying to use, the process of registration and login, it is always going to be the same. The way these algorithms hash the value is different. Other than that, all of them are secure and can be happily used inside the production applications. Inside this course as I said, we are going to use the BcryptPasswordEncoder.