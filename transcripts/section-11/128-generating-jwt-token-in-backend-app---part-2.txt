Let's try to integrate the JwtUtil into the AuthController. Before that, let me remove this null, because I'm getting an warning that it is an redundant. Now I'll go to AuthController and here I will try to inject as a dependency followed by I'm going to invoke the generate JWT token and to this I will pass the result authentication as an input. The output from this method, I'm going to catch it by using a variable with the name JWT token. The same variable I'm going to pass as an input to the constructor of LoginResponseDto. So as of now we are passing null as a JWT token. Let me replace this with the correct variable. So these are the only changes that we have to do on the AuthController. Let me save these changes to the build. Once a build is completed, I'll keep a breakpoint at this line, followed by, I'm also going to keep a breakpoint inside this method as well. Towards the end, I'm going to keep a breakpoint here. From the postman, I'm trying to invoke the REST API. I'm providing valid username and password. As soon as I click on the send button, the breakpoint is going to stop here. Let's try to see what do we have under this result authentication. So if you open these object under the principle, we have an object of type User. Inside this User object, we have username field, which is going to hold the current logged in username. Since under the principle, we have the User object inside the generateJwttoken() method if you see from the authentication() method, we are trying to invoke the getPrinciple() method, which is going to give me user object to the same User object. I'm trying to perform casting here. From these fetchedUser(), I'm invoking the getUserName() method, which is going to give me the logged in user details. Next, we can also find the roles information. If you go to the AuthController and look into these object one more time, under the authorities, you're going to have some authorities details. The logged in user, he has a role of user along with some other role. Let me release this breakpoint. And with that, the breakpoint should stop inside the JWT till, but it is not stopping. Let me check the console. Inside the console, I have an error saying that no class definition found error for these keys class. I forgot to restart my application. Since we added three dependencies, we should not rely on the dev tools. We need to do manual restart so that all the classes will be loaded properly into the class path. Once my application started, I'm going to invoke my REST API one more time. Let me click on this send button. I will release this first breakpoint and with that, the breakpoint is going to stop inside the JWT till as well. This is the JWT token that got generated. So if you go and check the raw, so this is how the JWT token, it is going to look like. All the details are going to be base64 encoded. This part is header followed by payload. And towards the end, we also have the signature as well. If you click on the JWT, it is going to show you the header details, payload details. So under the header, we have the hashing algorithm details that the library has used. Under the payload, we have the issuer, subject, username, roles, the roles information, they are separated by comma. Since we have two roles, both of them, they are mentioned here. Next, we have issued at time in milliseconds, followed by the expiration time. And towards the end, we also have the signature. Under this signature, we have the hash value. Let me release the breakpoint and go to the postman. So the postman, this time you'll be able to see, apart from message, under the JWT token, we are sharing the JWT token value to the client application. So next time when the client application, if they want to invoke the another secure REST API, they don't have to pass the credentials, instead they can pass the JWT token. I hope you're clear with the logic that we have written around the JWT token generation. As the next important step, we should also define the logic to validate the JWT token. Because once we share the JWT token to the client application, obviously they will start sharing the JWT token for all the subsequent requests. As of now, we don't have any mechanism inside our backend application to validate the JWT token. Let's try to do the same from the next lecture.