We want to trigger the Spring security authentication flow from these login REST API. To manually trigger the authentication flow of the Spring security framework. First, we need to create the bean of type AuthenticationManager. Let's go to the JobPortalSecurityConfig. So here just above this method, I'm going to create one more Java method, which is going to return the bean of type AuthenticationManager. So let me type public AuthenticationManager with the method name as authenticationManager() itself. From this method, I want to return the object of AuthenticationManager, but AuthenticationManager is an interface, but we know there is an implementation class for this interface with the name ProviderManager. So the same is being leveraged by the framework as well. When it is trying to perform the authentication using the automatic flow. The same class object we need to create when we want to manually trigger the authentication flow. So let me take this class name and here, I will just type return new ProviderManager. And with these behind the scenes, a bean of AuthenticationManager is going to be created. But this is not enough because we're simply creating the AuthenticationManager bean, but to these provider manager, we are not giving any clue on which DaoAuthenticationProvider it has to use and which PasswordEncoder it has to use and which UserDetailServiceImplementation it has to use. So all these clues we have to provide. From ProviderManager we know the authentication flow will go to the AuthenticationProvider. So that's why I need to create an object of AuthenticationProvider. So inside spring security, we have a predefined AuthenticationProvider available in the form of DaoAuthenticationProvider. In the coming sections, we are going to create our own AuthenticationProviders. For now, let's try to use these AuthenticationProvider, which is provided by the Spring Security Framework. Using these AuthenticationProvider, I need to invoke the method setPasswordEncoder(). To this setPasswordEncoder() I need to assign the output that I'm going to get when someone invoked this PasswordEncoder() method. So this PasswordEncoder() method, it is going to return the bean of type PasswordEncoder, the same I'm trying to provide here. Next in the similar lines, I should also provide what is a UserDetailServiceImplementation that these DaoAuthenticationProvider has to use. So we already have such implementation here. We created a bean of type UserDetailService behind the scenes, it is going to use the InMemoryUserDetailsManager to fetch the UserDetails and to store the UserDetails inside the storage system. So the same UserDetailService bean, we need to provide as an input to the constructor of the DaoAuthenticationProvider. So as of now, we are getting a error here because this constructor, it is expecting one argument. If you open this constructor, it is accepting a bean of type UserDetailsService. So what I can do is, from this constructor, I will invoke the UserDetailsService() method and with that, the bean of UserDetailsService will be provided as an input to the AuthenticationProvider. Once the AuthenticationProvider is created by providing the details around the UserDetailsService and PasswordEncoder, the same I can feed as an input to the constructor of the ProviderManager. So if I open these constructor, you'll be able to see this ProviderManager constructor, it is capable of accepting any number of AuthenticationProviders. As of now, I want to use a single AuthenticationProvider and the same I'm trying to provide here. So with this, we have created a bean of type AuthenticationManager. As a next step, I'll go to my AuthController and here I will try to inject AuthenticationManager bean as a dependency. Let me type private final AuthenticationManager with the variable name as authenticationManager. Now using this authenticationManager, I can invoke the authenticate() method. So to this authenticate() method, we need to pass the object of Authentication inside this Authentication object itself. We need to provide the username and password. If you open this authentication interface to this interface, there are implementation classes, but since we are trying to pass the credentials by using the username and password, we need to provide the information by creating the object of this class. So let me go to the controller and here to this constructor, I will pass the object of UsernamePasswordAuthenticationToken while creating the object of this class. I need to pass the username and password. I'm going to have the username and password that received from the client application inside this Dto. Let me use the same and invoke the username() method followed by password() method as well. So the output from this authenticate() method, again, we are going to receive in the form of Authentication object. Let me catch the same on the left-hand side by using a variable with the name resultAuthentication. So if the authentication is successful, we are going to have successful information inside this token. Otherwise, if the authentication is failed, the Spring security framework, it is going to throw a exception. That's why we should also take care of handling the authentication related or security related exceptions. What I'll do is, first I'll try to build a private method with the name buildErrorResponse(). So this method, it is going to build a response to the UI application, whenever the authentication fails. So I'm trying to send that response by using the LoginResponseDto inside this Dto, I will send the message, but coming to that UserDto and the JWT token, I'm going to send them as null values. So next I will try to keep all this logic inside a try block. And after this try block, I will try to have some catch blocks. Let me paste a few lines of code here. You can get these code from the GitHub repo as well. So whenever there is an exception happening during the authentication flow, the framework, it can throw the bad credential exception when we are sending the invalid username or password, or it can also throw an authentication exception if the authentication failed due to any other random reason. In case, if I'm not receiving any of these security related exceptions, I have the last exception catch block, which is going to send this message as a response. Yeah, you may have a question? Why you are not handling these exceptions by using the global exception handler? Let me answer the same. We're going to write our login logic only inside this method. We're not going to have multiple versions of this method, that's why I'm trying to handle the security related exceptions in this class itself. If you need to only rely on the global exception handler logic, if the same exception can occur at multiple controller classes, since that is not the scenario for this REST API, I'm going with this option. Let me also mention version details here. I will mention the version as 1.0. So with this, what is going to happen? From now onwards, whenever the authentication fails, we are going to send some unauthorized as an response. If there is some other exception, we're going to send internal server response. Let me save all these changes, do the build once the build is completed, I'll keep a breakpoint here. From the postman, I will try to invoke the same REST API under the body this time, I will try to send some invalid password. If I click on the send button, the breakpoint is going to stop here. If I release this breakpoint, behind the scenes, the Spring security authentication flow will be triggered. And since we send the invalid credentials, the framework throw the bad credential exception. If I release this breakpoint inside the response, you will be able to see I got the invalid username or password as a message with the 401 status. This time, let's try to send valid credentials. As soon as I click on the send button, the breakpoint is going to stop here. I will release the breakpoint. And with that, the request will move on to the next line. This time, since we passed correct credentials, the login operation is successful. That's why the request stopped in the next line. Now, if you go and analyze these resultAuthentication variable, you'll be able to see there is an information saying that authenticated is true. That means the given user is successfully authenticated. We also have username under this principal. Under this principal, we have the user object with the username as madan and with these authorities, we're not going to have password anywhere because once the login operation is completed, the Spring security framework, it is going to erase the credentials information from the request. It is doing this activity intentionally for security reasons. So now if I release the breakpoint and the postman, I'm going to have the successful response with the message as ok. I hope you're clear on how we are invoking the manual authentication. As a next step, we should also take care of sending the JWT token once the login operation is completed. As of now, we are sending the null value under the JWT token. There is a good history around the JWT token. From the next lecture, let's try to put some effort understanding about JWT token. Once we are clear about JWT token concept, we can come back and make the required changes in our backend application.