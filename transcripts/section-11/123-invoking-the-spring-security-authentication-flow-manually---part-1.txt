As of now inside our backend application, we have a couple of problems around the authentication. First, let's try to understand what are these two problems, followed by we can explore the best options that we can use to overcome these problems. The first problem is, right now the authentication logic is getting triggered by the Spring security Framework automatically whenever we are invoking a secure REST API. For example as of now, this company's public path is marked as a secured path inside my backend application. So whenever I'm trying to access this secured path by providing my username and password by using this Basic Auth standard, behind the scenes, what is Spring security framework it is doing? It is first trying to perform the authentication. Once the authentication is completed, then only it is forwarding the request to the controller layer. For enterprise applications, this is not a good standard to follow. We should not recommend to the UI developers saying that from your login page, please invoke any random secured API by passing the username and password using the Basic Auth standard. Instead, the recommended approach that we need to follow is, whenever an end user is entering some username and password in the UI login page, I'm talking about the login page that we are going to have on the react application, not the login page that is generated by the Spring security framework. So when an end user is entering the credentials in the react UI application, the UI developer, they are going to trigger a login related API. So these login related API, we should expose as a public REST API or a unsecured REST API and as part of the logic inside these login REST API, we need to trigger the authentication manually so that the Spring security framework will execute the authentication logic and provide the authentication result to us. If the authentication is successful as part of this login API operation, as a response to the UI application, we need to send a token. Why we need to send a token? Unless we are not sending a token, the UI application, they have to every time send the credentials to the backend application whenever they want to invoke a secured REST API. Where as with the token, that problem is not going to be there only during the login operation, the UI application and the end user they are going to provide the credentials, and once the login operation is completed, the backend application it is going to store a token value, which is specific to that end user and this token, it is going to have some expiration time as well. This token usually will be stored as a cookie on the UI side. Whenever the UI application is sending the next request to the secured page, the token value that's stored inside the cookie will be automatically sent to the backend application. And by looking at this token, the Spring Security Framework understand this fellow's login is already completed. I just have to validate whether this token is valid and if it's expiration time is not completed by doing all these validations, the Spring Security Framework will decide to allow the request to the controller layer or not. So this is the best practice that usually inside the industry that developers they follow. That's why as a next step, we're going to focus on solving both these two problems. First, let's try to build a public REST API that the frontend application can use to invoke the login operation. What I'll do is, under the job portal package, I'll try to create one more sub package with the name auth. Auth means authentication. Let me press enter. Inside this, I'm going to create a controller class with the name AuthController on top of these I'm going to mention REST controller followed by RequestMapping to these RequestMapping I'm mentioning the path as auth followed by, I'm also going to mention RequiredArgsConstructor because we're going to do some dependency injection in few minutes. Next I'm going to define a REST API that supports login operation. But before that, I need to create a few Dtos that supports my login operation. Let me go to the dto package. First, I'll try to create a class with the name LoginRequestDto, and I'll make this class as a Java Record. Using these DTO, I'm going to accept two fields from the UI application. So from the UI login page, the end user is going to enter the username and password. The same I'm trying to accept by using these DTO. Next in the similar lines, I'm also going to create another dto class with the name LoginResponseDto. This is also going to be a Java record class. By using these three different fields, I'm going to send a response to the UI application so that by using these response, it can display the next set of pages. Using message, I'm trying to share what is the overall status message, whether the login is successful or failed. And I'm also going to share current login user details by using this user DTO and towards the end, I'm also going to share the JWT token. JWT means JSON Web Token. As I said before, after the initial login operation, we need to provide some form of token to the UI application so that it can reuse this token for all the further invocations. I'm going to talk about JWT tokens in detail in the coming lectures. Let's try to create this user DTO class as well. I will try to create this as a normal Java class. Inside this DTO class, for now, I'm going to mention few fields. These fields are user Id, name, email, mobileNumber, role, company Id, companyname, and createdAt. So these are all the user related information that is required by my UI application. As of now, we don't have any mechanism inside our backend application to store all these details of the user. In the coming sections, we are going to enhance our backend application to store all the user related information in a given table. So all these user related information, we are going to ask the end user as part of the registration process. For now, let's not worry about these fields. They're going to make more sense to you in the coming sections. On top of this class, I'm going to mention @Getter annotation followed by @Setter. Let me also mention @ToSring annotation as well. So now my Dto classes are ready. Let me go to the AuthController here. I'm going to create a REST API that supports PostMapping. We should not use the get mapping because if we are using the get mapping, the UI application has to send the username and password by using the query params or the path variables, which is not a best practice because you will expose the end user details in the browser history. That's why it is a best practice to accept the credentials by using a DTO object under the request body. The same we are trying to do by using the PostMapping. The API path that this REST API method is going to support is login followed by public. Since we are looking to make these REST API as a public REST API, I'm mentioning these public as well. Let me mention the method name as apiLogin() and this method, it is going to accept LoginRequestDto as an input to receive the data in the form of this DTO object from the client applications. I need to mention @RequestBody annotation followed by, I'm also going to return the output from this REST API method in the form of LoginResponseDto. Let me type ResponseEntity and to this ResponseEntity, I will provide LoginResponseDto as an input. For now from this method, let's try to return some dummy LoginResponseDto. What I'll do is, first I'll try to create a UserDto object followed by, I will simply mention a return statement where I'm trying to return the response with the status as OK and under the response body, I'm sending the LoginResponseDto object. To this DTO object, I'm passing the OK ReasonPhrase as a first input parameter. Under this OK, we are going to have a ReasonPhrase with the name successful, the same we are going to return to the UI application. Now coming to the UserDto, I'm sending some dummy UserDto for now and the JWT token for now, I will send it as null. So this is what I'm trying to send as of now. Let me save these changes. Now I'll go to the PathConfig and here under the public paths, I need to mention login related path as well. So let me copy this entire path and paste it here. After the API, we need to mention auth followed by login forward slash public. So let me save these changes do the build once the build is completed, I'll go to the postman. Inside my postman, under the security folder, there is a request with the name Login. Let's open the same. This is going to invoke this REST API path by using the Http POST method. While invoking this REST API, under the body, we need to select the raw and JSON. And by using the JSON object, we need to provide the username and password. So the username is madan and the password is Madan @123. As soon as I click on this send, the REST API in the backend is going to be invoked and it is going to give me the response. So this is the response. The message is ok and under the user, we received all the null values and the JWT token is also null. So these are very first primary step that we have taken to build a login public REST API. So while I'm invoking this REST API, I'm not providing any authentication details. I'm simply sending my username and password as part of the RequestBody. Right now, even if these credentials are wrong, I'm still going to get that response because in the backend, we're not doing anything. We're simply accepting the credentials and returning a successful response, but we are going to enhance this REST API to perform the actual authentication. I hope you're clear on what we have done so far. Let's continue this discussion in the next lecture as well.