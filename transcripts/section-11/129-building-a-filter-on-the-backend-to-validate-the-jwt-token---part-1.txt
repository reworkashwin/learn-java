To validate the Jwt token provided by the client applications, we need to write our own filter. The purpose of filter on the backend application is, it is going to intercept each and every request and whatever logic that we have written inside the filter, the same will be executed. We know the Spring security framework also leverages a lot of filters to enforce the authentication and authorization. We're going to use the same filter concept to validate the Jwt token. Once we created our own custom filter, we're going to inject that into the Spring security authentication flow. It is very easy. Let's try to solve this problem step by step. I'll try to create a new package under the security. Let me create the sub package as filter. Inside this new package, I'll try to create a new Java class with the name JwtTokenValidatorFilter. This name can be anything. The first step that you need to take care while you're trying to create your own filter is, you need to extend a filter with the name OncePerRequestFilter. Whenever you're extending these OncePerRequestFilter, behind the scenes, the framework is going to take care of executing your filter logic only once per request. There are some scenarios where a request may be processed multiple times. Even under such scenarios, your filter logic will be executed only once. Whenever you are extending this filter, you need to make sure to implement a method with the name doFilterInternal(). To this method, we are going to receive the request, response, and filter chain as three input parameters. Let me get rid of all these documentation. On top of this filter, I need to mention, @RequiredArgs constructor because I want to do some dependency injection. We don't have to mention either component or any other stereotype annotation on top of this filter because we're not going to inject this class as a dependency in any other place. Inside this class, I'm going to paste few lines of code. You can get this code from the GitHub repo. I'm going to discuss this entire code line by line. At the starting, I'm trying to inject a Bean of type List of String. On top of this field, we should also mention @Qualifier annotation with the Bean name publicPaths. We're going to get an ambiguity related error because inside our PathsConfig class, we have created two different beans with the same data type. Next I'm also creating the object of AntPathMatcher. I'll explain you the purpose of this object in few minutes. Now let's try to understand what is the logic that I have written under the doFilterInternal(). As a very first step from the request, we need to read the JWT token. Inside the industry, whenever the client application, they want to send the JWT token, they're going to follow a standard. The standard is they are going to send the JWT token value under the request header with the header name as authorization. So inside the application constants, I need to create a constant with the name JWTHeader. Here just after these two JWT related constants, I'm going to create a new constant with the JWTHeader and the value as authorization. I'm expecting the client application to send the JWT token value under the header name authorization. I'm following the same industry standard, but if a enterprise application, if they want to accept through different mechanism, they should be able to do that. They just have to tweak the logic inside their validator filter. So once we read the value of the header, we're going to have the header value under these authHeaderString variable. We're performing the null check. If the given value is not null, we're trying to extract the JWT token while sending the JWT token value. Again, there is an industry standard that we need to follow. There has to be a prefix value with the name as Bearer space. So what is the meaning of bearer? Let me mention the same here inside the comments. When someone is using this bearer space inside the header value, they are telling whoever holds the token or bears the token is trusted and can access the protected resource. So that's the meaning of bearer. Since the first seven characters, we're going to have this bearer space. We're trying to read the actual JWT token value from the seventh char value. We know inside Java, the first index will start at zero. So from zero to six, we're going to have bearer space. That's why from seventh character, we're trying to read, and that is going to have the JWT token value. Next, I'm trying to invoke the getEnvironment() method available inside this parent. If you open this method, this method is present inside the genericFilter() bean, which is extended by the OncePerRequestFilter. So this method, it is going to give us the bean of Environment. By using the same, I'm trying to invoke the getProperty() method using this getProperty() I'm trying to check if there is any environment variable available that holds the secret value. If there is no environment variable, we are going to construct the default to secret value that we have defined under the constants. So once we have the secret value using the same, we are going to create the secret key object the same way that we have done under the JwtUtil. Next, after checking the null check around the secret key, we are trying to parse the JWT token value. Here by using the Jwts, we need to invoke the parser.verifyWith(). To this verify with, we need to pass a secret key object. If the token is tampered, this verifyWith() the method, it is going to throw an exception. Once the verification is completed, we need to invoke the build() method followed by parseSignedClaims() and to this parseSignedClaims(), we need to parse the JWT token value. And from that token value, since we are interested on the payload, we are going to invoke the getPayload() method, which is going to return me all the claims that I stored inside my token payload. We stored a couple of claims. Let me check what are those. Those are username and roles. The same I need to read from my validator as well. So here, let me mention claims.getUsername() and claims.getRoles(). So by using these username and roles, I need to create the object of Authentication. Since Authentication is an interface, we can't create the object of it. Instead, we are going to create the object of its implementation classes like UsernamePasswordAuthenticationToken to the constructor of this class. I'm trying to parse the username that I read from the token followed by null value. This null value indicates the password. Since we don't have any password in our hands, we need to parse null here. And that is completely acceptable. As a third parameter, we need to parse all the roles information inside the token. We are going to have the roles information as a single string under this string, all the roles, they'll be separated by using the comma. So inside the Spring security framework, there is a utility method under the authorityUtils(). This method is commaSeparatedStringToAuthorityList(). This is going to accept my string and separate all the roles from the string and convert them into the object of GrantedAuthority. That's why this method is returning me list of GrantedAuthority. What we're trying to achieve by invoking this constructor is if you open these constructor, you'll be able to see it is accepting the username or any other object as an input. As of now, we're trying to store username here, followed by credentials. So this can be nullable and towards the end, we're trying to parse authorities as well. The beauty of this constructor is whenever we are invoking this constructor, it is going to invoke this setAuthenticated() as true. And with that, we are telling to the framework, the authentication is completed and the authentication is also successful. So that's what we are trying to convey with our filter. So the same Authentication object that we prepared here, we're trying to set into the security context. How to set that? We have to invoke the SecurityContextHolder.getContext(). And this getContext(), it is going to give us a SecurityContext object using the same. We need to invoke the setAuthentication() method and pass this Authentication object. I'll explain you the purpose on why we are doing all this. It is going to be clear for you in a few minutes. So this is the happy path logic that we have written inside these doFilter() internal method. This method, it is also going to catch ExpiredJwtException in case the JWT token is expired. This logic is going to be executed where we are going to send 401 along with this message. For any other exception, we are going to throw bad credential exception. And towards the end, we need to make sure to invoke the filterChain.doFilter() method. To our method, we have three input parameters, what are those? request, response, and filterChain. We know inside the Spring security framework, there are multiple filters. Once a given filter logic is executed, it is the responsibility of the filter to hand over the request and response to the next filter inside the chain. That's what we're trying to do here by invoking the filterChain.doFilter() method I'm handing over the request and response to the next filter inside the Spring security flow. As of now, the Spring security framework does not aware about the filter that we have created here. But in a few minutes, we are going to make some configurations. And with that configurations, our own custom ValidatorFilter also will be injected as one of the filter into the Spring Security Filter Chain. If you forget mentioning this line, then the request to execution will stop inside your filter itself. And with that, the further authentication flow is not going to be executed by the framework. Please make sure you are mentioning this important line. You will see this line inside the Spring security filters as well. For example, if you open BasicAuthenticationFilter, inside this filter, we have a method with the name doFilterInternal() method. From this method only, the credentials extraction will happen. Once the credentials are extracted, this method, it is going to decide whether the authentication is required. So how it is going to decide, it is going to check security context. If there is any successful authentication is available. If yes, it is not going to invoke the further authentication mechanism. Let's go back to the doFilterInternal() method here if you see, in case if the authentication is required, all the authentication logic is going to be executed by using the Authentication Manager. Otherwise, this filter logic is not going to be executed. It is going to simply invoke the next filter inside the chain by using the chain.doFilter(). So please remember all this logic because it is going to help us on how this validator filter is going to work. I will explain end to end in few minutes. Just under the JwtTokenValidatorFilter, I also overrided a method with the name shouldNotFilter(). There is a very good reason why I'm doing this. Let's continue this discussion in the next lecture as well.