I said before, when we add the Spring security dependency into a Spring Boot backend application, by default, it is going to secure all REST APIs and webpages as well. So far we made these configurations, and using these configurations, we are able to access both our REST APIs without any security. But our Spring Boot application, it is also exposing some webpages. Do you know what are those? Those are related to the Swagger and OpenAPI documentation. If I try to access the path localhost:8080/api/swagger-ui.html, I am getting 403 error. This gives a confirmation to us that by default, Spring Security Framework, it is going to secure all our REST APIs and webpaths, either by using authenticated or denyAll behind the scenes. But I don't want my Swagger UI related pages to be secured. I want anyone should be able to access them so that they can see that documentation around my REST APIs. That's why what I'm going to do is I'll try to invoke requestMatchers() one more time. And this time I'll try to provide all the paths related to the Swagger UI, followed by I'm going to invoke the permitAll(). So let me mention the paths here. So as you can see here, these are all the paths related to the Swagger UI. How do I know I did enough research and read the official documentation? So by using the first two paths, we are exposing the webpages without any security. And by using this third path, we are making our OpenAPI specification document as public. Next behind the scenes, these HTML pages, they are going to leverage some CSS files, JavaScript files, and images. All such static files are going to be loaded from this folder by the library. It is also going to load some dependent jars from this location. That's why I have mentioned all these regex patterns. So with these regex patterns, what I'm trying to convey, any files that are present under this folder, they should be able to access without any security. So here I need to invoke permitAll() method. So let me save these changes, do the build. Now if I invoke the same path, this time I'm able to access my Swagger UI documentation. So here there is also a link to open the JSON file related to our OpenAPI definitions. This path also we configured with permitAll(), that's why we are able to access them. Always configure these Swagger UI related paths so that anyone can access your Swagger UI documentation and webpages without any security. If you have a requirement to secure them, then instead of permitAll(), you have to invoke authenticated() method. Right now, our application is a very small application and it has very limited number of REST APIs and functionality. But in real enterprise application, we are going to have hundreds of REST APIs and many other paths as well. If we keep configuring for all scenarios inside this file. So this file and this method, it is going to become very clumsy and it is also going to be tough for other developers to understand your configurations. That's why we need to follow a best practice where we can maintain all these path details in some other file. And from that file, we can refer all the paths and configure here. Let me show you a demo so that you will be clear on what I'm trying to convey here. Under the security package, I'm going to create a new file with the name PathsConfig. On top of this file, I'm going to mention @Configuration annotation followed by I'm going to create a couple of Java methods, which are going to help me to create the beans. Let me copy a few lines of code. You can get the same from GitHub repo. Using the very first method with the name public paths, I'm trying to expose all my public API paths or web paths as a list. The return type from this method is List of String. And this return object, I'm trying to create it as a bean by using a name called publicPaths. So inside this method, I'm simply creating a List object with all these paths. The first two are related to my REST APIs, followed by I have mentioned all the paths related to the Swagger UI. Next I also defined another Java method with the name SecuredPaths. Using this method, it is also going to return a list of paths. The name of the bean that is going to be created by this method is securedPaths. As of now, I have simply mentioned api/ with this regex pattern. But right now, inside our web application, we don't have any REST APIs, which are going to match with this pattern. But I just mentioned this path as a placeholder. In the coming sections, based upon the new REST API paths that we are trying to create, we are going to either update these public paths or secured paths. And with that, I never have to change the JobPortalSecurityConfig. I'm always going to come to this file and update the paths based upon my business requirements. Once you created these PathsConfig, what you have to do is you have to inject both these beans into the JobPortalSecurityConfig. So here, let me mention the annotation @RequiredArgs constructor, followed by I'm going to type private final list of String with a variable name as public paths. On top of this field, I'm going to mention @Qualifier annotation, followed by I'm going to mention the bean name, which is publicPaths. If I don't mention the @Qualifier, the spring framework, it is going to throw an error because there are two beans of same data type. Both these beans, they belongs to the same data type, which is List of string. To clear the ambiguity that my spring framework is going to have, I need to mention these @Qualifier annotation. Let me copy these two lines. And this time I'm going to mention the variable name as secured paths and the same name I'm going to mention under the @Qualifier. So now what I can do, I can try to iterate the paths available inside these public paths and invoke the permitAll() for all of them. What I'll do is I will comment all this code that I have related to the swagger UI paths. I'm doing these for your reference. Now I'm going to write some logic inside this Lambda expression. Since I'm going to write multiple lines of Lambda logic, I have mentioned these calibrations. So this is my Lambda body. I can get rid of these parentheses as well, because my Lambda expression has a single input parameter. To resolve the error, I need to mention parentheses so that all errors will get resolved. Now inside the Lambda body logic, I'm going to use the public paths using the same I will invoke the forEach() method to the forEach() method I will pass a Lambda expression. So the input to the Lambda expression will be path for each of the path that I'm receiving. I want to configure permitAll(). So using the request, I will invoke requestMatchers() method followed by, I'm going to pass these path and towards the end, I will invoke the permitAll() Here the second inner Lambda expression, since it has only a single line inside the body, I can get rid of these curly braces and that is going to solve all my compilation problems. Now in the similar lines, I also want to leverage other bean that we have. What is that other bean? It is a securedPaths. And this time, instead of invoking the permitAll(), I want to invoke the authenticated(). Do you see how simple our configurations become from now onwards, we're not going to make any changes in these two lines. We're always going to update these lists with these lists data set up. We are not able to use the regex requestMatchers(). Off course we should be able to do that, but we have to write some dirty logic here saying that if the path contains publics, then use these RegexRequestMatcher instead of requestMatchers(). So basically we need to write some if and else conditions inside the Lambda expression, but I don't want to do that. I'm fine with this simple approach because anyway, we're going to maintain all our APIs here inside this class. But in case if you want to still use RegexRequestMatcher, you know what to do. You basically during the each iteration, you have to check the path value. And if the path value is something related to the public REST APIs, under such scenarios, instead of passing the path, you need to pass this rejects pattern. We can further optimize this configuration. What I'll do is I'm going to invoke request.anyRequest().denyAll() With this, I'm telling to the Swing security framework. If someone is trying to access a webpage path or REST API path, which is outside of these public paths and secured paths, simply deny that request, even though they are authenticated, they are going to get 403 error. Both the approaches that we have discussed, I have documented them in the slides for your reference. This is the most basic approach where you're going to provide all your configurations within a single file. And that is going to make your file clumsy. If you want to build a clean and simple code, then please make sure to use the second approach where you can define all your public paths and secured paths in some other file and refer them inside this method and configure either permitAll() or authenticated().