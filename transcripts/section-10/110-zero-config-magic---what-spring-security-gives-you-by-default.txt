As soon as we add the Spring Security Dependency into our backend application, it started securing our backend REST APIs. Here you might be curious to know, what is the code that is responsible to secure all our backend REST APIs? Let me show you the same you need to search for a class with the name SecurityFilterChainConfiguration. So this class is present inside the Spring Security Framework. So please make sure you have selected all places here and classes here. So this is an inner class present inside the another class with the name ServletWebSecurityAutoConfiguration. This is the outer class and this is the inner class. Inside this inner class, there is a bean created by the Spring Security Framework team, and inside this bean, they have made the required configurations. So they have used a method and from this method, they are returning the object of SecurityFilterChain. The same object is being converted as a Spring Bean by using this @Bean annotation. So this method, it is accepting HTTP security as an input. By using this HTTP input variable, they invoked a method with the name AuthorizeHttpRequest and to this method, all the Spring Security configurations they are trying to provide by using this Lambda expression. If you open this method, this method, it is accepting a Lambda expression that is matching with this input parameter, the same they are trying to provide here. The Lambda expression, it is going to accept an input parameter of this type so this is the variable name. Using the same variable name, they have invoked anyRequest(). By using this anyRequest(), the Spring Security Framework developers, they are trying to communicate to the framework, any Request() that is coming towards this backend application, make sure to authenticate. . So after the anyRequest, they have invoked another method with the name authenticated(). Since authenticated() method is used here, it is a clue to the framework that each and every request has to be secured. Unless someone providing valid credentials, they should not be allowed to access any backend REST API. After providing these configurations, they also enabled form login under HTTP basic as well. In a typical backend web application, the request can be sent in two forms. The first one is, by using the HTML form pages and the other approach is, by using the REST APIs. In the scenarios where your backend application is responsible to display the HTML pages as well to the end user, under such scenarios, we need to make sure to enable the form login. With the form login option, the end user, they are going to provide their username and password by using a login page. We already saw default login page in the previous lectures. The other approach is by using the HTTP basic authentication if someone is trying to invoke our backend REST APIs by using traditional REST API communication, then they can send their credentials by using the HTTP basic standard. When we are using these HTTP basic standard, whatever username and password that we are providing here, they are going to be base64 encoded. If you go to the headers here, you will be able to see there is an Authorization header created by the postman. And under this authorization, there is a value mentioned. So this is the value so if you see first, there is a prefix with the value basic space followed by some base64 encoded value. So here in the scenes, what is postman is doing? It is taking our username and password, and it is trying to base64 encode this value. I can take this Base64 encoded value, and I'm going to paste that value inside this Base64 decode.org. So if I paste it here and click on this decode, you will be able to see this is my username and password. In between, there is also a colon mentioned to differentiate between my username and password. So this is the HTTP basic standard that industry follows. They will take the username followed by colon and mention the password and this entire value will be encoded by using base64 standard. And the value like this will come. The same will be sent as part of the request header by using the header name as Authorization. So here to this header, first, they're going to mention basic space followed by base64 encoded value. By looking at this standard, any backend framework, it can easily recognize that so-and-so fellow is trying to send the credentials by using the HTTP basic standard. So when the communication is happening directly between the REST APIs, the credentials, they have to be sent as part of the request headers by following the HTTP basic standard. So for that to happen, the HTTP basic also has to be enabled, to this form login and HTTP basic methods they are providing the output from the with defaults method. This withDefaults() method is a static method available inside the swing security framework. If you open this method, this method simply returns an empty lambda expression. So with this, we can conclude that by default, the swing security framework, it is enabling both form login and HTTP basic style of authentication for any given backend application and while enabling these two types of authentications, it is going with the default values that are being considered by the framework. When we want to go with the default values of the framework, we just have to pass on the empty lambda expression, or we can also invoke these withDefaults() method. So this is the magical code that is responsible inside the Spring security framework, which is taking care of securing our backend REST APIs. We will have our own custom requirements, like maybe I want to secure only 5 REST APIs and the remaining 5 REST APIs, and I want them to keep unsecured. To build these kinds of custom security requirements, as a backend developer, we should create these bean by ourselves inside our own configuration classes. When a developer is creating these bean by themselves inside the project, then the creation of these default bean will be backed off by the Spring security framework. Let me show you a demo on how to create this bean. What I'll do is, I'll try to copy this entire method and inside our project, I'm going to create a new package with the name security. Inside the security package, I'll try to create a new Java class with the name JobPortalSecurityConfig. On top of this class, I need to mention @Configuration because we want to create some beans from this class. We should also mention an annotation with the name @EnableWebSecurity. Mentioning this annotation is completely optional because behind the scenes, Spring Boot is going to take care of enabling this annotation. But still, please do mention this annotation as it is a standard. Next inside this class now I'm going to copy the same method and here we can get rid of this order annotation because we're going to define a single bean that is going to return SecurityFilterChain. In case if you want to create multiple beans of SecurityFilterChain, then you need to mention the order details so that the spring security framework will try to initialize these beans based upon the order number that you are trying to provide. So this is the default code. Let's try to optimize the same. Instead of invoking all these methods individually by using the HTTP, we can also chain them as well. For example, after this authorization request, I can try to invoke the formLogin() method to which I'm going to provide the output that I will get when I'm invoking the withDefaults() method. This withDefaults() method is a static method available inside the customizer functional interface of the Spring Security Framework. In the similar lines, I'm also going to invoke httpbasic() method as well. To this method also, I'm going to pass the withDefaults() method and towards the end, I'm going to invoke the build() method, which is responsible to create the object of SecurityFilterChain. So if you see here, I have built the same code, but I have used the chaining option. Now I can get rid of all these three lines and here I can mention the return keyword, which is going to take care of returning the object of SecurityFilterChain. So if I save this and do the build, we are going to have the same behavior. Let me show you the same. I'm trying to invoke this API one more time. It is asking me the username and password. The username is madan, coming to the password, it is Madan@123. As soon as I click on the sign in, you'll be able to see I have received a successful response. I hope you are clear with the default configurations inside the Spring Security Framework. From the next lecture, let's try to explore on how to customize these configurations based upon our business requirements.