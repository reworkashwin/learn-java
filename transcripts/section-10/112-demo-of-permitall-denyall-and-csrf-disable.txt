With the default configurations provided by the Spring Security Framework, every request has to be authenticated. Because here, authenticated is invoked after the anyRequest() method. Just like authenticated() method, we also have another method with the name permitAll(). When we are mentioning permitAll(), the Spring Security Framework, what it is going to do behind the scenes is, it is going to permit all the requests that are coming to your application without any security. This is as good as not using Spring Security Framework inside your Spring Boot backend application. From the Postman, I will select the authorization as NoAuth and if I try to invoke this REST API, this time I receive the response without any authentication. The same behavior you can also see through browser as well. Here you may have a question ? What is the scenario where I am going to permit all kinds of requests that are coming towards my application? This can be your question. While we are creating Beans by using Spring and Spring Boot, we can also create Beans conditionally based upon the environment where your code is executing. For example, I can create this Bean with the permitAll() configuration for a lower environment where there won't be any security involved, whereas I can create another Bean with the same configuration, except here instead of permitAll(), I am going to have Authenticated. So that second Bean, it is going to be created only for higher environments like inside production environment. We are going to explore on how to create the Beans conditionally in the coming sections, but I am trying to give a clue here when this anyRequest().permitAll() is going to be useful for you. Next, just like how we have permitAll(), we also have denyAll() method. So with this, what is going to happen? The Spring Security Framework, it is not going to allow anyone to access your REST APIs even after entering valid credentials. Let me save these changes, do the build. From the Postman this time if you see, if I am trying to invoke the same REST API without authentication, I am getting 401 Unauthorized. But if I invoke the same API with the basic authentication, here I have provided valid username and password. If I click on the Send button, this time I am getting 403 Forbidden Error. 403 is a standard HTTP status code, which conveys that access to a given resource is not allowed or is prohibited. So with this 403, what Spring Security Framework is trying to convey is, I know you provided valid credentials. That's why I'm not throwing 401. Instead I'm throwing 403. Though you are a valid authenticated user, still you are not allowed to access this REST API because behind the scenes, the Spring Security Framework, it is not accepting or processing any request. Usually this anyRequest().denyAll(0, you don't use to a great extent there are no valid scenarios where you are going to use this anyRequest().denyAll() but both these permitAll() and denyAll() methods, we can also use them by giving our specific API security requirements. As of now, we are blindly using anyRequest(). But in the coming lectures, I'm going to show you how we can permitAll() for a given set of APIs where as for other set of APIs, I'm going to secure them by invoking the authenticated() method. So when you have such kind of custom requirements to have both secured and unsecured APIs inside your backend application, then using these authenticated() permitAll() and denyAll() is going to make more sense. Usually in any enterprise application, no one is going to use a configuration like anyRequest().denyAll() or anyRequest().permitAll() or anyRequest().authenticated(). Because any enterprise application, they're going to have their own custom requirements where they want to expose certain REST APIs without security and other set of APIs with security. Let me mention permitAll() so that we can access all our REST APIs without any security for the next few minutes. I'm going to save all these changes to the build. This time what I'll do is, I'll try to test the contacts REST API that I have under the security folder. . So there is a request and here we are trying to invoke a REST API that supports POST method. We don't have to provide any authentication details because we used anyRequest().permitAll() in the backend configurations. In the body, I'm trying to send these details if I click on this send button, I'm getting 500 error because this is due to the CSRF protection provided by the Spring security framework. Ideally here, we should receive either 401 or 403 error, which are related to authentication, but we are getting 500 internal server based upon our global exception handling logic that we have written. In the coming lectures, I'm going to fix this problem. But for now, let's try to disable the CSRF protection provided by the Spring Security Framework so that this API also we can try to invoke without any security. Off course, disabling the CSRF protection completely is not a production recommended standard. In the coming sections, I'm going to explain in detail about what is CSRF protection and how to handle it properly by using Spring security framework ? For now, I'm just going to disable the CSRF protection. So to disable the CSRF protection, just like how we invoke this AuthorizeHttpRequest, formLogin and HTTPBasic method in the very similar lines, we can also invoke another method with the name csrf(). To this csrf() method also, we need to provide our own configurations by using the lambda expression. Let me mention the lambda expression variable name as csrfConfig. So the variable name can be anything. Now by using this variable name, I just have to invoke the disable() method, just like how we have invoked the disable() method to disable the formLogin(). In the similar style, we need to invoke the disable() method. Let me save these changes. Here IntelliJ is trying to throw some warning saying that lambda can be replaced with the method reference. If you click on this replace lambda with the method reference, you're going to see this updated code where the disable() method available inside this abstract HTTP configure is being invoked directly. But I don't like this syntax. I will go back to the lambda expression syntax. So let me save these changes do the build once the build is completed, I'll try to invoke the same REST API. This time we should get the successful 201 created response. Do you see behind the scenes, Spring security is doing a lot of magic. It is highly customizable you can disable it or enable it based upon your business requirements. The details around on how to configure permitAll() for all requests, I have mentioned them in this slide. Similarly, inside the next slide, you will see the configurations for the denyAll(). But as I highlighted here, using denyAll() and permitAll() on top of the anyRequest() method is not recommended for production applications. We should use either authenticated or we should provide our security configurations based upon our business requirements. Let's explore on how to provide our own custom API specific security configurations in the next lecture.