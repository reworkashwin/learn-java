So far inside our application we built some back-end REST APIs, and these back-end REST APIs they also interacting with the database by using the Spring Data JPA framework. Now is the time to talk about security as well. When a developer is building back-end logic for an enterprise application, security is also another important aspect that developer has to consider, otherwise no end user is going to use your application. Let's take a scenario, there is a bank application, but that does not have any security. Anyone should be able to perform any operation. If given a chance, are you going to open a bank account under this organization? Off course not. The same applies for any enterprise applications like insurance-based, financial-based, healthcare-based. That's why as a back-end developer, it is very important for you to learn about securing your REST APIs as well. This is where we have another project under the Spring ecosystem, which we can leverage to secure our back-end application. Let me introduce this project. There is a project with the name Spring Security inside Spring ecosystem. This Spring Security is also a powerful framework like Spring Boot that helps developers to protect their Spring-based applications. The main focus of this Spring Security framework is security. Just like how Spring Data JPA focuses on database interaction, in the similar lines, Spring Security framework or project, it is going to focus on security alone. By using this framework, we should be able to implement authentication and authorization inside our application. This framework has a very good integration with Spring Boot, and it is going to work seamlessly without any issues. By using this framework, we should be able to protect any kind of resources like REST APIs or web pages. . Based upon our business requirements, we should be able to customize this Spring Security framework to any extent. This framework is going to make developer life easy while implementing security, because it has a lot of predefined mechanisms to protect against most common security attacks like CSRF, CORs, Session Fixation, and so on. This framework also supports various login approaches. For example, we can have a simple username password style to authenticate an user. These usernames and passwords we can either store inside the database or inside LDAP. Spring Security also supports various security standards like OAuth2, JWT tokens, SAML. I discussed about all these advanced Spring Security concepts inside my Spring Security focused course. But inside this course, we are going to implement the security by storing our username and password inside the database tables. But if you are looking for some advanced approaches by using OAuth2 specifications, then please refer to my Spring Security course. To get started with the Spring Security framework, we just have to add this dependency. As soon as you add this dependency, the Spring Security framework is going to secure your entire web application. By default, it is not going to accept any request without any authentication information. Let's try to add this dependency to our backend application and see what is going to happen. Since we are into the new section, I'm going to create a new folder with the name section_10. Into this new folder, I'm going to copy the job portal backend code from the previous section followed by, I will open this project using my IntelliJ. To get started with the Spring Security framework, you need to come to the start.spring.io website and click on the add dependencies followed by, you need to search for the dependency with the name Spring Security. After adding this dependency, you can click on the explore, which is going to show you the pom.xml file content. So this is the dependency that we need to add let me copy the same. There is also another unit testing related dependency. Since we're not going to focus on unit testing inside this course, I'm not going to add this dependency. . Inside my pom.xml, just after the web MVC dependency, I'm going to add the new dependency related to the Spring Security. After adding this dependency, we need to sync the maven changes followed by, I'm going to perform the build, once the build is completed, I will try to start my backend application. This time you're going to see some magic. Inside my UI application, if I refresh my homepage, I'm not able to see any job details and a company details. Let me check the browser console here, we are facing some error due to CORs policy, but previously we made some changes to allow the communication between our UI application and the backend application. You can check the same as well if you go to the file with the name WebConfig, here we have add all the course related configurations, but these configurations, they are right now not working because we added the Spring Security as a dependency. When Spring Security is present inside your class path, you need to provide the CORs configuration by following the Spring Security standards. We're going to do that in the coming lectures. For now, let's try to test our backend REST APIs by using the Postman. Here I'm trying to invoke the GetAllCompanies request. As soon as I click on this send button, I am getting some HTML as an response. If I go and click on this preview, I'm getting a login page where it is asking me username and password. Let me test the other REST API under the contacts as well. Here also, if I try to invoke this REST API, I'm getting the very similar login page as a response. We can also try to access this REST API through the browser as well. I'll try to test these get REST API because through browser, we can't test any post or put or delete REST APIs. We can only test get related REST APIs. What I'll do is, I'll go to the console and here I'm going to have the complete URL of the company's REST API. I'm going to copy the same from the browser I'll try to access the same as soon as I press enter, you will be able to see I'm getting a login page. Who is providing this login page? It is the Spring Security Framework. So by default, as soon as we add the Spring Security dependency into our project, it started doing its job by securing all our REST APIs. If someone is trying to access these REST APIs, the Spring Security Framework, it is also showing them the login page to enter their username and password. Off course, this is the default login page provided by the Spring Security Framework we can have our own login pages. We're going to explore all those advanced topics in the coming sections. For now, let's try to understand how to access our backend REST APIs by entering a username and password here. So by default, the username will be user and coming to the password, you can find the password inside the application logs. Let me open the console logs, and here, if you scroll up slowly, somewhere you will see a password so this is the generated Spring Security password. So let me copy this entire value and copy here. As soon as I click on this sign in button, you will see I'm getting the company along with their job details inside the response. So that means the Spring Security Framework, it is only allowing our request when we provide valid login details. We can provide the same credentials through Postman as well. What you have to do is, while you are invoking any of the REST API, you need to provide your login credentials as well. So there is an authorization header. Let's click on the same and here under the Auth type, we need to select the Basic Auth option because by default, the Spring Security Framework, it is going to follow the Basic Authentication mechanism. So here the username will be user coming to the password we need to mention the same password that we saw inside the console. This time, if I click on the send button under the response, you will see I got all the company and job details. Let's try to test the contacts to REST API as well. So here, if I try to do the same by going to the authorization and selecting the Basic Auth you will see the same credentials populated here. If not, you can try to enter the username and password. If I click on the send button, I'm still getting the login page. These behavior we're seeing for the contacts, because by using these contacts REST API, we're trying to create the new data on the backend side. So any of the REST APIs that are going to be triggered by using post, put or delete, which has the potential to modify the data on the backend, such REST APIs, they are going to be blocked by default by the Spring Security Framework due to the CSRF protection. We are going to talk about the CSRF protection in the coming lectures, but for now, don't try to test any of the REST APIs other than HTTP GET()method. For the GET REST APIs, there won't be any CSRF protection because by using the GET REST APIs we're only going to read the data. We're not going to change the data, create the data or delete the data. So that's why Spring Security Framework does not bother about providing the CSRF protection for the GET REST APIs. Due to that, we are successfully getting the response. I hope you are clear with the simple demo of the Spring Security Framework. We're going to discuss a lot about the Spring Security Framework for the next few sections. Step by step, we are going to enhance our web application so that the authentication will happen from our UI application by using the credentials that we store inside the database table.