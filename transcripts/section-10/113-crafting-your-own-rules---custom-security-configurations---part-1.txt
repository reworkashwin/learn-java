Inside our backend application so far, we have developed only a couple of REST APIs. What I'll do is, I'll mark one of them as unsecured and the other one as secured. If you go to the CompanyController, we have one REST API, which is having path as api/companies. And the other one we have is ContactController. Here we have one REST API that supports post mapping. We also have one dummy REST API let me get rid of this. I'm also going to look for any other REST APIs that we have inside our application. We have one controller with the name ScopeController. We already discussed about these scopes in the previous section. That's why I don't want to maintain this controller just to keep my backend code simple and clean. I'm going to get rid of all these scopes related logic by deleting this entire scope package. With this, now we end up having only two REST APIs. Let me go to the JobPortalSecurityConfig. This time, instead of invoking the any request, what I have to do is, I have to invoke requestMatchers() method. To this requestMatchers() method, we can provide the REST API path. For example, if I want to keep my company's related REST API as a public REST API, so that anyone can invoke without any security, what I can do is here, I just have to mention the complete API path. We know inside our backend application, each REST API, they're going to have the same prefix path. That's why here, first I have to mention /api/companies. And for these REST API path, since I am looking to make it public or unsecured, I have to invoke the permitAll() method. In case if I have many other APIs, which I want them to be public, I can mention more paths as well by using comma as a separator. But as of now, since we have only one REST API, I'm providing a single input parameter to the requestMatchers() method. Next, I'm going to invoke the same requestMatchers() method one more time. So let me type requestMatchers(). And this time I'm going to mention the path as /api/contacts. And this API I want to secure, that's why here I want to invoke the authenticated() method. This way we can configure our API security requirements based upon our requirements by either using permitAll(), authenticated() and denyAll() method. Since I don't have any requirement to deny a given REST API, I'm not using denyAll(). There'll be very rare scenarios where you may want to use denyAll(). So now after making these changes, if I try to do the build and test these APIs, I will be able to invoke the companies related API without any security. Here I'm going to select the no auth option and I'm clicking on this send button. As you can see, I received successful response, even if I'm not providing any authentication details. According to the contact message, this is a secured API on the backend. So that's why we need to make sure to send the authorization details. If I try to invoke this API without any authentication details, I'm going to get the 401 unauthorized error. So let me select basic auth and make sure to provide valid credentials. If I click on the send button, this time you'll be able to see my request is processed successfully. So this is the approach that you need to follow. But here I also want to share some best practices or the standards that you can follow while you are trying to provide your custom security configurations. Let me update this method name also to customSecurityFilterChain(), because this is no more default SecurityFilterChain. We customize it based upon our requirements. So now the first standard that I want to share here is all your public REST APIs, they should have a common prefix because if all your REST APIs, if they have a common prefix, you can mention your security configurations by using regex configurations as well. I'll explain you on how to configure Spring security by using regex as well. But before that, let's try to implement these standards. I'll go to the CompanyController I want to make this REST API that supports by this getAllCompanies() method as public. So that's why what I'll do is by using this @GetMapping annotation, I'm going to invoke the path attribute and to this, I will mention the path as public. So anyone who wants to invoke this REST API, they have to mention api/company/public. In the similar lines, I'll go to the ContactController I'll mark this one also as public one. Coming to the secured ones, we are going to build many secured ones in the coming sections. I'll explain you what is the strategy that you need to follow for the secured ones in the coming lectures. For now, for this one also, by using the @PostMapping annotation, I'm going to invoke the path attribute and to this, again, I'm going to mention public path. So let me save these changes, do the build with this now both our REST APIs, they have a pattern. What is that pattern? These REST APIs, the end path, it is ending with public. Now I'll go to the JobPortalSecurityConfig. Here also we need to update the path because with the changes that we have done so far, these REST API paths became invalid that's why here I'm going to mention public as well. And for this one also, I'm going to mention public followed by, I'm going to update this also as permitAll(). With these configurations, both our REST APIs, they can be invoked without any security. Inside Postman, what I'll do is, I'll try to create two more REST API requests. So first I'll copy these GetAllCompanies and I'll update this to GetAllCompanies_public . So here we need to mention correct path as public. Now if I invoke these REST API, I'm going to get the response, even if I'm not providing any authentication. So from now onwards, you can't invoke the other REST API because this REST API path is no more supported by our backend application. If I click on the send button, you will be able to see I'm getting 401 unauthorized because by default, Swing Security Framework, it is going to secure all the REST API paths. The Swing Security Framework, it is assuming that there is a path available with these REST API path. That's why it is trying to secure. If you try to provide some authentication details and invoke the same, you are going to get 403 error. There is a reason on why we are getting this 403 forbidden error. Though we have provided valid credentials, Spring Security Framework, it identified there is no path for this REST API path inside our backend application. That's why by default, all such requests, it is going to deny the request and it is not going to process them. That's why we are seeing the 403 forbidden error. So from now onwards, don't try to invoke these REST API. Let me close this but if you try to invoke these public REST API, we are going to get the proper response. Let me do the same for contacts as well. I'm going to copy this request and update this request name to SendContactMessage_Public . Before I try to invoke this REST API, I need to mention the path as public. If I click on this send button with this body, I'm going to get the successful response. Here you may have a question. What is the advantage that we are getting here by maintaining this public in all the public REST APIs? Let me explain you the same. I'll try to comment these two lines of code just for your reference. And this time again, by using the request, I'm going to invoke the requestMatchers() method. And to this requestMatchers() method, I'm going to provide my regexs expression. To provide the same first, we need to invoke a class with the name RegexRequestMatchers using the same we need to invoke the method regexsMatcher(). To this regexsMatcher() method, I'm providing a regexs pattern with these regexs pattern what I'm telling to the Spring Security Framework is if there is any REST API path that is ending with the public, please make it unsecured. So let me save these changes to the build. This time also, you'll be able to see we are getting successful response for both our REST APIs, even though we are not providing any authentication details because we mark both these REST APIs as public REST APIs. With this configuration, what is the advantage that we're getting? Regardless of how many REST APIs that you have inside your backend application that are ending with the public, this single line of configuration should be sufficient. As a developer, you just have to make sure all your public REST APIs, they are ending with the path as public. So here I have followed this strategy, but this strategy can vary from company to company. As long as there is a pattern or strategy inside your REST API paths, you should be able to use the regex request matches to configure your security configurations.