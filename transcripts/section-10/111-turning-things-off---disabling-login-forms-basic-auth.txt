Let's imagine you have a requirement in your project to disable the formLogin(). You never want to accept the username and password by using a login page. Instead, you always want to follow the HTTP basic approach. To implement the same, you need to provide your own configurations to this formLogin() method. As of now, to this formLogin() method, we are providing an empty lambda expression. But instead of this, we should be able to provide our own lambda expression. Let me show you on how to do that but before that, let's try to understand right now what is happening whenever we're trying to invoke a REST API without passing any credentials. Inside the postman, under the authorization, if I select the No Auth option and click on the send button, I am going to get the login page. So under the preview also, you'll be able to see the login page is being displayed. Inside the browser also, we're going to see the login page here I'm trying to access the company's REST API. As soon as I press enter, I am getting the successful response because I already entered the credentials just a few minutes back and my browser remembered those credentials by using a cookie. If you go to the cookies under the application, so here inside the application under the cookies, you need to select the localhost 8080. So there is a JSESSIONID created by the Spring Security Framework. As long as this value is present inside my browser, I should be able to access my API again and again without providing credentials. But if I remove the JSESSIONID or if I tamper this value, then Spring Security Framework will force the user to log in again. I'll go to this value and maybe I'll remove some random lecture. Now if I try to refresh the page, you will be able to see I am seeing a login page. Now I have a requirement to not show the login page, I always want to accept the credentials by using the HTTP basic approach only. For the same, what I had to do is, instead of invoking these withDefault(), I had to provide my own lambda expression. So here the variable name instead of mentioning t, I will mention it as flc. So flc means formLoginConfiguration. Now using the same variable, which is of type formLoginConfigurer, I need to invoke the disable() method. Let me save these changes, do the build. Now let's try to see what will be the behavior after disabling the formLogin(). Inside the postman, if I invoke the same REST API without any authentication, inside the response this time, I am getting the 401 Unauthorized error, and I'm not seeing any login page as well. So what is the meaning of 401 status code? It is the standard HTTP status code to represent that someone is trying to access the resource or the REST API without providing any credentials. Let's see the behavior inside the browser. Inside the browser, this time I'll try to access the same API. The API is api forward slash companies. If I press enter, you'll be able to see I'm not getting any login page, but I'm getting a pop-up and this pop-up is being displayed by the browser. So the browser understands since there is no login is being displayed by the framework, it is going to show a pop-up and if I enter the username and password inside this pop-up behind the scenes, the browser, it is going to send the credentials by using the same HTTP basic standard. So let me enter the username as madan password as Madan@123. If I click on sign in, you will be able to see I received a successful response. Now let's imagine you have a requirement to disable() the HTTP basic, but not the formLogin(). Obviously we can't disable() both of them if we disable() both of them, there is no way for someone to send the credentials into our application. This time, what I'll do is, I'll try to provide the same Lambda expression to the HTTP basic and for the formLogin() method, I will assign the empty Lambda expression. So here, let me mention the Lambda expression, but this time the variable name, I'm going to mention it as HBC because when we are passing the configuration to this method, the input Lambda expression variable is going to be of type HttpBasiCconfigurer. So that's why here I will just mention HBC and by using the same HBC, I need to invoke the same disable() method. So let me save these changes, do the build. This time while invoking the REST API from the postman, even though I'm sending the credentials by using the HTTP basic approach, I'm not going to get the successful response. Instead I'm getting the login page. Inside the browser also, if you try to access this REST API, you are going to see the default login page. I hope you're clear on how to disable these formLogin and HTTP basic based upon your business requirements. Please either enable both of them or disable one of them. If you are building a pure Spring Boot backend application that is going to expose only the REST APIs, then it is a wise decision to enable only HTTP basic and disable the formLogin because for the backend applications, there'll be a separate UI application that is developed by React or Angular framework. Those UI technologies, they are going to take care of sending the credentials by using the HTTP basic standard. And we never have to show that default login page to the end user because the UI application, they are going to collect the username and password from the end user by using some beautiful login pages. We're going to do the same inside of our UI application as well. Since inside this course also we're building a pure backend application that exposes only the backend REST APIs, but not any HTML pages, what I'll do is, I will disable the formLogin and enable the HTTP basic. Let me save these changes,do the build. The details that we have discussed in the previous lecture and this lecture, I have documented them in these slides for your reference. Whenever you want to understand the Default Spring Security Configurations, you need to look for a class with the name SecurityFilterChainConfiguration. Inside this class, you need to look for a method that returns a bean of type SecurityFilterChain. And these are the default configurations. By using these default configurations, every request will be authenticated and both the formLogin and the HTTP basic will be enabled by the framework. In case if you are looking for an option to disable the formLogin style of authentication, you just have to provide a lambda expression to the formLogin() method. Using these lambda expression input variable, you have to invoke the disable() method. The same applies for HTTP basic style of login as well. If you want to disable this style of authentication to the HTTP basic() method, you have to pass a lambda expression inside the lambda expression logic you need to invoke the disable() method.