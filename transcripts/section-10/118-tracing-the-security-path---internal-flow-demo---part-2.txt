Let's try to understand the story on what is going to happen once the request reached to the authenticate() method available inside the authentication provider implementation classes. So here, if you closely understand the logic, they are trying to extract the username from the Authentication object. User details are already available in some cache. If not, they are invoking this retrieveUser() method. This retrieveUser() method is present inside the DaoAuthenticationProvider. If you go here and look for the method with the name retrieveUser(), here there is a logic to retrieve the user details based upon the username by using the UserDetailsService. Let me open this loadUserByUsername(). So this abstract method is present inside the interface with the name UserDetailsService. Just like how we have UserDetailsService, we also have another interface with the name UserDetailsManager. Inside the UserDetailsManager we have the methods related to the createUser(), deleteUser(), updateUser() and changePassword(). But this loadUserByUsername() is coming from the parent interface with the name UserDetailsService. So basically both the UserDetailsManager and UserDetailsService interfaces, they are going to help in the user management. The management can be like fetching the user details from the storage system, or it can be creating a brand new user or updating the user or deleting an existing user details in the storage system. So basically whenever the authentication provider, it want to perform the authentication, it is going to simply ask the UserDetailsManager and UserDetailsService implementation classes to provide the user details. Here you may have a question, which is why there are two different interfaces? Why can't they maintain all the methods inside the same user details manager? Why they separately moved these loadUserByUsername() into a parent interface with the name UserDetailsService? Let me explain the same. Few enterprise projects, they are going to create the user, delete the user and update the user by writing some logic outside of the spring security framework. Because at the end of the day, these operations are just a database operations. Such projects, they don't have to write any UserDetailsManager implementation classes. Instead, they can simply write the UserDetailsService implementation classes because by overriding a class of this interface, they can write the logic on how to fetch the user details based upon the username from a given storage system. I hope you're clear. Let me go to the Davo authentication provider here. I'm going to keep a breakpoint so that we can understand what is happening when we enter the credential. From the login page I'm going to click on this sign in button, which is going to stop the request inside the provider manager. So from the provider manager, the DaoAuthenticationProvider method will be called so here we have authenticate() method. If I release the breakpoint, the breakpoint stopped inside the DaoAuthenticationProvider. So we put a breakpoint in the retrieve user, but not inside the authenticate() method. I'll keep a breakpoint in the authenticate() method as well. Right now we are inside the retrieve user of the DaoAuthenticationProvider. Let's try to debug this code line by line. Here if I try to check what is this user details service implementation by executing this code. So here we have an evaluate expression, which is going to evaluate this expression and it is going to give me the implementation class. So what is that implementation class? It is a InMemoryUserDetailsManager. Let me open the same InMemoryUserDetailsManager. Inside this class, we have various methods, but we are interested about the loadUserByUsername. I'm going to keep a breakpoint here and release the other breakpoint that we have. So right now the breakpoint stopped inside the loadUserByUsername. So here, if you see as of now, we have a single user with the name madan. And since the user details we have configured inside the application.properties file by default in this kind of scenarios, the Spring security framework, it is going to store these user details inside the memory of the application. So that's why this manager is being used right now by the Spring security flow. In the coming sections, I'm also going to show you on how to store the credentials inside the database and how to fetch those user details by writing our own user details service implementation class. For now, if you try to understand the user details, it is going to fetch from these users hash map. By using this hash map only the framework, it stored the user details inside the memory of the application. Here, the username it is providing is madan. Based upon this madan, it is going to fetch a existing user details and the user details inside the Spring security framework, they are going to be represented by using these user details interface. So there is an implementation class for this interface with the name User. Anytime when we want to represent a user inside the Spring security framework, it is going to be represented by this class object. So let's go to these loadUserByUsername() method. So this method, it is simply returning an object of user along with the username, password and other details that we have inside the storage system. If I release the breakpoint, the breakpoint is going to stop inside the authenticate() method. So let me go to the authenticate() method. So here, once the retrieve user is completed, we have other logic where it is going to perform additional authentication checks. Let me open this method. So this is an abstract method. This method is overrided inside the DaoAuthenticationProvider. I'm going to look for the method with the name additionalAuthenticationChecks(). I will keep a breakpoint here and release the other breakpoint. So now the breakpoint stopped inside this additionalAuthenticationChecks()  method. To this method, the input arguments are the user details that we fetched from the database, which is going to have the username and the password details from the storage system. And the other parameter is Authentication object, which we received from the login page. It is checking whether the password is present inside the Authentication object. Since it is there, this if condition is not going to be executed. So from the Authentication object, we retrieve the presented password. So this is the presented password. So this presented password will be compared with the password available inside the storage system. We know this UserDetails object represents the user is stored inside the storage system. That's why when we execute these UserDetails.getPassword(), we are going to get the password which is stored inside the storage system. Both the presented password and the storage password, they are going to be provided as an input to the matches method of the PasswordEncoder. I'm going to share more details around the PasswordEncoder in the coming lectures. For now, I'm assuming you're clear on what is a component, which is responsible to validate the password. It is the PasswordEncoder inside the PasswordEncoder interface. We have a matches() method. If you look for a PasswordEncoder interface, there is a matches() method, which is going to accept two different passwords. And once the passwords are compared, it is going to return a Boolean value. So there are a good amount of implementation classes for the PasswordEncoder. We are going to discuss about them in the coming lectures. So let me go to the DaoAuthenticationProvider. So now if I try to debug this line, since the passwords are matching, this if condition is not executed. If the passwords are not matching, we are going to throw a BadCredentialsException. Let me go to the next line now we are inside the authenticate() method of the DaoAuthenticationProvider. So here, if you closely look, there is a method being invoked with the name createSuccessAuthentication(). I need to open the implementation method. Let me go to the DaoAuthenticationProvider and look for the method with the name createSuccessAuthentication(). I will keep a breakpoint here and release the request. So now the request stopped inside the createSuccessAuthentication(). So this successAuthentication, what the framework is going to do is it is going to do some housekeeping activities. So these housekeeping activities include storing the successful authentication information inside the security context as well. Let's go to the end of this method. At the end of this method, it is again calling createSuccessAuthentication() method available in the super class. Let me open the same. So from this method, if you see, they are trying to invoke the authenticated() method available inside the UsernamePasswordAuthenticationToken. If I open this here, they are simply invoking the constructor of UsernamePasswordAuthenticationToken by providing the username credentials and authorities information. Let me open this constructor. Inside this constructor, if you see, they are clearly invoking setAuthenticated() method with the true value. So this is how the framework is telling to the other components that the authentication is successful and any other components, if they want to know whether the authentication is successful or not, they just have to invoke isAuthenticated() method available inside the Authentication interface, which is going to return these Boolean value. And accordingly, they are going to decide to process the request or not. So these UsernamePasswordAuthenticationToken that we are creating right now with the setAuthenticated() as true, it is going to be stored inside the security context so that if the end user is trying to access the another secured API by using the same session and the same J session ID, the framework, it is not going to ask the credentials one more time. Instead, it is going to leverage the existing authentication details that are stored inside the security context. Let me release the breakpoint. If I release the breakpoint and go to the browser, I am getting 403 errors. Seems we have waited for too long time. What I'll do is, I'll try to invoke one more time, api/companies/public. As soon as I press enter, the breakpoint is going to be stopped again. This time, if I release all the breakpoints, this time I'm able to see the response without entering any login credentials because we already entered previously and the same being leveraged by the framework right now. I hope you are able to link all the components of the Spring Security internal flow. Please remember the responsibility of each of the component of this flow. Unless you are clear about this flow, we can't make progress in the advanced concepts of the Spring Security framework in the coming sections. In the coming lectures, we're going to define our own AuthenticationProvider, our own UserDetailService implementation classes. Unless you are clear about this flow, you will not have any clue on why we are creating our own AuthenticationProvider and UserDetailService implementation classes. We're also going to discuss to a great extent about the PasswordEncoder as well. So far, we have seen the flow of form login, but how about HTTP basic flow, whatever flow that we have discussed, it is going to be the same for HTTP basic, except for one filter. When we are using the form login flow, the credentials will be extracted from the request by using these UsernamePasswordAuthenticationFilter. Whereas when we are sending the request by using the HTTP basic standard, another filter will be executed. The filter is BasicAuthenticationFilter. Inside this filter, we have a method with the name doFilterInternal(). So please uncheck these inherited members and please make sure you are opening the overrided method inside the BasicAuthenticationFilter. This method has all the logic here also. You can see from the request, they're trying to extract the credentials and convert it into the Authentication object. So if you open these convert method, you will be able to see they're checking that request header with the name authorization. So from the postman, when we are sending the credentials by using the HTTP basic, we're also sending the credentials by using these header here inside the postman. If I select the basic card and enter my credentials under the headers, you will be able to see there is a header created with the name authorization. The same, the framework is trying to read because this is a standard that everyone follows inside the industry. So whatever value that we get from these header value, they are removing the basic prefix that is getting added. So here, if you look for the value of the header, we are going to send the basic space as a prefix value. The same, they're trying to check and remove that value. So here, if you see they're using the substring where from the sixth character, they want to extract. So whatever extracted from the sixth character, it is going to be in the form of base64 encoded value. They're decoding it, once the decoding is completed, they're separating the username and password by using these colon as a separator. So whatever username and password that they separated, they are passing to the constructor of UsernamePasswordAuthenticationToken. Once this object is created, the same is being sent here. So now this Authentication object will be forwarded to the authenticate() method present inside the AuthenticationManager. From this method, it will go to the Provider Manager from ProviderManager it will go to the DaoAuthenticationProvider from DaoAuthenticationProvider. It will go to the InMemoryUserDetailsManager. It will load the user details from the memory followed by passwords will be matched by using the password encoder. All the flow that we have discussed will be same even for the HTTP basic. Only the way the credentials will be extracted from the request will vary. So what I'll do is I'll keep a breakpoint inside this filter and I also have a breakpoint inside the UsernamePasswordAuthenticationFilter. I'll keep a breakpoint here inside attempt authentication. This time when I'm sending the request by using the postman, the breakpoint is going to stop inside the BasicAuthenticationFilter, but not inside the UsernamePasswordAuthenticationFilter. From here, the request will be forwarded to the ProviderManager. Then it will go to the DaoAuthenticationProvider followed by InMemoryUserDetailsManager. So all this logic we already discussed. So inside the postman, I got the successful response. Let me revert the changes that we have done here I'll try to disable the form login followed by I'll go to paths config and here also I'll move these companies public path from the secured paths method to public paths method. Now we should be able to invoke the companies related API without any security. From the postman, I'm not sending any credentials. If I click on the send button, you'll be able to see I got successful response. Everything is working fine as expected. I hope you are clear with the demos.