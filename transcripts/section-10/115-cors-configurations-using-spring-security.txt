Let's test our REST APIs by using the UI application I open the home page of the Job Portal UI application And here I am not able to see any jobs and companies. Let me check the error. You can see my UI code still it is invoking old REST API paths. But right now we have updated our REST API paths to have the public towards the end. To make this work, I need to update my UI code. That's why what I will do is, first I will stop my existing running UI application by pressing CTRL+C. Followed by, I will copy the UI project code present under the section 5 into the section 10. My UI code present under the folder with the name job-portal-ui. Let me copy the same and paste it here under the section 10. Because I need to make some UI code changes. I can update the UI code directly inside the section 5. But the same code will not work for the previous project's backend code. Because we are using two different kinds of REST API paths. That's why I have to copy the UI code under the section 10 and all the updated UI code related to this section you can find under this folder. Let me open this UI code using Visual Studio Code. So here I will go and select the option file Open folder and go to the section 10, and select job-portal-ui. Here if you don't see this node modules folder, you need to make sure to run the command npm install. But since I already have the node modules, I don't have to run this command I can directly run the command npm run dev which is going to start the UI code at the port 5173. Inside this UI code, I need to look for a file with the name apa.js. In this file, I need to look for the contacts related path and here I need to append public. So that contacts related path, it's going to have the public path towards the end. Let me do the same for companies as well. Here I need to type public you don't have to make all these changes because I'm going to give you the updated code in the github repo. You just have to simply open the job-portal-ui code present under the section 10 and start your application and it is going to work without any issues. In the similar lines, I need to open another file with the name httpClient.js. And here there is a list with the name public endpoints. Here also I need to mention public for both the companies and contacts rest api path. So let me save these changes now I'll go to the browser this time if I refresh the page, I'll be able to see the jobs and companies details. This time if you see, I'm not getting any cors related error. But previously, at the starting of this section, we saw the communication is blocked due to the cors policy by the Spring Security Framework. But right now, we're not getting any cors issue. Because as of now, our UI application, it is only invoking the public API's. But the moment your UI app tried to invoke some secured paths, the Spring Security Framework, it is going to block the communication due to the cors restrictions. To overcome this problem, what we have to do? We have to provide the cors configurations by following the spring security standards. As of now, under the WebConfig file, we have provided these cors configurations. Let me delete all these code instead, we are going to make the cors configurations inside these JobPortalSecurityConfig. At the end of this class, I'm going to create a method which is going to return a bean of CorsConfigurationSource. Let me import this URL based CorsConfigurationSource as well. Inside this method, I have written very similar logic that we have discussed under the cors concepts so whatever cors configurations that we are trying to provide by this method, we need to feed the same to the Spring Security Framework How I can do that? Just like how we have invoked the csrf() method, we should be able to invoke another method with the name cors(). To this cors() method, we need to provide a lambda expression. First, let me mention the input variable name to the lambda expression as courseConfig. Using the same variable I'm going to invoke configurationSource() method. To this method, we need to assign the output bean that we are going to return when this method is invoked So I'm going to invoke this method so that the output from this method will be provided as an input to the configurationSource(). I hope you are clear on how to make the corsConfigurations() by using the Spring Security Framework. We just have to make very similar changes that we have discussed in the previous sections. Once this method is built along with these required configurations, we need to feed the same to the Spring Security Framework by invoking the course method here and providing this lambda expression.