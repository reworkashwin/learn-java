Based upon the discussion that we had in the previous lecture around the Spring Security internal flow, you may have a question, which is, Madan, can you show me that demo of this entire internal flow so that I can clearly remember the responsibility of each of these components? It's a very valid question. That's why inside this lecture, I'm going to show you that demo of entire Spring Security internal flow. Whatever knowledge that you're going to gain as part of this lecture, it is going to be useful in the coming sections when we want to enhance our web application by using the advanced concepts of the Spring Security framework. At the very first step, we know when an end-user enter credentials, the request will be intercepted by the Spring Security filters. There are multiple filters available inside the Spring Security framework. Each one, they have their own responsibility. One of the primary responsibility of these filters is to extract the credentials information from the request and convert that into an Authentication object. That's what we are seeing at the step two. There is a very good reason on why we should extract the credentials from the request at the step number two. Let's imagine there is no step number two in the Spring Security framework. And with that, what is going to happen? Whatever requests that we are receiving from the client applications, the same needs to be carried forwarded to the other components inside the Spring Security framework, like Authentication Manager and Authentication Providers and so on. And doing that is going to bring some challenges because at every component levels, the framework has to look into the request that it receives and accordingly extract the username and password every time. Instead of that, what framework team they have done is, let's introduce a step where we are going to extract the credentials from the request into an Authentication object so that the same Authentication object can be carried forwarded to all the components at any point of time. If any of the components, if they want to read the username and password or any other information, they just have to invoke the getter method available in the Authentication object. First, let's try to understand what are the important Spring Security filters available followed by I'm also going to show you at which place the extraction of the credentials is happening from the request. So the very first important filter is AuthorizationFilter. So inside the Spring Security framework, we have this filter. So the purpose of this filter is, it is going to examine each and every request that the backend application is receiving, and it is going to understand whether the path that is invoked by the client application is a public path or a secure path. If it is a public path, then it is going to allow the traffic. Otherwise, it is going to make sure that the end user is authenticated. In case if the user is already authenticated in the previous sessions, this filter, it is going to leverage the existing session ID details and the authentication details that we stored inside the security context. And accordingly, it is going to reuse them and allow the request to the controller layer. In case if the user is not authenticated, this filter, it is going to hand over to the next filter, which is responsible to display the login page. So there is a filter with the name DefaultLoginPageGeneratingFilter. Inside this filter, I'm going to look for the method doFilter(). And inside this method, you will be able to see there is a another method being invoked, which is responsible to generate the login page history email. I'll keep a breakpoint inside this method so that we can see whether our request is stopping inside this method when we are testing the form login style of authentication. Let me open the generate login page HTML inside this method, they are using an HTML template with the name login page template. Inside this template, we have all the HTML code that is responsible to display the default login page that we see on the browser. To show you the demo of this filter, I need to enable the form login. As of now, we have disabled it. Let me replace the disable related logic by using withDefaults(). Now in order to test the login page, we need to invoke a secured API. As of now, we don't have any secured API inside of our application. That's why what I'll do is I'll go to the PathsConfig and here I will move these companies related API from these public paths to secured paths. These are temporary adjustment that I'm doing after the demo I will revert all the changes. By using an incognito browser, I'm trying to invoke the API, which is api/companies/public. As soon as I press enter, you'll be able to see the request stopped at our breakpoint. If I release this breakpoint, we will see a login page on the browser. So this login page HTML is being generated by a filter with the name DefaultLoginPageGeneratingFilter. So both these AuthorizationFilter and the DefaultLoginPageGeneratingFilter, they are responsible to identify if the authentication is completed or not. If it is not completed, they're going to display the login page whenever someone is trying to access a secured API. Now let's imagine an end user entered his credentials. So I'm entering the valid credentials to Madan at 123 with the M capital letter. As soon as I click on sign in, another filter is going to be executed, which is responsible to extract the credentials from the request to the Authentication object. Let me show you what is that filter. The filter is UsernamePasswordAuthenticationFilter. So this filter will be invoked only when we are trying to provide our credentials by using the form login page. Inside this filter, we have a method with the name attemptAuthentication(). To this method, the framework is going to provide the request in the form of HttpServletRequest. So from this request, both the username and password will be extracted. So here there is a method with the name obtain username. This method is responsible to fetch the username details from the request by executing this logic. In the similar lines, we also have another method with the name obtainPassword(). So this method is going to take care of extracting the password from the request. So once the username and password are extracted, they both will be converted into an Authentication object. Here, both the username and password, they are being passed as an input to the unauthenticated method available inside the username password authentication token. So inside this method, they're simply creating an object of UsernamePasswordAuthenticationToken by taking the username and password. Let's try to understand what is this class. So this class, it is an implementation of Authentication interface. If you open this class, you'll be able to see this class is extending AbstractAuthenticationToken. And this abstract class, again, it is implementing the Authentication interface. Usually by using these Authentication data type object, we can store various kinds of information like authorities, credentials, principle. By using principle, we can store the username. By using credentials, we can store the password and by using authorities, we can store the collection of roles or authorities that an end user has. There are some helper methods as well to identify whether the authentication is already completed or not. Whenever the authentication is completed, the framework is going to invoke the setAuthenticated() method, which is going to set the authentication Boolean as true. And at a later point of time, the framework, it is going to invoke the method isAuthenticated() to identify whether the authentication is already completed or not. Let's go back to our main filter here the object of UsernamePasswordAuthenticationToken is being created by this filter. So since this class is implementing the Authentication interface, we can assume that this is an Authentication data type object followed by once the extraction of the credentials is completed, the same filter it is handing over this object to the AuthenticationManager. If you observe our Spring Security internal flow, once the filters logic is executed as part of the step three, the Authentication object will be forwarded to the AuthenticationManager component. There is an interface with the name AuthenticationManager. And for this interface, we have an implementation class with the name ProviderManager. Inside this ProviderManager, we have a method with the name authenticate() to which Authentication object is going to be provided as an input. I'm going to keep a breakpoint inside this method. Inside this providerManager method, what is the logic that is written by the framework team is they're going to look for all the authentication providers that are registered inside the project, and each of them, they are going to iterate. And during the iteration inside this for loop, they are going to invoke the authenticate() method available inside the AuthenticationProvider. We know based upon our previous lecture discussion, the Authentication Manager, it is going to take help from the Authentication Providers to actually perform the authentication. The same logic we are seeing here, but let's imagine there are three or four authentication providers are registered in a given project. Trying out all of them will become performance problem. For example, if there is an Authentication Provider, which is going to handle the UsernamePasswordAuthentication style, whereas there is another Authentication Provider, which is going to handle the OTP style of authentication. Let's imagine if the end user is providing the username and password for the authentication, then the Provider Manager should not invoke the OTP related Authentication Provider. It should only invoke the providers which are supporting the username and password style of authentication. So that's why inside this for loop as a very first step, the Provider Manager, it is going to ask each of the provider, are you going to support the given style of authentication by invoking the supports() method? So to this supports() method, we are going to pass what is the style of authentication right now happening. So if you see this style of authentication information, we are going to get by invoking the authentication.getClass(). In our scenario, the authentication style will be UsernamePasswordAuthenticationToken. So the same will be provided to the supports() method of the provider. If the supports() method confirms, yes, I'm going to support this style of authentication. Then only the next logic will be executed where it is going to invoke the actual authentication logic of the provider. Otherwise, a given Authentication Provider will simply skip by using these continue keyword. As of now, inside our application, we have not defined any Authentication Provider manually in this kind of scenarios. The framework is going to consider a default authentication provider with the name DaoAuthenticationProvider. I'll come to this provider in a few minutes. But before that, let's go to the Provider Manager. And here, if you see from the provider manager, we are trying to invoke the authenticate() method available inside the authentication provider. So the authentication provider, it is going to give the results confirming whether the authentication is successful or not. So this result is nothing but an object of Authentication here based upon this Result object, this copy details method is going to be executed. Apart from that, this method also has some logic to handle various kinds of exceptions that may occur while the authentication is happening. Now let's go to the DaoAuthenticationProvider. And this is an implementation of AuthenticationProvider interface if you open this abstract class, you'll be able to see this abstract class is again implementing AuthenticationProvider interface. The AuthenticationProvider interface, it has only two methods. The first one is authenticate(), where all the logic of authentication is going to be defined and the other method is supports(). By writing the logic inside this supports() method, the provider has to communicate to the provider manager on what style of authentications that it is going to support. So if I go to the DaoAuthenticationProvider and look for the method supports(). So this supports() is present inside the parent abstract class. So please make sure you are selecting these inherited members. So this is present inside these given abstract class. If I open this, you'll be able to see they are simply performing a check. If the given authentication class is same as UsernamePasswordAuthenticationToken, then it is going to return true, otherwise it is going to return false. So this is how the provider, it is going to communicate to the manager on which style of authentication that it is going to support. Next I'll open the authenticate() method inside the DaoAuthenticationProvider class. So let me select these inherited members and open these authenticate. As you can see this overrided method also present inside the abstract class, which is extended by the DaoAuthenticationProvider. So far we understand on how the request it is going to reach the authentication providers. These authentication providers, they again take the help of UserDetailsManager and the PasswordEncoder. Let's try to understand more details about these components as well in the next lecture.