Since we are into the new section, I have copied the code from the previous section into this folder. You can find all the code changes that we're going to discuss inside this section under this folder. Since I'm looking to build a new rest API, which should return all the contact messages. First what I'll do is, under the dto package, I'll try to create a new record class with the name ContactResponseDto. . Inside this record class, let me mention the field names. These are the fields that I want to send from my backend to client application. The fields are id, which represents the primary key value of the contact message, followed by name, email, user Type, subject, message, status, and createdAt. Please make sure you are also using the same field names like me, because unless you send the data by using the same fields, the UI code is not going to work. Next, I'll go to the ContactController. Inside this, I'm going to build a new REST API that can be invoked only by admin. I'm going to paste a method. You can get the same from GitHub repo. Since you are already familiar with the developing of REST APIs, from now onwards, I am not going to discuss everything around on how to build a given REST API method. I hope you're fine. I'm doing this just to save some time of yours. On top of this method, I'm using getMapping since this method only reads the data. And to this, I'm mentioning the path as forward slash admin. This is very important because this is going to give a clue to me that any URL which is ending with admin, by looking at it, I can easily guess that URL or that REST API can only be invoked by an admin user. So the method name is fetchOpenContactMessages(). Instead of open, maybe I can mention fetchNewContactMessages(). This method is not going to accept any input parameter. From this controller method, I want to invoke a method inside the service layer. So let me go to this IContactService and here I'm going to create a new abstract method. This method name also I'm going to mention it as fetchNewContactMessages(). Let me use the same inside the controller now I need to go to the implementation class and overwrite this method. There is an implementation class with the name ContactServiceImpl. Inside this class, I'm going to overwrite the method fetchNewContactMessages(). From this method, we need to call the method inside the repository interface. That's why first what we can do is, we can try to build a new abstract method inside the ContactRepository. This method is going to return list of contact messages. And since I'm looking to findContactsByStatus(), I'm going to mention by status here and to this method, we need to pass status as an input. Here I'm using the derived query method. There is no need of using @Query annotation because my requirement is very simple. I want to load the data from a given table based upon the status. There are no multiple tables involved here. Let me go to the service layer, and here from this method, I will try to invoke contactRepository.findContactsByStatus and to the same, I need to mention the status value, which is NEW. The return type we know we are going to get list of Contacts. The same I'll try to catch as an output by using a variable name called contacts. This new status, instead of directly sending the hardcoded value, what I'll do is, I'll try to maintain it as a constant inside the application constants file. Here I'm going to create two different constants with the variable NEW_MESSAGE and CLOSED_MESSAGE. I need this closed for the future updates. For now, let's use this new message inside our business logic. Here I'm going to use application constant using the same I will invoke new message constant. Once we have the list of contacts records, we need to convert them into DTO objects. To convert from entity to DTO object, I'm going to create a new private method inside the same class. The method name is transformedToDto(). To this method, we are going to pass the contact object and it is going to create a object of contactResponseDto and return the same by using all the data available under this contact entity object. Let me remove this commented code. And here also, instead of hardcoding the new status value, I will try to refer it by using the new constant that we have created. Now by using this contacts object, I'll try to iterate all the records using stream API and during the each iteration, I'm trying to invoke the transformTDto() private method. Once all the data is copied from the entity objects to DTO objects towards the end, I'm trying to collect them into a list with the name responseDtos. The same I'm trying to return from this method. Now I can go to the controller layer and inside the controller layer, here I'm trying to accept the output with this variable name and the same I'm trying to send under the body of the response with the overall status as OK. Let me save these changes and go to the PathsConfig. This time, what I'll do is, I'll try to create one more bean, which is going to have all the parts of admin. So the bean name will be adminPaths, the same method name I'm going to mention it as bean name as well. Under this method, I'll mention the new path that we have developed. The path is api contacts admin. This path, I want to be invoked only by the admin user. To enforce that authorization, I need to go to the class JobPortalSecurityConfig. Here, just like how we are invoking the permitAll() and authenticated() method, very similarly we need to invoke the authorization related methods. What I'll do is first, I'll try to inject the new bean that we have created with the name adminPaths. Once the bean is injected using the same, I will try to iterate each of the path and invoke the authorization related methods. So here, just above the secured paths, I'm going to invoke the adminPaths variable and using the same, I'll try to invoke the requestMatchers and for each of the path, I'm again invoking the hasRole() method. To this method, I'm passing the ADMIN as a role name. And here I didn't invoke the hasAuthority() method because I don't want to use the authority style of authorization. Instead, I want to follow role style of authorization. And here, one more important point that you need to be aware is you don't have to mention this role underscore. The reason is when you invoke this hasRole() or any of the similar methods behind the scenes, it is going to add that prefix. On top of this method, you'll also see the details, the role that should be required, which is prepended with the ROLE_ automatically. That is USER,ADMIN, etc it should not start with the ROLE_. Let me close this. After making these changes, always please make sure you are providing the permittal configurations as the very first configurations, followed by your authorization configurations and towards the end, you can provide the authenticated configurations and if any path that is not matching with any of these configurations, then for that, denyAll() configuration is going to be applicable. So with these changes, I'm assuming we made all the authorization related changes. Let me save these changes,do the build once the build is completed, I'll try to start my application. Once the application is started, let's validate the data inside the contacts table. As of now, there are total 16 records inside my database table with the status as NEW. In case, if you don't have enough data in your contacts table, please create some test data so that you can test this scenario properly throughout this section. In the coming lectures, I'm also going to show you on how to implement the sorting and pagination using Spring Data JPA. To test those scenarios, you need at least 10 plus records. Now, if I invoke the new REST API that we have developed so far, I should get all these records inside the response. So there are total 16 records. From the postman, what I'll do is first, I'll try to invoke the REST API as a normal user. So let me go to the CSRF token. And first I will get the CSRF token this value I will use during the login operation. Under this login request, I'm providing the normal user details john@gmail.com and this is the password. Under the headers, I need to mention the correct  CSRF token value. If I click on the send button, I'm going to get the JWT token. Inside this JWT token, for this user, the role will be role underscore job seeker, but not the admin. That's why if I try to invoke the new REST API that we have developed so far, it is going to throw an error. Under this security folder, you will find another folder with the name Admin. Under this folder, you have a request with the name, GetOpenContactMessages. Maybe instead of open, let me mention, GetNewContactMessages and this is the path that we need to invoke. Under the Auth, I need to select the Bearer token so that I can send the JWT token value. Here I'm pasting my new JWT token value. The same value we can see under the headers with the header name as Authorization. Let me mention the correct to XSRF token. So this is the correct token value. The same I'm trying to mention here. If I click on the send button, I will get 401 unauthorized, but the user is already authenticated. Why I'm not getting any response because we enforced authorization rules as well. Apart from having a valid credentials, the user should also have correct roles as well. This time, what I'll do is, I'll try to log in as an admin. Let me go to the login API and here I will mention the username as admin@gmail.com and this is the password for this I'm going to get a JWT token. Let me take the JWT token and paste it here under the auth if I click on the send button, yes, this time we got the successful response. And these are all the contact messages that we have inside the database. So this gives a confirmation to us that authorization is working fine, but there is a problem. Whenever the authorization is failing, the Spring Security Framework, it is returning the 401 error instead of 403 error. Ideally, we should return 403 whenever authorization fails. By default, whenever there is an issue with the authentication or authorization, the Spring Security Framework, it is going to execute the logic under the BasicAuthenticationEntry point. Because right now we are using the basic style of authentication. Inside this, if you closely observe, there is a logic under this commence() method. And this logic, what it is doing is, whenever there is an authentication related exception, it is simply sending the 401 status code. I can keep a breakpoint inside this method and show you a demo as well. So what I'll do is, this time I'll try to log in as a normal user and this is the JWT token let me take the JWT token and paste it under this new REST API. If I click on the send button, the breakpoint stopped inside this BasicAuthenticationEntry point. If I release this breakpoint, we're going to get 401 Unauthorized. And inside the response Headers, you will see there is a header with the name www-Authenticate and with this basic real as real value. So from where this is coming? It is coming from this method itself. Inside this method only, apart from setting this unauthorized status code, the framework team, they are also sending this header with this value. But we don't want this default behavior. Instead, we want to return 403 whenever authorization fails. Let's make those changes in the next lecture.