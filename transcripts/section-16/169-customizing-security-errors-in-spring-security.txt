Right now, the problem that we are facing is whenever the authorization is failing, we are expecting the 403 error, which is a standard, but instead we are receiving the 401 error. And the 401 error is coming from these BasicAuthenticationEntryPoint. Let's try to understand why we are getting 401 error. First let's open this authentication entry point and try to understand what is this interface. So this interface, it is going to be used by the ExceptionTranslationFilter. I'm going to open this filter. We know inside the spring security framework, we have many filters which are taking care of various responsibilities. This is one such filter, which is going to have the logic on what should happen whenever there is an security related exception happens. So let's try to read the documentation of this filter. So this filter, it is going to handle AccessDeniedException and AuthenticationException thrown within the filter chain. So whenever the authentication itself is failing, the framework is going to throw an AuthenticationException. If an Authentication exception is detected by this filter, the filter will launch the authenticationEntryPoint. So basically whatever logic that is returned inside the authenticationEntryPoint, the same will be executed. But in the case of AccessDeniedException, the filter will determine whether the user is an anonymous user. If yes, then the logic inside the authenticationEntryPoint will be launched. Otherwise it is going to execute the logic present inside the AccessDeniedHandler. For this interface, there are many implementation classes, but this filter, it is going to use the AccessDeniedHandlerImplementation logic inside this class so there is a logic to handle AccessDeniedException. From here if you see, they are trying to return the forbidden status code or 403 status code. If I go to this filter and look for the list of methods, there is a method with the name handleSpringSecurityException(). Inside this method, they are checking whether the exception instance is AuthenticationException or AccessDeniedException. In our scenario, whenever the authorization fails, the handle AccessDeniedException is going to be invoked and inside this method, they are checking whether the user is an anonymous user. If yes, they're going to invoke this if condition where the logic present inside the authenticationEntryPoint is going to be executed. Otherwise, whatever logic that we have inside the accessDeniedHandler will be executed. So basically in our scenario, the accessDeniedHandler is not being executed for some reason Instead the authenticationEntryPoint logic is being executed. Even if the exception type is of type accessDeniedException. I know why this is happening. Let me go to the place where we have configured all our spring security changes. So here, if you see, we are enabled the HTTP basic, but nowhere we are using the HTTP basic style of authentication. As of now, inside our application, the authentication is happening manually based upon the logic written inside the AuthenticationController. So inside the AuthenticationController from these requestDto, we are extracting the username and password and invoking the authenticate() method. Since we are not using HTTP basic style of authentication, we can try to disable these right now What is happening? We have enabled this, but we are not using this instead, we are going with the manual style of authentication and that is creating some problem inside the spring security framework. So that's why what I'll do is, I'll disable the HTTP basic authentication as well, because we are nowhere using the style of authentication. So let me invoke that disable() method. So this time, if I save the changes and do the build, the issue should get resolved. So if I click on the send button, as you can see, this time we got the 403 forbidden error. So this solves the problem, but I want to send more details. So instead of sending only the status code, I also want some meaningful data inside the response body as well. So in this kind of scenarios where you want to send some custom response, either inside the 401 scenario or in the 403 scenario, you should be able to do that very easily, just like how you are invoking all these methods by using these HTTP. Similarly, there is a method with the name exceptionHandling(). To this method, we can provide all our exceptionHandling(0 logic. What I'll do is, I'll try to provide a lambda expression to this method. You can get this lambda expression from the GitHub repo as well. If you see here, my lambda expression, it is accepting a variable name exception using the same variable name I'm first invoking the accessDeniedHandler. So whatever logic that I have written inside this accessDeniedHandler() method, the same will be executed whenever an accessDeniedException is happening inside my application. And when this is going to happen, whenever the authorization fails. In the scenario of authentication exception, this authenticationEntryPoint logic is going to be executed. So basically inside these method bodies, if you see, I'm trying to send the status forbidden by using this response.status. And next by using the response.content type, I'm setting the content type as application slash JSON. And the next line is very important by using these response.getWriter.write, I'm trying to write the whatever content that I want to send inside the response body. So inside the response body, I want to send a JSON object with the error as AccessDenied and the message as you don't have permission to access this resource. Whereas for the authentication exception scenario, I want to send 401. And in this scenario, the error will be unauthorized and the message will be authentication required. Let me save these changes do the build. From now onwards, we're going to get some response body as well. If I try to invoke this REST API with the normal credentials, you can see I got an error saying that accessDenied with the message as you don't have permission to access this resource. Let me go to the login page and here I'll try to provide an invalid password. If I click on the send button, this time I'm still getting 403 forbidden error. I'm expecting the 401. Seems my CSRF token might have expired. Let me get a new CSRF token. This is the new token and I'll go to the login under the header I'll mention the correct XSRF token and coming to the body. I'm providing a invalid password. If I click on the send button, this time I'm getting a 401 unauthorized, but I'm getting this response structure. This is because from our AuthenticationController method, we are building our own error response by using these buildErrorResponse() method. We can decide whether we want to have this or we want to use the logic that we have written here. What I'll do is, I'll come in this logic because we are already handling the authenticationException properly inside the AuthController and we are also sending a meaningful response here. With this, all our problems should get resolved. Let me try to log in with the admin user. So for admin credentials, I'm going to get a JWT token. I will use the JWT token and I'll go to these get new contact messages. Let me paste the token here. I also need to mention the correct CSRF token. This I will mention under the headers. If I click on the send button, I got a successful response. So this gives a confirmation to us that the both authentication and authorization is working. I hope you're clear on how to enforce the authorization by using the methods like hasRole(),hasAuthority(). . As of now, we saw a demo by using hasRole() only, but it should be very similar even if you invoke the hasAuthority() or any other methods, but please make sure when you are using the authority style of authorization, you need to provide your complete name. So whatever you configured inside the database, the same has to be used here as well. But as I said previously, whenever you are using hasAuthority() methods, your authority names should have the action names, but not the role names.