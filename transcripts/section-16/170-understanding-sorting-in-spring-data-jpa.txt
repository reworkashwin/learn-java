With the whatever REST API that we have developed so far inside this section, it has some drawbacks. It is always going to return all the new contact messages that we have inside the database. Think of a scenario where there are thousands of records and sending all those thousands of records in a single shot is going to bring some performance problems both on the backend side and on the client side. And it won't give a good feel as well to the end users if we are trying to display all the data in a single page. That's why as a next step, we're going to focus on on how to sort the data. Once we are clear about the sorting of the data, we are also going to explore about the pagination as well. As of now, when we invoke this API, we are getting the response with the random order. There is no sorting order, but we should give a flexibility to the client applications on how they want to sort the data when they are trying to fetch large amount of data. Sorting of the data by using Spring Data JPA is going to be very easy. Inside the Spring Data JPA framework, we have a sort to class. By creating the object of this class, we should be able to communicate our sorting requirements. Spring Data JPA supports sorting and pagination in the form of PagingAndSortingRepository interface. When we're talking about sorting inside Spring Data JPA, there are two types of sorting. The first one is Static Sorting by using the derived query methods. Static sorting means sort order is going to be fixed at compile time, cannot be changed dynamically by the user. And sorting is defined inside the repository method name or inside the query annotation. Static sorting is best when the sorting requirements are always same. So here I have defined a method by using derived query method concept. The method name is findByStatus and towards the end, I'm using OrderBy. To this OrderBy, we can provide what is a field that we want to consider to perform the sorting. After the OrderBy, since I have mentioned the name behind the scenes, once the framework faced all the records based upon the status, it is going to perform the sorting operation based upon the name value. After the field name, I also provided ASC, ASC means ascending. In case if you are looking to sort the data based upon descending order, you can define a method by similar to this one, here the method name that I provided is findByStatusOrderByCreatedAtDesc, Desc means descending order. In this scenario, what is going to happen is, the Spring Data JPA framework it is going to fetch all the list of customers based upon a given status, followed by it is going to perform the sorting by using the CreatedAt field and descending order. In case if you are using query annotation to provide your requirements, then either inside the JPQL query or inside the native SQL query, towards the end, you can mention something like Order By what is a column name and ASC or DESC. If you are using JPQL, you have to use a field name here. Otherwise with the native SQL query, you have to use the actual column name. We should be able to use static sorting only when the business rule never changes. Same sort order is used everywhere simplicity is more important than flexibility. But for the scenarios where you are looking for the dynamic sorting capabilities, you can leverage the sort class that is provided by the Spring Data JPA framework. Using this sort, we can tell which field to sort by either ascending or descending order. And accordingly, behind the scenes, the JPA automatically creates the Order By query for us. So this is the basic sorting example. So first I'm trying to invoke the by() method to this by() method I'm passing the name of the field and whatever sort object that I created by using these by() method, the same I'm going to pass as an input to the Spring Data JPA methods. We know there is a method called findAll() to this, it is going to accept a sort object and based upon this sort object, while it is trying to fetch all the records, it is going to sort the records based upon the name field. In case if you want to provide ascending and descending information as well, after providing the field name details, you just have to invoke the corresponding method like ascending or descending and this object you can pass as an input to the methods that you are trying to invoke. So these are all the examples where we are trying to perform the sorting by using a single field. But if you have a scenario where you want to sort based upon multiple fields, here is such example by using sort first, I'm trying to invoke the by() method and to this by() method, I'm providing my various sorting requirements. My first requirement is to sort the records based upon the role field and by using the ascending. So that's why here I can use sort.order.ascending and to this ascending, I can pass the field name. I'm also providing sort.order.descending and to the descending, I'm providing the field name as CreatedAt, with this sort object What is going to happen is, first to sort by role by using ascending order, followed by in case if the role is same sort by CreatedAt and descending order. In case if you are looking to use this sort object along with your derived query methods, then it is very easy. Your derived query methods, they just have to accept the where condition field along with the sort object. So here I have defined a derived query method with the name findByStatus() to this method I'm passing two input parameters. The first one is, Status which is going to be used inside the where condition followed by sort object under this sort object I can provide whatever sort requirements that I have. Here, my requirements are to sort by name. The same I'm trying to pass as an input whenever I'm invoking this method. Here is an example. When I'm invoking this method along with the status, I'm also passing the sort object, filtering plus sorting is going to be a powerful combination because most of the times we don't want to randomly return the data to the client applications. There has to be some sorting logic so that the client applications can iterate those records based upon their business requirements. I hope you're clear with this theory discussion. In the next lecture, let's see the demo of this sorting.