As of now, inside our job portal application, we only implemented authentication. Any logged in user should be able to access any of the secured APIs that we have on the backend side. But as part of this new section, we are going to focus on authorization as well. In simple words, authorization is nothing but role-based access mechanism. For example, if I try to log in as an Admin, under the admin login, we should be able to see all this menu. As part of this section, we are going to focus on only on these contact messages functionality. On the backend side, we are going to build a REST API that supports contact messages page. Only Admin users should be able to invoke this REST API. If any other user is trying to invoke this API, they should get an error. Let me show you what we are going to build as part of this contact messages. As soon as Admin click on this link, this page is going to appear. Inside this page, we are going to display all the contact messages received to the Admin so that the Admin can review all the queries received and solve them. Once admin talk to the customer or the end user, and after solving the problem, the admin can update the status by clicking on this close button. The admin also has an option to see more details by clicking on the message. So these are the other details where the admin can see what is the subject and message submitted by an end user. Apart from displaying and updating the contact message records, this page is also going to support sorting of the records based upon a given field and direction. So here the admin has an option to sort all these records based upon any of these values. For example, if I select a name, the record sorting should happen by name and direction also the admin has an option which controls on how the records are being displayed on the UI. So whatever records are fetched from the backend, so all of them, they are going to be displayed by using the pagination concept. As of now, inside my backend database table, I have around 16 records. That's why two pages are being displayed here for each page by default, we're going to display 10, but the admin has an option to change this value. For example, if the admin select 25, all the records will be displayed as part of a single page. So this part is called as pagination. So where the admin is going to have to navigate to the next page, previous pages based upon his requirement. So the sorting logic and the pagination logic, we are going to build on the backend side by using the Spring Data JPA framework. Because in real enterprise applications, sometimes we may have thousands of records that we are supposed to be displayed on the UI, but sending all those thousands of records or millions of records at a time is not a recommended approach because it is going to bring a lot of performance issues, both on the UI side and on the backend side as well. Because you have to transmit a lot of data over the network and the UI also has to process all that record to display to the end user. That's why we're going to follow a standard where we are going to implement the sorting and the pagination logic on the backend side. So based upon what my front end application is asking, accordingly, we're going to send only those set of records. For example, from this UI application, if the sorting requirement is based upon name and the direction is descending and per page is 25, then based upon the sorting and pagination requirements, we're going to send only 25 records that are going to be displayed as part of the page one. If there are more records, then the end user is going to see page numbers like two and three. So based upon which page the end user is navigating, a backend request will go and the corresponding page records will be sent back by the backend API. So this is all the functionality that we are going to discuss throughout this section. Primarily, we're going to talk about three important topics. The first one is around the authorization. By using this authorization, we're going to enforce role-based access mechanism for our backend REST APIs. We are going to build a mechanism in such a way that only an admin login user should be able to access the backend REST API that is required by these contact messages page. Apart from authorization, we're also going to focus on second topic, which is sorting followed by the third topic, which is pagination. So these are the primary topics. I hope you are clear about the agenda of this course. If you're not using this UI application, don't worry I'm also going to show you how to test all these features by using the Postman as well.