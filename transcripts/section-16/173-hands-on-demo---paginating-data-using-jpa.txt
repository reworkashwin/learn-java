I'm going to build a new REST API that is going to support both sorting and pagination. Inside the Contact Controller, you can find this code. The path that is going to be supported by this method is, page slash admin. The method name is fetch new contact messages with pagination and sort. To this method, the client applications, they can pass four different input values. The first one is, around the what is the page number that they want to access, followed by what is the page size, next what is the sort by criteria and the sort direction. In case if the client applications, they're not providing these query params, then I'm going to construct these default values, all these query params that I received, I'm trying to pass as an input to the service method that I'm going to create inside the ContactServiceInterface. . Let me go to IContactService and here I'm going to create a new method with the name fetchOpenContactMessagesWithPaginationAndSort. The method name is very lengthy. I'm doing this for you so that you understand the purpose of the method by looking at the name. So this method, it accepts four input parameters and the return type of this method is, page of ContactResponseDto. So this page is an interface inside the Spring Data Framework, whatever objects that we're trying to send to the client applications, they need to be wrapped by using this page. Let me go to the implementation class and override the method that we have defined inside the interface. . Here, I'm going to paste few lines of code. As you can see, by using the four input parameters that we have received first, I'm trying to create the sort object. This sort object is very similar to what we have discussed in the previous lectures next to create the pageable object I'm taking the help of this pageRequest.of() method to this method I'm passing what is a page number and page size along with the sort object. So this pageable object, it is going to represent all our pagination and sorting requirements. Next inside the ContactRepository. As of now, I don't have a method which is going to accept this pageable object. Let me go to the ContactRepository and here I'll try to duplicate this method. This time instead of accepting sort, it is going to accept the pageable as an input. Before we try to use this method, we should also make sure we are wrapping the response by using the page. So this page we need to import from the org.springframework.data.domain. Now the compilation error that we have inside the service class is resolved. By the time our repository method is processed, we are going to get the output in the form of page of contact. The same I'm trying to accept by using this variable name. Next to using this variable name, I'm invoking the map() method and to this map() method, I'm passing the private method transformToDto, which is going to take care of converting the data from this contact entity object to ContactResponseDto. And towards the end, I'm trying to return this as an response. Let's go to the contact controller. Here I'm still getting the errors. Seems I have given an invalid name. Let me go to the IContactService. So let me mention the correct method name. The same I need to correct inside the ContactServiceImpl as well. So now there should not be any problem inside the ContactController. So whatever we received from the service layer, the same we are trying to send as a response to the client applications under the response body. Let me save these changes, do the build. Now since we are done with the business logic, as a next step, we need to enforce authorization for this path. Let me go to the PathsConfig and here I'll try to mention the new path that we have created. The new path is API contacts page slash admin. Let me save these changes, do the build once the build is completed. First, I'll show you on how to test this API using Postman, followed by we can also try to test using the UI application. Under the admin folder, you will see a new request with the name, GetNewContactsMessagesWithPage. While invoking this REST API, the client applications, they can send four different query parameters. The first one is page number, followed by page size, sortBy, and sortDirection. If I click on the send button, I'm going to get the response. We know we are sending the entire content by using the page. That's why I'm able to see this content here. Under this content, we are going to have 10 records, which belongs to the first page. If I keep scrolling after the 10 records, I'm going to have other metadata information like empty false, is this the first page, is this the last page What is the page number? What are the total number of elements inside this page? And towards the end, you should be able to see what are the total elements that the Spring Data JPA identifies inside the database. And these 16 records, it will be accommodated by using total two pages. This kind of metadata information is going to be helpful for the client applications as well. On the client side, they don't have to do the calculations again. They can simply rely on this metadata information to display the data by using the pagination. If I try to mention the page number as 1, then this time I'm going to receive only 6 records because inside my database, there are only 16 records with the status new. If I click on the send button, this time you will be able to see only 6 records are fetched, and these are the records belongs to the second page. So this is how you can test with the Postman. Now let me show you on how to test with the UI application as well. In order to test with the UI, you need to make sure that you have developed the same REST API path, because inside the UI code, I have configured this path to invoke when an admin is navigating to the contact messages page. First, we need to make sure we are logging in with admin credentials because only admin can see the menu related to the contact messages. Now under this menu, as soon as I click on this contact messages, a request will go to the backend REST API, which is going to fetch the first 10 records based upon the default sort criteria I have defined here. What is the default sort criteria? It is sort by date created and the direction is ascending and per page I want only 10 records. If I'm mentioning five here, then only the first five records will be displayed. Whenever I'm navigating to the next page or whenever I'm changing the sorting requirements, a new request will go to the backend REST API. I can show you the same as well. Let me clean this console. Now if you see, there are total four pages. If I click on this fourth page, I will see only a single record because in total, we have only 16 records. That's why the 16th record falls under the fourth page. And to display this data, which is specific to fourth page, the request will go to the backend that's why I'm able to see all these logs. If you see, this is the query that is fired by the Spring Data JPA inside the where condition, it is using the status and coming to the Order By it is using the createdAt and it is also using the limit keyword, which can be understand by the MySQL database. In case if you are using some other database like Oracle or Postgres, the Spring Data JPA framework, it is going to take care of using the corresponding keywords. Let me go to the first page and this time, the sorting I'm going to do based upon the name, the records are sorted based upon the name, since most of the names I have used my name, here also I have the same names. If I go to the next page here the names changed and coming to the last name, it is Will Smith because we have mentioned the direction as ascending. I hope you're clear on what we have done. We developed a REST API that is capable of handling the sorting and pagination. By taking all of our response, the client applications, they can understand and implement pagination very easily. Off course, inside the UI code also, we have to write some logic to display all these numbers and these dropdowns, someone is changing these dropdowns, a new request has to be sent to the backend application. So that is everything is going to be handled by the UI code. Since this is a backend focused course, I'm not discussing anything around the UI application, but I'm trying to test the same by using these UI applications so that you get an end to end feeling. In case if you are interested to learn about frontend as well, then please do check my full stack course, where I covered react on the UI side and the spring boot on the backend side. Now with this data, what admin is going to do? By looking at this data, the admin is going to understand what are the current open messages that needs to be addressed by him. For example, if he sees that so and so person is not able to create an account due to this technical issue, this admin can assign someone to reach this person and resolve this problem. Once the problem is resolved, the admin can click on this close button, which is going to update the status of that particular record from new to closed. For now, if you click on this close, you're going to get an error saying that failed to close message. Please try again because we have not implemented any supporting backend REST API. Let's try to create a new REST API, which is going to handle the updating of the Contact Message record from new to closed.