Inside spring security framework, there is an interface with the name GrantedAuthority. So using these interface only, we are going to represent the authority details of an logged in user. This interface has a single abstract method with the name getAuthority(), which is going to return the authority details or authority name in the form of String data type. For this interface, there are implementation classes. Most of the times developers, they will end up using SimpleGrantedAuthority. The same we're going to use inside these codes as well. This class has a constructor, which is going to accept authority details as an input. So whenever someone is trying to create an object of this class, they need to provide the authority detail. The same will be stored inside these role variable. And whenever someone wants to read the role detail or authority detail from this class, they need to simply invoke this getAuthority() method, which is going to return the roles information that we have stored while we are creating the object. Now with this information, let's try to understand what is happening during the login operation. We know the login logic we have implemented as part of the JobPortalUsernamePasswordAuthenticationProvider. Here inside this authenticate method as a very first step, we're trying to load the user details from the database by passing the username as an input. Whatever details that we stored inside the database, they are going to be represented by these object jobPortalUser. If you open these entity class inside this class, we have all the user details along with that we also have role information that is associated for a given user. That means whenever we are loading the user details from the database, the roles information will also get loaded into the jobPortalUser object. Now using this object, we are trying to invoke the getRole() method, which will give us the object of role. And by using the Role object, we are again invoking the name of the role. So whatever name that we stored inside that database for a given user, the same we are trying to convert that as an object of SimpleGrantedAuthority because Spring Security can understand the roles and authorities information only if you are passing that information by using these SimpleGrantedAuthority to the constructor of this class, we are passing the role information and with that, whatever we have provided, the same will be stored. And at any point of time, if the Spring Security framework want to know what is the authority or role information for a given user, it is going to simply invoke this getAuthority() method. Once we created the object of this class, I'm trying to convert that as a list, because inside the authentication object, we have to store the authority details or roles details by using a collection object. Inside our job portal application, we have a business requirement to have a single role for a given user, but in some real enterprise applications, they may have a requirement to have multiple roles or authorities for a given user. In that scenario, what you have to do? You just have to make sure you are defining the One-To-Many mapping inside the jobPortalUser against to the role mapping and whatever database setup that we have right now, it won't work because with the setup right now, we have all the roles, static information we are maintaining inside these roles table. And whenever a new user is getting created, we're simply mentioning these roles table ID value as a value under the role_id column. If I scroll right side there is a role_id, and this will work if the user has only a single role. But in case, if you want to maintain multiple roles, as I said, you need to make sure you are defining One-To-Many relationship mapping inside the entity class. On top of that, you should have one more table as well, which is going to store all the roles information for a given user. You can create a table like user_roles, where you're going to have the user_id along with the role_id. That way you can maintain multiple roles record for a single user. For our scenario, this database setup and whatever configurations that we have done should be good enough. So here, since we have a single role, I am invoking the getRole() method, which is going to give me a single role object. Whereas if you are defining One-To-Many relationship, then you're going to get a collection object, that collection object you need to iterate and from each element, you need to read the name and create the SimpleGrantedAuthority object. And all those SimpleGrantedAuthority objects, you can try to store them inside a list. Once you have the list is ready, when you're trying to create the object of UsernamePasswordAuthenticationToken or Authentication object, you have to pass authorities information as a third input parameter. If I open this constructor, you'll be able to see whatever collection of authorities that we are passing by using the same, this super constructor is getting invoked and inside this super constructor, they are trying to perform some basic null checks. Once all the validations are passed, whatever authorities details that we are providing, they are getting stored as an unmodifiable list inside the framework. That means once the authentication object is created, no one can change their roles or authorities information. This has been done intentionally for security reasons. So this is what is happening inside the login operation. Let's imagine once the login is completed, the user is trying to perform some other actions. We know after the login for every subsequent actions, the client application, they are going to send that JWT token because during the login operation, we are sending the JWT token based upon the logic that we have written under the JWT util class. So here we're trying to send all the roles information to the client application as well by reading the authorities details using authentication object and getAuthorities() method. So to the client application, we are sending all the authorities details as a single String, but all the multiple roles information, they are going to be separated by using comma as a separator. Now, since the same JWT token is going to be retrieved at a later point of time, as part of the JWTTokenValidation inside this filter, once we extracted the roles information from the claims, we are invoking a method available under the AuthorityUtil. So this method is going to accept all your roles information as a singleString, which are separated by comma and behind the scenes, this method is going to convert all your authorities data, which is in the form of String into a list of GrantedAuthority. The same again, we are trying to pass as an input to the third input parameter of the constructor. So again here, the same story is going to happen so whatever authority details that we are providing to the framework, the same will be stored by invoking the super constructor. So this way for all the subsequent operations as well, the spring security framework will have information about what are the authorities of an end user. I hope you understand on how we are storing the authorities or roles information by using the spring security framework. As a next step, we are going to focus on, on how to communicate our authorization requirements to the spring security framework. Unless we tell to the spring security framework that please protect so-and-so API with so-and-so authorities or roles, the spring security framework will not enforce any authorization. And that is what is happening right now inside our application. That's why as a next step, we are going to focus on enhancing our application with the authorization mechanism as well.