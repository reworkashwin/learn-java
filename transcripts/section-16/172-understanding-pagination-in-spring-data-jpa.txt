Let's imagine an application where it is trying to send 50,000 records as a response to the client application. Sending such humongous amount of data at once will create performance issues and it is going to bring some bad user experience as well. Imagine you opened amazon.com. Inside the homepage, if they try to load all the products that they have, which are in millions, how do you feel? You are going to feel frustrated because it is going to take good amount of time to load all those products and displaying all of them inside one page is not a good user friendly. Instead, if the pagination is implemented, that is going to bring some better user experience. So by using pagination, we can break large data into smaller and manageable pages. We can think of this pagination like a book. While reading a book, we are not going to read all pages at once. Instead, we're going to read one page at a time, followed by we're going to move to the next page when needed. Pagination works the same way with the database records. To implement this kind of pagination functionality on the client side, from the backend, we should support the pagination concept. Luckily, Spring Data JPA also provides pagination support. Since pagination and sorting,they are very common scenarios. The framework team, they thought about it and they provided required interfaces and classes using which we should be able to implement these features very quickly. Around the pagination, inside the Spring Data JPA, we have a pageable interface. This interface is implemented by one of the most commonly used class with the name pageRequest. By using this pageRequest, we can communicate our sorting requirements and which page records we want to see and what is the page size as well. I'm going to show you an example on how to use this pageRequest in few seconds. Coming to the next important component inside the Spring Data JPA pagination, it is page. Whenever you're trying to implement pagination by using Spring Data JPA, the response that you're trying to send to the client application, it needs to be wrapped by using this page. As of now, whenever we want to return list of contact response DTOs, we're sending by using the list of contact response DTO. But with pagination, instead of list, we should use the page. When we're sending our DTOs inside the page, the response, it is going to contain the data along with some pagination related metadata. When we are using pagination, we don't have to write any SQL logic on using limit and offset keywords. Everything will be taken care of by the Spring Data JPA. So this is a basic pagination example, so by using the pageRequest class, I need to invoke the of() method. To this of() method, I'm passing two input parameters. The first input conveys what is the page that we want to access. Always the page index starts from zero, just like inside arrays, the first element start at zero index. In the similar lines, the first page will be represented by zero. So since I'm looking to access the very first page, I'm trying to pass zero here. Coming to the second input parameter, it represents what is a page size , with 10, I'm telling you can send up to 10 records inside a single page. In case if I'm passing 1 ,10, then the next 10 records will be sent inside the response. So once we have defined this pageRequest object, the same we can pass to any of the Spring Data JPA methods. So there is a findAll() method to this, we can pass the pageRequest object. And the return type from these methods will be page of the entity object. So page of entity object means this page will have multiple customer entity objects. This page is more than just a list, it provides methods like getContent(), which is going to provide the actual data that is stored inside these page object. Similarly, we can invoke getTotalElements(), which is going to give us total records that are stored inside this page object, next, we have getTotalPages(), which is going to give what are the total pages that are possible for the number of records that we have inside the database. For example, if I'm going with the page size as 10, and if there are 60 records inside my database, then the total pages will be 6. Even if I have 55, still the number of pages will be 6. It's just that the last page is going to have only five records. Using the page object, we can also invoke has next and hasPrevious() methods to understand if there are any next pages and previous pages. All these methods are going to be super helpful for building UI pagination controls. Now let's see an example where we can apply both pagination and sorting. When we are creating an object of pageRequest by using these of() method, it is also capable of accepting the sort object as the third input parameter. This time, along with the 0 and 10, I'm passing a sort object, So the sort objects can be created by using this by() method to this by() method I'm passing the field name followed by I'm invoking these descending order. So whatever sort object that is going to be created by these code, the same will be provided as an input to these of() method. So this time, this page request is capable of handling both the pagination and sorting. So it is going to fetch 10 records sorted by latest to first, as I said before, pagination and sorting is a best practice to follow. So this is an example on how to use pagination when we are defining our own custom finder methods. So here, this is my derived query method to this I'm passing the where condition followed by pageable object. Since pageRequest is an implementation of pageable, we can pass directly this pageRequest as an input. And whenever we are passing pageable requirements or pagination requirements to the method, the return type of the method has to be page of your entity name. So this is how we can invoke these findByStatus() method. First I'm creating a pageRequest object, the same I'm trying to pass as a second input parameter when I'm invoking these findByStatus() method, let's see that demo of pagination and sorting in the next lecture.