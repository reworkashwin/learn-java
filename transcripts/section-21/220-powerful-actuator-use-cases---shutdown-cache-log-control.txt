So far, by using the Spring Boot Actuator REST API paths, we're only reading the data, trying to understand the health of our application. We're trying to read the metrics to monitor our application, but apart from read operation, we can also give some instructions to our Spring Boot backend application by using the Spring Boot Actuator paths. Inside this lecture, I'm going to show you a few most common use cases that developers they use Actuator on their day-to-day basis to perform some actions inside their backend application. The first action is, around the shutdown. By using the Actuator REST API path, the developers, they can shut down their application remotely. Let's imagine there is a scenario where a developer wants to shut down his application. Instead of manually logging to the production server or into the cloud server, they can invoke a REST API path and give instructions to the Spring Boot to shut down the application. By default, the shutdown path is not going to be enabled. To enable the same, we need to mention a property with the name management.endpoints.shutdown.access. So to this property, we can provide any of these valid values. None is the default value, let me mention unrestricted, followed by I'm going to do the build once the build is completed, we can invoke the shutdown API path. But we can't invoke it using browser because this shutdown REST API path is exposed by using the HTTP method POST. That's why this time I'm going to use my Postman. Inside the Postman collection that I have shared, there will be a folder with the name Actuator. When you select this Actuator folder under the scripts, please make sure to mention pm.request.url.port=9090 here, because we know for the Actuator, we are using a different port number. Under this pre-request, we need to make sure we are overriding the default port that we have set at the parent level. Spring is a parent collection. Here under the scripts, we have mentioned the port as 8080. We should be fine using the same protocol and host, but the port number has to be updated to 9090. That's why once you select this Actuator folder under the scripts, please make sure this value is present. Let me go to this Actuator_All. Here if I invoke this API, I'm going to get all the REST API paths that are supported by the Spring Boot Actuator. This time, you can see there is a shutdown related path also is enabled. To invoke this shutdown path, we need to use the HTTP POST method. Let me go to this shutdown request. So here I have selected the POST and this is the path that I want to invoke. If I click on this send, inside response, I'm getting 403 forbidden error. The reason why I'm getting this error is, since we are invoking a POST to REST API, we should take care of sending the CSRF token. Since I'm not sending that token, I'm getting this error. So here we have two options. Either we can send the CSRF token or we can request the Spring Security Framework to not enforce any CSRF security for Actuator related REST APIs. I'll go with the second option since you are already familiar with the very first option. I want to explain to you about this new second option. We need to go to the class with the name JobPortalSecurityConfig here. We have made some CSRF related configurations. So just after the csrfConfig, I need to invoke a method with the name ignoringRequestMatchers(). To this method, I need to pass the Actuator related API path. So this is the rejects pattern. Let me mention the same here. Followed by, I'm going to do the build, once a build is completed, I will try to invoke this REST API one more time. This time you can see I got a successful response saying that shutting down bye. Now if we go and check our IntelliJ, our application might have shut down. Yes, it is shut down. Let me go to the console. Inside the console, I'm able to see some statements around initiated and shutdown completed. Usually this feature can be leveraged in the scenarios where when a developer want to shutdown their server during night time when there is no traffic is coming so that they can save some money around cloud build. And in the morning they can start the application manually by logging into the cloud server. There is no actuator path that supports starting up the application, only shutdown is supported. Apart from shutdown, we can also perform some caching related actions as well. We know when we invoke these caches related REST API path, by using get method, the actuator is going to give me all the caches that are being used inside my application. If I click on the send button, I'm getting this error. I need to start my application. My application started successfully. Let me click on the send button. Inside the response, you can see we received three different type of cache details. By using actuator, we can also invalidate caches or delete caches whenever we want. Let's imagine there is a scenario where you are facing some problem around the cache and you want to start your cache from scratch. Instead of restarting your application, you can tell to the Spring Boot framework to evict all the data from the cache and start from scratch. And this operation you can perform whenever you want. You don't have to wait for the TTL expiration time, or you don't have to manually restart your application. . What I'll do is, first I'll try to invoke this GetAllCompanies Public request. If I click on the send button, I'm going to get all the companies along with their job details. Inside the console, you will see there is an SQL statement fired because I have invoked the REST API path very first time. During the first invocation, we know the company and job details will be cached. So let me delete these and invoke this REST API one more time. So this time I got the response without any SQL statement. That means the data is being pulled from the cache. Now let's imagine there is a problem inside my cache and I want to delete the data inside cache. You can easily achieve that by using actuator. You just have to open this DELETE_Cache_All requests. So here this time, when you are invoking the same caches API, you have to use the HTTP DELETE method. If I click on the send button, I'm going to get a response saying that 204 no content, which gives confirmation to me that the caches are invalidated or deleted behind the scenes. This time, if I try to invoke the same GetAllCompanies request, I got a response, but behind the scenes, the data might have pulled from the database. Inside the console, I'm able to see some SQL statement, which gives a confirmation to me that this data is pulled again from the database, but not from the cache because the cache is invalidated or deleted. When you are invoking these caches base path with the delete method, all the caches will be deleted. Not only companies, all the remaining caches like jobs and roles also will be deleted. In case if you are looking to delete only a given cache, then you need to invoke the same path, but this time after the caches, you need to mention the cache name. In case if I'm looking to delete the jobs related cache, I need to mention the same name here. So these names you can get when you invoke these caches API with the get method. So these are the cache names that we have configured inside the Spring Boot backend application. The same you need to mention after the caches to delete a respective cache. If I click on this send button, the jobs cache is deleted. Let me clean the console and invoke the companies REST API one more time. This time also, you can see an SQL statement is present inside my console logs. Just like how we are playing with the cache, we can also play around the logging as well. At runtime, we can change a log level based upon our requirements. Let's imagine you want to debug an issue inside the production environment. And we know inside production, we're not going to do a lot of logging for a given package, you can set a given logging level. Let me show you how to do that. Let's open this Set_LogLevel request. Whenever we are looking to update the log level for a given package, we need to use a POST method followed by this API path. First you have to mention the loggers REST API path. After the path, you need to mention your package name. We have a class with the name LoggingAndPerformanceAspect. Inside this method, we are logging all these statements like Entering method, Execution time by using info level. . Inside production environment, we are going to disable the info log level because we don't want too much of logging inside production. Inside production, we usually set error log level. First let's check what is the log level that we have, I will look for loggers and here under the com.eazybytes.jobportal.aspects. So for this package, the effective level right now is info and this package, we have all these classes. Right now we are only interested in updating the log level for this class logging and performance aspect. Since info is set as of now, there is too much of logging is happening inside my application. Let's try to update the log level to errors so that we can avoid all these info statements. What I have to do is, after the loggers, I have to mention the package name and the class name, in case if I am looking to control the log level for a given package, then I can skip the class name. Next under the body, you need to make sure to provide these configured level with the value error. If I click on the send button, the log level is going to be updated. If I refresh this page and look for the same class, this time the configured level is error and the effective level is error. Let me clean this console and invoke any of the REST API like GetAllCompanies. I got the response inside the console, I don't see any log statements because I have disabled the info log level. To bring back to the previous logging level, we have to invoke these Clear_LogLevel request. Here also I'm using the same path with the same post, but under the body I'm sending empty request data. That means I'm telling to the actuator to bring back to the original default value. If I click on the send button, I got a response inside this loggers REST API, if I refresh for the class logging and the performance aspect, the default log level info came back. Let me invoke the companies REST API, this time we will see a lot of log statements. This way you can always control the log level for any package or for any class based upon your business requirements. If you are debugging a specific class inside production environment, for that class alone, you can change the log level for the required amount of time. Once you are done with your analysis, you can roll back to the default log level. These are the most common use cases that developers they use on day to day basis. I hope you're seeing the power of actuator. I tried to explain most of the details around the Spring Boot actuator within the limited time that I have, . in case if you want to explore more about actuator, you can come to the Spring Boot official documentation. . Here there is a section actuator under the REST APIs. Under this actuator, you're going to have all the REST API paths that are going to be supported by the Spring Boot actuator. I request you to explore all these official documentation whenever you have time. The details that we have discussed around the Spring Boot actuator, I documented all of them in these slides for your reference. Please please refer to these slides whenever you're going for the interviews, so that these information will quickly help you to brush up your knowledge around the Actuator.