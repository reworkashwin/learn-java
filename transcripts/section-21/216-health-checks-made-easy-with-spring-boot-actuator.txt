After adding the actuator-related dependency inside pom.xml, we should be able to access all the actuator exposed REST APIs by navigating to the path localhost 8080 forward slash actuator. Since right now we deployed our application inside our local system, we need to use the host as localhost followed by the port number where our application started. Under the port number, we need to mention the actuator path which is forward slash actuator. If I press enter, I'm getting a 403 error. This is due to the Spring security. By default, we know Spring security, it is going to secure all the REST APIs that are being exposed, regardless of who developed those REST APIs, the Spring security will try to secure them. To access the actuator-related REST APIs, what I have to do is, I have to come to these PathsConfig and here under these public paths method, I need to add a new entry related to the actuator. The path is forward slash actuator followed by, I'm also going to mention two asterisk symbols. Because under the actuator, we are going to have many other REST APIs, all of them, they are going to have a common prefix path as actuator. Since I am looking to access all these REST APIs without any security, I'm trying to mention this path under the public paths. Please make sure you also configure the same path inside your code as well. Let me save these changes,do the build, once the build is completed, this time, if I try to refresh this page, I got a response. So by default, the actuator, it is going to expose only the health-related REST API. Let me open this second link. Under this health REST API, we're going to have a status as Up, which confirms that our application is up and running without any issues. We also have information related to liveness and readiness. So these liveness and readiness, they are going to be useful when you're deploying your Spring Boot application as a microservices inside a Kubernetes environment. I discuss about these liveness and readiness in detail inside my microservice course. For now, let's ignore these liveness and probeness. By invoking these health REST API, the operations team members, they can get to know what is the health status of my application by looking at this response. Using DevOps tools, the operations team members, they're going to build some configurations in such a way that every 10 minutes or every 5 minutes, they're going to invoke these REST API path through the tools, and they're going to look for what is the value under this status. If the value is equal to Up, then it is an assumption that the health of the application is fine. Otherwise, they're going to assume that the health is not good and accordingly alerts and notifications will be triggered to the support team or developers team. By default, under this health REST API, we're only going to have what is the overall health. But sometimes we may also want to know what are the underlying dependent components health as well. For example, our Spring Boot backend application, it is dependent on my SQL database. In case if we are looking to understand that dependent components health as well, then we just have to add a property under the application.properties here, we just have to type management.endpoints.health.showdetails. By default this property it is going to have never as a default value. Apart from never, we also have always and when authorized. By configuring always, we're telling to the actuator to always display the health details regardless of the user is authenticated or not. Whereas with the when authorized, we're telling to the actuator to display these healthy details only if the user is authenticated. Let's go with the always for our testing. I'm saving these changes, doing the build. This time, if I refresh the same REST API path, I have many other details as well. Under the components, I have all the dependent component details like database. In the similar lines, my backend application, it is also going to depend on the disk space available inside the server. So as of now, the server is my local system itself. So inside my local system, this is the total disk size out of which this is the space which is free and this is the threshold value. And this is the path where my code is present inside the disk. The overall status of disk space as well is Up. Similarly, for the liveness the status is Up, we also have ping component. The ping component status as well Up. For readiness as well, the status is Up. There is also an ssl dependent component. As of now, inside our local system, we're not using any ssl certificates. But when you're deploying your application into a production environment, the operations team members, they're going to configure ssl certificates for the domain. So those certificate details along with their expiration details will be displayed here. In local environment, all these details will be empty with the status as up. This way, all the dependent components, health details will also be exposed by the actuator. In case if you are using other components like RabbitMQ, Kafka, all those dependent component health details will be displayed under this REST API path. You can simply tell to your operations team member to invoke this health REST API, which is going to have all the details. It is also possible to configure a different port number for the actuator. As of now, we are using the same port number where our backend application started. To use a different port number, we just have to type management.server.port. Under this, I can mention the port number like 9090. The port number can be anything. After adding this property, please make sure you are restarting your application. This time if I access the same path, I'm going to get an error because right now the actuator related REST APIs are going to be exposed with the port  9090. When I'm accessing the actuator URL with 9090, I'm getting this response. We can try to access the health related REST API as well here. I have to mention 9090 and press enter. This time we receive the same response. In case if we are looking to know the health details of a specific dependent component, we should be able to do that as well. After this health, we just have to mention dependent component name. For example, my database component has a path as DB. If I press enter, I'm going to get only database health details inside the response. Let me go to the other window. So this is the URL that we try to access right now. So after the health, you can mention that dependent component path and that is going to give you only the specific component health details. By default, the base path or the prefix path for all your actuator REST APIs will be actuator itself. We can update the prefix base path as well. Inside the application.properties, we just have to configure a property with the name management.endpoints.web.basepath. Please make sure to use endpoints in a plural form. To this property by default, we're going to have a path as actuator. So let me update this path to forward slash job portal slash actuator. So from now onwards, this will be the prefix path for all my actuator REST APIs. The same I need to configure under the Paths Config here, as of now, we have mentioned actuator. Let me update this to job portal forward slash actuator. I'm doing the build, once the build is completed, I will restart my application. This time I have to mention the base path as job portal forward slash actuator. The same is going to be applicable for all the REST API paths that are going to be exposed by the Spring Boot Actuator.