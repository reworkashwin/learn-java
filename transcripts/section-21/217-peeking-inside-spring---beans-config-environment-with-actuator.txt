Spring Boot Actuator by default, it is only going to expose the health-related REST APIs. For security reasons, it is not going to expose other important REST APIs. To make Actuator to expose other REST APIs, we need to use one of the property, the property is management.endpoints.web.exposure.include So by default, under this property, health is the only REST API path that is included. Here, if I mention asterisk this time, all the REST APIs will be exposed by the Spring Boot Actuator. Let me do the build, if I try to refresh this path, this time we are seeing many many REST APIs. So there is a Beans REST API, Conditions REST API, ConfigProps, env. We are going to explore all these REST APIs one by one in few minutes. But before that, let's try to understand some few of the important details. Using this asterisk is not recommended inside a production environment, because it is going to unnecessarily expose a lot of details of your application. So that's why we can expose whatever REST APIs that are required for our application on a day-to-day basis. So here I can mention beans, health, metrics So please mention all these REST API names by using comma as a separator. So here beans is one REST API path, similarly, we have env, info, health. Whatever value that you are defining here, only those REST APIs will be exposed by the Spring Boot Actuator. This time, if I refresh this page, you will be able to see only beans, health, and metrics APIs are exposed. . Sometimes developers, what they are going to do is, they will mention asterisk here, followed by they are going to use another property with the value exclude. To this exclude property, they can provide all the REST API paths that they don't want to expose. So here, if I mention beans, then beans related REST API path is not going to be exposed. As of now, here if you see beans is present. If I refresh this page, I have all the REST API paths, but beans related path is missing. This is how you can always control what you want to expose and what you don't want to expose by using the Spring Boot Actuator. For our local testing, I'm going to have asterisk under the includes, and I will comment out these exclude property. So whatever properties that I'm defining under this application.properties are going to be leveraged by all the profiles. In case, if you are looking to set up different set of behavior inside prod profile or QA profile, then define the same properties with their respective values in the corresponding profile property file. So let me save these changes do the build. If I refresh this page, I'm going to see all the REST API paths that are going to be exposed by my actuator. Let's try to understand the purpose of each of these paths. So by using these beans REST API path, the Spring Boot Actuator, it is going to provide all the beans that are created by the Spring Framework and the Spring Boot Framework. For example here, if you look for Controller, there is a bean created based upon the configurations that we have defined under the class LoggingController. Similarly, if you keep searching, there are many other controller classes. For example here, there is a ContactController. This bean is created based upon the configurations that we have done inside this class. So this way, you should be able to understand all the beans that are created by the framework. In case if you are facing a problem in a specific environment like production, you just have to understand whether the bean is created properly based upon your configurations by coming to this path. Let's go to the other REST API path with the name Conditions. We know Spring Boot, it is going to follow the auto-configurations. Based upon these auto-configurations, it is going to create a lot of beans. The auto-configuration report we can see under this Condition REST API path. Under these negative matches, we are going to see all the bean details which are not created due to the negative match. If you are not using a library that requires these beans, then these beans are not going to be created by the Spring Boot auto-configuration. After the negative matches, we have positive matches. Under these positive matches, you are going to see all the beans that are created by the Spring Boot auto-configurations based upon the libraries that you are using inside your backend application. After the positive matches, we have unconditional classes. So these beans will be created always regardless of which libraries you are using. We saw a similar kind of report when we were discussing about the Spring Boot auto-configurations at the starting of the course. The same report you can see by accessing these Conditions REST API. After the conditions, we have Config Props REST API. Using this REST API, we can see all the properties that are being used by your Spring Boot application. Along with the properties, we can also see what are the values that are being used under the properties. For example here, we are able to see all the properties that we are using with the prefix management.endpoints.web. By using this prefix only, we have configured base path. Apart from base path, we also configured exclude list and include list as well. The default behavior of this REST API is, it is going to sanitize or mask all the property values for the security reasons. In case if you are looking for the property values as well, then you need to add one more property. The property is management.endpoint.configprops.show-values. To this property, by default, never is going to be used. Let me mention always. With this, now the property values will also be displayed. Since config props is a single REST API path, that's why here we need to use endpoint in a singular form. . Whereas all the properties which are having plural form of endpoints, they are going to have an effect in all the REST API paths. For example, this base path, it is going to be common for all the REST API paths. That's why here we are using endpoints. This time, if I refresh this page, I should be able to see the values as well. Under the base path, I'm able to see the base path that I have configured. Under the include list, I have configured asterisk. The same I'm able to see. If you keep scrolling, you can see many other properties as well. These are the properties related to the Spring Data JPA that my application is using. There is a property with the name show SQL that we are using. So to this, we have assigned this value. The same is being displayed here. This way, the developer or QA team members or operations team members, they can always come to these REST API path to understand what are the properties that are being used by the application along with their values. Developers, they don't have to always open the application.properties. They can simply come to these REST API to understand the values that are being considered by the Spring Boot application. After the config props, we have env path as well. Let me open the same. Under this env path, we should be able to see all the environment properties that are being used by the Spring Boot application. So these are server.port, management.port. By default, this REST API path also it is going to sanitize all the property values. To see all the property values, you need to define a property very similar to config props, here instead of config props, you have to mention env. Let me do the build and refresh this page. This time we are able to see all the property values as well. So under the server.port, we have configured the port 8080. Whereas for the management.port, we have configured 9090. Apart from these properties, inside my local system, I have all these environment variables like Java, class path, user time zone, all these environment properties, they are being fetched from my local system by the Spring Boot application. The same we can see by accessing these env REST API path. We have many other REST API paths as well. Let's continue this discussion in the next lecture as well.