To get started with the observability in a spring boot backend application, first we need to add the starter project related to the OpenTelemetry. Here I search for Open Telemetry, as you can see here, it is also highlighted that the starter project is used for observability. By using these, we can publish metrics and traces in open telemetry's OTLP format. So this starter project, it is only going to take care of metrics and traces. Coming to the logs, I'm going to talk about that in a few seconds. Let me add this dependency and click on explore. Inside the pom.xml, this is the dependency that is added. I need to copy the same, and paste it inside my pom.xml. Just after the actuator, I'm going to add this new starter project. Now coming to the logs, we need to add another library dependency. This dependency is from the Open telemetry team itself. The group ID is io.opentelemetry.instrumentation. And this is the artifact ID. Since we are using logback to perform the logging inside our backend application, we need to use the corresponding appender artifact ID, inside the maven central repository. If you look for this group ID, you're going to get all the dependency details, the same you need to copy into the pom.xml. As of today, when I'm recording, this is the latest version. Let me click on these sync maven changes. Here you may have a question, which is, why can't this open telemetry starter project itself can take care of this logging as well? The reason is very simple. When we are trying to perform logging, the underlying framework may vary from project to project. Inside our project, we are using logbacks. Similarly, some other project, they may use logback4j2 and someone can use java.util.logging. Based upon the underlying logging library, we need to use the corresponding appender to send the logs in an Open Telemetry OTLP format. As of today, it is the responsibility of the developer to add the corresponding dependency inside the pom.xml. Maybe in the future, the springboot team, they may update the starter project dependency itself to construct the corresponding log appender based upon the logging mechanism that a springboot backend application is using. For now, we have to add these two dependencies. If you open the starter project pom.xml here, you can clearly see that it is using some other starter projects with the name springboot starter micrometer metrics, which is going to take care of publishing the metrics. Similarly, we have another dependency, which is related to the micrometer tracing. And if you keep scrolling, there are some dependencies related to OTLP. There are some dependencies related to tracing bridge otel before springboot 4, it is the responsibility of the developer to add all these dependencies manually. But right now we just have to add this single starter project, which is going to take care of metrics and tracing. But coming to the logging, we have to add these extra dependency based upon the logging library that we are using. Next, I need to go to the logback xml that we have. So we have an xml with the name logback-spring.xml inside this xml, as of now, we have configured only the console appender and rolling file appender. By using this console appender, the logback framework, it is going to log the statements into my console. Similarly, by using these rolling file appender, the logback is going to take care of writing the logs into the log files based upon the configurations that I have done here. In the very similar lines, we need to define one more appender, which is going to take care of publishing the logs in an OTLP format. Just after this console appender, let me define a new appender with the name otel. This name can be anything, but against to this appender, we need to use a class from the library that we have added. The class name is OpenTelemetryAppender. This class is present inside this library that we have added. Please get these complete class name details from the GitHub repo. Once this appender is created, we need to use that inside our application as well. What I'll do is, I'll scroll down and just like how we are referring to other appenders in the very similar lines, I will refer to the otel appender as well. Under the prod profile, as of now, we are using rolling file appender. Let me also use otel as well. So otel is a new appender that we have created the same appender I will try to use here as well. Let me do the same for QA profile under the com.eazybytes as well. I'm going to mention otel, and at last under that default profile as well. I will add the new otel appender that we have created. This is going to take care of publishing my application logs into the observability backend by leveraging OTLP standards. As a next step, we need to set up the LGTM stack, which is going to act as observability backend and frontend. We should be able to easily set up that by using the docker. Long back when we were discussing about docker, I highlighted about one of the advantages of docker, which is we can easily set up any of the dependent components like databases or any other products by using that docker images. As of now, if you open the compose.yml file inside our application, we have set up a service with the name dbservice. Under this, we have set up all the configurations to use MySQL database. Whenever our application is starting, the MySQL database is also going to start. In the very similar lines, I'm also going to create one more service, which is going to take care of setting up the LGTM stack during the startup of my application. In real enterprise applications, it is the responsibility of the DevOps team members to set up all these databases and the LGTM stack. For our local testing and development, we can use this compose.yml approach. Just after the dbservice configuration, at the same line where my dbservice is present, I am going to create a new service with the name grafana-lgtm. The service name can be any name. Under this service name, I have to provide what is the docker image that needs to be used to set up the docker container. The image name is otel-lgtm-latest. Since this image is being maintained by the grafana team itself, we need to mention this repository name as grafana, followed by what is the docker image name. Next, under the port mapping, we need to provide these three different ports. By using this 3000 port mapping, I am trying to expose grafana-ui at the port 3000. Internally, this docker image, it is going to set up the grafana-ui application at the port 3000. Since I am looking to access the same from my local system, I need to expose that port to my local host system as well by mentioning 3000 here as well. So once this container is set up, we can access the port 3000, which is going to show us grafana-ui. Next, we have port mappings for 4317 and 4318. When we were discussing about OTLP, I said OTLP protocol supports the communication by using either HTTP or GRPC. By using these 4317 port number, the OTLP it is going to accept the data using GRPC protocol. Whereas with the 4318 port number, the OTLP, it is going to accept the data by using HTTP. So based upon how you want to communicate the corresponding port numbers, you have to use inside application.properties. So this configuration is going to take care of setting up the LGTM stack or observability backend and frontend products inside our local system. So from now onwards, whenever we are starting our application, a docker container is going to start and it is going to expose grafana-ui and OpenTelemetry related APIs at the port number 4317 and 4318. Let me stop my application so that next time when I try to start my application, both these containers will be created by the docker. Once the observability backend is ready, as a next step, we need to tell to the springboot framework, the location details of the observability backend so that it will start exporting the metric information, traces information and logging information by using OpenTelemetry and micrometer. To establish the communication between the observability backend and our spring boot backend application, we need to go to the application.properties and here we need to type for a property with the name management.otlp.metrics.export.url To this property, we need to mention this value localhost 4318 forward slash v1 forward slash metrics. So this is the OpenTelemetry framework standard. By default at this location, the OpenTelemetry is going to accept the metrics information. I'm using 4318 since I'm looking to send the data by using HTTP protocol. In case if someone wants to follow GRPC, they need to mention the 4317 port number. When these metrics are being exported from my spring boot application to OpenTelemetry and from there to the observability backend, by default, these metrics will be exported every one minute. We can increase that frequency or reduce that frequency. To control that here, we need to use a property with the name management.otlp.metrics.export.step. This property by default, it is going to have one minute, which is the default reporting frequency, but we can reduce these or increase these based upon our requirements. For our local testing, I'm going to mention it as 10 seconds. These two properties will take care of metrics. Now coming to the tracing, we need to look for the property with the name management.opentelemetry.tracing.export.otlp.endpoint. To this property, we need to mention the value as localhost 4318 slash v1 slash traces. Here we have mentioned metrics, but here we need to mention traces because we are trying to export tracing information. By default, the underlying micrometer library, it is only going to send the tracing information for the 10% of the traffic that is being handled by your application. This is done intentionally to reduce the burden on the observability backend and open telemetry. If we keep sending all the request to tracing information to the observability backend, it will end up storing a lot of data. Basically 10% of traffic data is more than enough to debug any production issues. But for our local testing, we can change that number. We have to look for a property with the name management.tracing.sampling.probability. So by default, 10% will be sent. The range is 0.0 to 1.0. So here I will replace that 0.1 with 1.0, which means I want to send 100% of the requested tracing information to the observability backend. After the tracing, we need to configure logging related OTLP endpoint as well. I can copy this property and paste it here. Instead of tracing, this time we just have to look for logging coming to the value here instead of traces, we need to mention logs. By using these properties, now my spring boot application and micrometer, they both know to which open telemetry endpoints that they have to send the metrics, traces, and logs information. Once the open telemetry receives all this telemetry data, it is going to forward the information to the observability backend. There is a last change that we need to do. As of now, we have defined an appender with the name otel-inside-logback-spring.xml against to this otel appender, we have configured this class, but this open telemetry appender, it does not have any clue about the logging OTLP endpoint configuration that we have done here. So to establish the link between this appender class and with this OTLP endpoint, we need to make a small change. Let me create a new package with the name otel. Under this otel package, I will create a new class with the name OpenTelemetryAppenderInitializer. Anything on top of this class, we need to mention @Component annotation so that a bean of this class will be created. This class, it is going to implement initialization bean interface so that we can execute some logic when this bean is being initialized by the spring framework. Into this class, I'm injecting OpenTelemetry bean as a dependency. Inside this OpenTelemetry, all the details around the logging endpoint will be maintained. In order to inject this bean as a dependency, I need to use @RequiredArgsConstructor. Now I need to implement a method from the initialization bean interface. The method is afterPropertiesSet(). Once this bean is initialized, whatever logic that we write inside this method will be executed. Here by using OpenTelemetryAppender, I'm going to invoke the install() method to this method, I need to pass the OpenTelemetry bean that we have injected. This way, we establish the link between the OpenTelemetryAppender that we are using inside the logback with the OpenTelemetry configurations that we have defined here. I hope you are clear with the purpose of this bean. We can get rid of this throws Exception since it is redundant. Let me do the changes. The build is successful. Inside the next lecture, let's see the demo of all our changes.