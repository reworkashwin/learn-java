Hi everyone. Welcome back. In this lecture let us understand what is functional programming with example. Well if you look at the code snippet over here, here we have a calculate some method. It takes a list as a parameter. Within this method we have some variable with initial value zero. And here we have. For each loop it iterates over a list within a. For each loop, we have a if statement that checks if a number is even within a if statement. If the number is even, then we add that number to the sum and finally return the sum. So if you can closely observe this code snippet over here, here we have explicitly written a step by step instructions to calculate the sum. And this kind of a programming style is called imperative programming okay. And here you can see in imperative programming basically we use for loops and if statements to control the flow of the execution. And we use the variables to hold the result. So here we are telling Java how to calculate the sum by providing step by step instructions. So this kind of programming style is called imperative programming. Now we pretty much understood the imperative programming style like in imperative programming, we use the step by step approach like we write. Each instruction manually, and in imperative programming we modify the state variable. So here sum is a state variable and we modify it within a for loop or a if statement. And in imperative programming we use the loops like for loop while loop do while loop. And we use the control flow statements like if statement, if else statements, etc. and in imperative programming, the developer or a programmer defines every operation. All right. So basically before Java eight we were writing the code using imperative programming style like this. But after Java eight, Java supports a functional programming using Lambda expressions, stream API's method, references, functional interfaces, etc.. Okay, so once we understood the imperative programming style, let us take a look into the functional programming style. If you look at the code snippet over here, here we have a calculate some method. It takes a list. And here we have a stream method that converts a list into stream. Stream has a filter method to filter the numbers. So within a filter method we have written a lambda expression. So this lambda expression basically, you know, keep the even numbers by using this logic. And here map to int method. Basically convert each wrapper class integer wrapper class into corresponding primitive integer type. And here some method basically calculates the sum of the even numbers. And if you can see this code snippet over here. So here we are not using any for loop or while loop or if else statements etc. and we are also not using any variables over here. So this kind of programming style is called functional programming. So in a functional programming we write the code that focuses on what to do rather than how to do it. So here we are just calling the methods. We are not implementing the logic step by step over here okay. So we are just calling the methods and we are telling like what to do. And in case of imperative programming here you can see we have written the step by step, you know instructions to calculate the sum. So here basically we have implemented like how to calculate the sum. But in case of functional type programming we are just calling the methods. So these methods internally perform the operations. Okay. So we are just telling what to do. We are not telling like how to do it over here. So in a functional programming we don't use the loops or if statements, we simply call the functions or methods over here and we tell like what to do. We are not telling how to do it over here okay. And again in functional programming we don't change the variables. So if you can see the imperative programming style. So here we are changing the some variable inside the for loop or if statement. But in case of functional style programming you can see here we are not changing any variable within this method. Now you may have a question like what are the advantages of functional programming over imperative programming. Well, if you can see the number of lines of code over here, in case of functional programming, we can reduce the number of lines making the code concise. So in a single line of, you know, code, you can see we have calculated the sum of even numbers. But here in imperative programming style we have written a lot of code. Okay. It means using functional programming. We can write the less code. So one more advantage is easier debugging. So in a functional style programming there is no hidden state changes. This makes the debugging simpler. But in case of imperative programming you can see there are state variables. And these state variables can be changed within a for loop or if statements. But in a functional style programming there is no if or you know, loops or variables. That is why this functional programming makes a debugging simpler. And one more important benefit is functional programming supports a parallel processing like here. In functional programming there is no state shared, so multiple threads can run safely. That is why this functional style programming supports a parallel processing. So we will learn all these things in upcoming lectures. So just remember in a functional style programming we write the code that focuses on what to do rather than how to do it. So there are some important concepts of functional programming. So we will see one by one in upcoming lectures. All right. Great I will see you in the next lecture.