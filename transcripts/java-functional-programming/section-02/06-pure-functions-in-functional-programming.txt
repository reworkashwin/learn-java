Hi everyone. Welcome back. In this lecture we are going to talk about pure functions in functional programming. Well pure functions are one of the fundamental principles of functional programming. By the end of this lecture you will understand what is pure function is the differences between pure and impure functions. Multiple real world examples of pure functions. Why pure functions are important in functional programming. Okay, so let us get started. What is a pure function? A pure function in a functional programming follows two main rules. The first rule is it always produces the same output for the same input. Second rule is node side effects. It does not modify any external variables, states, or data. Think of it like a vending machine. If you insert ₹10, you will. You will always get the same snack. It does not change the machine's internal working. So this is exactly how a pure function works. Well look at here the code snippet. Pure function example is a class within that apply. Discount is a method. So this apply discount is a pure function. So this apply discount method only depends on the input parameters. Price and discount. This apply discount method does not modify any external variables. And for the same input like 1010, it always produces the same output. For example, here you can see we have called this apply discount method two times with the same input and this apply discount method produces the same output for the same input. So it doesn't matter how many times you call this apply discount method with the same input, it always produces the same output. Okay, so here we are calling apply discount method two times with the same input. So it will produce the same output. For example, if I run the program, you can see here the output 900 900 okay. So pure function has a two main rules. The first rule is it. It will always produces the same output for the same input. And you can see here we are calling apply discount method two times with the same input. And it is producing the same output. Over here you can see 900 900. So this means apply discount is a pure function. And one more rule is like this. Pure function does not modify any state variable over here. So here you can see we are not defined any global variable within this class. So this apply discount method is only depends on the input parameters like price and discount okay. It means this apply discount method meets both the rules. That's why this apply discount method is a pure function. Now let us take a look into the impure function so that you will understand more about pure function. So here we have impure function example class. Within that we have global discount variable with a value ten. And here we have applied discount method. Within this apply discount method we have written a logic to calculate the discount. That is 10% discount. And if you can see again the logic of this apply discount method over here, it is using the global discount variable that is external variable. To calculate the discount, it means this apply discount method modifies this global discount external variable. And again you can see here main method. Within that we have called this apply discount method two times. And whenever we change the global discount variable value then this method basically produces the different results. For example, if I run the program over here, you can see the output 908 hundred okay. So this is a impure function because this function modifies the external variable that is global discount variable, and this function or method basically returns a different results for the same input when a global discount variable changes over here. So here you can see within a main method we have called this apply discount method two times with the same input. And if we change the discount global discount variable it produces the different results okay. So these are the reasons why this method or a function is impure. So pure function basically has a two main rules. The pure function basically produces the same output for the same input. So it doesn't matter how many times you call the pure function with the same input, it always produces the same output. And one more rule is like no side effects. The pure function does not modify the external state variable. Okay, I hope you understood the difference between pure function and impure function. So let us take a look into one more example. Like let us say we need to. Count the number of characters in a string. And if you look at the code snippet over here. So this is the count characters is a pure function because this count characters method is depends on. The input parameter that is input and it does not change any, you know, state variable. That is why. It has no side effects and this count character methods return the same output for the same input. For example, if I run the program, it gives the same output five five. You can see we have called this count. Characters method two times with the same input and it produces the same output that is five and five. Okay, so this count characters method meets both the rules like count character methods return. The same output for the same input and it does not change any state variables. That is why there is. No side effects. That That's why this is the pure function. Next here you can see impure function. Example class. Within that total characters is a state variable. It has a value zero. This is the count characters you know method. It takes input as a parameter. Within that it counts the number of characters by using length method and returns the number of characters. While this count characters function is not a pure function, it is a impure function because it modifies the external state variable that is total characters over here. Next, this count characters method produces the different outputs for the same inputs. For example, if I run this code snippet, you can see the different output over here. Even though we are calling this function two times with the same input, it is producing the different output. That is why this count characters. You know function is not a pure function, it is a impure function. Now we pretty much understood the fever function. Next, let us take a look into the advantages of pure functions. First is predictable behavior. Pure functions always returns the same output for the same input. There is no hidden dependencies, making debugging easier. Next is no side effects. So it does not modify external variables or data structures. It avoids unexpected behavior caused by shared state. That is why there is no side effects in a pure functions. Next is easy to test since the output only depends on the input. Testing is a straight forward. You do not need to set up an external environment. Fourth is it makes a parallel processing easier since pure functions does not modify the shared data. They can be executed in parallel without any conflicts. So these are the four main advantages of using pure functions in a functional programming. Next. When to use pure functions, you can use pure functions. Next. When to use pure functions. You can use pure function. When you write a clean, reusable and testable code, you can use pure function when avoiding state changes that causes unpredictable behavior. You can use pure function when developing multi-threaded applications. You can use pure function when working with data transformations, example streams, lambda expressions, method references, etc.. All right, just remember pure functions are predictable, testable and reliable. Impure functions modify the external state, lead to unpredictable behavior. Using pure functions improves the code quality, maintainability, and performance. All right so this is all about pure functions. All right great I will see you in the next lecture.