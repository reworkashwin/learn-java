Hi everyone. Welcome back. In this lecture, let us take a look into one more important concept in functional programming that is higher order functions. Well what is a higher order function. Well, a higher order function is a function that takes another function as an argument or returns a function as its result. For example, look at the code snippet over here. So here we have a higher order example class within that apply function. This apply function that takes another function as an argument. And here we have a higher order return example class within that create multiplier function. This function returns another function over here. So apply function is a function that takes another function as an argument. That is why this is a higher function. And here create multiplier is a function that returns another function. That's why this function is called higher order function. So this is how we define the higher order function. All right, just remember these two important points. A higher order function is a function that takes another function as an argument or returns another function. So this is how we can define the higher order function. So let's take a look into one more example. So here we have higher order stream example class. Within that apply function to list is a function that takes a list as a first argument function as a second argument. And here we have a min method within that. This is the list of numbers from 1 to 5, and here is a lambda expression that multiplies a number by two, and we have assigned this lambda expression to a variable of type function. And here we are passing this variable as an argument to this method. And this method basically converts this, you know, list into stream, and then call the map method to map each number into multiply by two, and then it finally collects the result and returns the list. Okay. so don't worry about the streamlined lambda expression over here. You will understand everything about stream APIs Lambda expressions in upcoming sections. So here just try to understand what is higher order function over here. So this apply function to list is a higher order function because it takes another function as an argument okay. So this is how we define higher order function in a functional programming. Now let us see what are the benefits of higher order functions. First benefit is code reusability. So you can pass different functions without rewriting the logic. For example here this is a higher order function because it takes another function as an argument. And here in a main method we have a list of numbers from 1 to 5. And here we have a lambda expression that multiplies the number by two. And here just we are passing this variable as an argument to this method. Next let us say we have a requirement to multiply each number by three. Then what we can do is we can simply copy this and paste it over here. And here we can change the logic from 2 to 3. And here we can call this function. And just we can pass this lambda expression that is assigned to this variable like this. We don't have to change this higher order function over here. We can simply make the changes to the lambda expression here. And we can pass that lambda expression as a function to method. That's it. So this is how we can pass different functions without rewriting the logic. If we don't use the higher order function, then we have to create one more method that will multiply the number by three. Okay. So this is how we can use a higher order functions in functional programming to dynamically pass the functions. Next benefit is cleaner and more readable code. So you can see this higher order function and the logic. There are no EPL statements. Right? So you can just call the methods. So that's why this looks cleaner and more readable. There are no for loops and if statements. That is why the complexity is reduced in higher order function. And this makes functions reusable. The third benefit of using higher order function is that it enables functional programming concepts. So it works great with stream API's, lambda expressions and callbacks. The fourth benefit of using higher order function is that it is easy to debug and test. So functions don't modify the state, making debugging easier. So you can see this. You know higher order function. It is not modifying any variable within this class. That's why it is easy to debug and test. So these are the four important benefits of higher order functions. Now the question is when to use higher order functions. So you can use higher order function when writing modular and reusable code like this, you can keep this higher order function as it is, and you can pass the functions dynamically. Next, use the higher order function when working with data transformations, for example streams lambda expressions. So whenever you use a stream APIs, lambda expressions and functional interfaces method references in your Java project. Then you can define the higher order functions like this. Next, use the higher order functions. When using callbacks and event driven programming. You can use higher order functions when improving code flexibility and readability. So this is all about higher order functions. Higher order functions take or return other functions. They make code clean, flexible, and reusable. Higher order functions can be used in streams, lambda expressions, and callbacks in Java. All right. So this is all about higher order functions. All right great I will see you in the next video.