Hey there everyone! Ramesh here back again with another video. In this video we will take a look into Java method. References. Well, Java method references are basically introduced in Java eight. Well, method reference is used to refer a method of a functional interface. It is basically a compact and easy form of lambda expression. Well, each time when you are using lambda expression to just referring a method, you can replace your lambda expression with the method reference. Well, basically we use a method reference to replace the lambda expression. Well, for example, look at here the code snippet over here. This is the lambda expression just to refer a method. For example System.out.println. So println is the method out is the object okay. So this lambda expression is just you know, to refer the method that is println. So we can easily replace this lambda expression with the method reference. And here you can see the syntax of you know converting the lambda expression into method reference. So basically we remove this lambda expression and we provide a double colon here in between object and function like this. So this is basically a method reference okay. Here we are just referring to the method using method reference. All right. So just remember each time when you are using lambda expression just to refer a method, you can, you know, replace your lambda expression with the method reference. Well let's take a look into the types of method references. Well there are basically four types of method references. First one is method reference to a static method. And here is the syntax class double colon and then static method. So whenever we use a method reference to call a static method and this is how the syntax looks like. Second is reference to a instance method of a particular object. And this is the syntax. So this is the particular object. And then we are using method reference to call its method. Third is reference to an instance method of arbitrary object of a specific type, and this is the syntax. Okay, so this is the class and this is the method reference to an instance method. Okay. And fourth is reference to a constructor. And this is a syntax class name double colon new. Okay. Don't worry I will explain these four types with an example. So you will be easily, you know, able to understand the types of method references. All right. Now let's go ahead and let's head over to the IntelliJ Idea. And let's implement these four you know method references with an example. So first we'll see method reference to a static method. We'll head over to the integer idea. And here I am going to create a new package. And let's call it as method reference. Hit enter. And then within this package. Let's create a class and let's call it as method references demo something like this. Hit enter. All right. And within this class let's create a main method. And first we're going to see method reference to a static method okay. So first we're gonna write a lambda expression. And then we'll convert that lambda expression with the method reference okay. So each time you have a lambda expression which is just referring to a method, then you can easily replace that lambda expression with the method reference. Right. So now let's go ahead and let's create a lambda expression. First we're going to use a functional interface that is function functional interface. And just pass here a integer and double as a you know type to this functional interface. Now let's go and let's assign a lambda expression to this functional interface. So here is the syntax we need to pass the input. All right. And arrow symbol. And then this example is basically you know to calculate the square root of a number in order to call the square root method we're going to use math class math class from Java.lang package. It has the square root method right. So we can use this inbuilt method to calculate the square root of a number. So here I'm going to pass input. All right so this is the lambda expression. And just say just push this this out and then function dot apply. And then pass the number. Let's say four when I calculate the square root of number four. Now let's go and let's run the program and let's see the output. And there we go. The square root of number four is two. Right now let's go. And let's replace this lambda expression lambda expression with method reference. Okay. So this is using method reference. Well in order to convert this lambda expression into method reference, what I can do is I can simply copy this and paste it here. And now let's convert this lambda expression into a method reference. So let me rename this let's say function method reference. And in order to convert a lambda expression into method reference it's pretty simple. We have to first remove this part. And in case of method reference we don't have to pass the parameter. So remove this and put the semicolon and just replace this dot with double colon. That's it. This is your method reference. All right. So this is how you can convert a lambda expression into method reference okay. So each time you have a lambda expression which is just referring to a method. Then you can replace this lambda expression with the method reference for example. So here you can see this is a lambda expression. It is just referring to a method called square root okay. And we can easily replace this lambda expression with the method reference like this. Now what we can do we can put this out over here and then call function method reference. And then call apply and then pass the number. That's it. Now let's run the program and let's see the output. And there we go. This is working as expected isn't it. So this is how basically we use a method reference to a static method. Okay. So this is how we use the method reference to call a static method. Right. So let's take a look into one more example to demonstrate method reference to a static method. So here what I'm going to do is I'm going to create a one static method here. Let's say public static it returns integer. And let's say addition is the method name and then pass int a comma int b. Okay. And this should return addition of a plus b. All right. Perfect. Now we're going to see how we can write the lambda expression to call this static addition method. Right. So here what I'm going to do is I'm going to write the lambda expression. And then we will convert that lambda expression with the method reference. Well in order to write the lambda expression for this static method we're going to use by function functional interface for example by function functional interface. And it takes three types. Okay. So let's pass the type integer. So here we're going to pass a wrapper class integer. Because you know this is a generic right. So generic don't support primitive. So we have to pass the wrapper class. And then let's call it as by function, let's say by function lambda. And then we create a lambda expression. And then we assign that lambda expression to. This you know functional interface. So here is the syntax for the lambda expression. A comma b and then arrow symbol. And then we're going to call the static method. So we use. A class name to call the static method right. So let's have a class name here that is method. Reference demo. And then call a static method that is additional and then pass a comma B. It's pretty simple lambda expression right. Now let's put a space out and then by. Function lambda and then call apply method and then pass ten comma 20. All right. Now let's go and let's run the program. And let's see how this works. And there we go ten plus 2030. 30 right now. Let's go. And let's see how we can replace this lambda expression with the method reference. So here I am going to write the comment let's say using method reference. All right. So in order to convert this lambda expression into a method reference I am going to simply copy this and paste it over here. And then let's rename this let's say by function. And in order to convert a lambda expression with a method reference, we need to first remove this part. And method reference don't have parameter. So remove this parameter list and then put semicolon and then replace this dot with a double colon. So this is your method reference. And then again put this out and then call apply method and then pass ten comma 20. Pretty simple. Now let's go and let's run the program. And there we go. All right. It's working as expected. So this is how basically we use a method reference to call a static method. It's pretty simple right. Now we understood the first type of method reference that is method reference to a static method. Now let's go and let's take a look into reference to a instance method of a particular object. All right let's head over to the IntelliJ Idea. And let's create an example. Well let me write the comment over here. Method reference to an instance method of an object. Well in order to demonstrate this type of method reference first I'm going to create a one functional interface. So let's have our interface. And then let's call the interface name as print table. Something like this. All right. And just create one method. Let's call it as print. And then pass the String as a parameter. Let's say message. And in order to mark this interface as a functional interface, we can also use add functional interface annotation. All right. Now within this class I am going to create a method. Public and let's say void. And let's give method name as display. Display. All right. And then just pass string as a method argument to this method string message. And put this result over here. Or first what you want to do is we're going to convert this message into. Uppercase. Okay. To uppercase. And then we're going to put this out. And then we print the message. All right. Perfect. So what we have done, basically we have created printable functional interface and we have created this display method. And then we have simply, you know, converted this input string into uppercase. And then we have just printed this string right. Now let's go. And let's first see how to write a lambda expression to implement this printable function interface. And then we'll later, you know, convert that lambda expression into method reference. Well here we are going to demonstrate method reference to an instance method of an object. Right. So first we need to create an object. So we're going to create an object of this class. Okay. So let's go and let's create an object of this class first. And then we call a method it's method right. So let's say method references demo equal to new method references to demo. And then what we will do is we will write the lambda expression lambda expression first. And then we will convert this lambda expression into method reference. So basically we use a lambda expression to implement a functional interface, right. In our case printable is the functional interface. And here is the syntax. Let's pass a message as an input. And then we have an object and then call its method called display. And then pass the message. So this is how basically we write the lambda expression to implement a functional interface. And if you can see here this display method should have the same, you know, parameter and should have the same return type of this abstract method. Right. So if you can see print method has a return type wide and as a parameter of type string okay. Then only you can able to assign this lambda expression to this, you know, functional you know interface. All right I hope you understood this one. Now let's go. And let's call this method printable dot print and then pass something like hello World. All right. And then let's run the program and let's see how this works. And there we go. The Hello World string is converted into uppercase. And then it is printed to the console. All right. So this is how we write the lambda expression to implement this functional interface. And if you can open display method. So display method should have the same return type and same parameter type okay. That's why we can able to implement this method using lambda expression. And then only you can able to assign to the printable functional interface. All right. Now let's go and let's convert this lambda expression with the method reference. Well if you are using IntelliJ Idea then you can have a shortcut over here. So just click on display and and just enter Alter and enter and you will get option here like replace lambda with the method appearance. So you can use this you know option to convert a lambda with method reference. Okay. So this is you know how you can see in IntelliJ Idea okay. In college Idea will basically provide a shortcut to convert Lambda with the method reference. All right. But we're going to convert manually this lambda expression with the method reference. So now I am going to simply write the comment here let's say using method reference. And then I'm going to paste this line. And let's convert this lambda expression with the method reference. So let's call it as print table method reference. And in order to convert this from lambda expression with the method reference, let's remove this lambda part. And let's also remove this parameter list. Put the semicolon and remove this dot with the double colon. So this is your method reference. And then simply call printable method reference dot print method and then pass hello world. All right. And then let's go ahead and let's run this program and let's see the output. Well let me run the program. And you can able to see hello world. You know string is converted into uppercase and it is printed to the console. All right. So this is how basically we use a method reference to an instance of an object. So this is basically object. And we are calling its method using method reference. So that's why method reference to an instance of an object. Okay. So this is the object. And this is the its method. And we are using method reference to call you know a method of this object right. That's the reason this is the name. Well so far we have seen meta references to a static method and a reference to an instance method of a particular object. Now let's go and let's see reference to a static method of an arbitrary object of a specific type. Okay, so this is a little bit confusing, but let me create an example so that you will be definitely going to understand this method reference type. Okay. Let me head over to the IntelliJ Idea. Well here I am going to write the comment first reference to an instance method of an arbitrary object. Well, sometimes we call a method of argument in a lambda expression. In that case, we can use a method reference to call an instance method of an arbitrary object of a specific type. So let's go and let's create one simple example to understand this method reference type. So let's use function functional interface. Well function functional interface basically takes a input and it returns the result okay. And just pass the type first type as a string and second type as a string. All right. And let's call it as string function. And then pass a lambda expression I mean assign the lambda expression to this function functional interface. So here is the lambda expression syntax. Let's have a input of type string. You can also mention string like this okay. But we can remove this type compiler will automatically infer the type here by looking into okay this this type here okay. So you can keep or you can remove. So let's put the arrow symbol. And then here what I'm going to do is we're going to convert a string into lowercase by using string provided two lowercase method I am going to call input up type string and then simply call to lowercase. All right. So if you can see here input is basically a argument or parameter of lambda expression, right. And here we are calling a method of this argument. Okay. So this is nothing but a reference to an instance method of an arbitrary object. So input is basically arbitrary object okay. If you can read this sentence over here, sometimes we call a method of argument in a lambda expression. If you can see this is the argument of the lambda expression. And we are calling its method over here okay. So this is basically called called a method reference to an instance method of an arbitrary object. So if you have a lambda expression like this then you can convert this lambda expression with the method reference okay. I hope you understood this method reference type. Well. Whenever you have a lambda expression it has an argument. And if you call its argument method then you can replace this lambda expression with the method reference. Okay. Now let's go and let's put this out. And then call string function and then call its method apply. And here let's say Java gets. Okay. Now let's run the program and let's see how this works. And there we go. Java gets is converted into lowercase. Now let's go and let's see how to replace this lambda expression with the method reference. So here let me simply copy this. And here first I'm going to write the you know comment here using method reference. Okay. And paste it here and simply remove this. And input is basically a string. Right. So let's have a string here and then replace this dot with double colon and then replace this parameter list. All right. This is your method reference. And this reference variable is already exist. So let's rename this let's say string function method reference okay. Perfect. So this is how we basically replace this lambda expression with the method reference. If you are using IntelliJ Idea then you can use IntelliJ Idea provided option to convert this lambda expression with the method reference. For example, just place this cursor on this method and then press alt enter and you will get option to replace a lambda with method reference and just hit enter. So this will convert a lambda with method reference okay. So this is another option. All right. So let me uncomment this and let me put this this out. And let me call the method and let me pass the Java guide as a input okay. Let me run the program and let's see how this works. And there we go. Okay. So the method that we have written is working as expected. All right. So this is how basically we write the method represents to an instance method of a arbitrary object. Well whenever you have a lambda expression with a method argument and you are calling a method of this argument, then you can go ahead and replace this lambda expression with the method reference. Okay. Well let me write one more example to demonstrate this method reference type so that you will understand better. So here I have created an array with these elements. And I am using arrays class to sort this array. Okay. So if you can see here this sort takes two parameters. First one is a string array and second one is the functional interface. And here we have provided a lambda as an implementation of functional interface. And this is lambda expression. And if you can closely observe this lambda expression it takes S1 and S2 as arguments. And you can see here S1 argument. We are calling compare to ignore case method of this S1 argument. Okay, so this method reference states that whenever you have a lambda expression with argument and if you are calling the method of this argument, then you can replace this lambda expression with the method reference. Right. So that is what we have done here. So here we have basically replaced this lambda expression with the method reference. Okay. So this is another example to demonstrate this method reference type. Well let's take a look into the next type of method reference that is reference to a constructor. Let us adore the IntelliJ Idea and let me demonstrate this type. So here I am going to write the comment that is reference to a constructor. Well in this example what I am going to do is I am going to convert a list into a set. And then I will show you how we can replace the lambda expression with the method reference. So here, first of all I'm going to create a list of fruits okay. Let's call it as fruits. And then let's use ArrayList implementation. Let's add a couple of fruits to this list. Let's say banana. Fruits dot add and then let's say apple fruits dot add and then pass mango. And then fruits dot add and then pass water. Watermelon something like this. Now let's convert this list into a set using lambda expression. So we're going to use again the function function interface. It takes two arguments. Let's say first argument is list of type string okay. And second argument of type set. It takes again string as a string as a type. And let's call it as set function. And then let's write the lambda expression and then assign to this functional interface. So we need to pass the list of string right. That is fruits let's say. Puts list let's call it as a fruit list. And then this is the arrow symbol. All right. And then here simply call new hash set and then pass. Fruits list. That's it okay. So whenever you pass a list to the hash set constructor then asset constructor will you know I mean this will convert a list into a set, right? So this is how basically you write the lambda expression to convert a list into a set. Okay. Now what we will do is we want to put this out over here. And then we call apply method. And then we pass a list let's say fruits okay. Now let me run the program and let's see how this works. And there we go. All right. We have printed the set right here by using System.out.println. Now let's go and let's see how we can convert this lambda expression with the method reference. So let me simply copy this. And here I am going to write the comment let's say method A reference using method reference basically. And then paste it over here and let's call it as set function method reference. Well, in order to convert this lambda expression with the method reference, first of all remove this. This part. All right. And parameter list is not required in case of method reference. So just put a double colon and then new. That's it. We have simplified a lambda with a method reference like this. Right. And then put this result over here. And then set function method reference. And then call apply method and then pass the fruits list okay. And then just go ahead and run the program and let's see how this works. And there we go. So this is how basically we use a method reference to you know reference to a constructor. Okay, here we have an object of Hashset and we have replaced this with a method reference to a, you know, constructor using method reference syntax double colon followed by new. Well, whenever you have a lambda expression with this logic, then you can go ahead and use method reference to a constructor type like this. All right. These are the four method references. And I have demonstrated all of them with an example. All right. And these four types of method references are pretty important. So whenever you have a lambda expression which is just a reference to a method, then you can go ahead and replace that lambda expression with respect to method reference type. All right I hope you understood what is a method reference and what are the different types of method represents and how we can use, you know, method references to replace the lambda expression. All right. Great I will see you in the next video.