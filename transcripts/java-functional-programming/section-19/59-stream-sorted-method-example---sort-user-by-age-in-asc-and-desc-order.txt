Hi everyone. Welcome back. In this lecture we will take a look into how to sort a user by age in ascending order and descending order. So basically we are going to use a sorted method along with a comparator to sort the user by age in ascending order and descending order. Okay. So let us go to our project and let us create an example. So here go to the sorted package. Right click on it new and then choose Java class. Let us give class name as sort user example. Next, within this class let's create a main method. Well, in order to sort the user by age, we need to first create a user class right? So here just type class and then user. Next let us define the instance variables within this user class. So just type private and the string name and then private int age To keep it simple, let us have these two fields. Next right click on this user class generate and then choose Getter and Setter and then choose these two fields. Click on okay. Next let us also create a constructor. So here right click Generate constructor. Select these two fields. Click on okay perfect. Next also create a toString method. So go at the end of this class right click generate toString. Select all these fields. Click on okay perfect. Now the user class is ready. Next go to our main public class. Go to main method. Within this main method let's create a list of users. So here just type list and then pass user as a type and lets you reference variable name as users and then arrays. Arrays is a utility class. It has as list method to create a list. So within this as list method just pass the user objects. So here new user and then pass user name as Ramesh. Let's view age as 30. Next let us create a few more user objects. New user and then pass name as Umesh. Let's give age as 28. Next new user. Let u user name as Sanjay. And then let's do age as 35. The age should be in a integer. And then new user. Let's view user name as Pramod. Age 32. And then new user. Let's give The username is Mina and lets you age as 25. Now we have a list of users. Next, let us go ahead and let us see how to use stream provided sorted method to sort a list of users by age in ascending and descending order. So here, first of all, we need to convert this list into a stream, right? So here just call this list. That is users. It has a stream method. So this stream method basically creates a stream by this list. And stream method returns a stream. So we can call the sorted method on that stream. So here just type sorted. And notice here, in order to sort the elements in a custom order, we have to use the sorted method that takes a comparator as a parameter. So just call this sorted method. And here we need to pass the comparator as a parameter to this sorted method. Right. So first what we are going to do is we are going to first implement the comparator here. So here just type comparator. And notice here comparator interface is a generic interface from Java.util package. So just call the comparator and pass user as a type. Next let us give reference variable name as user comparator and then new comparator. So here basically we are using anonymous class to implement this comparator interface. Okay. Next go to the compare method. And here we're going to sort the user by age right. So just call this object one. It has get age method. And then minus object two dot get age. So this is the logic to sort the user by age in ascending order okay. Next put the semicolon at the end. And then finally let's pass this user comparator to the sorted method. So here user comparator. Next this sorted method returns a new stream with the sorted elements. And we need to convert that new stream into a list. For that we can use two list method. Next, let us take the result of this two list method into local variable of type list and then pass the type as a user. Next, let us view the variable name as result. And finally put the system println statement and then pass result. Result is a user object. It has toString method. Next, let us run the program and you can see the output over here. So Meena has age 25. Umesh has age 28, Ramesh has age 30. Pramod has age 32, Sanjay has age 35. And if you look at the numbers 25, 28, 30, 32, 35 okay, so basically the users are sorted by age in ascending order. Okay. So this is how we can use sorted method with comparator to sort the user by age in ascending order. Okay. And notice here we have used anonymous class to implement this comparator interface. So instead of using anonymous class we can use the lambda expression for example. So just remove this user comparator from here and pass the lambda expression. So this comparator interface takes two parameters. So just pass two parameters to the lambda expression that is object one comma object two. And then followed by lambda symbol. And within a lambda body just pass this logic. Just copy it paste it over here. That's it. And just comment out this this is it okay. So by using lambda expression we can simplify a lot okay. Next let us run the program. And you can see the output over here. Same output. The users are sorted by age in ascending order okay perfect. Next we can simplify this logic further. So instead of using lambda expression, we can use the readymade logic provided by comparator. For instance, just remove this lambda expression and then call comparator. It has a comparing int method and then pass the age field to this comparing int method. For instance, just call user and then method reference and then call get age. That's it. So this comparing method internally provides a logic to sort the user by age in ascending order. For instance, let us run the program over here and you can see the result same okay. The users are sorted by age in ascending order. You can see age 25, 28, 30, 32, 35. Okay, perfect. So instead of writing the code manually, we can use the existing code like this. All right. Perfect. So this logic is to sort the user by age in ascending order. Next, let us see how to sort a user by age in a reverse order or in a descending order. Descending order. It's pretty simple. Just copy this code and paste it over here. As we know that this code will sort user by age in ascending order, right? So in order to sort, you know, the user by age in descending order, what we need to do is we need to just call the reversed method. For instance go to the sorted method, go to the comparator and then comparing it. So it is comparing int method returns a comparator and comparator has a reversed method. So here just call the reverse order method. That's it. So this logic will sort user by age in descending order. Okay so let us give different variable name. Let us say result one. And here also result one okay. Perfect. Next let us run the program over here and you can see the output. The users are sorted by age in a descending order. You can see the numbers 35, 32, 30, 28, 25. Okay. So this is how we can use a sorted method with the comparator to sort the user by age in a descending order. It is pretty simple, right? Next, let us say we have a requirement like sort the user by name in ascending or descending order. Then how we can do that. So here let me show you how we can sort the user by name in ascending and descending order. It is pretty simple. We just have to use the comparator provided methods. For instance here let me copy this code. And here let me paste it. And here let me, you know, change accordingly. Sort the user by name in ascending order. And here let me rename it as result two. And here, instead of using compare comparing int method, we have to call the comparing method and then pass the field that we are going to use for comparing. In this case, the field name is name. Okay, so just call user and then method reference and then call get name. That's it. Next here just pass result two. So this logic will sort the user by name in ascending order. Next let us run the program and let us see the output. So here let me run the program. And if you can notice the output over here the users are sorted by name in ascending order. You can see first letter upper name m and then p and then r s u. Okay. Perfect. Next let us see how to sort a user by name in a descending order. It is pretty simple. We just have to call this reversed method. Right. So here let me copy this piece of code and just paste it over here and let us change accordingly. Sort the user by name in a descending order. And here let us give the variable name as result three. And here just call the reversed method a reversed method basically returns the comparator that provides a logic to reverse you know the user by name. Just call this method. And here just pass result three. That's it. Next let us run the program. And here you can see the output. The users are sorted by name in descending order. You can see first letter upper name u s r p m. Okay perfect. So this is how we can use sorted method to sort the user by name in ascending and descending order. Okay, perfect. I will see you in the next lecture.