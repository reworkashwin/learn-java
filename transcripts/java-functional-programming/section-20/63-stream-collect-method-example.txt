Next, let us go ahead and let us understand more about collect method with example. So let us go to our project in IntelliJ Idea. And here go to the stream package. Right click on it new and then choose package. Let us give package name as collect. Within a collect package we keep all the collect method related examples. So within a collect package just create a new class. Let us give class name as collect example. All right. Within this class let's create a main method. The collect method is used to gather the elements of a stream into a container such as list, set or map. So in this example, we will see how to gather the elements of a stream into a different data structure like list, set and map. Okay, so for that first let us go and let us create a stream. Well we can directly create a stream by using stream provided op method, right? Otherwise we can also use the source like list, set or map. But this is the simple example. So let us use stream provided op method to just add elements to the stream. So here let us go. Element name as apple and then banana. And then mango. Okay. Next this op method returns a stream. So here let us introduce a local variable of type stream. Here stream is a generic. Let us pass string as a type and let us give the reference variable name as stream one. Now we have a stream of elements. Next, let us use collect method to collect all these elements of a stream into list. So here just call stream one. It has a collect method. And notice here there are two overloaded collect methods. Let us use the collect method that takes collector as a parameter. And this collect method. Basically expect the collectors utility class. So make sure that you import collectors utility class from Java.util dot stream package. Next, this collectors utility class has two list method, So just call this two list method. Next, this collect method returns a list. So let us take the result of this collect method into local variable of type list. Let us pass the type as a string and let us give the variable name as result. And finally, let us put the system dot println statement and just print the result. Next, let us run the program over here and there we go. You can see the output Apple, banana, mango. So this is how we can use collect method to collect the elements of a stream into a list okay. Similarly, let us go ahead and let us see how to use this collect method to collect the elements of a stream into a different data structure like set. Okay, so it is pretty simple. So here just copy this piece of code and paste it over here. Well once we use the stream we cannot reuse again we have to create a new stream. Okay. So here just create a stream and let's call it as stream two. And here stream two And then call collect method. And instead of two list, just call two set. Perfect. And here let us give result two. So here also change from list to set. Okay. Perfect. Next let us run the program. And here you can see the output setup elements. Okay. And here if I pass one more duplicate element to the stream for instance let us say apple. And if I run the program here you can see the output banana apple mango. Even though the stream has a duplicate elements. But after gathering the elements of a stream into a set by using collect method, you can see the result. Banana. Apple. Mango. The elements are unique. There is no duplicate elements from this set. So this is how we can collect the elements of a stream into a set by using this collect method. Next, let us see how to gather the elements of a stream into map by using collect method. So first we need to create a stream. Well, once we use the stream, we cannot reuse it again. We have to create a new stream. Okay. So here just create a new stream with the elements apple, banana, mango and just call this stream as stream three. Next just call stream three. It has a collect method. This collect method. Expect collectors as a parameter. And this collectors utility class has a two map method and the two map method expect key value pairs. So just call this two map method and notice here two map method takes two parameters. First parameter is a function you know interface, and second parameter as collector interface as a parameter. And we have to use the lambda expression to implement these two functional interfaces okay. So let us use fruit name as a key and fruit name length as a value. So here let us use the lambda expression. So just pass fruit as a parameter to the lambda expression and then followed by lambda symbol. And within a lambda body just give fruit name as a key okay. Next comma. Let us view fruit length as a value. So for that let us form the lambda expression. So let us pass Pruett as a parameter to the lambda expression followed by lambda body and then Pruett dot length. So this is the value. And finally put the semicolon at the end. Next let us collect the result of this collect method into a map. So here let us introduce a local variable of type map and then pass string as a key type integer as a value type. Let's give name as map perfect. And finally put the System.out.println statement and just print the map. Next, let us run the program over here. And here you can see the output. Banana, apple, mango. And you can see the corresponding value that is fruit name length. Banana has a length six. Apple has a length five. Mango has a length five. So this is how we can use collect method to collect the elements of a stream into map. Okay, perfect. In next lecture we will learn more about collect method with examples. Okay, great. I will see you in the next lecture.