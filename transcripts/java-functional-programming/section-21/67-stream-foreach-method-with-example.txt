Hi everyone. Welcome back. In this lecture we will learn everything about stream provided for each method. The porridge method in a Java streams is a terminal operation used to apply an action to each element in a stream, while the porridge method is basically used to iterate over all the elements of a stream and perform an action on each element in a stream. Well, in example section, we will see how we can use porridge method to perform an action on each element of a stream. Since the porridge method is a terminal operation, it is usually the last step in the stream pipeline, meaning it finishes the stream processing well. For each method is a terminal operation, and once we call the porridge method, the stream pipeline ends. We cannot use that stream again. If you want to use the stream, you have to create a new stream next. One important thing to remember is that porridge method does not give you a new result or let you collect data. It is only used to perform actions on each item. Well, the forest method is a terminal operation so it does not return any result. It only used to perform actions on each element in a stream. Next forest method is commonly used for tasks like displaying or logging each item in a stream well. Forest method does not return any result or collect the data. It is only used to perform an action. Action can be displaying data or logging the you know data to the console. Next, let us quickly understand more about forest method with this diagram. A stream can be created using a source. Source can be a list, set, map or array. Once we create a stream, then we can apply the intermediate operations like filter, map sorted, etc.. Next, the intermediate operations returns a new stream so that we can call the terminal operations like for each method. The porridge method is a terminal operation. It does not return any new result or collect the data. It is used to perform an action on each item in a stream. Okay, and this porridge method basically takes consumer functional interface as a parameter. And we have to use a lambda expression to implement this consumer functional interface. So this consumer functional interface takes a single argument and does not return any result. That's why we use porridge method to perform an action on each item in a stream. Okay. Perfect. Next let us understand more about porridge method with examples. So let us go to our project in idea. Go to the stream package, right click on it new and then choose package. Let us give package name as porridge within a porridge package. Let's create a new class. Let us give class name as stream for each example Within this class. Let's create a main method. All right. Next within a main method let's create a list as a source. So here just type list and then pass string as a type. Let's give the list name as fruits. And then let us use arrays. Utility class provided as a list method to create a list. So just add the elements. Apple. Banana. Mango. Now we have a list as a source. Next we can create a stream from this list. So here just type fruits. This is a list. It has a stream method. So stream method we use to create a stream from this list. Next, this stream method returns a stream object. We can call the for each method. And notice here porridge method takes consumer functional interface as a parameter, and we can use lambda expression to implement this consumer functional interface. And again notice here for each method returns a wide. It means for each method does not return anything. Okay. So here wide means nothing. All right. So basically we use for each method to perform an action on each item in a stream. This for each method does not return anything or collect the data. This consumer functional interface takes a single input argument and does not return the result. So just call this for each method over here and then pass the lambda expression that implements consumer functional interface. So here is the lambda syntax. And this consumer takes a single input argument. So let us pass single parameter to the lambda expression. So let us say element and then followed by lambda you know symbol. And then within a lambda body just put the System.out.println statement and just pass the element. So this element is nothing but apple banana mango okay. Next let us run the program. And here you can see the output apple banana, mango. So this is how we can use porridge method to iterate over all the elements of a stream and perform the action, like printing the elements. Okay, so we commonly use this for each method to print each element of a stream. Okay, perfect. Next, we can also use this for each method to print different types of data. So here we have printed the strings. We can also print the integers for example. So here let me quickly create a list and then pass integer as a type. And let's call it as numbers. And then arrays dot as list one comma two comma three comma four comma five. So this list is a source. Next let us create a stream from this list. So here numbers and then call its stream method. Stream method returns a stream object So we can call the forage method over here. So forage method is a terminal operation and it does not return anything. You can see the return type of the forage method wide okay. Just call this forage method and pass the lambda expression that implements consumer interface. And this consumer interface takes a single input argument. So let us pass single input argument to the lambda expression that is element and then followed by lambda symbol. And within a lambda body. Just put the System.out.println statement and then pass the element. That's it. Next let us run the program and you can see the output 12345 okay. So basically the for each method iterated each element of a stream and printed to the console okay. Perfect. Next let us see how to use this forage method with intermediate operations a intermediate operations can be a filter map sorted right. So let us see how to use this forage method to print the sorted elements are per stream. So here, let us sort this number in a reverse order and use this porridge method to print each element of a stream. So here just create a stream from this list again. So numbers it has a stream method. Stream method returns a stream object. So we can call the sorted method over here. So here we are going to sort the elements of a stream in a reverse order. For that let us use the sorted method that takes a comparator as a parameter. And here just pass comparator dot reverse order method okay. So this reverse order method basically returns a comparator that reverse the order of a stream. Okay. Next, this sorted method basically returns a new stream that contains the sorted elements. So this sorted method returns a new stream. It contains the sorted elements. Next we can call for each method to iterate over each element of a stream and print to the console. So just call for each method. Next, let us pass the lambda expression that implements consumer interface. So here just pass element as a parameter to the lambda expression followed by lambda symbol. And within a lambda body. Just put the System.out.println statement and just pass the element. Okay perfect. Next let us run the program. And here you can see the output. 54321. Okay. The elements of a stream are, you know reversed. Okay. The integer numbers of streams are sorted in descending order. You can see here. So this is how we use Java Stream provided for each method to iterate over all the elements of a stream and perform an consumer action on each element of a stream. In real time projects, we basically use for each method to iterate over elements of a stream and print to the console. Okay, perfect. I will see you in the next lecture.