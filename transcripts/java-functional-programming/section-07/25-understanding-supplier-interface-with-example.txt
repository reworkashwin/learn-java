Hi everyone. Welcome back. In this lecture let us take a look into supplier interface. The supplier interface is a functional interface introduced in Java eight. And we can use Lambda expression to implement supplier functional interface. The supplier interface is a part of Java.util dot function package. The supplier interface represents a supplier of results, takes no arguments and returns the result. The supplier interface represents a supplier of results, meaning it does not take any inputs or arguments and produces the result. Supplier interface is commonly used for providing or generating data, lazy evaluations, and retrieving the default values. Since supplier interface does not take any arguments and produces the result, so it is commonly used for providing or generating the data. Lazy evaluations and default values. Next, let us take a look into supplier interface methods. Get method. So this method is the only abstract method in a supplier interface. It returns a result of the specified type with no input arguments. Well, the supplier interface is a very straightforward with only one key method that is get. So this get method returns a result of the specified type. It takes no input parameters, and its only purpose is to supply or produce the data when needed. Okay, so let us go ahead and let us see how to use get method with practical examples. So here let us head over to the IntelliJ Idea and go to our project. And here go to Functional Interfaces Package. Right click on it new and then choose package. Let us give package name as supplier. Within a supplier package you will keep all the supplier supplier interface related examples. So right click on Supplier Package New and then choose Java class. Let us give class name as supplier get example okay. Next within this class let's create a main method over here Within a main method. Here let us define a supplier that supplies the constant. Constant value. Okay. Perfect. So here just call the supplier. Well notice your supplier is the generic interface from Java.util dot function package. So just call this supplier interface and just pass the type string. And let us give variable name as constant supplier. Okay. Next let us construct the lambda expression that implements the supplier interface. Well if you go inside the supplier interface code you can see supplier interface has only one abstract method get. And this Get method does not take any input parameters. It just returns the result. It will basically produce some data or the content and it will just return that data. Okay, great. So let us go back to our example here, here and here. Let us create the lambda expression that implements this supplier interface. And here is the lambda syntax. Well the get method does not take any parameters. That's why we don't have to pass any input parameter to the lambda expression. And in the lambda body just have a return statement and return some constant value. Let us say hello world okay. You can give any constant value over here okay. So this is the supplier that will supply some constant value okay. And you can see a lambda body contains a single statement. We can also remove the open and close curly braces. So just remove the return keyword as well. Okay. Now this is the supplier to supply the constant value. Next here let us call supplier and then call get method to get this constant value. So notice here. Get method returns string. Because here we have passed a string as a type to the supplier. That's why the get method returns a string over here. Next let us take the result of this get method into local variable of type string and let us give variable name as result. Next, let us put the System.out.println statement and just print the result. Next, let us run the program and you can see the output. Hello world. All right so this is how we can use lambda expression to implement the supplier functional interface and supply some data. Okay. So this is useful when you need to supply a default or a static value in your application. Okay. Perfect. Next let us take a look into one more example. Like we are going to create a supplier that returns the current date time current date and time. Okay. So here just type supplier and then pass local date time as a parameter. Local date time. All right. Next let us give a variable name as date time supplier. And then let us construct the lambda expression and assign that lambda expression to a variable of type supplier functional interface. So here this is the lambda syntax. Well the supplier does not take any input arguments. So here we do not have to pass any parameters to the lambda expression. Next supplier produces the result and it returns that result. So in a lambda body just write the logic to return the current date time. So here just have a return statement and then local date time. And then call null method. So this will basically return the current date time okay. And again if you look at here the lambda expression body has a single statement. So let us remove the open and close curly braces as well as the return keyword. Okay, perfect. Now we have created a supplier that returns the current date time. Next, let us get the date time from the supplier by using get method. So here just call the variable date time supplier. It has a get method. And next let us introduce a local variable of type local date time and then let us give name as current date time. All right next let's put the System.out.println statement and then pass this current date time variable. Perfect. Next let us run the program and you can see the output over here. This is the current date time. Okay. So this is how we can create a supplier that returns a current date time. And we can call the get method to get the current date time from the supplier. It's pretty simple isn't it? Great. I will see you in the next lecture.