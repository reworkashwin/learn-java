Hi everyone. Welcome back. In this lecture let us discuss int supplier long supplier and double supplier primitive functional interfaces. These interfaces are part of Java's functional programming specifically designed to work with primitive types int, long and double. So these interfaces are primitive versions of standard supplier interface. Well, the supplier interface is an interface that, you know doesn't take any input argument but returns some result. Okay. Basically this supplier interface supplies a values okay. To avoid the performance overhead of Autoboxing. Java provides primitive functional interfaces for supplier interface like int supplier, long supplier, double supplier to work with primitive types int, long and double. Well. Here Autoboxing is nothing but a automatically conversion of the primitive types like int long double into their corresponding wrapper classes integer, long and double. Well, if we want to use supplier interface with a primitive types, we have to use the corresponding wrapper classes because supplier interface is a generic interface and generic does not support the primitive types. If we want to use the primitive types with a supplier interface, we have to use the corresponding wrapper classes. Okay, so let us say we use the wrapper classes with supplier interface. Then Java compiler will automatically convert the primitive types into their corresponding wrapper class object. So this causes a performance overhead because Java compiler, you know, have to convert the primitive type into a corresponding wrapper classes whenever required. So in order to avoid this automatic conversion, the Java has introduced these three primitive, you know, functional interfaces like int supplier, long supplier and double supplier to directly work with primitive types like int, long and double. So in this case the autoboxing or the automatic conversion between primitive types and the corresponding wrapper classes will not happen. Okay. Next int supplier interface applies int values directly, avoiding boxing into corresponding wrapper. Integer class object. Long supplier interface supplies a primitive type long values directly avoiding boxing into corresponding wrapper. Long class object. Double supplier interface supplies primitive type double values directly avoiding boxing into corresponding wrapper. Double class objects. Okay. Next let us go ahead and let us understand. You know more about these three primitive functional interfaces with example. So let us go to our project over here and go to Supplier Package. Right click on it new and then choose Java Class. Let us give class name as supplier primitive interfaces example. All right. Next within this class let's create a main method. Well first we are going to see the supplier interface. And then later we will see how to replace the supplier interface with int supplier interface to avoid the Autoboxing. So here just type supplier and then pass integer as a type for the result. And next let us view the variable name. Let us say random value. Let us have the lambda expression over here. So this is the lambda syntax. So supplier interface does not take any input argument. So we do not have to pass any parameter to the lambda expression. Next go to the lambda body. And here let us write the you know formula to generate the random number. So here just have return statement. And then just type math and then call random method and then multiply with 100. Let us enclose in a parenthesis and just give a cost as a int. All right. Next the lambda body has a single statement. Let us remove open and close curly braces as well as the return keyword from here. All right. Now we have a supplier. Next, let us get the random value by calling the get method. So here just put the system println statement and then call the supplier. That is random value. It has a get method. So this get method basically we use to get the value from this supplier. Next let us run the program. And here you can see the output. So again if you run the program you can see this output because this basically generates a random number. Well in this example when returning an int from this supplier Autoboxing happens converting the primitive int into corresponding wrapper integer object. So this autoboxing leads to performance overhead, especially when working with large data sets or high frequency operations. In order to avoid this autoboxing overhead, we can use the primitive functional interface that is int supplier. Okay, so let us see how to use it. So here let me copy this code and let me comment out. And here let me paste it over here. So instead of supplier just call it supplier okay. And here instead of get method just call get as int okay. That's it. So this is how we can use int supplier to avoid the autoboxing. Next if we run the program you can see the output okay. So this basically supplies the random number in an integer format. Next let us look at long supplier interface which supplies long values directly. This interface is useful for situations where large numeric values are involved, such as time stamps, IDs, and counters. By avoiding the boxing of primitive type long into the corresponding wrapper long class, it helps save memory and processing time. Okay, so let us go ahead and let us see the usage of long supplier interface with example. So go to our project again. Go to the same example. And here let me write the comment long supplier to get the current system timestamp in milliseconds. All right so here just type long supplier from Java.util function package. And then let's give variable name as timestamp supplier. Next let us create the lambda expression and assign that lambda expression to this variable of type long supplier. So here is the lambda syntax. Well this long supplier does not take any input argument. So we don't have to pass any parameter to the lambda expression. Next go to the lambda body. And here just have a return keyword and just return the current system timestamp in milliseconds. So we have a system class. It has a method current time milliseconds. So this method returns a current system timestamp in milliseconds. Okay. And you can see current time milliseconds. Method returns a log okay. Just call this method over here. Next the lambda body has a single statement. Let us remove open and close curly braces and also the return keyword from here. Next let us put the System.out.println statement and then call timestamp supplier and then call get as long method. Next let us run the program and you can see the output over here. So this is the current system timestamp in milliseconds. All right. So this long supply returns the current system time as a long value. Avoiding the boxing into the corresponding wrapper long class object. So this is a practical use case when working with timestamps or the large numeric values. Okay. Next let us look at double supplier interface which supplies a double values directly avoiding boxing into the corresponding wrapper. Double class object. So this is useful in scenarios where precision is important, such as scientific calculations, financial applications, or any context that involves floating point values. So using double supplier interface avoids the overhead of boxing between, you know, primitive type double into the corresponding wrapper double class object. All right. So let us go ahead and let us create an example over here. So here just write the comment double supplier to simulate the temperature sensor reading. So here just type double supplier. And then let us give the variable name as temperature supplier. Next let us have the lambda expression syntax. Well the double supplier interface does not take any input argument. So we do not have to pass any parameters to the lambda expression. So just go to the lambda body here and have a return statement. And then just type pip dot five. Plus just generate the random number here math dot random and then 100 or let us say ten. Okay. So this is basically the formula to you know, simulate the temperature sensor reading okay. It will basically generate the random number as a temperature. Okay. Next let us remove the open and close curly braces as well as the return keyword from here because the lambda body contains a single statement. Next let us put the System.out.println statement. And here let us get the you know value from this supplier. So here let us add the string. Let us say temperature. And then just call this temp supplier. It has get as double method. Next let us run the program. And here you can see the output. All right. So this double supplier basically returns a double value representing a simulated temperature reading. So this is basically a dummy temperature that we have generated by using this formula. Next let us take a look into when to use int supplier long supplier and double supplier interfaces. Use int supplier when generating or supplying int values, especially in a performance sensitive applications, to avoid the boxing into corresponding wrapper. Integer class. Use long supplier for operations involving large numbers like IDs, counters or timestamps to avoid boxing into corresponding wrapper. Long class. Use double supplier for precision floating point values, such as in scientific or financial calculations, to avoid boxing into corresponding wrapper. Double class. All right. Great. I will see you in the next lecture.