Hi everyone. Welcome back. In this lecture let us discuss stream provided map method. Well map method is one of the widely used method in a stream API. The map method is used to apply a function to each element in the stream, and return a new stream with the transformed data. Well, basically, we use a map method to transform the data in a stream. Well, in example section we will see how we can use a map method to transform each element in the stream. The map method is a intermediate operation in Java stream as map method returns a new stream so we can change the method calls. For example, here list of strings is a list and we can use stream method to convert list into stream. And we can, you know, use a map method on a stream. And again map method returns a new stream. So we can call the collect method. So this is how we can change the method calls okay. So map is a intermediate operation. And it returns a new stream. So we can change the method calls okay. Next map method is commonly used to convert the objects or transform the objects, modify the data, or extract the specific fields from the complex objects. Well, in example section we will see how to use map method to convert the objects or modify the data or extract specific fields from the complex objects. Let's understand more about map method with this diagram. Well, in order to create a stream, we need a source. A source can be a list, set, map or array. Once we create a stream, next we can call its map method. Map method takes a function as a functional interface. So this function is functional interface. It represents a function that takes an argument of type T and produces a result of type R. So basically this function we use to transform one object into another object. Okay. So in example section we will see how we can use map method to transform or convert one object into another object. Next this function is a functional interface, and we can use lambda expression to implement the functional interface. Next, the map method is an intermediate operation. It means map method returns a new stream with a transformed data. So map method returns new stream so we can change the method calls okay. Next the map method is an intermediate operation and it won't immediately execute. Once we call the terminal operation, then the intermediate operations will be triggered. Next, let us understand more about map method with examples. So let us head over to IntelliJ Idea. Let's go to our project. And notice here I have changed the theme from dark to light. Earlier I was using the dark theme. For example go to preferences go to appearances. And here earlier I was using dark. But this dark was not looking that much of you know good. So I have changed it from dark to light. So in order to change the theme, go to the integer area, go to the preferences. go to the appearance and here just change the theme. So here. Change the light and click on okay. So the light theme looks good in English. Idea. So going forward I will be using light theme okay let's go back to the map method examples. So here go to the stream package. Right click on it new and then choose package. Let's do package name as map. So within a map package we will keep all the map related examples. So within a map package right click on it new and then choose Java class. Let us give a class name as stream map example okay perfect. Next let me expand it. So within this class let's create a main method. Within a main method let's first create a source as a list. So here just type list and then pass string as a type. And let us give the list name as uproots. And here are its. dot as list and then pass elements. Let us say apple and then banana and then mango. Okay, now we have a source as a list. Now we have source as a list. Next let us create a stream from this list. So here just call fruits as a list. It has stream method. So we can use stream method to create a stream from this list. Next, let us store the result of this stream method in a local variable of type stream. Stream is a generic. Let us pass string as a type and let us call it as stream. Now we have a stream. Now we have stream object. Next let us call its map method. So here we have a stream object and then it call its map method. And notice here map method takes a function as a parameter. Well this function is nothing but a functional interface. And we have to use lambda expression to implement this function interface. And again notice here this map method returns a stream. Okay. So this map method is basically intermediate operation. And it returns a new stream that contains the transformed data. All right. So just call this map method over here. And next let us pass the lambda expression that implements this function interface. So this is the lambda expression syntax. So this function interface takes a single input argument and it returns the output. So here just pass a single parameter to the lambda expression. Let us say element okay. And here if you can notice the list over here list contains the element of type string. Okay. So now what we will do. We will transform or convert each string element into uppercase okay. So here go to the lambda expression again. So here we have passed the element as a parameter to the lambda expression. And then go to the lambda body. And within the lambda body Let's have a written statement and then convert this element of type string into uppercase. So this element is nothing but a string. It has a two uppercase method okay. And again this lambda body has a single statement. We can remove open and close curly braces as well as the return statement. Because Java compiler will automatically infer the return type of this lambda body. Perfect. Now we have used map method to transform or convert each string element into, you know, uppercase. Okay, so this is how we can use map method to convert or transform the data. Finally, we need to collect the result of this map method. Right. So map method basically returns a new stream. So let us convert that new stream into list. For that let us call two list method. Next, let us take the result of this list method into a local variable of type list and let us pass string as a type And then let's call it as a list. And then put the system in and just print the list. Okay. Perfect. Next let us run the program over here. And here you can see the output. Apple banana mango. So these elements are in uppercase. All right. So this is how we can use map method to map or convert each element of a stream into uppercase. Next let us understand how this map method works internally. So here before calling this map method let us call the pick method. So here just call this pick method. Well pick method we use to, you know print each element of a stream. And this pick method is pretty useful for debugging. Like we can use pick method to print each element of a stream to the console. All right. And notice here pick method takes consumer interface as a parameter. And we have to use the lambda expression to implement this consumer interface. So this consumer interface basically takes input argument and it don't return the result. Okay, so just call this pick method over here and then pass the lambda expression. So this consumer interface takes a single input argument. So just pass element as a parameter to the lambda expression. And then followed by lambda symbol. And within a lambda body just put the System.out.println statement and just print this element. Next this map method is an intermediate operation and it returns a new stream of transformed data. So let us call a pick method on this new stream. So here just call pick method and then pass the lambda expression that implements consumer interface. So here just pass element as a parameter to the lambda expression followed by lambda symbol within a lambda body. Just put the system print statement and then print the element. Okay. So if you can notice here, this pick method will basically take each element of a stream. And it will print to the console. And after calling the map method. So this map method basically convert each element into uppercase and returns a new stream. And then here again we call the pick method. So this pick method basically takes each element of this new stream that returned by the map method and print to the console. For example, if I run the program over here, and if you can notice the output over here. So this apple basically printed via this pick method. So here what I will do I will add a string so that we can distinguish before map method. And here just pass after map method. Next let us run the program. And here we need to give the space. So here what I will do I will give the colon and space. And here also colon and some space. Next let us run the program again. Now it looks good. So if you can observe the output over here before map method you can see the element apple and you can see the list apple banana, mango. So this pick method basically takes each element of a stream. And then this map method basically transforms each element of a stream into uppercase. So this is what you can see. So this apple which is in uppercase is basically printed by this pick method. So this map method basically returns a new stream that contains the uppercase elements okay. And this pick method here we have used to print each element of a new stream that is returned by this map method. And that is what you can see here after map method. Apple banana mango. All right. So this map method takes each element of a stream and transform that element and create a new stream with all the transformed data and finally return that stream. Okay. So this is how the map method works okay. In next lecture we will understand more about map method with examples. All right. Great I will see you in the next lecture.