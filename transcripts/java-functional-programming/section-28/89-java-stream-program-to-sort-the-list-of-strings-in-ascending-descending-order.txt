Let us write a Java program to sort a list of strings in ascending and descending order using Java eight stream. Well, here is a given input list of strings. Next, we need to sort this list of strings in ascending and descending order using Java eight stream. All right. So let us see how to do it. So first of all we need to convert this list into a stream. Then only we can use the stream related methods right. So here just call the list that is fruits. And it has a stream method. So this stream method creates a stream from this list. So just call this stream method. Next in order to sort the stream elements stream provides a sorted method. Well notice here there are two overloaded sorted methods. And first we are going to see how to sort the list of strings in ascending order. For that, let us use this sorted method. It does not take any input arguments. So just call the sorted method over here. So this sorted method basically sorts the elements in a natural order that is in alphabetical. Well, if the input is a list of strings, then this sorted method will sort the list of strings in a alphabetical order. If the input is a list of integers, then this method will sort. You know the list of integers in ascending order, that is, in a numerical order. Next, after sorting the stream, we need to convert the stream of elements back into list. For that, let us call two list method. Next, let us take the result into a list of type string. And here let us give the variable name result. Next let's put the System.out.println statement and then print the result. Next let us run the program. And here you can see the output. The list of strings are successfully sorted in ascending order. Apple. Banana. Grapes. Mango. Okay, so if you can see the letter A comes first, followed by B and then G and then m, it means the elements are basically sorted in ascending order or alphabetical order. Okay. Next let us go ahead and let us see how to sort the list of strings in a descending order. So here first of all let me comment out this. Okay. So here first let us convert list into stream by using stream method. So here just call the list that is fruits. It has a stream method. So this stream method basically creates a new stream from this list and then stream has a sorted method. So here basically we are going to sort the list of strings in a descending order. And in order to sort the list of strings in a descending order, we have to pass the comparator to the sorted method, right. So here just call the sorted method that takes comparator as a parameter. And here just pass the comparator dot reverse order. So this reverse order method will sort the stream of elements in a descending order. Okay. Next we need to collect the result back to the list. For that just call two list method. Next. Here let us take the result into local variable of type list and then pass type string and let us call it as result. And finally let's put the System.out.println statement and then print the result. Next let us run the program. And here you can see the output. The list of strings are sorted in descending order. You can see the letter M comes first and followed by G, B and e. This is the descending order isn't it? So this is how we can use Java Stream to sort the list of strings in ascending order, as well as descending order. Okay, great. I will see you in the next lecture.