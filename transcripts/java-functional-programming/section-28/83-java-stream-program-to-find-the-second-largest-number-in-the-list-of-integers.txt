Hi everyone. Welcome back. In this lecture, let us discuss how to find the second largest number in a list of integers using Java eight stream. Well, here is a given list of integers. Next, we need to find the second largest number in the list of integers. And if you can closely observe the list elements over here. So 75 is the first largest number and then 65 is the second largest number. Okay. Next let us go ahead and let us see how to use Java eight stream to find the second largest number in the list. So first of all, we need to convert the list into a stream. Okay, then only we can use the stream related methods, isn't it? So in order to convert list into stream, we can use stream method. For example, list has a stream method. And notice here this stream method returns a stream of integers. So just call this stream method over here. Next stream has a distinct method. So let us call distinct method to remove the duplicates from the stream. And notice here again distinct method returns a stream. So basically this distinct method is an intermediate operation, and it returns a new stream which contains no duplicate elements. Okay, just call this distinct method to remove the duplicates. Next, just call the sorted method. Well, basically we will sort the stream in reverse order so that we can easily get the second largest number. For instance, just call this sorted method which takes a comparator as a parameter. Next, let us pass the comparator and then call its reverse order method. So this will sort the stream in a reverse order. For example, after sorting the stream you can see the elements 75 comes first, followed by 65, and then 50, and then 35, and then 20 and then ten. Okay, so after calling this sorted method, the elements are sorted in descending order 75 comes first, 65 comes second, and so on. Okay. Next what we need to do is we need to skip this first element so that we can get the second largest number that is 65. So in order to skip this first element from the stream, we can use skip method. For example here just call skip method over here. And we need to skip the first element. For that let us pass one. So this will skip the first element from the stream. Next skip method returns stream. And we can also call the methods over here. Let us say find first. So after skipping 75 we need to get the first element by using this find first method. Next. This find first method returns optional so we can call get method to get the element from the optional. So here just call get method perfect. Next, let us take the result of this get method into a local variable of type int over here int second largest. Perfect. Next, let's put the System.out.println statement and just print the second largest variable. Next, let us run the program and you can see the output 65. And if you can notice the elements in the list, 65 is the second largest number. And that is what printed over here. Okay. So this is how we can use Java eight stream methods to find the largest, you know, second largest number in the list. All right. It is pretty simple. We first need to convert the list into a stream. Next once we got the stream we can call its methods right. Next we can call the distinct method to remove duplicates. Next, once we sort the stream into descending order, 75 comes first and then 65 comes second, and so on, right? In order to get the second largest number, we have to skip the first element. For that we have used skip method. Next skip method returns stream. And we have again called find first method to get this 65 element and then find first returns optional. And we have called get method on this optional to get the second largest number as an integer. And that is what printed over here. Okay. It's pretty simple. All right great I will see you in the next lecture.