Now let us go ahead and let us understand lambda expressions with examples. So first of all, let us go ahead and create a new Java project. Within that we will create a lambda examples. So here open the IntelliJ Idea and then create a new Java project. And here let us give project name as Java Functional Programming. And here select the location where you need to keep the project. In my case I have selected desktop and next here build system. Choose a maven. I mean the default maven is selected so let us keep it as it is. Next select the JDK version. So here. As of now in my case the JDK version is 23. So here I am going to select the open JDK 23. Well if you want to download the JDK latest version JDK, then you can go ahead and click on this download JDK. I have already downloaded it. So here I am going to select open JDK 23. Next click on create button over here and here. Either you can open the new window or this current window. So here I'm going to select this window okay. Now we have created Java Functional Programming Project. Next let us go ahead and create a new package over here. So go to Base Package. Right click on it new and then choose package. Let us view package name as Lambda. All right. Next within a Lambda package let's create a new class. Let us call it as Lambda example okay. Perfect. Well the main use of lambda expression is to provide implementation for functional interface. Right. So let us first define the functional interface. And then we will see how to use lambda expression to implement that functional interface. And again the functional interface is nothing but a interface that contains exactly one abstract method. So here just type interface. Let us go. Interface name as shape. And within this interface let us define a abstract method. Wide draw. Well here we don't have to specify the abstract keyword over here. By default the interface methods are abstract. So we can remove the abstract keyword from this method. Now we have a save as a functional interface with single abstract method. And within this abstract method, we can also define any number of static and default methods. For example default wide m one. So this is the default method. And within default method we can put the System.out.println and we can simply print some string. Next here static wide m two. This is the static method. And within a static method we can put some System.out.println. Print some string. All right so functional interface is nothing but a interface that contains a single abstract method and any number of static and default methods. All right. Next, just remove these two methods. We are going to keep only the abstract method over here. Now we have a functional interface ready. Next let us go ahead and let us see how to use lambda expression to implement this functional interface. Well in order to understand the lambda expressions properly, let us first see the traditional way like before Java eight how we are implementing the interfaces. So basically we create a class. That class implements the interface and override the interface methods and provide the implementation. Okay. So here let us first see the traditional way like without lambda expression. So here just create a class called rectangle. And this class implements shape interface. Next override the draw method and provide the implementation. So just mouse over on this rectangle class click on this implement methods. Select the method that you want to implement. Click on okay. Next, within a draw method, let's put the System.out.println statement and just print something like rectangle drawing. Something like that. All right. This is how traditionally we create a class that implements the interface. Next let us provide multiple implementation for this safe interface. So here just create a class. Let's give class name as square. And this class implements safe interface and then moves around this class. Click on Implement Methods and click on okay. Next within a draw method just put the System.out.println statement and just print some string like square drawing. Or let us say square is drawing. And here rectangle is drawing Okay. Next, let us create one more class. Let's call it as circle. And this class implements shape interface and override the method and implement the method. Just put the System.out.println and just say circle is drawing. Next go to public class over here. And here let's create a main method within that. Let's create a object of rectangle class. So here let us use interface as a reference type. Let us use reference variable name as rectangle equal to new rectangle. And let us call the draw method of rectangle object. And let us run the program over here. This should print rectangle is drawing. Similarly, let us go ahead and let us create a object of square. So just use save interface as reference type and let us give the reference variable name as square. And the new square and then call draw method square dot draw. Similarly, let us create object of circle class. So here just use shape interface as a reference type. Let us give reference variable name as circle and then new circle and then call circle dot draw method. Perfect. Next let us run the program and you can see here rectangle is drawing. Square is drawing. Circle is drawing. All right. So this is how you know we create a class that implements interface. And in order to call the methods we create object of that class. And we call the you know, method of that object. All right. So if you can look at this example, this example demonstrates the Hoops concept that is method overriding okay. Now let us go ahead and let us see how we can use a lambda expression to convert this object oriented programming into functional programming. And if you can notice here again, we have written a lot of code over here. Okay. So we use lambda expression to write a shorter and concise, you know, code that simplifies a development greatly. Okay. So basically we use lambda expression to reduce a lot of boilerplate code over here. All right. So let us go ahead and let us see how we can use lambda expression to convert this object oriented programming to functional programming and reduce the boilerplate code. So this is the traditional way. So let us comment out this code. And here let me write the comment using lambda expressions. All right. So as we know that we can assign a lambda expression to a reference variable of type functional interface. So just type functional interface over here save. And let's give a reference variable name as rectangle. And then here let us construct the lambda expression and assign that lambda expression to this functional interface variable rectangle. So here this is the lambda syntax. Okay. And here we need to pass the parameters. And if you can notice the draw method there are no parameters. So we don't have to pass the parameter to the list over here. And then this is the lambda symbol that link the parameter into lambda body. Next within the lambda body. Just put the system dot print line statement and just say rectangle is drawing. Okay. Finally let's put the semicolon at the end. So this is the lambda expression that provides the implementation for this safe interface of type rectangle okay. Next in order to execute this we have to call the draw method. So rectangle and then draw. Next, let us run the program and you can see the output rectangle is drawing. Okay. So this is how we can use the lambda expression to implement the safe interface. Next lambda expression body contains a single statement. Well whenever the lambda body contains a single statement then we can ignore the open and curly braces as well. So let us simplify this lambda expression and remove the open and close curly braces over here okay. So this is your simplified version of lambda expression. And if you can compare this lambda expression code with a traditional way. So you can see in a traditional way, we need to create a class that implements the functional interface. And we need to override the method. And we need to provide the implementation. We need to write this much of code. And again, in order to execute this code we need to create object of that class. And we need to call the method okay. And if you can again notice the lambda expression within the two lines of code. We have done the job of this much of up code. Okay. We have reduced a lot of boilerplate code, isn't it? So here, if I run the program, you can see here rectangle is drawing. So this is what printed over here. Now let me show you my personal tip. Like how I convert a existing function into a lambda expression. So what I basically do I identify a class that implements the interface with a single method. And I convert that single method into lambda expression. For example, let us take this square class over here. So just copy this method from square class and go to main method. And here just paste it okay. And here just get rid of this override annotation because we are writing the functional programming not object oriented programming. So just remove this override annotation. Next, as we know that we can assign a lambda expression to a reference variable of type functional interface. So here just type safe. And let us give The, you know, reference variable name as square. Next here let us remove the unnecessary parts from this function. So here public. So public is a access modifier that belongs to a oops concept not a lambda expression. Just remove this public from here. Next return type white. Well Java compiler smart enough to infer the return type of the function. So we can also remove the return type. Well if you go to the functional interface method draw, it has a return type white right. So Java compiler will automatically infer the return type of the function. So we do not have to explicitly specify the return type of the function. Okay Java compiler will automatically detect that. So just remove this return type from here. And again lambda expression is an anonymous function. It means it does not have a name. So let us remove this function name as well. Next the lambda syntax basically contains three parts. Parameters followed by the lambda token followed by lambda body. Right. So this is how the lambda expression looks like. Next. Finally just put the semicolon at the end. All right. And again you can notice here the lambda body contains a single statement. So we can remove the open and curly close braces over here. Next just call square and then it has a draw method. Okay. Next let us run the program. And you can see the lambda expression is working as expected. So this is how I personally convert a method into a lambda expression. It's pretty simple right. So in order to have a hands on experience let us convert this circle draw method into a lambda expression. So just copy this method and go to main method. Within that just paste it. Get rid of this override method and then assign this lambda expression to a reference variable of type functional interface. Let us give the reference variable name as circle and get rid of this public. Because public keyword is belongs to oops concept. Like we can use public with method and class but not with lambda expression and compiler will automatically detect the return type of the method. So get rid of this return type and lambda expression don't have a name, so just remove the name and just link the parameter into lambda body by using this arrow token or arrow symbol. That's it. Next Lambda body contains single statement. Just remove this open and close curly braces. Perfect. Next let us call this lambda expression for that circle dot draw. Perfect. Next let us run the program and you can see the output over here. Circle is drawing okay. So this is how we can use a lambda expression to simplify the development greatly. So if you can again notice the the code that we have written using hoops concept. So we have written a lot of code over here. Right. But using lambda expression, if you can see in a few lines of code we have achieved the, you know, job of this much of code. Now let me recap the important key points about lambda expression. Well, lambda expressions were introduced in Java eight. And lambda expression is an anonymous function which means it does not have any name. And we can use lambda expression to implement the functional interface. And lambda expression facilitates functional programming. Well, if you can notice the example again how we have converted a object oriented programming into functional programming using lambda expression. So this lambda expression does not belong to any class or object. So this lambda expression is basically isolation to the class or object. All right. In next lecture we will create one more lambda expression to understand in depth. All right. Great I will see you in the next lecture.