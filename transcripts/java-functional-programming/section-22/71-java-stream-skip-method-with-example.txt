Hi everyone. Welcome back. In this lecture we are going to discuss stream provided skip method. The skip method in Java streams is used to ignore the first n elements in a stream and work with the rest. For example, consider we have a stream of 100 elements and we want to skip the first ten elements. Then we can use this skip method and we can pass the number ten. So this skip method basically skip the first ten elements from a stream. Next, the skip method is a intermediate operation in Java streams, which returns a new stream containing the remaining elements of a input stream after skipping the first n elements. For example, let us say we have a stream of 100 elements and we want to skip the first ten elements from a stream. Then we can use this skip method to ignore the first ten elements from a stream. So this stream is a intermediate operation, and returns a new stream that contains the remaining element of a input stream after skipping the first ten elements. Next, the skip method is particularly useful for a task like pagination, or when you only need to focus on a specific section of data. Next, let us understand more about skip method with this diagram. Well, in order to create a stream, we need to have a source. A source can be a list, set, map or array. Once we create a stream from a source. Next we can call its skip method. So this skip method is used to skip the first n elements from a given stream. So this skip method basically takes n as a parameter. So if you want to skip the first ten elements from a stream, then you can pass the number ten. Okay. So just remember this skip method is used to skip the first n elements from a given stream. And this skip method is an intermediate operation. So it returns a new stream that contains the elements after skipping the first n elements from a stream. Next, this skip method is an intermediate operation, so it won't get executed immediately. Once the terminal operation will get called, then this skip method will get triggered. Okay, so this is how the skip method looks in a nutshell. Next, let us understand more about skip method with example. So let's go to our project idea and here go to the streams package. Right click on it new and then choose package. Let us give package name as Skip. Next within a skip package. Let's create a new class. Let us give class name as stream skip example. All right. Next within this class let's create a main method. Within a main method let's create a list of numbers. So here just type list and then pass integer as a type. And let us give the variable name as numbers. And then arrays dot as a list. Perfect. Next what we need to do is we need to skip the first five numbers from the list. And we need to get the remaining elements. All right. So let us see how to do it. So here let us first convert this list into steam. So just call the list that is numbers. It has a stream method. So this stream method we use to convert the list into a stream. Next, this stream method returns a stream object so we can call its skip method. And notice here skip method takes n as a parameter of type long and it returns a stream. Okay, so just call the skip method and then pass the number five. So here basically we are going to skip the first five numbers of a stream. So this skip method will basically ignore the first five numbers from a stream. And this skip method basically returns a new stream that contains the remaining elements from 6 to 10. Okay, next, this skip method returns a new stream. So let us convert that new stream into list by using two list method. Next, let us take the result of this two list method into local variable of type list and then pass integer as a type and let us give local variable name as result. Next, let us put the System.out.println statement and then print the result. Next, let us run the program and here you can see the output. 6789 ten. Okay, so this is how we can use skip method to skip the first n elements from a stream. So in this example we have skipped the first five numbers from the stream by using this skip method. Next, let us see how to use this skip method along with a limit method. So as we know that we use a limit method to limit the first n elements of a stream, right? For example, here just call the limit method and then pass the number three. So this limit method basically limit the first three numbers from a stream. Okay. So this skip method. Basically skip the first five elements from a stream and returns the remaining elements. So here let me write the comment like it will return the stream. It contains the elements six, seven, eight, nine, ten. Okay, next here we are using limit method to limit the first three numbers from a stream. So this is the new stream. And we are calling limit method on this stream. So this limit method will basically, you know limit these three numbers from a stream. And it will ignore these two numbers okay. So this limit method basically returns a new stream that contains first three numbers. That is 678. And finally we have converted that new stream into list by using two list method. Okay. Next let us run the program and you can see the output 678. So this is what you can see over here. So this is how we can use skip and limit method. All right. Just remember we use a skip method to skip or ignore the first n elements of a stream. And we use limit method to limit or restrict the number of elements of a stream. Okay. Perfect.