Hey there everyone! Ramesh here. Back again with another video. In this video, let's go ahead and let's discuss Java Lambda interview questions and answers. Well, as we know that Java Lambda expressions are very important in Java. And basically lambda expressions were introduced in Java eight. And lambda expressions facilitates a functional programming in Java. That's why lambda expressions are very important to discuss in the interviews. Well, in this video let's go and let's discuss few of the interview questions on Java Lambda expressions. Well, in interview you may get a very first question like what is functional interface? Well, basically we use a lambda to implement a functional interface. That is why you may get a very first question like what is functional interface. Well functional interface were introduced in Java eight. All right. And functional interface is nothing but a interface that contains exactly one abstract method is known as functional interface. Well, consider the code snippet here. My functional interface is an interface, and it contains exactly one abstract method that is execute method. Well, as we know that whenever we define a method in a interface, by default that method is abstract, right? So we don't have to again mention the abstract keyword in a method. All right. So just remember a functional interface is an interface that contains exactly one abstract method. All right. Well functional interface can have any number of default and static methods but can contain only one abstract method. We will consider the code snippet again over here. Interface. My functional interface is a functional interface. It contains default and static methods, but it contains exactly one abstract method that is execute method. All right. So this is a valid functional interface. All right. So functional interface is nothing but an interface that contains any number of static and default methods, but contains exactly one abstract method. Well, interviewer may immediately ask you a question like how we can create a functional interface? Well, we can use a Java eight provided add functional interface annotation to mark an interface as a functional interface. Well let's go ahead and let's create one example. Well here I am going to create one interface. Let's call it as my functional interface. Okay. And in order to make this interface as a functional interface we have to define one abstract method. For example let's say wide print something like this. And I am going to pass a parameter something like message. Okay. So this is nothing but our functional interface because functional interface is an interface. It contains exactly one, you know, abstract method. All right. Perfect. Now, in order to mark this interface as a functional interface, we can use at functional interface annotation from Java.lang package. Well, basically this annotation were introduced in Java eight to mark the interface as a functional interface. Well, if you know that this interface is going to be a functional interface, then you can ignore this annotation. All right. Otherwise, if you want to explicitly mark this interface as a functional interface, then you can go ahead and use Add functional Interface annotation. And if you try to add another method, for example let's say wide display something like this, then this will give you a compile time error because you are here using add functional interface annotation to mark this interface as a functional interface. And functional interface is nothing but an interface. It contains exactly one abstract method, right? But here we are trying to add one more abstract method. Okay, we are totally added to abstract method. That's why it is giving a compile. Compile time error here because functional interface contains exactly one abstract method. Right. So let's go and let's remove this method. And now this is a valid functional interface because it contains exactly one abstract method. Well functional interface can contain any number of default and static methods. For instance let's say default void m1 is the method name. And default method contains a body, right? Let me put this out over here. And let's say default method and functional interface can contain any number of static methods. Right. Static void let's say m2. And let's put one out and let's say static method. All right. Perfect. Well by looking into this functional interface you can easily understand that functional interface is an interface. It can contain any number of default and static methods, but it should contain only one abstract method. And this is how we basically define our own functional interface. All right. Great. Well let's take a look into the next question. That is what is lambda expressions. Well lambda expressions were introduced in Java eight. Lambda expression is an anonymous function. It means lambda expression does not have a name, and it does not belong to any class or object. And the main purpose of lambda expression is to implement functional interfaces. All right. So let's understand more about lambda expression with an example. Well let me go to the idea and let me create create a class over here and let's say Lambda example. And here I'm going to quickly create a functional interface. So just remember the main use of lambda expression is to implement the functional interface. So let's go and let's first define the functional interface over here. Let's say the functional interface name is Sep. And let's create one abstract method let's say draw. And here we are not mentioning the abstract keyword, because by default the method is abstract in a interface. And let's first see the traditional way to implement this functional interface. So let's create a class and let's give a class name as rectangle. So rectangle is a shape right. So let's implement shape interface over here. And let's override the method that is draw method. And let's provide the implementation. Well to keep it simple let me put one result over here and let me call it as rectangle draw method. All right. Perfect. So this is how basically we write a code using traditional way to implement a interface. Right. So this is nothing but a method over overriding. Okay. This is basically a polymorphism concept. Now let's go and let's convert this oops concept that is polymorphism concept into functional programming using lambda expression. All right. So within this class let's create a main method. And now let's convert this you know code into a lambda expression. So pretty simple. So let me simply copy this method and paste it here. So this is my personal technique how I convert basically a method into lambda expression. So it is very useful. So just follow this step. So get rid of this annotation. And we basically in Java we can you know assign a block of code to the variable using lambda expression. So here what I'm going to do is I'm going to assign this block of code to the interface called save interface. So save is basically a functional interface right. And we can assign a lambda expression to the functional interface variable. So here what I'm going to do is I'm going to assign this block of code to the functional interface. Now let's go ahead and let's remove the unnecessary parts from this code to form a lambda expression. Well here is a public well public access modifier, which does not make sense with respect to the lambda expression because lambda expression is not belong to any class or object, right? This public access modifier is make sense with respect to a method or instance variable. All right. So get rid of this access modifier. And as we know that lambda expression is an anonymous function. So let's go ahead and let's remove this method name. Because lambda expression is an anonymous function and it don't have any name right. So let's get rid of this name. Well what remains wide so wide is basically a return type isn't it. And in a lambda expression, we don't have to mention the return type, because Java compiler will automatically infer the return type by looking into this method in a, you know, functional interface. Right. So this abstract method has a return type tabloid. So here we don't have to mention the wide in a lambda expression, because Java compiler will automatically infer the return type of this lambda expression. All right. So let's get rid of this return type. Okay. Now in order to complete this lambda expression we have to provide a arrow symbol over here okay. And we need to provide a semicolon at the end of this lambda expression. All right. And this is your lambda expression. It's pretty simple right. So I hope you understood how to convert a method into a lambda expression. And here you can see this lambda expression body contains exactly one, you know statement. So we can also simplify this a bit. So here I'm going to simply remove open and close curly braces. And this is how your lambda expression looks like. And in order to call this you know functional interface we are going to call here draw method. Okay. Now I am going to run this program and let's see how it works. And there we go. Rectangle. Draw. Method. Okay. It means that we have successfully written a lambda expression for this shape functional interface. And we can simply get rid of this code okay. And if you can compare the code over here, we have written only two lines of code to implement this functional interface. But in a traditional way. We need to write this much of code to implement this functional interface. But here lambda expression reduces a lot of code here, isn't it? So this is how basically we use a lambda expression to implement the functional interface. And just remember lambda expression is an anonymous function. It don't have any name and it does not belong to any class or object. The next question is lambda versus method. Well, sometimes the beginner may confuse with a lambda method. So you may get an interview question like what is the difference between lambda and method? Well, method is always belong to a class or object in Java, whereas lambda does not belong to any class or object. All right, so whenever we create a method in Java we should create within a class right. On the other hand lambda does not belong to any class or a method. Isn't it? So Lambda expression should be isolation to any class. And here you can see method contains these main parts. Method contains name, parameter, list, body and return type. Well consider the example over here. Addition method addition is the method name. Int is the return type int a comma, int b is the parameter list and return a plus b is the method body. So this is how we define a method in a class. Now let us see how we can define the lambda expression. Well Lambda expression contains these main parts. Lambda expression basically don't have name because lambda expression is anonymous function. So we no need to mention a name in a lambda expression, and lambda expression can have a parameter list. Lambda expression has a body, and we don't have to mention the return type in a lambda expression, because Java compiler will automatically infer the return type of the lambda expression by checking into the code. For example, look at here. This is the lambda expression. It contains the parameter list and it contains the body. And we don't have to mention the return type. And we don't have to mention the name for the lambda expression. Okay. So this is how the lambda expression looks like. We'll try to give an example. Whenever you get a question like what is the difference between lambda and method? Well let's take a look into the next question. Explain lambda expression syntax. Well this question may be, you know, especially asked for the beginners because interviewer just want to check whether a, you know, candidate is familiar with the lambda expression syntax or not. Okay. Just try to, you know, draw. I mean, try to write this syntax in a paper. If you have paper and pen in your hand and try to explain this syntax. Okay. So this syntax index basically contains three parts. First part is lambda input parameter, second part is arrow, third part is lambda body. Well, the first part contain lambda input parameters and second part is arrow symbol. So this arrow symbol basically link the input parameters with the lambda body. And within a lambda body we can define single statement or multiple statements okay. So this is how the lambda expression syntax looks like. Well let's take a look into the next question. Why to use lambda expression. Or what is the benefit of using lambda expression? Well, as we know that lambda expressions were introduced in Java eight and lambda expression facilitates the functional programming in Java. For example, if you can see the code snippet here. So this is how we use the oops concept to write the code. For example, here we have used a method overriding. It means it is a polymorphism concept right. So we have created a class which implements shape interface. And we have overridden the method and we have provided the implementation. So this is how we write the code using oops concept. And here we have converted this oops concept into a functional programming using lambda expression. And if you can see using lambda expression we have simplified our code a lot. Right. You know only two lines of code. We have done the job of all this much of code isn't it. So basically lambda expression facilitates functional programming in Java. So next is to provide the implementation of Java eight functional interfaces. Well the main use of lambda expression is to implement the functional interfaces. For example I can consider here Sep is the functional interface right. Because it contains only one abstract method. And here we have used lambda expression to implement this functional interface. Okay. Well next is reduce lines of code. Well consider the same example over here. By using traditional way or OOP concept. We need to write this much of code to implement the interface, and by using lambda expression, we need to write only two lines of code. Right. So this is how basically lambda expression reduces the code. Next is passing behavior to the methods. Well we can use a lambda expression to pass a behavior to the method. For example here I will create one more method something like private static void and let's say method name something like print okay. And I want to pass a functional interface as a parameter over here and here simply you know call the shape dot draw method okay. Now let's see how to pass a lambda expression as a behavior or a parameter to the to this method. Okay. So here what I'm going to do is I am going to remove this method call. And here I'm going to call this method that is print method. And I'm going to pass a lambda expression for this method. All right. So this is how basically we pass a lambda expression as a method parameter. All right. So here we are assigning this lambda expression to this variable. And we are passing this variable as a method parameter to this method. And if we run this program it should work. There we go. Rectangle draw method. All right. And here instead of assigning this lambda expression to this variable, we can directly pass this lambda expression as a behavior to this method. Okay. So this is how we write the lambda expression to simplify the things. Well the next question is write a Java lambda expression to create a thread. Well basically runnable interface in Java is a functional interface. So we can use a lambda expression to implement a runnable functional interface. And then we can create a thread. And then we can pass the lambda expression to the thread. All right. So let me create an example and let me demonstrate how we can, you know, write a lambda expression to create a thread. Let me head over to the English idiom. We'll go to the package, right click on the package new and then choose Java Class. And let's give a class name as something like Runnable Lambda example and hit enter. Now first we will see how to write a code using traditional way. And then later we will see how to write the same code using lambda expression. So here we will create a class which implements runnable interface. Let's say thread demo class which implements runnable interface and then simply override the run method. Over here just mouse over, click on implement method, choose a run method and there we go. And to keep it simple within the run method, we will put this out and just print run method called something like this. Okay, now go to the runnable Lambda example class and then create a main method. And within a main method we are going to create an instance of thread class. So let's create object of thread here. And then pass object of thread demo okay. And then simply start the thread here okay. So this is how traditionally we create a thread object okay. We create a class implements runnable interface and then override the run method. And we provide the implementation for run method. And we create a thread instance here. And we pass the object of thread demo class. And then we start the thread okay. So this is how we create the thread using traditional way. Now let's go and let us see how to use a lambda expression to implement runnable interface. And then we will create a thread object. Okay. well, if we can go inside runnable interface source code, you can able to see runnable is a functional interface because runnable contains only one abstract method that is run method. And if you can see runnable interface is annotated with add functional interface annotation. It means runnable is a functional interface. And we can use a lambda expression to implement a runnable interface. All right. So let's go back to our class. And here we're going to use a lambda expression to implement a runnable interface. Let's use runnable functional interface reference variable to assign the lambda expression. So this is the syntax of the lambda expression right. And simply put this out within a lambda expression body and just call it as run method called using lambda something like this. All right. And then we need to create an instance of thread over here. Thread. Thread let's say lambda equal to new thread and then pass runnable and then simply start the thread. Perfect. So this is how basically we implement a runnable functional interface using lambda expression. And then we create a thread object. And then we pass a lambda expression implementation as a parameter to this constructor. And then we start the thread. Okay. So let me run the program and let me show you. And there we go. So this is working as expected. Well we can simplify this lambda expression even more if you can see here lambda expression body contains only one statement. So we can get rid of this. Open and close curly braces over here. All right. Perfect. And if you can see here, we can simply pass this lambda expression itself as a, you know, a parameter to the constructor. For instance, I'm going to pass here and I'm going to get rid of this statement okay. So this is how we can pass a lambda expression as a parameter to the constructor as well. If I run the program it should work. And there we go. All right. So this is how basically we, you know, use a lambda expression to implement a runnable interface. And we pass that lambda expression to the thread object. And then we start the thread. All right I hope you understood how to use a lambda expression to implement a, you know, runnable interface, and how to create a thread and how to pass a lambda expression. All right. Great. All right, guys, these are the few, you know, basic interview questions on lambda expression. So if you are a beginner then you should check out these interview questions on lambda expression. All right. Great I will see you in the next video.