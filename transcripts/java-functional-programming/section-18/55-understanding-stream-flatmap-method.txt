Hi everyone. Welcome back. In this lecture let us discuss flatMap method in a stream. Well, map method does only mapping, but flatMap performs both mapping as well as flattening. Well. Here flattening means transforming data from stream of stream into a single stream. So this is the main difference between map and flatMap. Well, map does only mapping like it. Map one stream into another stream, but flatMap performs both mapping as well as flattening. So flatMap basically performs a flattening like a transform the data from nested stream into single stream. So here nested stream means stream of stream. Okay. Well whenever you have a requirement to transform the data from nested stream into a single stream, then you can go ahead and use flatMap method. And flatMap method does both mapping as well as flattening. So we will see you know how flatMap does both mapping as well as flattening in upcoming slides. The flatMap method is a intermediate operation in Java Stream. So as flatMap is an intermediate operation, it returns a new stream so that we can change the method calls. For example, here list of list is the source and we can create a stream from this list of list using stream method and stream method. Returns a stream so we can call the flatMap method on that stream. And again flatMap method returns a new stream. We can call the collect method on that new stream. So this is how we can change the method calls okay. So flatMap is a intermediate operation. So it returns a new stream. And we can call the other methods on a new stream like collect method. Next the flatMap method is commonly used to convert or flatten nested data structures such as list of lists, array of arrays, and stream of streams into a single unified stream. So basically we use flatMap method to convert a nested data structures into a single level data structure. Next, let us understand more about flatMap method with this diagram. In order to create a stream, we should have a source. A source can be a list of lists, an array of arrays, stream up streams. So in order to use a flatMap method, we should have a nested data structures as a source, right? So consider we have a list of lists or array of arrays or stream of streams as a source. Next we can create a stream from this source. Once we create a stream object from a source, then we can call its flatMap method for mapping as well as flattening. Next, the flatMap method is a intermediate operation, so it returns a new stream so that we can, you know, change the method calls. For example, flatMap method returns a new stream. So we can call the collect method to collect the result of this flatMap method. Okay. And again the streams are lazy. It means the intermediate operations do not get executed immediately. Once we call the terminal operation, then the intermediate operations will be triggered. Okay. Next, let us understand flatMap method with example. Consider we have a source like list of list. least. So here you can see we have a list. Within that we have a three list objects. Okay. Next we can create a stream from this source. So once we create a stream from this source you can see the stream looks like this stream. Within that we have a three list objects okay. Next we can call this flatMap method on this stream. So this flatMap method does both mapping as well as planning. For example this flatMap method map or convert each list object into corresponding stream. And next it does the flattening. Flattening means it merge all the streams into a single stream. Okay, so basically flatMap method map each list into stream, and then it will flatten the stream of streams into a single stream. So this is how the flatMap method does both mapping as well as flattening okay. And finally we can collect the result of this flatMap method using terminal operation like collect. All right. So this is how the flatMap method works.