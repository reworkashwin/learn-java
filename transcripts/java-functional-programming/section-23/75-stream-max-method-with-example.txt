Hi everyone. Welcome back. In this lecture let us take a look into stream provided max method. Well the max method is used to find the maximum element in a stream based on the provided comparator. Well, we can use max method to find the maximum or the largest element in a stream based on the comparator. Well, the max method basically expect comparator as a parameter. Okay, so max method uses comparator to compare the elements and find the maximum element from the stream. Next, the max method is a terminal operation, so the stream cannot be used after this max method has been executed well. The max method is a terminal operation, so once you call the max method on a stream, then stream pipeline ends and you cannot use that stream again. Okay, next, let us understand more about max method. With this diagram, we create a stream from a source. A source can be a list, set, map or array. Once we create a stream from a source, next we can call the intermediate operations. The intermediate operations can be a filter, map sorted, etc. and the intermediate operations basically returns a new stream so we can call the terminal operation like max method. Well, we use max method to find the largest or the maximum element in a stream based on the comparator that we pass in. And the max method is a terminal operation. So once we call the max method on a stream, the stream pipeline ends and we cannot use that stream again. Next, let us understand more about max method with example. So let us go to our project in IntelliJ Idea over here and go to the streams package. Right click on it new and then choose package. Let us give a package name as Max. Next within a max package, create a new class. Let us give class name as stream max example. Next, within this class let's create a main method. Well, in this example we are going to basically find the largest element or a maximum element from a list. So here let us first define the list. So just type list. List is a generic. So let us first integer as a type. Let us give the reference variable name as numbers and then let us use arrays. Arrays dot as the list method to create a list and then pass the elements one comma five comma four comma three comma 2978 comma six. Now we have a list of integers. Next, let us find the maximum element from this list by using steam provided max method. So here let us first convert this list into steam. For that just call the steam method on this list. So this steam method basically creates a stream from this list. Next, this stream method returns a stream object so we can call the max method. And notice here max method takes a comparator as a parameter. And this comparator is basically a functional interface. And we can use the lambda expression to implement this comparator interface. And again notice here max method returns optional of type integer. So just call the max method and then pass the lambda expression that implements comparator functional interface. So here just pass object one comma object two as a parameter to the lambda expression, followed by lambda symbol, and within a lambda body just call object one it has compared to method and then pass object two. Well, this object one and object two. So these are the objects of integer wrapper class okay. And this integer wrapper class has a compare to method. So this compareto method basically returns a positive one if the object one is greater than object two compared to method returns a negative one if the object one is less than object two. Compare to method returns zero if object one is equal to object two. Okay, so this is how this compare to method works. All right. And this max method basically uses this lambda expression to compare the elements and find the maximum element okay. Next this max method returns optional. So we can call the get method over here to get the maximum element. Next let us take the result of this. Get into a local variable of type int and let us give variable name as max. Next, let us put the System.out.println statement and then pass max. Next, let us run the program over here. And here you can see the output nine. And in a list you can see nine is the largest element. That is what got printed over here. So this is how we can use max method to find the largest element in a list. Okay. And this max method basically uses this comparator implementation to compare the elements and find the maximum element. Well notice here this lambda expression is just to call the compare to method. So whenever we have a lambda expression just to call the method then we can replace that lambda expression with method reference. Right. So here just replace this lambda expression with method reference. Even though the IntelliJ Idea will give the hint like replace this lambda with method reference, just click click on this link okay. That's it. Next, if you run the program again this will produce the same output. So this is how we can use max method to find the maximum or the largest element in a list. Okay. Perfect.