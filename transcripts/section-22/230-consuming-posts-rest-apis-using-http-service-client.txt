Let's try to build one more interface by using HTTP Service Client approach. This time, I want to invoke POST related REST APIs. So this is the base path. By using this as a base path for our REST APIs, we should be able to invoke various REST API operations around the POST. Since you already aware about all these details, please pause this video and try to build an interface very similar to this Todo service, followed by you can build a controller and towards the end, you can make the Spring security configuration changes as well. Give a shot and come back. I hope you're successful. Otherwise, don't worry, let's try to do this together. As a very first step, I need to create a dto. Let me create a class with the name PostDto. Inside this record, I'm defining the fields UserId, Id, Title and body. Now very similar to Todo service, I'm going to create one more interface. I can copy these and paste it again. This time, I will call this as PostService. For this service class, the base path will be post, not todos. Next, I need to use the same @GetExchange instead of list of TodoDto, this time we're going to GET list of PostDto. Let me replace all TodoDto with the PostDto. So other than that, I think we are good. We don't have to make any other changes. Do you see how easy it is to build a logic by using HTTP Service Client approach? Now I need to build a controller class. Let me copy TodoController and paste it again. I'm going to call this as PostController. Into this controller, I'm going to inject PostService as a dependency. Using the same, I need to invoke all the abstract methods that we have defined. So the return type should be PostDto, I need to use this in all the places. Here instead of TodoService, I need to use PostService, and coming to the @PostMapping method, here we need to use PostDto. The same applies here, here I'm getting a compilation error. Seems I made a mistake. I need to use PostService. Now coming to the @PutMapping, here also I need to use PostService instead of TodoDto, I need to use PostDto. The return type also is of type PostDto, at last for the @DeleteMapping method, we need to make very similar change, which is how to use PostService. And here the statement should be Post deleted successfully. I hope there are no Todos. Let me search for TOdO. There are no Todos, I can get rid of these import statements. Let me do the build, once the build is completed, I will go to the Paths config. Just like how we configured todos path, here we should also configure POST path as well. So that the POST related REST APIs also can be invoked without any security. I am going to test these new REST APIs from the Postman so that we can confirm everything is working fine without any issues. Under the client, there is a POSTS folder. First let me invoke this GetAllPostRequest. I got an error. Seems we forgot to make a configuration change. Inside the HTTPServiceClientConfig class, just like how we configure TodoService, very similarly we need to configure PostService class as well. Unless you provide your interface details to these import HTTP services, the bean of them will not be created and into these bean types, the RestClient bean also should be injected, so that the underlying REST API invocation will work smoothly without any problems. This time, if I invoke this API, I am still getting the error. There is a mistake that I have done inside the PostController, here we are using the same path todos. Let me update this to Post so that there won't be any problems. If I click on the send button, hooray, this time I got a successful response. Let me go to GetPost. So this is going to help me to retrieve a post based upon the ID 1. I can test the CreatePost as well, but for the CreatePost, we need to pass the CSRF token. So let me get a token, this is my token, under the CreatePost request. Before clicking on the send button, I need to make sure to populate the valid CSRF token. Under the body, I need to pass user ID, title and body fields. Let me click on the send button. This is also successful. I'm going to invoke the update REST API as well. But before that, I need to populate the CSRF token, Update operation is also successful. So under the path variables, I need to make sure to pass 1 and inside the request body, I need to pass the entire Post details. Coming to the delete operation, this should also work without any surprises. If I click on the send button, I got a successful response. I hope you are clear on how to consume REST APIs by using the HTTP Service Client approach. There are some advanced use cases as well that we need to be aware around the HTTP Service Client approach. . Let's explore the same in the next lecture.