With the concepts that we have discussed so far, you will be able to develop REST APIs, which can be invoked by other developers or other client applications. But apart from producing or developing the REST API, sometimes in real enterprise applications, you will end up in a scenario where you have to consume or invoke other applications REST APIs. Let's imagine there is a third party application, which you want to integrate into your backend application. So the communication between your application and the third party application, it is going to happen over HTTP protocol using REST API. This time you are sitting on the client side or on the consumer side. So whoever is invoking the REST APIs, we call them as consumers and whoever is exposing the REST APIs to the other client applications, we call them as producers. So far inside this course, we are only on the side of producing the REST APIs, but for the very first time, we are also going to explore on how to play a role of consuming the REST APIs as well, because there is a clear difference on the approaches on how we produce the REST APIs and on how we consume the REST APIs. Since I don't have any third party REST API to invoke inside my job portal, what I'll do is, I will take some fake REST API that is available inside the internet. One such famous option is JSON Placeholder. Once you navigate to this website, jsonplaceholder.typecode.com, here there are enough details provided about the fake REST APIs that are being exposed by this website. So here they have given a URL for one of the REST API. Let me copy the same and open in a new tab. If I press enter, this is going to give me a fake response, something like this. Inside the response, I have an userId, Id, what is the title of the post and the body of the post. If I remove this one, and if I invoke Posts API, it is going to give me all the list of posts that are being maintained by this fake REST API. So very similarly, we can use many other REST API paths. For example, there is a fake REST API path to create a resource. As you can see, by sending our request to this path, by using HTTP post method, we should be able to create or save a post against a given user. Off course, this is all the fake REST APIs, whatever you are providing, they are not going to be stored anywhere, but you're going to get that feel of invoking a REST API. Once I request this process, these fake REST APIs, they will also provide you the fake response. Similarly, there is a process to update a existing resource by using the PUT method. We have PATCH REST API as well, where we can use the PATCH method and towards the end, we have a DELETE resource REST API as well, using which we can delete a post by using the DELETE HTTP method. When you are using DELETE as well, no resource is going to be deleted on the server side. They're just going to give you some fake response. As you can see, POST is one of the REST API path. On top of that, we can perform various operations. Very similarly, this website, it also supports other paths as well. For example, there is an albums related path, users related path and todos related path. Let me take one of these base URL. This time, instead of POST, I can mention todos, which is going to give me all the list of todos. Under each of the todo object, we have a user ID to whom that todo belongs to, what is the title of the todo, and there is a boolean confirming whether that todo task is completed or not. So what I'm going to do is, throughout this section, I will try to invoke these POST and TODOS related REST APIs. In the same process, I will explain you how to make the code changes when you are acting as a consumer or when you are trying to invoke other third-party REST APIs. To get started with the consuming of the REST APIs, you need to look for the dependency with the name HTTP client. And when you add this starter project inside your pom.xml, this starter project, it is going to give you a couple of options to invoke the REST APIs. The first one is, ReatClient and the other one is RestTemplate. These HTTP client starter projects you need to add when you are building traditional web applications, which are going to process the request synchronously by using servlet tech stack. Inside Spring ecosystem, we also have reactive tech stack as well. By using these reactive, we should be able to build the reactive web applications as well. Throughout this course, we are only building a traditional web application where we are trying to process the request synchronously. But anytime if you are into a scenario where you have to build some asynchronous applications by using reactive tech stack, then you need to consider adding these reactive HTTP client starter project. . So this starter project, it is going to provide you an API with the name webClient. By using this WebClient, you can invoke other party REST APIs asynchronously. When I say asynchronously, you're not going to wait for the response to come. Instead, you will continue with the other logic that you have inside your business code. Reactive stack is out of the scope of this course. That's why I'm not going to focus on the WebClient. If you're ever into a situation where you have to build some asynchronous REST APIs, please do consider enrolling into the course where the tutor is going to talk about Spring reactive tech stack. For our web application, we need to select these HTTP client starter project, followed by we can click on the explore button. This is the dependency that we need to add into our pom.xml. Here just after my web MVC dependency, I'm trying to add the new dependency. The starter project name is spring-boot-starter-rest-client. Let me sync the maven changes, which is going to take care of downloading all the libraries under the starter project. As I said before, this starter project, it is going to give you a couple of options to invoke the REST APIs. The first one is, by using the RestTemplate, but I don't recommend you using this RestTemplate, in fact, I don't even also recommend to learn about this RestTemplate, because this RestTemplate is deprecated right now inside the Spring ecosystem. And in the future versions, the Spring team, they are planning to delete everything around the RestTemplate API. Instead, the Spring team is recommending to use the RestClient. Here there is also a note, as of 6.1, RestClient offers a more modern API for synchronous HTTP access. For asynchronous and streaming scenarios, we need to consider WebClient, which is going to come as part of the reactive starter project. Both the RestTemplate and RestClient share the same infrastructure. So many improvements made therein are shared as well. However, RestClient is a focus for new higher level features. The Spring team itself, they are telling don't learn and don't use the RestTemplate because they are planning to delete this. That's why inside this course, we're going to explore RestClient. So RestClient is a interface by using this interface and its associated API classes, we should be able to invoke other third-party REST APIs, by using HTTP synchronous pattern. From the next lecture, let's start making some code changes to invoke the REST APIs by using this RestClient.