To test all the code that we have written in the previous lecture, I need some logic invoking these methods. Off course, I can invoke these methods from some random place inside my backend application. For example, I can go to any service layer or controller layer methods, and there I can try to invoke these methods, but that is not a good approach. That's why what I'll do is, I will develop separate REST APIs, which can be invoked by us through Postman, because integrating this todo related logic into the job portal controller or service layer is not a good recommendation. Instead, we are going to have separate independent logic, which can be triggered through Postman. And from these new REST APIs, we are going to invoke the service layer method. And from these methods, we are going to invoke the third party REST APIs. To test a REST API invocation logic, we are building extra layer of REST API, but in real enterprise applications, it is not going to be the scenario because these service methods are going to invoke based upon user actions. I hope you are clear. Let me create a new package with the name client.controller, and inside this controller package, I'm going to create a new Java class with the name TodoController on top of this class, let me mention @RestController followed by @RequiredArgsConstructor. Next I'm also going to mention @RequestMapping annotation. To this annotation, I'm providing the path as todos. Let me inject the restClientTodoService as a dependency into this controller class, so that we can invoke the methods inside the service class. Here I'm going to paste few lines of code. You can get this code from the GitHub repo as well. Using all these methods, I'm going to expose REST APIs. I'm not following any versioning standards or other REST API standards that we have discussed through this course, because I just wanted to quickly develop something to test our code changes. So using the very first method, we are exposing this path by using the get() method. Whenever someone invoked this path, by using HTTP GET() method, this findAll() method is going to be invoked from this method, I'm trying to invoke the findAll() method present inside the restClientTodoService. So this method is going to take care of actually invoking the third party REST API and fetching all the list of todos. The same it is going to return. And whatever we are returning, the same we are trying to send back to the client application from this controller class. The next REST API method is going to help us to fetch a todo record based upon the ID. So whoever is invoking this REST API, they need to pass the ID by using this path variable. Inside this method, we are invoking the findById() method that we developed inside the service layer. So this way, each of these methods, they're going to invoke the equivalent methods that we have written inside the restClientTodoService. So this method, which is using @PostMapping, it is going to invoke the create() method, whereas the @PutMapping method is going to invoke the update() method and @DeleteMapping REST API method is going to invoke the delete() method. So now what I can do is, from the postman, I can try to invoke these REST APIs and these REST APIs will take care of invoking the logic that we have written inside the service class. So let me do the build, once a build is completed, I'll try to start my application. Once the application is started, we can start testing. But before that, we need to make sure to configure permit all for these todo's path. Let me go to PathsConfig and here under the public paths method, what I'll do is, I will try to configure a new path. The path is, forward slash API followed by todo's. After todo's, I'm going to mention two asterisk symbols. So any subpaths along with the todo's path, I want to configure permit all so that we don't have to pass any Spring security related tokens. Let me save these changes do the build, once the build is completed, inside the postman, we're going to have a client folder. Under this client folder, there is a ToDo's subfolder. If you open the same, you're going to have all these requests. By using the very first request, I'm trying to invoke api slash todo's REST API path. As you can see, we got all the list of todo's inside the response. So we are invoking this REST API and this REST API is internally invoking other third-party REST API. So this gives a confirmation to us, whatever RestClient related changes that we have returned, they are working without any problem. Let me test the next REST API path. Here I'm trying to pass the path variable 1, which is going to help me in retrieving a todo record based upon a given ID 1. If I click on the send button, I'm going to get this response. Let me go to the create todo. Here this time, I'm trying to invoke the REST API by using the POST method. Since we are trying to save a brand new record, we need to make sure under the body we're sending this payload. So under the payload, I'm passing the user ID, title and completed as false. We can't straight away invoke this API because this is a POST method, we need to make sure to send the CSRF token. So let me get a new CSRF token. So this is the value. I'm going to paste it here under the headers, now we can click on the send button, which is going to give us 201 created status. So there is a new record is created with the ID as 201. But this is all a fake response. There won't be any record saved with the ID 201 on the server side. Let's go to the update todo as well. Here also I need to mention the CSRF token followed by under the body, we need to mention what is the updated data that we want to update. So inside the path, I need to pass the path variable 1. And inside the request body, I will mention the title as Testing and completed as true. If I click on the send button, I'm going to get the successful response saying that a given record is updated with the provided data. But this is all a fake. If you try to fetch the one record again, you're not going to have these updated data. Let me go to the second request, and here, if I click on the send, I'm not getting any updated data here, because whatever we are testing, it is all fake. Let me click on this delete operation here, this time inside the path variable, I'm sending a value 1 and I selected the HTTP method as delete. Under the headers, I will make sure to pass the CSRF token. Let me click on the send button, and here I got a successful response. With this, we successfully tested all the methods that we have written inside this service class, with the logic that we have written, we are able to invoke all the REST APIs without any security. In case if you are wondering on how to pass the security credentials, when we are trying to consume REST API, let me quickly show you the same. So you have an option of sending the credentials directly when you are trying to invoke the REST API or you can configure the security details when you are creating the RestClient bean as well. Here, as you can see, by using this defaultHeader, we are passing some header and header name. In the similar lines, you can try to invoke defaultHeader one more time. This time I'm going to invoke the method, which is going to accept a consumer lambda expression. So to this method, we are going to receive list of HTTP headers. So now using these HTTP headers, I can invoke methods like setBearerAuth. To this method, I can pass the jwt token. In case if I want to pass username and password, there is also a method with the name setBasicAuth. When you are invoking the BasicAuth method, you have various options. You have all these overloaded methods, but using this third method, we can pass the username and password, or if you want, you can directly send the encoded credentials. So based upon your business requirements, you can invoke any of these methods. So just like these BearerAuth and BasicAuth related methods, we have many other methods which you can invoke based upon your requirements. So the same kind of logic you can also write when you are invoking the actual REST API from your service methods. So here, just before invoking the retrive() method, I can invoke the headers() method. Here also, I have an option of providing a lambda expression. So inside this lambda expression body, I can invoke same setBearerAuth or setBasicAuth methods. And these methods are giving me flexibility to send the credentials in the scenarios where I'm trying to invoke a secured REST API. I hope you're clear. Whatever examples that we have discussed, I'm going to share them through slides as well for your quick reference.