There is a advanced concept of grouping, when we are building multiple interfaces by using HTTP Service Client approach. To demo you the same, what I'll do is, I'll try to repeat this @ImportHttpServices annotation one more time. This time, I will try to separate these two interfaces into separate annotations. Once we define multiple @ImportHttpServices, we can give a group name for each of these configurations. I just have to invoke group annotation here. I'm going to give a name as todos for this group name, very similarly, I'm going to invoke group here as well. This time I will mention the group name as Post. In our scenario, I have only one interface around the todos logic, and similarly for Post, I have one interface. But in real enterprise applications, you can imagine that you're trying to invoke multiple third-party REST APIs. And these multiple third-party REST APIs may belong to multiple third parties. Under such scenarios, grouping your interfaces by using these approaches, they are going to be helpful for you. So once you define the group, you can further customize on how the REST API invocation is happening. To further customize on how the RestClient is being used behind the scenes by the framework, what we have to do is, we have to create a bean of type RestClientHttpServiceGroupConfigurer. So let me mention the method name as groupConfigurer() on top of this method, I'm going to mention @Bean annotation. From this method, I'm going to return a lambda expression. This lambda expression, it is going to accept a input variable of type RestClientHttpServiceGroupConfigurer. . So the variable name can be anything. So under this lambda expression, by using these groups, I will invoke the filterByName() method. To this method, I can pass one of the group name like todos followed by I need to invoke the forEachClient() method. Because under a group, we may have multiple clients. As of now, we have a single client. So whatever configurations that we're going to provide for these forEachClient(), they are going to be applicable for all the clients that we have assigned to the group todos. To this forEachClient(), again we have to pass a lambda expression. This time, the lambda expression, it is going to have two input parameters. The first one is group details followed by restClientBuilder. So to this, the parameter name, I'm going to mention it as REST ClientBuilder. If you see, this group variable is of type HTTP Service Group representing todos group. And this parameter restClientBuilder is of type RestClient.Builder using which a bean of type RestClient we can create. So let me use this restClientBuilder, and here I'm going to invoke the baseUrl() method to this baseUrl(), I can pass what is a baseUrl() that needs to be used when any client method is invoked that is assigned to this todos group. So if you go to this Todos service, we have mentioned this baseUrl() already on top of this annotation. But since anyway, we are trying to create the bean of RestClient by ourselves, mentioning this basePath here is completely optional. I'm going to get rid of this by commenting. Let me copy this path and mention it under this baseUrl() method. Next I can invoke the methods like defaultHeaders() or defaultHeader(), requestInterceptor(), anything that we are capable of invoking while creating the bean ourself manually, just like how I invoke to defaultHeader() here. All these kind of capabilities now will be available under the HttpServiceClient scenario as well. By using the defaultHeader(), I can pass a defaultHeader(), which is going to be applicable for all the requests that are going to be invoked by using the interfaces assigned to this group. In case if we are looking to pass some security details, as we discussed previously, which is how to invoke defaultHeaders() method. And to this method, we need to pass the lambda expression here, by using these httpHeaders, I can invoke methods like SetBasicAuth(). It is also possible to invoke the requestInterceptor(). To this method also, we can pass a lambda expression, something like this. This lambda expression, it is going to have request body and execution as method input parameters. So this request represents the whole request that you are trying to send to the third party. In case if you want to intercept that request and add some headers, or if you want to make any modifications, all those changes you can define inside the body of this lambda expression. And once you made required changes to your request and body, by using this execution.Execute() method, you need to forward the request so that the request will be forwarded to the third party REST APIs. Once you are satisfied with all the methods that you want to invoke while defining the bean of restClientBuilder, while you are trying to create the bean of RestClient towards the end, you just have to invoke the build() method. So whatever lambda expression that we are providing for these, forEachClient() method. So this is going to be executed for each of the interface that is assigned to the todos. For all these interfaces, the same baseUrl(), the same requestInterceptor() and same defaultHeaders() will be used. . So far we are done with the changes around one group. We have to repeat the very similar configuration for the second group as well. So the second group name is post. For this, the URL will be post. We can go to the postService and delete the Url here. Now coming to the other changes, I can have very similar requestInterceptor() and this time I can also invoke the defaultHeader() to this defaultHeader(), I can pass something like the name as Accept and header value as application slash json. You need to make very similar changes for as many groups as you defined in the top. I hope you're seeing the power of groups. By using groups, you're going to define separate RestClient beans for each of the group. In future, if someone wants to introduce a new interface that belongs to the same group, they just have to create a interface. They don't have to worry about the baseUrl(), they don't have to worry about requestInterceptor(). They don't have to worry about populating the security details. Everything will be done at a centralized location. We're getting a compilation error. I need to mention semicolon here. Let me save these changes do the build, once the build is completed, I will try to test the same REST APIs one more time. This is the delete REST API of the post. Let me click on the send button, I got a successful response. I'll go to the GetAllToDo's, here if I click on the send, I got a response. If I go to the GetToDo, I got a successful response. This gives a confirmation to us. Our grouping related changes, they are working perfectly without any issues. Always prefer this HttpServiceClient approach. Otherwise you can go with the RestClient approach as well. But don't try to use RestTemplate approach that we have. So this is a legacy approach and soon this is going to be removed in the future versions of the framework.