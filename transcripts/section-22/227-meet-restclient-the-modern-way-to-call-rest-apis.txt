Let's imagine a scenario where from my job portal backend application, I need to invoke these third-party REST API to fetch all the todos. Apart from fetching all todos, I also want to perform other REST API operations as well, like posting a new todoo, updating an existing todo, deleting a todo and fetching a given todo. To perform all these operations, I'm going to build some logic inside our backend project. But before that, let me create the required Dtos for our communication. So there is a dto package here, I'm going to create a new Java class with the name TodoDto. So this class is going to be a record class. Inside this record class, I need to mention the field names. So these field names has to match exactly with the field names that I'm getting inside the response. So inside the response, under each todo object, we're going to get these fields like userId, id, title and completed. . Let me mention those four fields inside my record class. So these are the fields. Now I'll go to the main package and try to create a new sub package with the name client. Now since my backend application is trying to act as a client application to invoke other third-party REST APIs, I'm trying to give this name to this package. All the logic around the REST APIs invocation are consuming the REST APIs, all of that I'm going to define inside this client package. Let me create a new sub package with the name service. Inside this service package, I'm going to create a new Java class with the name RestClientTodoService. On top of this class, I'm going to mention @Service annotation followed by @RequiredArgsConstructor. Into this service class, I need to inject the RestClient as a dependency. That's why I'm trying to create a field of type RestClient with the field name as RestClient itself. But here, if you see, I'm getting an error because there is no bean of type RestClient found inside our backend project. At the end of the day, this RestClient is just an interface. It is our responsibility to create a bean of type RestClient so that that bean will be injected into our service class. While we are trying to create a bean of RestClient, we can provide all the basic details around the third-party REST API, like what is the base URL, what are the default headers that we want to send. What I'll do is, I'll try to write some logic inside the constructor of this class. The constructor is public RegisterRestClientTodoService To this constructor, I need to pass an object of RestClient.Builder because this builder object of RestClient, it is going to help me to build a bean of RestClient. Since I'm going to use constructor injection, I can get rid of this @RequiredArgsConstructor because I want to write my own logic inside this constructor. Let me type this.RestClient is equal to builder.build()Method. So this build() method present inside this RestClient.Builder, it is going to give me a bean of type RestClient. Apart from this simple logic, we can also provide some other details while we are trying to create a RestClient bean. So by using this builder() method, if you try to see the list of methods available, there is a method with the name baseUrl(). To this baseUrl, I will pass the base path of my third-party REST API, so that this baseUrl(), I don't have to repeat inside my business logic whenever I'm trying to invoke a third-party REST API. So in this scenario, what is the base path? Ignoring this todo's path, remaining all the prefix path is the base path. I'm mentioning the same here. Apart from providing the baseUrl(), we can also provide various other details like defaultHeader, defaultCookie. . For now, I'll try to provide a defaultHeader. The header name will be accept and the header value will be application slash json. I'm telling to the third-party vendor that I'm going to accept only the json as a response. This way we can define many other details like we can define a request interceptor which is going to intercept each and every request, and inside this interceptor, we can write some logic as well. So there is a method which is related to the API version inserter, feel free to explore these methods and use them inside your real enterprise applications based upon your business requirements. For now, I'm fine with this baseUrl and defaultHeader. With this code, we know a RestClient bin is going to be created. Wherever we're trying to use this RestClient bean, by default, the request will be forwarded to this baseUrl path and a header will be added to the request with the name Accept and with this value. . Now using this RestClient bean, I want to perform various REST API operations like GET,POST,PUT,DELETE. Here inside this class, I'm going to paste few methods. You can get these methods code from the GitHub repo. Let's try to understand the new code that I have pasted. First, I have created a constant with the name TODOS_API. Under this constant, I have mentioned the REST API path that is specific to the todos. So after this base path, this todos is going to be appended. So since there is a forward slash here, I can get rid of these forward slash. Now let's try to understand what do I have inside each of these methods. So there is a findAll() method, which is going to fetch all the todos from the third-party REST API. Since find operation is a read operation, we need to invoke the get() method by using the RestClient. . When we are invoking this get() method behind the scenes, the request will be forwarded by using HTTP GET() method, followed by I need to invoke the URI, and under this URI, I'm mentioning the URI path that I want to append to the base path. So this is the path which I'm mentioning under this constant, that's why I have mentioned the constant name here. This way you can add many other details. Like if you want, you can add some headers, you can also add some cookies. Once you populated all the requested details, you need to invoke the retrieve() method. So this is the method which is responsible to actually make a REST API call to the REST API path that you have configured. Once the API invocation is completed, we want to understand what is the status that we have received inside the response. For the same, we need to invoke this onStatus() method on top of the retrieve(). Here to this onStatus() method, I have passed a lambda expression, which is going to implement predicate functional interface. So here, first I'm checking whatever HTTP status code that I have received, is it related to the error? If yes, I want to throw this IllegalStateException saying that failed to fetch todos. Otherwise, this body method is going to be invoked by the framework. Since I know inside the response body, I'm going to receive the list of todos, I have mentioned the return type of this method as list of todo. Based upon this return type, the framework is going to understand how it has to read the response and convert the response from JSON to Java objects. I can also mention this list of todo as a input to these parameterized type reference. So here under these diamond brackets, I'm mentioning the same list of TodoDto, but here I'm getting a warning saying that explicit type argument can be replaced with the empty diamond operator, because we already given enough clues to the framework on how the response body has to be converted by mentioning the details under the method signature. That's why let's get rid of this list of TodoDto here. I hope you're clear on what this findAll() method is doing. In the similar lines, I have defined one more method with the name findById(). So this method, it is going to accept a ID as an input value. And based upon this todo ID value, it will try to fetch a single record of todo, that's why I have mentioned the return type of this method as TodoDto. This time also you have to invoke the same get() method because still we are trying to read a single record. And under these URI, we can mention the todos as a URI path. On top of that, we also want to send a path variable. So that's why first I'm defining a placeholder and under this placeholder, I want these ID value to be considered. That's why as a second parameter to this URI method, I'm passing these ID value. Once we populated all the request related information, we have to invoke the retrive() method. Next time again, checking the status. This time I'm checking these is4xxClientError(). This method is going to check whether there are four series errors like 404, which indicates that there is no resource with the given ID on the backend side. If there is any such error, I'm throwing this exception, otherwise I'm simply trying to read the response body by invoking the body() method. And here I'm passing TodoDto as an input, which gives a confirmation to the framework on how it has to convert the JSON to a Java object. After the get related methods, I have defined create method as well, which is going to post a brand new record of TodoDto onto the third-party REST API. So this time, since I'm trying to create a new record, I have to use the post method. Whenever we're using POST,PUT,PATCH methods, under such scenarios, we can invoke the body() method and to this body() method, we need to pass the TodoDto object. So this method that we are trying to invoke on top of the URI, it is going to take care of populating the request payload based upon the data that you are passing. Here we are passing a Java object, but this body() method, it is going to take care of converting that into a JSON and forwarding the same to the third-party REST API. So once the request details and the request body is populated, I need to invoke the same retrive() method followed by we can perform some error checks as well. But here I'm ignoring error checks because I want to show you that it is completely possible to invoke the body() method without performing the error checks. So whatever body() method that I'm invoking on top of the retrive, it is going to take care of reading the payload that I'm getting inside the response and converting that JSON into a TodoDto object based upon the data type that I'm providing. Next we have update method, which is going to take care of updating an existing Todo object. This method, it is going to accept what is the ID of the todo record that we want to update along with the entire todo object, which is going to have all the new data. So this time, since I'm trying to perform the update operation, I need to invoke the put() method. Similarly, we are going to have patch() method in case if you are trying to update a single field, then using patch is recommended. Here I will use put() and after this put(), I'm invoking the uri()method and to this uri(), I'm passing the todo's path followed by the ID of the record I'm trying to pass as a path variable. And since we are trying to send a placeholder for the path variable, we should also pass what is the actual path variable value as a second input parameter to this uri() method. In case if you are looking to populate query params or request to params as part of the request, then you need to invoke the attribute method to this attribute method. You need to mention what is your query param along with the query value. After this URI, I'm invoking the body() method and to this body() method, I'm passing the todo object that I have received. This is going to populate the request payload followed by we need to invoke the retrive and body which is going to accept the response body and convert that into a TodoDto object. The same I'm trying to return from this method. Next I have delete() method, which is going to take care of deleting a given record based upon the provided ID value. This time, since I'm trying to delete a record, I need to make sure to invoke the delete() method followed by uri() and retrive(). Once the retrive() is invoked and once our request is processed, this time I'm invoking that toBodylessEntity(). . Since I'm not expecting anything as an output from the response, I'm simply invoking these toBodylessEntity(). . Whereas here, if you see, we are invoking the body() method because we are expecting the framework to read the response payload and convert that response payload into a Java object. Under such scenarios, we need to use this body() method. But here, once the delete of the record is completed, there is nothing for me to read. So that's why this time I'm invoking that toBodylessEntity(). method, which is going to return a ResponseEntity without a body. That's why the return type of this delete() method, I have mentioned it as void. I hope you're clear on how to consume other rest APIs. As a nextstep, we want to test this code that we have written inside this class. In other words, I would say, whenever you want to read all the todos from your business logic, you need to invoke these findAll() method that is being exposed by these service class. So these findAll() method is going to take care of invoking the third party Rest API. But the question or the challenge right now that we have is, how are we going to invoke these findAll() method inside our business logic? Usually in a typical enterprise application, a user action like clicking on a button or navigating to a page will trigger some business logic on your backend side, and from that business logic, you can invoke these service methods. But inside our job portal, there are no actions that an end user can perform related to todos. That's why what I will do is, as a next step I will try to define some rest APIs that can be invoked from the postman. From those rest APIs, we can try to invoke these methods, which are going to take care of third party rest API invocation. I know this is confusing. Let's continue this discussion in the next lecture. I'll try to explain you one more time on why we have to expose new Rest APIs.