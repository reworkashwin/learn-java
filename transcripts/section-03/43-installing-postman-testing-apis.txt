We are going to build many other REST APIs throughout this course. As of now, to test a backend REST API, we are trying to invoke the same by using the browser. But using the browser, we should be able to invoke only the GET REST APIs directly. In case if we want to invoke the REST APIs that supports POST method, DELETE method, PUT method or PATCH method, then we can't invoke them directly by mentioning the path inside these browser URL. We need to either invoke the REST API from a UI application or by using tools like Postman. Inside this lecture, I'm going to show you on how to set up Postman and test the REST APIs by using the same. If you're already aware about Postman, I would still recommend you watching this lecture because I'm going to show you some best practices that you need to follow while leveraging Postman to test the REST APIs. First, you need to make sure to visit the postman.com website and from this website, you should be able to download the Postman application based upon your operating system. Once you download and install the Postman, this is how it is going to look like you may not have any collections. By using these plus symbol, you should be able to create a new collection. Collection is like a Postman project. Under a collection, we can group multiple folders or multiple REST API requests based upon a department or based upon a domain. For example, here I have created a collection with the name Spring. Under this Spring, I'm going to create multiple folders and multiple REST API requests. This way I'm trying to group all the REST API invocations that I'm trying to make as part of this Spring course. Throughout this course, I'm going to update this Spring collection by creating multiple folders and multiple REST API requests. What I'll do is, I'll upload this collection into the GitHub repo please download this and import this collection into your Postman so that you don't have to create all the Postman requests that we are going to send to our backend application. You should be able to reuse whatever I have developed throughout this course. Whenever you want to share your collection to some other developer, you should be able to do that by right-clicking on this collection and select this more option and here there is an option to export. As soon as you click on this export, it is going to show you this pop-up window. You can go with the default selected version and click on this export. And with that, it is going to ask you to save this collection JSON file into your local system. Once the collection is saved, you can share the same with other developers so that they can reuse your collection. The same I'm going to do in my scenario as well. I will export this collection and upload to the GitHub repo for your reference. Once you downloaded the collection JSON file from the GitHub repo, you need to click on this import option and into this window, you need to drop the collection file that you have downloaded and with that, you're going to get the same Spring collection into your Postman tool as well. Under this collection, I have created a dummy folder. Whenever you want to create a folder under a collection, it's very easy. You just have to click on these three dots and select the option of add folder. Since we are right now testing some dummy REST APIs, I have named this folder as Dummy. Next, under this dummy folder, I created a new REST API invocation with the name Path Variable. Let me show you how I have done that. I clicked on this add request and it is going to show me a New Request. I can give the name to the request along with that, I can enter the URL and select the HTTP method. So that's how you can create a new request. Let me close this and delete this one. Now I'll go to this PathVariable. So the request name is Path Variable. Since we are trying to test some Path Variable related scenarios, I have given this name. To test a REST API, I need to make sure to select the supporting HTTP methods. So far we built the REST APIs that supports only the GET method. That's why I need to make sure GET. And coming to the path, I need to mention the complete path. For example, localhost 8080 /api/dummy/users/123/address/456. If I click on the send button, I'm going to get the response, whatever our Java method returns. If you see the response is coming in a JSON format, since it is a simple field, it is trying to display as a normal string, but behind the scenes, the framework, it is going to send the enter response as a JSON. In the coming lectures, we are going to build some complex REST APIs, where we are going to return an entire Java object as a response. Under such scenarios, you should be able to see the entire JSON object being displayed under this response. By default, the framework is going to send the response status code as 200 with OK message, the same we are seeing here. We're also going to discuss on how to send our own response status code based upon our requirements. In case if you are trying to invoke the REST API with an invalid HTTP post method then we are going to get the 405 Method Not Allowed as a response status. Because whatever REST API Java method that we have built in the backend with this path, it only supports the get() method, but not the post() method. That's what we're able to see inside the response. This time if you see the framework, it is trying to send all the error response as a JSON object that's why we're able to see these curly braces inside this JSON object we have various fields like timestamp, status, error, and path. Let me select get and invoke the REST API. This time we got the successful response. In real applications under a collection, we're going to have hundreds of REST API requests and mentioning this base URL for all those requests is not recommended. Because in future, if you change the port number from 8080 to some other port number, you need to open all those requests and update the URL. In case if you want to send the same REST API by sending the request to the development server or UAT server or QA server or production server, then you need to make sure to update the hostname port number in all the requests. That's why mentioning this base path directly inside the request is not recommended. Instead, we can follow certain standards. Let me show you the very first standard. You can select this entire base URL like HTTP localhost 8080 and right click on that. Inside this menu, you need to select this set as variable. Here we need to give a name to the variable. For example, I'm trying to give the name as base_url. Coming to the scope, I'm going to select the scope as collection. That means whatever variable that I'm trying to create with the value as localhost 8080, it is going to be applicable for all the requests that I'm going to store inside this collection. Whereas if you set it as Global, then all the collections inside your postman they should be able to use these variable. For now, I'll go with the collection and click on this set variable. As soon as I click on that set variable, you should be able to see the URL inside the postman, right now it is not referring to the direct hostname and port number. Instead, it is referring to the base url variable the syntax that we need to use is, we need to mention two open curly braces and two closing curly braces and in between them, we need to mention the variable name. With this URL, if I try to click on this send button, I'm going to get the same response. You can go and check the console as well. Inside the console, you can see the complete URL for which the request is forwarded. I'm going to clean this console and close this one. At any point of time, whenever you want to update this base url to some other value, you can either directly update here or you can go to the collection by selecting this spring and here you have a variable section inside these variables, you should be able to see the variable that we have created along with the value. You can update the value like 8081. Now, if you go and invoke this request, you're not going to get any response because the request is being sent to the port number 8081, where we don't have any backend application running. So this is one approach which majority of the developers and the testing people follow. Instead, there is also another better and advanced approach as well. The problem with this approach is, our URL still looking ugly due to these all these curly braces. We can get rid of this entire base url variable and with the new approach that we are going to discuss, we don't have to mention the base url path or we don't have to use any of the variables. Let me show you that approach. But before that, I'll go to the collection and update the variable to the port 8080. Now, inside the same collections, there is a tab with the namescripts. You need to click on the same and select these prerequests. So whatever JavaScript code that you are going to write here, the same will be executed before your request is forwarded to the backend application. So here, by using the JavaScript I'm going to set the details like the protocol that I want to use, the host name and the port number. First, we need to type pm, pm means Postman. By default the Postman tool, it is going to expose a JavaScript object with the name Postman. Using this Postman object, I need to invoke the request under this request, we are going to have url after this url, we need to invoke the protocol. To this protocol, I need to assign the value as http. In case if you want to use HTTPS, you need to mention the same. Please make sure you are mentioning the value within the double quotes and towards the end, you need to mention semicolon. The same kind of code we need to repeat two more times. Now this time instead of protocol, I'm going to invoke the variable host. To the host, I'm going to mention the host name as localhost. Next, I'm going to invoke the port. To the port, I'm trying to assign the value 8080. With this update, whenever I'm trying to invoke a request by using the Postman, this JavaScript code is going to be executed and it is going to append these details to the requests that are being forwarded to the backend application. So let me save this and go to this Path Variable this time, I will get rid of this enter base url and I'm going to simply mention the actual REST API path. Our REST API path starts from api, from/api. That's why I have mentioned this. Now, if I try to invoke this API by clicking on the send button, I'm going to get the successful response. Under the console also, you can see the request is forwarded to the base url HTTP localhost 8080. I hope you are clear on how to use the Postman to test the REST APIs by following the standards. Throughout this course, I'm going to show you how to test the REST APIs by using both Postman and the React UI application that I'm going to provide. It's up to you which approach you want to follow. Usually backend developers, they rely on the Postman, but in case if you want to get a feel of end-to-end application flow, then you can happily use the React UI application that I'm going to provide. You just have to follow certain instructions that I'm going to provide to set up that React UI application. That's going to be very easy. When the time comes, I'm going to guide you on how to integrate our UI application with the backend application that we are building.