If a project is using Spring 7 or higher versions, then they can use a new feature that got introduced as part of the Spring 7 around the ApiVersioning. With this new feature, we are going to do all the versioning related configurations in a centralized place. So whatever legacy approach that we have discussed so far, if we have to do these for all the REST APIs, you need to visit all your controller classesand you need to make these kind of configurations like mentioning the path variable or mentioning the RequestParam or mentioning the Header or mentioning the producers. So this is going to be a complex or cumbersome process in an enterprise application where they are going to have hundreds of REST APIs. So that's why let's try to understand how to build ApiVersioning by using the new feature that got introduced from Spring 7. What I'll do is, I'll try to create a new Java package with the name Config. And inside this package, I'm going to create a new Java class with the name WebConfig. This name can be anything. On top of this class, I need to mention @Configuration annotation. Long back we discussed about the purpose of this @Configuration annotation since we are going to define some configurations related data inside this class, we need to mention this annotation. First, we need to make sure that this class is, implementing a interface with the name WebMvcConfigurer. . Inside this interface, if you look for the list of methods, there is a method around the ApiVersion. . If you search for version, you will find a method with the name configureApiVersion. So this method is a default method, and it is introduced as part of the Spring 7.0 version. By using this method, we can configure ApiVersioning for our entire backend application. This method accepts a method input parameter of type ApiVersionConfigurer. Using this input parameter only, we can perform the Apiversioning configurations. Since this is a default method, we should be able to override inside our WebConfig class. What I'll do is, I'll press command-end, and inside this window, I'm going to use this override methods . and here I'm going to search for configureApiVersioning. I'm going to select the method and use this copy java doc and insert override as well, so that the entire method documentation will be copied into this class. So let me remove this default code. Now before I try to configureApiVersioning by using this configurer, I'm going to create a new controller inside the controller package. The name of the controller is VersionController. On top of this controller class, I need to mention @RestController annotation followed by @RequestMapping. To this @RequestMapping, I'm going to mention a parent path, api/version/ a path variable with the name as v. So this path variable name can be anything. Using this path variable, I'm going to accept the ApiVersion details from the client application. Inside this class, I'm going to create a couple of REST APIs. The first one, it is going to return ResponseEntity of String. The method name will be defaultVersion() and this method, it is not going to accept any method input parameter. From this method, I'm going to return ResponseEntity.ok with the response data as version 1.0.0. On top of this method, I'm going to mention @GetMapping annotation. Let me copy this entire method and paste one more time. I will update the second method name to v2Version and here the content I'm going to update to 2.0.0. If you see, we have two different REST API methods with the different methods like defaultVersion and v2Version. So with this setup, if I try to restart my application, I'm going to get an exception and my server is not going to start. The exception details that we are able to see inside the console is related to ambiguous mapping. Inside this Version controller, we have different Java methods, which are mapped to the same REST API path. So against to this @GetMapping, I'm not mentioning any path, which is unique to these methods. So both these methods are mapped to this parent path. So that's why we are getting this problem. Off course, you may say here, to resolve this problem, we can give different paths for these two different methods. But that is going to make these two different Java methods belong to two different REST APIs. But since we are trying to build different versions that belong to the same REST API, what we can do is, to resolve this problem, we can invoke a parameter or attribute with the name version. To this version, I can pass a value like 1.0 and with this, now anyone who is sending 1.0 under this path variable, this method is going to be invoked. Similarly, here, I can mention version as 2.0. So this version is a new attribute or parameter that got introduced from Spring 7. After making these changes inside the controller, you can go to the WebConfig and here, by using this configurer, you can invoke a method with the name usePathSegment(). Apart from the base path, inside the VersionController, we have defined this parent path. This parent path has three different segments, like 1, 2, 3. The first digit always will be 0. So this will be 0th path segment, followed by 1 and 2. That's why here, we need to provide 2 for this usePathSegment, because we want to consider the second path segment as a dynamic placeholder to decide the versioning of the API. Thereby, I can invoke the method addSupportedVersions. To this, I can mention list of supported versions inside my REST API. For example here, I can pass 1.0, 2.0, and 3.0. This way, I can mention any number of versions that I'm planning to support to my client application. So with this setting, now the client applications, they can send only these three values as part of the path variable. And based upon these path variables, one of these REST API is going to be invoked. As of now, we have not configured anything for the 3.0, but I have some plans to show you some demo around 3.0. So with this, now I'm assuming we have done all the code changes related to the ApiVersioning by using the path variables. So let me save all these changes, do the build and restart my application. During the startup of the application, I received an error or exception. The error console says API version specified, but no API version strategy configured. Seems my WebConfig configurations are not being detected by Spring Boot framework. Let me see what is the problem. I created the config package outside of the backend package. But inside this backend package only, we have the SpringBoot main class. Whenever we are creating beans outside of the main package, then we need to provide the @ComponentScan annotation on top of this backend application. We don't have to do all that stuff we can move this config package under these backend package. Previously by mistake, I have created the config package under this com.eazybytes but please make sure you are creating the config package under these backend package where the Spring Boot main class is present. Now this time my application should start without any issues. Inside the postman, this time, please go to the folder Spring 7 under the versioning. Under the Spring 7, we have a first request with the name path versioning here under the path if I'm mentioning the version number as 2.0, I'm going to get the output as version 2.0.0. In case if I'm sending 1.0, I'm going to get 1.0.0. Are you seeing how easy it is to configure ApiVersioning by using Spring 7? We just have to write these lines of code and inside the version controller, we have to use these version parameter. For all the REST APIs, where we are planning to use the API versioning, we need to make sure to provide the version values by using these version parameter. Apart from that, we don't have to make any other code changes. Now let me show you on how to use the query params or request params to implement the API versioning. What I'll do is, I'll comment this line of code and paste the RequestMapping one more time and I'll get rid of this path variable because we no more wanted to use the path variable. We want to use the request params. Now I'll go to the WebConfig and I'll try to duplicate this line let me comment the code related to the path segment. This time, instead of invoking the usePathSegment(), I'm going to invoke a method useQueryParam(). To this method, we can mention the QueryParam name this QueryParam name can be anything, but I'm trying to give a reasonable name, which is version followed by I'm using add supported versions. So let me save this and do the build once the build is completed, I'm going to restart my application. We don't have to make any changes inside the VersionController. We just have to take care of this RequestMapping, which we already taken care previously. Inside the Java methods, we don't have to make any changes. We just have to make sure we are using these version parameter with the correct values. Now from the postman, if I try to open this RequestParamVersioning, this time I'm going to send the QueryParam as version with the value as 1.0. If I click on the send button, I'm going to get the output. In case if I'm sending a version, which is not supported by my backend application, like 4.0, I'm going to get the 400 bad request. Whereas if I'm sending a valid value like 2.0, I'm going to get the expected output. One more beauty of these ApiVersion configurator is, we can also setDefaultVersion. That means if a client application is not sending the QueryParam, we can request the framework to consider a default version like 1.0. With this from now onwards, even if the client application is not sending this version QueryParam, the framework is going to add the RequestParam with the value 1.0 and with that, the first method logic is going to be executed. This API version configurer is very powerful. Please explore the entire documentation around it, because the Spring team they have done a fantastic job around this. If you want to explore more about this API version configurer, I strongly recommend you reading the official documentation. Now let me show you another feature of this API version configurer. Inside our REST APIs, while we are using these version parameter, we don't have to mention the exact value like 2.0. We can mention something like 2.0+. That means any version that are supported by our backend application, which is equal to or greater than 2.0, for those versions, this method is going to be invoked. So this way for multiple versions, you can have the same Java method to handle the request. So inside our backend, as of now, we are supporting three different versions, 1.0, 2.0, 3.0. If someone is sending either 2.0 or 3.0, this method is going to be invoked. But if the client application is sending like 2.1 or 2.5, they're going to get bad requests because that number is not present in the supported versions. If you add 2.1 under the supported versions, then when someone invokes our REST API with the 2.1, the same method is going to be invoked because 2.1 is greater than 2.0. I hope you are seeing the power of this feature. Let me save all these changes and do the restart. This time, I'm not going to send any QueryParam. And with that, we know what is going to happen the framework is going to add the default API version 1.0, and we are going to get the output version 1.0.0. In case if I'm sending the version number something like 3.0, we know what is going to happen we're going to get the output from the second method with the value 2.0.0. But don't try to be naughty and mention some value like 2.5 since this version is not supported. We're going to get that bad request as an output. Let me go back to 2.0 and click on the send button. Yes, everything is working fine. Let's continue this discussion in the next lecture as well.