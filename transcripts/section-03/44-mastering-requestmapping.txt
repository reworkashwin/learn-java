Inside this lecture, we're going to discuss about a special annotation available in the Spring Boot framework. This annotation is @RequestMapping. We can also develop REST APIs by using this annotation as well. So this is an annotation that we can mention on top of a Java class and Java method as well. Whereas if you look for other annotations like GetMapping, it can only be used on top of the method. Under the target, here we're seeing only the method as the ElementType. Whereas for the @RequestMapping, we are able to see type and method type means the Java class. You will see some developers developing REST APIs by using this @RequestMapping as well. That's why I want to spend few minutes explaining you about this annotation. Because you will end up with some mandatory scenarios where you need to use this @RequestMapping. Let me go to the UserController class. On top of this class, I can't mention the annotations like @GetMapping. I'm going to get the compilation error because these annotations can be only mentioned on top of the Java methods. But coming to the @RequestMapping annotation, we should be able to use it on top of the Java class as well. Now, let's try to understand under which scenarios we need to mention this annotation on top of the Java class. Whenever you are building some REST APIs inside a Java class, many of the times, you will end up having a common prefix path for all your Java methods. For example, inside this UserController, we have various Java methods. For all these Java methods, there is a common prefix, api/dummy/users. So this prefix path is common for all these methods. In these kind of scenarios, instead of repeating the same prefix path on top of all the methods, what I can do is, I can simply mention here against to this annotation. So let me type /api/dummy/users. With this, now I can get rid of all these prefix path. Let me do the same here as well. I need to update for other methods. Here also, I'm going to delete this prefix path. Let me save these changes, do the build, once the build is completed, I'm going to restart my application. From the postman, I'm trying to invoke one of the REST API that we have inside our Java methods. If I click on the send button, I'm still getting the same response. That means whatever prefix path that we are mentioning by using this @RequestMapping annotation, the same will be added before the API paths that we have mentioned on top of the Java methods. This is one of the great advantages of @RequestMapping. I strongly recommend you using this annotation at least on top of the Java class. Whereas on top of the Java methods, we have alternatives like shortcut annotations, like @GetMapping, @PostMapping. Let me explain you on how to use this annotation on top of the Java method as well. For the same, I'll go to the DemoController here as of now, we are using GetMapping annotation. What I'll do is, I'll simply mention @RequestMapping annotation. And to this annotation, I'm going to mention the path as forward /home. The primary difference of using @RequestMapping compared to the @GetMapping is when you are using this @RequestMapping annotation, your REST API method, it is not going to be restricted to a specific HTTP method like GET method. When we are mentioning this annotation @RequestMapping, we're telling to the spring framework or spring boot framework that we are fine to accept any kind of HTTP method while invoking this REST API. I'm going to show you the demo of the same by using postman. So from the postman, I'll try to add a new request. And I'll just mention the path of this request as HomeRequest, followed by I'm going to mention the path as /home. As we discussed in the previous lecture, we don't have to populate the base path of the backend server because we already configured it under the scripts tab, which is going to take care of populating these values. Now, let me go to this home request and if I try to click on the send button, I'm getting the hello world. Let me do the same with the post with the post. Also, I'm going to get the same response. Let me do with the delete with the delete also, I'm getting the same response. So whenever you are into the scenario where you don't want to restrict your REST API method to a given HTTP method, then you can go with the syntax that we have here. But following these syntax is generally not recommended because it is not a good practice to expose the same Java method for all kinds of HTTP methods. Instead, by using these @RequestMapping annotation, we should be able to define the HTTP method as well. For the same next to these @RequestMapping, we can either invoke the path parameter or value parameter to the path and value parameter. We can pass the list of paths that this REST API is going to support. Since I have a single path, I'm mentioning the same inside the double quotes. But if you are looking to provide multiple paths, you need to mention the curly braces. And inside the curly braces, you can mention multiple paths by separating them using comma. After the path, I can invoke the method parameter to this method parameter. I can pass a single method like RequestMethod.GET with this configuration. Now my REST API, it is going to support only the get HTTP method. Instead of defining this lengthy syntax, you can follow the shortcut annotation like GetMapping. The same applies for other type of methods like POST, DELETE, PATCH, PUT, and so on. But the beauty of this annotation is it is going to allow you to define multiple HTTP methods. I need to mention curly braces. And inside these curly braces, I can mention multiple HTTP methods. Now I have mentioned post method as well. With this, now my REST API, it is going to support both GET and POST method, but not the other methods. Let me save this restart my application. First, I'm going to invoke with the DELETE method. I should get 405 error. Yes, we are getting 405 error let me use GET with the GET, I'm getting a successful response. With the post also, I'm going to get the successful response. Apart from these method param and path param, this annotation supports many other params as well. If you open this annotation source code and look for the list of methods by pressing command F2L or control F2L shortcut, you will be able to see that this annotation supports all these parameters. So by using path or value, we should be able to pass multiple REST API paths that our Java method should support. Similarly, by using method, we can pass multiple request to methods. You'll be able to see the return type of this method is array of request method. That means we can define multiple request methods. Similarly, there are other important annotations like headers(). By using these headers(), we should be able to accept the HTTP request headers. When we are discussing about the headers concept, I'm going to explain about this the same applies for params also by using the params, we can accept the query params or the request to params that are coming from the client application. We have consumes as well. By using these consumes, we can restrict what type of content that our REST API can consume. For example, here, I'm going to mention consumes and to this consumes, if I mention the value as application/json, then my REST API is only going to accept json as an input format. If someone is trying to send the request by using the XML, it's going to throw an error. In the similar style, I can also mention producers parameter as well. To this also, I can mention application/json. By using these producers, we are telling to the client applications that what is the response content type ? What is the format that this REST API is going to use to produce the response? Since we have mentioned this value for the client applications, the framework is going to convey that this REST API method is sending the response by using the json format. If you open any of these consumes or produce documentation, you will be able to see what are the valid values that we can send. We can send a single value or multiple value based upon our requirements. As of now, we have mentioned application/json, which is supported by these asterisk mark as well. But apart from these json or any other applications format, we can also send the response by using text plane as well. So all these valid values you can get from the media type constant. If you look for media type inside these, we have values like APPLICATION_ATOM_XML, which is going to leverage this syntax. Similarly, if you keep scrolling, this is the value for GraphQL and this is the value for json. Instead of hard coding this value, you can also leverage MediaType.APPLICATION_JSON_VALUE. For example, we can go to DemoController and here I can mention MediaType.APPLICATION_JSON_VALUE . In the similar lines, we also have another important parameter with the name version. We are going to discuss about this version when we are discussing about the API versioning standards in the coming lectures. I hope you are clear on when to use this @RequestMapping. If your REST API supports a single HTTP method, then always go with the shortcut annotations like GetMapping or PostMapping. Whereas if you want to support multiple HTTP methods, you can happily use this @RequestMapping. The other scenario where you can use the @RequestMapping is whenever you want to define a parent path for all your REST APIs inside a Java class. Under such scenarios, you can mention this annotation on top of the Java class along with the parent path that you want to define. Inside the code, please make sure to comment these consumes and produces related logic and remove this comma and close the annotation. Because when you are using these consumes and produces, we need to make sure to send the request using json. We are ready to discuss about those advanced concepts. Since I don't want you to face any issues while invoking this home path, I'm requesting you to comment these configurations. We're going to explore them when we are developing advanced REST APIs. The details that we have discussed around the @RequestMapping annotation, I have documented all of them in these three slides for your reference along with the examples.