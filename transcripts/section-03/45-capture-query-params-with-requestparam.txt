As a next step, let's try to understand how to accept query params from our REST APIs. Apart from path variables, client applications can also send query params inside the request to path. Whenever requests our REST API methods, they should support query params as well. Multiple query params can be sent by the client applications as part of the API request. For example, here the static path is api/dummy/users. For this path, the question mark needs to be mentioned. This is the HTTP standard that everyone needs to follow whenever they want to start query params inside the URL. The client applications, they need to make sure to mention the question mark followed by the query params they can mention by using key and value format. So the name is a key and the value is a mother. In between, we need to mention these equal to symbol. This way we can mention any number of query params, but each of the query params, they need to be separated by using these ampersand symbol. The query params can also be used along with the path variables as well. The above URL, if you see, it has both path variables and query params. Inside the URL, the query params should come towards the end. The client applications cannot send the path variables after the query params. That is not acceptable. Now let's try to understand how to accept these query params. In other words, these query params, they also called as request params. To accept these query params, we have an annotation with the name @RequestParam inside the spring framework. So what I'll do is, I'll try to create a @GetMapping REST API path. I'll simply mention the path as /slash search. So there is already a parent path available on top of this class. So next to this parent path, if someone is providing the sub path like search, then this method is going to be invoked. Let me type public String searchUserWithQueryParams(). And from this method, I simply want to return a String statement saying that fetch user with query params, for now, I'll just keep the static String value. Later on, we can update these to dynamic value. Let's imagine we want to accept a couple of query params while someone is invoking these REST API method. To accept those query params inside our Java method, we need to mention the annotation @RequestParam. As I said before, query params, they also called as @RequestParam. That's why this name to this annotation, followed by I'm going to mention the data type as String. Coming to the method input parameter name, I'm going to mention as name. In the similar lines, I'm also going to mention one more input parameter with the data type as String. Coming to the name of this parameter, I'm mentioning the gender as a value. Now I can use both these query params variables inside my business logic. Maybe I can update this return response to fetched user with query params with so-and-so name and gender. If you see inside the path, we don't have to mention like we have done for the path variables. Whatever query params that are being sent by the client applications, they are going to be matched to these method input parameters. If the query param name is name, then this variable is going to be populated. If the query param name is gender, then this parameter is going to be populated. Let me save these changes and restart my application. Inside the postman, I'm going to create a new request and I'll name this to query params. And here I'll try to invoke the REST API search and mention question mark. Instead of defining the query params manually directly inside the URL, inside the postman, we should be able to define them by using these params. So here there is a params tab under the key I'm going to mention the value as name followed by, I will mention my name. In the similar lines, I'm going to create one more query param. As I'm creating the query params, the URL is getting updated. So the question mark is automatically added and this is the key and this is the value. Now if I try to create one more query param, the ampersand symbol is going to come into picture. To this gender, I'm providing the value as M. Maybe instead of M, I can mention it as male. Let me save this request and click on this send button. I'm getting the 404 because I forgot to mention the parent path as well. What is a parent path? If you go to the top of the class, this is the parent path that we have configured. We need to mention the same just before the search. Now if I click on this send button, this time I got the successful output based upon the values that we are sending. In case if I'm changing this name to John, the same will be reflected inside the response as well. That means whatever values that we are sending, they are getting mapped to the method input parameters. In case if you want to make any of these request params as optional, you should be able to do that. Before that, let's try to test some negative scenario. Let's imagine I am not sending this name query param. If I click on the send button, I'm going to get the 400 bad request because name is a mandatory query param on the backend. I can make this as optional by using required parameter. To this, I'm going to set the value as false. Let me restart the application. Now if I try to send the same request, I'm going to get the null value under the name. In case if I'm sending the name query param, I'm going to get the actual value under the name. Whenever we're marking a query param as required as false, if that value is not coming, then we are trying to display some null value instead of displaying a null, we can display some default value. For example, if some client application, if they're not sending a query param, we can consider or we can assume a default value. How to set that default value? We just have to invoke a parameter with the name default value and to this, I can assign a name as guest. So with this, now, if someone is not sending the name query param, the framework, it is going to inject the value guest into this name variable. We can try to test the same. I'll uncheck this name query param and send this request. You will be able to see inside the response, we are getting the guest, whereas when we are sending these name query param, we are going to get that john as the output. If you're looking to use different variable names as your method input parameters compared to the query params, you should be able to do that just like how we have done for path variables. Very similarly here, I can invoke name and to this name, I can pass the value as gender, which is the actual query param, but coming to method input parameter, I can mention something like sex and the same I can use inside the business logic as well. If you try to test this API, we are going to get the same response. Even though we are sending the query param with gender, it's still, it is going to map to this method input parameter because we have used this name as gender. Just like how we have used a Map to accept the multiple path variables, very similarly, we should be able to use Map to accept the multiple request to params as well. I can create one more REST API and this time the path I'm going to update to search/map. Let me update this to searchUserWithMapQueryParams() and instead of defining all these multiple request to params, what I can do is I can simply use Map, which is going to have the key and value as String and String. Let me update this to request to params. Now using these requestParams, I should be able to invoke get() method and to these, I can pass the query param name as name and here also, I'm going to use the same requestParams and invoke the get() method. And to this get() method, I'm going to pass the key as gender. Let me restart my application inside the postman I'm going to copy these query param requests one more time and update these to QueryParamsWithMap. We need to update the path as well. After the search, I need to mention map. As soon as I click on the send button, this time also, we are going to get the same output. I hope you're clear on how to accept query params or request to params by using these @RequestParam annotation. We're going to leverage these annotation a lot in the complex REST APIs that we are going to build throughout this course. Whatever we discussed around the @RequestParam annotation, I'm going to share all those details inside the slides as well for your reference.