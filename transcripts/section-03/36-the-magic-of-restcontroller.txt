In the previous lecture, we built a very small REST API by using these highlighted code. On top of that DemoController class, I used an annotation @RestController. This is a stereotype annotation, which is an extension to the @Controller annotation. Let me go to the maven tab and make sure to download all sources and documentation so that we can see the framework code properly. The downloading of the sources, it is going to take a few seconds of time. Let's wait for this to complete, followed by we can see the source code properly inside IntelliJ. If you try to understand the code that we have inside the RestController, it is a combination of two different annotations @Controller and @ResponseBody. Based upon our discussion from the spring core concept, we discussed there are multiple stereotypes like controller, which we are supposed to use on top of the classes that we are building inside the controller layer. Similarly, we have @Service annotation, @Repository annotation. In the similar lines, we also have @RestController annotation. When we are using this annotation, we are telling to the framework to use a combination of controller and @ResponseBody annotation. Let's open the source code of this @ResponseBody annotation. On top of this annotation, we have a documentation highlighting that whenever someone is using this annotation, that's an indication to the framework, the method return value should be bound to the web response body. Since we are using this @RestController annotation on top of this DemoController class, all the Java methods that we have inside this class, they are supposed to send their response as a JSON to the client applications by sending this JSON data inside the ResponseBody. Since we are sending the response in a JSON format, the client applications, they are responsible to understand this data and display it to the end user, either inside the browser or inside a mobile application. The client application can be anything. Whereas if you simply use @Controller annotation on top of this controller class, then all the Java methods that are defined inside this class, they are supposed to send their response using HTML format. That means this backend web application itself is responsible to prepare both view and data and send the same to the client application. In these kinds of scenarios, the client application, usually the browser or mobile application, they are going to simply display whatever response sent by the backend application. Since browsers, they can understand the HTML format, they can directly display the response without any processing. I have a couple of slides explaining this concept in detail. Let's try to discuss the same. We know by using Spring and Spring Boot framework, developers, they can build powerful web applications. These web applications can be classified into two major categories. The first one is Spring MVC based web application and the other one is REST API based web application. This Spring MVC approach is a legacy approach it is like listening to the music by using cassette. Before the invention of UI libraries and frameworks like React and Angular, developers, they used to build a web applications that holds both UI logic and the backend logic. So these kinds of applications, we call them as MVC based applications. MVC means Model View Controller. By using this model, developers, they used to control the format of the data that they want to share with the UI. And by using this view, developers can control on how to display this data on the UI side. And by using this controller layer, developers, they used to write their backend logic. So when someone is building MVC based application, it is entirely responsible to fully prepare the view along with the data in response to your client request. Basically, from the browser, the request directly come to this Spring MVC based web application. And this application, they need to execute their business logic, they can prepare the data and this data, they can render into an HTML template. And finally, a proper HTML will go as a response to the client application or to the browser where it is going to display to the end user. But this approach, as I said, it's a legacy approach. These days, no one is following this approach because nowadays, all the enterprise applications, they are building separate UI applications and backend applications. So these UI applications, they are going to be built by using frameworks and libraries like React, Angular on the UI side, whereas all the backend code will be developed by using frameworks like Spring Boot and Spring Framework. All the developers, they are building their backend logic by using REST API approach. This is like listening to the music by using Spotify, which is a modern and recommended approach. When you are exposing your backend logic by using REST APIs, you will have several advantages. These applications, they are going to send their response by using JSON. And this JSON can be consumed or processed by various kinds of client applications like a mobile application or another backend application or an UI application that is built on Angular or React. Regardless of what is the type of your client application, still, you should be able to use the same common backend logic. Whereas if you are building your backend logic by using Spring MVC, only browsers, they can act as a client application, a mobile application or another backend application or an UI application that is built by Angular or React all of them, they can't understand this data because you're trying to send the response in an HTML format. When we are building an application using REST API, the app is responsible to only process the request and response with only data ignoring the view. Since this is the modern and recommended approach throughout this course, we're also going to build our backend application by using REST APIs alone. That's why I'm going to put a good amount of efforts explaining you about how to build production ready or production standard REST APIs. In this next slide, I have a tabular representation highlighting the differences between @Controller and @RestController annotation. One of the major differences between these two annotations is when we are using @RestController annotation, we are using the combination of @Controller annotation and @ResponseBody annotation. I hope everybody saw that difference. Coming to the next set of differences, when a developer wants to build an MVC based application, the developer has to use@Controller annotation and with that, the methods inside the controller, they are going to send their response using the HTML format. All the rendering of the view based upon the dynamic data is going to happen on the server side. This is one of the serious disadvantages of MVC application. If all the view rendering is happening on the server side for each and every action, the request has come to the server and it is going to take some time to process and respond. That's why if you look at the older applications, which are developed a decade back, whenever you perform an action, they're going to be refreshed and they're going to be super slow. So the coupling between the view and your backend logic is going to be tightly coupled. The client applications, usually they will be browser. The typical use case will be UI based monolithic applications where you have all backend logic, UI logic as well. Now under the REST API based applications approach, we need to use @RestController annotation. And with that, all the methods inside the controller class, they are capable of sending the response either in the JSON format or in the XML format. We should be able to configure these REST APIs to send the response inside the XML format as well. Nowadays, no one is using XML because it is heavyweight in nature. Since JSON is a lightweight, everyone preferring these for performance reasons. In the REST API based apps, the view rendering is going to happen on the client side. Let's imagine there is a client application that is built based upon the React library. So this client application, it is going to invoke one of the REST API that we have on the backend. And this backend, it is going to respond the data in a JSON format. So whatever data that we are sending inside this JSON, it is going to be rendered and displayed to the end user by using React library on the client side, which means on the browser itself. So with this, we are reducing the burden on the server side and improving the performance as well. On the client side, the page navigation is also going to be very smooth and very fast, and they don't require refresh of the page as well. The typical client applications for these REST API based applications are separate frontend applications that are built by using React library or Angular framework, or it can be an Android application or an iOS application as well. Since UI and the backend logic is developed in two different applications, both UI and backend, they are going to have loose coupling between them. And these are the typical use cases where we are going to this approach when we want to expose our backend logic by using backend APIs or by using microservices so that other client applications can consume. These client applications can be mobile apps, UI apps, or they can be other backend applications as well. In all these kinds of scenarios, we need to use this approach. I hope you got some good introduction around the @RestController annotation. We're going to use this annotation throughout this course, but inside interviews, if people ask any questions around the difference between these two annotations, I hope right now you are in a position to answer them. I'm also assuming right now you have a clarity on what is the difference between Spring MVC based web application or REST API based application.