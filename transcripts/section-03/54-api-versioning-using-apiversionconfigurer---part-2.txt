Now, let's try to understand how to use request headers to configure these API versioning. I'm going to duplicate this line of code this time, instead of invoking the useQueryParam, we need to invoke useRequestHeader(). And to this method, we need to pass the header name that we are planning to use. The header that we want to use is X-API-VERSION and we don't have to make any changes around these addSupportedVersions() method and setDefaultVersions() method. Let me save this, restart my application. Inside the postman this time, I'll open this third request with the name RequestHeadersVersioning. So here, this time, while I'm invoking this API under the headers, I'm passing this header with the name X-API-VERSION with a value like 2.0 with this, we are going to get the output from the second method even if I mention it as 3.0, I'm still going to get the output from the second method. If I mention it as 1.0, I'm going to get the output from the first method. So whenever we are switching between multiple API versioning strategies, we don't have to make any changes inside the REST controller unless you are using path API versioning strategy. Because that approach, it expects a dynamic placeholder value for the API version number. And one more important point that you need to be aware is, if you want to use these features like 2.0+, you need to make sure you are using the version value as numeric value like 1.0, 2.0. So inside the WebConfig, we configured all the numbers. In case if you are configuring some alphanumeric version numbers like V1, V2, V3, under such scenarios, this option is not going to work because you are trying to follow alphanumeric value for the ApiVersioning. Next let's try to understand how to use the media type for the ApiVersioning. I'm going to duplicate this line of code followed by I'll come in the first line this time I'm going to invoke the method useMediaTypeParameter(). This method, it accepts two input parameter one is MediaType and followed by what is the param name that you want to leverage inside your MediaType to apply the ApiVersioning. Inside this MediaType class, you will see various media types available but since ours is a custom MediaType value, what we have to do is, we have to invoke MediaType class using this class, you need to invoke the method parseMediaType() and to this, we need to mention our Media Type value. So our MediaType value is application/ vnd.eazyapp+ json. So this time I'm not mentioning .v1 here because with this kind of MediaType, my framework will not have any clue around the place where I'm trying to maintain the ApiVersioning. That's why what I'm doing is, I'm trying to follow a different strategy this time I'm simply mentioning the hardcoded MediaType value followed by as a second parameter I'm going to mention the parameter name as v. That means my client application, after mentioning this value inside the request header, they have to mention v is equal to 1.0 or 1.2. I'll show you how to mention the MediaType value for these configurations inside the postman. After making these changes, you can save the file and restart the application. Inside the postman, you can go to this MediaTypeVersioning request under the headers you need to make sure to add these AcceptRequestHeader. And to this, you need to mention the value that we have defined inside the configurations and towards the end, after the static value, you need to mention semicolon followed by v. The same v we have configured as a parameter inside our backend code. So to this, if I pass the value as 3.0, I'm going to get the output from the second method whereas if I pass the value as 1.0, I'm going to get the output from the first method even if I'm not sending these accept header, I'm still going to get the output from the first method because inside these configurations, we have configured that default version as 1.0. I hope you're clear. All the code snippets that we have discussed around the four different approaches, I have mentioned them in these slides for your reference. Please note that in order to use this feature, you need to make sure to use the at least Spring 7 version. Since we are going to develop our backend applications by using the latest Spring and Spring Boot versions throughout this course, I'm going to use this new or modern approach to implement the ApiVersioning for the REST APIs that we are going to build for our job portal application.