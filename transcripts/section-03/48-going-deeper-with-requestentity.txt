As part of the client HTTP request, we are going to receive the data in various forms like path variables, query params, request headers, and request body. So far, we understand how to read this information by using various annotations like @RequestBody, @RequestHeader, @RequestParam, and @PathVariable. But sometimes, developers may prefer reading the entire request data with a single object Instead of reading individually by using various annotations, there might be a scenario where we may want to read the entire request data by using a single method input parameter. We can do that as well. There is a class with the name RequestEntity inside the Spring framework. Since this class, it is going to represent our entire request data. This name has been given. By using the object of this class, we should be able to read the entire data of the request. It can be headers or body or query params or path variables. So whatever generic T value that you see, it represents the data type of the @RequestBody. If you are expecting the UserDto inside the @Requestbody, you need to mention UserDto in the place of this generic while you are planning to use this RequestEntity. Let me show you a demo so that it is going to be very clear for you. What I'll do is, I'll copy the same REST API one more time. But this time I'll give the path as RequestEntity. I'll update the method name to createUserWithRequestEntity(). This time, what I'll do is, instead of using the @RequestBody annotation, I'm going to use the RequestEntity. This is not an annotation. It is a class. Your Java method input parameter data type should be RequestEntity. Since this RequestEntity expects the data type of your RequestBody, we need to mention the same inside these tags. The data type of the body is UserDto and the variable name I'm going to mention as RequestEntity. Now using these RequestEntity, I should be able to read all the headers by invoking these getHeaders() method, which is going to give me the object of HTTP headers. Using these HTTP headers object, I should be able to read individually all the request headers that I received as part of the request. In the similar lines, I can also invoke the getBody() method, which is going to return the object of UserDto. So how it is going to return the object of UserDto? Since we have mentioned the generic here as UserDto while creating the object of RequestEntity, the return type from this method will be UserDto object. Next by using RequestEntity object, I can invoke the method getUrl(). On top of this method, we need to invoke another method with the name getQuery(). So this getQuery() method, it is going to return all your query params as a String value. So from this String value, you need to separate the individual query params, which is a very cumbersome logic to write by a developer. That's why I don't recommend using this RequestEntity whenever you are planning to read the query params or the path variables. You can happily use the annotations like @PathVariable and @RequestParams. But I'm showing you the options that you have while you are using RequestEntity. Let me catch the output by using a variable called queryParam. In the similar lines, we can also invoke another method with the name getPath(). This is going to give you the entire URI path that your client application invoked. From this URI path, you need to manually extract all the path variables. Since writing this extraction logic is complex in nature, I don't recommend you using RequestEntity even for the path variables as well. In the similar lines, this RequestEntity has so many methods that you can invoke. For example, you can try to understand what is the method that is invoked by the client application by invoking this get method. There is an hasBody() method that you can use to check whether the body is present inside the request. Once you invoke the getUrl() method, you can also find many other supporting methods that you can invoke. If you are interested to know more details about the URI path that is invoked by the client application. What we can do is, we can try to keep a breakpoint inside this method and see what kind of information that we are getting under this RequestEntity object. I will restart my application, followed by I'm going to copy this RequestBody one more time into this dummy folder. I'll update this with RequestEntity. The path also I need to update. This time I want to invoke the REST API path that ends with request-entity path. I need to make sure under the body, I'm sending the correct user information. As soon as I click on the send button, the breakpoint is going to stop. Let's try to understand what information that we have by debugging this method one by one. So under these HTTP headers, we received total nine headers like User-Location, Content-Type, User-Agent. Based upon your business logic, you can read any of these headers from this object. Next if I debug the next line, you will be able to see under the UserDto, I have all the information like name, email, and gender. Let me debug next two lines under the query param, we have null because we didn't send any query params while invoking this API. Coming to the path variables, you will be able to see the complete path that is invoked by the client application. If there are some dynamic path variables available inside your path, the same we should be able to see here. By taking this String as an input, we need to extract the path variables manually. At last if I release the breakpoint, on the postman, I received the successful response. Always use this RequestEntity if you have plans to read both headers and request body inside your business logic. Instead of you catching the multiple request headers and the request body separately, you can happily use this shortcut of using RequestEntity. For the query params and the path variables, as I said before, please use those respective annotations because writing these logic manually is a complex process. Just like how we have RequestEntity, we also have another class with the name ResponseEntity. Using this class, we should be able to send various kinds of details to the client applications as part of the response. Let's try to explore about this class in the next lecture.