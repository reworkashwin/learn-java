Compared to requests, the anatomy of response is very simple. Inside the response, we can send three kinds of data. The first one is status code, which indicates what happened behind the scenes. When we are sending 200 OK, that's a successful message. We can send the 400, that means bad request 500, that means there is some runtime exception. So based upon this status code, the client applications, they are going to understand what happened when they invoked a given REST API. Apart from this high level status code, we can also send some metadata related information inside the response headers. These are very similar to the request headers. We can tell to the client application that we are trying to send the response in a format like application JSON or any other data that we can send as part of the response headers. And the final part of the information that we can send as part of the response is the Body. The client application is asking some user related information. We can read all that user information from the database and send the entire information to the client application. And there is no limit to what amount of data that we can send as part of the response. We should be able to send a simple String value or a complex object. Based upon our business logic, we can send the data inside the response body. While the status code is always mandatory, sending the headers and body inside the response, they are optional in nature. So far, inside all our REST APIs, we are returning the response by using String. That means, whatever data that our method is returning, the same is being sent inside the response body by the Spring Boot framework. But this is not a good practice to send the data directly as a method return type, instead, we need to wrap the written data by using ResponseEntity. Just like how we have RequestEntity on the request side, we have ResponseEntity for the response side as well. And while sending the response by using the ResponseEntity, the developer is going to have a chance to send a custom status code at the response headers based upon the business logic. By default, if we are not sending any status code, always the Spring Boot framework, it is going to send the 200 OK as a response. For example, if you see here, the client application, they are trying to send a post to REST API which is responsible to create a user based upon the data that they are trying to send. So whenever the user data is created on the backend side successfully, we should send the status code as 201 instead of 200. Long back, we discussed the standards that we need to follow while sending the response status code. Please revisit this slide one more time and try to understand what are the different status code that we need to send based upon the scenarios that we are into. First, let me update one of the GET API. Maybe what I'll do is, I'll go to the top of this class and here we have a GET REST API, which is going to work with the path variables. So here, if you see, we are directly trying to send the response without using ResponseEntity. Instead, as I said, we can try to use the ResponseEntity. What I can do is, I can try to comment this code and I'll maintain this for your reference. First I'm going to use written statement followed by I'm going to invoke the ResponseEntity class by using this ResponseEntity class, I can invoke the ok() method. There are two overloaded OK methods. If I'm invoking the very first method to this method itself, I can pass the data that I want to send as part of the response body. Since I'm looking to send this response String variable as part of the response body, I can pass the same here but with this code change, we are going to get an compilation error because the return type of this method is an object of ResponseEntity, not the normal String. So to resolve this problem, we need to update the method signature to ResponseEntity of String. So we're telling to the method that we are going to send a ResponseEntity object, which is going to have the String data type as a response body. When we are using the ok()method, behind the scenes, it is going to use normal ok()method on top of that, it is going to invoke the body() method followed by whatever body data that we are trying to send. So if you go to these normal ok() method, it is adding a HTTP status as OK to the response status code. So if you open this, OK, it is 200 successful. This is a shortcut that is provided by the Spring Boot team. In all the REST APIs, where you are sending the response inside all the getREST API methods, you can use this shortcut because the standard response status code that we need to send for getREST API methods is 200. Instead of invoking this overloaded method, you can also invoke normal ok() method followed by body() method and to this body() method, you can pass the response body data that you want to send. This is one standard of sending the ResponseEntity object. Now let me take you to the one of the post REST API towards the end we have this RequestEntity post to REST API. Here also, I'm going to comment this line of code and I'm going to type return ResponseEntity.status. . This time, since I'm looking to send the status code as 201, I'm going to invoke the set()method and by using HTTP status, I'm going to invoke the constant related to created. Inside these HTTP status enum, you will be able to see all the enum constants related to various standard response status codes. So there are 100 related numbers. Similarly, there are 200 related numbers. 200 means OK, 201 means Created, 202 means Accepted. If you scroll down, you will see 300 related numbers, 400 related numbers, 500 related numbers. You don't have to manually create these constants. The framework is providing all the required constants. You just have to use them by invoking the status() method. So here I'm using the created constant followed by I'm invoking the body() method, and as part of this body method, if I'm planning to send the userDto object, then I need to update these signature of this method to ResponseEntity of userDto. But usually for the PostMapping, we will not send the entire object, we will only send the output saying that the user is successfully created. That's why here instead of userDto, I'm going to send the String value let me mention the String value saying that Created User with the data with so-and-so user data object. Since right now I'm returning the String object, and here I have mentioned the userDto inside the signature, I'm getting a compilation error. To resolve this, I need to use String. I'm going to save all these changes and restart my application. I'll go and invoke this path variable request. Instead of address, I need to mention post here. If I click on the send button, as you can see, we are getting 200 ok, along with the response data. Let me invoke the RequestEntity request. So here, if I click on the send button, this time instead of 200, we are going to get 201. As soon as I click on the send button, I got the status code as 201 with the value created and with this response body. In case if you are looking to send some response headers, just like how we invoke the status() and body() methods, we can invoke one more method with the name headers. And to this header, we need to pass what is the header name and the header value. So here I'll try to send a Custom header. The header name itself is Custom Header, and to this, I'll try to assign some example value. So the same information will reach to the client application as part of the response. So if I save this and restart my application, once the application is restarted from the postman, if I try to invoke this RequestEntity REST API one more time, under the headers, this time you will be able to see there is a Custom Header present along with the example value. These are the headers that are being added by the SpringBoot framework. I hope you are clear on how to send various kinds of response data by using ResponseEntity. It is a recommended practice to always use Response Entity while you are trying to send the response information to the client application. Using request entity is always optional in the scenarios of get REST APIs you can use the annotations like path variable or request to param based upon your business requirements. With all these examples, I hope right now you are clear on how to accept request information and send the response information by using the Spring Boot framework. The Spring Boot framework, it is doing a lot of work behind the scenes. We as a developer, we can create the REST APIs very quickly by using these annotations and inside the Java methods, we can put our focus on writing only the business logic. All these concepts that we have discussed so far, we are going to use them a lot throughout this course, and that is going to brush up your skills as well at regular intervals of time so that by the end of this course, you will get used to all these annotations. That's the whole intention. So first I make sure that all these annotations are very clear to you by building some small REST APIs and the same concepts I'm going to reuse one more time while we are building the complex REST APIs for our job portal UI application. The details around the RequestEntity and ResponseEntity you can find in this slide.