Inside this lecture, let's try to understand anatomy of an HTTP request and HTTP response that we are going to use inside REST APIs. Anytime if a client application, if they want to send some requests to the backend application, they are going to use HTTP protocol. We already know about this story. So this is a client application and this is a backend application. So while they are trying to send the request, the request is going to come in an HTTP request format and once the request is processed, the backend application it is going to send the response by using HTTP response. So the response is always going to travel from backend  client and the request, it is always going to travel from client to backend. So first request will come, followed by the response will be sent to the client applications. Let's try to understand what exactly is allowed as part of these HTTP requests. For example, here I have given an example that client applications, they can use to send the request to the backend REST API. This is the formula that you need to remember about the HTTP request. So whatever marked with the asterisk symbol, they are optional in nature. HTTP request is a combination of HTTP Method, the Path to the REST API, any Path Variables, any Request Params, Request Headers and Request Body. As I said before, whenever a client application want to consume a REST API, first they need to provide the HTTP method that is supported by the REST API. Here they are providing the GET as a HTTP method. This method indicates what is the type of action that the client want to perform. And we know what are the valid values that anyone can send as part of this HTTP method. After the HTTP method, the client application they have to provide the path to the REST API. Here the path is api slash customers. Using this path only, the backend application it is going to identify what is the resource that the client application they are trying to interact. From a backend application, we can expose hundreds of REST APIs. For example, I can expose a REST API related to users and another REST API using profiles and another REST API related to products. So based upon these API path, the backend application is going to identify which resource the client application want to interact with. So here the path is api slash customers that means the client applications, they want to interact with the customer related resources. After the path the client applications, they can also send the path variable. Path variable is a dynamic value that client applications can send as part of the REST API path. So this is completely optional. So here after the customers, they are providing a path variable with the name 101. This is going to be completely dynamic value based upon these path variable, the backend application, they can identify the customer. In this scenario, the customer ID will be 101. Similarly, we can define any number of variables. For example, I can have a path like api slash users slash 123 slash profile slash 456. So this way I can have any number of dynamic path variables and whatever non-dynamic values that we have inside the path, those represents the static path. So while building a REST API path, that is going to have a dynamic path variable. Inside the Spring Boot application, we're going to define it with by using these curlibrace and within the curlibraces, we're going to mention some placeholder like ID. So whatever dynamic value that client application is going to send, that will be mapped to this placeholder and by using the same, we can execute our business logic. When we see the demos, this is all going to make sense to you for now, try to understand the important components inside a HTTP request. After the path variables, we can also have @uery params. Again, these Query params are also completely optional. We can pass any number of Query params. So once the path is completed, we need to have question mark followed by all the query params can be passed by using key and value. Here the query param is active and its value is true. Usually these Query params are used by the REST APIs to filter the data or sort the data or modify the data. Here I have provided the example. First we are going to have a question mark followed by key and value and a key and value. The Query params, they need to be separated by using these ambassad symbol. This way we can define any number of query params that are supported by these ambassad symbol. So these four paths, they are going to be present in the API path. These four parts we can identify by looking at the API path that the client application is trying to invoke. So this is the complete API path that they are trying to invoke. This is the HTTP method. This is the API path. This is the path variable. This is a query param. So the path and the query params, they should be separated by using question mark followed by we can define any number of query params that are supported by ambassad symbol. So this HTTP 1.1 indicates what is the version of the HTTP protocol that someone is trying to use for the communication. This is not part of the HTTP request. I just mentioned it for your reference on how an example is going to look like from the client perspective. Next the client applications, they can also send some metadata related information as part of the Request Headers. Inside these headers, they can send some authentication related information like username, passwords or content type, language details or location details. All this kind of metadata they are going to send as part of the request headers. If you see, this is an example of request header that the client application is trying to send. Every header, it is going to have a key here the key is authorization followed by colon. So the key and value, they are going to separate it by colon. After the colon, we are going to have the value for that header. Next under the request body, the client applications, they can send the data that they want to send to the backend server. Usually the body of the request, they are going to be leveraged for the POST and PUT operations. For example, if a client application is requesting my backend REST API to store the user information, the user may have a lot of details like first name, last name, mobile number, email, address details, gender details. Since such humongous data is not possible to send as part of the path variables or Request params, usually this kind of large data client applications they have to send as part of the Request Body. So this Body in other words, we can also call it as payload. Here I have given an example. Some client applications, they have sent the user data by using JSON as a syntax. This is how the JSON object is going to look like. To keep it simple, I just mentioned a single field in the example. So this is the key and this is the value. Just like how we have HTTP requests, we also have HTTP response that will travel from backend application to the client applications or the UI applications. So client applications are the applications who are trying to consume our REST APIs. Understanding the anatomy of HTTP response is very simple. It is going to have status code, which is a mandatory field followed by Headers and Body. Both Headers and Body are optional. As part of the status code, the backend is going to send the status like 200 or 400. These status codes we already discussed in the previous lecture. By looking at this status code, the client application, they can understand whether the request to processing is successful or not. Next, as part of the response Headers, the backend application they can send some metadata or any extra information that they want to share to the client applications. For example, content type is one famous response header. Under this content type header, the value is application slash JSON. By sending this value inside the response header, the backend application it is telling to the client application that I'm sending the response data by using the JSON format. Next under the response body, the actual data will be returned by the backend server. And this data usually it will be in JSON format, but if required we can also send it using XML format as well. For example, if a client application asked for the user details with an ID 123, we're going to send all the user details as part of this ResponseBody. I hope you're clear with the anatomy of the HTTP request and HTTP response. From the next lecture, I'm going to build some simple REST APIs that I'm going to use to show you the demos around the anatomy of HTTP request and HTTP response. Once we are clear with the basics of REST APIs, then we can start building actual REST APIs that are required by our UI application.