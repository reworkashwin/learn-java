The next part of HTTP requests that we are going to focus is headers. As part of the HTTP requests, the client applications, they can send some metadata related information inside the RequestHeaders. For example, inside the postman, if you open any of the requests, there is a section with the name headers. By default, postman is going to send all these headers for each and every request. For example, by using this header with the name user-agent, the postman, it is going to send this value. By looking at this value, our backend application can easily identify that so-and-so REST API is invoked by a postman client application. This way, any kind of metadata related information like user location, user language, all those details we should be able to send by using these headers. Let's try to understand how to read these headers inside our backend REST APIs. To show you the demo around the request headers, I am going to create a new REST API path with the value headers. This API, it is going to return the response by using String. Let me update the method name as readRequestHeaders(). Whenever we want to read a request header, we need to use an annotation @RequestHeader. To this annotation, we need to pass what is the header name that we want to read. For example, inside the postman, we have a header with the name user-agent. The same I am going to mention here within the double quotes, followed by I am going to mention the method input parameter data type as String and the input parameter variable name as user-agent. Similarly, I should be able to read multiple headers by mentioning these request header any number of times. This time, I am going to read a header which I am going to send manually from the postman. The header is user-location. Let me mention the input parameter name as string user-location. Now these variables User-Agent and User-Location, I should be able to use inside my method logic. What I can do is, I can return a String value saying that received userAgent plus userLocation. This is the output that I want to send as a response from the Java method. Now from the postman, what I will do is, I will try to create a new REST API with the name RequestHeaders. Here I need to mention the path as api/dummy/users followed by headers. Before I try to invoke this API, under the headers, I need to make sure that User-Agent and User-Location header is present. By default, User-Agent header will be populated by postman. I will try to create a new header with the name as User-Location. To this, I am trying to pass a value Hyderabad. If I click on this send button, I am going to get the response received so and so user agent value with so and so user location. In case if I am not sending any of these mandatory request-headers, I am going to get bad request response. To make any of these request-headers as optional, we can use required as false, but before that we need to make sure to assign this User-Location to either name or to the value parameter followed by I can mention required as false. Now this time, even though if I am not sending user-location, I am going to get the output post-runtime as so and so value and the location is null. If required, we can also define a default value to this header so that if the client application if they are not sending this value inside this header, the spring boot it is going to inject this default value. This time, if I try to invoke the same REST API without user-location, I am going to get the output as Hyderabad. In case if I am sending this user-location with some other value like Delhi, I am going to get the Delhi as a location inside my response. We should be able to read multiple request-headers using Map. Let me copy this REST API and paste it here. I'm going to update the path to /header/map and the method name I'm going to update to readRequestHeadersWithMap(). And here after this @RequestHeader annotation, this time I need to mention Map followed by String, String with the variable name as requestHeaders. Now using these requestHeaders, I should be able to invoke the get() method and to this get() method, I need to pass the name of the header. Let me do the same for userLocation as well. I'm going to restart my application and this time to this path, I'm going to append map path as well. So if I try to send the request, I'm getting null value under both header values. Seems I have made a mistake. I'm going to keep a breakpoint here and send the request. If we try to look at these requestHeaders map, it has total seven request-headers that my postman is sending. So user-location is the one that we are sending manually, but remaining all the six headers the postman is sending. Whenever we are using this Map, it is very clear that using this map, we should be able to access all the request-headers that the client application is trying to send. While I'm trying to fetch these values from the Map, I need to mention these values like user-location, user-agent. I made a mistake around that. So here instead of User-Agent, I need to mention this user-agent. In the similar lines, I should also mention User-Location. This time, if I invoke the same REST API, I'm going to get the successful response. Apart from Map, there is also another approach to read all the headers available inside the request. Let me create one more REST API. This time I'm going to update the path to http-headers. I need to update the method name as well. The method name will be readRequestHeadersWithHttpHeaders(). After this @RequestHeader annotation, this time instead of using Map, I'm going to use HttpHeaders, which is present inside the Spring framework. Please make sure you are importing this from the Spring package, not from the Java package. So now using these requestHeaders, I should be able to invoke the same get() method and to the same, I can send the header name. I can keep a breakpoint here so that we can debug what values we are getting under these http headers object. Let me restart my application. Inside the postman, I'm updating the path to http-headers. As soon as I click on the send button, the breakpoint is going to stop here. And under these http headers object, I have around 7 headers. So these are all the headers. Under the user location, I have the value as Delhi. So this way we should be able to read the headers by using http headers as well. Inside the postman, we got the successful output. The code snippets that we have discussed around the @RequestHeader annotation, I have mentioned them in this slide for your reference. In the next lecture, let's try to understand how to read the entire data available as part of the @RequestBody.