So far, we are building GET Rest APIs and by using these GET Rest APIs, we can read the existing data. It's like read-only operation. The client applications, they are going to ask what kind of data that they are looking for by sending the required information as part of the path variables or the query parents. But when coming to the POST RestAPIs or PUT or PATCH RestAPIs, the client applications, they may send an entire object inside the @RequestBody. For example, if I'm exposing a Rest API, which is going to allow the client applications to create a brand new user inside the database. In this kind of scenario, the client applications, they are going to send all the user related information inside the @RequestBody, like the name of the user, email of the user, mobile number, gender, his address details. Based upon our business requirement, we can accept any kind of data. To read that data that is coming as part of the request body, we need to use the @RequestBody annotation. Just like how we have @RequestHeader annotation, in the similar lines, we have @RequestBody annotation. This time, let me create a @PostMapping annotation, and I'm not going to mention any path against to this annotation because whatever parent path that we have, if the client application, if they're mentioning these, and if they're invoking the POST Rest API, then the request will be forwarded to this method. Let me create the method as public String createUser(). And to this method, I need to mention the annotation @RequestBody. Now the challenge comes, previously, we are using the data type of the method input parameter as a String or Long. In all the places we are using primitive data types. By using these primitive data types, the client applications, they can only share a single data. So in this createUser() scenario, we are expecting a lot of data inside the @RequestBody. For example, here, I'm going to create a new request with the name @RequestBody. And this request, it is going to follow HTTP POST method. Coming to the path, it will be api/users. Now by using this body tab, I need to select this raw option. And in this dropdown, I need to make sure I'm selecting the JSON because we are going to use the JSON as a format to communicate the data between the client applications and the backend server. To start a JSON object, we need to mention curly braces. And inside this JSON object, we can create any number of fields related to the user. For example, I can mention the name and the name will be Madan Reddy. Similarly, I'm going to mention email. And to this email, the value will be tutor@ezbytes.com. Next I'm going to mention gender. So this way, I can send any amount of data as part of the @RequestBody. So whatever data that we are going to send inside the body of the request, that is not going to be visible to the end user inside the address URL, whereas whatever data that we try to share by using path variables or the query params, that is going to be clearly visible inside the request URL or inside the address URL. That's one of the advantages of sending the data as part of the @RequestBody. Usually the HTTP methods like POST, PUT, PATCH, they're going to send the data to the backend by using this @RequestBody option. As part of this @RequestBody, we can send any amount of data. There are no limitations on what amount of data that you can send to the backend server. Now when a client application is sending the user related information like this, my backend application, it is not going to understand because this is in JSON format. Whereas inside Java, we deal everything with the Java objects. So that's why we need to create a Java class representing same kind of structure. What I'll do is, I'll create a new package with the name dto, dto means Data Transfer Objects. Inside this dto package, I'm going to create all the classes which are going to help me to transfer the data from client to server and server to client. I'm going to explain about dto pattern and why we should use it in the coming sections. For now, let's try to create a class inside this package. The class name will be UserDto. Here instead of creating it as a Java class, I'm going to create it as a record. So record is a new feature that got introduced as part of Java 16. I discussed about this in detail inside my Java course. By using record inside Java, we can create a Java class with a given set of fields, which is going to have only the getter methods, but not the setter methods. So whenever we are creating a record behind the scenes, the Java library, it is going to create a constructor and a getter methods, and it is also going to mark all the Java fields as final. Once the object of this record is created, no one can change the data inside the object. The objects of record are always immutable. Once they are created, we can't edit the content inside them. So I need to create the fields inside this record class. Within these parentheses, we need to mention the name of the fields. It should be String name, followed by String email and String gender. If you closely look at these field names, I make sure to use the same field names that I have defined inside my JSON object, name, email, gender. We should use the exactly same values on the backend side. When a client is sending this JSON object behind the scenes, the Spring Boot framework is going to take care of converting the JSON data into a Java object by looking for the exact matching fields. If an exact matching field is identified, it is going to copy the data into the Java object. So once the record class is ready, we can go to this UserController and here, after the @RequestBody, we need to mention the data type of the method input parameter as UserDto, which we just now created. The variable name can be anything. Now I should be able to use this UserDto object inside my method logic. From my method, for now, I'm going to return a simple output saying that created user with the data, whatever data that we have received. Let me restart my application. From the postman, if I try to invoke this API, I'm going to get the output saying that created user with the data with so-and-so name, email and gender. Did you see whatever data that we're trying to send here? The same is mapped to the UserDto object. If I try to change these, any of the fields instead of name, if I mention it as first name and send the request this time under the name inside the UserDto object, I have null because Spring Boot can't assume this first name is same as name. That's why always please make sure to use the exact field names on the backend so that the mapping between JSON and the Java object is going to work without any issues. Usually in the real enterprise applications, the developers from the client applications and the developers who are developing the REST APIs, they're going to have some discussions on what is the format of the request to data that they are looking for a given REST API under the payload of the request. For example, if a developer is developing this REST API, is going to communicate to the client applications to send the user information only by using these fields like name, email and gender. Here, you may have a question, which is, Madan, we are trying to accept a lot of data from the client application, like in terms of query params, path variables and request body. What is going to happen if the client application is sending some invalid data? For example, here, instead of sending a valid email, they may send some invalid email value without following the email format. And similarly, under the gender, instead of sending male or female, they may send some random value or numeric value. It's a great question. Let me try to answer the same. Whenever a backend developer is building REST API, the developer is also going to take care of performing the validations on the input data that we are getting from the client application. In the coming sections, I'm going to put some good amount of efforts on explaining you on how to perform the validations on the request to data that we receive while building the REST APIs. So please hold your thoughts around the validations when the time comes, I'm going to discuss about it in detail. Before I try to close this lecture, let me highlight to you that behind the scenes, SpringBoot, it is going to leverage a library called Jackson. By using this library related to JSON, the SpringBoot is going to perform the conversion of JSON to Java objects and Java objects to JSON. So far, we're only worrying about the request, but while sending the response as well, sometimes instead of sending a single string, we may want to send an entire Java object as an output. Under such scenarios also, though our method is returning a Java object, but behind the scenes, the SpringBoot framework, it is going to take care of converting that returned Java object into a JSON format so that eventually the client applications, they are going to receive the response in a JSON format. The example that we discussed around the @RequestBody is mentioned in this slide for your reference.