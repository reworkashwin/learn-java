Based upon our discussion in the previous lecture, an HTTP request can contain all these values. Inside this lecture, let's try to understand how to accept these path variables inside our REST API logic. We already discussed on how to configure HTTP method and path. The next optional value that we may receive from the client application is path variables. When invoking an REST API, the client applications they should be able to send some path variables like you can see inside this path. Inside this REST API path, api/dummy/users is the REST API path followed by 123 is a path variable because it is a dynamic value that get replaced with any value. Followed by, we again have static path with the name POST. After the POST, we again have another path variable with the value 456. Inside this path, we have two different path variables. This way, we can have any number of path variables at any location inside a REST API path. By invoking this REST API path, client applications, they are trying to ask the backend server to provide the user details with the user id 123 and against this user, we are going to have the POST or comments that this user posted and in the list of posts that this user posted, I am more interested in a specific post with the id 456. So this is what the client applications they mean when they are invoking this type of REST API path. As a Java backend developer, we should also understand on how to build REST API path that can accept dynamic path variables. Inside controller package, we already have a DemoController which is having a path with the name home. Now I'm going to create another controller with the name UserController. On top of this class, we need to mention @RestController annotation because we are looking to expose REST API path from this class. What I'll do is, I'll try to copy this method from the DemoController into this UserController. As I said before first, we need to decide whether our RESTAPI method should support HTTP GET method, POST method or any other method. Accordingly, we need to mention this annotation. In case if I'm looking to expose a REST API path that supports a HTTP POST method, then I need to mention the annotation @PostMapping. For our example, I'll go with the @GetMapping. Throughout this course, we are going to explore all the HTTP methods and the associated annotations. Now coming to the path, I need to copy this entire path and paste it here but I can't use this exact path because this REST API, it is always going to look for the user 123 with the POST 456. But we want these values to be dynamic. That's why as a very first step, we need to update the path with a placeholder. Instead of 123 I'm going to mention the placeholder variable as userId Similarly here also, I'm going to have a placeholder with the variable as postId. With this now anytime if the client applications, if they try to invoke this REST API path, any value that they try to send at this location will be mapped to this UserID by the SpringBoot framework. Behind the scenes, the Spring and SpringBoot framework, they are going to do all this magic for us. We don't have to worry on how to extract these variables from the HTTP request. Let me update the method name to searchUserPostWithmultiPathVariables(). Now in order to use the values that we are going to get at these placeholders like userId and postId inside our Java method, we need to make sure to add some method input parameters. Since we are looking to accept the path variables into our Java method, we need to mention an annotation @PathVariable followed by, I need to mention what is the data type of my userId variable. . Since we are expecting a numeric value, I'm mentioning the long as a data type followed by the same value we need to mention as a method input parameter. The variable name is userId, the same I'm trying to mention here. Since we are supporting two different path variables, we need to mention one more method input parameter by mentioning @PathVariable annotation along with these Long and postId. So these variable names and these method input parameters, they need to match exactly. Otherwise, the data transfer from this variable to this method input parameter by the SpringBoot framework or the Spring framework is not going to happen. Once I accepted these variables as an input parameters to my method, inside my method logic, I should be able to use them. For example, I can return a string statement from my Java method saying that Fetched user with id so-and-so userId and postId with so-and-so postId. So this is a dynamic String value where we are trying to append the userId and postId value to the String. So let me save this,do the build once the build is completed, I'm going to restart my application. From the browser, I'll try to invoke this complete path. So whatever value that we are trying to send here, those dynamic values will be available for our Java method logic as well. As soon as I press enter, I'm going to get the response based upon the business logic that we have written. For example, if I replace this 456 with 458, the same I can see inside the response as well. That means we are able to read the path variables by using these annotations. I can also keep a breakpoint here. Now if I try to invoke this path, this time with the 460 as a postId and 124 as a userId, you'll be able to see under the userId, we have a value called 124 and under the postId, we have a value called 460. So this is the beauty of Spring Boot. As a developer, we don't have to worry much. We just have to use some annotations and certain configurations and with that, the heavy lifting tasks like extracting these data from the HTTP request that is sent by the client application is going to be taken care of by the Spring Boot framework. With these code changes, now if someone is trying to invoke the API path without passing these dynamic values, then they are going to get 404 error. For example here, if I try to invoke the API path without passing these 460, I'm getting a 404 error. That means whenever client want to invoke this REST API path, they should definitely make sure to provide both this userId and postId. In case if you want to make a particular pathVariable as optional, you should be able to achieve that by simply mentioning required as false. With this, now the PathVariable postId, it is an optional PathVariable. But there is no supporting API path that we have configured because inside the API path, we are always expecting the postId and post as a mandatory, whereas here we have marked this as optional. To make this work, either you can create one more RESTAPI with something like this, where you can update the REST API path as api/ dummy /user/ userId and you can update the method name also. And here you can maintain the required is equal to false, whereas in the top method, you can maintain the PathVariable as mandatory. That's one option. Instead of that, we can also follow a better alternative. As we discussed previously, a REST API method can support multiple API paths. So here what I can do is, I can mention curly braces and inside this curly braces, I'm going to define one more API path. This time I'll mention the API path up to this userId only. So whenever a client application, if they are passing the PathVariable postId, then this API path is going to match and this method will be invoked. In case if the client application, if they're invoking this API path then still this java method is going to work. But since the postId PathVariable is not sent by the client application, the postId value will be null. Inside our business logic, we should handle these null postId value as well. What I'll do is, inside my method logic I'll first create a response String variable followed by I'll try to have a if condition where I'm going to check if the postId is null, I will update this response String variable to have the value like Fetched user with id userId. So let me mention the same here. Otherwise inside the else block, I'm going to populate the response with this value. So let me type response is equal to Fetch user with id userId and postId with this postId and towards the end, I'm going to return the response from this method. So let me save this, do the build once the build is completed, I'll restart my application. Now from the browser, if we try to invoke the complete path by providing userId and postId, we are going to get this output. In case if I'm invoking only the user and userId path, then I'm going to get this output. This way, we should be able to play with the Path Variables based upon our requirements. Let's continue this discussion of PathVariable inside the next lecture as well.