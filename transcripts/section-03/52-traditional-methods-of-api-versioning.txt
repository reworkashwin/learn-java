As part of this lecture, I'm going to show you the demo on how to implement API versioning without taking any help of Spring or Spring Boot framework. In the next lecture, I'm going to show you how to achieve the same by using the Spring and Spring Boot framework. What I'll do is, inside this controller, I'm going to create a new Java class with the name LegacyVersionController. Whatever manual style of handling the API Versioning approach that we are going to discuss inside this lecture, it is a legacy approach, but still I want to discuss about this because even today, many developers in the organization, they are still using these legacy approaches. Whatever recommended or modern approach that we are going to discuss in the next lecture, that is only available from the Spring 7 version and Spring Boot 4 version. Since even today many of the real projects, they are using older versions of Spring and Spring Boot I want to discuss about this approach as well. On top of this class, I'm going to mention @RestToController annotation followed by @RequestMapping. Let me mention the API path as /api/legacy /versions. . This is the parent path and inside this controller, I'm going to paste a couple of REST API methods logic. You can get the same from GitHub repo as well. Just to save some time, I'm not typing this code. The first API, it is a GET API it supports three different paths like someone can invoke this API by simply mentioning this path or by mentioning the forward slash after the versions or in case if they are mentioning v1 after this parent path, then this logic is going to be executed. Whereas if they are mentioning v2, this logic is going to be executed. So what we want to return as part of these responses, it's up to us. This way for the same REST API path, we are trying to provide various versions. So this method supports v1 version. I configure these empty path and forward slash paths so that this method is going to be invoked even if one of the client is not sending v1 as well. If they simply invoke this parent path, still I want to fall back into these v1 method. That's the whole intention of configuring these empty path and forward slash. Otherwise, if you don't like to have a fallback and you always expect either v1 or v2, then you can get rid of these first two API paths. Here instead of returning simple String, let's try to return as ResponseEntity of String, and from the return statement, I'm going to mention ResponseEntity.ok, and to this ok() method, I'll pass the String data. In the similar lines, let me update the second method as well. Let me copy this entire statement here and replace the String value. Now I can get rid of the last written statement. Let me save this. Once the build is completed, I'm going to restart my application. Inside the postman, under the spring collection, I have created a new folder with the name Versioning. And in this folder, I created two different folders, one with the name Legacy and the other one with the name spring 7. Since we are following right now legacy approach, let me click on this folder and there is a request with the name PathVersioning. If a client application is sending the request by mentioning v2, then we are going to send the response saying that response from path v2 version API 2.0.0, whereas if I try to mention v1, I'm going to get the response from the first method if I don't mention this v1 still, I'm going to get the response from the first method. I hope you're clear on how these PathVersioning is working based upon the path variable we're going to decide which version of the REST API needs to be invoked. If you don't like these v1 or v2, you can also follow something like 1.0 or 1.0.0. So usually the versions, they are going to be maintained by using three digits. The first digit indicates the major release, and the second digit indicates the minor release number, and the last digit indicates the patch release number. Let's imagine there is a company which they release their APIs with a version 1.0.0. If they are making some minor cosmetic changes, they are going to update this version number to 1.0.1. Let's imagine after a few months, they are making some enhancements which are not major, but they are minor in nature. Whenever they are making minor changes, they are going to increment the second number. When they increment the second number, the third number will reset to zero. Let's imagine after six months or after one year, they are making a lot of critical changes and all these are major changes. So with that, now the first number will be incremented from one to two and all the remaining numbers, they will reset to zero. So this is how the life cycle of the version numbers or release numbers, they are going to happen in the industry. The same kind of release versioning numbers are being followed by the spring team as well. Whenever you look at the spring framework version number or spring boot version number, you're going to have three digits, which are separated by dot symbols. Whether you want to use these kinds of numbers or whether you want to use V1 or V2, it's completely up to you, so please discuss with your team internally and accordingly, you can decide on how you want to maintain the versioning numbers of your APIs. So this is the PathVersioning. Next let's try to understand how the RequestParamVersioning is going to work. To show the demo around the RequestParam, I'm going to copy both these methods again followed by I'll get rid of all these path configurations. Let me do the same for a second method as well. I will update the method name as defaultRequestParamVersion(). The same I'm going to update here coming to the second method name I'm going to mention it as V2RequestParamVersion(). Let me update the same inside the response as well. Now, what I can do is, by using these @GetMapping annotation, I can invoke an attribute with the name params. To these params, I'm going to mention a @RequestParam, the @RequestParam, it is going to have a param name as version, and if the value is mentioned as 1, then I want this REST API method to be invoked. In the similar lines, I can copy these annotation and paste it here. This time I'm going to mention version as 2. So whenever someone is sending QueryParam with the version as 2, this method is going to be invoked. So let me save all these changes, restart the application. Inside the postman, I'll open the second request with the name RequestParamVersioning. So here, if I mention the version as 1, the first method will be invoked. The output is response from defaultRequestParamVersion API 1.0.0. Whereas if I mentioned the version as 2 under the QueryParams, then I'm going to get the second method output. In case if I'm not sending any of these QueryParams, then do you know which REST API it is going to invoke . It is going to invoke the first rest API because for the first rest API, we are allowing blank value as well, and with that, we are going to get the output from this method. So let me invoke this. As you can see, we are getting the expected output. Whereas if you are trying to send some invalid RequestParam name, like instead of version, if you are sending V as 2, you're going to get the output from the first method. Because whatever QueryParam that we are sending, it is not matching with any of the rest API methods. It is falling back to these first method where the empty path is allowed after these parent path. Once I mentioned the correct QueryParams, I'm going to invoke the actual rest API that supports version2 by using the RequestParam. Next let me show you on how to leverage the request headers to perform the API versioning. I'm going to copy these two methods and paste again. First, let me update the method names and the String value inside the methods the method name I have updated to defaultRequestHeaderVersion. Coming to the last method, I'm updating its name to V2RequestHeaderVersion. The same I'm updating here as well. Now instead of params, since we are looking to use the headers, I need to invoke the parameter with the name headers, which is supported by the @GetMapping annotation, regardless whether you are using @GetMapping or @PostMapping or @RequestMapping for all those annotations, these attributes are parameters like headers, params, they are supported. So now under this header, we need to mention what is the header name that we want to use along with its value. The header name that I want to consider is X-API-VERSION and the value will be 1. Whenever a client is sending the @RequestHeader with this 1 value, this method is going to be invoked, whereas if the same client is sending the same header with the value 2, then this method is going to be invoked. Let me save these do the build and restart my application. Inside the postman, I'm going to open the third request with the name RequestHeadersVersioning. So here I'm simply mentioning the complete parent path, api legacy versions and under the headers, I created a header with the name X-API-VERSION. If I'm sending a value 1, the first method is going to be invoked when I'm sending 2 under this header, I'm getting an unexpected output. I'm getting the output from the defaultPathVersion method. Let me see what is the problem. Next to these @GetMapping, I'm still using params, instead it should be headers. After making this change, I should get a proper response. If I send the request with these header and value as 2, this time I'm getting the output from the method v2RequestHeaderVersion with the value 2.0.0. Now let's see the last approach where we are going to use the media type to control the versioning of the REST APIs. I'm going to copy these two methods and paste again. The first method name I will update to defaultMediaTypeVersion. The same I'll update inside the method as well. Coming to the second method, I'll mention the method name as v2MediaTypeVersion and inside the method Body() as well. This time, since we are relying on the MediaType, we need to invoke a param with the name producers. So the backend application, it is always going to use these producers to tell to the client application, what kind of media that it is going to produce, whereas the client application, it should always send the accept header to convey to the backend server on what kind of MediaType it is going to accept. Next to these producers, I'm going to mention the value application/vnd.eazyapp.v1+ json. . So I have given my own app name like eazy app. This name can be anything and for the v1 REST API, we need to make sure we are using v1 instead of v1, you can also mention 1 or 1.0.0. It's up to you. Similarly here, I'm going to mention it with the v2 value. Now I'll go to the postman. Inside the postman, I'm going to open this last request with the name MediaTypeVersioning. Here when a client application is invoking this path, they need to send a RequestHeader with the name Accept. So by default, postman is going to provide another accept to header this is a default Acceptheader, which is going to send these wildcard value to the backend. That means the client is okay to accept any kind of response. But since we can't edit these default header provided by the postman, we need to manually create another header with the name accept and with the value that we have configured in the backend. When we are sending this v2 plus json, we are going to get the expected output. But since I made a mistake one more time here, instead of headers, we need to mention producers. This time, if I invoke this API, I should get the expected output. If I try to update this header value with v1, I'm going to get the output from the first REST API. In case if I'm not sending this, then the first Java method inside our control is going to be invoked. What is that first Java method? It is the defaultPathVersion. Whereas if I send this accept, I'm going to get the output from the method defaultMediaTypeVersion. All the four legacy approaches that we have discussed inside this lecture, I have documented them in these two slides. I also highlighted these approaches are legacy and using those approaches is not recommended. If you are using the latest versions of Spring Framework and Spring Boot, but still these approaches, they are not bad approaches it's just that you will end up writing a lot of manual code. When I'm going to show you the modern approach in the next lecture, you will realize how easy it is to implement the API versioning by using the latest version of Spring Framework.