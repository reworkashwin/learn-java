While accepting the PathVariable by using this annotation, we need to make sure that the method input parameter variable name is matching exactly with the placeholder that we have defined here. But what if there is a scenario where we want to use a different name for this method input parameter? We should be able to achieve that as well. Let me show you a demo of the same. I'm going to copy this entire method and paste it here. Let me get rid of the second RestApi path followed by instead of post this time I'll mention orders and inside this placeholder I'll mention orderId. Let me update the method name to searchUserOrderWithMultiPathVariables. I'll remove this required is equal to false for the second path variable. Let me update this second input parameter to orderId. I need to update the method logic as well. I will get rid of all these if else condition. I'm going to simply return a String statement saying that Fetched user with id so and so userId and orderId with so and so orderId. All this story we already know. Now we want to use a different method input parameter. For example here instead of userId, I may want to use the customerId and the same I want to leverage inside my method logic as well. With this change, the data mapping from this user id with this customer id, it is not going to happen. I can also show you a demo. Let me restart my application. Once it is restarted from my browser, I'm trying to invoke this path api/dummy/users with so and so user id/orders with so and so order id. As soon as I press enter, I got an error message. Inside the server console logs, if you try to see the error, there is an runtime exception happened with these logs required URI template variable customer id for method parameter type long is not present. That means whatever data that is coming to the user id, the same, the framework is trying to assign to this customer id. But since these variable names are completely different, it is not able to copy the data. By using these @PathVariable annotation, we need to invoke a parameter with the name name To this name we can mention the actual placeholder, which is user id, with these changes now the spring framework or spring boot framework, they are going to have a clue that this placeholder value of user id should be mapped to the customer id because we have given a clue here by using these name parameter. This time we can try to test our REST API path one more time. Before that, we need to restart our application. Since I have made a spelling mistake here. Let me update this to orderId the same I'm going to mention in all the places. Let me save these changes do the build once the build is completed, I'm going to restart my application. Now from the browser, if I try to invoke the same API path, this time I got a successful output Fetched user with so-and-so id and so-and-so order id. I hope you are clear on how to use these name parameter. As a next step, let's try to discuss about another scenario that you may face while using these @PathVariable annotation. Let's imagine your REST API path has too many path variables, like four or five path variables. Accepting all those path variables manually one by one by using this @PathVariable annotation, it is going to be a cumbersome process and it is also going to make your method code clumsy and lengthy. In these kinds of scenarios, instead of defining this path variable multiple times, we should be able to accept all PathVariables at a time by using a single method input parameter. Let me show you how to do the same. What I'll do is, I'll try to copy this method and paste it here this time I'm going to update the REST API path to api/dummy/user/userid/address. After the address, I'm going to have the placeholder value as addressId. Let me update the variable name to searchUserAddressWithMultiPathVariables. I'm going to get rid of these multiple input parameters. Instead, I'm going to have a single method input parameter. First I need to mention @PathVariable as usual, followed by I need to use the map from the java collections. So this map, it is going to have key and value, both of type String. Let me mention the variable name as pathVariablesMap. So with this what is going to happen is, the spring boot framework or the spring framework, whatever placeholder values that it sees at runtime, all those variables they are going to be copied into this map. So the key will be whatever placeholder you have mentioned here and the value will be whatever dynamic value that clients are trying to send. Inside the business logic, you need to use these pathVariablesMap and invoke the get() method. To this get() method, we need to mention the key as userId and similarly here also, I need to use pathVariablesMap and invoke get()method. To this get() method, I need to mention the key as addressId. Let me update the statement here from order to addressId. So this is how you can accept any number of PathVariables as an input by using map as an input variable data type. Let me save these changes and restart the application. We can try to test the new REST API path. The path is, instead of orders, we need to mention address as soon as I press enter, I'm going to get the output from the new REST API that we have developed. All the concepts and the examples that we have discussed around the PathVariable, I have mentioned them in these two slides for your reference. Always feel free to refer these slides whenever you want to quickly brush up the concepts that we have discussed throughout this course.