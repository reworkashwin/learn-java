To create a REST API inside our Spring Boot backend application, on top of this DemoController class, we have mentioned the stereotype annotation with the name @RestController. But if you recall our Spring Core concept, in order to create a Bean from this class, we need to tell to the Spring Framework, the location where we have defined all the stereotype annotations. On top of that, we are supposed to use @Configuration annotation. How we are going to communicate that? By using annotations like @Configuration and @ComponentScan. But nowhere we have defined those annotations inside our application, but still our code is working fine. We are able to invoke our REST API. All this magic is happening due to a single annotation inside our application. The annotation is @SpringBootApplication. Whenever you are working on a Spring Boot application, if you want to know what is the main class that you can use to start your backend application, you need to look for this annotation. You just have to go to the search menu and select this text. And here, if you type the annotation, it is going to show you the class which is acting as a main class inside the Spring Boot application. Our project is a very simple project and we should be able to manually search this class. But in real enterprise applications, we are going to have thousands of classes. To find the main class in such a big project, it's a cumbersome process. That's why I recommend you searching for this annotation. Now let's try to understand what is this annotation and how it is able to perform the magic of automatically creating the bins without redefining @Configuration and @ComponentScan annotation. If you try to open the source code of this annotation, this is majorly a combination of three different annotations. The first annotation is @SpringBootConfiguration followed by A@EnableConfiguration and @ComponentScan. Each of these annotations, they have a purpose. Let's try to understand these annotations in detail. The concepts around this Spring Boot application annotation is a very good interview topic as well. You will see very often inside the interviews, people asking about this annotation. As we discussed, it is a shortcut annotation that combines three different annotations. The first one is SpringBootConfiguration. This is very similar to @Configuration that we have discussed while exploring the Spring Core concept. This annotation, it is going to tell to the Spring Boot framework to mark this main class as a configuration class. So any beans definitions or any other configurations that you write inside these main class, they does not require any @Configuration annotation because behind the scenes, this main annotation, it is having this annotation as one of the annotation. The next annotation is @EnableAutoConfiguration. This annotation is very powerful annotation. I'm going to show you a demo of auto configuration in the next lecture. By using this annotation, we are telling to the Spring Boot framework, please automatically configure all the bins that are required to my web application based upon the dependencies that I have added in the pom.xml. Inside our pom.xml, if there is a dependency related to the web application, then behind the scenes, Spring Boot framework is going to create hundreds of bins that are going to help us to expose our REST APIs, to deploy these REST APIs into an embedded Tomcat server. And similarly, if you are adding a dependency related to database, then any database interaction related beans also is going to be automatically configured by the Spring Boot framework. We as developers, we can only focus on defining the bins that are required for our business logic, like for the REST APIs or for the service class beans or repository beans, where we are going to write majority of our business logic. The last annotation is around the @ComponentScan. This annotation also, you're already familiar with, the bins that are required by our web application based upon the dependencies that we add, they're going to be automatically created by using the auto configuration concept. But how about the bins that are created by the developer? In our application, we have a class so far that we have annotated with the @RestController stereotype annotation. So behind the scenes, someone has to create the bean of this class. Then only our REST APIs are going to be exposed by deploying into the embedded Tomcat server. So who is doing that job? It is the @ComponentScan. Since this scan is part of the Spring Boot application annotation, it is going to take care of scanning and registering the Spring components, like all the Java classes, which we have annotated with the @Component, @Service, @Controller or @Repository, regardless of what stereotype annotation you're trying to use, all the beans of those classes will be created by the Spring Boot framework and all this magic is happening because of this annotation. So why Spring Boot team, they have introduced this annotation? They want to make our life easy. They don't want to developer manually defining @Configuration annotation or defining @ComponentScan annotation. Since they want Spring Boot to take care of the majority of the startup tasks, they have introduced this Spring Boot application. So this is going to save our time, no need of long XML configurations or no need of mentioning other annotations manually. And by using the auto configuration concept, it is going to detect all our dependencies and configure the required bins behind the scenes during the startup automatically. With all these advantages, the setup of Spring Boot application is going to be very simple. Everything is going to work with a minimal code. To define a REST API, do you know how many lines of code we have written? We hardly written 10 lines of code. I challenge you, if you want to define same REST API using Java without using any framework, you may end up writing more than 500 lines of code. And with the Spring Framework alone, you will end up writing at least 100 lines of code. Whatever class that we have annotated with this annotation, it is going to act as a main class. And inside this main class, we are also going to have main method. The main method, it is going to serve as the application entry point following the standard Java convention. Inside CodeJava also, the main method is the entry point to our Java program. The same standard is also followed inside the Spring Boot application as well. Whenever you want to start a Spring Boot backend application, you need to run the main method that you are going to see inside the class where this annotation is present. There are multiple approaches to start a Spring Boot application. The most easiest one is inside your IDE like IntelliJ, you are going to have a start or debug button next to the main method or next to this class name. In case if you want to start with the maven build tool or Gradle build tool, you can use these commands. If you type this command from the location where the pom.xml is present, it is going to start your Spring Boot backend application. The command is mvn spring-boot:run. In case if you are using Gradle build tool, you need to go to the place where the build.gradle file is present. From the same location, you need to run this command gradle boot run. I hope you are clear about this @SpringBootApplication annotation. Let me go to the main class, which is the backend application. Here I'm going to stop my application. This time I'm going to share you an important information. By default, the @ComponentScan that we have under this @SpringBootApplication annotation, it is going to look for the stereotype annotated classes under the packages where this main class is present. If you see, the main class backend application, it is present under the package com.easybytes.backend. In this package or inside the sub packages, it is going to scan for the classes which has stereotype annotation, followed by it is going to create the beans out of them. In case if you have defined the Java classes and mentioned the stereotype annotations in a package which is outside of this main package, then you need to manually tell to the Spring Boot framework about all the package details where you have defined stereotype annotation. I can show you a demo as well. Maybe what I'll do is, I'll try to create a new Java package with the name com.easybytes.outside. This is just a package name that I'm trying to give. This name can be anything. Into this outside, I can move this controller package and with that, I can delete the package present under the backend. So now if you try to understand the backend package is the main package because the main class is present and this outside package, it is an outside package for backend. That's why this time after doing the build, if you try to start our application, the REST APIs that we have developed inside the DemoController, they are not going to be created and exposed. Let me start my backend application by using these debug option. Our application started properly at the default port 8080 by using the Tomcat server. But if you try to test the REST API, it will not work. From the browser, I'm trying to invoke the home REST API. If I press enter, I'm getting a 404 error. That means there is no REST API exposed with this path. If you ever present in these kinds of scenarios to fix this problem, you need to manually add @ComponentsScan one more time. And by using these components scan, we need to invoke the base packages followed by, we need to provide all the list of packages where we have defined stereotype annotations. Where did we define? We have defined under this package. Let me copy these and paste it here. So now I'm going to do the build and restart the application. This time we should be able to access our REST API. I'm refreshing this page and I got the expected output. So whatever components scan that is present inside the spring boot application, it is only going to scan for the classes that has stereotype annotations. If they are present inside the same package or under the sub packages where this main class is present. But the moment you start defining all your beans outside of this package, then you need to make sure to manually define all the packages, including the outside packages and inside packages as well. Because when you define your own components scan, the internal components scan functionality is going to be disabled by the spring boot framework. But you will see developers rarely defining an outside package. My recommendation is always define your packages under the main package itself. So let me create a new package with the name controller and into this, I'm going to paste this DemoController. Now I'm going to delete this outside package. And with that, we can comment these @ComponentsScan annotation. Let me do the build and restart my application. Once the application is restarted, if I try to refresh this page, I'm still getting the same response.