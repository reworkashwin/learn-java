Inside the industry, developers they use these four famous approaches to implement API versioning. The first approach is, by using the Path Versioning or URI Versioning. Inside the REST API path itself, we are going to have version numbers like v1 and v2 and so on. Instead of this v1 and v2, you can also have other values like 1, 2. It's up to your business logic what value you want to maintain to represent the various versions of REST APIs. In the similar lines, we also have RequestParameterVersioning. If you don't like maintaining a path variable representing the API versioning, you can leverage the Query Params or RequestParams as well. Here, the client applications, they have an option to send a QueryParamVersion and with a value like 1,2. . Again, these Query Param name and value can be anything based upon your business requirements. In case if the developers, they don't want to pollute their API paths by using these versioning numbers, they can go with the last two approaches. The third approach is by using the request headers. Since client applications can send any kind of metadata, we can ask the client applications to send a specific header, for example, I can ask them to send a header with this name and with this value. Again, this header and this value can be anything and you can decide based upon your business requirements. And the last approach is by using the MediaType. So this approach, in other words, we can also call it as Content Negotiation. Both the client applications and the backend applications, they can perform the negotiations on how they want to exchange the data. So that communication they can perform by using the Media Type. Inside the industry, there is a standard header that the client applications, they can use. For example, here, if you see while they are invoking this REST API inside the request headers they can send these standard request header, this header cannot be changed always it should be accept only. But the value can be anything. Here I'm following one of the industry standard. Based upon this value, the backend server, it can easily identify that so and so client application, they are looking for a JSON response by using the V1. V1 means version 1. Instead of V1, we can have any other values like version 1 or 1 alone it's up to our business logic. Whenever the client application is sending this kind of value, it's an indication to the server saying that Return Version 1 of this API response formatted as JSON. There is a good history around this value. Let's try to understand the same. Whatever value that we are trying to send along with the accept header, we call it as MIME. So what is MIME? It is a standard way of telling the computer what type of data is being sent so that it can be handled correctly. In simple words, MIME describes the format of the content or format of the communication between the client and server. We have used all these words like application, vnd, myapp, v1, +json inside these values. Let's try to break down each one of them and try to understand the purpose of them. The first application part represents top level MIME type. This is the general category of representing data, by mentioning this application value, we are trying to convey that two different applications are trying to communicate with each other. And vnd means vendor specific. Different organizations, they are going to have their own specifications on how they want to send the response and the format that's why every vendor, they can have their own vendor specific format. That is what this vnd conveys. So this application, vnd they are always static. But coming to the myapp, it represents an application name or company name. Instead of myapp, you can mention your company name. And after the myapp, you can mention the V1. So if you see, there is a syntax that we are following here. After the application, we are mentioning forward slash followed by vnd.app name.v1 and at last, we are going to mention +json. So with  V1, we are providing what is the API version that we want to use. Similarly, V2, V3 values can be sent by the client applications. And the last part +json indicates what is the format of the data that is being used for the communication between the client and the backend server. Since we are mentioning JSON, the JSON is going to be used for the communication. You may have questions like why vnd? What is this vnd? As I said before, vnd stands for vendor specific. It means the media type is specific to a given organization, not a global standard. For example, inside the industry, very bigger organizations like GitHub, Stripe, Twitter, they all use this standard to implement API versioning. They'll follow these same syntax, but at the place of app name, they're going to have their company name like GitHub. So this is a custom JSON format defined by GitHub, and this value will get used only by the GitHub REST APIs. In the similar lines, developers can define their own company values. Usually this media type versioning is majorly used inside the company. The reason is, they're not going to pollute the URLs with V1, V2 by using path variables or by using the query params. As always, the API versioning is going to be done using the headers rather than paths. The REST API URL, it is always going to remain stable and it is never going to change even though more number of versions are getting released on the backend. If you don't like using these media type versioning, you can also use the request header approach as well. In that approach also, we have all these advantages. From the next lecture, let's try to understand how to implement these approaches inside our Spring Boot application.