With the current value that we have under the Spring.jpa.show-sql property, the SQL statements are going to be logged inside the console and into the log files as well. I can show you the proof as well. If I go to the console and clean all these logs, if you try to invoke any of the REST API that involves database interaction, you will see the SQL statements. Let me try to trigger the login operation. We know during the login operation, the framework will try to interact with the database. As soon as I click on this send button, inside the console logs, I'm able to see some SQL statements being printed. So this confirms right now, the Spring Boot framework is considering the default value that we are providing inside the application.properties. What is the default value? It is true. Let's try to change this behavior. So this time, what I'll do is, I'll click on these three dot icons. Inside this pop-up, I need to select the edit. Now inside this window, under these modify options, I need to select the option environment variables. As soon as I click on this option, I'm going to have a new text box asking me to feed the environment variables. Ideally inside higher environments, the operations team members, they are going to create the environment variables directly inside the server or inside the system where the code is being deployed. But right now, since we are using IntelliJ, we can take help from this product to simulate the behavior of injecting the property values by using environment variables. Here, I need to provide the property key name, which is SPRING_JPA_SHOW_SQL. . Since I'm using environment variable option, I need to follow this syntax. Followed by I need to mention the value, which is false. And if you are providing only a single property, you can leave like this otherwise you can mention optional semicolon as well. But if you are feeding multiple properties and their values, then you need to make sure that each property is separated by using semicolon. Now with these changes, the Spring Boot framework during the startup of the application, since it is seeing a environment variable with the same property key name, it is going to construct the false value because the environment variables has highest precedence compared to the values defined inside the application.properties. Let me click on apply, ok followed by I need to restart my application because during the starting of the application itself, all these properties are going to be loaded into the application by the Spring Boot framework. Once the application is restarted, let me clean the console. Now I'll try to log in again. This time inside the console logs, I don't see any SQL statement. Are you seeing the power of externalizing properties? Without making code changes, we're able to control the behavior of the Spring Boot framework by feeding the property value using an externalizing approach. The same kind of process is applicable for our own properties as well. If you go to the JwtUtil, inside this class, we are trying to read the secret by using an environment variable with the name JWT_SECRET_KEY. Long back when we are testing JwtToken, we created an environment variable with the key at JWT_SECRET. . Let me do the same one more time. I'll go to these edit and here, since we are trying to mention multiple environment variables, we need to mention semicolon followed by I'll mention the property key name and coming to the value for our testing purpose, I'll just mention the value as madan. Let me click on apply,ok, restart. I'll keep a breakpoint inside the JwtUtil so that we can see what value is being considered inside our business logic. So there is a breakpoint here. Let me click on the send button. The breakpoint stopped. Let me see what value I have under this secret. So under the secret, I have the value madan. So since there is an environment variable available with this property key, the same is being leveraged by the Spring Boot framework, otherwise it is going to consider these default value. So this way any property, regardless whether it is belongs to the framework or it is your own custom property, you can always follow the same process. Now let's see the other approaches that we have discussed. I need to go to these edit and here this time, I want to inject the property values by using the JVM options for the same here, you need to select an option, Add VM options. So this is going to ask you to enter the VM options. So let me type hyphen D followed by I need to provide the property key name. Here IntelliJ is helping me by showing all the properties that right now I'm using inside my backend application. Let me type spring.jpa.show-sql. This time I'll pass the value as true. Though we are mentioning faults under the environment variables, this is also going to be ignored because the VM options are the JVM options that are going to have highest precedence. So let me save these, apply, ok. Before I try to restart my application inside the application.properties, I will try to comment these properties that we have so that you're going to get a confirmation that the true boolean value is being considered from only the VM options, but not from these default value. Let me save these, do the build and restart my application. Once the application is restarted, I'm going to test the login REST API. This time we are expecting the SQL statements to get displayed on the console. I'm clicking on this sent, but I'm getting 500 internal server error because I'm using a very short JWT_SECRET_KEY. Let me go to the environment variables here, I will get rid of these JWT_SECRET so that that default JWT_SECRET is going to be leveraged by the Spring Boot framework. And the default one is a very lengthy one and a complex one. There should not be any issues around that. Let me click on apply, ok, and restart my application. This time, if I click on the send button, I'm getting a successful response. Let me check the console logs inside the console logs, I'm able to see the SQL statements as we expected. This gives a confirmation to us that JVM properties have highest precedence compared to the environment variables. Now let's try to explore the CLI arguments approach as well. Here inside this edit window, this time we need to select the option of program arguments. And this is going to allow us to enter CLI arguments to provide the property and its value by using CLI arguments, first, we need to type hyphen hyphen followed by the property, so this is a property key. And under the value, I'll mention it as false. With this this time, the SQL statement should not appear because we know the CLI arguments, they are going to have the highest precedence compared to any other approach. Let me click on apply, ok. I'll also go to application.properties here also, I will uncomment this line. I will do the build, restart my application, followed by, I'm going to clean the console. From Postman, I'm invoking my login API. This time inside the console logs, I don't see any SQL statements. By using any of the approaches that we have discussed, the operations team members, they should be able to feed the properties and their values without the need of changing the code or property value inside our source code. They don't have to repackage the application again and again, the same jar, which is packaged one can be reused across multiple environments while the application is being deployed across various environments. The operations team, they can leverage any of these options to inject a dynamic property value based upon their business requirements. For example, the team can have this property as true in all the lower environments, whereas for the production environment, they can set this value to false by using either environment variable or CLI approach or JVM properties approach. I will delete all these inputs because I want to have the default true value that we configured inside the application.properties. Inside the local system, displaying the SQL statements always going to help us, to debug any kind of issues.