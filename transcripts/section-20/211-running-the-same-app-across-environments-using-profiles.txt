When you want to get started with the Spring Boot profiles, first, you need to decide how many profiles you want to create. The number of profiles, usually they are going to depend on the number of environments that an enterprise application is going to be deployed. In a typical enterprise application, there'll be development, QA and production environment. These are the mandatory environment. Apart from these three environments, usually projects, they also use many other environments like system integration environment, staging environment. But for our discussion purpose, let's stick to three different environments. Inside dev environment or inside the local environment, I'm fine using these default profile, which is going to be always activated and with the default profile, whatever properties that we define inside the application.properties, they are going to be leveraged. Now for the other two environments, like production and QA, I need to create corresponding property files. So what I'll do is, I'll try to copy this file into the resources couple of times. The very first time I'll name this as application-qa.properties. This qa will be my profile name. In the similar lines, I'm going to paste the same file one more time, And this time I'm going to mention the name as application-prod.properties. So inside the qa.properties file, we need to maintain the properties, which needs to be specific to the QA environment or the properties which has to have their own unique value compared to the default profile. Let me identify such properties. So application name, logging pattern console and logging level I'm fine with the default property values. Coming to the data source, URL, username and password, these three properties definitely has to be maintained with the different values for different environment. Because inside the QA environment, the application has to point to the QA database. As of now, we are having a single database that we set up using Docker, but in real enterprise applications, database administrators, they are going to create a database servers for each environment. You just have to come to this property file and mention what is the hostname of the QA database, what is the port number, what is the username and password. For now, I'm going to keep this as it is because we are using a single database. Coming to the jpa.showsql, I'm fine displaying the SQL logs inside the QA environment so that if there are any defects identified, these SQL logs are going to help it. That's why I will delete this from this file because I'm fine with the whatever property that is being used as part of that default profile. Let me delete the batch of exercise property as well. All these properties as well I'm fine to use same set of values for all kinds of environments. Coming to the jobs.ttl-minutes, I want to have 15 minutes for QA environment, max-size, I'm fine with the default and roles.cache-information, I'm fine with the default values for the QA environment as well. Now coming to the cors configuration, I'm fine with all these allowedMethods, allowedHeaders, credentials and max-age in using the same properties from the default environment. But under these cors.allowed-origins here, instead of dev.jobportal.eazybytes.com, I want to mention the domain name as qa.jobportal.eazybytes.com. Off course, we don't require these localhosts 5173 inside the QA environment, but I'm still maintaining here because when we start our application inside our local system using the QA profile, we want our UI application to talk with the backend application. That's why I have mentioned this, otherwise in real enterprise application, you can simply mention these actual domain name of the QA environment. Let me copy the same properties into the application-prod.properties. When the code is being deployed into the production, we're going to activate the prod profile. And under these properties file, we have to mention what is the prod database username and password. Coming to the cache-ttl-minutes, I will mention it as 5 minutes for the production environment. And here under the allowedOrigins, instead of QA, I want to mention the domain name as jobportal.eazybytes.com. This is the domain name, which is going to be used by the actual end user. Since I'm looking to disable the SQL logging inside the production environment, I had to make sure I'm overriding the property that I'm using here. Let me copy this and paste it here. What I'll do is, for these property inside the production environment, I always want to use the false, because displaying the SQL statements inside the production environment is not recommended for security and performance reasons. So this way I can define the list of properties that are specific or unique to a given environment. When you are defining a property inside a profile property file, you have a flexibility to introduce a property which is entirely new or you can overwrite the value for the property that is already defined as part of the default profile. Here I have defined total 7 different properties for the prod profile and for QA, I'm using 6 different properties, but in real applications, you may have 50 or 100 different properties for a given environment. Regardless of how many properties you have defined inside a profile specific property file, the process of activating and defining the profiles is always going to be same. Let me save these changes. I'm going to do the build once the build is completed, what I'll do is, I will stop my application and as a next step I'm looking to activate one of the profile. Let me click on this more actions and select this edit. I can activate the profile by creating an environment variable or CLI argument or VM properties. For example, if I want to use environment variables option, I just have to select environment variables and here I need to mention the property key name as SPRING_PROFILES_ACTIVE  is equal to prod. . If you are looking to activate multiple profiles, then you just have to mention all those profile names by using comma as a separator. Let me first activate the prod profile. I'm clicking on this apply,ok. And now I'm going to restart my application. When you start your application inside the logs, the framework, it is going to clearly print a statement saying that which profiles are activated during the startup of the application. If you go to the top here, there is a statement saying that The following 1profile is active, which is prod. Apart from this prod, behind the scenes anyway default profile will be activated all the time. So now since the prod profile is activated, the SQL statement, they should not get displayed on the console logs because here we are using false. Apart from these false value for the remaining properties, the respective values will be picked by the framework. Let me clean the console and trigger the login operation. I got the successful response inside the console logs, I don't see any SQL statement being printed. This is because for the prod profile, we disabled the SQL logging. Let's try to take some tricky scenario. Apart from prod, I'm also going to activate QA profile. I'm clicking on this apply, ok, and I'm also going to restart the application, but before restarting the application, let me go to the CaffeineCacheConfig and here I will keep a breakpoint so that we can see what are the TTL minutes that are being injected under the jobs and companies cache. I'm restarting my application. The breakpoint stopped inside the CaffeineCacheConfig. If I hover on these jobsCacheTtlMinutes here, I'm seeing a value 15 and the same applies for companies cache as well. So from which profile these value is being considered, it is from the QA profile. So inside the QA profile, we have mentioned 15, whereas inside the prod profile, we have mentioned 5. So when you are trying to activate multiple profiles and in all these profiles, if you have the same property with the different values, then the last profile that you're trying to activate that profile specific values will be considered. As of now, when we are trying to activate multiple profiles, I have mentioned QA towards the end that's why this profile will get activated towards the end. So whatever default values that will get loaded will be overrided by prod and prod values will be overrided by the QA. Inside the console logs as well, you should be able to see about a statement that the following two profiles are active, which are prod and QA. This time, let me switch the profile details. First, I'll mention it as QA followed by comma and prod. I'm clicking on this apply, OK, and restarting my application. This time also the request is going to stop inside the CaffeineCacheConfig class. So let's see what value we have this time we have five minutes in both the places. I hope this is giving a confirmation to you on how the property values will be picked by the Spring Boot framework when you are trying to activate multiple profiles. Now let's try to understand on how to write conditional business logic based upon profiles that are activated. First, I'll go to the Jwt.properties file here, just like how we have JWT.expiration.hours with a 24 value. . In the similar lines, I'm going to create one more property with the key jwt.prod.expiration.hours with the value as 1. So inside the production environment, I want to use expiration time for JWT token as 1 hour, whereas inside the non-production environment like QA, local, dev, in such lower environments, I want to use 24 hours as the expiration time. I want to use one of these values based upon the profile that is activated. I'll go to JWTUtil class, and here we already have a Java field that reads the expiration hours from the property jwt.expiration.hours in the very similar lines, I'm going to create one more field with the name jwtProdExpirationHours and here let me mention the property key name as jwt.prod.expiration.hours, the default value will be 1. . Inside the business logic, just after this JWT token, I'm going to first get the details of the profiles that are activated. So I just have to use env.getActiveProfiles() method. So this method, it is going to give me list of String as an output. Inside this String, I'm going to have all the profiles details that are activated by a developer or operations team member. Seems this getActiveProfiles() it is going to give me profile details as a String array, but here I'm trying to catch as a list of String. So what I can do is, I can simply convert array into a list. How to do that? It's very easy. You just have to use arrays.asList() method. To this, I will pass the output that I get from the env.getActiveProfiles(). Now I just have to check whether this profiles list contains a element with the value prod. If yes, I will try to construct this JwtProdExpirationHours, otherwise I will use the JwtExpirationHours value. Let me create a int variable with the name expirationHours. So to this variable by default, I will try to assign the value from the JwtExpirationHours. In case if the profile contains prod, then I will override the variable value with the jwtProdExpirationHours. . So this expiration hours, I will use here where I'm invoking the expiration method. Let me save these changes, do the build, once the build is completed, I will keep a breakpoint at this line. Let me trigger the LoginRest API. The request is stopped at the breakpoint. First, let's check what do we have under this profile. Under these profiles, we have profile names like QA and prod. Since prod is present, the if condition is going to be executed, and with that, we have set the expiration hours to 1 hour. So this is how you can make your life easy by adopting profile information inside your business logic. Sometimes as a developer, we get requirements based upon the environment. All such environment-specific scenarios can be handled beautifully by using SpringBoot profiles concept. With these SpringBoot profiles, all the problems that operations team members use to complain, all of them are resolved. As a developer, you can maintain as many properties as you want inside these profile-specific properties file. . The operations team members, they just have to use a single property with the name SPRING_PROFILES_ACTIVE. If you are using IntelliJ, mentioning these environment variables is also completely optional. You are going to have these active profiles text box here. You can simply mention prod comma QA. And with that, behind the scenes, the IntelliJ is going to take care of activating the profiles by using the property spring.profiles.active with the values that we have provided. Let me click on OK and restart the application. This time also, you will be able to see inside the console logs that two profiles are activated. If I go to the top here, I have a statement saying that the following two profiles are active, which are prod and QA.