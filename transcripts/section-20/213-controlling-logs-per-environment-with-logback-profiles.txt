Long back as part of the logging section, we discussed on how to control the logging inside our application, by using the log back configurations that we have defined inside these logback.xml. Using these logback.xml, we have enabled both the console logging and file logging as well. By using these console appender, we are allowing the framework to log the logs inside the console by using this pattern. Similarly, we have another appender of type rolling file appender, this is going to take care of adding the logs to the log files. Both the appenders that we have defined with the name rolling file and console, we are using them in the down. If you come towards the end of the file here by using these root element, I'm trying to set log level to info at the root level. When I say root level for all the classes that we have inside the framework and dependent libraries for all of them, I'm setting the level as info, and by using these appenders, I'm trying to log them both inside the file and onto the console. Whereas coming to the logs that I have defined under the package com.eazybytes.jobportal for this also, I'm setting the level as info. That means any logs that I have defined under this package or under its sub package, they're going to be logged by taking the info as a logging level severity. But these logging also, I have enabled both the rolling file and the console logging. But the drawback with these approach is, for all kinds of environments, the same configurations will be leveraged. This is where we can do a smart job,we can try to integrate the springboot profiles into these log back XML. So we can tell to the log back framework as well, If a given profile is activated, please do so-and-so logging. First, what I have to do is just after these appender related configurations, I had to create a new tag with the name spring profile and using the spring profile tag, I need to invoke the name attribute. And to this name, I can mention the profile name. For example, I can mention the profile name here as prod, Whenever the prod profile is activated, I want to have certain logging. So to convey that logging requirements, what we can do is, we can copy both of these into spring profile tag. So here, what I'll do is for the prod profile, I will set the log level as error, and moreover for the prod profile, I don't want to use the console appender, because inside the production application, we are not going to use any IntelliJ IDEA to execute our business logic. That's why rolling file appender should be enough. Coming to the package com.eazybytes.jobportal for this also, I will set the log level as error and around the appender here also, I'm going to delete the console appender. So these are my logging requirements for the prod profile. In the similar style, we can define for non-prod profile as well. Let me paste the same configurations one more time. So this time, what I'll do is, I'll mention the prod profile name as QA,for QA also, I want to have only the rolling file appender, but coming to the root level, I'm fine to have info level,that's why I'm setting info level in both the places. And towards the end, I want to define the configurations for the default profile as well. Let me copy these one more time. So this time, instead of QA, I will mention it as default. Coming to the root level, I will use it as info,but when I'm using default profile along with the rolling appender, I also want to use console appender as well, because we're going to use the default profile in local environment and inside local environment, definitely console logs, they're going to be helpful to us. Let me mention the same under the com.eazybytes.jobportal logger as well,here just above this rolling file appender, I'm mentioning console appender as well. Let me save these changes, do the build,once the build is completed, first I'll try to start with the prod profile, this time if I restart my application, I should not see any console logs. The logs should be written only inside the log file. Even inside the log file, when the prod profile is activated, only the error severity log should be written. As you can see, I'm not seeing any logs under my console after the spring boot banner. let me clean the console,if I trigger my login API, I'm not seeing any logs because I have disabled the console logging. This time, what I'll do is, I will activate that default profile so that we can see the console logging. Let me go to this edit and here I just have to remove this so that only the default profile will be activated by the spring boot framework. This time, if I restart my application, lot of logs are getting printed onto the console. Let me clean this and invoke this REST API. Inside the console logs, I'm able to see many logs along with the SQL statements. So this is how you can control your logging as well by using the spring profile concept. During local testing, always prefer to use default profile because that is going to provide you info level logging, both inside the console and log files. In case if you want to test something with the prod profile, please be aware about the restrictions that we have mentioned here around the logging. Inside this course, most of the times I'll try to use the default profile so that we can log in into our application with any random password, on top of that, we are going to get detail logging inside our console and log files.