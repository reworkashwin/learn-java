The three approaches that we have discussed so far, they expect the properties to be present inside the file application.properties file or application.yml file. But what if we have a requirement to maintain the properties in a separate file. Mentioning all the properties inside a single file is also not recommended. That is going to bring some maintenance issues. That's why inside real enterprise applications, developers, they will try to maintain properties in separate files for easier maintenance and tracking. In this kind of scenarios, whatever three approaches that we have discussed, they will not work out of the box. We need to use another approach. We're going to explore the same inside this lecture. This time, I want to define some JWT token related properties. Inside the JWT Util, while we are trying to generate the JWT token, we're setting this issuer name, subject name, and along with the expiration time. So these three values, I want to inject them by using properties. So that in future, if I get a requirement to change the expiration time or the issuer or the subject of the token, the change can be done inside the properties file instead of the Java code. And this change can be done by the operations team without depending on the developers. This time, I'm going to define the properties in a separate file. As of now, we're defining all our properties inside this file. Let me create a new file with the name jwt.properties. This name can be anything. Inside this file, I have created three different properties with the keys jwt.user, jwt.subject,jwt.expiration.hours. . So these are the values that I want to use inside my business logic. Now I need to go to the jwtUtil and here on top of this class, I need to use the annotation @PropertySource. . To this annotation, we need to provide the file name where the properties are present. Let me type value. To this value, first I have to mention classpath followed by colon, next I need to mention the property file name. The file name is jwt.properties. Since I have created this property file under the resources, I need to mention classpath because the property file is present inside my project itself. This annotation also allow you to load the property file from a file system location as well. If you open these value documentation here, they gave an example instead of classpath, if you mention file colon, then the framework is going to look for the property file based upon the file location that you provide. This open up new possibilities to the developers. If needed, we can maintain the property file inside the server location outside of our project code, so that we don't have to check in that property file into the GitHub repository. If there is a scenario where you don't want anyone to look at the property content, then you can maintain that property file inside the server location and that folder location or file location you can provide by using these file prefix. Once we define this property, the framework during the startup of the application, it is going to look for this file under the classpath and all the properties that we have defined in that file will be read by the framework. Now to use those properties along with their values inside our business logic, we have to rely on the @Value annotation. Let me read the JwtToken related properties here, inside this class by using @Value annotation, I need to import this annotation from the Spring Framework library. If you don't like this @Value annotation, you can read the properties by using the environment approach as well. . In case if these properties are not defined, I'm going to consider these default value like JobPortal, JWTToken and the default expiration time as 1. What I'll do is, I will try to use this field. So here instead of hard coding the value, I will mention jwtIssuer and here I will use the field JWTSubject. Now coming to the expiration time, instead of 24, I will use JWTExpirationHours. Let me save these changes do the build, once the build is completed, I will keep a breakpoint inside the generateJwtToken method. From the postman, let me trigger the login operation. The request stopped at my breakpoint. Now if you try to look at the Java field values under the JwtIssuer, we have the value easy job portal and under the subject, we have this value. Now coming to the expiration hours, we have 24. All these values are being pulled from these custom properties that we have defined with the name Jwt.Properties. Let me share more details around this annotation and approach. So this is the process on how to use this annotation. Please make sure when you are using this classpath approach, your custom property file should be present under the resources folder. And this is how you can read the properties by using the @Value annotation when you are using @PropertySource annotation. In case if your project has multiple custom property files, then you can provide all those file names by using these syntax. To the same annotation, this time you just have to mention curly braces and inside the curly braces, each of the file name can be separated by using comma as a separator. This annotation also supports a property or attribute with the name ignoreResourceNotFound. By default, Spring and Spring Boot, they are going to throw an error if the specified custom property file does not exist in the given location. In case if you want the framework to ignore and proceed with the startup of the application, you need to use this ignoreResourceNotFound with the true value. This makes your application more flexible if the file is optional. So this is the syntax on how you can use this property. In our Jwt.Properties scenario, I have not used this property because without Jwt.Properties, my application won't work. The Jwt token itself won't get generated. Since that file is a mandatory for my application to work properly, I ignored using this property. In case if you have a scenario where your business logic will still work, even if the custom property file is not present, under such scenarios, feel free to use these attributes when you are using @PropertySource annotation.