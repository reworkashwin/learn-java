Inside our application.properties file, we have defined a good amount of properties. By reading these properties and their values, the SpringBoot and Spring Framework behind the scenes, they are doing a lot of magic. For example, if you closely look at the properties that I have highlighted, by using these three properties, the Spring Boot Framework, it is going to understand what are that database credentials that the developer has provided, and by using them, it is going to create a database connection behind the scenes. And the same connection is being used whenever we are trying to interact with the database by using derived query methods or JPA provided methods. In the very similar lines, in an enterprise application, there might be some scenarios where the developer has to read a property value in their Java code. Based upon the value inside this property, they may want to execute various flow of their program. That's why for the next few lectures, we're going to focus on what are the various options that we have to read the properties and their values from our Java code. Inside this slide, I have documented about the multiple approaches that developers can follow to read properties. So these are the most commonly used approaches. The very first approach is by using @Value annotation. When we are using this approach first, we have to define a property inside application.properties file, followed by we can read that property or refer to the property value inside our Java code by using this annotation on top of a Java field. To this annotation, I need to pass what is the property name or property key while passing the property name I need to follow this syntax. First, I have to mention double quotes and inside the double quotes, I need to mention the dollar symbol followed by inside the curly braces I need to mention the property key name. The next approach is by using the environment interface. The spring and spring boot frameworks behind the scenes, they're going to provide a bean of type environment, which we can inject as a dependency into our classes. For example, here into your Java class, I'm injecting a bean of type environment by using @Autowired annotation. . Next, by using these env bean or env object variable, I can invoke that getProperty() method and to this method, I need to pass a property name or property key name. The beauty of this environment is, it is capable of reading the properties present inside the application.properties. Apart from that, this is also capable of reading the environment variables that are defined inside a system or inside a server where the code is deployed. Inside this lecture, let's see the demo of both these two approaches. In the next lecture, we can explore on this third option. Inside our application, we have a class with the name CaffeineCacheConfig. Inside this class, we have defined all the caching related configurations. For example, for the jobs cache, I want to have the expiration time as 10 minutes with the maximum size of 5000. In the similar lines, we also defined cache related configurations for the companies and roles caches as well. This is a great approach, but it has a drawback. Let's imagine in future, you may want to increase these cache expiration time from 10 minutes to five minutes or 20 minutes based upon your business requirements. Whenever such requirement arises, the developer has to come to these Java code and you need to change the code, build the application and redeploy the application. But in real enterprise application, depending on the developer, every time they want to change the cache is not a great approach. That's why what operations team members, they're going to tell to the developer is, hey buddy, please expose this caching related configurations as a properties and just give me those property details to me so that in future, whenever some caching requirements comes, I'm not going to depend on you or on a developer as a operations team member or as a DevOps team member, I'm going to simply update those property values and trigger the deployment process. As part of the deployment process, both the build and the redeployment of the application is going to happen. That's why wherever possible as a developer, we should give some flexibility to the operations team members as well to control the behavior of our application without the need of changing the Java code. I hope you understand this scenario. As a next step, let's go to the application.properties file here towards the end, I'm going to create some caching related properties. So these are the properties, cache.jobs.ttl-minutes and cache.jobs.max-size. These two properties I'm going to use for jobs related cache. Next, I have companies related cache properties as well. And towards the end, I have roles related cache properties as well. By using this ttl-minutes property, I'm going to control the expiration time for the cache. Whereas by using this max size, we can provide what are the maximum number of objects or elements that we can store inside a cache. Next, let's go to the CaffeineCacheConfig. And here, as of now, we had coded those values like 10 and 5000. Instead of mentioning the values directly, we can try to read the values from the properties file and provide those values as an input here. So this is one of the classical example where we can use @Value annotation. So this annotation, you need to import from the org.springframework.beans.factory.annotation. The syntax is very simple. We just have to mention double quotes followed by dollar symbol curly braces. Inside these curly braces, we need to mention the property key name. So this is the key. Let me paste it inside the curly brace followed by I'm going to create the variable name as private int jobCacheTtlMinutes. In the similar lines, let me do for the other properties as well. I need to copy these total 5 times. I'm going to update this variable name to jobsCacheMaxSize. And I need to get the property key here. So this is the key. I'm pasting the same here. Behind the scenes, I updated the variable names along with the property key names as well. With this code changes, during the startup of our application, the spring boot framework, it is going to read the properties from these application.properties file based upon the key name that we have provided, and the corresponding value will be assigned to the Java fields that we have defined. We need to use the Java fields inside our business application. Here instead of passing 10 directly, I can simply look for the variable with the name jobscacheTtlMinutes. Let me do the same for 5000 as well. I'm going to replace this with jobsCacheMaxSize. Let's do the same for other configurations as well. Here I need to use the variable name companiesCacheTtlMinutes, and instead of 500, I just have to use companiesCacheMaxSize. Let me remove this 1 and replace this with rolesCacheTtlDays. And this 100 with rolesCacheMaxSize. After making these changes, I'm going to do the build once the build is completed, I'll also try to keep a break point at the last line inside this method followed by, I'm going to restart my application during the startup of the application, we know that the framework will try to create a bean of CaffeineCacheConfig. The break point stopped at the last line. If you look for the variable names that we have used here under the rolesCacheMaxSize a 100 value is present. Similarly, for rolesCacheTtlDays, 1 is present. So this gives a confirmation to us, the fields that we are using inside our business logic, they holds the value that we have defined inside the application.properties. Let me release this break point. By using these properties inside our business logic, we enhanced our backend application such a way that even operations team members, they can control the behavior of the cache expiration and cache maximum size. They just have to come to these application.properties file. And here, instead of this 10, they have to mention 5 minutes or 20 minutes based upon the business requirement. And to perform this update, the operations team member, they don't have to know any Java code and they don't have to rely on Java developer. They can simply update these property value and trigger the build and redeployment of the application. . Let's see a scenario. What is going to happen if one of the property is missing? For example, I'll try to comment the very first property and I will do the build and restart my application. This time the application is going to fail to start. The reason is very simple. If you look at the console logs here, it is clearly highlighted that the framework is looking for a property with the key with this value. And since it is not able to identify any property with the given key, it is throwing an exception and stopping the entire application. So this can be a problem in real enterprise applications, because there might be a scenario where all the required properties may not be present inside the application.properties file when the application is being deployed across various environments, like testing environment, uat environment and production environment. That's why to handle these kinds of scenarios as a developer, what we can do is, we can update the @Value annotation configuration in such a way that if these property is present, please read the value otherwise, please consider a default value. So here I'm going to mention the default value as 5 after colon. So this is the syntax that we need to follow. So if these properties present, the value is going to be read from the application.properties, otherwise, this default 5 value will be assigned to the Java field. Let me define here 2000 as the default maximum size. Here I'm going to mention 5 minutes as a default ttl time and here 100 as a maximum size. Coming to the ttl days, here I will mention 2 as a default value and the max as default value as 50. So with this kind of setup, now our application is going to start without any problem, even if one of the property is missing. This time, if I try to start my application, there won't be any surprises. The breakpoint stopped inside my CaffeineCacheConfig for the very first variable, which is jobsCacheTtlMinutes. If you see here, the default value 5 is assigned because there is no property with the given key inside the application.properties file. Let me release this breakpoint and go back to application.properties. I'm going to uncomment this property. As a developer, always make sure that all the required properties are defined inside the application.properties. Along with that, you can also have this as a backup plan by defining these default values when you are using @Value annotation. I hope you are clear on how to use @Value annotation. Now let's try to understand how to use the environment interface to read the properties inside our Java code. We are already doing this inside our application. If you open JWT related like JwtUtil or JWTokenValidator, inside these classes, we are injecting the environment bean as a dependency. So this environment is a interface, for this interface, there are good amount of implementations. So during the startup of the application, the framework will inject the corresponding bean as a dependency into this class. Now using this env, I just have to invoke the getProperty(). And to this getProperty() method, we need to pass what is the property key name. For example, here I can read the same property that we have defined here let me take this property key name. And here I'm going to create a variable with the name as ttlTime. Now on the right hand side, I just have to invoke the getProperty() method and to this method, I need to pass a property key, which is cash.jobs.ttl-minutes. This environment approach is also capable of reading the environment variables that we defined inside the system where the code is deployed. While we are trying to read the JWT SECRET key, we are following that approach. So to this getProperty(), we are passing the environment variable name as JWT_SECRET. If this environment variable is defined inside your system, the value against it will be read by the Java code that we have written here. Otherwise we want to use a default value. The same kind of default value we can also provide for the ttlTime as well. Here while invoking this getProperty() along with the property key name, I can also provide a default value 5. Let's try to test this code. I'm going to do the build, once the build is completed, I'll keep a breakpoint here. In order to test the logic present inside the generate JWT token method, we need to trigger the login flow. Inside my postman, first I'm going to trigger the CSRF token related API. This CSRF token, I need to mention inside the request headers while invoking the login operation. So here I'm mentioning the latest CSRF token. So this is the username and password. As soon as I click on the send button, the breakpoint is going to stop inside the JwtUtil. Let's try to debug these two lines. So under the ttlTime, you can see I have the value 10, which we have defined under the application.properties. Whereas under the secret variable, we are having the default value that we have defined under the application constant. Why default value is considered here? Because I have not defined any environment variable with this name inside my system. We already discussed on how to define this environment variable in the previous sections while we are discussing about the JWT tokens. We're going to discuss about environment variables again in the coming lectures. I hope you are clear with the first two approaches, the code configurations that we have discussed around the @Value annotation and the environment interface. I have documented all of them in this slide for your reference.