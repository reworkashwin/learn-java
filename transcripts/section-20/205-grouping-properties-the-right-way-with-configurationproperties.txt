Let me comment this line of code, I have written this code just to show you the demo around the environment. So far, we are reading the properties by using the environment approach and by using the @Value annotation approach. Though these two approaches are good enough for an enterprise application, but they have a small drawback. The drawback is you need to hard code the property key name inside your business logic. One more disadvantage around these approaches is, we need to repeat all this code for all the properties that we want to read. So far, I have repeated this code for six properties. In case if there is a scenario where I have to read 20 different properties, then I need to repeat this code 20 different times. The same applies for environment approach as well. Here I need to invoke the getProperty() method 20 different times and this can make my business logic clumsy. Anytime if you are into these kind of scenarios where you want to read a good amount of properties, then there is an advanced option available, which is by using @ConfigurationProperties annotation. This annotation enables by using this annotation, we can try to read a group of properties into a Java POJO object. That POJO object can be injected as a bean into the business logic, and that is going to make our code look much better. Let me show you an example so that it is going to be clear for you. This time, I'll try to target cors related configurations that we have done inside the JobPortalSecurityConfig. So here, if you closely look inside this class, there is a method which is cors configuration source. By using these method, we are providing all the cors related configurations. These cors related configurations also, instead of hard coding them directly inside my business logic, I want to read them from properties. But reading these properties individually, it is going to make my life complex. That's why this time I'm going to use a third approach before I try to use that approach first, inside the application.properties file, I'm going to define all the cors related properties. These are the properties. You can get them from GitHub repo as well. Whenever you're looking to leverage @ConfigurationProperties annotation to read the properties, one of the important prerequisites that you need to follow is, all your properties, they should have a same prefix. If you look at all these properties, they have a same prefix like app or app.cors. So this app.cors is same for all the properties. By using the very first property allowed-origins, I'm defining the list of UI origins that can communicate with my backend application. Allowed methods, I'm mentioning asterisk, since I want to allow the communication for all HTTP methods. Under the allow-headers, I have mentioned asterisk, that means I'm fine to accept all kind of headers. And next under the allow-credentials, I have mentioned the Boolean value too, which means by using true here, I'm making sure that my backend application is accepting credentials related properties as well, like cookies, JWT tokens, and so on. Next, the last property is around the max-age, I'm using 3600 seconds as a maximum age that my browser can cache these cors details on the client side. Now what I'll do is, under this util, I will try to create a new java class with the name CorsProperties. . On top of this class, first I will try to mention @Getter annotation followed by @Setter annotation from the Lombok library. Next I'll mention @ConfigurationProperties. While using this annotation, I need to invoke the prefix attribute and to this attribute, I need to pass the prefix which is common for all my properties. Here I can either mention app or app.cors as well. So this app.cors is the common prefix for all my cors related configurations. Once we define the annotation, as the next step, we need to define the java fields inside these cors-properties file, representing all these properties. For the first property, we need to provide the name as allowed-origins itself. While giving a name to the java field, you can ignore the special characters like hyphen and every second word first letter should get started with a capital letter. Since against this property, I'm trying to store multiple values, I need to define the data type of the allowed-origin java field as list of String. The same kind of logic applies for other properties as well. First, let me create the required java fields inside this class. I'm going to paste few lines of code. You can get this code from the GitHub repo. This is the very first property, allowedOrigins. By using this, the data type of this is list of String. During the startup of the application, the values that we have assigned under these properties will be copied into this list of String java field. The next java field is allowedMethods, which represents the property allowedMethods. This java field also, I have defined it as list of String. Off course, with the current value that we have, String should be good enough, but in case if you want to define the allowedMethods like GET, POST, PUT, DELETE, then having list of String is going to make sense. That's why I have defined this data type here. The similar explanation applicable for the next property as well. Coming to the allowedCredentials, we know by using this property, we are going to simply store a boolean value, true or false. That's why the data type of this field, I have defined it as boolean. And the last property is around the maxAge. Under this property, we are going to store a numerical value, that's why here I have defined the data type as long for this java field. So once we have defined a POJO class with the required fields that are representing your properties. . As a next step, you need to go to the Spring Boot main class, which is JobportalApplication. On top of this class, you need to mention @EnableConfigurationProperties annotation. By using this annotation, we need to invoke the value attribute. To this value attribute, we need to pass the POJO class name. As of now, we have a single class, the same I have mentioned here. In case if a project has multiple POJOs with various prefix values, all those class details has to be mentioned here by using comma as a separator. With the changes that we have done so far behind the scenes, the Spring Boot framework, it is going to create a bean of this class. As a developer, I just have to use this bean as a dependency inside my business logic. Let me go to the JobPortalSecurityConfig and here I'll try to inject the bean. The bean is private final CorsProperties. The field name, I'll mention it as CorsProperties. Now I just have to use this Bean object and read all the Cors related configurations. Let me remove these hardcoded value. Instead of that, I'm going to invoke the field name getAllowedOrigins(). Let me do the same for allowedMethods as well. After allowedMethods, I can invoke the next property method, which is getAllowedHeaders(). And here instead of true, I'm going to read the value by using corsProperties.getAllowedCredentials(). And towards the end, I'm going to remove these 3600 instead of hardcoded value, I'm passing corsProperties.getMaxAge. Let me save these changes,do the build. I'm going to restart my application. This time, I have to test the scenario by using UI application because we want to test the cors related configurations. Let me try to log in as a admin here. I'm able to see the list of company details, list of message details. I'm able to invoke all the REST APIs. That gives a confirmation to me that my client application, which is my UI application is communicating with a backend application. Both these applications are deployed at two different ports, still the communication is happening due to the cors related configurations that we have done. From now onwards, by using these properties, the operations team members, they should be able to update the allowedOrigins, allowedMethods based upon the business requirements. Inside the production environment, if they want to accept the communication from the domain name like jobportal.eazybytes.com, the same can be mentioned here. And with that, the production backend application will start allowing communication only with the production UI application. I hope you are clear on when to use these @ConfigurationProperties annotation approach. This approach is going to help us in maintaining our code in a much cleaner way. All the code snippets that we have discussed inside this lecture, I have documented them inside these two slides for your reference.