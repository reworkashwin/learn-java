After you explaining about externalizing properties approaches to the operations team, they will get excited and they will start adopted these approaches. But after using two, three months, again, they are going to come back and ask you, we like these approaches, but again these approaches, they have certain drawbacks. You're going to ask them, what are those drawbacks for which operations team, they're going to answer saying that with these externalizing properties, we don't have to repackage the application, the same jar or the same packaged application can be reused across multiple environment. That is perfectly working there are no issues on that. But the problem is, since we are an enterprise application, I may have to inject 40 or 50 properties on their values based upon a given environment. And managing all these 40 or 50 properties manually is a very tough job. For example, whenever operations team, they are trying to set up a new server for a new environment, they have to make sure that they have created all the environment variables required for our backend application similarly, they have to take care of creating the VM properties, CLI arguments based upon the approach that they are trying to adopt. While they are doing all these manually, there is a very good chance they may introduce some typo mistakes inside the properties names due to which the application will not work as expected. So that's why again, operations team, they are not satisfied with this approach completely. They are again looking at you for a better solution. Let's try to understand if there is any better solution around this problem inside Spring ecosystem. Right now, what operations team members they are looking is, they are looking for a solution which is going to support environment specific configurations. For example, inside a typical backend application, we may have these kind of requirements across multiple environments. When the code is being executed inside the development environment, we may want to define the jobs cache with 10 minutes TTL, enable SQL logging, set the JWT token expiration to 24 hours and so on . For the testing environment, we want to maintain the jobs cache with 15 minutes TTL, enable SQL logging and set the JWT token expiration to 24 hours. But coming to the production, we want to set the jobs cache with the 5 minutes TTL and disable SQL logging. And more importantly, to set the JWT token expiration to only 1 hour. This way, as you are trying to deploy your application across various environments, your non-functional requirements, they will keep changing. Changing the application.properties file manually before the deployment is not feasible. And also injecting all these properties manually by using the externalized approaches that we have discussed in the previous lecture is also not possible because there is a manual involvement is required. Operations team, they are looking for a solution around this. There'll be a single code base and this code base is going to be compiled and packaged as a build, which is going to be common for all environments. That means once the application is packaged, the same package or jar has to be used for all the environment's deployment. But before this build is getting deployed into all these environments, the operations team, they are looking for a solution to inject all the development related configurations as a one go. The same applies for QA environment configurations and the prod configurations. Operations team, they don't want to maintain configurations or properties related to a environment manually. They're looking for an option which is very easy to manage. This is where we have a beautiful concept with the name Spring Boot Profiles. This is a very, very powerful concept. Understanding this is also very easy. Let's try to explore about profiles. By using Spring Boot profiles, we should be able to create multiple configurations for different environments in a single application. We can define different properties, beans or behaviors for each individual environment. Typically in a typical enterprise application, we're going to have environments like Development, Testing, Staging, and Production. For each of these environments, we can define set of properties, beans, behaviors that needs to be unique for these given environment. By using these profiles concept, we should be able to deploy our backend application with various configurations. For example, inside a local developer's laptop, we can make the Spring Boot application to use local database, whereas inside the QA server, we can make the Spring Boot framework to use testing database and similarly inside production, it can use secure production database. As the code is being migrated from one environment to other environment, Spring Boot automatically is capable of picking the corresponding set of properties that are specific to a given environment. With the profiles, we're going to get a lot of benefits. We can use the same code, but with the different configurations across multiple environments. Spring Boot profile saves developers and operations team from manual code changes between the environments. You can think of these Spring Boot profiles as clothing for seasons. For example, during summer season, we're going to wear light clothes, whereas inside of winter, we're going to wear warm clothes, for rainy season, we're going to wear raincoat and boots. Here, as per the seasons, we're changing the clothes, but our physical body is going to be same. In the similar lines, the application source code is always going to be the same, only the profile configurations, they're going to change depending on the environment where we are trying to deploy the application. By using profiles, we should be able to control the properties that we have defined inside the application.properties we should be able to control the database connections, API URLs, logging levels, bean creations and so on. Creating Spring Boot profiles is very easy. By default, we know the Spring Boot is going to give us a file with the name application.properties or application.yml, where we can store all our properties related to the backend application. In the very similar lines, you need to create more property files by following a naming convention, application-yourprofilename.yml or application-profilename.properties. For example, these are the valid names. In between your application and profile name, you have to use hyphen, not the underscore. When you create multiple application.properties file by using these naming convention, based upon the profile that you are activating, the corresponding properties will be picked by the Spring Boot framework. For example, if you are activating the prod profile, which is specific to the production environment, then the properties that you have defined inside this file will be considered, but not inside the QA file. By default, always the default profile is going to get activated. Spring Boot always going to load all the properties that are present inside the application.properties file. We can activate a specific profile apart from regular application.properties file based upon which profile we are activating, the corresponding file properties also will be loaded by the Spring Boot framework. Now here you may have a question? How can I activate a given profile? To activate a specific profile, we need to use a property with the name spring.profiles.active. To this property, we can pass a profile name like prod or QA or combination as well. For example, you can activate multiple profiles as well. If I'm looking to activate some common profile and production profile, I just have to mention common comma prod. So with this common and prod, the framework is going to look for the file names like application-common.properties and application-prod.properties. All the properties inside those files will be loaded during the loading process, if any of the property is already present inside the application.properties file, which is going to be always loaded, then such property values will be overrided based upon the values that we have defined in the profile specific property files. To activate a profile, we can use any of the approaches that we have discussed previously. In case the command line approach is being used, we just have to mention hyphen hyphen followed by this property key name and on the value side, we have to mention the profile name like prod. For the environment variables approach, we have to use this syntax. And in case if you want to directly mention inside the application.properties, you can mention that as well. But there is a problem with these application.properties approach. Once you package your application, your application is always going to consider prod as an active profile. In case if you want to override, again you have to update this value and repackage your application. That's why activating the profile by directly mentioning these property inside the application.properties is not recommended. You can use any of the externalized approaches that we have discussed in the previous lecture. But here you may have a question, which is, Madan, you said operations team, they don't want to have the burden of providing the properties by using command line environment variables or JVM system properties. But again, here you're telling to activate a profile by using this property. It's a very valid question. But if you closely understand in this scenario, the operations team member, they need to feed only this property along with its value. By just using this property behind the scenes, they are going to activate all the good amount of properties that developer is maintaining in the profile specific properties. When I show you the demo around these profiles, it is going to make more sense to you. Apart from activating environment specific properties, we can also execute some business logic based upon the current active profile. Based upon the current active profile, we can easily identify where the code is being executed, whether it is being executed inside the production or inside the local environment or inside the QA environment. And accordingly, we can write to various kinds of conditional business logic. For example here, if you see into a Java class, I have injected these two properties by using @Value annotation. The first field, it is going to represent what is the expiration hours that the JWT Token has to consider inside the non-production environment. Using the next field, I'm trying to represent what are the expiration hours that the Jwt libraries they have to consider while they are generating the JWT token inside the prod environment. For example, here for the non-prod applications, I can have the value as 24 hours, whereas for the production environment, I can have something like 12 hours or two hours or one hour based upon my business requirements. I can leverage one of these Java fields and their values based upon the current environment. By using the environment  bean object, we can invoke the getActiveProfiles() method. This method is going to give us what are the list of profiles that are activated right now in the environment where the code is being executed. By using these profiles, I can check whether they contain any profile with the name prod. If yes, I'm going to assign the value under this field to the JWT expiration hours. Otherwise I'm going to simply leverage the JWT to non-prod expiration hours to this variable. And this variable will be feed to the JWT token generation process. I know this is too much. Let's try to see the demo of these spring boot profiles in the next lecture. So that it is going to be super clear for you.