As a next logical step, we need to set up some data into this brand new H2 database. It is going to be an easy process. Under the resources folder, if you create SQL file with a name schema.sql, during the startup, the spring boot framework, it is going to execute the schema related SQL scripts that we are going to provide. Inside this SQL file, I'm going to paste an SQL statement. So this is an SQL statement, which is going to help us in creating a new table with the name companies. This is a simple create table script, you should be able to easily understand, we're trying to create a table, if not exist, the table name is companies. And inside this table, we are trying to define all these columns, the ID is going to represent the company ID, which is also going to act as a PRIMARY KEY. Using the name, we are going to store the company name under the logo, we're going to store the path of the logo image. Using industry column, we are going to store what is the industry that the company is working, whether it is manufacturing, or it is technology, or it is an entertainment. So such industry details, we can store using this column. And coming to the size, we're going to store values like whether it is a small company or big company or medium company. Under the rating, we are going to store the rating of the company under the locations, we can store the list of locations. Under the founded, we can store the year when the company was founded description will hold the company description details employees will hold the number of employees details website will have the website details. And towards the end, we're going to have audit related columns, like when the record is created, who created it. In case if someone is updating a record, we're also going to store when a record is updated and who updated the record. So these four columns, they are going to be common for all the tables that we are going to create inside this course. These kinds of columns are mandatory in enterprise applications tables as well, because by using these audit related columns only, we're going to identify the user details, who created the data or who updated the data. Once this table is created, as a next step, we also want to populate some company's data into this table so that we don't have to manually create one by one through the UI screen. Off course, in the coming sections, we're going to create the REST APIs, which are going to be invoked from the UI application that helps an admin of the website to create a brand new company. We will eventually go there. For now, since we are looking to develop a REST API, which is going to simply return the list of companies details. What I'll do is, I'll try to insert all the company's details by using the insert SQL statements. So just like how we created a schema.sql file, very similarly, I need to create another file. So here seems I made a spelling mistake. Let me rename the file name to schema.sql. The L needs to be removed. In the similar lines, I'm going to create a new file with the name data.sql. We need to make sure to use the same names because by default, Spring Boot is going to look for these files under the resources folder. In case if you want to use different file names under different folder locations, don't worry, I'm going to explain about that as well in few minutes. For now, let's try to create the files with the default recommended names. Inside the data.sql file, I'm pasting the 30 different insert statements. If you see all these are insert statements, which are going to insert the data into the company's table. So once you have created this schema.sql and data.sql under the resources folder, as the next step, you just have to restart your application. So this time during the restart of the application, since Spring Boot identified schema.sql and data.sql files in the resources folder, it might have created the tables and inserted that data that we have provided. From the H2 console, I'm going to refresh this page and connect back again. So this time you'll see there is a table created with the name companies. As soon as I click on that, this SQL statement will be populated inside this query console. If I run this select command, I'm going to see all the 30 different company details that we have created. So this is a company name, this is a part of the logo, industry, size of the company, rating, location, founder, description, and so on. How this is all magic is happening? Whenever we are using the embedded database, by default, Spring Boot is going to look for these SQL files and it is going to create them. There is a property to control this behavior as well spring.sql.init.mode. So by default, this property is going to have the embedded as a default value. So whenever you are using an embedded database like H2 database, then only the initialization of the database is going to happen. Whereas if you set the value as never, regardless of which database you are trying to use, the initialization of the database is never going to happen. But if you select the always, then the initialization of the database is going to happen if you are using some production database like MySQL, usually we should be fine with these default embedded value. In case if you want to create the tables and data inside the H2 database manually, you can try setting these never. This time I will try to set these never and I will restart the application. This time, even though the SQL files are present under the resources folder, the table and the data will not be created by the Spring Boot. If I try to refresh this page and connect again to these database, you will not see company's data, we got a brand new database. Here you might have observed one interesting point as well. What happened to our previously created data? So this is one of the drawbacks of H2 database. H2 databases, they are going to store the data inside the memory of the application. Whenever you restart your application, since the memory will be wiped off and a brand new memory will be assigned to the application, whatever you stored previously inside the H2 database, all those details will be wiped off and the database will be created from scratch again. We are going to discuss on how to overcome these drawbacks in few minutes. But before that, let me switch back to default value embedded at any point of time, if you want to see what are the valid values of a property, you can hover on to this property. In case if you are not able to see these documentation details, you can click on the property and press the button F1. So that is going to show you the details. So these are the documentation details by default, it is going to have embedded, but other valid values which are allowed are always and never as well. So this time, if I try to restart my application, do you know what is going to happen? We are going to get the company's table along with the data. So let me refresh this page, and if I try to connect the database again, under the company's table, we're going to see the data as well. Let's try modifying one of the record and see whether it is going to be retained after restarting of the application. Off course, it won't be retained. That's what we discussed. But let's see the demo. To update an existing record, I'll try to write an update statement. Table name set, column name is equal to column value where column is equal to so and so value. I'm trying to update the rating value that I have under the first company. So the rating value here we have is 4.5. But I'm trying to update this to 5.0 by using company ID as 1. As soon as I execute this command, one row is updated. Let me run the select command again. This time you will see rating we have 5.0. Now what I'll do is, I'm going to restart my application during the restart What is going to happen? The entire memory of the application, including the H2 database will be recreated from scratch and during the recreation process, the scripts inside the schema.sql and data.sql are going to be executed. And with that, we are going to last our updates that we have executed by using the update SQL statement. Let's confirm the same, I'm restarting my web application and inside the browser, I'll try to refresh this page. Since we have to connect again, whenever we restart the application. And this time, if I execute the select query on the companies, you will see under the rating for the company one, we are seeing the initial value 4.5, but not the 5.0. So this should give a confirmation to you that by default H2 database, though they are very useful in setting up very quickly, but they don't retain the data when you restart your server. But with these drawbacks, you may face some challenges as well, while you are working with the H2 database. Think like you have developed a demo application with all the required data. You updated the data, everything you have taken care, but when you restart the application or when you go to the demo the next day, the data might have lost due to the restart of the application. . There is a very good solution, which we are going to discuss in the next lecture. Before we wind up this lecture, let me explain you on how to provide custom file names to the Spring Boot. . So by default, as we discussed, the Spring Boot is going to expect these two files like schema.sql to create the tables and data.sql to create the data. In case if you are providing only schema.sql, but not the data.sql, then only the table will be created, but not the data. So we know all this story. But what if I want to store the scripts in a file with my own custom name and under a different folder, let's try to understand the same. This time what I'll do is, I'll try to create a new folder with the name sql, and into this folder, I will try to copy both these files. Once the files are copied, I'll also try to update the file names as well. Let me click on this rename, and this time, the file name I'm going to mention as jobportal-schema.sql. The same name I'm going to give for data.sql as well. Let me mention the prefix value as jobportal-. So these are the file names. With this setup, if I try to restart my application, we are going to get the h2 database, but we're not going to have any table and data inside it because the Spring Boot is looking at the default location. Let me refresh this page and connect again to the h2 database. As you can see, we don't have any company's table. To communicate about these file name details to the Spring Boot, we have to define a couple of properties. The properties are spring.sql.init.schema locations. To this, we need to mention the class path, and under the class path, we have placed the SQL file under the SQL folder. Under this SQL folder, what is the schema file name? It is jobportal-schema.sql. So this is about the schema locations. How about the data locations? We just have to duplicate the same property, but this time, instead of schema-locations, we need to mention data-locations. The file name also we need to update. Instead of schema, we need to mention data.sql. With these properties, now my Spring Boot framework should be able to recognize the files that I'm providing. And with that, it is going to create the h2 database along with the tables and the data that I'm providing. I'm going to refresh this page and connect again to the database. We have the company's table, and we also have the data as well. When you are defining these properties, please be very careful in case if these files are not present at a given location, the application startup itself is going to fail. For example, here, instead of data.sql, if I mentioned data1.sql and schema1.sql, this time the application itself is not going to start because it is not able to identify the file that you have provided. No schema script found at a location, and similarly, we are also going to face the error for that data.sql as well. So first, since it is looking for the schema.sql, that's why it is throwing a runtime exception related to the schema.sql. In case if the schema.sql is present, but not the data.sql, then it is going to throw the error related to the data.sql with a message saying that no data scripts found at a location provided. To overcome this kind of problem, what we can do is, just before this classpath, we can mention optional as well. So with this now, even though my file is not present, the startup of the application will continue. I can confirm the same as well. Let me start my application. So this time the application started successfully, but since it didn't find the data.sql, only the table might have created, but not the data. Let me refresh this page and connect to the database again. The companies table is present, but if I try to execute this SELECT statement, there is no data present it. Feel free to use this optional in case if you have a scenario where the file is not going to be present under certain scenarios. In our scenario, we don't require that optional. I'm also correcting the file name as well. Let me do the build and restart the application. Once the application is restarted, I'm going to connect to the H2 database again. The companies table is present and the data also is present.