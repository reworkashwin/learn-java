Inside a job portal UI application, there will be two major domains. The first one is, around the company and the other one related to the jobs. Off course, we are going to have other domains or features as well, which we are going to explore as we are progressing inside this course. Since inside this section, we are looking to expose a backend REST API that is responsible to send the company's details to the UI application. What I'll do is, I'll try to create a new package by using the feature name or domain name called company. So please make sure you're not giving the plural name. It should not be companies. It should be company. Under this company package, I'm going to create one more sub package based upon the technical role. So since we are looking to develop a REST API, I need to create a controller package. Inside this controller package, I'll try to create a Java class with the name CompanyController. On top of this class, I'm going to mention @RESTController annotation followed by I'm going to mention @RequestMapping, to this @RequestMapping, I'll mention the path as /api/companies. Since the REST APIs inside this controller, they are going to deal with the resource with the name companies. I need to mention the companies here. So while giving the name inside the REST API, always follow the plural noun form instead of singular noun name. Since we also need to take care of API versioning, what I'll do is, I'll try to create a separate package under this root package. The package name is config. Under this config package, I will create another sub package with the name web. Inside this web package, let me create a new Java class with the name WebConfig. This class, it is going to implement WebMVCConfigurer. On top of this class, I'm going to mention @Configuration annotation next I'll try to override the method the method is configureApiVersioning. Inside this method, by using this configurer, I will invoke useMediaTypeParameter. Because I want to useMediaType to control the API versioning inside my application. So this is the standard that majority of the enterprise applications that they follow the same I'm trying to follow here. To this method, as we discussed previously, we need to pass two different input parameters. The first one is around the MediaType value that we want to use. I want to use application/vnd.eazy app. eazy app is the name that I have given for my own application+json. Coming to the parameter, I have given it as v. It can be v are version as well. IntelliJ is throwing a warning since this is not a dictionary word. What I can do is, I can click on this more actions and select this option save eazy app to dictionary. After providing the MediaType details, I can invoke addSupportedVersions() method. To this, I will pass various version numbers like 1.0, 2.0, 3.0. In real applications, you're going to decide the supported versions based upon the discussions within your team. For our application, I'm going with these three simple supported versions followed by I'm going to set a default version, which is 1.0. In case if the client application, if they're not sending any of the Media Type details inside that request, I'm going to consider DefaultVersion as 1.0. So now I can go to the CompanyController and here I will try to create for now a simple REST API, which is going to return a String as a response. So let me wrap it by using ResponseEntity and the method name, I'll mention it as getAllCompanies(), and this method, it is not going to accept anything, but it is going to return a String value saying that companies list. On top of this method, I need to make sure I'm mentioning @GetMapping followed by I need to invoke the version attribute and to this, I will pass 1.0 value. If anyone invokes this REST API by using this version number, this method is going to be invoked. So with the changes that we have done so far, in future if there is a requirement comes to enhance our REST API application, we don't have to touch this 1.0 version API. Instead, we can introduce another method, which is going to handle another version number. The next improvement that I want to do inside my application is, if you see throughout this course, we're going to build a good amount of REST APIs and these REST APIs, they are going to be placed in many of the controller classes related to company. We're going to create a REST APIs related to admin, job seeker, employer. So for all these REST APIs, as we discussed, we need to make sure we are mentioning a prefix like api. This gives a good confirmation to the newcomers or to the client applications that these classes are exposing some REST APIs. And in enterprise applications, apart from mentioning api, they also going to mention their product name or organization name, for example, I can mention a prefix as eazyapp followed by api. So this is the prefix that is going to be common for all the REST APIs that are going to be exposed from this Spring Boot backend application. But repeating the same prefix in all the controller classes is not a recommended approach. Instead, we are going to follow a standard approach where we can maintain this prefix at a single place. So let me get rid of these prefix. I'll only mention companies followed by I'll go to these WebConfig. Inside the WebMVCConfigurer, there is a method that we can override to configure a prefix for all the controllers. Let me look for the method with the name configurePathMatch. So this method, it is going to have PathMatchConfigurer as an input using the same configurer I'm going to invoke addPathPrefix() method to this method I can pass a PrefixPath like /api. So with this, now all the REST APIs that I'm going to have inside my application, they are going to have this PrefixPath. In future, if you want to update this PrefixPath to something else, we should be able to easily achieve that because the change is going to be only inside this file. Apart from accepting the PrefixPath, it is also going to accept a predicate lambda expression. So based upon the output that we are going to return from this predicate lambda expression, the framework is going to decide whether to add the PathPrefix to a given controller class. Inside the method documentation also, you can find the details that the predicate logic is going to be useful for matching controller types. In case if you have a requirement where you don't want to apply this PrefixPath for a few controllers, you should be able to achieve that by using the predicate lambda expression that you are passing here. So let me mention the input to the predicate as controller type and inside the lambda expression, we can write any logic. For example, you can check the controller class name and accordingly you can decide whether to return true or false. If you are returning true, then this PrefixPath is going to be applied otherwise it is not going to apply. But since we are looking to apply this PrefixPath for all the controllers that we are going to build inside this application, I'm always returning the true from this predicate lambda expression. Since we are not using this variable inside our lambda logic, we are getting a warning saying that Parameter controller Type is never used. So in this kind of scenarios, we should be able to fix this issue by using unnamed variables. In the recent versions of Java, a new feature introduced with the name unnamed variables. If a method input variable, if you're not using inside the method logic, to avoid this kind of warnings, we can simply replace the variable name with underscore value. I discussed about the unnamed variables inside my Java course. You can check out the same to understand more details around the unnamed variables. This unnamed variables feature is introduced as part of the Java 22 release. I can save all these changes and try to start my application. Once the application is started, we can quickly try to validate if our REST API is working. Under the Spring collection, I created a new folder with the name Company and under the same folder, I created a request with the name GetAllCompanies. So here I'm trying to invoke the get REST API with the path /api/companies. Whenever we are invoking these API, please make sure you are sending these accept request header with the value as application forward slash vnd.eazyapp plus json semicolon v is equal to 1.0. If I click on this send button, I'm going to get the expected output companies list. In case if I am changing the version number to 2.0, I will get 404 because there is no REST API method that handles 2.0 version inside our logic. In case if you're not sending these entire accept request to header, you're still going to get the output as companies list because we mark the 1.0 version as the default version. But still, I recommend you always sending this accept with the value as 1.0 because it is a good practice to follow. With 1.0 if we invoke the API, we're going to get the same output.