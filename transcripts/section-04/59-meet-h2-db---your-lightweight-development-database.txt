Now is the time to integrate our Spring Boot backend application with the database. Because we are going to store our company's data inside a database from the database, we are going to read the data and send the same as a response to the client applications who are invoking our REST API. Usually in an enterprise applications, we are going to use the databases like MySQL, Oracle, Postgres, or MongoDB. But right now inside this section, I want to show you on how to use an in-memory database with the name H2. This H2 database is not recommended to use for production you should only use for a low critical applications. And here you may have a question ?  Madan, if this H2 database is not going to be used in a production application, then why are you teaching about these to us? It's a great question. Let me try to answer the same. When you are working in an enterprise setup, not always you are going to work on a production application. Sometimes you are supposed to build a quick markup application or proof of concept application or sometimes you may want to build an application as part of a hackathon and similarly, sometimes you may want to execute your test case scenarios with some dummy database. In all these kinds of scenarios, using the H2 database is the recommended one, because to use this H2 database, you're not going to have any dependency on the external database like MySQL. If you want to use a proper database like MySQL, Oracle, or Postgres, you need to make sure to install them, set up them inside your system, which requires some setup process whereas with the H2 database, as soon as you add the required dependencies, the Spring Boot itself, it is going to provide you a database, which is going to store the data inside the memory of your application. We already added a dependency related to H2. So the groupId is com.h2Database and the artifactId is h2 and the scope is runtime. We already discussed about the purpose of scope, but the H2 database also, we're going to mention the scope as runtime. Along with these H2 database, you need to make sure to add that dependency related to the data JPA as well. If you scroll up, there is a starter project that we also added with the name SpringDataJPA. So this is the group ID and the artifact ID for the same. Please make sure these two starter projects are defined inside your pom.xml. Once these dependencies are available, you can try to restart your application. And this time inside the console, you will see some details around the H2 database. If I scroll up, scroll up, there are some details around the database info. This is the database URL, which SpringBoot has created by using the H2 database. And this is the dialect, this is the driver. Basically, by looking at this information, we have a clarity that a H2 database is created during the startup. And this is one of the beauty of SpringBoot as we discussed in the previous sections, based upon the dependencies that we are adding inside the pom.xml, the SpringBoot is going to take care of creating the required beans, configurations, and any other details. As a developer, we just have to connect to this database and start storing the data inside it. The h2 database is ready. As a next step, we need to understand how to connect to this database so that we can verify what is really happening behind the scenes. Unless we connect to this database, we can't understand which tables it is creating, which columns it is creating. Since we also want to execute some scripts to store some data or to create some tables, to achieve all this, we should definitely try to connect to the H2 database. So with the starter project that we have added around the H2 database, by using this dependency, the SpringBoot is only going to create the H2 database inside the memory. In case if you are looking to access this H2 database using the browser URL, then you need to add one more dependency. Let me copy the new dependency details just below this actual H2 database. You can either type these dependency details or you can get the same from the GitHub repo. The group ID is org.springframework.boot and the artifact ID is spring-boot-h2console. After adding this dependency, we need to make sure to sync the maven changes followed by we can try to restart the application. This time you will be able to see there is a statement saying that H2 console available at the path h2-console. . So by using the application port number 8080 along with this path, we should be able to access the console of this H2 database. Let's try to access this path. Inside my browser, I have given the hostname as localhost followed by 8080. After the port number, I have given the path h2-console. Let me press enter. And this is how the console page is going to look like. The driver class should be org.h2.Driver. Coming to the jdbc URL, you can find the same inside the console. Here there is a statement, database is available at so and so location. You need to copy this entire value without single quotes. And the same you need to mention under the jdbc URL. By default, the username will be sa and the password will be a blank value. After giving all these details, you can click on this Test Connection, which is going to display this kind of message confirming whether the test connection is successful or not. Once the test is successful, you can click on this connect to button, which is going to show you this page, and at this page, so far you will see there are some details around the INFORMATION_SCHEMA and the users. So these are the schemas and tables which are created by the H2 database for its own functionality. Apart from these predefined details, we are not able to see anything else. That means we got a brand new H2 database. As a next step, in the next lecture, we are going to explore on how to create tables and records inside these H2 database. But before we jump into those details, let me show you a few of the properties that you can configure when you are using the H2 database. If you go to the Spring Boot documentation and click on these appendix common application properties, it is going to show you all the properties that you can use inside the Spring Boot. Here, you need to search for .h2. and these are the properties related to the Spring H2 console. By default, it is not going to be enabled since we added an extra dependency related to the console behind the scenes, the Spring Boot enabled the H2 console. The next property is around the H2 console path. By default, this is the path that framework is going to consider if required, you can change this path as well. What I'll do is, for your reference, I'm going to mention both the properties inside the application.properties file. So here I have mentioned spring.h2.console.enabled as true, and I also configured the console path property to use the default H2 console path. If you are planning to use a different path, you can update this default value. We are good from the console perspective. So far, we are connecting to the H2 database by accessing the console page from the browser. But we want our Spring Boot application also to connect to this H2 database so that we can start interacting with the database from our business logic. To convey the connection details of the H2 database to Spring Boot framework, we have certain properties. Inside the same page where we have all the application properties details, here you can search for spring.datasource.url. This is the property that we need to use to provide the JDBC URL of the database and you can use this property to provide the username. Similarly, there will be a property to provide the password details of the data source. So by configuring these properties inside our application.properties file, we are going to provide the details of the H2 database to the Spring Boot application. But I have a problem. If you go and check the console, whenever I restart my application, this database URL is going to keep on changing. So whatever value that we are seeing here, this is a dynamic value, which is going to be different for the every restart that you're going to do. And replacing these dynamic value every time under the application.properties file, it's a very cumbersome process. That's why what we can do is, we can tell to the Spring Boot framework to follow the URL that we are providing as a H2 database URL. What I'll do is, I'll go to the application.properties and I'm going to define four different properties. These properties details you can get from GitHub repo as well. The first one is spring.datasource.url. At any point of time, if you want to know what is the purpose of a property, you can hover on to the same, the IntelliJ is going to show you more details around the property, like what is the purpose of that property. So to this property, I'm providing jdbc colon h2 colon mem colon test database. So this prefix has to be the same like jdbc colon h2 colon mem. In fact, if you go and check the console, here also we have the same prefix. Once we make sure the same prefix is mentioned, at last, we can provide a name to that database so here I'm providing the test database. The same name will be constructed by the Spring Boot framework, and it is going to use this as a H2 database name. Coming to the username and password, by default, as I said, H2 database, it is going to use the default username as sa and the blank value as password. I'm going with the same values, but in case if you want to change this to something else, you can change them as well. Here instead of sa, if you give some other username like admin and the password also as admin, then those details will be constructed by the Spring Boot framework. Always Spring Boot, what it is going to do is, whenever we are adding the H2 database details as a dependency to our project, it is going to check whether the developer is providing URL, username and password. If yes, it is going to use the same details to set up the H2 database. Otherwise, it will go with that default assumptions, like username will be sa and the password will be blank and coming to the database name, it is going to generate some random value. And towards the end, by using these spring.datasource.driver-class-name, we are giving a clue to the Spring Boot framework that it has to use the h2Driver to connect to the H2 database. So let me save all these details, do the build once the build is completed, I'm going to restart my application. So whenever you are adding new dependencies or adding new properties, please make sure you are restarting manually. Don't rely on the DevTools. In most of the scenarios, DevTools is going to work only for the Java code. Inside the console, this time you will be able to see the database is available at the URL that we have provided. Now the same details we should use while connecting to the H2 console from the browser as well. I can show you that demo as well. If you try to refresh this page, the logout is going to happen. This time while connecting, if you try to use a older JDBC URL, the test connection is going to fail. We should give the same value that we have provided in the application.properties. With these details, this time the connection is going to be successful, and if I connect, I'm able to see a brand new H2 database there are no tables data present. In the next lecture, let's try to explore on how to create tables and data inside these H2 database. Off course, we can execute the create and insert SQL statements directly inside this console. But that is not a recommended approach. Instead, we have a better approach, which we are going to explore in the next lecture.