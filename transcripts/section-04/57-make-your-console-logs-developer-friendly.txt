The Spring and Spring Boot Frameworks, they're going to generate a lot of logs inside the IntelliJ console during the startup of our application or during the execution of our business logic. But by default, these frameworks, they're going to follow a default format. Most of the times the developers, they would like to change this default logging mechanism because the default format is very boring. That's why developers, they'll try to make it colorful and use their own format, which is going to help them on a day-to-day basis when they're trying to debug their code. That's why inside this lecture, I'm going to show you on how to change that default logging format inside the console by using a simple property. Let's go to the application.properties file. Here you need to look for the property with the name logging.pattern.console. Similarly there is also another property with the name logging.pattern.file. In case if you have a requirement to save the logs into a log file, then you can try to define what is the format that the framework has to use to make the log entries into the log file. We're going to discuss everything around the logs and logging in the coming sections. For now, let's try to set these logging.pattern.console property with a logging format. Instead of typing these logging.pattern.console, you can also type the first character of each word as well. So l, p,c, these are the first letters of each of the word inside IntelliJ, you can type l.p.c, which is going to display you the matching properties. In case if this is not working for you, then this might be a premium feature, which is allowed only in the premium version of the IntelliJ. This is just a simple productivity hack. Even if it is not working, we can try to manually type the property. So to this property, I'm going to assign a value. This is a very lengthy value. You don't have to type this you can get this property from the GitHub repo. So if you see here, instead of directly passing the pattern value, I'm following a syntax. So this is the syntax. So first dollar followed by curly braces inside the curly braces. First I'm mentioning an environment variable using which we can try to set different, different pattern format values for different Environment. If this Environment variable is not available, then I'm telling to the framework to use a default value, which is defined after the colon. The same syntax I have followed here. First dollar symbol, next curly braces after the curly braces, I have mentioned the environment variable name. This variable name can be anything. Here I have mentioned the name as CONSOLE_LOG_PATTERN. If this environment variable is not defined, or if it is not present, then it is going to consider the default value present after the colon. So let me press enter so that you can see the value properly. So this is the entire default value towards the end you're going to have the closing curly braces just before that this default value is going to end. With this format, what I'm trying to tell is I'm telling to them spring boot framework as a first detail inside my log, display that time, minutes, seconds, and nanoseconds and please make sure to use the green color. I'm not trying to print the date details because usually inside the local development, we're not interested in the date, we always know what is the date it will be always the current date. After the date values, I'm requesting for the space followed by, I have mentioned these -5 level. . Each logging message that is going to be printed by the spring boot framework, it is going to have a severity like info, warning, error, debug. We're going to discuss about these logging severity levels in the coming sections. For now with these, I'm telling to the framework to display the log level details by using only up to five characters and aligning them to the left side. Coming to the color, I'm requesting to use the blue color. After the log level, I'm requesting the framework to display the current thread name, which is executing the code and these thread details I'm requesting to print using red color. After the thread, I'm requesting the framework to display the logger details by taking only up to 15 characters in yellow color. After the logger details, I have mentioned the hyphen, which is always going to be present in each of the message. And towards the end, I'm requesting the framework to log the log message with the cyan color and once the log message is printed, it needs to navigate to the next line so that the log message can be printed from the next line. So this is the pattern that we are trying to follow. Feel free to change it based upon your requirements. I'm trying to use different, different colors so that our log messages, they're going to be colorful and they also help us to easily identify the issue. Otherwise if the entire log message is in the same color or with some dull colors, we may face some productivity issues that's why I'm trying to follow this format. I recommend you also following the same. If you don't like my color selection, please provide your own colors. Let me take this to the upper line because we can't break the property into multiple lines. So as you can see, this is the environment variable, and this is that default value. In case if you want to change this default value in any of the scenario or in any of the higher environments, you just have to make sure to inject the new value by using these environment variable. Inside IntelJ, we should be able to do that by clicking on these three dots and here there is an edit option. And in this window, under this modify options, you're going to have an option environment variables and once you select that, you're going to have a new text field asking for the environment variables. Here you need to mention first the environment variable key name, which should be the same as the value that you have configured in the application.properties, followed by you can mention the new value that you are trying to set. Once you set the new value, you need to close the environment variable using semicolon. This way you can define any number of environment variables by using the syntax like variable name and the value. We're going to discuss in detail about environment variables in the coming sections. For now, I'm trying to give some quick introduction around them. Let me delete these and click apply. Okay. This time, if we try to restart the application, the logs they are going to be printed basedupon the format that we have provided. If you see first, we have the time details followed by the log level. Next, we have the thread name, after the thread name, we have the logger details like the class details, which is printing the log message followed by hyphen, and after the hyphen, we are going to have the actual log message. Each component inside our log message, they are getting displayed using different colors. And this is going to make our life easy. In case if you don't like having these timestamp value or these thread details, you can try to get rid of them. Even the logger class details, since we have restricted to up to only few characters, it's not displaying the entire package name instead, it is taking the shortcut like o.h.e.t and so on. By default, the logs inside IntelliJ console, they are going to be displayed in a single line. But if you click on this soft wrap option, the log details, they are going to be wrapped to the next line. Once you wrap the logs, you don't have to scroll onto the right hand side every time you can read the logs by scrolling your mouse up and down. This is a very good productivity. I recommend you also using the same throughout this course, I'm going to use the same logging pattern console. The details that we have discussed around the logging.pattern.console property, I have documented them in these slides for your reference. Along with that, I also have a tabular information explaining about each of the component details that we have mentioned inside our project. And in the next slide, I highlighted on why we should use the environment variable syntax whenever we are setting a property inside the application.properties file. If you feel a property needs to be different for different environments, then it is a recommendation to always use this syntax so that you can overwrite the default values without changing any code. You can always change these values from outside of your application. For example, if you take the same logging pattern.console for the development environment, I can go with the detailed colored logs, whereas for the production, I can go for the JSON format for the log messages so that the log aggregators that we usually have inside the cloud providers like AWS, they're going to aggregate the logs and we should be able to search the logs by using the log aggregator products. Whereas for the testing environment, I can have some minimum logging this way by using environment variables. We should be able to control the behavior of a property without actually making any code changes. We're always going to inject that dynamic or the latest value from outside of the application using environment variables. Off course, there are other approaches as well. When we are discussing about advanced concepts in the Spring Boot, I will discuss about the various approaches that we have to change the properties by using an external option.