So now with the use hook added and used here, we can also take this to the next level by adding another new component, which you also find attached. And that would be the ErrorBoundary component. Now that's a type of component I covered earlier in the course in the class-based components section. Because this component is built in a different way than all those other components. It is built as a class that extends the component class, which is provided by React. And normally you wouldn't build components like this anymore, but this component, this ErrorBoundary component must be built like this because it uses some special error related React features that in the end allow you to catch errors that are thrown by wrapped components and handle them in any way you want. You then have a render method here where you can define what should be rendered on the screen if an error did occur. And I do cover this in greater detail in that earlier course section where I do explore class-based components. Here I'm providing this component to you and you can use this ErrorBoundary to wrap it around this code that also uses suspense. So ErrorBoundary here is wrapped around suspense and therefore also around UsePromiseDemo. And if this component now would generate an error because fetching the data fails, for example, this ErrorBoundary would become active and would show this output here. An output where the error message is displayed and some fallback that can be passed as props to this ErrorBoundary component. So this ErrorBoundary component is built such that you can add a fallback prop and the there we could say, something went wrong. And if you do that and you then go to this promise that does the data fetching, and there, you accept this second parameter, this reject parameter here so that you can reject the promise and therefore throw an error. If you do that and instead of resolving, you reject with a new error where I say, error, and you save that. If you reload, you will see that error output here and that's coming from that ErrorBoundary. And I'm showing you this here, not because ErrorBoundary would be a feature that needs Next.js or some special project set up, that's not the case. ErrorBoundary components can be created in any React project, but I'm showing you this feature here because it is quite a nice combination if you have suspense and ErrorBoundary working together. Because with that setup here, you have a component that handles the data fetching, then you have a component that handles the loading state, and then you have a component that shows an error if loading goes wrong. Now, when working with Next.js, you also have other ways of handling errors as you learned in the previous section, but this is an interesting pattern that you should be aware of when using features like suspense and the use hook or React server components and data fetching.