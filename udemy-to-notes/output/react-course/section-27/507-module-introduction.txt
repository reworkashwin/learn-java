Now, in the previous course section you learned a lot about next JS. And there you also learned a lot about features like React server components. In this section, we'll now revisit those features and kind of summarize these core concepts, which are actually part of React, but which are not usable in all React projects. Therefore, you can also take this section, if you did not complete the previous section, though, I would recommend going through that Next JS section first, but it's not a hard requirement. Now, in this section here, as mentioned, we'll take a look at some key React features that are part of React, but that are not usable in every React project. And I'll explain why that's the case. And we'll specifically take a closer look at React server components and client components, what these are and how you use them. We'll explore the idea behind server actions and compare them to form actions, about which you learned in a separate course section already. And we'll revisit React's Suspense component and how it relates to the use hook, which is a hook that was added with React 19. It's a hook I already introduced in the React context section, because you can use it to get access to context, but in this section you will learn that you can also use it to get access to data that's resolved by promises, at least in certain projects. So let's dive in.