And that's now it for this theory part here. You did now learn about these crucial React features, which are part of React, but which can't be used in all React projects and I hopefully could explain why that's the case, because these features, at least to some extent, need a server side environment and the code therefore must be split. And I hopefully also was able to explain these different features, what they do, how you can use them in a Next.js project here because at this point there aren't too many projects that support them. That's why I used Next.js as an example. And I also showed you how these different features can work together. And therefore, if you are working in a React project that does have these features enabled, like a Next.js project for example, you should now also be able to use them.