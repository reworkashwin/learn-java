Now, something we'll not see a lot in this course is that we'll reach out to the DOM and select elements in the DOM manually in JavaScript code to then read or change them. Now, being able to reach out to the loaded website and read and manipulate it at runtime is of course a strength of JavaScript, but it is also something we won't do when using React, because React will do it for us, because we write React code in that declarative way as you learned in the first course section. So selecting elements, for example with querySelector to then remove them from the screen, for example, like this, is not something we'll do a lot or at all in this course. Instead, we'll use React for that.