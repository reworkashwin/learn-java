So now that we got this form action set up, let's extract more values from the submitted form. And for that, I'll first of all rename this to just email so that it's a bit shorter, and then I'll extract all those other values. For example, the password by using formData.get('password'). And as mentioned, these values here, which we pass to the get method must be the values that were set up as names for these inputs. So the password is one of them, then I also want to get the confirmPassword value with formData.get, and then the name here is confirm-password. So I'll extract that here. And then get the firstName with formData.get('first-name'). That's the name that's set for this input. Then the lastName with formData.get('last-name'), and then the role with formData.get('role'). Then also the terms with formData.get('terms'). And then last but not least, the acquisition channel, which is in the end, this collection of inputs down there where all inputs have the same name, acquisition, but where the user can then check one or more of these inputs to select one or more channels they came from. And we can extract that value or those values since it can be multiple values, and store them in a constant called acquisitionChannel with formData.getAll, and then the name of those inputs, acquisition. GetAll simply makes sure that if multiple boxes are checked, we get back an array of values here instead of just one value. And these are then the values that can be extracted from that form, from that formData, which we get automatically from React. And now of course, you can use these values and validate them because of course, the user may submit this form in an invalid state if nothing's been entered, for example. And in that case I want to show an error to the user just as we also did it earlier in the course in an earlier course section. Now there are of course different ways of implementing validation here. Here, I'll add an errors array, which is empty initially. And then I'll add a couple of if checks to check for the validity of different fields. And for that, I prepared a validation.js file in the util folder, which contains some helper functions we can use for validation. And therefore in this Signup.jsx file, I'll import something from that validation file. So by going up one level here and by then targeting this validation file, and I then want to import isEmail here, isNotEmpty, isEqualToOtherValue, and hasMinLength. These four functions because I'll use these four functions here, starting with isEmail. And I want to check if the value stored in that email constant is a email address, or to be precise, I wanna check the opposite by adding this exclamation mark here so that I check if it's not an email address. And in that case, if we make it into this if check, I wanna push a new error message into this errors array, and that would be the, "Invalid email address," message. Now the next check then could be that we check if the password field maybe is empty. So if it's not not empty, since I only have a isNotEmpty function. I'm checking for the opposite here and I'm checking if the password is not not empty. So if it is empty, or if it doesn't have a min length, the password of six characters. If one of these two conditions is met, I also wanna push an error message here to this errors array, and that would be, "You must provide a password with at least six characters," or something like that. Then I want to check if my passwords are equal. So the enter password and the confirmation password. And for that, we can use the isEqualToOtherValue function and check for the opposite here as well and check if the password is not equal to confirmPassword. And in that case here, I'll push, "Passwords do not match," into this array. Well, and then we can also validate the name and check if isNotEmpty, or check for the opposite to be precise if it is empty for the firstName, or if the lastName is empty. And if either of these two names is empty, I also want to push another error message into this array here, and that error message now is, "Please provide both your first and last name." And then I also wanna check if the role is maybe empty. So the role that was selected in that form. And if that's the case, I'll push this error message into this array, "Please select a role." Now I'm almost done. I'll just add two additional checks, and check if we don't have the terms check box clicked. So if terms is falsey, and terms is simply the value I get from this, "I agree to the terms and conditions," checkbox down there. So if that is falsey, the user has not checked it. And in that case, I'll push, "You must agree to the terms and conditions," into this array. And then last but not least, I'll check if acquisitionChannel, this array has a length of zero, which means the user didn't select a single acquisition channel. And in that case, I also wanna show an error and say, "Please select at least one acquisition channel," like this. So that's lots of validation logic here, lots of steps to validate this input, but with that, we're now really using these submitted values. And the next step now of course is to show these error messages to the user if they did submit an invalid form, and that's therefore what we'll do next.