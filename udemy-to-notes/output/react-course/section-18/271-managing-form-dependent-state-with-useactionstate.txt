So how can we now use this errors array here to show error messages to the user when they submit an invalid form when using this React form action's feature? Well, in this action function we could check if our errors array has a length that is greater than zero. So if we have at least one error. And if that's the case, we could return an object, let's say, though you can return anything in such a action function. You don't have to return anything at all actually, but if you do want to return something, it can be anything. And here it's an object, an object where I'll set the errors key, let's say, to my errors array. But that key name is totally up to you because you can, as mentioned, return any value of your choice, or nothing at all. And therefore, if you return an object, the shape of that object is also totally up to you. But here I wanna return an object that has an errors key and the value for that errors key is my errors array. And since I have the same names here, I can use this JavaScript shortcut to create this object with this key value pair like this. Now, if we make it past this if check, so if we have no errors, I therefore wanna return an object where errors is set to null so that I have some indicator whether we have errors or not. If it's null, we have no errors. If it's an array, we have errors at least one. But how can we use this return value when using this form action's feature? Because this action function is passed as a value to this action prop, how do we now get hold of that value that's returned by that action function? Well, for that, React gives us a special hook that we can use, a special hook that must be imported from React, a special hook that's called useActionState. And that's a new hook that was added with React version 19. So if you're using an older version, you can't use this hook, but that shouldn't be a problem because you also can't set a function as a value for the action prop there. So I have this useActionState hook, how do we now use it? Well, like all hooks, you must use it in your component or inside of some other hook. And here I'll use it after this signupAction function, which I collapsed here so that it takes up less space. So useActionState is called here after this action function, and I'm calling it after this action function because this action function now actually must be passed as a first argument to this useActionState hook. So this useActionState hook wants a action function as a first input value, but that's not all useActionState wants. Instead, as its name implies, this hook aims to manage some form-related state for you or some action-related state. But actions of course are related to forms, so in the end, it's some form-related state that's being managed here. And therefore this useActionState hook actually also wants a second value where you provide an initial state value. Because this action function may then return an updated state, this object which we return here, but if the form has never been submitted, of course this action function will have never executed, and therefore no value will have been returned at any point of time. That's why you also must provide an initial state for this hook, which will be active if the action has not been executed yet. And in my case, that is an object where I'll set the errors key to null because initially if the form hasn't been submitted, I don't want to show any errors to the user because they didn't even have a chance of inserting any values. So that's the initial state here. Now, useActionState does not just take input values, but it also returns something, and it does return an array and therefore we can use array destructuring here to store the different array elements in different variables or constants. So just like the useState hook, which you already know, useActionState also returns an array and it does return an array with three elements. And the first element it returns is the current formState. So initially, this initial state here. But then once the action has been executed, the state that was returned by that action. So those return values here, that's then what will be stored in this formState constant or variable. And that's how we can get access to these return values because formState will eventually contain them. But that's not the only thing we get in this array that's returned by useActionState. Instead as a second element in that array, we get an updated formAction. Now we pass an action as a first argument to useActionState and internally React then kind of creates a new function that's wrapped around our action. And it does that so that it can listen to the invocation of this action, you could say. That's why we get back and updated formAction, which in the end is our formAction, but enhanced by React, you could say. So this formAction which we get back is our formAction with extra features or with React being aware of it if you want to call it like this. And it's there for now. This formAction which we get back from useActionState, which should be set as a value for this action prop here. So this action is set as a value here for the action prop. Well, and the last element we get back here is a pending element, which is true or false depending on whether the form is currently being submitted or not. Now in this case, this doesn't make much sense because this action executes pretty much instantly anyways, so it won't be pending for long. But later in this section when we explore async actions, that will change. But for the moment, I don't need that pending value. But I do want to work with that formState now because as mentioned, eventually it may contain the error messages which we pass back here if we have errors. And therefore we can use it here in our form, maybe at the bottom of that form, let's say right above these buttons to conditionally output some error messages. So here I'll check if formState.errors is truthy, so if it's not null, which it is initially and which it is if no errors were found. But if it's not null, so if it contains this errors array, then I wanna output something down there. I wanna output an unordered list with a class name of errors for styling purposes. And in that unordered list, I'll go through my errors and map every error object to a list item where the key can be the error message, and where I then also wanna output that error or message. And that should make sure that error messages are displayed if we got some. Now with that, if we save that, if we go back to our form here and I click sign up without entering anything, I get an error that formData.get is not a function. And I get this error now because when using this useActionState hook, this formAction function, which we pass to useActionState, so this signupAction function in our case is called in a different way than it was called before. Previously without using useActionState, our action function was called and the formData was passed as the first and only input value to this action function by React. But when passing signupAction to the useActionState, this action function will be called differently. And formData will actually be the second input value, the second parameter, because the first parameter will be the previous form state, so the old form state, because of course it's possible that this action is invoked multiple times. And in that case, React gives you the last form state it was aware of as an input value in case you wanted to base your new state on that old state. And the first time this is being executed, the previous form state will simply be that initial state that's being passed to it. Now here, I don't really need that previous form state, but nonetheless we need to accept it and extract formData as a second parameter since we'll get an error otherwise. But with that done, if we now save this and go back, if I now click sign up here, you see now I get these errors down there. And the styling is a bit off because in my form, the class here should be error, not errors. But with that done, this now looks better. So that works. But we currently have a problem. If I enter a valid email address, for example, and I then click sign up, the overall form of course is invalid. And that's why I'm getting some errors though I'm not getting the email error message anymore because that was a valid value. But as you can see, the field here was cleared because every time you submit a form, when using this React form action's feature, React resets the form for you in the end. And that's of course not what I want here. If I have some invalid values, I definitely don't wanna throw the valid values away. Or in general, I don't want to throw the values the user entered away. That's not a good user experience. So that's the next thing we'll have to fix here.