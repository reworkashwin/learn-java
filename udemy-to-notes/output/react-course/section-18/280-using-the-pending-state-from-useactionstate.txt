So how could we improve the user experience here? Well, for example, by again, disabling these buttons after they have been clicked. And I wanna disable both buttons if one of them has been clicked so that we have to wait for the vote to be registered before we can vote again. Now we could use this useFormStatus hook for that, but in order to do that, we would have to outsource these buttons into separate components, as you learned. And of course we can do that. There's nothing wrong with that. But here, I'll show you another way of handling this because instead of outsourcing the buttons, we can use the useActionState hook, which you already know because as I already mentioned before, this hook also gives us this pending information. So this information, whether the form is currently being submitted. Here, I have to call this hook twice though because I have two actions, and one hook can only handle one action. So the first hook here handles upvoteAction, and the second time I call it, I want to handle the downvoteAction. Now the initial form state is null because I don't manage any form state here. We could also omit this therefore and not pass any initial state to it. And then just as before, we'll of course get back those elements I mentioned before. So we'll get that upvoteFormState, we get an upvoteAction, an upvoteFormAction if we want to call it like this, so that we don't have any name clashes. And the information whether this upvote form is currently pending. And of course the same is true for the second time we call this hook. But now it's all about downvoting here. So downvoteFormState, downvoteFormAction, and downvotePending. Now I don't care about the states here to be honest, but when destructuring the array like this, I still have to extract those elements. But here I'm now mostly interested in the actions and the pending states. So it's now the upvoteFormAction that's set as a value for the first button here. So this upvoteFormAction is set as a value for this first button. And downvoteFormAction is set as a value for the second button, like this. Well, and then we can use these pending states to disable these buttons. So the first button is disabled if upvote is pending, or if downvote is pending. So if either of these two buttons has been pressed, that's the idea here. And therefore it's exactly the same code that should be added to the second button here. And with this adjustment made, if you go back, if you reload, if you click the up upvote button here, for example, you see the buttons are disabled until this up upvote has been registered and handled. And the same is true for downvoting. So that's now arguably a better user experience. But we can do even better than that.