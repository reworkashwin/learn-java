So let's handle this form's submission with this form actions feature. And for that we should go to the new opinion component, because it's that component that contains this form. And in there we can, of course, now add a new form action function. And this is now actually also a great exercise for you, because before we dive deeper and turn this into an async function and send an HTTP request, I want you to handle the submission of this form and this component with help of a form action as you learned it over the last couple of lectures. So you should also validate the input in any way you want. Doesn't matter, I'll show you my way soon, but you can validate it in any way you want. And then show some error messages if invalid values have been entered. But also make sure that the form is cleared if it has been submitted with valid values. And at the moment, you don't need to do anything with the entered values, just use them for validation, nothing else. We'll connect this form to the backend together soon. But to practice what you learned, handle the submission with help of use action state and by adding validation. Here's a short pause for you to pause the video. Thereafter, we'll implement this together. And we could define this function outside of the component function as well, but soon I'll use a feature in that function that requires it to be inside of the component function. And that's why I'll define it in that component function right from the start. So it's again, a new function, a share opinion action function, for example. Obviously the name is up to you. And as you learned, we get a form data object here, at least if we now use this function as a value for the action prop of this form, which is what I'm doing here. So now this function will be called by React when that form is submitted. And in here we can then extract the entered values, like the username, the title, and the body of this opinion that is being shared. So here I got my title with help of formData get title. I got the body, so the text that, in the end, contains the opinion from the body input field, which is a text area. And then I got the username from formData get username, like this. Now of course we might want to validate that user input, and therefore I'll do it in the same way as before. I'll add an errors array and then add a couple of if checks, and check if the title, after trimming it to remove access white space at the beginning and end, if it has a length smaller than five. So if it has less than five characters. In that case, I want to push an error message onto this errors array, and that should be, "Title must be at least five characters long." Now my second check is for the body, which I also trim, if that has a length smaller than 10, or if, after trimming it, it has a length greater than 300. In that case, I want to add an error message and say, "Opinion must be between 10 and 300 characters long." And last but not least, for the username, I just wanna check if, after trimming it, it's falsy. So if it's an empty string after trimming it. So after removing excess white space. And if that's the case, if the user did not provide a username, I'll say, "Please provide your name." Like this. Now, just as before, I then wanna check if errors length is greater zero. So if I have errors. And in that case I wanna return an object, which we then will soon handle with help of use action state, an object where my errors are stored in an errors key using the shortcut here. But of course I wanna make sure that the entered values, so the values entered by the user, aren't lost. So just as before, I'll add this entered values key here and store a nested object in there where I then store all the entered values. So the title, the body, and the username, so that we can then use these values with help of use action state to repopulate these form inputs. Now if you make it past this if check, I know that we have a valid form, and in that case I wanna submit those values to the backend. We'll do that in just a second. But I also wanna clear the form now. So I'll return a new form state object where errors is set to null. And with that, we can now add the useActionState hook here to this component. So import useActionState from React and use that with that form action here to handle the state that's managed by that action. So call useActionState here, pass the shareOpinionAction as a first input value to that hook. And as a second input value, the initial state, where errors is set to null, for example. And then I get back these two main values I'm interested in. So I get back the formState and the formAction, and it's now the formAction that should be set as a value for this action prop. And as you learned, once you start using useActionState, your action function will be called slightly differently, and it will no longer receive this formData object as the first and only input parameter. Instead, this formData object will now be the second input parameter, and the first parameter will be the previous formState. So that adjustment must be made here, even though I'm not using that previous state here, but we must accept it, and accept form data as the second parameter. With that, we can then use the formState here to, for example, output errors. So here, above my button, I'll check if formState errors is truthy. And if that's the case, I'll output an unordered list with a class name of errors. And now it is errors, not error, where I then go through my errors like this, with the map method, and where for every error I output a list item where the key is set to error, and where the error text is then the content of the list item. But of course, I also wanna make sure that we keep the values that have been entered by the user, and therefore we can also use that formState to again set the defaultValue here. So the defaultValue for this username input is formState.enteredValues, checking if that exists. And if it does, I access userName written like this. And then I'll repeat that for the title and access title. And for the text area where I set this to body, because that's the name of this input. And that's it. That will make sure that these entered values are kept. So if we save this now, and I reload and open my console to check whether I get some errors, if I enter a first name but nothing else and I click submit, I get some errors here. But my value is kept around. If I enter a title and click submit, I still get an error, because it's too short. If I add a longer title, this error goes away. And if I now also enter an opinion text, this all goes away and the form is cleared thereafter, and I got no error messages here. So that works. And as a next step, we can now turn this into an async formAction, and actually make sure that an HTTP request is sent.