So now that we're successfully fetching the places here, there's one additional thing I wanna do to that data before I set those places as my available places. Because in this application I actually also wanna sort these places by distance to the user's location. And for that I got this loc.js file, which got all the functionalities and functions to do that sorting. In the end here, I'm simply calculating the distance between two positions on the world made up of latitude and longitude coordinates. So this file is already there, but now in AvailablePlaces I need to fetch the user's location before I set my AvailablePlaces. And that's something we also did earlier in another section. And I'll do it again here because we can fetch the location of a user in the browser by using the navigator object that's built into the browser. So that's not provided by me or React. Instead, this is coming from the browser. And on this navigator object, there is a geolocation object, which then has a getCurrentPosition method. And you can call this method to get the current position of the user. Now get current position, just like sending an HTTP request takes some time. The position is not available instantly and therefore getCurrentPosition works such that it takes a function which will be executed eventually by the browser in the future once the position has been fetched. So here we can't use Async await because getCurrentPosition does not yield a promise but here we're using this callback pattern to define the code that should be executed once the position is there. And this function which will be executed by the browser once the data is there, will then receive a position object that contains the user's coordinates. And it's therefore now inside of this function where I want to set my availablePlaces, but I first want to sort them. And to do that we can add a sortedPlaces constant, or variable which holds the result of calling sortPlacesByDistance, which is a function from that loc.js file, which must therefore be imported in this AvailablePlaces.jsx file. And with it imported, we can call it here and pass our resData.places to it. We also must pass the user's latitude and longitude coordinates to sortPlacesByDistance and we can get that out of the position object. Here, it's position.corrds.latitude for the latitude and position.coords.longitude for the longitude. And it's now the sortedPlaces that I want set as my AvailablePlaces. And my key point here is that I wanna show you that of course you can work with that data you are getting back in any way you want and you can perform other long taking operations thereafter and simply update your AvailablePlaces in this case once you are done. But now here's something important to watch out for. Since we're not using Async await here, but instead this callback function we have to change the place where we call setIsFetching and set it to false. Because doing this down here would set it to false too early. In the end, I also wanna wait until we're done sorting and fetching that position. And since we can't await this here since this won't work here, we can't call setIsFething here because this code would now execute too early because JavaScript will not wait with executing this code until this callback function here is done. Instead, it will call this code here right after we initiate it, that position call. And therefore now we should move setIsFetching into this callback function after setting the AvailablePlaces. And in addition, we should also call it after setting the error if we have one, so that we set isFetching to false if we're done fetching and sorting our places or if we have an error. And with that, if we save this we see that once we load this page we're asked for permission to get the user's location and we see that loading text until we do that. And once I do that, it will start getting my location and we still see the loading text whilst that's happening. But then once the location has been fetched the places are displayed. And if I reload the app now that I granted the permission, this will happen quicker. But still it can take some time until my location has been fetched.