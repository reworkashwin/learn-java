So we're now also able to send non-GET requests that may carry some data. But of course if we take a closer look at how we're sending that request here there are a couple of noteworthy things here. For one, I'm for example not managing a loading state here. And I did mention before that it's quite common to have these three pieces of state when sending HTTP requests. And that is absolutely true, but here in this App component, instead of managing some loading state and showing some loading text or loading spinner to the user I'm doing something which is called optimistic updating. I'm updating my local state before I'm sending this request and before I'm waiting for the response. So when I pick a place here, as you can tell the UI updates instantly and the request is basically sent at the same time behind the scenes. Now of course, sending that request could fail though. Therefore, here when catching this error what I want to do is I want to set my userPlaces again and I wanna reset them to the state they were before. So in the end here, simply to the old userPlaces state and I'm not using this function form for updating here because here I don't wanna have that state that includes the new place. But instead I wanna simply use the old userPlaces state that will not include that newly selected place so that if something goes wrong, we roll back that change and we update the UI again. And often optimistic updating can provide a better user experience than showing a loading spinner or some loading text. Though it of course depends on your preferences and on the exact situation you are facing. It's not always possible and especially when fetching data you of course simply have to wait for it until you can use it. But when performing updates as we're doing it here optimistic updating can be a good alternative. Alternatively, we could of course also move this code here where we update our places in front of this state update here. And now this state update would wait until this request is done. But if we're doing that, we should be showing some loading text or loading spinner because otherwise it might seem to the user as if the application is stuck. So I'm not doing that here. I'm going to comment out this code, but that would be an alternative way. And again, it simply is up to you which approach you prefer. Here I'm sticking to this optimistic updating. Nonetheless, if we are facing an error here we still might want to inform the user about it because of course rolling back the change is nice but it still looks rather strange to the user if a change made by them suddenly disappears without any explanation. So we still might want to manage some extra state in this component here, an extra errorUpdatingPlaces state, for example with a setErrorUpdatingPlaces state updating function, which initially holds no value, which we do update if updating our places goes wrong. So here in this catch block, besides rolling back the change I now all the wanna call setErrorUpdatingPlaces and then set my state to an object where we have a message property, which is equal to error message if this is available or as a fallback failed to update places, for example. And now we can use this errorUpdatingPlaces state here to show an error message on the screen. And of course, as always, there are lots of different ways of showing such an error message. It is up to you in the end, but here I'll simply show a modal. So I will add a modal here using that Modal component and set this to open if errorUpdatingPlaces is truthy. So if we have an error. In that case, I will show my Error component, which I shared with you earlier and which therefore of course also should be imported. And there I'll set the title to an error occurred and the message is equal to errorUpdatingPlaces.message. Now of course, this modal here should then actually also be closable and clear the error message and therefore I'll add another function here. Let's say maybe right here, which I'll name handleError where I'll simply call setErrorUpdatingPlaces and set this back to null so that we clear the error and then we can use handleError here as a value for the onConfirm prop, which allows us to close the modal therefore, since we clear the error, and also on the onClose prop on the modal to clear the error if the modal is closed by pressing the escape key for example. I also wanna make sure that this error component here, which is wrapped by Modal, is only rendered if errorUpdatingPlaces is truthy because otherwise trying to access message here will fail. Even though it will only be visible if the modal is visible, it will actually be part of the DOM right away because the modal component is always part of the DOM as well. It's just not always open, but it's always there. And therefore we would always be trying to access errorUpdatingPlaces.message. This will fail though if we have no error and that's why I'm actually rendering this error here conditionally. And with that, if we save that and reload I'm fetching my places, I can edit them. But if we now force an error, for example by again using an invalid URL, by adding multiple S's here at the end, and I save this. if I now click on an item here like this, you see I get this error modal and I can dismiss it. But of course now we would have to fix the error before we can continue. And that is what I'll do here by getting rid of all those S's. But that's how we can perform optimistic updating and how we can and should still handle errors.