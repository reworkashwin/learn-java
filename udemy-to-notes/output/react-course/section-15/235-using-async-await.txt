We're now successfully fetching data here, but this data fetching code can be improved now. For one, using all those then blocks here works and is definitely readable, but many people prefer the async await syntax I briefly mentioned before, and you can use it here as well. Now, what you should not do when using useEffect is add the async keyword here and then try to use await here. As you see, I'm already getting a warning in this project that this is in the end not allowed or not supported by React. Instead, what you can do to use async await inside of such a effect function is you can create a new function. Because, of course, in JavaScript, you can create functions inside of functions which could be called fetchPlaces. And here you can add async because this is now simply a function defined by you, not a function that would be used in any special way by React. Now you can grab this code where you send the fetch request and await this, and as a result, you'll get back the response. Then as a next step, you can get the response data by awaiting response.json. And then as a last step, you can set the places as we did it before and now get rid of all that code here. And this code is, of course, now a bit more readable and structured. Now, of course, now you also must make sure that fetchPlaces gets executed though because at the moment, that's not happening, and therefore here still in the effect function. But outside of fetchPlaces, you should call fetchPlaces like this. So you define it and then you immediately call it. But by defining it and decorating it with async, you can use this await keyword. So that's a nice little and pretty common trick for using async await in an effect function. And with that, if you save that, we're, of course, still fetching the data as we did before.