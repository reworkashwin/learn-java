Now, in this application here, selecting places is not the only way of updating our places. Instead, we can also get rid of places we picked by again clicking on them in our selected box here. And then here, I can clear a place. At the moment, though, this is ignored. The place will still be part of my user-places.json file on the backend, so it will not be cleared there. And how would it? We're not sending any request to the backend if the place should be deleted. But we can now easily change this. In the app JSX file, in the handleRemovePlace function, we can also send the request to the backend to get rid of the place. For this, we can turn this into a async function. And I actually already did this for you here. And then we can again perform optimistic updating by first updating the state and then sending an http request. For this, we can again call updateUserPlaces here, and send our updated places to the backend for which I'll use the userPlaces state, call filter, and then filter out the place that has the same ID as the selected place I'm getting here or I currently selected before, which is stored in my ref here. So I'm using the same logic here as I did here for updating the state. And we can now await this before we then proceed. Now, since I'm using this userPlaces state here inside of handleRemovePlace, we also should add this as a dependency to the useCallBack dependencies array, though, to make sure that this function is indeed recreated if the userPlaces state changed. Which is crucial to ensure that we are sending the right updated places array to the backend. Now of course, just as before, this could fail. So I wanna wrap this with try catch again. And just as before, if it does fail, I wanna roll back my change and show an error message to the user. So here I wanna set userPlaces equal to the old userPlaces before they were updated to roll back the change. And we can set our ErrorUpdatingPlaces state here to an object where we have this message property which is equal to error.message or as a fallback, failed to delete place. Something like this. And with that, if we save this, if I open this and delete a place, you see that seems to work. This request was sent to the backend and if on the other hand, I force an error by again adding all my S's here, though I'll only save that after I added at least one place, but now if I save that, and I then go back and try to delete that, you see I got that error and the change is rolled back. So, that's working, and I'll therefore get rid of those places, and hence, the last remaining piece that needs to be added to this application is of course the logic to fetch those user places when the app loads. Because that's currently not happening here. We're never fetching those user places.