Now to clean up the code in this AvailablePlaces file a little bit and make this file a bit leaner, I will now add a separate helper file which I'll name http.js next to the loc.js file which is totally optional, but where I wanna add a separate function, an async function called fetchAvailablePlaces which should be exported. And my idea is to place this data fetching code, this code here in the end where we fetch the place data in this http.js file So I will grab this code here where I sent the request and check whether it's okay and cut it from AvailablePlaces and instead move it in here into fetchAvailablePlaces. And then there after we checked that the response is correct if we did not throw an error, I also wanna return resData.places here. So that this is a utility function that can be called to send that request and either get back an error or the places. And back in AvailablePlaces, I now will import this function from this newly added http.js file, and here I named the function fetchAvailablePlaces. And we can now call this here, still in that try block because of course, fetchAvailablePlaces may throw an error either because we throw it or because the fetch function throws one. So we'll execute it here in the try block and still await it because fetchAvailablePlaces will return a promise because every function you decorate with async will yield a promise. So here we can await this and we'll get back our places or an error, but now we can use the places here instead of resData.places. And the rest of the code can therefore stay the way it is. But now we were able to outsource this actual fetching code, the actual code that sends the HTTP request. And we could now potentially reuse that in other places of the application that needs the same data. And with that, if we save that and reload, you will see that this should still work and we still fetch our places here.