So fetching the data works, but here's something you should try. Go to your developer tools, and then there to the network tab. And now, there enable throttling and turn on slow 3G. If you do that and you reload, you'll see that the page takes some time to load. But then once it did load, you also see that it takes some time to fetch the places, and I don't just mean the images. Instead, if I do that again, we can see that initially we see no places available until at some point of time the places data arrives. And that's of course not really a great user experience. It would be better if we would be showing some fallback here, some fallback text or some loading text whilst we are waiting for the data to arrive. Now, this places component indeed supports a fallback text prop here where we can set some text in case we have no places. But that's not really what I wanna show here, because we might have places, we just didn't fetch them yet. So I wanna show some separate loading text, let's say, where we could say fetching place data, dot, dot, dot or something like this. And at the moment, this places component does not support such a prop, but it would be nice if it would and if it would then also have a prop like isLoading, where we could pass true as a value as long as we're waiting for the places to arrive and falls once the data is there. So inside of places JSX, we should accept these two props isLoading and loadingText, and then use these props to show some special output. For example, here right below the title of this category box here, we could check if isLoading is true, and if it is, show a paragraph with a class name of fallback text for styling reasons, where inside of that paragraph here we output our loadingText. This could be our fallback if this isLoading prop is true. And on the other hand, I only want to show the other fallback text if we got no places or the places data if isLoading is not true. So here we could check if not isLoading and places length equals zero, and if not isLoading and places length greater zero. With that, if we set isLoading to true here, in available places, we see that fallback text here. But of course, this at the moment is not a great solution, because at the moment we always show that. And instead of always showing this, this should of course be controlled dynamically and it should depend on the progress of our HTTP request here. As long as it's on its way, this should be set to true. And once the data is there, this should be set to false. And therefore when sending HTTP requests like this, it's quite common that you don't just wanna manage the data that's going to arrive with state, but that you also want to manage some loading state here. So some extra state that could be called isFetching and setIsFetching, which initially is false, for example, but which is set to true inside of this effect function here. So that here we call setIsFetching and set this to true, because we're now starting to fetch places. We therefore could also do that inside of fetchPlaces. Doesn't really matter. And then once we're done, once we've got the data here, we set this to false again, because we're not fetching the data anymore. Now, it might look like there are just three lines of code between these two state updates, but keep in mind that this step here can take a couple of seconds. So there actually may be a couple of seconds between these two lines of code being executed. And now we can use this isFetching state as a value for isLoading, so that it will either be true or false depending on whether we just started the HTTP request or we already got a response. And therefore, if you save this and you reload with that network throttling still enabled, you will of course see that again fetching the React code takes some time. But that, once it's there, we see that fallback loading text, and then the actual place data eventually. And with that, I'll disable that throttling, but this is how you can display such a loading state based on your data fetching.