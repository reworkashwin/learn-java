So that's how we can add an item. Of course, we also wanna make sure that we can remove items. And for that I'll actually also start by getting my existing cart item index. So I'll copy that code from add item and add this here in remove item. Because again, we wanna update that existing cart item in an immutable way. Now here, when removing items, we don't have to check whether an item exists already or not because in this app we'll only be able to remove items if they do exist because we'll only be able to reduce the amount of items in the shopping cart from inside that shopping cart screen. So therefore we don't need to check whether an item is part of that screen, but we will need to check what the quantity of that item is because if it's greater than one we wanna reduce the quantity. If it's equal to one, we wanna remove the entire item from the shopping cart items array. So therefore here we'll need to grab our existing cart item by reaching out to the state items and accessing this existing cart item index. And then we wanna check if this existing cart item quantity is equal to one. Now, if that's the case, we wanna remove the entire item from the shopping cart. So I then wanna get my updated items, which in the end should be my old items minus this item here. So minus this existing cart item. And there are different ways of achieving this. One way of removing the item is to again create a copy of the old items. So create a new array based on the old items and to then call the splice method on that array. Splice takes an index, in this case the existing cart item index and then the number of items that should be spliced, which here simply means removed. So this will remove one item at this index which simply means it'll remove the item at that index. So that's how we can remove a item if it's the last item in a shopping cart. If the quantity is greater than one though we wanna update that quantity. And for this, we'll need an updated item which is a fresh copy of the existing item. And we then need to update the quantity and set it equal to existingCartItem.quantity minus one. So that we create a new item based on the old item where we reduce the quantity. And now we just need to update our existing items. And to do that I'll actually move this updated items array creation here out of that if block, so that I can use it in both the if and also the else branch of this if check. And then in the else branch, I'll set updated items for this existing cart item index equal to my updated item here, which has that reduced quantity. And that's again it. Now we just need to return that updated state outside of this inner if block, but still inside of this if block. And here it's in the end the same logic as before in the add item if block. We wanna return a new object that copies in the old state and updates the items. And that should be the cart logic. Now we can connect this cart logic with help of our context here to our different components. So now to connect this context to our different components we first of all must start using this reducer inside of this context provider component here correctly. Now for that I'll start by using array destructuring on this useReducer hook because here we'll get back our cart state, I'll just name it cart here and a dispatch function. And I'll name it dispatchCartAction. And now I want to pass my cart state to this CartContext provider component here because that then implicitly passes it to all other wrapped components that are interested in that state. So for that here I'll set up my cartContext value object where I want to have an items property because I did define that up here in this dummy initial value that my context value should have items property and it should then also have these methods. We'll add the methods in a second, but here's the items property and that should be equal to cart items. And therefore of course whenever this cart state changes this here will also change and this new context will be distributed to all interested components. At least once we start adding the value prop here to the provider component and we set that equal to this cart context object. Now we'll distribute that value. Of course, we must make sure that the items also can get updated and therefore here I'll add a add item function and remove item function to this CartContext provider component. And here I expect to get a item as an argument, here I expect to get the id of an item as an argument. And in addItem, I now want to call dispatchCartAction and dispatch an action object, which again can have any shape of your choice but which here to make this code in the reducer work should have a type of add underscore item. So I'll add such a type property of add item of this string here to this dispatched object here. And then I also will add a item property here to this action object because we're looking for such a item property here. So we must make sure that this property exists and I'll set it equal to this item which we're getting here though of course since we're using the same name here and here we can now also use this JavaScript shortcut. Now for remove item, we also want to dispatch a cart action but now the identifier is REMOVE_ITEM, or whichever identifier you used in your reducer. And I now actually also have to change my reducer because here I'm getting an id as an input. That's what I'm expecting. And therefore I wanna forward that through the action to the reducer. But at the moment in my reducer, here in the remove item case, I'm looking for an entire item property here, but that's no problem because I only need the id here anyways so I can simply access action id here and everything should work. We don't need the full item here, unlike with the addItem case because when removing the item we just needed the id to identify it. So that's therefore these two actions and those should now also be added to this cartContext. Therefore I'll move that below the actions and then add an add item property to my context and as a value set a pointer to this addItem function. And again, since we have equal names we can write this a bit shorter and do the same for remove item. And therefore now these two functions are also part of this context object and can also be accessed from other components in the app and can therefore be triggered from inside those components. And that therefore is the next step to use this context from inside other components. Now as a first step we therefore have to wrap this CartContextProvider around all the components that are interested. And in the end here, id offer one wrap, basically all my app components and all their nested components with that provider component. And hence the best place to do that is the App component, the root component of this application. Here we can import as a named import CartContextProvider from ./store/CartContext.jsx. And then simply wrap this around Header and Meals. And this will allow all these components and their nested child components to access this context and its properties. And we can now start in the MealItem component, which has access to the context because it's used in the Meals component, which in turn is used here in the App component and wrapped by CartContextProvider. So in MealItem, I want to add a item to a cart whenever this button here is clicked. And to do that, we therefore can add a function here, handleAddMealToCart, could be the function name and we can connect this function to this button through the onClick prop, which we can set on this custom button because we are forwarding all props to the underlying native button element. So in MealItem.jsx, we can set onClick equal to handleAddMealToCart like this. And here in handleAddMealToCart, I now want to call this addItem function of my context. Therefore we of course need to get access to that context and we can do that with help of the useContext hook, which must be imported from React. To use context, you must pass your context object as an identifier. So therefore here I'll import CartContext, not the provider component, but instead the context object itself. So this default export and therefore this object that's defined in the CartContext.jsx file. And I'll import that from going up one level, diving into the store, and then into CartContext.jsx. Now we must pass CartContext as a value to useContext. And as a result we get back a CartContext object which we can use. And now this object can be used here inside of handleAddMealToCart to call addItem and pass that meal item, which we're getting as a prop here as a value to addItem like this. And that should do the trick. That should add items to the cart. To see whether that works I'll go to the CartContext.jsx file and in there for the moment we can add a console.log statement in that provider component right before returning. And we can output our CartContext here to see whether it updates as expected. With that out of the way, if we go back to our application reload and open the dev tools, we should see some output here. And we're seeing two outputs here, two objects because this application is running in strict mode and therefore every component gets rendered twice by React during development to help us catch potential bugs and errors. And the object we do see twice here is now our CartContext object with the items array, which initially is empty and these two functions, which we can call. And if I now clear this and I click add to cart, I get another output, which is a good sign. And if we inspect this object, we can see that now the items array does indeed contain this meal item with a quantity of one. If I clear this again and click add to cart again, I see that we still have the same item in the array, so not the same item twice, but still only one item in the array. But that one item now has a quantity of two So that's working as expected. So now to kind of finish this first step of implementing the cart, I wanna make sure that we also use the items data, not just to log it here, but to update this number here in the header. And that's why we used this context feature in the first place so that we can access that data from different places. Because whilst I wanna update my cart from inside the meal item component, I now want to access the items data and get the number of meals in the cart in my header component. And doing so now thankfully is pretty easy because all we have to do is call useContext, which must be imported from React in the header component, pass the CartContext object, which also must be imported as an identifier to useContext so that we get access to the CartContext. And then here in the header, we can get our total cart items by taking a look at the CartContext items. Now, just taking the length would not be enough now because we add every item only once to that cart and thereafter we just increase the quantity. So instead, I'll call the built-in reduce method, which in the end is a built-in method that allows us to reduce an array to a single value, to a single number for example. For this, reduce takes a function as a first argument and a starting value which I'll set to zero here as a second argument and I'll get back to that starting value in a second. And that function, which we pass as a first argument then itself gets two arguments, passed in automatically by JavaScript. And that's the new value which we want to derive. So in this case, the total number of items, to make it very clear what this is. And then thereafter as a second value every item of the array on which we call reduce. Step by step, item by item because this function will be executed once for every item in that item's array. And we then must return a new updated value and extend this updated value that will be fed into the next function execution as a value for total number of items. And this therefore allows us to add up all these different values. And this zero here is simply just the starting value for the first execution of this function for the first item. So here I therefore want to return total number of items plus item quantity to add up all the quantities of all the items in this items array. With that, we got the total cart items number and we can now output this dynamically down here like this. And therefore, now if we save that and I reload, we start with zero here. But if I add items to the cart you see that this number increases. And therefore that's all looking good. The next step now is to make sure that when we click this cart button, we open up a modal and we show some cart data in that modal.