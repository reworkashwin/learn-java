So let's improve this user experience of sending a request. And to do that I'll actually build a custom hook because we have two components, the Checkout component and the Meals component, that both need to send requests, even though those requests are sent at different points of time, but they both do it. And they then also, both in the end need to deal with different request states: failing requests, loading requests and requests that succeeded. So we have that same logic which we in the end need in two different components to update the UI. And since it's some stateful logic that should impact the UI and where changes should impact the UI, we need a custom hook, because just creating a custom standard function won't do the trick. So in the source folder I'll add a hooks folder. And I'll name it, useHttp. This is the file name and in there I'll also export a function called useHttp, and as you learned in this course, custom hooks must start with 'use' to signal to React that the special rules of hooks should apply here. Now, it's now in this custom hook function where I wanna send an http request. Though it's of course worth noting that as mentioned, the request should not always be sent right when the component to which this hook belongs gets rendered. That is the case for the Meals component, but not for the Checkout component. There the request should be sent on demand, so to say, when the handleSubmit function runs. Therefore, as a first step, I'll actually add another function here in this useHttp.js file, which I'll just name sendHttpRequest. A request a little helper utility function that wraps this general http request sending logic. It should be an async function and it should accept a URL. and a configuration object, just as fetch does. And then in here the idea is to wrap fetch and pass the URL to fetch and the config object. So, simple like this. And then we'll eventually get the response by awaiting this call to fetch. Now, this might fail, so the response might not be okay if something went wrong on the backend, and in that case I wanna throw a new error using that built-in error constructor function that's provided by the browser. Now, here the error should get a message and we could hardcode a message like "Something went wrong, failed to send request". But that's pretty generic. To be more specific, I'll actually do something else before I check if the response was okay. I'll take a look at the response data, because even if the response has an error code, it will still carry some data. Because on my backend here, if we take a look at that code here, if I am returning an error response, like here if some data was missing in the orders route, I still return a response that carries a response data object. In this case, an object with a message property that has a more specific error message. Also here, if incorrect data was entered. So, therefore, I'll extract that data by calling await response.json like this and now here when throwing that error, we can take a look at the response data and see if this has a message property. And if that's the case, use that, and otherwise as a fallback, use the more generic error message. And if we make it past this if check, we know that everything's fine and I'll just return the response data here in my helper function. But with that, we can now go back to the hook, because there I wanna use this helper function. Because it's in here, in useHttp, where I'll add another function nested in this custom hook function, which I'll call sendRequest. So almost like my helper function up here, but this helper function here is for in general dealing with sending requests. This function down here in my hook will now be about updating some state based on the request status. So I'll name it sendRequest, and in this function body I wanna call sendHttpRequest, so that other helper function we defined up here. That's being used in sendRequest in a custom hook now. Now, sendRequest can also be an async function here. This is absolutely possible in custom hooks, no problem with that. And now here we'll of course get back our response data, because that's what my custom utility helper function in the end returns up here. But we now also should wrap this with try catch, because of course this might fail here. Because in sendHttpRequest, we are, for example, throwing an error if we got an error response code. And we would, for example, also get an error if sending the request fails in the first place because we have no internet connection or something like this. So we definitely wanna try this and catch errors that might occur. And we now of course also have to manage some state to reflect those different request states in the UI, because the idea with this custom hook in the end, of course, will be to use it in a component. So here I'll use the useState hook and import that from React, of course, so that in here we can, for example, manage the error state. So I'll name it error and setError here like this. And initially this is undefined. But here in my catch block I want to call setError and set it equal to error.message like this. And in case we somehow still get some error that doesn't have a message, I'll also set up a fallback error message here, which could be "Something went wrong", but this should really never show up on the screen, I guess. But nonetheless, we can add it here just to be safe. Now, of course, besides the error state, we also want to have some states for the success case and for the loading state, so when we're waiting for a response. So I'll call useState once again and here I'll manage that loading state with a isLoading state and a setIsLoading state updating function. And initially this will be false, because initially let's say we're not loading. But here in sendRequest we of course wanna call setIsLoading right away and set it to true because we're about to send a request. And I wanna set it to false after this try catch block, because no matter if we had an error or not, we're definitely not loading anymore. Well, and then of course we also have the main case, so to say, where we get back some data from the backend, which we also wanna manage as some state, because in the end that's our success case, you could say. So here in the try block after we got the data, I wanna set it here, I wanna set my data state. And the idea behind this custom hook is now that we can, of course, now return an object here where we expose the data, the isLoading state, and the error state to whichever component is using this custom hook. So that whichever component uses this hook can use this data, this isLoading state and this error state and benefit from those state changes that are triggered in this function. But speaking of this function, this function at the moment of course is never executed. It's just sitting here but we're never calling it. And the reason for that is that it in the end needs to be called in different places. In the checkout component, as mentioned before, it should be called here instead of this fetch call in handleSubmit. On the other hand in the Meals component, it should be called in useEffect. Now, of course a simple solution would be to simply also expose sendRequest like this. And now every component that uses this hook could call sendRequest wherever it wants to call it. And this wouldn't be a bad solution, to be honest. But we can make this custom hook a bit more convenient by also calling useEffect in here. So, therefore, of course you also must import this hook from useState. And now here we wanna call sendRequest for you, so to say, so that in here we call sendRequest like this. Now sendRequest is a dependency of this effect function because it's defined outside of the request function and, therefore, it should be added here. And, therefore, to make sure that we don't end up in some infinite loop because we're updating some state in this function which will cause the component that uses this hook to execute again, which would cause this effect function to run again, because sendRequest will change every time the surrounding component executes again, because a new function object will be created. To make sure that we don't enter this infinite loop, we should wrap this function with useCallback, as you also learned before in other course sections. We should wrap it with useCallback and add our dependencies array here and we get back a send request function here from useCallback, which will now only be recreated when its dependencies change. Now, at the moment I have no dependencies here, but that's not entirely correct, because inside of this sendRequest function, we actually need to pass some data to sendHttpRequest to configure it correctly, because sendHttpRequest at least needs a URL, potentially also a config object. Therefore, our custom hook should get that URL and that config object, or maybe in general, a requestConfig object that should include both. That's, of course, up to you. Here I'll instead go for two parameters and we can now forward URL and config to sendHttpRequest. And we, therefore, also have to add both as dependencies to useCallback because if one of these two would change, we would need to create a new function object because the code in there would change. After all, sendHttpRequest would be called with different data. But with that we can now use sendRequest here in useEffect and, therefore, send this request whenever the component that uses this hook here loads. And whilst that is what we want for the Meals component, it's of course not what we want for the Checkout component. Therefore, here in this effect function in the custom hook we also should add some check that makes sure that send request is not always getting sent. And one check we could add here is that we take a look at this config object which we're getting as an input to our custom hook, and there we take a look at the request method. And if it's 'GET' or if no config object was provided in the first place, we wanna send the request right away. Because here in this app, every 'GET' request should be sent when the component to which the hook here belongs gets rendered. If the request method is something else, I don't wanna send this request. So here we can check if config is truthy, so if it is set at all. And if it is set, if the method is equal to 'GET'. In that case, we wanna execute sendRequest, otherwise we won't do anything. Now, of course, config also must be added as a dependency here because I'm using it here. But with that we should now be making sure that this request is only sent if we have some kind of 'GET' request. And for those other requests I wanna expose sendRequest here by putting it into this object that's returned by the custom hook, so that other components that use this custom hook can also get direct access to this sendRequest function and can execute it whenever they want. So, for example, when a form is submitted. And that now allows us to go to the Meals component and get rid of useEffect here, delete all of that, and delete this useState code here, and delete these imports here. And instead import useHttp from the hooks folder and there the useHttp.js file, and call useHttp here. And now we just have to pass that URL to which the request should be sent as a first argument and a config object as a second argument. Though here, since it's a 'GET' request, we don't really need any config object. And we'll get back an object on which we can use object destructuring to get hold of the data that's eventually returned, the isLoading state and the error state. And we can now use these different states here in Meals. For example, the data which should be output down there. Now, here I know that my data in the end will be the loadedMeals so I'll give it that alias so that the code down there continues to work. And we'll take care about the loading and error state later. First, I simply want to check whether this works and whether it works without any infinite loop or anything like that. So if we save that and I reload this page, I don't see anything, and if I open the console, I get an error. And the error is "Cannot read properties of undefined" when calling map in the Meals component. So mapping here failed, so it looks like the data here is undefined, which of course can only be the case if sending this request fails. Or is that the reason? Keep in mind that sending the request takes some time so the data won't be there initially. Instead, useState for the data sets the initial state to undefined because there is no initial value here for the data. So the initial value of loadedMeals will be undefined until the request is done, but that will take some time and the component function will not wait for the request to finish to do its work. Instead, this JSX code will be parsed and converted to HTML code right away, so to say, and of course it therefore fails to run this code, because loadedMeals, as I just explained, is undefined initially. And that's where this error message here is coming from. Now, to work around that issue, we could try to check if we are in a loading state and in that case return something else. For example, a paragraph where we say "Fetching meals...". But even though you might think that this does the trick, you'll see that if you save this and reload, you still get the same error. The reason for that is that technically in our custom hook isLoading is false initially. It is set to true once we start sending the request, but that's only the case once this effect function here runs which, as you learned, happens after the component rendered. So the first time this component renders will be with isLoading set to false and, therefore, will not return this here, but instead this code which fails for the reasons mentioned before. Now, we could of course set isLoading to true initially, but that will give us problems later once we also wanna use this hook in the Checkout component where we won't start in a loading state. Therefore, the better workaround, the better solution is to accept a third parameter here, for example, which could be called initialData, which is the initial data we wanna set to that data state in our custom hook. Because this now allows us to go to the Meals component and there to useHttp and maybe pass an empty object for the configuration, but then pass an empty array of meals as initial data for the loadedMeals. Because that's what we had before and this will make sure that when this renders for the first time, we won't output any items here, but we also won't fail. Alternatively, of course, you could have also checked if !data, so if data is undefined and you could have returned some fallback texts like "No meals found". That would've been an alternative, but here I'll go for that initial data approach instead, simply to also show you these alternatives. And with that, if we save that and reload, finally we have no error anymore, but we also don't see any meals here. Also no loading text, but also no meals. And the reason for this behavior can be found if we open the network tab and we reload, because here we won't find the request to the backend that would fetch any meals. So it looks like we're still not entirely there yet. We got our logic here, but it looks like this request is not being sent here. And the reason for that is that here we're checking if the config object is set and if it is set, if we have the 'GET' method chosen. The problem is that I am now setting it, but I'm setting it to an empty object here. And, therefore, this part of the condition is met, but this part of the condition is not met. Now, one way to work around that and make sure that we treat an empty config object where the method is not set as an object where we wanna set a 'GET' request is by adding the or operator here and checking if config.method is undefined by adding an exclamation mark in front of that. So we're running this code here if we have a config object and the method is set to 'GET', or if we have no method set, and we should group this here with parentheses so that we always check for the config object and then we have one of these two options to send the request. And there now actually is also another condition which I wanna check for to send this request. If we have no config object at all, I also wanna send it because if I have no config object, that also implies that I wanna go with the default behavior, which is that a 'GET' request should be sent and that the request should, therefore, be sent right away here. So by adding this condition where I either have no config or I have a config with the method set to 'GET' or not set at all, with this check added here, this request should now finally be sent. But after making this change, you'll see that if we reload again, we're back in error land, but this time not because loadedMeals would be undefined, but because it instead seems to be something on which we can't call map. And here it helps to go to the Meals component and simply console.log loadedMeals to see which data is stored in that state. If we add this and open the console and reload, we see that we start with this empty array, but thereafter it's a promise. And that's not the right value, it should instead be an array of meals, not some promise. So why is it a promise instead of that array of meals? To find out, let's go to the custom hook. Here we're triggering sendRequest and it's inside of send request where we're setting the data. And the problem is here with this line. I'm setting my data in the end to the result of calling sendHttpRequest. But calling sendHttpRequest will only eventually give me the data. Since I'm using async await here, sendHttpRequest will return a promise object which will resolve to that returned data. So I'm storing the promise in resData here and that's the problem. The solution is to add await here, which I can do because we're using async here. With that we'll instead store the resolved data in resData and, therefore, finally in our state. And with all that out of the way, we should now be able to reload without an error and at least get this "Fetching meals..." state here. But if you open the network tab, you'll see something unpleasant. Looks like we got some infinite loop here because I'm sending tons of requests, thankfully only to my dummy back in here, though. Now, why is that happening? Well, that can only happen because at least one of our dependencies here on useEffect seems to be recreated whenever the component in which we call our custom hook gets executed again, so either sendRequest or config. Now, theoretically sendRequest should not be recreated all the time because we wrapped it with useCallback. Though it could be recreated if one of its dependencies would change all the time. And here we have URL and config. Now, if we take a look at the Meals component, we'll see that we're passing the URL to it and this object here. And also our initial data of course, but that's not interesting here, because that isn't a dependency of useCallback or useEffect. But this here is the config object we're passing to useHttp and that's the problem here. It doesn't look like a big problem here, but this is a plain JavaScript object. An empty object, but still an object value that is created here in our component function and that's, therefore, recreated in that component function every time this component function runs, which happens every time the request finishes. It is a new object being created all the time. The solution is to create it outside of this component function. Here we can create our requestConfig and pass it as a parameter to useHttp like this. This might look like a simple trick, but it indeed is what we need to do here because now we're only creating this object once when this file here is parsed for the first time, and thereafter in the component function, we're always using the same object in memory here. So if we now save this and reload, you'll see that there is no infinite loop anymore, and finally, our meals are loaded. And this was a lot of work, a lot of errors to fix, but I deliberately went through that entire process because there are a lot of things that can go wrong when working with side effects and http requests and React in general. And I find it important for you to be able to deal with such problems and to know common error sources. And, therefore, now here, finally we got the working hook and we can continue working on this app, but along the way you learned a lot about sending requests and dealing with React and side effects.