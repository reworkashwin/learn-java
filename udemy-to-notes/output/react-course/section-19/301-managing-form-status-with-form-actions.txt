Now, of course, now that I'm using this formActions feature here, I also, again, wanna make sure that we show this Sending order data text here whilst the order is on its way, and since we're using formActions here, we actually no longer have to manage that state manually, so I don't care about this isLoading state that's returned by my useHttp hook anymore. I don't need it anymore, so I won't extract it here anymore. It was nice to have it, but now we don't need that extra information from here because I am awaiting my sendRquest call here in my formAction, and as you learned before, earlier in the course in this section about formActions, you can now use another hook offered by React, the useActionState hook, to get information about the current form status, so whether it's pending or not. Alternatively, in some nested component, you could use the form status hook, but here, I'll use the useActionState hook instead. Now, therefore, all I have to do is go below my checkoutAction, and then, here call useActionState and pass the checkoutAction to it as a first argument and the second argument is my initial formState, and here, for the moment, I'll set this to null. Now, as a result, I'll get back my updated formState about which I don't care here, but I then also get back my updated formAction and this pending state information, so this pending value, which is true or false. It's now this formAction that should be set as a value for this action prop on the form, like this, and it's the pending value here which I wanna use to show this Sending order data text. Now, here I'm checking for a variable named isSending, so either we replace this with pending or I use isSending here as a name when destructuring this, and I'll do the latter. With that done, again, if I add items to the cart, and I go to the Checkout page, and I enter some information here, like this, if I click Submit Order, I saw that state briefly, but then I got an error, and, of course, that is that error, kind of, which we saw before earlier in the course already because when using useActionState, your action function will no longer get this form data as a first parameter. Instead, it will now get the previous state as a first parameter, so this action function must be changed. With that, again, if we pick some items, go to checkout, and enter our data here, now you see Sending order appeared there briefly, and we would see it a bit longer if we added a delay to the back end again, so if we go to the back end code and there to the post orders route, we could add code like this to await a promise that will resolve after one second, and if you do that, and you restart that back end code, and we go through that process again, you'll see that text a bit longer, so that works the way it should. Now, of course, we could do more here. We could, for example, also use this action state to manage some formState and to show some validation errors in case the user does submit invalid values. Though, of course, we already have some basic browser-based validation in place here where I am getting some warnings already if I do omit values, so we're relying on the browser here, and, of course, we can still do that when using formActions. There's nothing wrong with that. That's why here I also won't add any extra logic to use formAction-based validation in addition to the browser-based validation, but you could, of course, do that in your projects, but with that, this is how we could migrate this application to use formActions instead of manually handling form submissions, and, as mentioned before, it simply is up to you which approach you prefer. Now, you saw both in action.