Now this approach here where we forward this ref to this dialog has one small potential flaw, because the problem with that is that the timer challenge component, which uses the ResultModal component in the end, needs to know that this dialog ref here will be attached to this dialog element. And of course here, in this tiny demo application where we are the only developer working on it, that's absolutely no problem because we wrote this code, we know what it does. But keep in mind that you might eventually work on bigger React projects and that there, you might be writing some components and other developers write other components. And you might be working with some of those components written by other developers and vice versa. If you don't always have to dive into those components to understand how they work internally, so that you use them correctly and you for example, call the correct method on that ref here, that can be a problem. If that other developer, for example, switches from a dialog to a div, you are lost, because now calling showModal won't work anymore. Therefore, it might be preferable to build this ResultModal component such that it exposes its own function that can be called with help of a ref outside of that component. So from here for example, which will work independent of how this JSX code might change in the future, so that the person working on that ResultModal component can change it however they want, as long as they keep this one function which is exposed to you, which you call on this component, on this ResultModal component, instead of calling showModal on that internal dialog element. And this is something that can be achieved by using another special hook in the component that wants to expose such a callable function. It's the useImperativeHandle hook. You can call this hook in this component function here to define properties and methods that should be accessible on this component here from outside this component. And I will say right away that you won't use this hook too often because in most cases, you should prefer working with props and so on. But for use cases like this, this hook can be very helpful to make this component more stable and reusable and useImperativeHandle wants two arguments now and the first argument should be a ref object, as you for example receive it from your forwardRef function, so this second parameter I'm getting here. Now of course, if you are not using forwardRef, because you are in a React project that's using at least React version 19 or higher and you therefore don't need forwardRef, you would of course simply use the ref that you are getting and extracting as a regular prop and you would pass that to useImperativeHandle, because ultimately, useImperativeHandle simply wants a ref, a ref that's passed to this component. Now here, I'm getting it with help of forwardRef since I deliberately used forwardRef in this project to show you how this function would work because you might need it in older react projects. But useImperativeHandle of course also works with more recent react versions where you might not use forwardRef. Either way, the ref parameter, the ref argument here isn't the only argument that's wanted by useImperativeHandle. Instead, it also wants a second argument. And this second value then must be a function that then returns an object which groups all the properties and methods that should be exposed by this component to other components. For example, here we could expose a open method, though this name here is totally up to you, but it's this method that will soon be callable from outside this component. Now with that added here, we should also import the useRef hook here in this ResultModal component file and execute useRef in the ResultModal. Because now, we'll need a separate ref for reaching out to the dialog because the idea now is to detach this dialog element, which is used in this ResultModal component, from any other outer components. So from the timer challenge component for example. Therefore, we'll add a new ref here, which I'll name dialog like this. And it's now this dialog ref which is passed as a value to the ref prop to the dialog. And it's now in this open method in this object returned here by this function, which is passed as a second argument to useImperativeHandle. It's this open method where I now want to use this dialog ref and they're the current property to call showModal, so that when this open method here is called the showModal method on this dialog is called. And now with that setup here, we can go back to the timer challenge and there, this dialog ref is now connected to our ResultModal still, but under the hood it will basically, you could say refer to this object now. That's what useImperativeHandle does here. And therefore, we're now able to use dialog.current.open here. Because this open method, which I'm calling here is this open method I defined here. Because as I just mentioned, it's now this object that's in the end stored in this dialog ref here in the timer challenge component. Because we're passing dialog as a value for ref to ResultModal. And thanks to forwardRef and useImperativeHandle, this connection to this object here is established. So it's now this open method which is called from here and therefore now, if you save this as a result, if you start this challenge and you don't stop it, we got the same result as before, but now we detached the timer challenge component from the dialog element in this ResultModal component. And we could now change this here and everything would still work as long as we would also adjust the logic here in this open method. That now of course, would have to change. But now the person working on that ResultModal component is totally free to change it however it wants, as long as this component keeps on working, and as long as it keeps on exposing a method called open because it's now this open method which we're calling here. And again in this basic simple demo app, this of course doesn't matter, but in bigger projects, this is a pattern and a feature that can be helpful. And with that, I'll switch back to a dialog here, and you now know how you could use this feature to expose callable functions from inside your components.