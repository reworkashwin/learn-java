So let's now make sure that this player component here works and that I can set my name here so that "unknown entity" gets replaced with the value I entered here, and let's start by adding this functionality without using refs, so with only the features we already know, and here, we could therefore, of course, useState. That's how we solved similar problems before in this course. In order to use the useState hook, we must import useState from React, of course, and then here, we can register an empty string as a default state and get back this array where we, for example, get the enteredPlayerName and the state updating function that could be called setEnteredPlayerName, and of course, you could also use a shorter state name here, but with that added, we can then add a function, handleChange would be an appropriate name, which we connect to this input here through the onChange prop, and this is, of course, nothing new, this is something we did before in the course, and we'll get this event object here automatically. As you learned, React will pass it to this handleChange function, when it executes this handleChange function, which we're passing to the onChange prop, and React will execute that function for every change that happens to the value in that input field, so for every character we type, for example, and then here, we can therefore then set the enteredPlayerName to event.target.value, like this, and then we can feed that state back into the input field so that the latest value is reflected there through the value prop, of course, and we can set value equal to enteredPlayerName, so to our state here, and with that, we got a component that should store that player name. Now, we can also add another function, handleClick, which should be connected to this button through the onClick prop of course, like this, and it's in this function where we wanna take that state and output it here. Therefore, we'll essentially need a second piece of state, which we could name "submitted," or something like this, and initially, that's false, and here in handleClick, we want to set "submitted" to true so that when this button is clicked, we change this state, and we can therefore now conditionally output this part after "Welcome" here by wrapping it with curly braces, and we can check this submitted state with a ternary expression, and if it's true, I wanna output the enteredPlayerName, if it's false, I wanna output "unknown entity" as a string, and with that, we should have everything we need in this component here so that if we save that and we go back and I enter "Max" here and click "Set Name," it says, "Welcome Max," here, and initially, when we reload, it does not update with every keystroke, but only set this when I click here. Thereafter though, it does update with every keystroke because thereafter, we got enteredPlayerName as a value being output here. If we would want to prevent that, we would have to call setSubmitted here in handleChange as well and set it to false here so that as soon as we start typing in that input field again, this state is set back to false and we output "unknown entity" again. So that with that, if I type "Max" here and I set this name and I continue typing, we go back to "unknown entity" until I set my name again. Now of course, with that, we always jump back to "unknown entity" as we type again and we only change that once we set the name again, so still not ideal, and we could work around that by adding more states or computed values to save that name, which we already set in the past, but as you can already tell, it's quite a lot of code we have to write here just to read the value that's entered into this input field at the point of time where this button here is pressed. Because we have to manage that value, that underlying enteredPlayerName state value, with every keystroke, with that onChange prop, to store it and to have it available for outputting it, and we have that second state just to know whether this button here was pressed or not, and this component here is now a great example for a component that can be simplified by using refs.