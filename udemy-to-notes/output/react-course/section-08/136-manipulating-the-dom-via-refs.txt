So as you could see in the previous lecture refs can be really useful. But what if you now want to clear this input whenever we set a name? What if you wanna reset it? Well, you could theoretically in 'handleClick' again use 'playerName.current', so this input element which is stored in that ref, and then set value equal to an empty string. That would clear it. If I save that and I now click 'Set Name', it gets cleared. And you can now argue whether that's good or not, because you must not forget that React is about writing declarative code. And it's about not directly manipulating the DOM yourself. Instead, you wanna let React do that. Now, here in line nine, we're already crossing that line a little bit, but here we are just reading a value. We're not changing anything in the DOM. But here in line 10, we definitely have imperative code. We're not in the declarative world anymore. Instead, we're clearly instructing the browser to set the value of that input to an empty string. And we're therefore basically violating that rule, or that idea, that React should handle all those DOM interactions. Nonetheless, for a use case like this, where you just clear an input, which is then not really connected to any other state or anything like that, you can definitely consider writing code like this. Because overall it allows you to save a lot of code. But you should definitely be careful that you don't start using refs to read and manipulate all kinds of values on your page, because that's really not the idea behind React. But if you can make your life easier as we're doing it here, using refs can be a great alternative to using state and all these other mechanisms you learn about.