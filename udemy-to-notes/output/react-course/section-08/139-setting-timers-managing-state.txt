So let's now make sure that we start a timer when this start challenge button is pressed and for that, of course, we'll need a function, handleStart, sounds like a fitting name, where we wanna set a timer and a timer can be set in JavaScript by using the built-in setTimeout function. This allows us to set a timer, maybe, of 1,000 milliseconds, for example, that will execute this function once it expired. So that's built into JavaScript, not React specific. Now, of course, the time here which has to be specified in milliseconds should be based on this target time, but target time is in seconds. So we have to multiply it with 1,000 to get milliseconds and then we'll need some state to know once the timer expired. For that, I'll of course use the useState hook which is imported from React and initially, that's false, let's say, because I'll name it timerExpired and we have a setTimerExpired state updating function, let's say, and of course, the goal is inside of this timeout function, to setTimerExpired to true. So if we reach this line of code, if this function gets executed, the player basically lost because the timer expired before it was stopped by the player. So we can now use this state to, for example, conditionally output a paragraph here where we check if timerExpired is true and if that's the case, we output a paragraph where we say "You lost". This is not the final solution I want, but it's good enough for now to get an idea of whether the game's over or not. Now, of course, handleStart now also must be connected to this button here. Here, on click, we want to execute handleStart like this. Now, if we save that and reload, if I click this button, nothing changes, but after one second, I see "You lost" and at the moment, there's of course nothing we can do about that. Therefore, we can also add a second piece of state here, which initially also is false, where we store whether the timer has been started. So timerStarted could be the name and setTimerStarted and we want to set this to true right before we set the timer, not inside of it once it expired, but right before it, setTimerStarted to true, or right after it. That also works because this line will execute immediately after setting the timer, not after it expired. It's only the code in here that will only execute once the timer expired. This code here will execute right after the timer was set and with this second state added here, we can now also update the UI to reflect this information that the timer is running. For example, here, instead of always saying "Start Challenge", we can check if timerStarted is truthy and if that's the case, we wanna show "Stop" instead of start. Like this. Here, I want to add the active CSS class conditionally. So we can check if timerStarted is truthy, in which case we add active as a class, otherwise, we add nothing. So we set the class name to undefined and then in here, we check if timerStarted is true and if that's the case, I wanna output "Time is running". Otherwise, I want to output "Timer inactive" like this so that the UI is updated based on whether the timer is running or not. So with that, if we save this, if I click "Start Challenge", the UI updates, but I of course still have no way of stopping this challenge and therefore, we'll also need another function here, handleStop, sounds like a fitting name, where I, in the end, want to stop this timer and the question there for now is how do we get access to this timer in this function here? And that's where a ref can help us.