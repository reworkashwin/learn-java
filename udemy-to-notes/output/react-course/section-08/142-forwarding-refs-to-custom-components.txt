So how can we reach out to this dialog from TimerChallenge with help of a ref? Well, we can, of course, start by creating a ref here in the TimerChallenge component, and I'll name it dialog. The name, of course, is up to you. And now we could, of course, do the following. We could go to our ResultModal and add a prop that could be named ref, though since it's our component, the name, of course, is up to you. And pass the dialog ref as a value for this ref prop to our ResultModal component. Inside of that component, we can now destructure it. And now we could try to assign it here as a value for this special ref prop, which is supported on all built-in components. So with that, we're in the end forwarding a ref that's defined in TimerChallenge, this ref here, to the dialog element in ResultModal. And therefore we should now be able, here where the timer expired, to use this dialog graph and access current, which should then hold this dialog element as a value and call showModal. Because the built-in dialog element has such a showModal method, which you can call to show it. Because as I mentioned before, it's basically invisible out of the box, but that changes if you call this showModal method. So that's nothing related to React. Instead that's a standard browser feature here. We now just have to make sure that this ResultModal is always rendered because since it's invisible and not really part of the visible dom anyways, having it around like this doesn't cause a problem. If we save that and we reload the app, we don't see the dialog. So having it around all the time isn't a problem. And we can now try starting one of these challenges like the easy one. If I click this, you'll see that after one second, I indeed do get that modal here. So passing this dialog graph here to ResultModal and extracting and using it there clearly works. However, this is only the case because this project here uses at least version React 19. Prior to that version, so in React projects that use older React versions, you would've gotten an error. Their React would've complained that you can't accept a ref like a regular prop. This simply didn't work in the past. And therefore, since my goal with this course, of course, is to teach you react even if you are using a slightly older version, I'll also show you how you would set up this component to receive a ref if you couldn't use this approach, so if you were using an older React version. Because back then when using an older version, instead of extracting that ref prop as we just did it, you would've needed to wrap your component function with some function provided by React. So a function that must be imported from React. And that function was called forwardRef, or is called forwardRef because you can still use it, even if you are using a more modern version of React. This forwardRef function then needs to be wrapped around the component function that aims to receive and use a ref prop. So in this case it's wrapped around the ResultModal function. And as a result of calling forwardRef, you'll get back an adjusted version of this component function, you could say. An adjusted version, which I'll therefore store in a constant called ResultModal. So that's now my adjusted component here, so to say. And you should then still export that adjusted component here to make sure that it's still available outside of this file. Now with forwardRef wrapped around this component function, this component function then receives an additional parameter. So a second parameter besides receiving that props parameter. And that second parameter then is this ref prop, so to say. So this special ref prop. So whichever value you are setting to the ref prop on your component, in this case this dialog ref, whatever ref you are setting to this ref prop here is received as a value through that special ref parameter when using forwardRef. And again, just to be very clear, here in this project, since we are using a recent version of React here, forwardRef is not required. Using it here is optional. I'm just showing you this syntax here so that you know about it because you will encounter it in many projects out there and you will need it if you are working on some project that might not be using the latest React version. So that's why I will also use this approach here in this section even though technically here we don't need it, we could instead just accept and extract a ref prop as I showed it to you earlier and that approach would work just fine. This approach here is optional, but is required when using an older React version.