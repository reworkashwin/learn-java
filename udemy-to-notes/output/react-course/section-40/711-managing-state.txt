So now that we're handling events, it's time to start managing state because that's the next task we have. Now for that, of course, we first of all have to identify the state that should be managed. And of course, as you learned, there typically isn't just a single right or wrong way of building such an application. But I would argue that we probably have some state to manage in the userInput component. All these values entered by the user should be managed as state. We need them as state so that we can submit them when the form is submitted. And we also need them as state so that we can reset them if the reset button is clicked. Therefore, in this userInput JS file, in this userInput component, I'll use useState, this useState hook which is imported from react, to start managing some state. And now the question just is, which state values do we want to manage? And the answer is, well, one for every input, most likely because we need to store and use all these input values. And it now in the end comes down to your personal preference, whether you want to manage one state object with multiple keys inside of it, one key per input element, or if you wanna have multiple state slices by calling useState multiple times, once for every input element. It's totally up to you. And here, I'll go for the one bigger state object which is being managed here, where I'll have one key per input element. So for example, we could add a key named current-savings here to this object. Not like this though, since the dash is not an allowed character like this in a property name. Instead, you have to wrap it in quotes. This is valid JavaScript code and allows you to use property names which otherwise couldn't be used, for example names that include such a dash. Alternatively, you could of course use camel case notation here. That is totally up to you. But I'll go for the identifier I'm also using on my input. And then we need a value for that key. And for the current savings, so in the end, our starting amount of savings we have, we could start with a value of 1,000 or 10,000 or anything like this. Here, I'll go for 10,000. Now that's of course not the only key. Instead, we also have this yearly-contribution input, which in the end is about getting that amount that will be added every year. We could set this to 1,200 so that we save 100 bucks per month. The next key value pair in this state object will be the key value pair for the third input, which is the expected return rate. So therefore, I'll use this identifier. And here, we could set this to seven percent, for example. And then the last key I wanna manage here is for the last input, which is the duration over which we wanna save. Hence, I'll use duration as a key here and maybe set this to 10 years as a default. With that, we're creating this state object. And as you learned, useState now returns an array with exactly two elements. And we can use array destructuring to get elements out of this array. And here, we'll get the userInput and then a setUserInput state updating function though, as you learned, these names of these elements are up to you. Now the idea now is to call set userInput whenever the inputChangeHandler is triggered because, there, we want to update this state, this state object here, and store the latest entered values in that state object. Therefore, in inputChangeHandler, we want to set our state to a new object, which of course should be based on the old object because we don't want to discard the values that we're not changing. And therefore, as you learned, you should use this function form for updating the state by passing a function to this state updating function. And this function will automatically receive the latest values, so the previously stored user input here, and should then yield a new object representing the new state. So here, I'm returning a new state object, where I copy my previous input state with the spread operator, so with these three dots here, and where I then want to override the value for this one input for which this change handler was triggered. Now here, we can use a special JavaScript syntax where we can set a property dynamically, so where we can dynamically access a property name by wrapping the identifier that contains the property name we want to access into square brackets so that we don't set the property named input, but instead we set the property which name is stored in input. So either current-savings or yearly-contribution or expected-return or duration. So we're setting one of these four properties depending on what's stored in input to our value. That's how we can dynamically update this state object whenever the inputChangeHandler is executed. And with that, we took an important first step. Now at this point, we're not done regarding managing the user input state though. It's good that we updated whenever an input changes. But of course we for example also want to update it when the resetHandler is triggered, so when the reset button is executed. Therefore, here, we should also call setUserInput. And now in the end, reset our state to this initial state. So we can copy this initial state and also use this here in the resetHandler. Now copy and pasting is always bad, or not ideal, and therefore I'll instead add a new constant outside of my component function because it doesn't need to be recreated every time the component function executes. And I'll name this initialUserInput and set this to this object. And now we can use this initialUserInput constant here to initialize the state here and to then reset it here in the resetHandler, like this. Now in order to reflect those updated values in our UI, we also must go to those inputs though and set up two-way binding so that we don't just listen to changes and update our internally managed state, but so that we also pass this updated state back into the input. For that, we can set the value prop to userInput. That's the name of our state. And now we need to access the current-savings property here because, here, we need the value for the current-savings input. So with that here, with this code, we access the property named current-savings, and we use its value as a value for this input. And this step can and should now be repeated for all inputs so that here, on the second input, we access yearly-contribution and set this value as a value for the input. And we do the same for the third input here for the expected-return property. And then last but not least, also here for this last input where we access the duration property. Here, since this property name doesn't contain a dash, we could also use the dot notation. But to be consistent, I'll use this square bracket access notation. And with that, all these different values are being accessed and fed back to the inputs. And hence, if we reload here, we see our initial values, and we can also update these values. And if I do update them and I then click reset, you see that we jump back to the initial values.