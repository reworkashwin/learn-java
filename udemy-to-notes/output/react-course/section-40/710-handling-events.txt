Now, the next task is to handle the Submit and Reset button click events. And for that, of course, we'll need to make sure that whenever these events occur, some code of our choice is getting executed. So therefore, I'm back in the UserInput.js file because that's where I got this form. And of course, I wanna, for example, listen to the submission of that form. We can do that, as you learned, by adding the onSubmit prop to this built-in form element, to this built-in form component, which it is in the end. And then, we can define a function inside of a function which you can do in JavaScript as you learned, and for example, add our submitHandler function like this. That's one way of writing it and we can now set this as a value for onSubmit, like this. So, submitHandler is passed as a value to onSubmit, and what's important here is that we don't add parentheses because we don't want to execute this function when this line of code is being parsed. Instead, we want to pass it as a value to onSubmit so that in the end, React internally can trigger this function here whenever this form here is submitted. Now, when it comes to handling form submissions in React, it is quite common that we wanna prevent the default, which we can do by accepting the event argument here which will be passed in automatically by React when that event occurs. And on that event object, we can call preventDefault to make sure that the browser's default behavior is prevented and blocked, because that default behavior would be to reload the page which we don't want, because that would reload and therefore in the end, restart the React application, which we don't want. So therefore, we are preventing that default so that the page does not get reloaded and instead, our JavaScript code continues to execute and takes over. And here, we'll then have to do something once the form was submitted. Now, I'll get back to that do something here soon. But before we do something here, I want to add another function which would be the resetHandler, because we also want to handle the scenario that this reset button here was clicked. That's why I'm adding this resetHandler function because it's now this resetHandler function that can be assigned as a value to the onClick prop on this reset button. We don't set the onClick prop on this calculate button because this button is of type submit and therefore will submit the form, and therefore will lead to this event being triggered. But we do set resetHandler as a value on this reset button so that when this button is clicked, we don't submit the form, but instead we reset it and we reset all input values to their defaults. Now, of course, we're also going to add some logic for this soon. For the moment, we just added the function though and connected it to the handler. And we can simply console.log('RESET') here to see whether this works, and console.log('SUBMIT') here in the submitHandler. With that, if you go back to your website and you open the browser developer tools, you should see that if you click reset, it says RESET here in the console, and if you instead click calculate, it says SUBMIT. So those event handlers are working, but these aren't the only event handlers you need to add. Instead, we also got all these input elements here and even though we're not managing state yet, we'll soon have to manage the values entered by the user as state and we'll have to listen to changes to those inputs to update the managed state. So, to save the values entered by the user. And therefore, we can also add the onChange event to all these inputs, and then define a function that should be triggered when that change event occurs, so when the user changes the value in an input. For that, I'll add a new function here, which I'll name changeHandler. And what I'm doing here is I'm defining one function which I then plan to use for all my inputs. So, I'll set up a generic eventHandler function which I want to reuse for multiple events of the same kind from different sources. For this here, in this changeHandler or this inputChangeHandler, the name, of course, is up to you, I'll expect to get two parameters. The first identifier should identify the event source, so in this case, the input for which this event occurred. The second value should then be the value that was entered. And with that, we cannot use inputChangeHandler in conjunction with the onChange prop. Though we can't set it as a value like this or to be precise, we could but we would then not get this information. Instead, I'll create an anonymous arrow function down here as a value for onChange, and in this arrow function I'll execute inputChangeHandler. So here, we now need these parentheses to call this inputChangeHandler function, but this function will now not be executed when this line of code here is being parsed, but instead only when this arrow function will be executed. And this arrow function, since it's passed as a value to onChange, will only be executed by React whenever the value in this input changes. So therefore, indirectly, inputChangeHandler will only be executed when the input value changes. But since we now control the execution of inputChangeHandler or to be precise, we control how it will be executed, we can now also pass some data here to inputChangeHandler. And the first piece of data, the first argument, should be this identifier of this input, so that value should be provided. And here, for this first input, we could simply use current-savings, so this ID here as an identifier. The second value then should be the currently entered value and for this, of course, we can use the default event object which will be dispatched by React and which will be passed by React to this arrow function here automatically, because that event object allows us to get access to the event.target, which will be this input element and then, it's currently-entered value. So, after reformatting the code, this is how my onChange prop and its value looks like. And this event.target.value is simply the value that will be provided for this parameter. On the other hand, current-savings, this hard-coded string will be provided as a value for input here. And with that, we now got this generic event handler function, and we can therefore now copy and paste onChange to the next input in line and just update the identifier here, in this case, yearly-contribution, for example. And then, copy and paste it again to the next input and update the identifier, which now is expected-return. And then, copy and paste this again one last time to this input, where the ID should be duration. And with that, we're now making sure that for change events on all these inputs, it's always the same change handler function that will be executed, but it will be executed with different identifiers and values. Therefore, here, if I console.log input and value like this and I save this, you will see that if you open the developer tools again, if I type 500 to current savings I'm getting the identifier here and then the value. And of course, the same is the case if I type 10 here into investment duration. Identifier and value. So, that's working.