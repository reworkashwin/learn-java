So let's now make sure that we output the results data, and that we do output it conditionally, which means only if we have results. Now, for that, back here in the code, I only wanna show ResultsTable if we have results. Now, of course, here in this code, we could say that we have results if userInput is not null. Because if it is null, which is the initial state, all this code where the results are derived will not execute, which therefore also means that if userInput is not null, this code will run, and we will have some calculation results. Therefore, here, we could dynamically output a fallback text, if not userInput. So if userInput is falsy, if it's null, for example, then we could output a paragraph where we say, no investment calculated yet, or something like this. On the other hand, we want to output ResultsTable if userInput is truthy, because if we have a userInput, if it's not null, we'll have performed the calculations here, and we'll therefore have a result. And with that, if we save this, I initially got this text, and the styling of this text is a bit off, but I'll get back to this later. But as soon as I press Calculate, we instead get this table. At the moment, this table only outputs some dummy data, but nonetheless, it's a good starting point. So now it's really time to pass our calculation results to the ResultsTable, and to output the data in the table there. So therefore, the goal in the end is to pass this yearlyData here, this array of data to ResultsTable, because it's this array of data that will in the end contain all the data that should be output in the rows of that table, because this yearlyData will contain one entry per year, and that's exactly what we want to output in ResultsTable. Therefore, I'll add a new prop to ResultsTable, and I'll simply name it data, though the name, of course, is up to you. And the value I pass to this prop are my yearlyData. So the yearly results data I'm deriving here. That's passed under that data key to ResultsTable. In ResultsTable, we now wanna output that data. So here, we should accept props, like this. And then here, we wanna dynamically loop through these rows. So for that, here we can use curly braces, as we always do, and then access props.data, and map our data to these table rows. So here, we'll get the data per year, because again, as mentioned, yearlyData here will, in the end, contain one entry, one object of this shape per year, since we're calculating our returns for all these years, and therefore, here we'll get the data for a year, and we want to return an updated table row. So that's what we return in map here. And in that row, we now, for example, wanna output the number of that year. And for that, here we can access yearData.year. Why yearData.year? Because in these objects that are passed to yearlyData, we'll have a year key, which is the current loop iteration index plus one, so that we don't start at zero, but one for this year identifier here. In addition to the year identifier, we'll have the interest gained in a year, the savings at the end of that year, and the yearly contribution that was added in that year, so the amount of money that was added to our investment in that year. In the ResultsTable, we therefore can output the number of the year by accessing yearData.year. For the total savings at the end of the year, we can output yearData.savingsEndOfYear. That's this key we have here. For the interest gained in that year, we can output yearData.yearlyInterest. yearlyInterest is another key we have here. Now, for the total interest gained, it will be a bit more difficult, because here, we'll need the yearData, and there, the savings at the end of the year, minus our initial investment, which we don't have here yet, we'll add it soon, minus our yearly contribution, times the number of years we've been contributing it. Now, for that, at least we have the yearly contribution here. So we have yearData.yearlyContribution times yearData.year, because we're making this contribution every year, and since here, we want to get the total interest we gained, we have to deduct our initial investment and the ongoing investments we had every year. So therefore, it's now the initial investment that's missing. And that data is not part of this yearlyData array, but it is part of this userInput, because that userInput, which is coming from that form, has the current-savings key. And the current-savings, in the end, are our initial investment, the amount we initially invest. Therefore, in App.js, we have the current-savings in that userInput object, and we can pass that as a prop to ResultsTable. There, we can add the initialInvestment prop, and set this equal to userInput, and then there, the current-savings. That's our initial investment, and we're now making it available through the initialInvestment prop in the ResultsTable component. So in ResultsTable, we now can deduct props.initialInvestment here from the savings at the end of the year, and also deduct all our yearly contributions to get the total interest gained. Now, the last thing I wanna output here is the total invested capital, and that in the end is our initial investment plus all our contributions. So basically, everything except for the interest we gained over the years. And with that, we're outputting all that data, and therefore, if we now reload this, and we click Calculate, we get a table with some data in there. Now, we're also getting a warning in the console, a warning you should already know from the earlier course sections. And of course, the formatting is a bit off. Now, we'll take care of the formatting soon. But before we do that, I wanna get rid of that warning by adding a key prop here to this table row. The key should be a unique identifier, and here, I'll simply take yearData.year, since that will be unique for every year, and therefore, for every row. So with that, if we save this, reload, and click Calculate, at least this warning is gone. Now we can work on the formatting. Now, when it comes to formatting these numbers as currencies, I gave you a little hint in that hints lecture earlier. You can use a built-in JavaScript feature to format those values. You can create a formatter by using the Intl.NumberFormat constructor, which is built into JavaScript, which in the end, gives you a formatter utility object that provides a method to format numbers as currency, for example. Now again, this code was provided as part of the hints lecture, and you can therefore, of course, directly copy it from there. This formatter can then be used to format that data. For example, the savings at the end of the year can now be formatted by using formatter.format, and passing the savings of the year as a value. And I'll do the same for the yearly interest gained, and also for the total interest gained. So I'll wrap this calculation here with format, and I'll do the same for the total capital invested, like this. And that's how we can format all these numbers, and therefore, they now look much nicer. And speaking of that, with that, we can tackle the last task, the bonus task I gave you, and we can work on the app's styling, or how those styles are being applied, to be precise.