So let's add more pages. And for that I'm first of all going to add another component to the pages folder. And I'll name it "Products JS." The name of course is up to you, though. Here I'll add a products page, but it will still just be a dummy page, without any actual real content on it. Here I will then return an H-1 element, where I say "the products page." And with that component added, we can also add a route for it. We do that by adding a second element here to this array of route definitions. And now the path could be "slash products." It is of course up to you which paths you wanna support. But "slash products" might be a path that makes a lot of sense if we're loading a page that might display some product items. So therefore, here, we load slash products. And, as an element, I want to load the products page. Therefore, we're importing the products page, and we're using it in our JSX code for this element property. And that is all. With that we're supporting this second page, for "slash products," and therefore if we save everything, and we go back and we enter "slash products," after a local host 3000, and hit enter, we load the products page. And, of course, hopefully, unsurprisingly, if we delete "slash products," we load the homepage. If we enter something totally different, we get an error, though, because we try to visit a URL, a path, that's not supported yet. Now I'll get back to errors and error handling and showing good error pages instead of this default page that's generated by React-Router-Dom, a little bit later, though. For the moment, let's just go back to "slash nothing," and dive a bit deeper into the React-Router.