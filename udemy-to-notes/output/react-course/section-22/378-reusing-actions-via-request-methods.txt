Okay, so now we added almost all functionalities except for editing. That's still not entirely working as it should or to be precise, we can't really submit this. We get an error if we try to do that. The reason for that is that at the moment we're simply not supporting submission for this EditEventPage component. We are rendering the EventForm there and the EventForm has a form component which tries to submit the form data, but we haven't registered an action for this edit event route. Here, for this route, there is no action. Now actually, the action which I do wanna trigger here is almost the same as for the new event route. We wanna send almost the same kind of request to just a slightly different URL with a different request method. But other than that, it's basically the same action which I wanna trigger because it's the same form with the same data. So it would be great if we could reuse that action which we're using for creating a new event. And we can, for that, I'll grab this code here where I define and export my action in NewEvent JS. And I'm adding it to EventForm JS but I could be adding it to any file. We could also create a brand new utility file that stores this action. Here I'm adding it to EventForm though, and I now wanna change it a little bit. I wanna make the code in this action a bit more dynamic to be able to send both a request for adding a new event as well as for editing an existing event. And how could we do that? Well, we could make sure that in our EventForm we use different methods for creating new events or for editing events. We can use post for creating new events and patch for editing an event. And I'm actually expecting a method prop here. I'm already extracting it with object de structuring, which I can set here. And this could now be set from inside NewEvent and from Inside EditEvent. In NewEvent, I could set method to post and in EditEvent, I could set it to patch, so that would set the method of the form and I could use the same form with different methods. Now how is this method helpful? Well, we can get access to that method here in our action on this request object, there, we can extract the method like this. Now this method can be used to set the method of the request we're sending to the backend. Again, here, we're still on the front end, but we're sending a request to the dummy backend here. And instead of hard coding, this method here, I can set this method to the method of the request that was generated by a react router. Now the URL also needs to be changed though, it's not always the same. The URL is this URL here, if we're creating a NewEvent, but it's actually, this URL plus the eventId, if we're editing an existing event. So here I'm using this URL variable and I'm then checking if method is equal to patch, which means we're editing an event instead of adding an event. And in that case, I want to get the idea of the event which we're editing by accessing my params here. So this params object, which we also get from react router in this overall object that's passed to actions. And here I can extract eventId, now eventId here because in my route definitions I chose eventId as a placeholder name. So with that, I'm getting the eventId, if I know that the action was triggered because I submitted the form for editing an event, instead of adding. And in that case, my URL is basically this URL plus eventId, and an extra slash added here. With that, I have a dynamic way of sending a request to different URLs with different methods, but with always the same data depending on how this action was triggered. Now we just must make sure that this action is used on all the routes where we need to use it. And for this, I go back to App JS And instead of importing the new event action here from page's new event, I remove that and I instead import my action as manipulateEventAction or however you wanna name, it from components EventForm, because that's where we just edit the action to. And now it's this manipulateEventAction, which I add to both my NewEvent page route here, as well as to this EditEventPage here. So we use the same action on different routes but this action is written such that it will do slightly different things depending on how the form was submitted. And that should allow us to use the same form with different actions. Now I'm getting an error here, where I'm missing some imports, which makes sense. In my NewEvent JS file, I was importing json and redirect, which I no longer need to do here. But these imports are now needed in EventForm because here in my action I'm using json and redirect. So I should add those imports here and import json and redirect from react-router-dom. And with this, this all compiles successfully. Now if I try to add a couple of exclamation marks to this title and I click save, I got an error. Now I'm getting this error because actually I'm checking for patch, all lowercase here, but the request will include it in all caps. So I wasn't extracting my parameters and setting the URL correctly. But if I check for PATCH all caps here, which is how it's written in this request object that's generated, which you can tell if you console log this request object like this. If I do it like that, you will see that now if I add a couple of exclamation marks here and I save this, this was updated. And of course I can still add a new event, if I add one here with a URL that doesn't exist, but it doesn't matter, we can delete it anyways. You will see that this also is created. And of course we can edit this or delete this, and I'll delete it here because we don't need it anymore. But now we're able to delete and edit events with the same dynamic action code being used, and by utilizing this method which we can set on the form, and again, this method is only used to set the method for this client site request, which is generated by react-router and which is passed to action. And it's exactly this client side request which we're then using in the action to extract the method and configure our backend request which we send to the backend appropriately.