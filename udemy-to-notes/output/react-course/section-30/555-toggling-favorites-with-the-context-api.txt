To mark a product as a favorite, back in the context folder in the "products-context.js" file. And in there we manage our product list with "useState", right? Now in that same component we can add a function stored in a concept, maybe named "toggleFavorite". It's a function so I'll use a arrow function here, where I expect to get the productId. So the ID of the product for which I want to toggle to favorite status. The logic in here is pretty similar to what I did in my reducer before. Here I want to call "setProductsList" and I will use the function form of this hook to get my "currentProdList" and return a new one based on that current one. And I'm using that function form to be guaranteed that I'm working with most recent state. So here I want to return a new list. That new list should be based on the old list of course. Now again for that we can simply grab the logic from our products reducer here. This logic here so I'll copy all that logic, move it into the context folder into this update function here, so in this nested arrow function. And in here state now simply "state.products" now simply is the "currentProdList". "Action.productId" doesn't exist but we get to the "productId" here. And everywhere where I have "state.products" I just use "currentProdList". To get my product index, get the new favorite status, create our updated products list based on the old list and then swap the one item we're updating by copying all the properties of the old item and replacing the favorite status and then here I've returned my updated products. So now we have the "toggleFavorite" method, which we can call to toggle the favorite status of a single product. Now in my context I'm creating up there, besides the products in this object, and that's why I'm creating the object, I'll not only have that list of products I'll also have that "toggleFav" function which here is an empty function. And I'm just adding it here to this initial value so that we get better ID support and auto completion. But I will also add "toggleFav" to the object we're passing here as a value for products context provider. There, I can now also add "toggleFav", and set this equal to "toggleFavorite", "toggleFavorite" of course is that function we just created. So now this function, a pointer at this function is also stored in our context value and now therefore, everywhere where we tap into our context we can call this function and then this function will change something in that file, will change our state and therefore emit a new state, a new value, to all components that are listening. So now let's go back to the product item J-S file in the components folder and in there, let's now also import "ProductsContext" from the context folder of course and there, from "ProductsContext", get rid of that action import which we don't need anymore. And with "ProductsContext" imported here we can also import "useContext" this hook from React and then here, simply get access to our "productsContext" or to the "toggleFav" function by using "useContext(ProductsContext).toggleFav" and that's the pointer at this function. And now here in the "toggleFav" handler instead of just patching this, I can just call "toggleFav" and forward the ID. Now to get the best ID completion I'll go back to the productsContext file, and here at this dummy function I'm defining up here, I'll actually add the ID as a parameter. so that my I-D-E knows that an ID should be passed here when I call it. And the ID I wanna pass here of course is "props.id" so the ID of the item we're rendering here. Now, we should actually be able to mark something as a favorite. So now if we save this and we go back favoriting works again. Now let's also make the favorites page work again because right now that is broken. By going to "favorites.js" in the containers folder and there let's get rid of the "useSelector" thing here. Instead import the "ProductsContext" here, from "context/products-context" that file here and then here our favorite products can be selected with the help of the "useContext" hook again by using "useContext" here, and by then, selecting "productsContext.products". And then here we can call filter and filter every for product by checking if it is a favorite. It only if this returns true, it will be included in the newly returned array which is stored in "favoriteProducts". So now this page should work again. Got no favorites initially but now if I add let's say these two favorites, we see them here. So now we're managing our state with the context API. And on first look that looks amazing we're not using Redux and that argued wasn't too hard. We basically now have our story here in the context folder in the "productsContext" file here and we're using React only features to pass that data around and to change it from anywhere and to read it from anywhere. Now what's the down side of this approach?