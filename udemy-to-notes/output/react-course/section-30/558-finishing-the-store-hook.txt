So now we're managing the listeners. Now we need to be able to change our state, and for that, we should be able to kind of dispatch actions in our components. So the same idea as if we were using Redux. Hence here in useStore, I'll add a new constant, and I'll name it "dispatch", and this should actually be a function, where I, let's say get a action and then something should happen when that action is triggered. And actually here I just want to get the name or the identifier of that action because we'll define the concrete actions in a different place. This here is still the overall abstract definition of our Store Management System. So here we get some action identifier and keep in mind that we have this actions object up there. My idea is that we can later define concrete usages of our store with their own actions and therefore such actions will soon be registered here in this actions object. Hence here, where we get a action, I can reach out to actions for that action identifier, because that should be a key of that actions object later once it is registered. And this action should actually be a function. So, actions should be an object where we have keys which match my identifier here and where the value then is a concrete function, which is called by me adding parentheses here. The function which I call here should get my globalState, so my current globalState, and should return me a newState, that's essentially all what happened in Redux, right? In a Redux, in the reducer we in the end also got a action, and the reducer then checked the action and returned a newState in the end. It's the same concept I'm following here I get a actionIdentifier, I search for that action in my actions object, I expect that actions is a function, which I then execute, so my reducer function, so to say, which returns a newState. That newest state is that what I merged with my old state. So globalState should then be essentially, my old copied globalState merged with everything in the newState, so that my globalState is a new object with the old data plus the new data. The newState. And now we just need to inform all listeners about that state update. So I'll go through all my listeners, so, "for (const listener of listeners)" using a for of loop here, and listeners of course, is that global listeners array here, and then, keep in mind, that what we add to listeners is just set state, so it's a function. So here I'll simply call listener, and pass my globalState here. So my new globalState is what pass to listener. What this does is that it updates this React state, because that's where my listener comes from, ultimately, with the new globalState, and hence, React will re-render the component that is using my custom hook here. Now I know we're still in a relatively abstract world. But this was an important first step, we're now having an abstract dispatch function, we're able to register listeners, this is my custom hook. Now this custom hook should also return something. And the something is an array with exactly two elements. And that's the globalState and dispatch. And if that looks familiar to you, then this is exactly what the built-in useReducer function always returned. It always returns a state and a dispatch function. It's just not suited for managing state across components, which is why I have to build my own version kind of. But here I returned to globalState and dispatch. Now, this is one thing I have here, and I will export this constant hook. We need something else though. This custom hook is nice, but it's still very generic. For example, right now, we got no way to change these actions we can call them, but we can't set them. So there is another thing I will export from this file, besides the custom hook. And that's the initStore function This function here, which I also expert, as a named export, will get some actions which are defined by you as the developer, and maybe also an initialState, so that you can define this too, because right now globalState is always an empty state, maybe you have some initialState like we do, actually, we do have some products initially, for example. And in here, I will simply check, if initialState, whoops, initialState, whoops, that should be always initialState, so, if initialState is not now, in which case I'll set globalState equal to an object, where I copy the current globalState, because keep in mind that you'll share that file, and you'll share that state. So just like in Redux, you have multiple reducers that make up one global store. So therefore, in Redux, you had to call combined reducers to create one big store state object, it's the same what we're doing here. I merged to be currently having a globalState with the initialState of this concrete instantiation of this state, so to say, so that we could manage multiple things with this global store. So here, I'll merge in my initialState. And I'll set actions equal to all the current actions, and then merge in the userActions. So now we have our globalState. We have already userActions all registered here. Now we need some place where we can create a concrete products store based on this store setup. That's the next thing I'll add.