So to use that custom store, let's go to the index.js file and get rid of the ProductsProvider, which is our context API in action. Can we get rid of that? And instead import configureStore from hooks-store/products-store and call that here to step up our store. Now, the cool thing is we we don't need to wrap this in a provider component or anything like that. This will make sure that we initialize our store, since we import initStore from the store file. These things here will all be set up and they will be initialized with the values we're passing in our products-store.js file. If we had multiple stores, we would simply call ConfigureStore for these different stores. We would just have to give these functions different names. You can, for example, name this ConfigureProductsStore, of course, because it's the default export. You can name this whatever you want and you would call all these configureStore functions for all the different store parts you might have in your app. And with that, the store is ready to use. Now, to use it, we can simply go to the place where we need it. Like here, the Products.js container file. Instead of importing our context there, we can import useStore, so our custom hook from hooks-store/store now, so not from the productsStore but just store and useStore gives us access to the globalStore and the globalDispatch function. So now here, we can useStore. Don't need to pass anything there and what we get back is our state and a dispatch function. That's what we get back from useStore. Now, of course, here I'm not interested in the dispatch function so I can simply just get my state here by just accessing the first element returned by the custom store, by the custom hook because the custom hook returns two things, globalState and dispatch. If I only need the State, you just extract the first element from the array with this syntax, of course. So that's our state and therefore, here we can output state.products because we know there will be a products key in our state because that's what we're setting up in the products-store.js file when we initialize our state, then here the second argument we passed to initStore is our object where we add a products key that is what will be initialized, second argument, as our initialState what will be merged with the globalState and therefore the globalState will have a products key and that's what I'm accessing here. Now, to test this, let's go to ProductItem and get rid of our context thing there because that won't work right now. Get rid of this and comment this out and save that. And if you reload this page, you should still see all your products there. Now let's make the favorite button work again. And if it's in the ProductItem file, instead of importing useContext, we can import useStore from the hooks-store folder and there it is Store.js file. And here we can useStore and now I'm only interested in the dispatch function which is our second argument we return in our store here. We return this array. Dispatches the second argument. Or the second element I should say. So I'm extracting that second element here. That's the dispatch function. So I store it in this dispatch constant. We can call dispatch here and now what I want to dispatch is an action identifier which is TOGGLE_FAV and we'll then look for such an identifier in our actins map which I set up in products-store there. This is the actions map which I used to initialize my store. It'll be merged into the global actions map and there I have TOGGLE_FAV as an identifier and this needs a payload of productId which is good to know because that's the second argument we can forward here. Props.id and that should be all. With that change, now we can mark things as a favorite again. Now let's make sure the Favorites page works again because right now it's not working, of course, even though I have favorites because it still tries to get them from the context. So in the Favorites.js file here, let's not import the context anymore, but again import useStore from the hooks-store folder and there the store.js file and then here, we got our state which is useStore and there the first element. And I'm interested in my favoriteProducts which I get by accessing state.products, oops. State.products.filter and then I search for all products which are a favorite like this. So yeah, by the way, make sure you of course, execute your custom hook, that's important, add parentheses here. Save this and give it a try. Got no products yet after the reload but now let's add two and we see them here. So this is now working. And I'm totally aware that this kind of hard to wrap your head around at first. So let me sum this up by briefly going through that again in the next lecture.