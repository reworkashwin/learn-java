No matter which kind of web application you are going to build, and no matter if you are going to use React or not, at some point of time, you'll need to deal with user input. You'll need to deal with Forms, and that's exactly what this course section will be about. Because it's actually a bit trickier than it might seem. But at the same time, thankfully, quite straightforward to handle once you know how to handle Forms and user input. And therefore, in this section, we'll take a look at what exactly can be difficult about Forms and handling user input. We'll explore how you can handle forum submission and how you can validate user input, because it turns out that the validation part is typically the trickier part. And we'll then also explore how the browser and built-in Form and validation features can be utilized to simplify your life as a developer. Last but not least, we'll also explore how to build custom React specific solutions to deal with user input and validation. And of course, we're going to explore all these things by again, diving into a demo project, which I prepared for you. And speaking of that, you find that demo project attached, of course, in a local and a CodeSandbox version. For the local version, as always, download and extract it. Run NPM install once and thereafter, NPM Run Dev to start the dev server. For CodeSandbox, this is not required. And once you got everything up and running, you are ready to dive into Forms.