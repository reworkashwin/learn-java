Now with all those steps taken, the code in this StateLogin JSX file got a little bit leaner. But of course, if we're honest, most of the code that we had to write here is about managing the entered values and didEdit states and about updating those states with the handleInputChange and the handleInputBlur functions. And we therefore also might wanna outsource and reuse this code. And how could we do that? Well, with help of a custom hook. And we need a custom hook because we are managing some state in that code that should be outsourced, and therefore we can't use a regular function. But we can now build such a custom hook by, for example, adding a hooks folder in that source folder. And as you learned, that name is up to you, doesn't have to be named hooks. And in there, we could add a file named useInput.js. Doesn't need to be JSX because I'm not going to write any JSX code in there. And here we can now export a useInput function, and it must start with use, as you learned in the custom hooks section, so that it's identified as a hook function by React and the rules of hooks are applied to it. And then we can grab this state management code, copy it, and add it to useInput, though we'll of course have to tweak it a little bit because my idea here now is that I build a useInput hook that should be used once per input element we have in our form. So if we have two input elements, we need to call the hook twice. So instead of managing some object state here that groups the values of multiple inputs together, I just want to manage one single value here, and therefore I'll rename that state from enteredValues to EnteredValue, and my initial value should be settable from outside so that we could also pre-populate an input with any value of our choice. Therefore here we could accept a default value parameter in this custom hook and use this to set the default state here of the entered value state. And then here of course, we must import useState from React. So that's one piece of state. DidEdit now also should not manage some object that combines the states of multiple inputs but instead just manage a single value, where I again will start with false, though we could of course also accept this as a parameter here if we would wanna make it configurable. Now that we've got the state in here, we'll also have to bring the functions that update that state into the custom hook though. So I will copy handleInputChange and handleInputBlur and add those to my custom hook here. And since we're now just managing one single input and one single value in here, we can also simplify those functions here. And, for example, again, just expect to get the event object here on handleInputChange and then set the entered value, not values, to event.target.value again, and setDidEdit here simply to false because, again, we just have a single inputs value which we're managing here. And then handleInputBlur, we therefore setDidEdit to true. So now we got much simpler code in this custom hook. Now of course, just having the state and these functions in here won't do much for us though. What we need to do is expose them to the component where this hook should be used. And therefore, here I'll return a value in my custom hook, and you could return an array to group multiple values together, or what I'll do, return an object. And here I will then return my value, so this enteredValue state which we're managing here. I'll expose it through a value property in this returned object. And I'll also add pointers at handleInputChange and handleInputBlur to this returned object so that this object also has these properties, which contain values that point at these functions. Now with that, didEdit is not getting used yet, but that will change soon. What I wanna change right away is this identifier here, which should no longer be accepted on handleInputBlur. But with that, we now got a first basic draft of this use input hook, and we can start using this hook. And for that, I'll go back to the StateLogin JSX file, and in there, we can now get rid of this state here and get rid of handleInputChange and handleInputBlur, and also of these commented-out functions here, so that we got much leaner code in here, and instead call useInput and, of course, import useInput from the useInput.js file in the hooks folder. We can now also get rid of the useState import here because I'm not managing any custom state in this component anymore. Instead, we have useInput, and useInput wants the default value of the input to which it should belong as an input. So therefore, here I'll pass an empty string, and we'll get back an object which we can destructure, and that object will have a value to which I'll assign an alias here, which I'll name emailValue, because I plan on using useInput multiple times for different inputs. Therefore, I need an alias here to clearly identify which value this is. I will also get handleInputChange out of here and alias it to handleEmailChange, and get handleInputBlur and assign an alias of handleEmailBlur so that I'm using my custom hook like this. Now with that, that validation logic here won't work, but we can now use the emailValue to assign it as a value for the value prop on the email input. And here for onBlur, we no longer need this wrapper because we no longer need this identifier because we now have a dedicated handleEmailChange function since this hook is about managing the state of a single input. So therefore, this code down here can be simplified to point at handleEmailBlur and here simply at handleEmailChange. And this here will, for example, work because inside of the use input hook, this input change function just wants the default event object, and that is exactly what we will be getting here when binding this as a value for the onChange prop. But, of course, as mentioned, validation will be a problem. But thankfully, we can also outsource this into our custom hook. Here we can add a new computed value, which could be called valueIsValid. And then I want to validate that value based on some validation function, which should now not be hardcoded into this hook because, of course, this hook should be usable for different inputs with different requirements and different validation functions. Therefore, it makes sense to accept a validation function as a second argument on this hook function so that we can then compute valueIsValid by calling this validation function and passing the enteredValue, which we are managing in this hook, to this function, so that from outside this hook, so in the place where we use the hook, in the login function, for example, we just define what the validation logic and function should be, and we then execute this logic inside of the hook. And then we could use valueIsValid here to return another value, the hasError value, for example, which is the combination of didEdit and not valueIsValid like this. So if we did edit the input, if this is true, which we are managing a state here, and the value is not valid, we wanna set hasError to true. And this change now allows us to pass the validation function that should be executed to useInput, so, for example, pass a pointer to isEmail to useInput so that this will be executed as a validation function. And we can get rid of this email validation code here. Now, if we would want to run multiple validation functions, we could either tweak the logic here to accept an array of functions and go through that array and execute all those functions. Or we change the logic here, where we set that second argument on useInput and instead of pointing at a validation function, we pass an anonymous function, which will then be the value for this validation function parameter and which will be executed here. And in this anonymous function, we expect to get a value because, inside of this hook, as I just said, this function will be executed here, and the entered value will be passed to this function as a value, so we should expect it as a value here. And then we simply run all the code we wanna run to validate that value here, and we just return true or false once we're done. So here we could then run isEmail and isNotEmpty, for example. And that's how we could easily execute multiple functions. And we could also shorten this code here, get rid of the return statement and of these curly braces and all of this semicolon, and use useInput like this. And now we can repeat this approach for the second input which we got here, the password, where I also have an empty string as an initial value and where I have hasMinLength, which is a function where I need full control over how it's executed because I wanna set that min length parameter. So again, we can pass in that wrapper function where we get a value, and then make sure that once it's called from inside the hook, we call hasMinLength and we forward the value, and also set the second parameter to six. And now we can extract all those values here by again using destructuring and getting the value, which here could be the passwordValue, by getting handleInputChange, which I'll name handlePasswordChange here, by then also getting handleInputBlur, which I'll name handlePasswordBlur like this. And by then, of course, also extracting hasError, which I'll here name passwordHasError and which I'll also do here for the first input, which belongs to the email. There, I also want to extract hasError and rename it to emailHasError. And now we can also get rid of that password validation constant here and go down to our form. And here on the error prop of the email input, I'll check for emailHasError and then forward that error message if that should be true. And on the input for onChange, it's now handlePasswordChange. For onBlur, it's handlePasswordBlur. For the value, it is the passwordValue, and for the error, it is the passwordHasError constant I'm interested in. And with that, this code got easier again, and we got rid of a lot of code up here by putting that logic into a custom hook. And, of course, here in handleSubmit, we can now also check if emailHasError or passwordHasError, in which case we might wanna return here so that we don't continue executing logic if we have some invalid input. But if we have valid inputs, we could output the emailValue and the passwordValue here and, of course, potentially also send that information to a backend. And with all that done, we're using this custom hook here, the useInput hook, and we can therefore now go back to our form and we get the same behavior as before where I got this validation experience I showed you before, but now all powered with help of a custom hook that allows us to keep track of these different states and get hold of the entered values.