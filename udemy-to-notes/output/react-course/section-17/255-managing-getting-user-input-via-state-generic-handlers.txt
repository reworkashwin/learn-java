So therefore, here we typically want access to the values entered by the user. And one way to get that access is, of course, to useState. So to use the useState hook, which is imported from React. And now, we could set up one piece of state for every input field or set up one combined state where we store all those state values in an object. Both approaches would work, and here I'll go for the multiple state slices. So we could have our enteredEmail and the setEnteredEmail state updating function, and then, also have a second piece of state here in this login form where we store the enteredPassword. So here we can get the enteredPassword and setEnteredPassword and work with these two pieces of state. Now, the initial state might always be an empty string here so that we start with no text being entered in those input fields. And now, we typically want to set up change listeners, like a new function where we handle the email change, where we also get an event object automatically passed in by React. And where we then set this function as a value to the onChange prop of this email input here. So handleEmailChange is now connected to this input here and therefore, it's this function that will be triggered on every value change that's made on that input here, so every keystroke for example. And here we could now call setEnteredEmail and set the value to event.target.value because the target of the event is that input field and that underlying object that represents this input field has a value property which gives us access to the currently entered value. Now, we just also wanna feed that value back into the input by setting the value prop and by setting it equal to our state value. So that for one, we're setting our initial value here as a value that's shown in the beginning in this input field, but also so that we feed every change the user makes back into this input field so that it is reflected there. And that is, of course, an approach about which you learned way earlier in the course already. With that, if I save that, I can enter values here, so that works just fine. And I can also use that state in my handleSubmit function now. And for example, console log user email and then output the enteredEmail here like this. And therefore, if I save that, and I do enter some email address here and I click Login, I see this output here. So that's how we can get hold of that value entered by the user. And therefore, now we could, of course, also add a second function here handlePasswordChange where we repeat that process for the password, which we also set to event.target.value. But of course, if we're creating separate handling functions for all our inputs, we might end up with a lot of handling functions if we have a complex form with a lot of inputs. And we'll end up with a lot of state snapshots. Now you could work around the many state snapshots by instead using some combined state. So an alternative here would be to have the enteredValues state, let's say. And, of course, the name as always is up to you, which might be one single object here or an array of multiple values. But here, I'm using an object where you might have an email property and a password property. And, of course, those property names are also up to you which initially might be set to empty strings. And now you could set up one generic event handling function. The handleInputChange function, for example, where you call setEnteredValues and where you then update one of those entered values. For that, this handleInputChange function must not just get the event, but also some identifier of the input for which the event occurred. So that you could then update the entered values state by using this function form of calling this state updating function. So where you pass a function to the state updating function, so that we get the previous state snapshot, and we can then return a new state snapshot, a new object, wrapped in parentheses here to make it clear to JavaScript that I want to immediately return an object here, and that those curly braces here do not mark the function body, but instead create an object value. And here you would then paste in your existing key value pairs by using the spread operator because I only wanna update one of these two fields and of course, the other field with its current value should not be lost. And you would then update the field that's identified by this identifier. For this, you can use a JavaScript syntax that allows you to dynamically access a property in an object by adding square brackets here and then putting the variable or parameter that holds the name of the property that should be set between those square brackets. So that's how you can dynamically target and set a property where the name of the property is stored in a variable or parameter. And then you still add a colon as you always do after the property name, and you set up the value that should be stored under that property name, event.target.value, for example. And that's how you could set up a more generic, versatile change handling function. Of course, now this function must be connected to the input values though. And therefore, you can't just set it as a value for onChange anymore because React when calling this function will not give you this identifier which we need here. So that would not be provided by React. Therefore, we need more control over how handleInputChange will be called which we can get as you learned in this course before by wrapping this in an anonymous function, so that it's now actually this anonymous function that's passed as a value to onChange and that will be executed by React, and it's therefore this anonymous function that will get the event object as an input. And now in this anonymous function, we can control how handleInputChange will be called. And we can now make sure that we pass an identifier of the input of the property that should be targeted in our state here as a first parameter to handleInputChange, so that this identifier here is set. And here for this email input, that should be email, so that in the end, by setting email here we're getting this as an identifier here, and we're targeting the property with the name email, so this property. That's what we're achieving by adding email as a first argument to handleInputChange. And the second argument can then be that event. So this event here, which I'm just forwarding to handleInputChange as a second argument. And therefore, of course, we could also simplify the code here and in handleInputChange just expect to get the value instead of the event object so that we can directly set that value here. And then here, when calling handleInputChange we therefore extract that value so that it is the value that's passed as a second argument to handleInputChange. And now we, of course, want to feed back that value to the input and therefore we want to access the enteredValues state, and then there the email property because that will hold the currently enteredValue. So enteredValues.email that's the code we need here. And that's how we can use this more generic handleInputChange function and we can now copy and paste that code here to that password input, and just change the identifier from email to password here and also access enteredValues.password. But that's all we have to do and that's how we can then use this generic change handling function on both inputs. And therefore, this allows us to get rid of these separate handle change functions and use this one combined function instead. And if we now wanna output the entered values here in handleSubmit, we can simply output the enteredValues state. And with all that done, if you save that and reload, if I enter an email address here and a password here, and I click Login, you see that data is being output here on the right and you see the values that were entered here in the console, therefore.