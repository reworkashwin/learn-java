So how can we validate inputs on lost focus? Well, for that, we need to react to an input field losing focus. For example, the email input field again. And you can react to this input losing focus by adding another event listening Prop to it, the onBlur Prop because the blur event is a built-in default browser event that will be fired whenever this input loses focus. Therefore, we can add a new event handling function here, handleEmailBlur or of course, set up a more generic handleInputBlur event handling function. And then here, we could again accept an identifier, like this, and manage a new piece of state. Because now, we don't just wanna keep track of the value that has been entered into an input, but we also wanna keep track of whether the user touched this value or not. Because when the blur event is fired and this input field therefore loses focus, we of course, also know that it seemed to have focus in the first place. So that the user did touch it, that the user worked with it. Hence, we need that second piece of state or we could of course, also merge it into this existing state by managing some nested object here for the email where we have the value and the didBlur or didEdit property which initially is false and then might be set to true. But here, I'll simply go for an extra piece of state with useState. And then again, I'll manage an object here with an email property which initially is false. And the password property, which initially is false because this here will be my didEdit state and set didEdit where I keep track of whether these inputs have been edited. Which in my case here, simply means that they lost focus. So the user interacted with them and then they lost focus. Therefore, now with that state added here, here in handleInputBlur, I wanna update this didEdit state by again, using the function form where I return a new object, where I spread in my old state so that we don't lose any data and information. And then just as with the handleInputChange function will dynamically target a property with this syntax. And then here, simply set it to true because this will be fired when an input is blurred. So therefore, then I wanna set this state to true. Keep in mind that initially, it is false. And the offer now it's handleInputBlur, which I wanna trigger here. And again, we need to wrap this with a function so that we can control how it will be called so that we can pass this identifier to handleInputBlur. In this case, email as an identifier. And now, we can use this new didEdit state to perform validation. And we can check whether emailIsInvalid by not checking for whether it still has the initial value but by instead keeping this check and combining this check with didEdit.email. If the user did edit the email address and the email then does not include an @ symbol, I want to set emailIsInvalid as true. And with that, we make sure that we give the user a chance of editing before we run this check. And therefore, before we show an error message. So with that, if you save this all and you go back and reload, you see there's no error message here initially. And if I start typing, there still is no error message. But if I then type out of there and I lose focus, there is an error message. And I also get that if I just click in there, don't enter anything, and click out of here. As soon as I start typing, the error goes away once I have a valid value. And that's now arguably a good user experience, therefore. Now on the slide I mentioned that when validating on lost focus, errors may be shown too long though. And what do I mean by that? Well, here, you could argue that the error is shown a little bit too long because if I have that error message and I start typing, it only disappears once we did enter a valid value. Which you could argue is the experience you might wanna provide. But you could also argue that this error message should disappear as soon as the user starts typing again. Because once the user starts typing again, they have another chance of entering a valid value and therefore, we might wanna remove this error message until they're done typing and the input loses focus again, at which point of time, we might wanna run this validation again. And therefore, the solution we have at the moment could be improved by also updating didEdit on every keystroke. And by resetting this to false whenever the user starts typing again. And we can do this by going to handleInputChange and also updating setDidEdit here. And again here, we would use the function form for updating this and copy in the old state and then target and input dynamically via its identifier to reset it to false here. So that whenever the user starts typing again, we reset this didEdit state for the input in which the user types again. And with that, you'll see there's no error initially but we do get one if I lose focus and have an invalid value in there. But if I then start typing again, the error is removed again and it reappears if I, again, lose focus without a valid value. And therefore, this might then be the best of both worlds. And indeed, combining validation on every keystroke with validation on lost focus and resetting that focus state whenever the user starts typing again can be a good pattern for validating user input. But it's not the only way of validating input. You can also validate upon form submission.