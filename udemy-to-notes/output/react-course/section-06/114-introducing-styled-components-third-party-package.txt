So now that we had a very detailed look at solutions that all built up on vanilla CSS, in the end, even CSS modules, in the end, do as you saw, we'll now explore a totally different way of styling your React components. And that would be the styled components package, which you can use in React projects. And the idea behind this popular package is that you do not define your CSS rules and styles in separate CSS files, but also not as inline styles but instead in special components that are built with help of that package. And for that, to understand this package, we have to install it, which you can do locally by opening your terminal and then running npm install styled-components or encode sandbox by going to that dependencies area here and simply adding styled components like this as a dependency. This will install it in this project and make it available in this project. Now with it installed locally, you need to restart your development server. On CodeSandbox, this is not required. And now with that installed, let's say here in AuthInput, we now wanna style this div here, let's say with help of that styled-components package. For this, in AuthInputs.jsx, you now need to import something from styled-components and that something is called styled. So you need to import styled from styled-components and with that imported, here in that same file for now, you can then use styled, which turns out to be a JavaScript object to access different properties with the dot notation. And you'll see that these properties in the end map to HTML elements, for example, the h1, h2 and so on elements and also the div element. And what this will do is it will create a div as a separate component but a component that will have any styles you want applied to it. Now, for that, you now need to use a very strange syntax, which you maybe haven't seen before. You need to add two back ticks right after this element property here. So right after this div here. So again here, these are not single quotes, it's also not a double quote. Instead, these are two back ticks, a special character you can find on your keyboard. And this code here, in the end is a so-called tagged template, which even though you maybe never heard about it turns out to be a regular JavaScript feature. So not React specific and not specific to that package. Attached, you find a link to this MDN article where you can learn more about tagged templates if you want to. But in the end, a tagged template is like a function which receives this template literal as an input you could say. And it's now this template literal, which should now contain all the styles you wanna apply to this div here. And the great thing, of course, in JavaScript about template literals is that you can write multiline code like this without breaking the string. So now with that added, we can see that this div, which I wanna replace here, has a class name of controls. So therefore, we can go back to the index.css file and there look for all the styles that should be applied to that div. So to that class here. Of course, I then also have some styles that depend on that class being on a parent element, but we'll take care about those later. For now, let's focus on these styles here, which should directly be applied to that div and we can now copy those styles and paste them into this template literal here like this. And you don't need to use a camel case notation or anything like that here. Instead, you can write standard CSS code in here. And what this overall code will give you will be a component, a React component that automatically returns a div that has these styles applied to it. And it will be a div that internally also uses the special children prop so that it can be wrapped around other content. So with that, we can now store this component in a new constant or variable that here could be named ControlDiv or something like this, ControlContainer maybe. The name is up to you but it will be such a styled div now. And you can now use this custom component here to replace this div, here the opening tag, and then further down below, here the closing tag. Now, with that, I, of course, have a second component in this AuthInputs component file and we could outsource this into a separate file if we wanted to. But for now, since I really only use this component in this file here, I'll keep it in that file, If I were using that in other components as well, storing it in a separate file, of course, would make more sense. But with that, we can now save this. And if we now reload, we, of course, see that we lost some styles here because I did remove that controls CSS class that was applied here before. But what we can see if we inspect this and we take a look at this diff here is that in there, we got these styles being applied to it, these styles, which we did add here. And by inspecting this, we also see how this works. Under the hood, this styled-components package simply also creates unique CSS class names and defines the rules for these classes in the head section where this gets injected here. And then it also adds those created classes to your elements, here to that div, which it created because we used div here. If I would've used p here and I save this, this would become a paragraph instead of a div. But here I want a div. But this is how you can work with styled components. But of course, as mentioned, we now lost some styles. So let's fix that and let's learn a bit more about styled components.