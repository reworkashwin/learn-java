So, as shown in the last lecture, you can use Vanilla CSS by simply adding CSS rules in CSS files and importing those CSS files into your JSX files. That all works and is really great if you want to work with Vanilla CSS, or if you've got another developer or designer who's working on those CSS files because that person can just deliver these files to you. You just import them, and therefore you can work on the components and the JSX code, and the other person can work on the CSS code. That's one advantage of using this Vanilla CSS approach. And of course, you can simply write CSS code as you know it and you don't have to follow any special conventions because it is just Vanilla CSS code. Therefore, when it comes to using Vanilla CSS for styling React apps, one advantage is that you get your CSS code out of the JSX code and you will soon see that this is not necessarily the case for all available styling options. Another advantage is that you do write Vanilla CSS code. So, CSS as you know, and maybe love it or maybe hate it. Depends on you. And of course, another advantage related to that decoupling from JSX code is that different people can work on that code so that you can work on a component without getting distracted or interrupted by the person who needs to add some styling. Of course, we also got some disadvantages with Vanilla CSS though. For example, you need to know CSS in order to write it unless you have a colleague who does that. And then, there's another which we'll take a closer look in the next lecture. Because when writing Vanilla CSS code, when using the solution I just showed you in the last lecture, then that code and those CSS rules are not scoped to components which means you can have styling clashes between different components if you are using CSS rules that may affect JSX code stored in different components. But we'll take a closer look at that disadvantage in the next lecture.