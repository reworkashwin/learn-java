So let's make sure that, by default, those items here are not open. And instead we can open them by clicking on them, making sure though that only one item is open at most. Now since we're working with multiple components here, to make sure that every component on its own is highly configurable in a very easy way. Since we're doing that, we can't add the logic directly inside the Accordion component, for example, because there we can't easily interact with the children because the children can be anything. We're not rendering them inside of this component, we're just forwarding them. We're forwarding the content that's passed between the Accordion tags, between those </ul> tags in the end, nonetheless, we can add the core logic for controlling which item is open to that component. We can do that with help of React's context feature with its context API. So we can import { createContext } from 'react'; and call createContext here, and then create an AccordionContext. And I'm deliberately doing that here in that same file where I created the Accordion component, because now I'm not trying to build some context that should be usable in all kinds of places of the entire app, but instead I'm building a context that's directly tied to this component and all other related Accordion components as you'll see, because now here, instead of directly returning the on unordered list, we can return the AccordionContext.Provider wrapped around the unordered list like this, and set the value, just as you learned it earlier in the course. Now the value that should be set will be managed by this Accordion component. So this component now basically also acts as the provider for this context. Now, the value I wanna set here for my context should be an object, let's say, which in the end contains the id of the item that should currently be open. So the openItemId, because again, it should only be one item that's open at the same time, so we can manage it like this. We wanna store the id of the item that should currently be open. Now, initially this can be null because initially no item should be open. And then I also want to add two functions here inside of that Accordion component. One function that could be called openItem, for, well, setting a new openItemId in the end, and one function that can be called closeItem, which will set the openItemId back to null. Therefore, the openItemId should be dynamic, and hence we can use the useState hook from React and import that to manage the openItemId here with that state. So the openItemId and a set openItemId state updating function. Now, initially we have no open id here, but the openItem function here should receive the id of the item, which should now be opened as a parameter, and therefore here we can call setOpenItemId and pass that id to that state updating function. And here in closeItem, we can call setOpenItemId(null). Now down here for the context value, we can store the openItemId state here as a value for that openItemId key in the context value object. And in addition, we can, and should add a pointer to the openItem function. We could do it like this, or since we have the same name on both sides, use that shortcut. We can also do that for the openItemId by the way and also add the closeItem function. And that's now the context value that should be distributed to all interested components, but which components are interested here. Now the components interested in that context value of course, are the other Accordion components that are meant to work together with that wrapping Accordion component, so in this case the AccordionItem component, since that is the only other Accordion component we have. In here, we now wanna rate that Accordion context, and to simplify this process and also make it a bit more secure, I'll actually start by adding a custom hook to the Accordion component file. I'll export another function here, which is called useAccordionContext. So that will be a custom hook here. And the idea behind that custom hook is to use the built-in useContext hook in here and read that AccordionContext and get hold of that context. But of course here, since I'm exporting it, it is possible, that in theory, this hook here could be used in some other component that's not wrapped by the Accordion component, and that therefore doesn't have access to the AccordionContext because it therefore wouldn't be wrapped by the provider of that context. That's why inside this custom hook, I'm checking if not context. So if this is undefined, if we weren't able to connect to that context because the wrapping provider is missing, and in that case we could throw an error, so that if we or some other developer who might also be working on that code uses this hook in the wrong place, we throw an error telling the developer that this Accordion component must be used as an extra wrapper around whichever component wants to use this hook. That's an extra safety measure. So here we could then output an error message of, 'Accordion-related components must be wrapped by <Accordion>', something like this. Now, after this check and this error being thrown, we can then return that context. And here we know that it will exist. Therefore, now in AccordionItem, we can now use this useAccordionContext hook we just added, importing it from the Accordion.jsx file. And then here we can use the structuring to pull out the parts that are interesting to us. And in that case, that is the openItemId, the openItem function, and the closeItem function, so all three values that make up this context. Well, and with that we can try to find out whether it's this item, so the concrete instance that's using this component function here that is currently open. For that, we can expect to get an id on the AccordionItem and we can then use some derived state here to find out whether this concrete item instance is open by checking whether the openItemId which we're getting from our context, which manages all items together, is equal to the id we received via props. And if that is the case, we might want to add an extra CSS class to this div here, which outputs the actual content of that item, because it's that content that should normally be hidden. So here we can add a className and check if isOpen is truthy, and if that's the case, add the open class, which is another class I prepared as part of the starting project, otherwise we can add an empty string or undefined here, so that no clause is added if this item is not open. So that's the first step. Of course, we also must make sure that we do open or close the modal, and that can be achieved by adding a new function here in AccordionItem, which we could call handleClick. And I wanna connect this function here through the onClick prop to my title here, so that clicking the title opens or closes the accordion, so this specific item. For that we can check if isOpen is true, and if that's the case, we want to call closeItem because if it's open, we want to call it else, I want to call openItem. And to openItem, I of course wanna pass my id. Now with all that, we can go back to the app component and there we should now set that id prop on every AccordionItem. And we should give every AccordionItem a unique id, like for example, experience, for that first item where we talk about some experience. And then for the second item, maybe local-guides. The ids of course are up to you. With that, we now just also have to make sure that by default the content in here is not visible. And this can be achieved by adding another CSS class to this div. And that would be the accordion-item-content class, which should also be added instead of undefined if the item is not open. So it's either that class here and open being added, or just that class. With that, if we save that, we see that both items are closed, and if we now click on one item, it opens. If we click on the other item, on the other hand, that item opens, and we can use that approach to open and close items whilst also making sure that at most, one of the two items is open at the same time.