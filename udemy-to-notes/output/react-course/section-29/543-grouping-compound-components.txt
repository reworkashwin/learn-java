So this basic accordion is working, but we're not done yet. It's not just the styling that's not optimal yet. Instead, we can also improve the state management logic. At the moment, we have two functions for opening and closing, but it turns out that we can merge this into one function, the toggleItem function where I expect to get an ID and where we then call setOpenItemId. So our state updating function, and where we then update the state based on what was previously stored in that state. And if we are updating the state based on the old state, it is considered a good practice to use this function form of deriving the new state where in this case, I'll get the previous openItemId as an input automatically by React and where we then should return the new state value that should be stored. And that now could be the ID we are receiving here if that's not what we stored previously because if the previous ID is equal to the ID we're receiving here, then we clicked on an item that was already open. And in that case, we wanna close it. So here the new state I wanna set is a conditional state where I check if previous ID is equal to this ID we received here as a parameter. And if that's the case, that means the item was open and should now be closed. So in that case, I'll return null as a new value for the openItemId. Otherwise, if these IDs are not equal, some other item or no item was open, and in that case, I wanna set that received ID as a new state. And now we can add that toggleItem function to our context value. With that in AccordionItem, we can simplify this handleClick function and simply call toggleItem and pass this ID, which we received through props to toggleItem. Of course, for that, we also need to pull toggleItem out of this context. And now we could even get rid of this extra function here and just grab this code and delete this and then here set up this handler like that. That's, of course, optional though. Now with that, if we save this, this still works as before, but now we improved the code and there's one other code improvement we can implement here because of course, as you can tell, Accordion and AccordionItem are meant to work together. After all, that's the idea behind this Compound Components pattern. Now, it's kind of obvious that they are meant to work together because they're in the same folder and they both start with Accordion. But we can make it more obvious because it is a common pattern and a common practice when building such Compound Components to merge all those component identifiers into one object you could say. And it is indeed also quite common to use the main wrapping function object as an object to which all the other identifiers should be merged because in JavaScript, functions are, in the end, just values, to be precise, they are object values and therefore, even though you typically don't do it that often, you can add properties and methods to your function objects. And here in the Accordion.jsx file, we can use this feature to access the accordion function object by its name, and then instead of calling it or using it as JSX, we instead simply add a property by using the dot notation. And then I'm adding a new property called Item, that name is up to you though, and I'll set the value of that property equal to AccordionItem. So to my AccordionItem component function, which I am importing here into this Accordion.jsx file now. So this AccordionItem component function was now added as a value for that Item key on the Accordion function component object. And with that, we can go to the App component and there we now no longer have to import AccordionItem. Instead, we only import Accordion and we now output the item by using Accordion.Item. And I'm using that everywhere where I used the other component before. Now, it will still work as before, but it now makes it much clearer that this item component belongs to this accordion component and should be used inside of the accordion component because indeed, if we ever would try to use this AccordionItem component somewhere else outside of such an accordion, you would see that you get an error in your JavaScript console in your dev tools. To be precise, you'll get that error, which we threw ourselves in that custom context hook that this accordion-related component, in the end, must be used inside of an accordion. That's why we also added this custom hook with this extra check to make sure that we're not accidentally using AccordionItems that rely on being wrapped outside of the accordion. So with that, I'll get rid of that outside AccordionItem and instead go back to the code we had before now with this change here where it's much clearer that the items belong to the accordion.