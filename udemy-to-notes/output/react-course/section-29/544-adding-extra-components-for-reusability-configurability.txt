Now that's the Compound Components pattern in action and it can be a useful pattern if you wanna build highly configurable components that must work together. Now here we can actually even take it a step further and add more Accordion components to make them even more configurable and reusable. And for that we could outsource both the title and the content into separate standalone Accordion components. So here in the Components Accordion folder, I'll add another new component file, AccordionTitle.jsx, and also another one, AccordionContent.jsx. Now let's start with the title. Here, I'll export that default AccordionTitle component function. And in that function I want to return something from the AccordionItem component, this h3 element to be precise. I'll copy that and return that here. And now of course, for that in order to work we need to get access to toggleItem and the ID. So to get hold of toggleItem, we can simply use the custom useAccordionContext hook inside of that title component. So import it from Accordion.jsx and then call it. And with that we'll get hold of toggleItem. And of course, we need the ID and the title. Now we can accept both as props and we could replace title with children here so that we can wrap our AccordionTitle around the text or content that should be output as a title. Again, making this much more configurable since we for example now would also be able to output more complex markup as a title. So that's the AccordionTitle. For the AccordionContent, I'll add another function, AccordionContent is the name. And here I wanna output this div. So I'll copy that and output that here. Now for this div to work correctly, we need to get the children and we need to find out whether this content should be open. Now we can find out whether it should be open by again using useAccordionContext. So using that here in this new AccordionContent component. And here I'm then interested in this openItemId Now we also need to get the ID here so that we can check if this item is open by comparing the openItemId with the received ID. Now with that, we can go back to the accordion item and get rid of all this markup between the list items and get rid of this code and this code and simply accept the children as a prop here, children and className to be precise. And then we can output children here. So this component now for the moment got much simpler. Now in AccordionTitle I now actually also want to accept the className so that we can set this on this h3 element from outside this component. Again, making this component more configurable. And I'll do the same for AccordionContent. There I also expect to get a className or at least I wanna support getting one. And now instead of hard coding accordion-item-content here, I'll use string interpolation here with the special backticks notation in JavaScript To dynamically inject that className in addition to that open class. And to make sure that we're not adding undefined as text here I'll check whether this is undefined with this special operator. And if that's the case, I'll add an empty string as a className. Otherwise, I'll just add className. So if we are not in this open mode, I'll just set the className as the className of this div, hence making this component more configurable as well. Now I guess the even better way or the most versatile way of writing this className code here, would be to grab this code in the open case and set this in the not open case as well, but add a close class here so that We can go to the index.css file and there search for this accordion-item-content class. Then below that we could add a close class where we then move that display none css property from accordion-item-content into the close class so that it's decoupled from this class, which is really only meant for styling purposes to the separate class, which is a core part of the Accordion component itself. That's of course not required, but makes sense if we wanna write reusable, configurable code. So with that we got this AccordionContent component which adds either the open or close class in addition to whichever class we might be setting on that component. And therefore now we can go back to the Accordion.jsx file and add more properties to the Accordion function object and add a title property, which we set equal to AccordionTitle and add an AccordionContent property which we set to our AccordionContent component. And of course, you therefore must import the AccordionTitle and AccordionContent Components into this Accordion.jsx file. With that back in the App.jsx file, we can now output that title by using Accordion.Title here as a component selector. And now this title here can be moved between those tags and then below that, Accordion.Content. And then we move this article here between those tags. Now both title and content need the ID prop, so I'll add them to both. And with that, we migrated this code to this new structure. The advantage is that we can now easily add classes here, to the title for example, accordion-item-title. And for the content it could be accordion-item-content. With that, we can apply the same structure to the second accordion item and add the AccordionTitle here with a className of accordion-item-title and set this to this title and get rid of the title prop here. And then add Accordion.Content with the className of accordion-item-content and move this article here, this one, between those content tags. And now again, we must grab that ID and add it to both the title and the content. With that, this now looks much better and of course, works still in the same way as before, but now using these extra components for greater reusability and most importantly, configurability.