Now, there still are two last things that could be improved here regarding the accordion component. And the first thing that could be improved is that we have to set that id on both the title and the content. It would be nicer if we would just have to set it once on the Item, and that Item would then pass it down to the Title and Content. Now, we can't do that with help of props because the AccordionItem component, which is meant to receive that id, doesn't have direct access to its children. It just forwards them between those list item tags. We're not outputting them as components here in that component. But we can of course, again, use context as a solution here. We can create another piece of context here, and that would be the AccordionItemContext in this case. And here, it's a super simple local context, which will only be needed by a few components that are wrapped by one specific AccordionItem. So, inside of the AccordionItem component, we can return AccordionItemContext.Provider. And the value I wanna provide here will not be dynamic at all. Instead, the value I wanna set on this Provider component will simply be that id that's received on AccordionItem, so that components wrapped by this AccordionItem will be able to access that id through the context. Now for that, I'll again add a custom hook, useAccordionItemContext, so that we can safely extract that context with help of the built-in useContext hook, using that Accordion, I had a typo there, so that should be a AccordionItemContext.Proovider, like this. And then, using that here to select that context. And then, of course we wanna return it, but in between, we wanna check if it's maybe undefined, so if we used some component that uses this hook in the future outside of an AccordionItem, so not wrapped by such an AccordionItem, which would not work. So, we wanna check if the context is maybe not defined and in that case, I want to throw a new Error again to make sure that we're not doing that where I wanna say AccordionItem-related components must be wrapped by Accordion.Item, like this. Well, and now we can go to the AccordionTitle component and remove that id prop here, and instead use the AccordionItemContext, which we just added, imported from the AccordionItem.jsx file, to get the id of the item this title belongs to. And we can do exactly the same in AccordionContent. Get rid of the id prop, use our custom AccordionItemContext function, our custom hook here, and get the id. And with all that, back in App.jsx, we can indeed remove the id from title and content and instead, only add it on the AccordionItem, here for the local guides and also here for the experience. So, remove this id from title and content and instead add it to the item. And with that done, if you save everything and reload, this still works as before without any errors, but now with a better developer experience where we only have to set the id once and it will, behind the scenes, be consumed by all the components that need that id. Now, we're pretty much done with this compound components pattern here, and there is just one last potential improvement I want to implement here. Something you don't have to do, but something I want to do here. Because at the moment we are, of course, grouping all those Accordion components into that Accordion function object. And that's great because that makes it very clear that these components are meant to be used in conjunction with Accordion. But at the moment, it's also theoretically possible to import AccordionTitle like this and use it like this. And then, it might not be that clear that it should be inside of an Accordion. Now, you could say that it is clear and you could of course also be the only developer working on this project, in which case it's clear to you. But if you wanna be super safe that those components never get used outside of the Accordion context, or if you want to at least make it as clear as possible that they should be used in conjunction, you could of course grab all those components and merge them all into one single file so that you don't have to export those individual Accordion components. I won't do that here, because I wanna keep that code separated to make it as easy to read and understand as possible. But I wanted to mention this here, that this is something you could do if you want to make sure that those components can only be accessed through the Accordion component function object and not on their own. Here, I'll not do it though.