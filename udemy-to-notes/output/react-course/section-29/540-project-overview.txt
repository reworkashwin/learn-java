Now for this section, as so often in this course, I prepared another demo project. Also, of course, a CodeSandbox project. And attached, you find a link to both the CodeSandbox project and the local project. If you're using the local setup, you'll have to run npm install once to install all required dependencies, and then npm run dev to start a development server. On CodeSandbox, this is not required. Once the development server is up and running, you should see a page that looks something like this on the screen, and you should have this folder structure with essentially just some basic files, which we're now going to populate with some logic.