Now kind of related to render props is another little improvement I wanna make here in this app, and that improvement is related to my key here because the problem with that key here in "searchable list" is that it's currently that index, and as you learned before in the course, that's not a great key because it's not directly linked to the data. Now, linking it to the data is difficult though because we, for example, can't use "item ID" as a key here because not all items we're passing to "searchable list" will have an ID property and not all items will be objects to begin with. For example, our strings here are not objects and have no ID property therefore, so that won't work. What we can do instead here is again work with props and expect to get a item key function prop, for example, the name obviously, is up to you, and here, I expect this prop to get a function as a value, which I can now call here, hence the parentheses to dynamically generate a key for a specific item, so I'll pass that item to that function and it's now the job of the component that uses "searchable list" to provide a function which will handle the item I'm giving to that function to return a unique key for every item. So back in the "app" component on "searchable list for places", we can set that item key function to a function that will receive the item, and which in this case then, will return "item ID", and we can do that for the other searchable list as well, but here, it's the item itself that should act as a key, and this now extends that render props pattern for this specific use case here by passing another function to that searchable list component. Now a function that will not be used to render some JSX code, but instead a function that's needed to get a unique key. With that all, if you save that, you'll see that we got no errors here, and of course, the app still works as before, but now with render props and this additional key function.