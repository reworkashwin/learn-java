So at this point, I am quite happy with this modal here. And of course we can now use it as we always could to add new challenges to our list of challenges here. And that's indeed the part I wanna work on next because here we got these different tabs, and at the moment we don't really have any animations in those tabs. We have no animation as we switch tabs and we got no animation if we have multiple items here and we, for example move one item to a different tab by marking it as completed it just disappears here and appears here. But this list updates without any animation which also of course could be improved. And that's what we'll do next. And therefore here in this project I'll switch to the challenges JSX file which is where I have my challenges tab and this list of challenge items. And here we could, for example start by animating these challenge items as we mark them as completed, to be precise, what I'd like to achieve is that as those items are removed the list does not instantly update and snap into place like this. But instead, the item that remains here should be animated smoothly as the item before it was removed. And this can easily be achieved by going to this challenge item component where I have this list item that makes up such a challenge item in my list by replacing this list item with a motion list item. Now I'm already importing motion here in challenge item so we just have to Convert LI to motion.li, also the closing tag of course. Now when we do that out of the box, nothing happens. These items still move instantly and the list updates instantly. So nothing changed. But now that this is a motion component we can add a special property here. The layout property, if you add this prop to a motion component, Framer Motion will automatically try to animate layout changes in that component. So in that list item here, in this case, which means for example, that if we have a list with two items and we move the first item by marking it as failed the second item, this item in this case here, of course remains, but it now moves to the top. And this move to the top would then be animated by Framer Motion. And therefore, now with layout added here to this motion component, if I marked this first item as failed and its therefore gets removed from this list you see the second item moves up there. Now, of course, we have no animation. If I mark this as failed as well because now it's not moving to a different place. It's just gone here, it just reappears if I go to this tab. But if I have a scenario where an element needs to move to a new position, like here if I mark the first item as completed, now this is animated. If you add this layout prop to the element that is moving.