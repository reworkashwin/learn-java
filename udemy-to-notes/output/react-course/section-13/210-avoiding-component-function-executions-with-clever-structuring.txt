So 'memo', if used with care, can be useful, but it's not the only way of preventing unnecessary renders here. Another technique that is often even more powerful than memo is a clever component composition in your application. For example here, I have this section with that input, where we register every keystroke and change the state with every keystroke, in this app component. Now of course, we could put this into a separate component, and then this state that changes on every keystroke would also live in that separate component and would not affect this app component. So here we could add a separate component, which could be named 'ConfigureCounter', something like this, where I export my 'ConfigureCounter' component function and we can then grab this section code here from the app component, cut it, and return it here in 'ConfigureCounter' and also grab this code here and copy that and add it here. Though we don't need the chosen count state here, just the entered number. And now we just need to lift some state up, so to say. So that of course when that number that was entered is confirmed by clicking this set button. We propagate this information from 'ConfigureCounter' to the app component because that state will be needed here, so that it can be passed down to the counter component where it will in the end then be needed. And therefore in the app component we can get rid of these functions and of the 'EnteredNumber' state. But we must keep the 'ChosenCount' state and we should now add a new function here, 'handleSetCount' could be the name, where we expect to get the new count as an input, as a parameter. And where we then call 'setChosenCount' and set 'newCount' as a value. And we should use 'ConfigureCounter' here, and of course therefore also import it here. And then on 'ConfigureCounter', we must set a prop, through which we can pass this function. So that this function here can be executed from inside 'ConfigureCounter', a pattern we used many times before in this course. So we could name this prop 'onSet' and point at 'handleSetCount'. And then in 'ConfigureCounter' we can destructure this 'onSet' prop or whichever name you chose. And we must import 'useState' of course from React. And now here in 'handleSetClick' instead of calling set chosen count, we can call 'onSet' here, and pass the entered number as a value to that function. So in the end, two 'handleSetCount'. And by doing that we now make sure that this state that does change on every keystroke lives in this separate component. And therefore causes this component function to be executed again. But the app component function will not execute on every keystroke in this 'ConfigureCounter' component because, as I mentioned before, state changes and re-executions of child components don't trigger parent component executions. To visualize this, I'll add this log function here, which must be imported from the log JS file, to output 'ConfigureCounter'. And this will be on level one. Because it's one level below the app component here. And if I save that all and I reload here, you see that 'ConfigureCounter' is executed initially. But if I now clear this and I change this value it's just 'ConfigureCounter' that's being executed. The app component is not executed. The header which was executed before is not executed. And of course the counter also is not executed. And now here we could and should actually also remove memo from the counter because now memo is useless here. Because the app component now isn't executed anymore when we enter a new value here, but it will be executed when 'ChosenCount' gets updated. So when we click this set button. But when that happens we also will always re-execute the counter function because 'ChosenCount' is received as a prop here and therefore the memo check will find a different value, most likely, unless we entered the same counter value as before. Which would be one scenario where memo still could be useful, but in most cases it won't be useful because we'll have a different value. And therefore here we could argue that memo should be removed because it's not going to prevent many counter function executions. But it will of course always perform this check, which also costs performance. Here, I'll leave it around, so that you still see how it could be used. But be aware of the fact that here you could consider removing memo because it doesn't make too much sense here.