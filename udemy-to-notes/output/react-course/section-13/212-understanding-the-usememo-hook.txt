Now in this output from the last lecture, you also see that this calculating if is prime number code is output every time we click this button. So this clearly is code that is executed, again, if the counter changes. And that makes sense because it's this isPrime function, that is called directly inside of the counter component function, that is being executed whenever this component function changes. And it's this function where this log statement is coming from. Now, this function is just there for demo purposes, to be honest. But this function does, in the end, use this initial account value as an input. And this value technically only changes if we enter a new value here and click set. Though at the moment this doesn't work and we'll fix it later. But still it's only at this point of time where a new initial count value will be received and isPrime will yield a new result. Other than that, if initial count does not change and if the counter component function is only re-executed because the counter state changed, isPrime will always produce the same result. And that can be a problem if you have a function that performs a rather complex and performance intensive calculation. Which is not necessarily the case here but it's also not a super trivial calculation, especially if you enter a bigger number in here, like a million, or a trillion, or something like this. Re-executing all this code all the time, even though it will produce the same result as before is then not very efficient. And therefore just as you might want to prevent component function executions with memo, you also might want to prevent the execution of normal functions that are called inside of component functions, unless their input changed. And React gives us a hook that can be used for this scenario and problem, the useMemo hook. Which is not to be confused with memo, memo is wrapped around component functions, as explained a couple of lectures ago. useMemo is wrapped around normal functions that are executed in component functions to prevent their execution. And useMemo should really only be used if you have a complex calculation that you want to prevent. For example, here if we wanna prevent isPrime from executing we can call useMemo here. And then through useMemo, we pass a function, for example, an anonymous arrow function. Which then returns the result of the function that we want to prevent executing. So it's in this anonymous function where we now call isPrime. useMemo then also wants a dependencies array, just as useCallback wanted it. And the idea behind useMemo is now that react will now execute this function, which you pass to useMemo. So this anonymous arrow function here. And it will then store the result of this execution, so the result of calling isPrime then in the end since that's returned in this anonymous function here. And it will only re-execute this function if one of those dependencies here changed. So if you have an empty dependencies array this will never re-execute, because there are no dependencies that could change. But of course here you can see that I'm already getting a warning, because here I should add initial count as a dependency because that is used as a input by isPrime. And indeed, if initialCount changes isPrime will yield a different result, and therefore you should add initialCount as a value here in this dependencies array. And with that useMemo, we'll now make sure that in the end isPrime only gets re-executed if initial count changed. But it will not get executed if this counter state changed, for example. So if we save this and we go back and reload, clear this, you'll see that if I click increment we no longer get this output from inside the isPrime function. On the other hand, if I set a new counter value we do get that output here. Now I wanna emphasize though that you really should not overuse useMemo. You should not start wrapping it around all your functions, because just like Memo, of course it does need to perform this extra dependency value comparison. And if you have a function that for example simply needs to be executed on basically every component function, re-execution. Adding this extra check doesn't make any sense and instead just cost extra performance. On the other hand here where indeed a lot of executions can be avoided, and where we potentially execute code that could take some time to finish using useMemo makes more sense. So just as with memo, you should use it with care. But if used in the right situations it can help improve the performance of your react application.