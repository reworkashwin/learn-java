So let's now make sure that we output different content here depending on which tab button was pressed. And for that we could of course, try using a variable. We could add a variable here in this App Component function and maybe name it tabContent. And initially here we could say, please click a button but then let's say when handleSelect is executed we wanna update this tabContent variable and set it equal to the selectedButton. So to this identifier, which we're getting here and that's of course not the final result I want to have but it should be a good first step. So with that, we can then use this tabContent variable as we learned it before, and output it dynamically down here with help of those curly braces. So with that, we're outputting tabContent and if we save that, we indeed do see please click a button as a starting text here. But what you'll notice is that as you press those buttons and I am pressing them here, I am clicking. But as I'm doing that you'll see that this text here does not change. It stays the same. So it looks as if this code is not working. And just to be sure that I'm clicking these buttons I'll actually console.log tabContent inside of handleSelect after setting it. But as I do that, if I reload you'll see that I get the log statements on the right here. But this text here is not updating. So clearly the function is executing and we're updating the variable, but the UI is not updating. And the user interface is not updating because this App Component function is not executed again, just because this handleSelect function executes. But therefore, since the App function is not executing again, this entire JSX code here is not getting reevaluated. So it still only knows the initial tabContent value because this code must get reevaluated by React in order for the UI to be updated because React in the end takes a look at this JSX code and it compares it to the currently rendered UI. And if it detects any differences it will update the UI accordingly. But of course here, since this code only gets executed once, the new content is never picked up and the UI is never updated. Because by default, and that's really important to keep in mind, React will only execute a Component function once when it's first encountered somewhere in your code. So the App Component function, for example is first encountered in the index JSX file. And that's there for the point in time where React executes that function and renders it. And the TabButton Component, for example is first encountered when that App Component is executed and rendered. So that's when the TabButton Component function will be executed and rendered. But thereafter, neither of these functions will be re executed. And you can see that and understand that if you add a console.log statement right inside of the App Component function. So not inside of handleSelect but inside of the App Component function. And there we could log App Component rendering or executing whatever you want, it's the same. So we could log this here and we could also log this in a TabButton for example. And here we wanna say TabButton Component executing. And if you do that, you'll see that if you save this and you reload, you see App Component executing being output once, and you see TabButton Component executing being output four times because we have four such buttons. So therefore, indeed the TabButton Component is encountered four times in the UI. And React will execute the respective Component function for every time it encounters it in the JSX code. But you'll also see that as I press one of these buttons, these log messages here are not print it again only these other log messages from inside the event handler function. And that's therefore why updating the UI with a regular variable like this, does not work because this Component function is not executing again. So what we need is a way of telling React that this Component function should be executed again. And that's exactly what the very important concept of state is all about. And we'll explore that concept next.