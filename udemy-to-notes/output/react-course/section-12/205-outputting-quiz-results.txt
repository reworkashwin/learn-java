So to come to an end for this section and this quiz, I now wanna work on that quiz completed screen here, and there I wanna make sure that we basically tell the user how many questions were answered correctly, how many were answered incorrectly and how many questions were skipped, and then I wanna show the user details for every question. For this back here we can add a new component which could be named summary, in a summary.jsx file. And therefore a component function also is named summary. And here I then wanna return a div with an ID of summary. And in the end it's of course this div which we already have in the quiz JSX file, this div here. So I will actually cut that and move it from the quiz file to the summary file. And of course we also need to bring over that image import so that should also be removed from the quiz file and be moved to the summary JSX file so that the image gets imported here. And then here below this H2 element I want to have a div with an ID of summary stats where some overview stats should be presented. And here my goal is to have multiple paragraphs where in every paragraph I have a span with the percentage number of, for example, skipped questions, and that should get a class name of number for styling purposes, and then a second span with a class of text where I say skipped. And then we copy this paragraph and repeat it twice so that we also can output the correctly answered questions and the incorrectly answered questions. And of course those percentage numbers should be derived dynamically based on the actual answers picked by the user. Now next to this div with the summary stats I then want to have an ordered list where I output more details about all those questions. So that here we in the end output list items where every list item will have an H3 element where I wanna output the number of the question, for example two, then a paragraph where I output the question text. And this should receive a class name of question for styling purposes. So here we'll have the question text then, and then another paragraph with a class name of user-answer, where I wanna output the user's answer. And of course again this will all be replaced with actual data soon. But therefore we'll need to collect that data. And of course we're already doing that to some extent, in the quiz JSX file we are storing all these selected answers, and that is of course the most important piece of information, because it's this list of selected answers that can then be used to calculate all these numbers and output these details here. So in the end, it's in the quiz component file where we want to use the summary component file we just worked on. And therefore here we should import summary from summary JSX, and then output this summary component here if the quiz is complete, if it's over. And we can then pass the user answers as a prop to that summary component. So here it's this user answers state value which I want to provide as a value for this user answers prop to summary. Of course you could use a different prop name here, but I'll stick to user answers. And in the summary component we can therefore destructure the props object to get hold of this user answers object. And now we can use this user answers object to derive all the different data points we need in this component. For example, we can output those answers and questions down here. Now for that, we'll also need the questions of course, so we should import questions from, and then going up one level questions.js, so that we have the raw question data available as well. With that imported down here in this ordered list we can map through the user answers to output a list of items based on those user answers. And here I then wanna return this list item for every answer, and I therefore wanna output the answer here, and we should add a key here, which also can be the answer, since this will be a unique value across all answers, we can output the number of the question to which this answer belongs, with help of that index which we also get automatically in this function that we pass to map, that will be provided automatically by JavaScript. And that index of course starts at zero, so we should output index plus one here, so that we show the number one for the first question, the number two for the second question, and so on. And to output the question text here we can now use the questions raw data and use this index to access the different questions to which the different answers belong, and then output the text of that question here. With that if we save that, you see we're getting that list of questions on this screen, and we also see which answers were picked for every question. Now it is of course possible that a user skips a question though, if they don't pick an answer before the timer expired. Therefore, instead of always outputting answer down there which could be null if no answer was picked, because in that case in the quiz component, we are storing null as a answer if the timer expired and no answer was picked. So therefore, instead of outputting null down there, we should check if answer is truthy and as a fallback value if it's not, so if no answer was picked, we could output skipped here. In addition, I wanna set this class name here conditionally to show the user on this screen here whether an answer is correct or not. To do that I'll dynamically derive the CSS class that should be added here, for the answer we're currently looking at here. And I'll add a CSS class variable for that which is always user-answer. So that's the base CSS class that should always be added. But then here we can check if answer is equal to null which means it has been skipped, in that case, I want to add something to that string. I want to add a white space and then thereafter the skipped CSS class to apply a special style if this answer was skipped. Else if we have an answer, I wanna check if it's correct. So then I wanna check if answer is equal to questions for the current index, and then there we know it's the first answer that's the right answer. So if this here yields true, we know that the right answer was picked. And in that case, I want to add the correct CSS class here. And this white space here is important by the way, to make sure that as we're concatenating to this string here, we're separating those CSS classes with white spaces. And then last but not least, in the last else case here we know that an answer was picked but that it's the wrong answer, and therefore here I want to add the wrong CSS class. And this here should be an else if branch by the way, I forgot the if keyword. And now it's this CSS class that should be added down there. And with all that done, you now see that incorrect answers have a reddish text and right answers, correct answers, have a greenish text. Now we also need the summary statistics up here. And for that of course we need to take a look at all answers and calculate the share of skipped, wrong and right answers. So here I'll start by calculating the share of skipped answers, by taking a look at my user answers and then filtering for all the answers where the answer is equal to null. Because as I mentioned before, we add null to this answers array if an answer is skipped. So with that, we get the skipped answers. The correct answers, are all the user answers where the answer is equal to the first answer in the raw data. So therefore here we actually also need the index of that answer in that answers array, so that we can select a correct question in that questions raw data array. And then we compare the answer to the first answer in that raw data. And if we have equality, we know that it's a correct answer and it will be added to this correct answers array. And therefore we can, for example calculate the share of skipped answers, so skipped answers share, by taking the length of the skipped answers array so the number of skipped answers we have, and dividing that by the overall answers length, so the number of answers we got in total. We can then multiply this with 100, and then use math round to round that overall result. And we can of course apply the same logic for the correct answers. So the correct answers share is simply the correct answers length divided by the user answers length. Well and the wrong answers share is then of course simply 100 minus the skipped answers share minus the correct answers share. So whatever is left must be our wrong answers share. And with that we can output the skipped answers share here. And I'm keeping this percentage sign thereafter so that we still see that as text on the screen. Here we then have the correct answers share. And here, for the last summary statistics number, we have the wrong number share. If we save that, we see these numbers here. Now to check that it all really works as it should, I'll restart the quiz, and I'll let the first question expire here without picking answer, then we can try to answer the next one. Looks like this here is wrong. Maybe try to pick a right answer for the one thereafter. And then I'll again let the next question thereafter expire, really just to see whether this then all behaves correctly and looks right on the summary screen. Here we can again pick an answer. Looks like it was wrong, maybe you can do better. Take this answer here, and here for the last question, I'll also pick a wrong answer. And with that here, you see I'm back on this screen and this is all looking good to me. I got these different questions, I skipped two questions. I also got two questions right and therefore it makes sense that we have the same percentages here. And then we got the incorrectly answered percentage where I got three answers wrong. So that's looking good here, but if we open the developer tools, I'm getting an error here, that I got two children with the same key. And the problem here simply is that of course I got two skipped answers, and since I'm using my answer as a key, I got twice the key null being used here. Therefore, here's a situation where the index is a better key. And typically you wanna avoid using this index of the map method, because this is now not tied to the data but instead by the position of the data in the array. So if two items in the array would swap position the data would be changed, but the index would not change because the position one still has the index zero, for example. But here it's fine because we aren't swapping any answers in this list, we're not swapping data here, and index indeed is the the best key we have here. Therefore, here, I'll change this. And with that, the error is still there from before but we would now not get it again if we would go through the quiz again. And that's therefore, this quiz finished.