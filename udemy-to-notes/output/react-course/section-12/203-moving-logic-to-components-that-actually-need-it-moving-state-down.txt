So now that we added this new Question component, we fixed this problem with our key, but we also kind of added a new problem, because now we have to pass a lot of props that are managed in the Quiz component to the Question component just to make sure that it works correctly. And this is something we can improve here, and at the same time, we'll improve this Quiz component, because, for example, all that logic where we manage the answerState, why does this need to happen here in the Quiz component? Well the answer is, it doesn't. That is a great example for a state that does not need to be lifted up. Because we can simply go to the Question component and then there, add a new state with the useState hook that must be imported, of course, that will belong only to this Question component. And this could be a state where we wanna store an object, where we have the selectedAnswer, let's say, which initially is an empty string, and then let's say, isCorrect property, which initially is null, but which could then be true or false. This is some state we could manage here and I'll name it answer and have my setAnswer state updating function. And the idea now simply is to have a function, handleSelectAnswer, that should be triggered whenever an answer is selected here. So therefore I'll point at handleSelectAnswer down there, and I then expect to get my answer text here. We can then call setAnswer and set this object to a new object where selectedAnswer is equal to the answer we received here and isCorrect now must be derived. But it should only be derived after this one second which we actually spend here in the Quiz component. So for the moment, I'll still set it to null here, which basically should say that we don't know yet whether it's correct or wrong. So therefore here, I then wanna set a timer with setTimeout where after one second we do set isCorrect to true or false. So where I call setAnswer again and where I now set isCorrect equal to, and then I wanna set this to true if this is the right answer or false if it's not. But for that, we, of course, need to know what the right answer is. Now the great thing about this project is that we have all questions and answers in this questions.js file. So we can simply import this file into Question.jsx to take a look at the current question and get the right answer. So here in this file we can also import QUESTIONS from and then going up one level, questions.js. So that now, we can take a look at those questions here. And the great thing is that we also, of course, can find out what the index of the currently active question is because in the Quiz component, we're setting this index as a value on key on this Question component. And we can, of course, try using that key on our own component for our own purposes as well. So in our Question component, we can therefore destructure this key prop and also use it here. So now with that here in this Timeout, we can access QUESTIONS for the key, so for this activeQuestionIndex to take a look at a specific question. And then we take a look at the first answer there, which we know is the correct answer for the selected question. And we compare that to our answer here. And if that here yields true, we know that the correct answer was picked, otherwise it's the wrong answer. And with that, we can now use our own answerState to update the UI. So the value for answerState here should now be correct or wrong. So we can add a little helper variable here, which I'll name answerState, which initially is simply an empty string, just as my old answerState here was in the Quiz component. But if in our answerState here in the Question component, if there we got a selectedAnswer, I wanna set answerState depending on answer.isCorrect. And if that's true, I wanna set it to correct, otherwise I'll set it to wrong. And then it's this answerState that will be passed to answers. Now of course, that's now clashing with my answerState prop, but this prop can and should now be removed because we don't need it anymore. Now onSelectAnswer will not be removed though because we will still need to pass the selectedAnswer to the Quiz component, because, of course, in the Quiz component, we still wanna store all the selected userAnswers so that we can also find out when the quiz is over. Therefore, I'll call onSelectAnswer here once this Timeout expired and once we show the user whether the picked answer is correct or not. To be precise, here, I then wanna set another timer again so that we don't instantly move away, where after two seconds, we call onSelectAnswer and pass the selectedAnswer to this function, and that will then trigger the connected function in the Quiz component. Now with that, we can still get rid of some props here in this Question component though. For example, answers and questionText, because we are importing the raw data now and we know that the key is the activeQuestionIndex. Therefore, we can get rid of those props here and instead, here, simply use questions for the key and then text, because the key, as mentioned, is the activeQuestionIndex. And for the answer, it's QUESTIONS[key].answers. So with that, we got two props less that we have to accept. The selectedAnswer is also not needed anymore because we're now storing this here in our state that belongs to this Question component. We now just have to update the selectedAnswer down there and set this equal to answer.selectedAnswer, which is that selectedAnswer we are now managing in our own state here in this component. And with that, this question component now contains more logic and needs less props. Therefore, in the Quiz component, we can and should now remove all those redundant props. To be precise, selectedAnswer, answerState, answers and questionText, and get rid of all the logic that we don't need anymore. Specifically, we don't need this answerState anymore, and we can, again, derive the activeQuestionIndex just based on the userAnswers.length because this will now only be updated from inside the Question component once we did log in an answer and show the user for two seconds whether that answer was correct or not. And we don't have to update the answerState here anymore and we should remove those timers here in the Quiz component. Therefore, useCallback here also no longer needs the activeQuestionIndex. And with that, the Quiz component function got leaner again, which is great, and the Question component got more powerful. Now if we save all that, we'll see that we're now facing an error though. And the error is, if we open the DevTools, that we are using the key prop. This is a prop that's exclusively used by React and must not be used by us. Therefore, we should pick our own prop, for example, questionIndex or just index, any name of your choice. And we should use this prop instead of key in all the places where I did use key in the Question component. And in the Quiz component, we should now also set this index prop here even though it contains the same value as the key prop. But since this is a prop exclusively reserved for React, we must use our own prop here. But with that, if we reload, we see our questionnaire, we can log in questions, and of course, we also see that now I instantly get the feedback that tells me whether the question is right or wrong. And we also seem to move through questions pretty quickly now. Now to make sure that we don't instantly mark an answer as right or wrong, we should actually go here to the answerState in our Question component and not always set it to just correct or wrong, but instead here I wanna check whether answer.selectedAnswer is truthy and answer.isCorrect is not equal to null, because if isCorrect is null, that means that we might have a selectedAnswer, but we don't wanna show the result yet, because we deliberately set isCorrect to null here when we log in an answer. Now to make sure that we mark the selectedAnswer as selected, we also should make sure that if this condition here is not met, but if we have a selectedAnswer, we set the answerState equal to answered, which is then the condition we're checking for here in the Answers.jsx file to mark an answer as selected. Now we can stick in that file to make sure that we then can't move through all those questions too quickly, because here I actually wanna make sure that we can't click those answer buttons if we have a selectedAnswer, so that we can't switch the answer thereafter. And therefore I'll set the disabled prop here and set that based on whether answerState is an empty string, which means we don't have an answer selected yet or anything else. It should be disabled if answerState is not equal to an empty string. Because if it is an empty string, it should be enabled. It should not be disabled because we have no answer yet. But if it is not equal to an empty string, we know that it should be disabled because we either have a correct or wrong answer or we don't know yet, but we did select an answer. And with that, if we save that and reload, now I can select an answer, it gets highlighted as such, the other answers get grayed out, and as you can tell, it then gets marked as right or wrong, and we move on to the next question thereafter.