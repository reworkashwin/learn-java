Now that we can see this component here and enter values here, the next step is to collect these values when we click save, and to then validate them. And if invalid values were entered because at least one of these inputs was left empty, I want to show a little error dialogue. But if correct values have been entered in all inputs, I want to be able to click save and add a new project, which then should also show up here in the sidebar. And let's start with this success case. For this, in this NewProject component here, we need a way of collecting those values that were entered into the inputs. And one way of course would be to listen to the change event and update some state with every keystroke and store the entered values like this. That's one possible approach about which you learned earlier in the course. But here, since I really only wanna read those values when I click this save button here, I don't want to add and manage all those extra state. Instead I'll simply use refs, because as you learn, you can use refs to connect them to HTML elements and then interact with those elements, for example, to retrieve the value of an input. So for that, we of course must import useRef from React. And then here I'll have my title ref or just title, however you want to name it, then my description ref. And last but not least, the dueDate ref. So three refs since I have three inputs down here. Now it's important to keep in mind that this input here, of course, it's a custom component. It's not the built-in input, and therefore adding a ref prop like this might not work depending on the React version you are using. If you are using React version 19 or higher, this will work and you can simply use and extract this prop named ref which we added here in the input component like any other prop, just that it will hold a ref as a value. But if you are using an older version of React, React 18, let's say, then this won't work as you learned. Instead, you'll then have to go to that input component and there, instead of using this ref prop like a regular prop, you have to wrap this component with the forwardRef function that must be imported from React because that will then forward any incoming refs. And as you learned, wrap your component function with forwardRef, and then store it in a variable or constant, which then is exported as a default like this. But with that, we're wrapping this component function and this component function therefore now receives that extra parameter, this second parameter after the props, this ref parameter to be precise. And with that extra ref parameter, we can now go to the text area and to the input and there, add this special ref prop, which exists on all built-in elements and set it equal to this incoming ref here, like this. And we do the same on the input because of course either of the two is being rendered depending on the value of that text area prop. Now with that, we're able to set this ref prop on our custom component and it will get forwarded and can be used inside of that custom component as we're doing it here. And we can, and should now repeat that for the other inputs, here for the description input, and then here also for the dueDate input, so that we're setting all these refs here, and with those refs being set, we can add a function, handleSave could be the name, which should be connected to that save button. So here I'll split this across multiple lines to make it easier to read and add the onClick prop and point at handleSave here so that this handleSave function is executed when this button here is clicked. Now with that inside of handleSave, we can get the enteredTitle by reaching out to this title ref and there the current prop as you learned, you always have to do that when working with refs. And then since this will be referring to an input or a text area and both elements will have a value property, we can access value to get the entered value. And I'll then repeat that here for the entered description using my description ref, and also repeated of course for the enteredDueDate using this dueDate ref. Now we also want to add validation, but we'll do that later and show that modal if invalid values were entered. But for the moment, we'll just assume that valid data was entered, and we therefore now need to pass that data back to the app component because that's where I'm managing all these projects and where I want to add this new project because I will need to then pass it to the sidebar, for example, so we need that state lifted up to the app component since only the app component has access to the NewProject component where the data is generated and the state change will be initiated, and the project sidebar component where the state data will be needed thereafter. So therefore, here in the app component, we must add a new function, which I'll name handleAddProject, here in this function we start this adding process, here, we finish it. And in this function we now of course need to call setProjectsState again using that function form, since we're updating the state based on the old state so that we don't lose any old state. And we'll return a new object here where I'll start by spreading all the existing state so that the existing data is copied into this newly returned object. And we then update the project's Array. And I want to update this Array without losing any old projects. Therefore, I'll start by copying all the old projects with the spread operator into this new Array. And then as a last element, we'll add the new project that should be added. And for that here I'll add a newProject, which will be a new object, which in the end should contain a title and a description and then a dueDate property, but to keep things simple here, I'll simply expect to get some project data here as a parameter from the place where this function is invoked. And I expect project data to be an object with a title, description and dueDate property, hence, I'll just spread my project data into this new object and enrich it by also adding an id, which will, for example, later be important for selecting a project. Now this id should be generated here. And here I'll simply generate it by calling Math.random, a function built into JavaScript, which does not give us a perfect id because theoretically I could generate the same number twice, but it's unlikely, and good enough for this dummy project. So with that, I'm creating this newProject and it's now this newProject that should be added here to this project Array. Now, handleAddProject should be invoked from inside the newProject component, which is that component that has this handleSave method where we are collecting all these input values. And of course that works via props. Here we can expect to get a onAdd prop, for example. Name as always is up to you, which we can then call as a function here in handleSave, and to which I then want to pass an object as an argument, an object with a title property, which contains my enteredTitle, a description property, which contains of course, my enteredDescription and a dueDate property with my enteredDueDate, like this. So that's an object of that shape I'm expecting in the app component. And it will be this object, which I'm passing to onAdd that will be received here as an argument, as a parameter in handleAddProject, because we will now set handleAddProject as a value for this newly added onAdd prop on the newProject component. So here, I'll add this handleAddProject function, and with that, the connection should be established and this project's state should be updated. To see whether that works, I will for now add a console.log statement to my component function, so to the app component function here, where I simply log my project's state so that I can see whether that list of projects changes over time. Therefore, now, if we save that and I reload and open my log here, initially I see an empty Array, but now if I click create new project, the app component got updated, so I get another output here with an empty Array. But now if I enter Test, and, This is a test, and then a due date of today, though I'll soon make sure that this year is actually a date picker, and I click save. Now indeed, I get a state with a project Array that includes this data. So here's the data I just entered. So this connection works. By the way, in case you're wondering why I'm getting two outputs here every time the app component is executed, that happens because I have StrictMode enabled, and as you learned before in the course, StrictMode causes component functions to execute twice. But now with that, I wanna make sure that this here is actually a date picker so that we really get the right data, and hence back in the new project component, I'll set the type of this input to date, which is a built-in supported type. And on the first input for completeness sake, I'll set it to text. With that, if I save that, I got this nice date picker here, which I can now use to select the date. And if I save that, I can see that this project was also added and that there we got a proper date. So updating that state works, as a next step, the projects that are added should be shown in the sidebar. And I also wanna close this component here when I click save with valid data.