So we wanna make sure that we either show this or this NewProject component depending on whether this add project button or this button here was pressed. And for that, we'll, of course, need state because state then allows us to render content conditionally. And we'll need to manage this state here in the App component because that's where we make this decision about which component to display. Hence, in here, we should use the useState hook, which, of course, must be imported from React. So this import must be added. And then here we have this state that controls which component is displayed. Now, there are definitely many valid ways of managing the state here, but I'll go for an approach where I'll manage an object as a state which has a selectedProject property, which I'll initially set to undefined, not to null, but to undefined. And I'll soon show you why I'm doing that. And I'll add a projects array here where we'll later add the projects that are created by the user. Now, I'll name that state projectsState because that sounds like a good name for this object and setProjectsState to update it. Now, my idea behind adding this selectedProject property and setting it to undefined initially is that this selectedProject property will be used to either store the ID of the project that was selected when we later have multiple projects that can be selected here or null if we want to add a new project or undefined if we're not adding a new project and we also did not select any project. Now that's, of course, just one way of managing this. You could also add another state, which you maybe name currentAction, and that could be something like adding or selectedProject or nothing selected, which you could then also use to control what's being displayed. Here I just wanna work with as little state as possible, which is why I'm reusing this one property and I'll rename it to selectedProjectId to be a bit clearer about its purpose. So initially, it's undefined, which simply means that we're neither adding a new project, nor have a project selected. But that should change if we click this add project button. Then it should change to null instead. So I'll add a new function here in the App component. handleStartAddProject could be a fitting name because this should be triggered once we start adding a project by clicking one of these two buttons here and in there, I wanna update my project's state. And of course, I wanna update it such that we don't lose the old state because that old state, for example, includes all our projects, which later, of course, will not be an empty array anymore. And therefore, we need that function form where we get the previous state and return the updated state, the updated state object here. And in here, I'll set my selectedProjectId to null because as I explained before, that should act as a signal for us that we're now adding a new project. Undefined means that we're doing nothing. Null means that we're adding a new project. And that's, of course, just my definition in this project. You can definitely also go for a different solution. I'll also make sure that we don't lose the old state by spreading the old state into this newly returned state object. Now we need to connect handleStartAddProject to this NoProjectSelected component so that in there, we can connect it to this button. Therefore, I'll add a prop, which I'll name onStartAddProject, for example, where I'll point at this handleStartAddProject function we just added. And I'll do the same on my sidebar because there we have this other button that should start this add project process. Now we, of course, must go to those components and accept and destructure this onStartAddProject prop there. So for example, in the ProjectSidebar component, I'm destructuring it here and we can now connect it to this button where we add the onClick prop and then point at the onStartAddProject prop so that in the end, this handleStartAddProject function pointer is forwarded to the onClick prop in this button. And keep in mind that in this button, the onClick prop is simply collected with all the other props and is spread onto the native built-in button element, so that this entire connection can be established. So with that, we should be able to start adding a new project from inside the sidebar in the NoProjectSelected component here. We of course also need to accept this OnStartAddProject prop and there connect it to this button by forwarding it to the onClick prop there. Now both components are connected and we therefore now can use the project's state here to either render this component or the NewProject component. For that, I'll add a new variable here, content, where I'll check if projectsState.selectedProjectId is equal to null, which as explained before, should mean that we wanna add a new project. So therefore, in that case, I'll set content equal to this NewProject component like this. Else if projectsState.selectedProjectId is equal to undefined, I wanna set content equal to my NoProjectSelected component. So in the end, to this component here. Therefore, I'll grab and use that and later, we'll also make sure that content could also hold a selectedProject. But for the moment, it's either of these two components and therefore, now we can simply output content down there next to the sidebar. And with that, by using state here like we're doing it, if we save that and reload, initially, we should see this fallback screen but if I click this button, we switch to this NewProject component. Or if I reload and click this button, the same happens. Now as a next step, we wanna gather these inputs and create a new project. And of course, also make these buttons here work.