And therefore, to make sure that this clear button works, we must fill this handleDeleteTask function in the app component with Sun Life and then connect it to that clear button in the tasks component. Now let's start with this handleDeleteTask function here and let's make sure that it does delete a task. For that, I'll again copy the code where I update my projects to delete a project because again, the code for deleting a task will be quite similar and I'll paste it in here. But now of course, I won't update my projects but my tasks and I expect to get the idea of the task that should be deleted as an input here. And now I need that as an input because now, I have no other property that would contain the idea of the selected task or anything like that. So I expect to get my ID as an input here. And then I update my tasks by going through my previous tasks using that same filter trick. But now, we're going through all the tasks here and I'm comparing the task IDs to the idea I'm getting here. Now I don't wanna update, select the project ID but with that, we should have the code we need to delete a task from that list of tasks. Now we must pass handleDeleteTask as a value for onDeleteTask to SelectedProject. And that's something we're already doing. And in SelectedProject, I'm already forwarding onDeleteTask through the onDelete prop to tasks. Therefore, now the last step is in the tasks component to use this onDelete prop, which in the end points at this function on which we just worked and to use it to connect it to this clear button. Therefore here, we add onClick, but now we must not just pass on the lead as a value for on click like this. Because if we would be doing this, we would not pass this ID to this function, to this handleDeleteTask function, this ID here, which we need. Therefore, we need more control. And we should again, wrap this with a function, so that we have full control over how this is executed and we can now pass the task ID of the task we're currently rendering here to that delete function. And with that, we should hopefully be able to delete tasks. If I now click clear here, that's deleted, that's deleted, and and that's deleted. So clearing these tasks works. And with that, we added this last missing building block to this demo app here. Now, there's just one remaining problem. If I add a new project real quick, if I select that project and I open the dev tools and I start typing here, I'm getting a warning, a strange warning, which in the end stems from my new task component. And the problem here is that my EnteredTask state initially is undefined because I'm not passing an initial value to useState. But then thereafter, once I start typing into this input field, the value gets changed to a string and therefore this value prop here first receives undefined, which kind of tells React that this element is not controlled by React state. And then it suddenly changes to a string which tells React the opposite. The solution is simply to add an empty string as a default value for useState here. And with that, if I clear this, now you see the arrow goes away as I type here. Also, if I reload and I create a brand new project, if I save that and select it, now we got no error and I can add tasks and also delete such tasks. I can also add empty tasks here, but you could of course as an extra exercise, add extra validation here to make sure that this is not possible. For example, a quick and easy way of handling this would be to go to handle click where we, in the end make sure that a task gets added. And there, we could check if the enteredTask once we trim it to remove access, white space is an empty string, in which case, we could simply return here so that this code never gets executed. With that, it would not be possible to submit more empty tasks. That would be a quick and easy way, and you could of course now also enhance this app even more by showing that error modal or something like that. Another small bug I wanna fix in this project is that at the moment if I got multiple projects here, the selected project isn't actually highlighted here because I'm not passing the selected project ID to my project sidebar, but I'm using this prop inside of that component. But at the moment, it will always be undefined because up to this point, I did not pass a value for this prop to this component. So we must add this prop here of course, and then pass projects, state SelectedProjectid as a value for that prop. And with that, the currently highlighted project can be seen here, but for this module as an exercise, I'm quite happy with the current state of this project and we did now practice a lot of core concepts that you need to know and about which you learned in all those core sections that led up to this section here. As you can of course also see this app component became quite complex now though, and we're passing a lot of props through multiple layers of components. And these are therefore problems we're going to tackle in the next course section.