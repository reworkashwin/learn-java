Now to do that, let's start in the NewTask component where we have that input. And we now need to extract the value that was entered by the user into that input when that button is clicked. Now we could absolutely do that with a ref again, but now I also want to clear this input and set it back to an empty input after we click Add Task. And whilst this could be done with a ref, we would kind of be violating that idea of React being in charge of the DOM and of DOM updates. And therefore, also to practice it again, I'll instead use good old state here. So import useState from react. And then here I'll have my enteredTask and my setEnteredTask state-updating function. Then we can add a function here called handleChange and connect this to this input here by adding a onChange prop to it that receives handleChange as a value. As you learned in this course, we now get an event object here which will also contain the target of this event, which is this input, which then in turn will contain the value that's entered into it. So we can update this state by calling setEnteredTask and pass event.target.value to it to store the entered value as a new value for this state. And now we should complete the two way binding by feeding the entered task text back into this input field. And with that, we're registering all those keystrokes. Now we must make sure that when this button here is pressed we also forward this task information to a place where it can be stored. And that place indeed should again be the App component because there we're storing all our projects. This is probably also the place where we wanna store the tasks that are related to those projects. Now, this of course also implies that we'll need to pass these tasks and the function to update these tasks state in the App component through multiple layers of components. And we're already doing a lot of state management in this App component here. But in the next section we'll actually learn about an alternative way of managing this application-wide state that will be a bit more convenient and easier. But in order to understand that approach it's first important to understand this approach and understand that this approach we're using here can be cumbersome, as you already see and as you will see once we start managing our tasks here. Because I wanna do that here as a separate array, in this state we're managing the App component and we'll therefore need separate functions here for adding a task, for example, named handleAddTask, and for the deleting a task, because that should all be possible in this app. Now, we'll work on this state and these functions later. I just already wanted to mention that we will manage these tasks in this component here. But with that, let's go back to this NewTask component because there we get this button, this Add Task button, that will eventually be pressed. And therefore we should add a function, handleClick could be the name, that should be connected to this button through the onClick prop, of course, like this. And in handleClick I wanna do two things. I wanna forward the entered value to the App component in the end and I then wanna reset it back to an empty string so that this input field here is cleared after clicking Add Task. Therefore in there I'll start with that resetting by setting setEnteredTask to an empty string. But now we of course also must take care about getting that entered task to the App component. Now for that, in there I have these functions already, now I need to pass this handleAddTask function to my NewTask component. Now this NewTask component is in the Tasks component, so I will need to go through that component. And that Tasks component, of course, is inside of the selected project component, so I will need to go through that component as well. And this is something that's called prop drilling. And we'll make sure to get rid of that prop drilling in the next core section. For the moment, to understand the problem with it, we will simply do it and pass our props through all these layers of components. Therefore, to get started in the App component, let's pass handleAddTask as a value to the SelectedProject component by adding a new prop here, which could be named onAddTask and which now receives this handleAddTask function as a value. We can also already add onDeleteTask and set this equal to handleDeleteTask because this of course will also in the end be triggered from inside this component, or to be precise, not from this component, but from a component used inside of this component. And therefore we need to forward these props through this component here. So next, let's go to SelectedProject here and extract these newly added onAddTask and onDeleteTask props. But now, as mentioned, we don't need them here. Instead we just forward them to the Tasks component. So here I'll add a onAdd prop and set this equal to onAddTask. And I'm using different names here to make this a bit less confusing. And add a onDeleteTask prop, which receives the onDeleteTask task prop we just extracted. And with that, we can and should go on to the Tasks component and there use destructuring to get that onAdd and onDelete prop we just added. Now, onDelete will actually be needed in here later once we output the tasks. onAdd should be forwarded to NewTask. So here I'll add a onAdd prop to NewTask and forward the onAdd prop I'm getting here as a value to that. So therefore, now finally we are done drilling our props. And in NewTask we can and should extract onAdd with destructuring here. And now finally, here in handleClick I'll call onAdd and forward my enteredTask value, so the value entered by the user to onAdd, and therefore in the end to the onAdd prop I'm getting here in the Tasks component, which in the end is this onAddTask prop I'm getting here in the SelectedProject component, which then in the end finally is this handleAddTask function defined in the App component. And therefore here we'll now get that task text as an input, and we can now use that to update our state. And for updating that state, I'll use that logic which I also used for adding a new project, because it will be similar for adding a new task. And I'll copy that into handleAddTask, though of course now it's a taskID I wanna generate here. And this newTask object which I am creating here will simply have a text property which stores the text I'm getting here. So this text is this text which is stored in this text property. And I'll have an ID, which is this taskID, this random ID here which I'm generating. In addition, we also need to store the ID of the project to which this task belongs. And we can extract that from our previous state because it's of course the currently selected project because we can only add tasks if we selected a project. So we can get that from our previous state, and there it's this selectedProjectID. And now I wanna add this new task here to this tasks array, where I first of all want to spread all my existing tasks so that we don't lose any task here. And then we add the new task as a last element or as a first element maybe, to mix things up, simply however you prefer it. And I'm not doing anything with the projects here, I'm just copying them into this object. I'm just editing my task. And I don't wanna update the selectedProjectID here because I don't wanna deselect the project or anything like that just because I added a task. Instead, I simply wanna do that; I wanna add a new task, and that's all. Therefore, now with that, theoretically we should have done everything that needs to be done to allow us to add new tasks. Of course now we must make sure that tasks are being output here in the Tasks component though. And therefore here we need another prop. We need to get those tasks that belong to this project in which this component is rendered. Therefore, here we now should get a tasks array, and we can now use this array to conditionally output this paragraph or this list. So we can check if tasks.length is equal to zero, so if we have no tasks for this project yet, in which case I wanna output this paragraph. Or if tasks.length is greater than zero, then I want to output this unordered list. And in this unordered list, I now wanna output the actual tasks, again, by simply mapping them to list items. Just as we did it multiple times before, we get our task here and we we then output a list item which inside of it should display the task text. In addition, we also need to set a key here, which should be set to task.id. Now I will actually wrap this task.text here with a span because next to that text I then also want to output a button, a button that can be used to clear, so to delete that task. Now, for some styling, I'll add some classes to that unordered list and add a padding in all directions and a margin to the top, some rounded corners, and a background color to set this apart a little bit; just a slight background color, but still. And on the list item, I'll also add some classes to use flex box to position this delete button here in the same line as this text, but ad some spacing between the two items. And for that we can use justify-between to push them apart and add a margin to the top and bottom of every list item. Last but not least for this button, I'll simply give this a dark gray color; and upon hovering over it, I'll actually give it a red color because it will delete this task. We could have done the same for the button that deletes the project, here I'll simply do it for the button that deletes this task. And with that, we're outputting tasks, but we must make sure that this tasks prop here does actually receive values. And for that, back in the App component, we must pass the tasks which we're managing in our state here, again, through all components to the Tasks component. So here, I'll go to my SelectedProject and add a tasks prop here, because it's this project that contains the Tasks component, and set my projectsState.tasks value here as a value for this tasks prop. And then in SelectedProject, we can extract this newly added tasks prop here and pass that as a value for this tasks prop we added here to the Tasks component. And yes, I know that are a lot of tasks, but that's the last one here. And pass these tasks, which we're getting as a prop here on selected project, as a value to the tasks prop on that Tasks component. And it's now this tasks prop here which we are setting. With that, we should be able to save that, add a new project with any date, select that project, and add a task. And indeed it is showing up here. And as you see, we can add multiple tasks here. We got this button. And we therefore now just have to make sure that this Clear button also works.