So to make this delete button here work, we, of course, need to do basically the same, we did a couple of times before already. We need to add a function to the app component, a function which edits this state and removes a project. And we need to pass that function to that selected project component in this case here. So let's do that. For that, I'll add a new function here in the app component a handleDeleteProject function and in there, we wanna update the state. So I'll copy one of those state update function calls here and paste it into this handleDeleteProject function. And now here for one, the selectedProjectID should indeed be set back to undefined so that we no longer select that project. But in addition, the project's array must be updated and it should be updated in an immutable way so that we don't edit the original array in memory. Now an elegant way of doing that is to use the previous state and the projects that are stored there and calling the filter method, which just like map and find, is a method built into vanilla JavaScript. And filter works such that it takes a function as an input just like map and find and this function will be executed for every item in this array. Again, just as it's the case for find and map. But this function should then return true if an element should be kept or false if it should be dropped. And the filter method will then return a brand new array that only contains the elements that were not dropped. So, for which this function yielded true. Now this function, just like find and map will receive every item here as a input. And then here we should return true if we want to keep the item and false if we want to drop it. Therefore, here I want to drop the item and I wanna return false if the idea of the project I'm looking at is not equal to the ID that was previously selected because it's this selected item that we're deleting. So, we can use the previous state and then there selectedProjectID. If the IDs here do not match, I know that I'm not looking at the item that should be deleted and therefore I'll return true to keep that item. If these IDs do match, I'm looking at the item that should be deleted and therefore thanks to this comparison here, I'm returning false and the item will be dropped. That's how this handleDeleteProject function here will work and it doesn't even need the ID of the item that should be deleted as input because we can get that from our previous state. So, therefore, now we should pass a pointer to this function to the selected project component, maybe through a prop named "onDelete" like this so that we can call this function through that prop from inside that component. So, in SelectedProject, we again should extract this newly added onDelete prop and then connect it to this delete button. And since we don't need to pass any data to this function, we can just set it as a value on the onClick prop like this so that on this delete button, we're in the end calling the function received on that onDelete prop. And with that, if I save that, and I open my dev tools here to check if I'm getting any errors, if I click delete, indeed the project is gone and I'm back to this screen. And if I quickly add two new projects just to check if the right project is being deleted, like this and I now select the first one and click delete, you see that the other one is still there. So that all seems to work and therefore, now we can add projects, we can select projects, and we can also delete projects.