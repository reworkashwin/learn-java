Let's now work on the styling, and I'll start with the backdrop. For that, I'll go to this Modal component again. And there, we can add a special CSS class provided by Tailwind to this dialog, which starts with backdrop colon or to be precise, we can use this prefix to then add all kinds of classes we want to that automatically-rendered backdrop element that is displayed behind the dialog. And here, I'll set this to bg-stone-900 /90, which looks a bit weird, but which is Tailwind's class name for adding a background that has some transparency. Now, in addition, on the dialog itself, not the backdrop, I'll add some padding in all directions, give it rounded corners, and add a shadow behind it. With that, if we save that, this looks much better already. But of course, the content in there doesn't look great, and here, to make sure that it does look better, I'll go to the NoProjectSelected component, and simply copy the classes I added to the h2 tag here. And therefore, we could also put this into a separate reusable component, but here, to save some time, I'll just copy and paste these styles. So in the NewProject component, I'll add this class name to the h2 tag, and then grab this className here from the paragraph, and add that here to my paragraphs I have in that modal. Again, we could build reusable components to avoid copy and pasting this, but since I'm only using these classes here on a few elements and I wanna save some time, I will just copy and paste them here. And with that, this definitely looks a bit better, I'd say. Now, the button doesn't look great though, but here, I'll fix this by going to the Modal component, and there, on this form, I'll add a class name, which adds some margin to the top and then moves this button to the right by setting the text alignment to right with this text-right clause. And in addition, instead of using the standard button here, I'll use my own button, which I'll import from Button.jsx. Of course, also for the closing tag here so that this button looks nicer. Last but not least, I think the text should be a little bit darker, and therefore, I'll actually tweak those styles here, and choose darker text colors for the title, and also for the paragraphs. With that, that looks quite decent. So now, we got this modal, which will appear if we have at least one invalid input. So even if I enter values here, if I don't enter a date, I'll get this. But as soon as I also do pick a date, and I therefore have valid values in all inputs, I can save this and the project is added here. So therefore, now as a next step, let's make sure that this Cancel button also works. And for that, of course, we have to make sure that in the NewProject component, this Cancel button here, in the end changes the state in the App component, because there, we just need to switch back to this undefined state to show this NoProjectSelected component again. Therefore, here in the App component, we can add another new function, handleCancelAddProject could be the name. Since that's what we're doing, we're canceling the creation of a new project. And in there, I'll copy that code here from handleStartAddProject, but I'll set the selectedProjectID to undefined, and now, it's a pointer at this function here that should be passed to my NewProject component. So that here we have a onCancel prop baby to which we pass the handleCancelAddProject function we just worked on. Inside of NewProject, we now must accept this newly added onCancel prop. So here, we should add it, and then make sure that we connect it to this Cancel button. So here on this Cancel button, we can simply add onClick and pass onCancel as a value to onClick, so that if this button is clicked, onCancel is triggered, and onCancel, of course, simply is this Cancel, CancelAddProject function, which will reset our selectedProjectID to undefined. And therefore, with that, if we do click cancel, we're back on this starting screen so to say. So that all works. And the next step there for now is to finally make sure that we can select those projects, and view some project details here.