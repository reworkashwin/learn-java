At the moment, the context value here is pretty static. It's always this empty array. The goal now, of course, is to link it to our state. So to link it to this items array which I'm managing here as a state. And doing so, thankfully, is pretty straightforward. Now for convenience reasons, I already made sure in the starting project that our state here is an object of the same shape as that value I'm providing to my context here. But we could also change that state and make sure that we're just having an empty array here and then update the state updating functions accordingly. But here I'll keep that object shape with an items array. And now it's this items array in my state here which I, of course, kind of want to use as a value because this then is dynamic. So, what we can do, of course, is we can go down here and set our value here equal to our state, the shoppingCart state, because that state has that same structure as I just mentioned. And therefore, if you now save that and you reload and you start adding items to the cart here, you'll notice that they do show up in the cart now. So now with that, we actually already linked our context to the state because now the state simply is the value we're providing to the wrap components and their child components through that context feature. And that's how easy it is to link context to state, but we're not done here, because if I just set my entire state object as a value here, I can definitely read it, and therefore, we can use that context here in the Cart component for example, but editing the state does actually not work through context yet. Instead, I'm editing the state still by passing props to our components. And of course, in a perfect world, we might want to use context for everything, not just for reading values but also for updating those values. So that we also don't have to pass props if we just wanna update a value. And that's thankfully also pretty straightforward to do. For that, I'll go back to manually creating my value here and I'll actually create it in a separate step to make the code more readable. Here's my context value, it's an empty object for now. And in there I wanna have an items array which should be my shoppingCart.items array so that state items array. But that's now not the entire value I want to share. Instead, I also want to share a addItemToCart function. So a property called addItemToCart which value should be a function that does add a new item to the cart. And thankfully we have that function here. It's this handleAddItemToCart function I got here. So I can use this function here as a value for this addItemToCart property I added here. And with that, we're also exposing this function through context. And therefore, any component that can read this context, so any component that's in the end wrapped by this provider component here or that's in a child of a wrapped component, can in the end call this handle addItemToCart function through that addItemToCart property in my context value. So therefore, we can now provide this ctxValue constant. So this object here as a value of our context, and hence now, for example, in this Product component which wants to be able to add items to the cart, we can go there, and now there actually get rid of this onAddToCart prop because we no longer need that. And instead also get hold of our context here. So import this CartContext from going up one level, diving into the store, and then there the shopping-cart-context.jsx file and connect this component function to that context via reacts useContext hook, which also must be imported therefore, like this. And then in the Product component function, we can get hold of our cartContext by calling useContext and passing the cartContext value here, this imported value, as a value to useContext. And now here we can again use the destructuring and you now see that I'm not getting auto completion for this addItemToCart function. I just added to my context value because I just added it here, but not to my initial context value. Therefore, I'll grab this function name and I'll go to my Context object here and add an empty dummy function here. This will not actually get used, but it helps with auto completion. So that's just an empty arrow function here which in the just makes sure that I get better auto completion. So now here, if I try to destructure this in the Product component, I now do get this suggestion that I could use addItemToCart. And now it's this function that I want to call here and pass my ID to it. But now we were able to get rid of those props and if we save everything and we reload this app here, we get no errors and I can still add items to my cart and see them there. So this now all works but now it works with help of context. And we're now using the context not just to provide values that can be read but also values functions that can be called in order to then change the state the shoppingCart state here, which then is linked to our context because it's used as a value in that context. And that is how you can use context to share values and state updating functions with multiple Components in your app without using prop drilling. So let's now also use that to get rid of all the other cart related props. We were passing to header and so on.