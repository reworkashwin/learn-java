In this course section here we are going to explore more advanced State management features and approaches offered by React, because as you maybe already saw in the previous course section and the demo app we built there, and as you will definitely experience if you are building more complex React apps in general, managing State in such more complex applications can become tricky, and can lead to problems like prop drilling, a problem we'll explore in greater detail in this section here, so that we all fully understand what it is and why it's a problem. And then we're of course not just going to focus on this problem in this section here, but instead also on solutions. We'll take another look at component composition, and how that could help us solve this problem, at least to some extent. But even more important, we're also going to explore React's context feature, it's context API, and you will learn what exactly this feature is all about and why it's amazing if you need to manage State that's shared across multiple Components. Last, but not least, in this section we are also going to take a look at another React hook that allows us to use so-called Reducers to manage more complex State data with these. And you will learn what Reducers are and why you sometimes might want to use this hook instead of the use State hook we used all the time.