Throughout this course, we used a lot of React hooks, like the useState, or useEffect hook. In this course section, you'll now learn how to build your own custom hooks, and we'll start by repeating the rules of hooks, which are important especially also when building custom hooks. And we will, of course, also explore why you might want to build custom hooks, what the idea behind custom hooks could be before we then start building and using custom hooks. Now, for this section here, I got a new starting project for you and it's actually the finished project from the data fetching course section. Therefore, this starting project comes with a dummy backend and it comes with React code that actually sends HTTP requests to that backend to fetch data from there. And therefore, I do indeed recommend that you finish this data fetching section before diving deeper into this section here, or you should at least know how data fetching in React apps can work. Now, if you are following along locally, you will need to run npm install, and then as always, npm run dev. And you will also need to do the same in the backend folder of this project. Run npm install, and then not npm run dev, but actually node app.js to start that backend server. On Codesandbox, none of that is required since everything is set up for you there. Therefore, you also need to install NodeJS from nodejs.org if you wanna follow along locally because you need that to be installed in order to run this command here. But with that up and running, we're ready to dive into custom hooks.