So that all works, but how can we bring back that sorting logic now? Well, there definitely are different possible solutions you could come up with. One solution I can think of is to create a separate function here, "fetchSortedPlaces" could be the name, which should be an async function, where you start by calling an "await fetchAvailablePlaces." So where we call this function, which we're importing, which will yield those places or throw an error if it fails. That will give us our places, and now, you could move this code here into this function, comment it back in, get rid of the state updating function, and make sure that you return a promise here after you got the current position, and to do that, you can return a new promise here by creating a new promise object, a feature that's built into the browser, and this promise constructor function takes a function as an argument where you then get two parameters passed in by the browser, a resolve and a reject parameter, and in this function body, you could now execute this code where you fetch the user's location. By using this promise constructor, we are promisifying this code here, which means we're wrapping it in a promise, and we're turning it into a promise, which we need to do because I wanna return a promise here because useFetch expects the fetch function to yield a promise since we're awaiting the result here. So the fetch function must yield a promise and that's why I am creating a promise here. Now, we just make sure that this promise, which I am creating here resolves to a value, and lets use fetch, know that the wait time is over, that the value is there, by calling Resolve once the value is there, which is the case in here after we have our sorted places. Here, we need to call Resolve and pass our sorted places as a resolved value to Resolve, and this will now make sure that when we wait for fetchSortedPlaces, because we, for example, use this as a fetch function, it's this value that will in the end be returned by the overall promise returned by this function. This is not React-specific. Instead, this is a standard JavaScript feature and a standard JavaScript pattern and approach for converting a non-promise feature and API into a promise-based feature. Now here, I therefore actually don't care about the Reject feature, which I could use to throw an error inside of a promise, but here, for this case, we can ignore this, and it's now this fetchSortedPlaces function that should be used as a data-fetching function here, fetchSortedPlaces. This function still calls fetchAvailablePlaces, but it then also performs this distance-based sorting and therefore, our useFetch hook does not have to change. It still calls this fetching function, which does all that work, and it's still catches any errors that might occur during that work, and it still updates all these states, and that's therefore a straightforward solution for this problem here. Now, I can also get rid of setFetchedData here because I actually don't need it in this component, and we can get rid of the useState and useEffect imports because we don't need those either. Instead of, you now save that and you reload, you should see that we're still fetching those places and we're still sorting them by distance, and if I open the dev tools to check for errors, you can tell there are none. So this works, and of course, adding places still works, reloading works, removing places works, that all works, and it now all works powered by a custom hook.