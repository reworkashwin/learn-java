Well, to make this useFetch hook functional, we also need to manage some state in there, because every component that later uses this custom hook should of course also get that state that's managed by that hook. And it should not have to register that state on its own, because that would of course reduce the reusability of the custom hook. So this custom hook should not just send some requests, instead, it should also manage all the related states. And therefore here, to make this hook useful and reusable, we should also import useState here so that we do, for example, manage the isLoading or isFetching state as we also named it before like this. In addition, of course, we might face an error. So that's another piece of state we should manage here, error and setError. And eventually, since we're sending an HTTP request here, we of course will end up with some data. Though since the idea behind this hook is to use it in different places with different pieces of data, I don't wanna name my data state something like userPlaces, but instead I wanna give it a more generic name like data or fetchedData and setFetchedData. And then here I have my state which initially also is undefined. And now here where I store the fetchedData, I would do that with help of setFetchedData. But of course this code here is still not very generic. We are still explicitly fetching the userPlaces, and it would be great if we would be a bit more flexible here. Well, keep in mind that, of course, a custom hook is just a function, and like all functions, it can receive parameters. So here we could expect to get a fetch function as a parameter. And of course this parameter name is always up to you. And then here, we could simply execute this fetch function, and now this hook would be usable with all kinds of fetch functions. And here we then get back our data, which we then set as the fetched data. And now this is fairly generic except for this error message. So we could either accept another parameter here which allows us to set this error message or we choose a more generic error message here like failed to fetch data. Another thing that should be changed Is the name of this function here. Instead of naming it fetchPlaces, I'll just name it fetchData so that we have this function name here. Now here we can also see that we now got some yellow squiggly lines down there because I'm now using this fetch function in here, as I also learn if I hover over this. And I therefore have to add fetch function as a dependency, because that's some external data which is not defined inside of useEffect and which theoretically could change. And if it would change, this effect should be executed again so that we're fetching data correctly based on the latest available fetch function. Therefore, fetchFn should be added as a dependency here. It should not be executed, instead just a function like this as a value should be added as a dependency. And with that, we got a fairly reusable useFetch hook, but of course at this point, we're not doing anything with all those states. We're just setting them, but then they're basically getting lost inside of this hook here. So what could we do here to make those values available to the component that will later use this custom hook? Well, what can you do in a normal JavaScript function? You can return values so that in the place where you use this hook, you will get these values returned. And therefore here, at the end of this hook, outside of the effect function, we can return these values by grouping them into an array or into an object. That's of course up to you. And keep in mind that useState, for example, also the returns a value, it returns an array with two elements, the state snapshot and a state updating function. And we can do the same with our custom hook. Here, we can return an array or an object. And here I'll go for an object. We could also return just a number, but here, since I wanna return free pieces of data, we need to group them together and we can do that with arrays and objects. So here I'll go for an object and I'll simply add those different states as key value pairs so that we got isFetching, data, and error. And data is named fetchedData here. So I'm returning these three state values grouped together into an object in my custom hook. And now last but not least, to make this hook usable in Components, we must export this hook function. And now we can go to the app JSX file, for example, and get rid of the state we managed there. And I already deleted the useEffect hook there. And instead we import useFetch from hooks and then useFetch.js. And now we just have to pass the fetch function to useFetch, which in my case here is fetchUserPlaces. So a pointer at this function which contains the actual code for sending the HTTP request. And then, as a result of calling useFetch, we will get those state snapshots that we are returning there grouped into an object. So therefore here we can use object destructuring to get isFetching, error, and of course the fetchedData. And the great thing is that if you use a custom hook in a component, any state that is managed by that custom hook will also belong to the component in which you are using your custom hook. So therefore, if the state gets updated in the custom hook, for example, here when we setIsFetching to true, when that happens, the component where your custom hook gets used also gets executed again. So that still works as if you would be managing the state directly inside of the component. That still works in exactly the same way even when using a custom hook as an extra layer in between so to say. So the behavior of this component won't change, but now we can use the state returned by useFetch in this component. Now here, this of course also means that we face some problems here because we also previously worked with that fetched data to update it and to, for example, change it when a place gets selected. Now, we'll take care of that later. And for the moment, I'll just comment out these different functions here, and I'll also comment out those props where I pointed at those functions, also here. And I'll just replace userPlaces with fetchedPlaces now or fetchedData was the name. So I'll use that down here. Alternatively, if you wanna stick to user places, you can also assign an alias up here by adding a colon after the destructured property name and then assigning any alias of your choice, like userPlaces. And with that, you'll notice that if you save this, we get an error here. And we get an error because initially it looks like we're expecting an array in someplace as we're trying to access the length property, but instead we get undefined. And that's happening because previously, I initialized my userPlaces here with an empty array. Now in the more generic useFetch function, this instead is undefined initially, because of course we could be using this hook in all kind of situations where we don't necessarily always expect to get an array back from the server. Therefore, we could simply accept another parameter here, let's say an initial value, which we could then set as an initial value to make this a bit more configurable. And with that back and app JSX, we could now pass our wanted initial value for the userPlaces to useFetch as a second argument, and in this case here it would be an empty array. So that at the point of time where we don't have any data yet, we are falling back to this empty array. With that, if you save that and reload, you'll see that fetching those places works again, but now this works with help of our own custom hook. And that's the idea behind using such a custom hook. The logic for managing those different built-in hooks like useState and useEffect, and for orchestrating this HTTP request, all that logic lifts in a custom hook. And in our Component, we can simply use that custom hook and we get all that behavior, all that functionality in this component without adding all that code here.