Instructor: As of now, inside our gateway server, we are just checking if the client application is authenticated or not. That means we are only performing the authentication but not the authorization. What if you have a scenario where you want to only process the request if the client application has certain roles or authorities or privileges? So, in such scenarios, instead of these authenticated, you need to invoke a method which is hasRole. To this hasRole method, you can pass what is a role that you are expecting which your client should have configured inside the auth server? So, here, for all accounts-related APAs, I'm going to expect my client should have a role with the name, ACCOUNTS. Very similarly, I can configure role-based access for cards as well and loans as well. But instead of ACCOUNTS, here, I'm going to keep the role name as CARDS, and post that. And the LOANS, I'm going to mention LOANS role name. So, with this now, I have enforced the authorization inside my gateway server. Now as a next step, we need to configure these roles to our client applications. So, where do we need to configure those? Inside the auth server. So, let me go to the auth server. Inside the auth server, let's go to the Clients and another clients. Here, you'll be able to see the client which you have created. So, please click on that. Now, here, there are two tabs like Roles and Service account roles. Since this client is an client, which is following the client credentials grant type flow, that means this is also another application. This is not an end user. So, in such scenarios, we should not configure under roles. We should configure under service accounts roles. Here, you can see there is a button, Assign roles, but I don't have any role that I want to assign. So, let me close this. And I can click on these Realm roles. So, let me click on that. And post that, I'm going to click on this Create role. And here, first, I'll create a role with the name ACCOUNTS. And description, I'll just keep it as Accounts Role. And post that, I'm going to click on this Save button. Intentionally, I'm not creating loans and cards. After doing some negative testing, we are going to create those roles as well. Now, I'll go to clients and I'll open eazybank-callcenter-cc and go to the service account roles and click on this button, Assign Role. This time, you should be able to see the role with the name, ACCOUNTS. So, let me select the same and click on this Assign. Now, if I go to the request where we are trying to get an access token, this time, I'll try to get a fresh access token. So, let me click on the Send button. So, I got a new access token. Let me take this. Once I copied this access token, I will go to the jwt.io website. And here, I'm going to paste the same. So, let's try to see where the roles information is coming inside the access token. Under the payload, if you can scroll down, there is a data with the key, realm_access. And at this realm_access key, we have another object. And inside this object, we have one more key with the name, roles. And at this roles, you can see we have list of roles assigned to this client. And here, we have our own custom role that we have created with the name, ACCOUNTS. All the remaining roles are default roles that are available inside the Keycloak, but we are more interested about these ACCOUNTS role. So, now, it is confirmed that the role information is coming inside the access token. But I should write a logic inside my resource server or inside my gateway server to extract this role information and convey the same to the Spring Security framework to use and validate my authorization rules that I have configured. For the same, inside the same package of config, I'm going to create a new class. And the class name that I want to give here is KeycloakRoleConverter, because this class is going to help me to extract the roles that I'm getting from the Keycloak into the format that Spring Security framework can understand. So, let me create this class. Now, in this class, I need to make sure that it is implementing a interface with the name converter. Let me try to import these Jwt. So, please make sure you are exporting these Jwt present inside the Spring Security or tool library. Once we added these implementation details, it will expect you to override and abstract methods. So, let me click on these implement methods. And override this method which is convert. So, I'm trying to click on this okay button. Now, you can see I got a new override method where I need to write the logic to read the role information from the JWT access token that I'm going to receive, and convert the same into the collection of GrantedAuthority. So, GrantedAuthority is an interface available inside the Spring Security framework. Whenever we want to convey the roles information or privileges information, we need to make sure we are sending those details with this interface format only. So, inside this method, I'm going to paste few lines of code. Post that, we can try to discuss. I'm trying to save some time here. You can also try to refer this code inside the GitHub repo. If you see here, first, I'm trying to invoke the method, get the claims with the help of this Jwt class. The purpose of this getClaims method is it will give you access to the payload data available inside your access token. Post that, I'm going to invoke the get method, because this getClaims is of type map. And inside this getClaims map, I'm going to invoke the get method with the key as realm access. If you see here inside our access token, we have a key with the name, realm_access. So, whenever I try to access that, I will get the value assigned to this realm_access. But this value also is of type map, because this is a key and these are the values. That's why I'm trying to assign that to the map of type string and object. For some reason, if realmAccess is empty, that means I'm going to return an MTRL list. Otherwise, from this realmAccess, I am again, going to invoke the get method with the key as roles. Inside these realmAccess, like I said, we have a map and there is a key with the name, roles. That's why I'm trying to pass this key name which will give me all these values available here. So, once I have all these roles available, I'm trying to use this stream APA. And using the stream APA, I'm trying to use the map method. So, what is the map method is going to do? Map method is going to iterate all the values available inside this stream. We are going to get four different values but we are more interested about these accounts. The stream is going to iterate all these four values. And inside this map method, we are going to take each of the role name and we are prefixing the value ROLE_ for each of the role name available inside the roles map. Why I am trying to assign this prefix ROLE_? Because whenever we use this hasRole method inside the Spring Security framework, internally, it is going to use a prefix which is ROLE_. You can see inside the documentation also, if we configure the role as USER, internally, the Spring Security will convert that to ROLE_USER. Since we have this prefix, we need to make sure inside the converter, we are prefixing the same. But inside the security config, we should not add that, because behind the scenes inside the framework, the Spring Security is going to add the same. Now, after adding this prefix name, my role name object is going to still be a string, but my Spring Security cannot understand the roles if I pass as a string. So, I need to pass it as an object of SimpleGrantedAuthority. So, this SimpleGrantedAuthority is an implementation of this interface which is GrantedAuthority. Inside this SimpleGrantedAuthority, there is a constructor which accepts the role name in the format of String, and post that, it is going to return the object of SimpleGrantedAuthority by using these role information. So, the same constructor I'm trying to invoke with the help of this method reference. So, with the new operator, I'm trying to create the default constructor available inside this object. So, first, with this map logic, we are going to append a prefix value for each role and post that, again, I'm going to iterate each role and convert that into a SimpleGrantedAuthority. And at last, the entire stream, I'm trying to convert that into a list which I'm going to return from this method. Now, we have successfully created a role converter, but how to communicate about this role converter to the configurations that we have done inside the Security Config.java class? But the same inside this class, I'm going to create a new small method. Here, let me paste a new method. Like you can see here, the method name is grantedAuthoritiesExtractor. We can make this method as private as well. So, this private method return type is going to be this value, which is Converter<JWT, Mono<AbstractAuthenticationToken. Inside this method, you can see first, I'm trying to create an object of JwtAuthenticationConverter. Using this JwtAuthenticationConverter object, I'm trying to invoke a method which is, setJwtGrantedAuthoritiesConverter, which means I need to tell where I have returned my logic to convert the roles present inside the JWT token into a granted authorities format. Since we have written all that logic inside the KeycloakRoleConverter, we need to pass the object of the same with the help of this new KeycloakRoleConverter. At last, I need to pass this jwtAuthenticationConverter object to the constructor of ReactiveJwtAuthenticationConverterAdapter. So, from this return statement, it is going to return an object of this data type. So, once we have created this method, we need to mention this method inside the configurations. As of now, you can see I'm trying to use that default configurations per my JWT method. That means I'm telling to the Spring Security framework, please go with your default assumptions. I don't have any role-specific information. But now since we have role-specific information also to be processed by the Spring Security framework, we need to make sure we are removing these Customizer.withDefaults. And post that, we need to write a Lambda expression by creating some Lambda variable with the name, jwtSpec. And I'm going to use the same Lambda variable name. Using this Lambda variable, I'm going to invoke the method, which is JwtAuthenticationConverter. To this JwtAuthenticationConverter, I need to pass the method, which is grantedAuthoritiesExtractor. So, this way, I have established a link about my key role converter inside my Spring Security configurations. So, with this, we should be good. Let me save these changes and do your build. In the next lecture, we can try to test these changes. Thank you and I'll catch you in the next lecture. Bye.