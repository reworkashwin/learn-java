Now, inside this lecture, let's try to understand how to implement rate limiter pattern with the help of Spring Cloud Gateway. I came to the official documentation of Gateway, and like you can see, I open the section related to RequestRateLimiterGatewayFilterFactory. So here we have all the details, like what is request rate limiter and how it is going to work. You can see using this rate limiter implementation, we can determine if the current request is allowed to proceed or not. If it is not a status of Http 429, which is too many requests will be returned by default. So whenever we are trying to implement these rate limiter pattern, we need a key resolver. So what is the purpose of KeyResolver using this parameter only we need to tell to the gateway server or to the rate limiter pattern. What is the criteria that we want to enforce this rate limit? Do we want to enforce this rate limiter based upon an user or based upon the session or based upon the IP address or based upon the server? So based upon your requirements, you need to provide the details with the help of KeyResolver interface. There is also a default implementation of KeyResolver, which is PrincipalNameKeyResolver. If you are using spring security to secure your microservices, then with the help of these PrincipalNameKeyResolver, it is going to fetch the current logged in user name and accordingly it is going to enforce RateLimiter and by default you can see if the KeyResolver does not find a key, the requests are going to be denied if needed. We can also adjust this behavior by setting these properties. So this is all the concept or the specification, how things are going to work. To make our life easy inside the Gateway server, we have an implementation with the help of Redis server. So what is Redis server? It is a cache based storage system. Using this Redis, we can implement these RateLimiter. So this Redis implementation is based upon the work done at the stripe. So there is a block from the stripe team which if you can open, you will be landed on to this page. So here there is a good information about what are the use cases that Stripe team considered to implement the RateLimiter pattern. So you can see they have mentioned some scenarios like you can see the very first scenario is one of your users is responsible for a spike in traffic and you need to stay up for everyone else. That means a single user is trying to send a lot of traffic and with that, all other users are getting impacted. And similarly, one of your user has a misbehaving script which is accidentally sending you a lot of requests or even worse, one of your users is intentionally trying to overwhelm your servers. So this is also one scenario where we can avoid with the help of RateLimiter pattern. And very similarly, a user is sending you a lot of low priority request and you want to make sure that it does not affect your high priority traffic. There is also an example where an user is sending high volume of requests for some analysis of the data and this is affecting some critical transactions. And other scenario is something in your system has gone wrong internally and as a result, you can't serve all of your regular traffic and need to drop low priority request. So these are all the scenarios where rate limiting is a good option. If you are interested, please read this blog. This is going to be super interesting. Now, if I go to the Spring Cloud Gateway here they're saying the RateLimiter pattern based upon the Redis is inspired, based upon the work done at the stripe. And in order to implement this, we need to add this dependency inside the pom.xml. And this is going to use the algorithm which going to work based upon the token bucket. To this token bucket algorithm. We need to pass three different type of properties. ReplenishRate, burst capacity and requested tokens. So what is replenishRate? This property defines how many requests per second to allow. In other words, this is the rate at which the token bucket is filled for this replenishRate. If you define 100, that means for every one second behind the scenes, 100 tokens will be added to your bucket. So if you wait for two seconds and if you are not consuming anything, then your bucket will have 200 requests and every bucket is assigned to a user or any other criteria based upon how you have defined KeyResolver. Now coming to the burst capacity with the help of replenishRate, we will be keep adding the number of requests per second. For example, if my replenishRate is 100 per second after 10s, if my end user is not using anything within this 10s, the bucket is going to have 1000 requests and if you keep filling it. At some point of time, he will have millions of requests inside his bucket. So to avoid this kind of overfilling the bucket with a large number of values, we are going to set the burst capacity. Using this property we are going to set what is the number of tokens that bucket can hold. So if I set 200 as the burst capacity after two seconds, my replenishRate is going to fill 200 tokens. And from the third second, if my end user is not using any of the tokens present inside his bucket, then since the bucket is already full, the replenishRate cannot add more number of tokens. And the third property that we have here is requested tokens. So using this property we are going to define how many tokens a request cost. Usually by default each request will cost a single token, but if needed, you can change this to 10 or 100 accordingly, the number of tokens will be consumed in the bucket for each request. Here we also have some examples like a steady rate is accomplished by setting the same value in replenishRate and burst capacity. So if you mention replenishRate and burst capacity as 100, that means every second inside my user bucket we are going to add 100 tokens. And if he's not using them, they will get wasted and the tokens inside the bucket will be deleted and replaced with the new tokens every one second. But in case if you want to allow temporary burst, maybe if you want to give some flexibility to your end user since you have not used the 100 tokens in the previous second and in the current second, you can use all of them in such scenarios you can mention burst capacity higher than replenishRate. For example, if you keep burst capacity as 200 and a replenishRate as 100, then that means in the first second, even though the end user is not able to use his tokens in the second second, he should be able to use all his 200 tokens at a time. So that's how we need to handle the temporary burst. There is also an example. Suppose if you have a requirement that you want to allow only one request per minute. So for these kind of scenarios, you can see they have given an example where replenishRate you have to mention as one, which means for every one second you are going to add one token into the bucket so that by the end of 60s or one minute, the replenishRate is going to add total 60 tokens into the bucket and the burst capacity we need to set as 60. That means the maximum number of tokens that we can add to the bucket is going to be 60 at any point of time. The bucket will have only maximum 60 tokens. And after the burst capacity we need to mention requested token is equal to 60. That means whenever he make a single request, 60 tokens will be consumed. With this we are making him to be able to send only one request per minute. So accordingly, based upon your requirements, you can play with these properties. Now if you can scroll down. So this is how you can activate the rate limiter and configure these three properties like replenishRate burst capacity and request tokens. So here they have given the example with the help of Yaml configurations. But in the next lecture I'm going to show you how to achieve this with the help of Java based configurations. And if you can scroll down, they also gave an example on how to create a bean. So you can see from the bean they are trying to return the user details. So inside the request they are some query params inside the query params there is a query param with the name user, so they are trying to get the value present inside the query param with the name user. So this is how they are trying to provide some KeyResolver, but it is up to us based upon our business requirements. We can write any kind of logic inside this KeyResolver which is going to act as a basic criteria to enforce all these properties. If you can scroll up here we have an interesting information, like if you mention the burst capacity value to zero, that means it is going to block all the requests. So please make sure you are not mentioning the value as zero. Otherwise your end user will not be able to send any request towards your microservices. I hope you are clear about this quick introduction. In the next lecture, let's try to implement this inside our GatewayServer. Thank you and I'll catch you in the next lecture bye.