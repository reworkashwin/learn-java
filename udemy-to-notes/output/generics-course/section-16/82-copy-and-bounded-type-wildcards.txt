In this lecture, I just want to tell a few words about generics that we have already talked about in one of the previous chapters. Because you can learn a lot about generic implementation when taking a look at the methods and at the concrete implementations in the collections utility class. So we have been talking about bounded type wildcards. So as you can see we have the question mark. And sometimes we have extends and sometimes we have super. So I want to show you that here for example we have super. Whenever we have to write to a given collection. Then as you can see we have question mark super T. This is how we can insert a given value into that generic data structure. And whenever we have the extend, this is when we can read from that data structure. So if we take a look at the copy method then as you can see we have the destination with super. So essentially this is how we can write to that given list. And we have the extend keyword when dealing with the source. So essentially we read values from the source and we write values to the destination. So this is exactly what we have been talking about in the previous chapters. And this is why bounded type wildcards are extremely powerful.