Hi. In this video, we are going to take a look at how to get private fields and private methods. I'm going to change back the modifier to be private instead of public. And if we iterate through in this case, I'm going to get the fields as usual. Fields, for example, is equal to the person class dot get fields. As we have discussed in one of the previous videos, it's not going to detect the private fields. So that's why we have to use instead of get fields, we have to get the declared fields declared and notation declared fields and okay, we have to import that fields Sorry for that. It's going to be a one dimensional array of fields and we just have to iterate through this field. For example, F in the one dimensional array of fields. And if we want to manipulate that given field, we have to set accessible to be equal to true. And after that we are able to get the parameters from that given field. For example, get the name. So if I save it and run it, as you can see, the name and the age is going to be detected, even though the modifier is set to be private. And basically the same is true for I'm going to rename it to be a private field, the string that's going to return the name, and we just have to iterate through and get the one dimensional array of methods. So we have to get the declared methods instead of the declared fields and we just have to iterate through. For example, I'm going to rename it to be methods. We have to set accessible true and we have to print out the names. Okay, we have the get name, set name. But as you can see, we have a private method, the return name, and it is detected with the help of reflection. So this is how we are able to detect private field variables and private methods. We just have to call the declared methods and get declared fields built in methods and we are able to set them accessible through and we are able to get the parameters we are looking for. Thanks for watching.