Hi. In this video we are going to talk about how to get the superclass and the interfaces that the given class implements and extends. So I'm going to create a class, for example, class, employee, whatever. Okay. And we say that this class person extends the employee class. So basically this employee is superclass of the person. So we are able to get it with the help of I'm going to print out that person class dot, get superclass and we have to call the getName method. So if we save it and run it, as you can see, the employee and of course it's concatenated with the package name in which it is located is going to be the superclass. Okay, let's consider interfaces. For example, this person implements the comparable interface as well as, for example, the serializable interface. Of course, we have to add the unimplemented methods. I'm not going to do anything. We are curious about the concrete implemented interfaces, so we have to declare something like this. Interfaces is equal to the person class dot get interfaces and we just have to iterate through these classes as far as the interfaces are concerned. And I would like to print out the names of these interfaces. So if I save it and run it, as you can see, it has the comparable interface, it has the serializable interface, and because every single class is the subclass of Object, that's why it's going to be printed out as an interface as well. So this is how we are able to get the superclass as we have seen for employee and this is how we are able to get the interfaces that the given class implements so the comparable and the serializable. Thanks for watching.