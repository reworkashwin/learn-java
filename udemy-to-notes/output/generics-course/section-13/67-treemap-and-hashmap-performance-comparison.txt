In the previous lectures we have been talking about how to implement Treemap and under the hood it uses a red black tree. So a balanced binary tree where the operations have order log and logarithmic running time complexity. And we have been talking about the standard HashMap with a one dimensional array. Under the hood. And it relies extremely heavily on a hash function. And what's crucial that we can achieve, although one constant running time complexity if we assume that there are no collisions. So the hash function is nearly perfect. So in this lecture we are going to compare the performance of Treemap and hash map. So first we use a Treemap, which means that we insert 500,000 items and then we get all the 500,000 items from the Treemap data structure. And of course we are going to measure the time needed for these operations. And then we do the same for a hash map. So we have 500,000 items that's going to be inserted and going to be removed from a standard hash map. And we print out time taken with hash map and we will print out the time in milliseconds. So if we save and run it, then this is exactly what we expect. Because although one running time complexity is way better than logarithmic running time complexity on average, hash map outperforms treemap, which means that it takes only 32 milliseconds to execute these operations, while on the other hand, it takes approximately 130 milliseconds to execute the same operations with Treemap, of course there may be collisions. So in worst case scenario, this order one running time complexity may be reduced to even linear running time complexity. So sometimes treemap is better, but on average hash map is working extremely fine. Okay, so we can come to the conclusion that Treemap has a favorable order. Logan Guaranteed running time complexity, but hash maps are faster on average. Thanks for watching.