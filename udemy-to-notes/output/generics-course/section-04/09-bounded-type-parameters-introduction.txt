In the previous lectures we have been talking about generic types, and in this lecture we are going to talk about bounded generic types. So what is the motivation behind bounded generic types? The motivation is that sometimes we want to restrict the types that can be used as type arguments. So for example a method that operates on numbers such as integers, floating point numbers or doubles might only want to accept instances of the number class or its subclasses. For example, let's assume that we would like to execute some mathematical operation, and of course we can do mathematical operations on integers, floats, and doubles. We are not able to execute mathematical operations on strings or characters. So this is why sometimes it is crucial to restrict the generic types. And this is exactly why there are bounded type parameters and bounded types allow you to invoke methods defined in the bounds. So we are going to take a look at a concrete example why it is absolutely crucial okay. So we can use bounded type generic types with the help of the extends keyword. And it is quite misleading because no matter that we are dealing with a class or an interface, we have to use the extends keyword. So despite the fact that usually we implement interfaces comparable is an interface. But anyways, as far as bounded types are concerned we have to use the extends keyword. So in this case we make sure that we are going to use a generic type. But there's going to be some constraint. In this case the constraint is that this generic type is going to implement the comparable interface, which means that we are able to compare those given generic types. So now we can invoke the method defined in the comparable interface when dealing with the generic variable. And what's extremely crucial that we can define a multiple bounds. So for example we can make sure that the generic type is a number for example. And it is a comparable and some other class. So we can define multiple bounds as well.