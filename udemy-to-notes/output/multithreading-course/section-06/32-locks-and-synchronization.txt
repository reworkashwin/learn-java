In this lecture, I would like to summarize the crucial differences between re-entrant clocks and synchronized approach. So first of all, we can achieve everything with the help of re-entrant clocks that we have seen with the synchronized approach. But there are several additional features such as for example, we can use Trylock method that lets you attempt to get the lock without blocking. Of course, it is not possible with synchronized. Then we can define a timeout that allows waiting for a certain time. Of course it is not possible with synchronized approach. Then we can use this lock interrupt method that lets a thread be interrupted while waiting for the lock itself. On the other hand, a thread is blocked until lock is acquired and cannot be interrupted with the synchronized approach. Then what about the fairness? There's no fairness guarantee when dealing with the synchronized approach. On the other hand, we can weekend. Create a fair look. When we instantiate a new re-entrant look, we have to define true as the Boolean variable to grant access in request order. Then we can use multiple condition objects with re-entrant locks. On the other hand, there is only one wait set per object using the wait and notify methods with the synchronized approach. And finally, you can manually lock and unlock when dealing with reentrant locks. On the other hand, the lock is automatically managed by the Java Virtual machine when dealing with the synchronized keyword. So these are the crucial differences. And we can come to the conclusion that we should use re-entrant locks because we can achieve everything that is possible with the synchronized keyword. But there are several additional methods. And we will come to the conclusion that it is easier to avoid deadlocks and live locks because of the methods that are available in the re-entrant lock class.