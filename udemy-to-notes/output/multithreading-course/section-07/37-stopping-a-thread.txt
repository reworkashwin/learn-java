In the previous lecture, we have been talking about the volatile keyword and accidentally we have been talking about how to terminate a given thread. If we take a look at the documentation so oracle.com about the thread stop method, it is deprecated because it is inherently unsafe. So if we want to stop a given thread from running, then we have to set up a boolean variable, a flag like this. We have to make sure that it is going to be volatile, so it is not going to be cached with the central processing unit. And within the run method, we keep checking whether the thread is terminated or not. Whenever someone updates the terminated variable to be false, then the file is not going to be executed. And this is how we can stop a given thread from running. So this is how we can stop a given thread. This is how we can make sure that it is not going to execute the run method anymore. So we just have to make sure that this terminated variable, this boolean flag, is going to be volatile. It is absolutely crucial. And this is how we can stop a thread from running. Thanks for watching.